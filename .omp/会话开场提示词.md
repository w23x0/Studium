# 会话开场提示词（2026-09-23 更新）

> 用途：下次开新会话时，把下面「提示词本体」整段发给 agent。
> 更新方式：每次收尾时重写这段；只写状态与入口，不写结论。

## 提示词本体

```text
先读 `任务线路.md`（现在在哪一步、队列、待裁决项），之后按你自己的判断往下做，不用等我给任务。

读文档时按这两条边界走：
- 产品结论只认 `docs/模块需求设计/模块审查单/`（M01–M15）与 `docs/模块需求设计/00-第三次重构-产品设计总览.md`。M11 / M13 是暂缓草稿、不作结论，M12 已取消。`docs/Harness设计/` 是组合层，较早写成，和审查单冲突时以审查单为准并直接修正。
- 以下不算依据：`docs/_archive/`、`docs/图工程调研/` 里已删实验的结论。`docs/学习理论/` 是参考材料，取舍表里判“采纳”的可作对应模块知识层的来源，但不是产品结论。

技术方向（2026-09-23 已改定）：按经典智能辅导系统（ITS）结构——确定性循环程序 + 在固定判断点隔离调用大模型；不用 DSH、不做多身份 agent；第一版 CLI。见 `docs/Harness设计/00-技术方向确认.md` 与 `_research/前沿架构对照-发现.md`。

当前任务：学习理论内容取舍已完成（`docs/学习理论/` 各份开头的取舍表）。下一步是单闭环最小原型（CLI）：先和产品负责人定范围——真实概念场景、判断点、对照组（“同信息单模型”），再动手。

交流方式见 `.omp/AGENTS.md`（一次只讲一条、不做大总结、先人话后术语、不确定就说不确定、讨论期不落盘、默认短）。
```
