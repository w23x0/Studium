# 会话开场提示词（2026-09-22 收尾时写）

> 用途：下次开新会话时，把下面「提示词本体」整段发给 agent。
> 更新方式：每次收尾时重写这段；只写状态与入口，不写结论。

## 提示词本体

```text
先读这两个文件，之后按你自己的判断往下做，不用等我给任务：
1. `任务线路.md` —— 现在在哪一步、队列、待裁决项
2. `docs/学习理论/README.md` —— 材料分类口径（待定）与转入进度

读文档时按这两条边界走：
- 产品结论只认 `docs/模块需求设计/模块审查单/`（M01–M15）与 `docs/模块需求设计/00-第三次重构-产品设计总览.md`。其中 **M11 / M13 是暂缓草稿、不作结论，M12 已取消**。组合层设计看 `docs/Harness设计/`（不新增产品结论）；`docs/实现基线/` 是 2026-08-31 的快照，部分过期。
- 以下不算依据：`docs/_archive/`、`docs/图工程调研/` 里已删实验的结论与 schema 提案、`docs/学习理论/`（从 Deep 搬来的理论，只是参考材料）。

旧材料在前代项目的外部档案 `/home/w23x/Deep`（已冻结）：那里有 `docs/学习理论/`、`docs/实际使用/`、`docs/项目归档/` 等。要查旧理论原文就去那里读；它不是本仓库的设计依据，进去前先看它的 `CLAUDE.md`。

两处已经作废，不要再引入：
① 「判据靠人工校准 / 人工审核」这类口径——2026-09-22 已从产品总览横切原则 4 删除，改为「判据由系统在隔离上下文里按证据判，缺料就输出证据不足」。
② M08 与 M13 还没按当前架构重新设计——不要拿它们的旧描述当输入。

当前状态（2026-09-22 收尾）：阶段 6「执行图 + 上下文编排」的首会场景仍在讨论中，**没有落盘**；首要任务是「学习理论逐份搬完」（全量 39 份，已转 2 份），下一步是先定分类口径里的模块命名那一套，再逐份搬。交流方式见 `.omp/AGENTS.md`（一次只讲一条、不做大总结、先人话后术语、不确定就说不确定、讨论期不落盘、默认短）。
```
