# 会话开场提示词（2026-09-23 更新）

> 用途：下次开新会话时，把下面「提示词本体」整段发给 agent。
> 更新方式：每次收尾时重写这段；只写状态与入口，不写结论。

## 提示词本体

```text
先读 `任务线路.md`（现在在哪一步、队列、待裁决项），再读 `proto/README.md` 与 `docs/Harness设计/_research/原型实验-1-发现.md`。之后按你自己的判断往下做，产品负责人已授权你自主判断，拿不准的产品决策再问。

边界：
- 产品结论只认 `docs/模块需求设计/模块审查单/`（M01–M15）与 `docs/模块需求设计/00-第三次重构-产品设计总览.md`（含「设计取向」：做模型进步替代不了的五项；模块 = 职责与数据归属，不等于独立调用）。M11 / M13 暂缓草稿，M12 已取消。
- `docs/Harness设计/` 是组合层，与审查单冲突时以审查单为准并直接修正。`docs/学习理论/` 是参考材料（取舍表判“采纳”的可作知识层来源）。`docs/_archive/` 不作依据。

当前状态（2026-09-23 收尾）：
- 技术方向：ITS 结构；确定性循环程序 + 隔离调用。教学主链 M02/M03/M04/M10 在**一次教学调用**内完成（诊断记录 + 回复）；独立调用只给闭环守卫、M15、M05。
- 原型 `proto/`：判断点 = `claude -p` 隔离调用（无需密钥）；场景 `scenes/kernel.md`、`scenes/newton2.md`；真人试用记录在 `proto/runs/me-1`（不进 git）。
- 真人试用发现：对已会内容，系统按验收主张反复索取口头证据，像考试、深不下去。已改文档：改线依据双向（学习者已超出范围 → 跳过 / 加深）；验收主张分“教学主张 / 确认主张”；有限视角随实际表现更新（M04 / M05 / M02 审查单）。

下一步：在原型里接入 M05 独立调用——教学调用在诊断记录中写出改线依据（两个方向）时，程序调用 M05 改写当前闭环的验收范围（转确认 / 加深 / 补前置 / 维持并给理由），写回场景并由 M01 记为路径事实，再继续教学调用；场景文件的主张标注“教学 / 确认”类型。做完请产品负责人用 `scenes/newton2.md` 再真人试一次（他会按“已经会”的方式快速作答，看能否往深处走）。

交流方式见 `.omp/AGENTS.md`（一次只讲一条、不做大总结、先人话后术语、不确定就说不确定、默认短）。改文档攒批提交，不自动 push。
```
