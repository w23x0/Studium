# M15 状态模块 · 进一步设计（讨论稿）

> 这份是聊天里那轮的完整内容，整理成文档给你慢慢看。
> 结论待你拍板；拍完我再写进仓库的 `docs/Harness设计/`。
> 日期：2026-09-16

---

## 一、先回顾：M15 产品设计已基本完成

M15 = **学习者状态与条件**。它保存：学习过程中会变化、带**时间范围、来源、不确定性**的非知识类状态与现实条件，以及这些条件对**承载能力**的影响。

已被产品文档确认的关键点：

| 项 | 内容 |
|---|---|
| 输入通道 | 两条互不相通：**对话通道**（主，与 M04 共享同一份 M01 过程记录）+ **现实约束通道**（预留） |
| 频率 | 低于 M04（暂定事件触发 + 会话收尾） |
| 输出分型 | **承载读数**（直连 M05/M10）与**解释候选**（经 M04 裁决） |
| 信息流 | **严格单向 M15 → M04**；M04 只能回发"重新确认请求" |
| 子模块 | **状态阶段判断**（跨会话，只出类型与时间跨度，不出特质、不给动作、不与 M04 侧合成"压力量"） |
| 明确不做 | 状态撤回权、否认后失效、敏感状态屏蔽、人格/心理疾病判断 |

**产品设计基本写完了。真正缺的是"组合层机制"。** 下面 5 个缺口就是这个。

---

## 二、缺口 1：频率与触发（D-001，最大的坑）

产品说"M15 频率低于 M04（暂定事件触发 + 会话收尾）"，但**具体触发事件没定**。这里有个真矛盾：

> **M15 是低频的，但它的承载读数要喂给每轮跑的 M10。**

低频 → 读数会**过期**，而 M10 每轮都要用。

### 我的方案

**触发条件**：

| 触发 | 说明 |
|---|---|
| 事件触发 | M02/M03 报到"状态相关可观察事实"（反复放弃、要提示、长停顿） |
| 显式自述 | 学习者主动说（**走快路径**，见缺口 4） |
| 会话收尾 | 闭环结束时汇总 |
| M04 重新确认请求 | 唯一的反向边 |

**时效规则**：每条读数带**时效窗口**；窗口内 M05/M10 可用，**过期视为"未知"**（不是沿用旧值）。

### 我的判断

这是 D-001 里最该先定的一条。**"过期即未知"** 很重要——它避免 M10 拿三天前的"疲劳"给今天减载。

---

## 三、缺口 2：承载读数 vs 解释候选，按什么分

产品说输出分两型，但**按什么分**没写。

### 我的方案：按推断深度分

| | 承载读数 | 解释候选 |
|---|---|---|
| 是什么 | 近观察的事实（当前负荷、可挂任务数、可承载时长、环境限制） | 对**本次表现下降**的**假设** |
| 谁消费 | M05 / M10 **直连** | **M04 裁决**后才可用 |
| 例子 | "本轮已连续 40 分钟无中断" | "这次答错可能是疲劳，而非不会" |
| 依据 | 可观察、低推断 | 有竞争解释、需排除 |

### 我的判断

分界线是"**是否与知识诊断竞争**"。不竞争的（负荷、时间）→ 读数直连；与"他不会"竞争的 → 候选交 M04。这条线清楚，**能机械判断**。

---

## 四、缺口 3：与 M04"同源不同契约"具体差在哪

产品说两者读同一份 M01 过程记录、但**读的字段不同**。落具体：

| | M04 读 | M15 读 |
|---|---|---|
| 内容 | 学习者的作答、步骤、理由 | 时间间隔、中断点、求助频率、放弃点 |
| 粒度 | 每轮 | 跨轮聚合 |
| 目标 | 判"会不会" | 判"现在什么状态" |
| 输出 | 诊断 | 读数 / 候选 |

**关键**：不是"同一份数据 + 不同 prompt"，是**取不同字段**。这再次说明：靠**组合**而非提示词。

---

## 五、缺口 4：显式请求的快路径

场景：学习者说"别问了，直接讲" / "我赶时间"——这种话必须**马上**影响 M10，但 M15 是低频的。

### 我的方案

| 类型 | 路径 | 速度 |
|---|---|---|
| 显式自述 / 约束 | → **M01 事实槽** → M10 直接读 | 即时 |
| 推断状态（没明说） | → **M15 慢提取** → 读数 | 低频 |

**理由**：显式自述是**事实**，可以直接走 M01；推断才是 M15 的活。这样既保住 M15 低频，又不让显式请求迟到。

---

## 六、缺口 5：状态本身 = 日志 + 视图

你提过"实时改 + git 追踪"。我的方案：**日志只追加（权威）+ 当前状态是视图（可重建）**。

**理由**：M15 的每条输出必须带**时间范围 / 来源 / 不确定性**；如果当前状态能被原地覆盖，这些就丢了。

---

## 七、需要你拍的两件事

1. **时效规则**：承载读数带时效窗口，**过期即"未知"，不沿用旧值**——认不认？
2. **快路径**：显式自述 / 约束走 **M01 事实槽 → M10**，不进低频的 M15——认不认？

拍完我就把 M15 写进仓库，D-001 标"部分解决（时效规则已定，具体窗口值待定）"。
