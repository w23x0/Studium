# 任务卡 B2b — PG-Schema 与属性图 schema 语言状态
角色：深度调研树分支调研员（B2b，B2 属性图系 R3 子任务）。
分支名：PG-Schema（属性图 schema 语言）规范状态与封闭词表表达力。

## 问题清单
1. PG-Schema 是否已发布为规范/草案？由谁发布（ISO/开源社区）？官方原文与版本日期。
2. PG-Schema 能否表达"边标签封闭枚举（9+other）""节点标签必填""属性类型枚举"这类封闭世界约束（原文语法示例）。
3. PG-Schema 与 GQL/SQL:PGQ 的关系：GQL 是否内建 schema 语言，还是 PG-Schema 独立演进？
4. 属性图数据的 schema 校验工具链现状（有没有 PG-Schema 校验器、库）。
5. 对 M08"候选池+审计晋升+处置单"状态：属性图 schema 能否表达状态机（边状态属性枚举+转换规则），还是必须靠应用层？

## 候选空间与线索
PG-Schema GitHub 仓库与规范页（Linked Data Benchmark Council 系）、GQL 标准、openCypher schema 讨论、属性图 schema 论文。

## 搜索关键词
中文：PG-Schema 属性图 schema 规范、属性图 标签 枚举 校验、GQL schema 语言、openCypher schema 约束
英文：PG-Schema property graph schema language specification、property graph schema constraints enumeration、GQL schema language PG-Schema、property graph validation tooling

## 工作量红线
可核来源>=15 且原文到手>=10；来源类型>=3 类；候选逐一查证>=5；关键词>=4 组。

## 产出格式（发现.md，写入 research_tree/findings/B2b_PG-Schema_发现.md）
0 一句话结论 / 1 逐条回答 / 2 关键发现（论断+URL+标题+机构+日期+原文摘录+等级 A/B/C）/ 3 冲突与张力 / 4 未决 / 5 来源清单 / 6 判死自查。

## 禁止项
不比较厂商；不越界（SHACL/ShEx 归 B5）；不捏造来源；抓不到写"未核"；推断标 C。
