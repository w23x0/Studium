# 任务卡 M4 — 原型实测——rdflib canonical 替代实现（rdf-canonize npm）在 921 锚点的指纹实测
> 引擎 v3 原型实测 / 状态 archived / 优先级 3 / verdict accepted (2026-08-13)

## 问题清单
1. rdf-canonize 5.0.0（node）是否真正实现 RDFC-1.0（对比 P1 证实的 rdflib canonicalize 静默 no-op）？用 P1 生成的 anchors_921.nq 实测。
2. 921 锚点 N-Quads 的 canonical 指纹是否稳定（不随三元组插入顺序漂移）且可复现？
3. canonical 指纹用于迁移校验的可行性：指纹比对能否替代全量重读？

## 工具/环境
- env: node v25.6.1 + npm rdf-canonize@5.0.0
- data: research_tree/_sources/P1/anchors_921.nq

## 范围边界
- 只验指纹稳定性与可复现性，不做性能
- 用 P1 的 anchors_921.nq 真实数据
- 复现命令必须写入报告
