# 任务卡 M1 — 原型实测——yaml-ld 全链路迁移原型（B7a U1：真实 frontmatter 语义无损往返 + 双真相消解）
> 引擎 v3 原型实测 / 状态 archived / 优先级 3 / verdict accepted (2026-08-13)

## 问题清单
1. python-yaml-ld 能否把真实带 frontmatter 的 md 文件语义无损转成 RDF 并往返（YAML→RDF→YAML）？用 docs 下真实文件实测。
2. 双真相风险（YAML 保留 vs RDF 派生）在迁移路径上如何消解/避免——迁移后权威载体是哪一个、如何验证不漂移？
3. 迁移产出的 RDF 能否回到可 grep 文本（守住 L0 锚点红线）？

## 工具/环境
- env: tmp/venv_b7a/Scripts/python.exe (yaml_ld, rdflib 7.6.0)
- data: docs 下真实带 frontmatter 的 md 文件

## 范围边界
- 只验语义无损与双真相消解，不做性能
- 用真实文件（docs 下任意带 frontmatter 的 md）
- 复现命令必须写入报告
