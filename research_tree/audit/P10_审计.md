# 审计 P10 — PASS

> 正式审计 2026-08-15。审计员独立复跑脚本 + 抽查判定与源数据一致性。

## 1. 复跑可复现（A）

- 复跑 `p10_conjugate_discriminator_v3.py`：corpus 81 nodes / 186 edges / 120994 chars 与 findings 一致；12 题判定分布 satisfied 6 / confound 3 / at-risk 3 完全复现。
- `p10_results_v3.json` 落盘，命中节点/边/原文摘录均可逐条核对。

## 2. 关键判定独立核验（A）

| 判定 | 审计独立核验 |
|---|---|
| N1 confound | grep "|x| < 1" 确命中 4 节点（basic-sequence-limit-formulas/geometric-series-manipulation/gregory-series/theorem-10-5），共享属实 |
| N2 satisfied | "Warning" 在 L1 仅 ratio-test anchors 含该词；原文 10.15 一处 "Warning. If the test ratio... always less than 1"，唯一性属实 |
| R1/R2 satisfied | comparison-test scope "Series of nonnegative terms"（≥0）；limit-comparison-test "a_n>0 and b_n>0"（>0），字符串层严格性差异属实 |
| I1 at-risk | dominated-integral 定义确引用 Theorem 10.24（非误命中）；答案"不是同一定理、是类比"须经 e-G4-019（from comparison-test-for-improper-integrals to comparison-test, label 类比） |
| V1 at-risk | "similar to the corresponding results for series" 原文仅 10.23 一处；gt 三节点文本不含该串，仅在 e-G4-018/019 边 label/证据——答案由边承载属实 |
| C1 at-risk | "not sufficient" 命中 nth-term-test 文本；"调和级数是反例"确在 e-G2-005（harmonic-series → nth-term-test, label 反例）|
| N3/N4 confound | "the test is inconclusive" 在 ratio-test 与 root-test 定义文本同时出现（L=1 与 R=1 均 inconclusive），共享属实 |

## 3. 判死四条件（原型实测适配版）

- 伪造素材：无。全部命中来自审计过的 nodes-audited/edges-audited 与 source/ch10 原文。
- 时间锚定：N/A（原型实测自有语料，非文献检索）。
- 工作量红线：12 查询覆盖 6 维，每维 1-4 题；脚本可复现，判据清晰。达标。
- 诚实边界：proxy 边界（人工构造查询集/决定性串选择/单书单版本）已在 findings §3 显式声明；未把 C 推断当 A。

## 4. 结论

PASS。P10 在自有语料上把 K4 §5 风险 1 的「简单检索能否满意」代理**可操作化**为「决定性串 grep 唯一性检查」，并给出两型真正涉险点（共享串 confound / 跨边 at-risk）。M1 对 K4 (a) 的收紧（多跳不必然涉险）有据可查。
