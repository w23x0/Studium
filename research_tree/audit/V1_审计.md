# 审计 V1

## 判定：FAIL（来源抽检命中一处伪原文摘录；其余红线与工作量指标达标）

被审计：`C:/Users/Wang/Desktop/Studium/research_tree/findings/V1_发现.md`
快照目录：`C:/Users/Wang/Desktop/Studium/research_tree/_sources/V1/`（22 个快照文件，全部读取）
审计方式：对列出来源直接 HTTP 抓取实时原文，与发现文件"原文摘录"逐字比对；全部 22 个快照逐一读取比对。抽检 21/22 个来源（不含 arXiv API 发现日志 URL）。

---

## 一、判死四条件逐条核对

### 1. 关键论断无来源 —— 命中缺陷（不通过）

**KF8（及 §1 Q2 补充 1 复用同一句）的"原文摘录"首句是伪原文**：

> "Table 1 shows that fine dependencies consistently exceed unique explicit references per theorem (e.g., 11.57 vs. 2.7 for xboole_0)."

标注来源 `https://arxiv.org/html/1108.3446v2`（全文），但该句在 1108.3446 的 **v1 全文、v2 全文、abstract 页三处均不存在**。核验证据：
- v2 全文：`exceed` 出现 **0 次**；`fine dependencies` **0 次**；`fine-grained dependencies` **0 次**；`unique explicit references` 仅 1 次，且出现在表 1 列注 "Uniq. Expl. Refs.: Average number of unique explicit references per theorem"。
- 真实原文是表 1 列注 + 数据行：`xboole_0  7  4  2.7  11.57  12.62`（Uniq. Expl. Refs.=2.7、Fine Deps.=11.57），列注为 "Fine Deps.: Average number of all (both explicitly and implicitly) used items ... per theorem as computed by dependency analysis."
- 即：**数据为真，所引句子为拼接改写，却被加引号作为逐字"原文摘录"**；快照 `alama_premise_selection_fulltext.md` 同样复写了该句，导致快照逐字比对被"洗净"通过。

严重性判断：该论断是本任务承重结论之一（"手写引用系统性低估真实依赖 → M08 须配删除测试"，见 §1 Q2 补充 1 与 §3 决策点 4）。更重要的是，这违反本引擎自己的红线"锚点判等=原文对应（机器可校验文本对应）"——发现文件第 0/4/8 节还在反复强调这一红线。属最高等级的来源诚信缺陷，按红线从严判 FAIL。数据本身可被表 1 证立，故不是"查无出处"，而是"出处文本被改写冒充逐字原文"。

### 2. 二手当原文（该标 B 却标 A）—— 未发现

- Wikipedia en/zh 均正确标 B（KF5）；关键句"元语言逻辑无关"已在 mmset.html（A 级官方页）独立核对成立，作为 B 级佐证使用合理。
- 小疵：Q1 点 1 引用维基"$p 语句的证明是引用列表"句，却把整体 Q1 记 A（该子条来源实为 B）。但因该结论被 mmset.html 的 A 级原文 "For brevity, a proof may also refer to earlier theorems, but in principle it can be expanded into references to only the initial axioms and rules" 独立佐证，不构成系统性误标，仅表述口径问题（列入整改建议）。

### 3. 越界或漏答 —— 未发现

- Q1/Q2/Q3 全部作答且各有结论与依据等级。
- 未评估 Lean/Coq/Metamath 作为运行时（发现文件第 4 节明确声明"无运行时结论"），在范围内。
- 以 06-M08 §8 为基线深化验证，未重复 B6a 已核的存储/版本化结论，聚焦依赖图/前驱选择/引用坍缩，Metamath 为 B6a 未覆盖项——无越界。

### 4. 推断未标注写成结论 —— 基本合规

- KF7"与 M08 §4.2 删除测试同构"、§1 Q2"对应 M08 §3 机理"均显式标 C（推断），合规。
- 小疵：Q1 各点"→ 对应 M08 §10 Q1 实测定论 / §3 修法"的映射与 Q2 同类映射性质相同，Q2 标了 C、Q1 未标（Q1 整体记 A），口径不一致（列入整改建议，非致命）。

---

## 二、来源抽检记录（21/22 个来源，全部可达；快照比对 22/22）

> 抽检方式：对每个来源直接 HTTP 抓取实时页面文本，对发现文件"原文摘录"逐字比对（等价于 WebFetch 打开验证，且覆盖度远超 20% 红线）。全部返回 200，无 404。

| 来源 | 可达 | 逐字比对结果 |
|---|---|---|
| arxiv.org/abs/1108.3446 | ✓ | 4 句命中（essential when… / 50% improvement… / 2078 / newly available minimal dependency analysis） |
| arxiv.org/html/1108.3446v2 | ✓ | refactoring…/brute-force…/greedy…/minimality means…/suppressed facts 命中；**"Table 1 shows…"句不命中（伪句，见判死 1）**；数据 11.57/2.7/xboole_0 在表 1 存在 |
| arxiv.org/html/1108.3446v1 | ✓ | 对照：该句亦不命中（排除版本差异） |
| us.metamath.org/mpeuni/mmset.html | ✓ | proof 定义句、"in principle it can be expanded into references to only the initial axioms and rules"、2,913 subtheorems、26,323 steps、184 layers、show trace_back 2p2e4 /essential 全部命中 |
| us.metamath.org/mpeuni/2p2e4.html | ✓ | "This theorem is used by" + "depends on axioms/definitions/syntax axioms" 三分类 + ax-mp/ax-gen/df-bi/df-tru/wceq/2t2e4/4bc2eq6 全部命中 |
| lean-lang.org/doc/reference/latest/Axioms/ | ✓ | #print axioms 4 句 + 公理 4 句（postulated constants / even false propositions / trusted only to the extent / cannot check whether new axioms are consistent）全部命中 |
| arxiv.org/html/2306.15626v2 | ✓ | first tool / intercepting the elaborator / DAG files-import relations / constants / 128K→33K 等 8 处命中 |
| arxiv.org/html/1107.4721v2 | ✓ | over-approximation→minimal set / site bidir / path query / hide implicit / Mizar still verify 命中 |
| arxiv.org/abs/1107.4721 | ✓ | decomposing fine-grained / 50000 theorems / website 命中 |
| arxiv.org/abs/2505.12305 | ✓ | proofs as terms / grammar compresses gigantic proof trees 命中 |
| arxiv.org/abs/1109.3687 | ✓ | Two methods / various kinds of dependencies 命中 |
| arxiv.org/abs/1107.3209 | ✓ | smart re-verification / precise tracking of mathematical dependencies 命中 |
| arxiv.org/abs/2210.02336 | ✓ | dependency graph of the MML / three types of search 命中 |
| arxiv.org/abs/2510.23637 | ✓ | bottleneck / heterogeneous dependency graph / relational information / 25% 命中 |
| arxiv.org/abs/2304.00994 | ✓ | suggests relevant premises / random forest trained on mathlib / suggest_premises tactic 命中 |
| arxiv.org/abs/2404.09939 | ✓ | autoformalization, premise selection, proofstep generation, and proof search 命中 |
| arxiv.org/abs/1709.09994 | ✓ | selecting mathematical statements / graph / 83% to 90.3% 命中 |
| arxiv.org/abs/2506.07477 | ✓ | hammer integrates premise selection… / dynamically adapts to user-specific contexts 命中 |
| arxiv.org/abs/2306.15626 | ✓ | fine-grained annotations / 98,734 theorems 命中 |
| en.wikipedia.org/wiki/Metamath | ✓ | "$= and are given as a list of names of axioms, rules and theorems"、metalanguage / no specific logic embedded、ZFC / intuitionistic / New Foundations 命中 |
| zh.wikipedia.org/wiki/Metamath | ✓ | "是用來發展嚴格形式化數學定義及證明的一款語言"命中 |
| github.com/…/mathlib4/tree/master/scripts（经 raw 文件核验 dag_traversal.py / topological_sort.py） | ✓ | "Parses the import DAG from `.lean` source files and parallelizes…"——原文为 **parallelizes**，发现文件/快照写 parallelises（拼写小偏差）；"Print Mathlib modules in topological (import-DAG) order"——原文为 **Print**（无 s），发现文件/快照写 Prints（拼写小偏差） |

**快照比对**：22 个快照全部存在、内容与发现文件一致。对上述约 40 处引文做逐字实时比对：**20/22 个快照完全逐字命中**；`alama_premise_selection_fulltext.md` 含 KF8 伪句（未命中）；`mathlib_scripts.md` 两处 docstring 拼写与原文有出入（parallelises/parallelizes、Prints/Print，语义无差）。

**未抽检项（作者已如实声明）**：mizar-items/mizar.org 网站在线可用性（作者标"未核"，不属审计范围）；arXiv API 发现日志 URL；2026 新论文（2605.13137、2602.15511）仅 API 摘要行（作者标 C/未核）。

---

## 三、工作量红线核对

| 红线 | 要求 | 实际 | 判定 |
|---|---|---|---|
| 可核来源 | ≥15 | 22（19A+2B+1 发现日志） | ✓ |
| A 级原文 | ≥10 | 19（含 3 篇全文 HTML + 3 页官方文档） | ✓ |
| 来源类型 | ≥3 | 4（论文/官方文档/工程仓库/百科） | ✓ |
| 候选查证 | ≥5 | 5 组（method lib、MML 依赖分析、Metamath、premise selection 一般方法、Lean 依赖查询） | ✓ |
| 关键词组 | ≥4（中英各半） | 中 5 + 英 4 = 9 组记录在案 | ✓ |

**工具异常声明核验**：作者如实声明 WebSearch 对全部 9 次查询（中 5 英 4）返回空结果（工具侧故障），源发现改由 arXiv API + 直接 WebFetch 完成；中文一手来源缺失、仅 zh.wikipedia 一条中文二手（B）。该声明透明、无把空结果包装成命中，可接受。红线全部达标。

---

## 四、整改意见

1. **【必改·红线级】** 将 KF8 与 §1 Q2 补充 1 的伪原文句替换为**真实逐字原文**。可用的真实原文：表 1 列注 "Uniq. Expl. Refs.: Average number of unique explicit references per theorem" 与 "Fine Deps.: Average number of all (both explicitly and implicitly) used items … per theorem as computed by dependency analysis"，或表 1 数据行 "xboole_0 … 2.7 11.57"。若确需转述结论，改为不带引号的转述并标 C，不得加引号冒充原文摘录。
2. **【必改】** 同步重写快照 `alama_premise_selection_fulltext.md` 为逐字文本（去掉伪句，换上表 1 真实列注/数据）；并修复 `mathlib_scripts.md` 及 KF17 两处 docstring 拼写（parallelises→parallelizes、Prints→Print），保证快照逐字可校验。
3. **【建议】** Q1 各点"→ 对应 M08 §10/§3"的映射与 Q2 同类标注对齐（标 C 或注明"对照，非新增推断"），统一口径。
4. 修复后重跑"判死自查"并更新快照时间戳；复核一次即可放行——其余 20/22 快照逐字命中、全部红线指标达标、判死条件 2/3/4 未触发。
