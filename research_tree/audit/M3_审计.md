# 审计 M3

- 被审计发现：`research_tree/findings/M3_发现.md`（原型实测——双写/影子读迁移原型，B8c）
- 原型目录：`research_tree/_sources/M3/`
- 审计员复核方式：逐条核对报告数字 ↔ `run_output.txt`；**亲自重跑三个脚本**（解释器 `tmp/venv_b7a/Scripts/python.exe`，rdflib 7.6.0）；独立验证若干关键事实主张（M4 丢节点被引用边数、零锚点节点 id、顶层 vs inherited 数据计数）。

## 判定：PASS

## 一、判死四条件

逐条判定，四条件均不触发。

1. **关键结论无实测依据（纯推断当结论）** — 不触发。
   三问结论均有实测支撑：问题 1（回放可行且一致）由 build_wall_seconds、graph_triples、五项一致性自检、RDF→dict 逐字段回读、两次重建字节一致等 stdout 数字支撑；问题 2（影子读落地与差异暴露）由 clean 零差异基线 + M1–M4 各查询 MATCH/MISMATCH 及 diff 内容支撑；问题 3（成本结构）由三条路线实测数字与 100 边批次对比支撑。结构性结论（"一次重建比一轮影子读便宜"、"计数级对对称变更失明"）均由数字直接推出。

2. **实测数字非来自真实运行（伪造/估数顶替）** — 不触发。
   报告 §3 每个关键数字都能在 `run_output.txt` 对应行找到，且**审计员重跑全部对得上**（详见第二节）。无估计值顶替。唯一随运行波动的计时/比率类数字（build 0.09–0.34 s、Turtle 3.5–3.75 s、双写 ms、比率 14.75×/113.18×），报告在 §1 与 §5.1 已如实标注为"单进程可行性数字、非性能基准、随运行波动"，且波动已给出区间，非伪造。

3. **越界或漏答** — 不触发。
   三问（回放时间与一致性、影子读落地与差异暴露、三条路线成本结构）均有实测结论与数字。任务卡范围是全量 522/1433 实测回放，原型正是如此（含 inherited L1）；未做性能基准，但报告明确标注成本数字为可行性数字而非基准，属于如实声明范围而非越界。未测场景在 §5.6 如实列出（并发/多版本/增量回放未测）。

4. **未标注实测/推断** — 不触发。
   报告通篇标注来源：全部数字注明"来自 run_output.txt 真实 stdout（§3 已标行号）"；成本数字明确标注"单进程可行性数字，非正式基准"；未测/未覆盖事项在 §5 独立成节。

## 二、可复现性核验（脚本路径、复跑命令、复跑输出与报告差异）

**脚本与产物存在**（`research_tree/_sources/M3/`）：`common.py`、`m3_replay.py`、`m3_shadow_read.py`、`m3_cost_structure.py`、`README.md`、`run_output.txt`、产物 `m3_replay_graph.nt`（2441880 B）与 `m3_replay_graph.ttl`（1293827 B）。解释器 `tmp/venv_b7a/Scripts/python.exe` 存在，`rdflib 7.6.0` 确认。

**复跑命令**（与报告 §7 一致，审计员实际执行）：
```
cd /d C:\Users\Wang\Desktop\Studium\research_tree\_sources\M3
C:\Users\Wang\Desktop\Studium\tmp\venv_b7a\Scripts\python.exe m3_replay.py
C:\Users\Wang\Desktop\Studium\tmp\venv_b7a\Scripts\python.exe m3_shadow_read.py
C:\Users\Wang\Desktop\Studium\tmp\venv_b7a\Scripts\python.exe m3_cost_structure.py
```
三个脚本均成功跑完（无报错，无新增依赖安装）。

**复跑输出与报告差异**：

- **确定性数字全部精确吻合**（逐项比对）：
  - `node_records 522` / `edge_records 1433` / `duplicate_(src,rel,dst)_groups 0` — 吻合。
  - `graph_triples 16121` — 吻合。
  - `peak_python_alloc_bytes 18144824 = 17.30 MiB` — **逐字节相同**。
  - 五项一致性自检 verdict：`node_subjects 522 OK`、`direct_rel_triples 1433 OK`、`reified_edge_records 1433 OK`、`dangling_ids 0 OK`、`nodes_with_zero_anchors 4 FAIL`（4 个 id 完全相同）— 吻合。
  - `total_anchors 921` — 吻合。
  - RDF→dict 逐字段回读：15 字段分母/通过率全部 100%，`node_field_mismatches 0`、`edge_reified_roundtrip_ok 1433/1433`、`direct_edge_rows_exactly_one 1433/1433` — 吻合。
  - 重建确定性：`nt_bytes_run1 == nt_bytes_run2 == 2425759`，`rebuild_byte_identical True` — 吻合。
  - 序列化产物字节数：`nt_written_bytes 2441880`、`ttl_written_bytes 1293827` — **逐字节相同**。
  - 节点类型分布 concept 258 / theorem 173 / method 69 / notation 22；关系分布 requires 707 / part-of 314 / is-a 148 / contrasts 125 / other 40 / applies-to 33 / generalizes 31 / implies 25 / alias-of 6 / equivalent 4 — 全部吻合。
  - 影子读 clean：6 查询全 MATCH、diff 0；M1 q_rel_counts MISMATCH（requires 707,706）、q_edge_inventory MISMATCH（1）、q_edge_provenance MISMATCH（1）；M2 q_rel_counts MATCH（计数盲）、q_edge_inventory MISMATCH（2）、q_edge_provenance MISMATCH（2）；M3 q_node_inventory MISMATCH（1，diff 中可见 "TAMPERED."）；M4 q_node_type_counts MISMATCH（theorem 173,172）、q_node_inventory MISMATCH（1，new='<absent>'）、q_dangling MISMATCH（1，in_new_only）— **verdict 与 diff 内容全部一致**。
  - 影子读 verdict 跨运行可复现：复跑结果与报告逐查询一致，属实。

- **波动性数字（如实标注为可行性数字）**：复跑 build 0.3273 s（报告 0.3383，均在报告声明的 0.09–0.34 s 区间内）、serialize_nt 0.1181 s（报告 0.1395）、serialize_turtle 2.9132 s（报告 3.7500）；cost 脚本复跑 build 0.0926 s、双写每边 0.0599 ms（报告 0.0728）、100 边批次比率 20.94×/171.35×（报告 14.75×/113.18×）。绝对值有偏差，但**结构性顺序与量级完全一致**：重建（0.13 s）远便宜于影子读（1.06 s），双写边际最小，比率同为"影子读 >> 重建 >> 双写写入"。报告已声明此类数字随机器负载波动、非基准。

- **独立验证的关键事实主张**（非仅复跑）：
  - M4 丢节点 `apostol:theorem-15-15-orthogonal-decomposition`：22 条边引用、其中 15 条以它为 src → 30 条以它为 subject 的三元组（直连 rel + hasEdge）——与报告 §1 问题 2 的"RDF 实现陷阱"论证一致。
  - 4 个零锚点节点 id 与报告完全一致（数据侧 SPEC 违规如实保留）。
  - 数据计数澄清：顶层 glob 397 节点 / 1220 边，`inherited/` L1 底座 125 节点 / 213 边，合计 522 / 1433——与报告 §1 先决发现完全一致。

- **微小瑕疵（不影响判定）**：M2 的 2 行 diff 在多次运行间输出顺序可能互换（集合迭代顺序非确定），内容完全一致，非实质差异。

## 三、工作量

- **问题逐条有实测回答**：三问均有"实测结论 + 具体数字 + 证据出处"；clean 基线、四类偏离、成本三条路线覆盖充分。
- **复现命令完整可复制**：报告 §7 与 README 给出了逐条命令、解释器路径、依赖声明（rdflib 7.6.0 已装无新增依赖），审计员照抄即跑通。
- **未决/风险如实**：§5 六条（数字波动、内存口径、查询集覆盖面、RDF 集合语义丢顺序、4 个零锚点数据违规、未测场景）诚实列出，未隐瞒局限。
- **数据侧发现**：除任务三问外还产出了两个有价值的数据质量发现（4 个零锚点节点违反 SPEC；任务卡 522/1433 需含 inherited 才能对上），均标注清楚。

## 四、整改意见

均为可选改进，不构成驳回理由：

1. **影子查询集扩展**（报告 §5.3 自认）：`x:` 节点专用字段（apostol_ids/strang_ids/divergence）与 `rel_note` 尚未进影子比对清单，仅逐字段回读验过。若该结论要作为"语义等价"依据外推，需补进查询集。
2. **差异暴露的定量表达**：M2 反向边"计数盲"是当前实验设计的核心洞见，可在报告中补一句"任何对称变更（总量不变）都逃逸计数级检查"，以便后续审计复用该判据。
3. **性能数字标注位置**：复跑发现 cost 脚本的比率类数字对负载极敏感（本次 20.94×/171.35× vs 报告 14.75×/113.18×）。建议在报告结论处把比率也标注"随负载波动、仅供量级参考"，避免被当作稳定结论引用。
4. **数据侧跟进**：4 个零锚点节点建议按报告 §6 建议尽快补锚点或标注 `origin: model` 豁免，避免后续管线误判。

（审计日期：2026-08-13；复跑环境：`tmp/venv_b7a/Scripts/python.exe`，rdflib 7.6.0，Windows。）
