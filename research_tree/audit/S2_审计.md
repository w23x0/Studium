# 审计 S2

> 任务：S2 图数据库作为查询层的组合方案评估（L0/L1/L2 之上叠只读图引擎）
> 被审计对象：findings/S2_图数据库查询层_发现.md（2026-08-13）
> 快照目录：research_tree/_sources/S2/
> 审计员：独立审计员（2026-08-13）

## 判定：PASS

判死四条件全部不触发；来源可核性抽检与快照逐字比对均达标；工作量红线五项全过。
（附 1 条不构成判死的整改建议：K8「意义」一处无直接来源的归纳未标 C。）

---

## 一、判死四条件逐条核对

### 1. 关键论断无来源 —— 否（不触发）

- K1–K13 每条均含 URL + 标题 + 机构 + 日期 + 原文摘录 + 等级，结构完整。
- 四问（Q1 触发/Q2 形态/Q3 能力边界/Q4 红线）全部锚定 K 或本地原文；本地引用逐字验证通过：
  - 06-M08技术选型.md §7：`出现需要三跳以上遍历、或需要在图上做实时交互查询的真实产品需求时`（L420）、`按 id 取、按边遍历一两跳、全表校验`（L415）、`不物理删除，写 graveyard`（L416）、`append-only JSONL`（L413）、§9 三种图刻意不决定（L473–478）、`四种图不能合并`（L489）。
  - M07 审查单：`按结构、文本、标签或问题查找`、`可浏览资料目录`、`来源引用与原文跳转`、`跨资料检索标签与重复线索`（均逐字命中）。
  - M08 审查单：`用户可查看节点、上下游、来源、状态和关系理由；基础实验可采用类似 Obsidian 的关系图，但图形界面只是查看方式`（逐字命中）。
  - M05 审查单：`从多个可能的学习闭环中选择当前和下一目标`——研究者对「批量决策」的转述与原文范围一致。
- 结论「等触发」可判定：触发四条清单逐项可核，Kuzu 与「双写保索引」明确不上。

### 2. 二手当原文 —— 否（不触发）

- 本次新取 12 项外部 URL 全部直接抓取原文（A），另有本地 4 份（A）。
- 沿用前轮 17 项中仅 Two Generals（Wikipedia）为 B，且发现文件中等级保持 B 标注、引用时明示；其余 16 项沿用 A 级，与前轮 B8c/T1/B7/B1c/B2c 发现文件逐字复核一致。
- 抓不到的（jiqizhixin/juejin/Neo4j 旧 URL）标「未核」且未入关键论断——已核验这些失败为真实失败（见二）。

### 3. 越界或漏答 —— 否（不触发）

- 未评估 LangGraph/Temporal 等运行时编排（明确归 M11）；未做性能基准（明确声明）；未改变红线。
- 四问逐条回答、8 节齐全、来源清单 33 项、未决/风险 6 项、下一步 5 条。

### 4. 推断未标注写成结论 —— 基本通过（1 处小瑕疵，不判死）

- Q2 成本结构（标「推断 C」）、Q3 能力归并（标 C）、Q4 红线风险推导（标 C）、未决项 6 条（标 C 或「未核」）均显式标注。
- 小瑕疵：K8「意义」中「文档-图混合的现实落地多靠『各存各的 + 应用层编排』，而非一个成熟统一框架」为无直接来源的归纳，未标 C。属注释性分析、不支撑主判定（主判定由 Q1 本地文档 + T1 + LDBC 驱动），不构成判死；建议补标。

---

## 二、来源抽检记录

### URL 抽检（11 个，占 33 来源的 33% ≥ 20%）

| URL | 结果 |
|---|---|
| github.com/kuzudb/kuzu | 可达，逐字命中 K1：`This repository was archived by the owner on Oct 10, 2025. It is now read-only.` + `Kuzu is working on something new!` |
| github.com/apache/age | 可达，逐字命中 K2：首句、`AGE supports Postgres 11, 12, 13, 14, 15, 16, 17 & 18`、v1.8.0 |
| postgresql.org docs rules-materializedviews | 可达，逐字命中 K9：`persist the results in a table-like form`、`the data is not always current; yet sometimes current data is not needed` |
| martinfowler.com/bliki/CQRS.html | 可达，逐字命中 K10：`use a different model to update information…`、EagerReadDerivation、`adds risky complexity` |
| jena.apache.org documentation/fuseki2 | 可达，逐字命中 K11：`Apache Jena Fuseki is a SPARQL server…` |
| ldbcouncil.org/benchmarks/snb/interactive | 可达，逐字命中：`complex read queries that access the neighbourhood of a given node`（沿用 T1 F2 同源） |
| github.com/block/elasticgraph | 可达（首查 502 瞬断、重试成功），逐字命中 K7 首句 + MIT |
| github.com/orgs/kuzudb/repositories | gh api 复核：`archived_count=24, total=24`，与 K1「24 个全 Public archive 无后继」一致 |
| neo4j.com …/configuration-settings | WebFetch 403（bot 拦截，非 404）；快照 neo4j_readonly_extract.txt 逐字命中 K3 两条设置 |
| neo4j.com/labs/apoc/5/overview | WebFetch 403；快照 HTML grep 命中 K4：`apoc.cypher.runFileReadOnly(file or url,[{timeout:10,parameters:{}}]) - runs each …` |
| neo4j.com/docs/aura/virtual-graph | WebFetch 403；快照 neo4j_virtual_graph.txt 逐字命中 K5：`It neither moves nor extracts data…`、`Translate Cypher to optimized SQL queries` |

> Neo4j 三页对 WebFetch 返回 403 属站点 bot 拦截（研究者用 curl 抓取），快照内容逐字验证命中，非死链、非伪造。

### 快照逐字比对（12 条全部命中，远超 ≥10%）

_sources/S2/ 目录存在且含 19 个文件。逐字比对命中 12 条：

1. kuzu_repo_status.txt ↔ K1 归档句 — 命中
2. kuzu_org_repos.txt ↔ K1 org 状态 — 命中
3. apache_age.txt ↔ K2 — 命中
4. neo4j_readonly_extract.txt ↔ K3 — 命中
5. neo4j_apoc_overview.html（grep）↔ K4 — 命中
6. neo4j_virtual_graph.txt ↔ K5 — 命中
7. graphar.txt ↔ K6（`serve as a direct data source…`）— 命中
8. elasticgraph.txt ↔ K7 — 命中
9. neo4j_es_connector.txt ↔ K8（gh api 复核 pushed_at 2020-10-13、archived:false）— 命中
10. pg_materialized_views.txt ↔ K9 — 命中
11. fowler_cqrs.txt ↔ K10 — 命中
12. jena_fuseki.txt ↔ K11 — 命中

### 失败页诚实性核验

- jiqizhixin_search.html 标题 = 「找不到您请求的页面」（确为 404）。
- juejin_search.html 为无结果搜索页壳（`图数据库` 出现 1 次、无结果列表）。
- neo4j_readonly_databases.html / neo4j_cypher_admin_databases.html 内容 = `Page not found`，确为 404。
- 与发现文件「未核/不可用」声明一致——未把失败页当来源。

### 沿用来源复核

B8c 发现文件逐字复核命中：Fowler `discard the application state completely and rebuild it by re-running the events`（K19）、Azure `This process is known as rehydration`（K20）、microservices.io `there's no guarantee that the transaction will commit`（无 2PC）、Two Generals（B）、Stripe 4 步双写、Scientist `use=control / try=candidate`。T1 发现文件复核命中：LDBC SNB IC1 ≤3 跳、四条触发清单、`当前判定：未触发`。

---

## 三、工作量红线核对

| 红线 | 要求 | 实测 | 判定 |
|---|---|---|---|
| 来源数 | ≥15 | 33（本次 16 + 沿用 17） | ✓ |
| A 级 | ≥10 | 逐条清单实计 A 32 / B 1（研究者自报 A 30，略保守，不影响达标） | ✓ |
| 类型 | ≥3 | ≥6 类：官方文档、GitHub 仓库、博客、论文、百科、本地项目文档 | ✓ |
| 候选查证 | ≥5 | 6 项逐一查证 | ✓ |
| 关键词组 | ≥4 | 6 组中英各半；全部被搜索引擎拦截、已诚实标注空结果并改用直接导航权威原文（与 B8c 环境记录一致） | ✓ |

---

## 四、整改意见（PASS，非阻断）

1. **K8「意义」补标 C**：`文档-图混合的现实落地多靠「各存各的 + 应用层编排」`一处无直接来源的归纳，建议补 C 标注；K8 摘录本身（仓库元数据，A）不动。
2. **统计口径对齐**：§7 自报「A 级 30」与逐条清单实计 32 不一致，建议按清单修订（不影响达标）。
3. **触发判据操作化**：维持判定「等触发」正确；按建议下一步第 2 条，把 T1 实验 A（当前规模 3 跳计时）与实验 B（合成图拐点）纳入 E 系列实测，避免触发条件停留在纸面。
4. **环境限制说明**：WebSearch 全空 + Neo4j 站点对 fetch 反爬是真实环境约束（本审计员复测同样遇 403/502），研究者改用 curl/gh api 直接导航并保存快照，方法与 B8c 一致，可接受。
