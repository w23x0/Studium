# 审计 P3

> 任务：P3 原型实测——ShEx valueSet '~' 表达 9+other 词表封闭的校验语义
> 被审计对象：findings/P3_ShEx9+other_发现.md（2026-08-13 20:27）
> 原型目录：research_tree/_sources/P3/（p3_shex_valueset.js、p3_shacl_compare.py、run_output.txt 133 行、README.md、package.json、node_modules/）
> 审计方式：独立复跑两个脚本 + 逐项核对 run_output.txt + 独立次级断言（数据词表、源码、规范引文、rdf:type 特例）

## 判定：PASS

判死四条件全部不触发；复现命令实际执行成功（node v25.6.1 exit 0 + venv python exit 0），复跑输出与 run_output.txt 逐项一致（仅 3 处毫秒级计时抖动），报告中每个关键数字均在 run_output.txt 可溯源。附 2 条不构成判死的整改意见（见第四节）。

---

## 一、判死四条件

### 1. 关键结论无实测依据 —— 否（不触发）

每条关键结论都有 run_output.txt 直接 stdout 佐证，且经审计员复跑验证一致：
- **`[9 ~]` 粘成 IriStem（B5b A 级写法被证伪）**：run_output.txt T1 `解析为:` 行末尾为 `{"type":"IriStem","stem":"http://s.example/rel/requires"}`，other/arbitrary/literal 三行 REJECT，第 9 个固定值 requires 是前缀自匹配假象。报告 §0/§1 如实呈现，未把"9 固定值全过"当真实通过。
- **语法探针**：run_output.txt `[~]`/`[ . ]`/`[~ - rel:part-of]` PARSE ERROR、`[ . - rel:part-of]` PARSE OK，与报告一致。
- **Q2 CLOSED/EXTRA 16/16**：run_output.txt `Q2 汇总: 16 次校验, 0 次与预期不符`，ClosedShapeViolation/MissingProperty 违规类型逐行可见。
- **Q3 pySHACL 对照**：run_output.txt 后半段 S1/S2 逐节点 ACCEPT/REJECT、S3 两行 conforms、语法长度 132/138/124/165，均与报告一致。
- **EXTRA 语义**（不放行外来谓词、放行表达式内超基数副本）：run_output.txt Q2 四场景齐备，且审计员在 validator 源码 `whatsMissing()` 层复核一致。

### 2. 实测数字非来自真实运行 —— 否（不触发，附 1 处次要溯源瑕疵）

- run_output.txt 由 `node p3_shex_valueset.js > run_output.txt 2>&1` + venv python 追加生成，两命令 exit 0，输出为程序真实 stdout。
- 审计员独立复跑，diff 仅 3 处差异且全部为毫秒级计时（详见第二节差异表），所有语义数字与报告完全一致。
- **唯一溯源瑕疵**：报告 §3/§8 声称"总耗时 127.4ms 来自 run_output.txt"，但当前 run_output.txt 为 `总耗时: 119.962ms`（审计员复跑 120.274ms）。该数字是毫秒级计时、报告已标注"非性能基准/随复跑抖动"，属报告未随最终 run_output.txt 对齐的编辑不一致，不构成伪造或估数顶替，也不影响任何语义结论。

### 3. 越界或漏答 —— 否（不触发）

- 三问（值封闭/谓词封闭/SHACL 对照）逐条以实测数字回答。
- 任务卡范围边界"只验语义、不做性能测试"遵守：耗时仅作量级参考，对比的是语法长度而非运行性能。
- 环境依赖全部可用：shex 已 npm 装成（1.0.0-alpha.29），pyshacl 现成 venv（0.40.1），无"装不上"。

### 4. 未标注实测/推断 —— 否（不触发，附 1 处次要标注瑕疵）

- B5b"`[ ex:t1 ... ex:t9 ~ ]` 表达 9+other"由 A 级被实测证伪，报告 §0/§1/§4/§6 明确标注"被实测证伪/应降级"，未含糊。
- shex.js 通配拒字面量与规范 §5.4.6"通配匹配所有 RDF 词项"的出入，报告 §4 以"规范说 X / 实测 Y"双栏标注，并附源码证据。
- **次要标注瑕疵**：§1 Q2 注记"rdf:type 特例两侧对称（实测均 nonconformant）"——两个脚本均未跑该用例（Q2 节点无 rdf:type 三元组，S3 已含 sh:ignoredProperties(rdf:type)）。审计员独立实测该论断为**真**（ShEx CLOSED + 带 rdf:type 节点 → nonconformant ClosedShapeViolation；SHACL sh:closed 无 ignoredProperties → False），故属于"正确推断误标实测"，内容无误，仅标注不精确。

---

## 二、可复现性核验

### 脚本与产物路径

- 脚本目录：`C:/Users/Wang/Desktop/Studium/research_tree/_sources/P3/`
- 脚本：`p3_shex_valueset.js`（shex.js Q1/Q2 + 语法探针）、`p3_shacl_compare.py`（pySHACL Q3 + 语法长度）；`run_output.txt` 133 行真实 stdout；`README.md`、`package.json`、`node_modules/` 齐备。
- 依赖版本（脚本头部打印）：node v25.6.1、shex meta 1.0.0-alpha.29（parser 1.0.0-alpha.28、validator 1.0.0-alpha.29、n3 1.26.0）；python 3.14.3、pyshacl 0.40.1、rdflib 7.6.0。审计员逐一核验与报告一致。

### 复跑命令（审计员已实际执行）

```bat
cd C:/Users/Wang/Desktop/Studium/research_tree/_sources/P3
node p3_shex_valueset.js > rerun.txt 2>&1   :: exit 0
C:/Users/Wang/Desktop/Studium/tmp/venv_b7a/Scripts/python.exe p3_shacl_compare.py >> rerun.txt 2>&1   :: exit 0
```

与报告第 7 节命令等价（报告命令为写入 run_output.txt 的单行式）。

### 复跑输出与报告差异

`diff run_output.txt rerun.txt` 全量 diff 仅 3 行差异，全为毫秒级计时：

| 行 | run_output.txt | 审计员复跑 | 性质 |
|---|---|---|---|
| 103 总耗时 | 119.962ms | 120.274ms | 毫秒抖动 |
| 111 S1 耗时 | 1.8ms | 1.9ms | 毫秒抖动 |
| 117 S2 耗时 | 2.2ms | 1.9ms | 毫秒抖动 |

其余 130 行逐字符一致，包括：Q1 汇总 30 ACCEPT / 18 REJECT (合计 48)、T1 解析 AST 含 IriStem、语法探针 4 行、Q2 16 次/0 不符、S1/S2/S3 逐节点 ACCEPT/REJECT、语法长度 132/124/138/165。报告每个关键数字均与 run_output.txt 逐项对应。

### 独立次级断言（审计员另做）

1. **数据词表**：全量扫描 `docs/图工程调研/M08实验-第15章分层图谱/data/edges-*.jsonl`（rel 字段），distinct rel = 10 个（9 固定 + other），other 恰 35 条边，与报告建模完全一致。
2. **粘合机理源码**：`@shexjs/validator/src/shex-validator.ts` testValueSetValue IriStem 分支 `if (value.termType !== "NamedNode") return false`（1170 行附近），与"通配拒字面量"证据吻合。
3. **通配拒字面量偏离**：IriStemRange 分支（1172 行）同款 NamedNode 守卫 → 字面量必拒；规范侧经 shex.io/shex-semantics §5.4.6 确认 Wildcard 匹配**所有 RDF 词项**（Example 3：`<issue8> foaf:mbox 123` 通过 `[ . - … ]`），故报告"shex.js 偏离"标注成立。
4. **rdf:type 特例**（报告标"实测"）：审计员独立跑最小用例——ShEx `CLOSED { rel:part-of [rel:part-of] }` + 节点带 rdf:type → nonconformant ClosedShapeViolation；SHACL sh:closed 无 ignoredProperties → False。论断为真，但非两脚本所测，建议改标注（见第四节）。
5. **字符长度**：132/124/138/165 由报告 §7 复现命令中的脚本直接打印，审计员按同款字符串重建逐一核对一致。
6. **Q1 计数**：T1/T2 各 9过3拒、T3 10过2拒、T4 2过10拒 → 30/18/48，与汇总一致。

### 结论

复跑成功且关键数字全部吻合。未发现数字伪造、估数顶替或漏答。任务卡/B5b 的一处 A 级假设被实测证伪并如实写入报告，属本任务核心产出而非问题。

---

## 三、工作量

| 项 | 要求 | 实测 | 判定 |
|---|---|---|---|
| 问题逐条有实测回答 | 三问（值封闭 / 谓词封闭 / 与 SHACL 对照） | Q1 4 种 valueSet × 12 值、Q2 4 类 shape × 4 节点、Q3 pySHACL 3 shape 逐节点 + 语法长度，均真实 stdout | ✓ |
| 复现命令完整可复制 | 含解释器路径、可逐条执行 | 第 7 节一行命令（node + venv python + 脚本 + 依赖），审计员全部复跑成功 | ✓ |
| 未决/风险如实 | 不隐瞒、标明来源 | 第 5 节：shex.js 1.0.0-alpha 版本风险、B5b A 级降级建议、字面量边界、内存/大图未测 | ✓ |
| 范围边界遵守 | 只验语义、不做性能测试 | 耗时仅量级参考，未做性能基准 | ✓ |

---

## 四、整改意见（PASS，非阻断）

1. **总耗时数字溯源**：报告 §3/§8 的"127.4ms（来自 run_output.txt）"与当前 run_output.txt 的 `119.962ms` 不一致（复跑 120.274ms）。建议改为 run_output.txt 现值，或注明该数字来自较早一次运行。毫秒级计时、非性能基准、不影响任何结论，故不构成判死，但应修正以保持"所有数字可溯源"的自查声明成立。
2. **rdf:type 注记改标注**：§1 Q2 的"实测均 nonconformant"改为"推断/规范推演（独立验证成立）"，或把该最小用例加入脚本并重捕 run_output，以便留痕可复现。
3. **B5b 回改落盘**：把 `[ ex:t1 ... ex:t9 ~ ]` 由 A 级改为"被原型实测证伪"，正确写法闭合 10 列表 `[ v1 ... v9 rel:other ]`（132 字符）回写 B5b K5 条，供图工程决策引用。
4. **shex.js 版本窗口**：报告 §5 已标 1.0.0-alpha.29；若后续改用发布版，建议重跑 Q1 语法探针与字面量用例确认行为不变。
