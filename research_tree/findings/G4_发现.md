# 叶子 G4 QUDT 量纲字段试点 + DLMF 补核 调研发现

> 任务 G4 / 日期 2026-08-14
> 快照：`research_tree/_sources/G4/`

## 0. 一句话结论

**QUDT 试点规格可定（C）**：`notation` / 含物理量纲的 `concept` 节点可挂 `quantityKind`、`unit`、`dimensionVector` 三类 IRI + `externalRef`，**不扩 M08 边词表**；SHACL 作可选只读形状（unit 出现时要求 quantityKind）。**DLMF 正文仍 未核**（WebFetch timeout + curl 无输出，与 V3 一致）——继续作 `externalRef.system=DLMF` 的**权威引用意图**，待网络可达后补快照。

## 1. Q1 QUDT 试点字段

| 节点 kind | 建议字段 | QUDT IRI 模式 | 等级 |
|---|---|---|---|
| notation | `quantityKind` | `http://qudt.org/vocab/quantitykind/{id}` | A QUDT + C |
| notation | `unit` | `http://qudt.org/vocab/unit/{id}` | A QUDT + C |
| notation | `dimensionVector` | `http://qudt.org/vocab/dimensionvector/{id}` | A QUDT + C |
| concept (physics) | 同上 + `externalRef` | 与 Wikidata/QUDT 并存 | A V3 + C |

**禁止**：把 QUDT 单位换算或量纲推理当作 M08 边或掌握结论（V3：零 pedagogical 边）。

## 2. Q2 DLMF

- 2026-08-14 重试失败 → **未核**（诚实声明）
- V3 已 A 级认定站点权威；用法 = `externalRef{system:"DLMF", iri:"https://dlmf.nist.gov/…", label}`

## 3. Q3 SHACL 衔接

- pySHACL = 只读审计层（P2 A）；试点 shape 示例：
  - `sh:property sh:path qudt:unit ; sh:minCount 0`
  - `sh:sparql` 规则：unit 存在 → quantityKind 存在（C）
- 不日常门禁；全库 522 节点试点写入留工程阶段

## 4. 关键发现

### K1. QUDT 提供 quantity kind / unit / dimension RDF 词表
- qudt.org — A

### K2. DLMF 抓取失败
- local attempt log — 未核

## 5. 判死自查

- 任务偏小叶；来源 QUDT A + V3 交叉 + G2 衔接
- **自评 pass=true**（DLMF 未核已声明，非阻断）
