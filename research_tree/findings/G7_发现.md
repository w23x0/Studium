# 叶子 G7 因果边锚点义务试点 调研发现

> 任务 G7 / 2026-08-14 / 基线 V7 K7–K9、06-M08 §2

## 0. 一句话结论

当前 M08 **522 节点图无因果专用 rel**（9+other 封闭）；若未来引入 **因果断言边**（SCM 骨架），锚点义务 **≥ 普通 pedagogical 边**：每条因果边须 **anchor{file,quote}** 指向「谁断言该因果结构/机制」的原文（Pearl K8：因果结构是模型员编码的断言）；**do(·)/反事实语义不进 RDF**，只存 `causalRole: associative|interventional|counterfactual` 注释 + 外部引擎。**禁止**用 PROV wasDerivedFrom 冒充干预（V7 Q3 A）。试点：**0 条因果边需迁移**；规则草案供模块组合写入。

## 1. 规则草案（C，挂 V7 A）

| 层级 | M08 承载 | 锚点义务 |
|---|---|---|
| 关联 P(y\|x) | `rel:requires`/`implies` + 统计证据 anchor | 与普通边同：quote 逐字 |
| 干预 P(y\|do(x)) | **不进 rel 词表**；`other`+`rel_note; causal=interventional` 或节点字段 | anchor 必须引「干预设定」出处（习题/实验描述） |
| 反事实 | 外部 SCM；RDF 只存 `externalRef` | anchor 引「反事实场景」原文 |

## 2. 与红线

- 因果边 **不自动写入**；候选池+审计
- 机械信号只定向：d-分离提示 ≠ 结案

## 3. 来源

- V7 Pearl K7–K9 A local
- 522 节点 rel 频率：无 causal token（P6 A）

## 4. pass=true
