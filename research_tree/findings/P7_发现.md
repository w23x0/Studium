# 叶子 P7 G2/G4 SHACL shapes 原型 调研发现

> 任务 P7 / 原型实测 A / 2026-08-14
> 脚本：`p7_validate.py`；shapes：`P7_shapes.ttl`

## 0. 一句话结论

**G2 JSONL 规则实测通过**：40 条 `other` 边 **100%** 带 `rel_note` 或 `label`。**P7 SHACL shapes 可跑**（522 节点 2.20s），全库 **conforms=false**、4130 条结果——主因仍是 P2 已知的 **edgeEvidence⊄anchorQuote**（锚点引用存在性语义待产品裁决），非 P7 回归；**QUDT 可选 shape 零触发**（当前库无 qudt 三元组）。定位维持：**只读审计层**。

## 1. 实测（A）

| 项 | 值 |
|---|---|
| nodes/edges | 522 / 1433 |
| other+note | 40/40 ✓ |
| pySHACL 耗时 | 2.20s |
| sh:results | 4130 |
| conforms | false（继承 P2 锚点语义分歧） |

## 2. P7 shapes 增量（C）

- 继承 P2 NodeShape（9+other 封闭 + 锚点/端点 SPARQL）
- 新增 `NotationQudtShape`：unit ⇒ quantityKind（G4 试点，当前 0 节点）

## 3. 判死自查 pass=true
