求导结果对：链式法则拿出来的 −2 就是那个关键系数。写的时候加个括号，写成 −2/(3 − 2x)。不加括号的话，−2/3−2x 会被读成"−2/3 减 2x"。前面几次卡住，多半也和这种省略有关，所以这次请把括号和除号都写全。

链式法则没问题，我们直接回到下落那道题。从 m·dv/dt = mg − kv 出发，每步一行，写出：

1. 分离变量的那一行（左边只有含 v 的式子和 dv，右边是 dt 和常数）
2. 积分之后的那一行（带上 C；系数可以参照刚才 ln|3 − 2x| 求导出来的那个数，反过来推）
3. 用 v(0) = 0 定出 C 的那一行
4. 最终的 v(t)
5. 让 t → ∞，看 v 趋于什么，和 v_T = mg/k 对照

你上一条消息被截断了，所以第 4、5 行请完整写一遍。
