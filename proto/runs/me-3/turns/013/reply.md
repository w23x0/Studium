你把上一轮的问题自己找出来了：第 135 行那个式子确实不对，这次改正后 mg 回到了该在的位置。我按 v = mg(1 − e^(−kt/m))/k 来读你这一条（消息在 "mg/" 处被截断了）。这就是正确的形式，t → ∞ 那一步放到下面一起做。

打公式确实累。下面这题可以用最省事的写法，比如 e 的指数写成 exp(-kt/m)，不用 LaTeX，不用排版。只有一个要求：每一步单独一行，不用省略号。这样我能看清你每一步是怎么走的。

最后一道，从头到尾自己做：

> 质量为 m 的小船在水面上从静止出发，受一个恒定的向前牵引力 F₀，以及水的阻力 −kv（k > 0，向前为正）。
> (1) 求最终速度 v_T。
> (2) 求 v(t)。
> (3) 让 t → ∞，检查 v(t) 是否趋于 v_T。

请依次写出这几行：
1. 运动方程
2. v_T
3. 分离变量那一行
4. 积分之后那一行（带 C）
5. 用 v(0) = 0 定出常数那一行
6. 最终的 v(t)
7. t → ∞ 时 v 趋于什么

这题做完，这个闭环就可以收尾了。
