你说的没错——我们要研究的集合确实是"所有被 $T$ 映射成 0 的向量"。这一点你已经确认了。

不过这还不是我请你做的那一步。我要你做的是**动手写**：

(1) 把"$T$ 保持加法和数乘"写成两个等式（用 $T, u, v, c$ 这些符号）；
(2) 从这两个等式出发，推导出 $T(0)$ 必须等于什么。

现在请你试着写出这两个等式和推导过程。

【练习条件】
任务：写出线性变换保持加法、数乘的两个符号等式，并推导 T(0)=0。
完成条件：等式形式正确（T(u+v)=T(u)+T(v)，T(cv)=cT(v)）；推导过程能说明用了哪一条等式、如何代入（例如令 c=0 代入数乘等式，或用 u+0=u 结合加法等式）。
独立程度：学习者独立尝试期间不主动介入；卡住到确实无从下手时，只给一个最小起点提示（如"试着在其中一个等式里让某个变量取 0"），不直接给出推导结果。
验证意图：区分"没读懂任务/未注意到任务"（提示后仍不动手或反问要做什么）、"不会符号化或推导"（写错等式，或等式对但推不出 T(0)=0，需进一步定位卡在列式还是代入）、"主动确认后能顺利完成"（学习者说明想先确认概念，随后顺利完成任务）三种解释。
