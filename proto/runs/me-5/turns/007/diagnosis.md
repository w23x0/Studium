[观测]
- `transcript.md:68`：学习者指出 m·dv/dt = −kx 里同时有 v(t) 和 x(t)，所以"解不出来"。他不确定该说"两个变量"还是"三个变量"。提示条件：多次提示（`transcript.md:63` 给了具体引导问题"出现了哪几个变量，为什么没法分到两边"）。
- `transcript.md:68`：第 2 问的结果写成 mv² = −kx²。没有积分常数，也没有用上题目给的 v(0) = v₀，尽管题面 `transcript.md:64` 明确要求"用初始条件定常数"。提示条件：轻微提示。
- `transcript.md:68`：学习者说换成 v·dv/dx"把变量 t 消去了，剩两个，就可以解出 v(x)"。提示条件：多次提示（`transcript.md:59` 指向"没有 t"，`transcript.md:66` 直接问"换来了什么"）。
- 学习者在前几处都写了积分常数：`transcript.md:14`（C，并定出 C = −1）、`transcript.md:43`（C₁、C₂，并说出其物理意义）、`transcript.md:56`（+C）。

[推断]
- 要点④在引导下已经有初步证据：推导见 `transcript.md:56`，用途"消去 t，得到 v 与 x 的关系"见 `transcript.md:68`。
- 学习者说了"得到 v(x)"，但还没说出什么时候选这种写法（力只含 x，或只问速度与位移的关系）。
- 要点③里"F 只含 v"这一类还完全没有碰到。
- 本轮漏写常数，但他此前三次都保留了常数，所以更可能是笔下省略，不像是不懂常数的作用。不过这一次初始条件确实没有被用上。

[替代解释]
- 关于漏常数，有三种可能：
  (a) 写得匆忙，简写时默认 C = 0；
  (b) 把"初始位置 x = 0"误当成"常数为 0"，混淆了 x = 0 和 C = 0；
  (c) 真的忽略了初始条件的作用。
  这一点值得追。代价很低，只需让他把 x = 0 代回去自查。它又直接关系到要点②和主张 3 的定常数步骤。
- "两个还是三个变量"只是表述上的犹豫。学习者对"x 是另一个未知函数、无法分离"的判断是对的，简短回应即可，不必追。

[未满足项]
- 主张 1：已有证据（`transcript.md:14`），不再索取。
- 主张 2：
  - 要点①：在引导下出现过（二阶方程见 `transcript.md:32`，常力为特例见 `transcript.md:43`），还没有无提示的连贯讲述。
  - 要点②：他认出了两个常数就是初速度和初位置，但"力相同、初态不同，运动就不同"还没有明确讲出。本轮又漏了初始条件。
  - 要点③：F 只含 v 的解法完全缺失，三类选法也还没有整体讲过。
  - 要点④：在引导下已给出，还需要在整体讲述里无提示再现，并说出适用场合。
  - 总体：还缺一次无提示、连贯的整段讲述。
- 主张 3：全部未做。

[改线依据] 无

[下一闭环候选] 无

[策略与强度]
- 先用一个最小自查动作处理漏常数：让他把 x = 0 代回去看是否得到 v₀，不直接指出错在哪。
- 简短回应"两个还是三个变量"的疑问，并确认"消去 t"这一点说得对。
- 零件基本到位了，所以请他整段复述主张 2 作为收尾尝试。复述里要主动推一推没做过的"F 只含 v"这一类。
- 信息量低，提示强度低（只列出要讲的三个问题，不列要点），挑战中等，节奏推进。
