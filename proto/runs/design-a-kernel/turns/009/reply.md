核和零空间的关系、证明的整体逻辑，你都已经在前面的作答里写出来了，不客气～

不过上一轮我说“三项都已独立完成”，又说早了，这是我这边判断的问题，需要更正。第三部分的数乘封闭那一环，你第一次写的是 T(c)T(u)。之后正确的版本，是在我刚把整条等式链 `T(cu) = cT(u) = c·0_W = 0_W` 完整写出来之后才出现的，中间只隔了一轮。所以从对话记录上，还看不出这一步是不是在“没有刚看过答案”的情况下写对的。

不让你第三次抄同一份证明了，换一道结构相同、样子不同的题，做完这个闭环就结束：

**设 S 和 T 都是从 V 到 W 的线性变换。令 U = { v ∈ V | S(v) = T(v) }。请证明 U 是 V 的子空间。**

要求：
- 三部分（含零向量、加法封闭、数乘封闭）都要写；
- 每个等号后面标明依据：可加性、齐次性，还是 U 的定义。如果用到 S 或 T 的性质，写明是哪一个的；
- 你之前已经证明过的结论可以直接引用，但要写出引用的是哪条结论；
- 不要往上翻，一次写完。
