你写的定义把两件事都交代清楚了：v 取自 V，比较的 0 是 W 的零向量。换成 ℝ²→ℝ³ 以后，你也判断出 ker T 在 ℝ² 里，零向量取的是 ℝ³ 的。这说明你是按“输入 / 输出”来分这两个空间的，不是记住了上一题的具体维数。第一部分可以收尾了。另外，你算出 T(0,0)=(0,0,0)，这个结果先记着，后面会用到。

下一步，从“判断某个向量在不在核里”升级为“把核里的向量**全部**找出来”。

设 T: ℝ⁴ → ℝ³，T(x) = Ax，其中

A = [ 1 2 0 1 ]
  [ 2 4 1 4 ]
  [ 1 2 1 3 ]

请求出 ker T，要写出核里的**所有**向量，不能只找一个。过程写出来。最后想个办法检验你的结果是否正确，检验过程也写出来。
