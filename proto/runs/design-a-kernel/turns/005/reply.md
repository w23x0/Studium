你的直觉是对的：**T(c) 在这里没有意义。** T 是从 V 到 W 的映射，只能作用在 V 里的向量上。c 是一个标量，不在 V 里，所以"T(c)"根本不是一个定义过的东西，"T(c)·0" 也就无从谈起。

问题只出在第三步的第一个等号上，其余两步的推导和依据标注都没问题。请回到题目给的线性变换定义（T(u+v)=T(u)+T(v)，T(cu)=cT(u)），看看 T(cu) 按齐次性应该等于什么，然后**把第三步重写一遍**。最后得到 0 的那一步，请写明这个 0 是哪个空间的零向量。
