好的，我们从一个具体例子开始，先动手算，再给这个概念起名字。

设变换 T 把 ℝ³ 中的向量送到 ℝ² 中，规则是 T(x) = Ax，其中

A = [ 1 0  2 ]
  [ 0 1 −1 ]

请你分别计算下面三个向量的像，把乘法过程写出来：

- v₁ = (−2, 1, 1)
- v₂ = (1, 1, 1)
- v₃ = (4, −2, −2)

算完后回答两个小问题：
1. 哪几个向量被 T 送到了零向量？
2. 这里说的“零向量”是哪个空间里的零向量？被送过去的那些向量本身又属于哪个空间？
