这道变式题三部分都写对了，而且都是独立完成的。

- **数乘封闭**：你在 S 和 T 两侧各用了一次齐次性，S(cu)=c·S(u)，T(cu)=c·T(u)，再用 S(u)=T(u) 两边同乘 c 把两侧接起来。S、T 始终只作用在 V 里的向量上，也标明了用的是哪一个变换的齐次性。这一步的结构和 ker T 那题不一样，没法照搬之前的等式链，所以它能说明齐次性的用法你是真正掌握了。第一次写出的 T(c) 可以确认只是笔误。
- **含零向量**：你把"线性变换把 0_V 映到 0_W"当作已证结论直接调用，并写明了引用来源。这正是证明里复用结论的规范做法。

你在最后说"就是把 T(v)=0_W 换成 S(v)=T(v)，逻辑是对称的"。这个观察是对的：两题都按子空间的三条逐一验证，每条落到可加性或齐次性上，只是"要保持的那个等式"换了。

到这里，核的定义与判断、求完整的核并检验、子空间证明这三项都已经有你独立完成的作答。这个闭环可以结束了。你只需回一句"好"确认结束；如果临时又想到什么问题，也可以直接问。
