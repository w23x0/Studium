[观测]
- `transcript.md:21-35`（独立）：三次矩阵乘法都按行写出了逐项乘积和求和过程，T(v₁)=(0,0)、T(v₂)=(3,0)、T(v₃)=(0,0)，全部正确，没有漏算或符号错误。
- `transcript.md:39`（独立）：正确指出 v₁、v₃ 被送到零向量，v₂ 没有，并写出了 v₂ 的像 (3,0)。
- `transcript.md:41`（轻微提示：题目第 2 问专门问了“哪个空间”，把注意力引到了这一点上，但没有给答案）：答出零向量是 ℝ² 的零向量，v₁、v₃ 属于 ℝ³。两条都给了理由：“T 的像都落在 ℝ² 里”和“它们是 T 的输入”。
- 按 `transcript.md:18` 的约定，这道题只作引入，不计入主张 1 的证据。

[推断]
- 矩阵乘法这一前置可靠，计算又快又准。
- 学习者能从“输入 / 输出”的角度区分定义域和陪域，这正是核的定义里最容易混的一点。在具体例子里这一点已经站住了，讲定义时不必在这里多花力气。
- 还不知道学习者能不能把具体例子里的观察，概括成一般的 T: V→W 下的集合定义。

[替代解释]
- 第 41 行的区分可能只是记住了“这道题是 ℝ³ 进、ℝ² 出”，还没抽象成“核在定义域、零向量取陪域的”。区分方法很简单：下一题把维数反过来（ℝ²→ℝ³）。这个检验本来就包含在主张 1 的判断题里，不额外占学习者的时间，值得做。
- 学习者口中的“像”只是“T 的输出”的日常说法。本闭环不讲像（值域），这里不追究，也不借这个词展开。

[未满足项]
- 主张 1：还缺无提示写出的定义（要明确“定义域 V 中的向量”和“W 的零向量”两点），也缺在一个新的 T 上逐个计算、判断候选向量的证据。
- 主张 2：全部未开始（转化为 Ax=0、消元、用自由变量写解集、代回验证）。
- 主张 3：全部未开始（证 T(0)=0、对加法封闭、对数乘封闭，并标明每一步用的是可加性还是齐次性）。

[改线依据]
无。

[策略与强度]
- 策略：学习者从例子里已经自发得出定义的两个要点，本轮只告诉他“核”这个名字，由他自己把定义概括到一般的 T: V→W。紧接着在一个维数反过来的新变换上判断三个候选向量，一次拿齐主张 1 的证据。候选向量里放一个零向量，为主张 3 埋个伏笔，但本轮不讲。
- 信息量低（只给名字和记号），提示强度无，挑战程度中等偏低。学习者计算熟练，节奏可以快一些：定义和判断合在同一个动作里完成。
