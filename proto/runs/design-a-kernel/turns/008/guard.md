**主张 1：有**
- 证据位置：`transcript.md:68-70`（定义）、`transcript.md:74-92`（逐个计算与判断）、`transcript.md:191`（延迟复现定义）
- 条件：独立
  - 68-70 行写定义之前，系统在 44 行已用文字描述"被 T 送到零向量的向量构成的集合"，48 行又要求交代两个空间，所以这一处只能算轻微提示。
  - 191 行是在证明题中自发写出 `ker T = { v ∈ V | T(v) = 0_W }`。当时题干没有要求写定义，上一轮系统也没有复述定义，距 93 行的系统复述约 100 行，中间隔了一整道求核题。这是无提示的独立复现，明确包含"v ∈ V"和"0_W"两点。
  - 两个空间的区分，学习者在命名之前就已独立做出（41 行）。
  - 74-90 行对 B 的三次乘法过程可见，结果全对，判断 u₁、u₃ 在核中、u₂ 不在，全部正确。92 行指出 ker T ⊆ ℝ²，零向量取 ℝ³ 的。
- 证据强度：含变式、含延迟。变式是维数反向（ℝ²→ℝ³），延迟是 191 行的复现。
- 缺口：无

**主张 2：有**
- 证据位置：`transcript.md:105`（转化为 Ax=0）、`transcript.md:114-131`（消元）、`transcript.md:135-146`（自由变量与完整解集）、`transcript.md:154-166`（代回验证）
- 条件：独立
  - 题干（103 行）没有提到 Ax=0，也没有给检验方法。学习者自行把求核转化为解 Ax=0。
  - 化到 [1 2 0 1; 0 0 1 2; 0 0 0 0]，正确。
  - 自由变量取 x₂=s、x₄=t，得到解集 (−2s−t, s, −2t, t)，完整。
  - 代回两个非零向量 (−2,1,0,0) 和 (−1,0,−2,1)，逐行算出结果为 0。
  - 168 行关于"是否找全"的疑问属于学习者追问，不影响已独立写出的完整解集。
- 证据强度：含变式。矩阵是会话中新出现的 3×4 非方阵，有两个自由变量。
- 缺口：无

**主张 3：无**
- 证据位置：`transcript.md:193-211`（首次证明，含零向量与加法封闭正确）、`transcript.md:219-220`（首次数乘封闭错误）、`transcript.md:241-243`（提示后重写）、`transcript.md:286-330`（再次完整书写）
- 条件：数乘封闭一环为答案已暴露。含零向量、加法封闭两环为独立。
  - 含零向量：197-200 行用齐次性推出 T(0_V)=0_W，290-304 行又独立换用可加性推出，这条路会话中此前没有出现过。推导来自线性性，没有只说"显然"，这一环达标。
  - 加法封闭：208 行、314 行都标明了可加性，独立完成，这一环达标。
  - 数乘封闭：首次独立书写错误，写成 `T(cu) = T(c)T(u)`（219 行）。229-231 行系统指出 T(c) 无定义并提示回看定义后，才在 241-243 行写对。随后 250 行系统又把完整链条逐字复述了一遍（"T(cu) = cT(u) 用的是齐次性，代入 T(u) = 0_W 得到 c·0_W，再用'标量乘零向量仍是零向量'得到 0_W"）。
  - 280 行后的重写与 250 行的复述几乎逐字一致，间隔只有一轮问答。"不要往上翻"无法从对话中核实，因此这一环的正确写法始终是在提示和答案暴露之后给出的。
- 证据强度：仅当场
- 缺口：数乘封闭一环缺少一次在答案未刚被展示的条件下独立写对的证据，条件不达标。需要以下任一种：
  - 在间隔其他内容之后，再无提示地完整写一次"ker T 是 V 的子空间"的证明；
  - 做一道同结构的变式证明，例如对线性变换 S、T: V→W，证明 {v ∈ V | S(v) = T(v)} 是 V 的子空间，其中数乘封闭一环要独立、正确地使用齐次性，并标明依据。

【守卫结论】证据不足：3
