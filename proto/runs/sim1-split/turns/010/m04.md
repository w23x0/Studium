[观测]
- 见过“零空间”，能描述为“解 Ax=0 找出所有解”；对“核”说不清，只猜是“线性变换里类似的东西”。`transcript.md:8`，独立。
- 正确解出 A=[[1,2],[2,4]] 的解集 t(-2,1)，三项检验结论都对。加法、数乘封闭的理由是“参数形式不变”，加法一项带“我猜是的”。方程写成“1×1+2×2=0”，属于记号错误，但推导按 x1+2x2=0 进行。`transcript.md:27-33`，独立。
- 正确给出 T(x)=Ax 的定义域 R²、陪域 R²，空格填 0。`transcript.md:47-49`，轻微提示（题干已给出“满足 T(x)=____的所有 x”的框架）。
- 自发提出“核是不是就是让 T(x)=0 的那些 x”，并追问它与零空间是否只是碰巧一致。`transcript.md:51`，独立（但紧接在上一题的填空框架之后）。
- 论证“Ax=0 的解”与“T(x)=0 的解”永远相同，理由是“T 就是 Ax”。猜测核是更一般的概念，零空间是核在矩阵情形下的叫法。另说“零向量在任何线性变换下（我猜）应该都会被变成零向量，所以核至少不会是空的”。`transcript.md:62-66`，独立，自标为猜测。
- 加法封闭：写出 T(u+v)=T(u)+T(v)=0+0=0，并标注用的是可加性、没用数乘那条。`transcript.md:82-91`，答案已暴露：题干 `transcript.md:76` 已点名“可加性 T(u+v)=T(u)+T(v)”。
- 数乘封闭：写出 T(cu)=cT(u)=c·0=0，并标注用的是数乘性质。题干未点名性质。其中“c·0=0”一步没有给依据。`transcript.md:102-112`，独立（但结构与上一题相同）。
- 零向量：用 T(0)=T(0·u)=0·T(u)=0 推出，并标注用数乘性质、取 c=0。u 的来源先写“比如就用核里那个 u，或者随便什么向量都行”，随后自发发问“u 是不是随便取都行？”。`transcript.md:125-133`。题干 `transcript.md:117` 同时列出了两条性质可用，未指定用哪条，属轻微提示。
- 识别出 0=0·u 与 0·T(u)=0 是同一事实“0·w=0 对任意向量 w”，分别以 w=u、w=T(u) 代入，理由是“T(u)本身也是个向量”。自发提出这条事实属于向量空间本身，与 T 是否线性无关，语气为“好像……是不是”。没有明确说出两处分别位于定义域和陪域。`transcript.md:145-152`，独立（问题本身已提示“是不是同一个事实”）。
- 从公理推导 0·w=0：由 (0+0)w=0w 和分配律得 0w=0w+0w，两边加 -(0w) 得 0=0w。自列所用公理为标量 0+0=0、数乘对标量加法的分配律、加法逆元。右侧化简实际还用到加法结合律和零向量公理（v+0=v），她没有列出。结论是“全程没用到 T”。`transcript.md:163-185`，独立。

[推断]
- 她能在抽象设定下，用线性变换定义中的某一条性质写出单步等式链（数乘封闭、零向量两步是独立完成的）。加法封闭能否独立完成，目前无法判断。
- 她对“等式需要依据”已有初步意识：能区分线性变换赋予的性质与向量空间自带的性质，并能从公理推出 0·w=0。推导骨架正确，但对“用了哪些公理”的记录不完整，漏了结合律和加法单位元。
- 卡点可能在于：把陪域中的“c·0=0”（标量乘零向量）当作不需要依据的显然事实，而这是另一条事实，不是 0·w=0。同时尚未显式区分定义域零向量、陪域零向量和标量 0。
- 三步目前仍是分散作答，没有出现把三步组织成“核是子空间”完整证明的输出。

[替代解释]
- 数乘封闭的独立性：可能是真正从定义中选对了性质，也可能只是把上一题模板里的“可加性”换成“数乘”。两者形式高度对称，本轮证据无法区分。
- `transcript.md:133` 的“u 随便取”：可能是对“从核里取 u 来证明核非空”这一循环的警觉；也可能只是觉得任取即可、表述上的自然延伸，并未意识到若限定取自核内会预设核非空。她先写的就是“核里那个 u”，这一点支持后者，但证据不足以判定。
- 能从公理推出 0·w=0，可能是理解了依据，也可能是在向量空间一章见过这一标准证明后复现。复现与理解在单次输出上不可区分。
- `transcript.md:64` 的“零向量被变成零向量”：可能来自矩阵直觉（A·0=0）的推广，也可能此前听过。之后的一般证明（`transcript.md:127`）说明她现在能推出这一点，但无法反推该猜测当时的来源。

[未满足项]
- 主张1：没有任何关于“定义回答什么问题”的动机性表述。`transcript.md:64` 只说明定义“说得通”“不会是空的”，不是动机。“T 非线性时是否还有好性质”未被问及，无证据。
- 主张2：
  - 缺一次由她本人整合的完整证明，要求三步齐全、各步标注依据。
  - 加法封闭缺一次不点名性质下的独立完成（现有证据被题干污染）。
  - 数乘步中“c·0=0”的依据未给出，也未辨析它与 0·w=0 是否为同一事实。
  - 零向量步中 u 的取法未澄清：应取定义域任意向量或直接取 0，而非核内向量，这里存在循环风险。
  - 三种零的区分未显式出现。
  - 公理引用不完整（漏结合律、加法单位元），属次要项。
- 主张3：完全无交互，无证据。
- 主张4：无变式题。`transcript.md:45-51` 的识别是在系统给出 T(x)=____ 框架后发生的，且题目本身就是 Ax=0，不构成“题目不出现核字时自行识别”的证据。

[需要验证]
- 针对 c·0=0：她能否说出这是“标量乘零向量”，与“零标量乘向量”是不同事实，并给出依据（例如 c·0=c(0+0)=c·0+c·0 的推导）。用于区分“记住模板”与“理解依据”。
- 针对 u 的取法：追问“如果只允许从核里取 u，这个证明还成立吗？”。看她能否指出这会预设核非空、构成循环，并改为取 u=0 或定义域中的任意向量。
- 针对加法封闭：换一个不点名性质的情境独立重做，排除模仿格式的解释。
- 针对性质选择：给出一个只满足可加性的 T，问数乘封闭是否仍能推出。检验她是按依据选性质，还是照抄题干措辞。
- 针对整合：请她一次性写出“ker T 是定义域的子空间”的完整证明，看三步顺序、依据标注，以及是否说明 ker T⊆V。
- 针对三种零：观察她在整合证明中能否标出 T(0)=0 两侧的零分别属于哪个空间。
- 主张1、3、4：各需至少一次无提示的探底输出。

[证据限制]
- 【练习条件】行是否对学习者可见，transcript 中无法判断。若可见，`transcript.md:160` 已写出分配律，0·w=0 推导的独立性要下调。
- 无法判断她在向量空间一章是否已见过 0·w=0 的证明，因此无法区分当场推导与复现。
- `transcript.md:185` 的“感觉这个证明……跟 T 没关系”是自述，不能作为她理解“向量空间性质 vs 线性变换性质”这一区分的证据，只能作为待验证线索。
- 当前策略的四项验证安排（①~④）本轮只部分推进了 ① 中 0·w=0 这一半，其余尚未出现相关输出。
