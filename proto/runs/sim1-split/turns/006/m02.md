你这一步推导是对的：T(u+v) = T(u)+T(v) = 0+0 = 0，所以 u+v 也在核里,用到的正是可加性。

现在只改一个条件,别的都不变。设 u 仍然是核里的向量,也就是 T(u)=0;再取任意一个实数 c(标量)。T 依然只知道是线性的,不知道别的信息。

**请你推导:cu 是不是也在核里?也就是说,T(cu) 是不是等于 0?**

写出你的完整推导过程,包括每一步依据的是什么。

【练习条件】
任务:在抽象设定下(u ∈ ker T,T 线性,c 为任意标量),独立推导 T(cu)=0,从而说明 cu ∈ ker T。不点名应使用哪条线性变换性质(齐次性/数乘性质),由学习者自行选择并写出。
完成条件:学习者写出完整推导链(至少包含 T(cu)=cT(u) 这一步,并代入 T(u)=0 得到 T(cu)=c·0=0),且能说明这一步用到了线性变换定义中的哪一条性质。
独立程度:不给提示、不点名性质,完全由学习者自主完成;若卡住,先追问她卡在哪一步,不直接给出该用哪条性质。
验证意图:区分"能自主从线性变换定义中选出数乘/齐次性这条性质并独立推导"与"只是模仿上一题(83-91行)的结构、把可加性替换成猜测的其他性质、或无法独立定位该用哪条性质"。若她需要提示才能推进,记录提示强度,不算独立完成。
