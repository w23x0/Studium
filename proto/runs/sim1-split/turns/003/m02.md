你解出的解集 t(-2,1) 是对的，三条检查的结论也都对。

有个写法上的小问题你可以留意一下：你写的是"1×1+2×2=0"，但这里的 x1、x2 是未知数，不是具体的数字，所以方程本来应该写成 x1+2x2=0。从你后面推出 x1=-2x2 这一步看，你其实是按 x1+2x2=0 算的，估计只是落笔时手误，以后写的时候注意一下就行。

现在往前走一步。你手里这个矩阵 A，其实还能换一个身份看待。定义一个映射：

$$T:\mathbb{R}^2\to\mathbb{R}^2,\quad T(x)=Ax$$

**请回答三件事：**
1. **这个 T 的定义域是什么？**
2. **它的到达空间（陪域）是什么？**
3. **你刚才解方程组 Ax=0 找到的那些解 x，如果改用 T 来描述，应该说成"满足 T(x)=____的所有 x"——空格处该填什么？**

【练习条件】
任务：给出映射 T(x)=Ax 的定义域、陪域，并把之前解出的 Ax=0 的解集用 T 的语言重新表述（即"满足 T(x)=？的所有 x"）。
完成条件：三处回答均正确——定义域为 R²，陪域为 R²，空格处填零向量（0 或 (0,0)）。
独立程度：独立完成，不给提示，卡住再介入。
验证意图：区分学习者是"尚未建立矩阵与线性变换之间的对应"，还是"只是此前表达不够准确但其实已经理解"，为下一步引出核的定义动机做前置确认。
