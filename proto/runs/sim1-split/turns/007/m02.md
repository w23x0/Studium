你这一步推导是对的：T(cu) = cT(u) = c·0 = 0，用到的正是数乘性质，逻辑链完整。

现在我们把核作为子空间的三个条件过一遍：加法封闭、数乘封闭，你都推过了。还剩第三个条件——**零向量本身在不在核里**，也就是 T(0)=0 是否成立。你之前在别的情境下猜过这一点，但还没有真正推导过。

这次不用矩阵，只知道一件事：**T 是一个线性变换**（定义域和陪域是什么向量空间不重要，可加性和数乘性质你都可以用）。

**请你独立推导：T(0)=0。写出完整过程，并说明每一步用到了线性变换定义里的哪一条性质。**

不用参考之前矩阵的例子，就从"T 是线性的"这一句话出发去想。

【练习条件】
任务：证明对任意线性变换 T，T(0)=0（0 为定义域的零向量），只能使用线性变换定义（可加性 T(u+v)=T(u)+T(v)、数乘性质 T(cu)=cT(u)），不得借助矩阵或具体例子。
完成条件：写出完整推导链（至少一种合法路径，如 T(0)=T(0+0)=2T(0)⟹T(0)=0，或 T(0)=T(0·u)=0·T(u)=0），并标注每步依据的性质。
独立程度：不点名用哪条性质，不给模板，先让学习者尝试，卡住后再提示。
验证意图：区分"矩阵直觉/事后猜测"（如 transcript:64 的"我猜"）与"能否独立完成一般性证明"；同时为后续追问"该假说来源"（自己推理 vs 事后合理化 vs 此前听过）做铺垫。
