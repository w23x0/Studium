你的计算都对，第 1 步从方程组到几何图形的描述也很清楚。顺带提一个书写上的小点：第 31 行写的是“T(u+w) = u+w = (−1,1)”，你想表达的其实是“u+w = (−1,1)”。T(u+w) 和 u+w 不是同一个东西，这个等号要去掉。

再看你的直觉：“T 把 w 吃掉了，变成 0 了，所以加不加它结果都一样”。这个方向是对的，下面把它变成一个严格的说法。

**请回答：**
设 u 是 ℝ² 中**任意**一个向量，w 是第 1 步那条直线上**任意**一个向量（也就是 T(w)=0）。
不要代入具体数字，只用线性变换的定义，推出 T(u+w) = T(u)。

写的时候请注明：每一个等号用到了线性变换定义里的哪一条性质。
