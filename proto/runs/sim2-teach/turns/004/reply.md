你的推导里，处理 −v 那一步主动拆成了 (−1)·v 并用上"保持数乘"，每个等号的依据也都分清了。小补充：第 77 行只用到减法的定义（a − b 就是 a + (−b)），用不到交换律。

你最后那段总结，正好把两个方向合在了一起：
- 差是"那条直线上的向量" ⇒ T 分不出这两个输入；
- T 分不出两个输入 ⇒ 它们的差一定在"那条直线"上。

现在正式给它起名字。对一般的线性变换 T: V → W，把所有被送到零向量的向量组成的集合叫作 T 的**核**，记作

  ker T = { x ∈ V : T(x) = 0 }。

在上面的例子里，ker T 就是那条直线 { t(−2, 1) }。再回头看你刚才的两个推导：它们只用了线性变换的定义，没有用到矩阵 A 的具体数字。所以你那句总结对**任何**线性变换都成立，ker T 恰好记录了 T 把哪些输入"看成一样"。

**请回答：**
设 S: V → W 是一个线性变换，并且它的核**只含零向量**，即 ker S = {0}。
现在已知 S(u) = S(v)，你能推出关于 u 和 v 的什么结论？请写出推导（可以直接引用你刚证过的结论，写明引用的是哪一个）。然后用一句话说说：这意味着 S 作为一个映射有什么特点？
