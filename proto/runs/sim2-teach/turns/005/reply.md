对，“一对一”在这里就叫**单射**，这个词用得完全合适。单射的定义正是：只要 S(u)=S(v)，就一定有 u=v。这和你第 112 行推出的结论一字不差。所以你刚才证明的是：

> 若 ker S = {0}，则 S 是单射。

你的推理链很干净：先引用上一轮的结论得到 S(u−v)=0，再用“核里只有 0”这个条件落到 u=v。关键在于你用的是一般的 S，已经不依赖“那条直线”了。

接下来看核本身是一个什么样的集合。第一个例子里，你注意到 ker T 是一条**过原点**的直线。现在把它推广到一般情形。

**请证明：**
设 T: V → W 是任意一个线性变换。证明 ker T 是 V 的一个子空间。

要求和前几次一样：
- 按子空间的判定，把需要验证的每一条都写出来；
- 每个等号注明依据：是线性变换定义的哪一条（保持加法 / 保持数乘），还是向量空间本身的性质。
- 如果某一步你觉得“显然”，也请想一想它到底靠的是什么，再写下来。
