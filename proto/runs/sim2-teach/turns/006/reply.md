你的证明三步齐全，而且 T(0)=0 是自己推出来的，没有当成显然。这个写法是对的，而且是很标准的做法。另有一种常见写法只用“保持加法”，从 T(0)=T(0+0) 出发，你以后可以自己试着补完。两种都可以。

有一处标注还没写全。第 139 行其实有三个等号，你只标了中间那个（保持数乘）。另外：

- “0·0”里前一个 0 是标量，后一个是零向量，写的时候最好区分开，比如写成 0·**0**。
- 第一个等号 **0** = 0·**0** 和最后一个等号 0·T(**0**) = **0** 各靠什么？

请用一句话补上这两个依据。

然后进入正题。你一路追踪了“每一步用了线性的哪一条”，现在反过来看，如果映射**不是**线性的，会怎样。考虑

  g: ℝ² → ℝ，g(x₁, x₂) = x₁² − x₂

令 Z = { x ∈ ℝ² : g(x) = 0 }，也就是被 g 送到 0 的全体向量。

**请回答：**
1. Z 在平面上是什么图形？它是 ℝ² 的子空间吗？零向量、加法封闭、数乘封闭三条逐条检查，不满足的给出具体反例。
2. 回看你刚写的 ker T 子空间证明：对 g 来说，是哪一个等号不再成立？
