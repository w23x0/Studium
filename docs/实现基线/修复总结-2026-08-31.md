# 实现基线问题修复总结（2026-08-31）

## 本次修改范围

本次只处理由现有仓库原文直接证明的文档链接和状态错误。审查开始时运行 `git status --short --branch`，结果为 `## main...origin/main`，工作树没有用户已有修改需要覆盖或回滚。

用户指定的以下输入材料在当前工作树和 Git 历史中均不存在：

- `docs/实现基线/模块关系提取.md`
- `docs/实现基线/模块关系提取.json`
- `docs/实现基线/模块检查点.md`
- `docs/实现基线/模块检查点.json`
- `docs/实现基线/模块问题清单.md`

因此本总结中的问题编号是本次审查的临时编号，不替代缺失问题清单中的原编号，也不据此重建关系或检查点产物。

## 修改文件

修改前：

- `README.md`
- `docs/自学方法调研/README.md`

修改后：

- `README.md`
- `docs/自学方法调研/README.md`
- `docs/_archive/深度调研树/README.md`
- `docs/实现基线/修复总结-2026-08-31.md`（本文件）

## 已修复问题

### AUTO-LINK-001：根 README 的知识组织索引失效

- 修改前状态：`README.md:13` 链接到不存在的 `docs/知识组织调研/README.md`。
- 修改后状态：改为直接链接现存的 `research_tree/synthesis/知识载体选型决策点_LLM时代v1_K线终稿.md` 和 `存储选型决策点_v1.3.md`。
- 修复依据：`docs/_archive/README.md:12` 明确记录 `知识组织调研/README.md` 已删除，且其内容已由 `research_tree` 的存储选型结论替代；目标文件在当前仓库存在。
- 可自动修复原因：这是已删除路径到现存、明确承接其用途的索引文件的机械替换，不改变模块职责或产品语义。

### AUTO-LINK-002：根 README 的调研引擎路径及状态过期

- 修改前状态：`README.md:16` 链接到不存在的 `docs/深度调研树/_engine/ENGINE.md`，并标为“现行 v3”。
- 修改后状态：链接改为现存的 `docs/_archive/深度调研树/README.md`，状态改为“已归档”，并保留 `research_tree/` 运行时产出入口。
- 修复依据：`docs/_archive/README.md:27-31` 记录深度调研树全部归档且引擎休眠；`docs/_archive/深度调研树/README.md:39-43` 记录主线已到叶归档、下一阶段为模块组合。
- 可自动修复原因：归档记录直接给出文件位置和状态，属于明确的文档状态纠正。

### AUTO-LINK-003：自学方法 README 引用已删除资料索引

- 修改前状态：`docs/自学方法调研/README.md:29` 链接到已删除的 `../图工程调研/02-资料索引.md`。
- 修改后状态：改为不带失效链接的本项目通用证据分层说明；本节自己的证据等级表保持不变。
- 修复依据：`docs/_archive/README.md:13` 明确记录 `图工程调研/02-资料索引.md` 已删除，原因是外部链接清单无独立价值。
- 可自动修复原因：删除失效引用并保留当前章节已有的证据分层内容，不引入新的产品或研究结论。

### AUTO-LINK-004：归档调研树 README 的运行时索引错误

- 修改前状态：`docs/_archive/深度调研树/README.md:16-26` 使用错误层级的 `../../research_tree/`，且链接到已经删除的 `state/`、`tasks/`、`_sources/`、`logs/`。
- 修改后状态：现存的 `findings/`、`audit/`、`synthesis/` 改用正确的 `../../../research_tree/`；已删除目录改为带日期的状态说明，并补充现存 `总目录.md` 链接。
- 修复依据：`research_tree/总目录.md:1-4, 43-55` 明确记录 2026-08-16 删除的运行时目录和当前保留结构；`docs/_archive/README.md:31` 确认运行时产出仍保留在 `research_tree/`。
- 可自动修复原因：这是按仓库现状校正相对路径和已删除状态，不改变归档内容、运行时语义或模块关系。

## 验证方式和结果

- 活动文档链接扫描：排除 `docs/_archive/` 后扫描全部 Markdown/JSON 的相对 Markdown 链接，结果 `COUNT=0` 个失效链接；归档调研树 README 的现存链接单独复核通过，已删除条目不再伪装成链接。
- 目标文件存在性：上述两个新链接的目标文件均通过 `Test-Path` 检查。
- JSON 结构：仓库现有 JSON 文件可被 PowerShell `ConvertFrom-Json` 解析；指定的关系提取和检查点 JSON 因文件不存在，无法执行针对性解析。
- 关系提取与检查点：未修改、未重建任何关系或检查点产物；由于五个指定基线输入缺失，只能确认路径缺失，不能声称产物一致性已验证。
- 术语和非法边：本次补丁只涉及索引路径与文档状态文字，没有改动模块关系、边、职责或术语定义。
- `git diff --check`：通过，无空白错误。
- 模块索引机械核对：`docs/模块需求设计/02-模块资料汇总与审查清单.md` 仍包含 M01-M15 的模块标识；本次未改变其关系文本。

## 未修复问题

- 指定的模块问题清单、模块关系提取和模块检查点输入材料缺失；无法逐项映射原问题编号，也无法重新生成或逐项校验这些产物。
- `docs/_archive/` 中仍有若干历史原文的图片相对路径失效。这些文件属于已归档实验/调研材料，不在本次活动模块索引修复范围内，未擅自改动。

## 必须由产品负责人决定的问题

以下事项在现有模块审查单中仍明确标为未决，本次未作产品裁决：

- M08/M09 的存储结构、选型及历史版本归属。
- M15 的读取频率，以及状态读数影响 M05/M10 的证据强度和时效闸门。
- M04/M09 的闭环记录、候选审计、晋升/退回/撤回规则。
- M11/M13 是否以及如何在后续“上下文工程 + 图工程”阶段重组。
- 任何涉及 M04/M09/M15/M10 语义边界、模块职责重新分配、新增/删除产品关系的事项。

上述事项的证据仍保留在 `docs/模块需求设计/02-模块资料汇总与审查清单.md` 及各模块审查单中，未被本次修复删除或弱化。

## 对模块关系和检查点产物的影响

本次没有改变模块关系、触发方向、职责边界或检查点内容。根 README 的入口现在指向现存的研究终稿和归档说明；模块关系的权威文本仍为 `docs/模块需求设计/02-模块资料汇总与审查清单.md`。由于指定的“实现基线”关系/检查点文件不存在，不能对其内部一致性作出更强结论，后续应先补齐这些输入再进行产物级复核。
