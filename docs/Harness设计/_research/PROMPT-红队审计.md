# 红队审计：Studium Harness 设计

> 用法：`claude -p --permission-mode bypassPermissions "$(cat 本文件)"`
> 目标导向。**不要修改任何文件**，只产出报告。

---

你是**独立审计员**，任务是冷读并**对抗式**检查一套设计文档，找出问题。你没有任何先前上下文——这正是你的价值：**你就是"下一个会读这些文档的 agent"，哪里会被误导，你就报哪里。**

## 读什么

1. `docs/Harness设计/` 下所有 `.md`（00/01/02 系列、README、`_research/` 里的调研发现）。
2. 产品权威：`docs/模块需求设计/00-第三次重构-产品设计总览.md`、`docs/模块需求设计/02-模块资料汇总与审查清单.md`、`docs/模块需求设计/模块审查单/`（全部）。
3. 实现基线：`docs/实现基线/模块关系提取.md`、`模块检查点.md`、`模块问题清单.md`。
4. `README.md`、`任务线路.md`。
5. 归档但可能误导人的：`docs/图工程调研/06-M08技术选型.md`（只看它是否会误导，别采用它的结论）。

## 你要找的五类问题

| 类别 | 含义 |
|---|---|
| **C 冲突** | Harness 设计与产品文档矛盾（例如 Harness 让某模块读/写产品文档禁止的东西） |
| **X 内部矛盾** | Harness 文档之间或同一文件内部自相矛盾 |
| **R 红线违规** | 违反产品已确认的禁止关系（如 M09↔M15 无引用、M02/M03 不直读 M09、M04 不写 M15 结论、M08 不直收个人内容） |
| **M 误导点** | 一个冷读者会被带偏的地方：术语未定义、前后不一致、把未验证实验当依据、状态标记含糊 |
| **G 缺口** | 设计明确依赖但从未定义的东西 |

## 硬性要求

- **每条问题必须带证据**：`文件:行号` 或原文引用。不许凭印象。
- **对抗式**：假设设计有错，去找。不要复述文档、不要夸奖。
- **区分严重级别**：`阻断` / `重大` / `次要`。
- **不改任何文件**。只报告。
- 特别注意：Harness 文档里凡是引用 `docs/图工程调研/06-M08技术选型.md` 的 id 前缀 / 关系词表 / 节点类型 / 锚点长度 / Reading B"不变量层"，都应报为"误导点"（它们已被明确排除）。

## 产出

写到 `docs/Harness设计/_research/审计-红队-发现.md`：

- 结构化表格优先，按严重级别排序；
- 结尾给「**最该先修的 5 条**」；
- ≤400 行，面向 agent 阅读。
