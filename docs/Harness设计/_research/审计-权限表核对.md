# 审计：Harness 集中权限表 × 实现基线核对

> 日期：2026-09-17 ｜ 执行：主 Agent（此前子 Agent 版本因网关中断未产出）
> 对象：`../01-容器与路线设计.md` §「读范围 = 集中权限表」草案
> 基线（权威）：`../../实现基线/模块关系提取.md` 的**正向关系矩阵 R01–R76**、**禁止关系矩阵 P01–P44**、**数据对象归属表**；`../../实现基线/模块检查点.md` 全局不变量。
> 判定口径：**漏**=基线要求能读/写、表里没有；**越权**=表里给了、基线禁止；**错**=表里写的与基线相反或措辞致误；**一致**=两边相符。
> 硬性：每条给 R/P 编号；只比对，不改产品结论。

## 0 结论摘要

| 类别 | 数量 | 说明 |
| --- | --- | --- |
| **漏读** | 7 | 表给少了，见 §1 |
| **漏写 / 写入目标不清** | 3 | 见 §2 |
| **措辞需精确** | 2 | 见 §3 |
| **越权读 / 越权写** | **0** | 表中未出现任何 P01–P44 禁止的授予 |
| **继承产品冲突** | 1 | C01（M09 输入 vs P30），非本表可解 |
| **红线** | 5 条基本正确，1 条措辞需补例外 | 见 §4 |

**主结论**：权限表**方向全对、无越权**；问题集中在**漏读 7 条**（尤其 M04/M05 缺 M07、M08 缺 M06、M14 缺 M05/M10）与**两处措辞**。修完即可从"待核对"升为"已核对"。

## 1 漏读清单（表给少了）

| # | 模块 | 权限项 | 权限表说的 | 基线说的 | 判定 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | M04 | 可读 | 无 M07 | M07 → M04：诊断核查所需原文/来源 | **漏** | R43（M07:47；M04:11,56） |
| A2 | M05 | 可读 | 无 M07 | M07 → M05：补资料、路线规划的来源信息 | **漏** | R44（M07:47；M05:11） |
| A3 | M14 | 可读 | 无 M05 | M05 → M14：长周期阶段推进、优先级、复检节奏 | **漏** | R35（M05:50；M14:56） |
| A4 | M14 | 可读 | 无 M10 | M10 → M14：高阶任务策略与强度条件 | **漏** | R66（M10:44；M14:60） |
| A5 | M08 | 可读 | 只有 M07 / M09 / M14 | M06 → M08：概念/方法候选及来源片段 | **漏** | R38（M06:11,43；M08:11） |
| A6 | M07 | 可读 | 只有「M06 交付；检索请求」 | M08 → M07：通用检索标签 | **漏** | R48（M08:73；M07:21,30,46） |
| A7 | M06 | 可读 | 只有「原始资料」 | M07 → M06：重提取/重定位/重分类反馈 | **漏** | R37（M07:24,43；M06:9,21） |

> A1 与 `_research/干跑-导数闭环.md` 断裂点 B5「M04→M07 漏」一致，可互为佐证。A1 另注：产品 M04 审查单的输入清单也没列 M07，属**产品文档与关系基线的口径差**，修表时一并标注。

## 2 漏写 / 写入目标不清

| # | 模块 | 权限项 | 权限表说的 | 基线说的 | 判定 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| B1 | M01 | 可写 | 容器树、路线事实、目标面、产出归属 | M01 → M07：项目对资料的**选取关系** | **漏写** | R02（M01:48；M07:17,44） |
| B2 | M06 | 可写 | 入库资源与处理状态 | M06 → M08：概念/方法**候选** | **漏写**（与 A5 同源） | R38 |
| B3 | M08 | 可写 | 通用对象/关系/候选 | M08 → M07：通用**检索标签** | **漏写**（与 A6 同源；受 P20 约束，只能写标签、不能改正文） | R48；P20 |

> 结构性说明：本表 `可写` 只列"自己的区"，因此 `M02/M03→M01 归属`、`M04→M01 关环通知`、`M10→M01 策略记录归属`、`M15→M01 状态关联事实`（R16/R20/R26/R64/R67）在表里**看不见**。它们不是越权（M01 自己记录），但**当前表无法表达"跨区写入"**。建议在表下加一行注：归属/位置类写入由 M01 统一落账，不逐个模块列。

## 3 措辞与边界

| # | 模块 | 权限项 | 权限表说的 | 基线说的 | 判定 | 证据 |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | M15 | 可读 | "**不读 M04 / M09**" | M15 可读 M04 的**重新确认请求**（唯一反向边）；不读 M04/M09 的**知识结论** | **错（措辞）** | R28；P10；红线行本身已写"只能收 M04 的重新确认请求"，与 M15 行自相矛盾 |
| C2 | M01 | 可读 | "**全（容器）**" | M01 只读容器/位置事实，**不复制其他模块语义**（解释另存） | **措辞致误** | P02（M01:26,54-57）；`01-容器与路线设计.md` §M01「只存事实，不存解释」 |

## 4 红线核对

| 红线（表） | 基线 | 结论 |
| --- | --- | --- |
| M09 ↔ M15 互不可读（经 M01） | P24 / P25；C4 | ✅ 一致 |
| M15 ✗ M04 / M09 知识结论 | P10 / P24 | ⚠️ 措辞需补"仅请求"例外（C1） |
| M04 ✗ M15 结论写入 | P10；`01` §2.6 | ✅ 一致 |
| M02 / M03 / M14 ✗ M09 | P26（M02/M03）、P37 | ✅ 一致 |
| M08 ✗ 个人区（只收去个人化候选） | P21 / P23 | ✅ 一致 |

## 5 已核对一致（不逐条列，抽样确认）

- **M10** 可读（M01 / M05 / M04 / M08 / M15）对应 R08 / R34 / R25 / R54 / R70，**无漏读、无越权**；P27–P30 全部未违反。
- **M02** 可读（M01 / M05 / M10 / M04 / M07 / M08）对应 R04 / R32 / R61 / R22 / R41 / R49；不读 M09（P26）✅。
- **M03** 可读（+ M02 交接）对应 R05 / R33 / R62 / R23 / R42 / R50 / R13；不读 M09（P26）✅。
- **M09** 可读（M08 / M04 闭环提交 / M01）对应 R53 / R27 / M01 事实；P24 未违反 ✅。
- **P04 / P06 / P08 / P09 / P14 / P15 / P19 / P20 / P33 / P35 / P36**：逐条比对，表中**没有**任何授予违反这些禁止。

## 6 对 `01-容器与路线设计.md` 的建议修正（已应用）

1. **补 7 处漏读**：M04+`M07`、M05+`M07`、M14+`M05`/`M10`、M08+`M06 候选`、M07+`M08 标签`、M06+`M07 重处理反馈`。
2. **补 2 处漏写**：M01 可写 +`资料选取关系`；M06 可写 +`概念/方法候选`；M08 可写 +`检索标签`（注明受 P20 约束）。
3. **修 2 处措辞**：M15 可读改为"…M04 重新确认请求（仅请求）；**不读 M04 / M09 知识结论**"；M01 可读改为"全（容器 / 位置事实；**不读语义**）"。
4. **表下加注**：归属/位置类跨区写入由 M01 统一落账（R16/R20/R26/R64/R67），不逐个模块列。
5. **状态**：表头由"草案（待核对）"→"**已核对 2026-09-17**；权威以 `模块关系提取.md` 为准"。

## 7 遗留（非本表可解）

| 编号 | 冲突 | 处理 |
| --- | --- | --- |
| C01（继承） | M09 输入列举「M10 必要教学条件」，但 P30 / M10:74 禁止 M10→M09 直写 | 产品口径冲突；只允许经 M04 闭环总体记录或 M13 过程引用进入，需产品裁决 |
| A1 附带 | 产品 M04 审查单输入清单亦未列 M07，与 R43 口径不一致 | 记为产品文档口径差，待统一 |
