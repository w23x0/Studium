# 审计：Harness 红队（自审）

> 日期：2026-09-18 ｜ 执行：主 Agent **自审**（⚠️ 不是冷读；作者自审，价值有限，宜日后用新鲜 agent 重跑）
> 对象：`docs/Harness设计/` 全部
> 基线：`docs/模块需求设计/模块审查单/`（产品权威）+ `docs/实现基线/`
> 分类：C 冲突（与产品）· X 内部矛盾 · R 红线 · M 误导点 · G 缺口

## 最该先修的 5 条

1. **C1** M08 最小单位（Harness=原子断言 vs 产品=概念/方法）
2. **G3** "M05 索引的 M08 片 → M10 读那片"未落产品
3. **C2** M15 显式请求（Harness 不建通道 vs 产品 M10 输入含"当前用户需求"）
4. **G1** "面"的取值 / M09 知识面未进 `模块检查点`
5. **X1** 主链图仍写"四项强度"，节点设计已改"强度条件（不必四项）"

## C 冲突（Harness vs 产品审查单）

| # | 位置 | 冲突 | 建议 |
| --- | --- | --- | --- |
| C1 | `01-知识层设计.md` §6.2 | 写"最小单位 = 原子断言"；产品 `M08-…md` 写"概念/方法对象"，问题 8 未答 | 把"原子断言 + 链接 + 粒度=遍历深度"写进 M08 审查单（已定） |
| C2 | `01-状态层设计.md` §9.8 | "不建显式请求通道"；产品 `M10-…md` 输入含"学习者主动提出的现实需求" | 澄清：M10 **读**该输入（产品口径），但**不建专用通道**（D-20） |
| C3 | `01-节点设计.md` §3.2 ③ | 列 M08 前置/组成为 M10 条件接入；产品 `M10-…md` M08 边界只写阈值 | 已改为"M10 可/非必需"；再补"M05 索引的 M08 片"（G3） |

## X 内部矛盾

| # | 位置 | 矛盾 | 建议 |
| --- | --- | --- | --- |
| X1 | `01-模块组合设计.md` §1 主链图 vs `01-节点设计.md` §3.3 | 图写"M10 策略与**四项强度**"；文已改"**强度条件**（不必四项）" | 图同步为"策略与强度条件" |
| X2 | `01-知识层设计.md` §6.1 vs §6.2 | §6.1"权威=原文+引用、抽象派生" 与 §6.2"最小单位=原子断言"的层次未明说 | 加一句：原子断言 = 接地表示；概念 = 断言经链接聚出的视图 |

## R 红线（抽查）

| 红线 | 结论 |
| --- | --- |
| M09 ↔ M15 互不可读 | ✅ 一致 |
| M02/M03/M14 ✗ M09 | ✅ 一致（P26 已补 M14） |
| M04 ✗ M08 写 / ✗ M15 结论写 | ✅ 一致 |
| M15 → M04 严格单向（仅请求） | ✅ 一致 |
| 不物理删除（M09 版本化） | ✅ 一致 |

**未发现越权读/写。**

## M 误导点

| # | 位置 | 误导 | 建议 |
| --- | --- | --- | --- |
| M1 | `00-技术方向确认.md` 通篇 DSH | 冷读者可能以为"设计结论 = DSH" | 00 头部加一句醒目声明（README 已有） |
| M2 | `01-知识层设计.md` 正文引用 `图工程调研/06-M08技术选型.md` | 归档实验，易被当依据 | 已标"非依据"；可在引用处直接删掉文件名 |
| M3 | `01-容器与路线设计.md` M15 行 | "仅收 M04 请求" 与 "不读 M04/M09" 并存 | 已写清"仅请求、不读知识结论"；保持 |

## G 缺口

| # | 缺什么 | 建议 |
| --- | --- | --- |
| G1 | "面"的取值（覆盖/部分/未知）+ M09 知识面 | 已进 M09 审查单（B1）；再进 `模块检查点` |
| G2 | M08 节点粒度 = 原子断言 + 链接 + 粒度=遍历深度 | 进 M08 审查单（= C1） |
| G3 | "M05 设计闭环时索引 M08 片 → M10 读那片" | 进 M10 审查单（替代"M10 直读 M08"） |
| G4 | A-03（输入缺料/冲突规则）只落了 M04（§2.9） | **已落齐 9 模块**（2026-09-20）：M04/M10/M02/M03（`01-节点设计.md`）、M05/M01（`01-容器与路线设计.md`）、M08/M09（`01-知识层设计.md`）、M15（`01-状态层设计.md`）；口径待拍板 → `待确认项.md` §一 A-03 |
