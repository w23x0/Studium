# Harness 待确认项（我方主张，未经产品负责人确认）

> 用途：把 Harness 设计里**由我方提出、尚未经产品负责人确认**的条目集中成一页，供逐条裁决。
> 性质：**索引视图**，内容仍以出处文件为准；本页不新增结论。
> 裁决方式：每条只需「采用 / 否」；采用后回写出处文件并从此表移除。
> 更新：2026-09-20

## 一、我方主张，待确认

| 编号 | 一句话 | 出处 | 待你定 |
| --- | --- | --- | --- |
| D-07 | 梯子不是文档，是 **M08 图上的路线**；基础段复用书本路线，前沿段由外部工具汇总且**全部候选化（带锚点）** | `01-模块组合设计.md` §3.1 | 采用 / 否 |
| D-08 | **不给学习者贴水平标签**（高中/大学/能力评级）；水平只由实际知识面表达 | 同上 | 采用 / 否（与 M09「明确不做」一致） |
| D-09 | **新能力 = 组合实例，不是新模块**；但产出必须有主（目标畅谈 / 网络调研都是组合实例） | 同上 | 采用 / 否 |
| D-10 | **目标畅谈**：输出 = 目标面（**锚定 M08 对象**）+ 项目目标陈述 → M01；对话循环是组合实例，非新模块 | 同上 | 采用 / 否 |
| A-10 | **M08/M09 环路默认关闭 + 不对称**：易写 M09、极难升 M08；上升"递归"只作方向、不作承诺 | `01-模块组合设计.md` §3.2 | 采用 / 否 |
| F-01 | **M15 自调频**：调频信号来自 M15 自身（阶段子模块的波动读数 / 自己的窗口即将过期）；不让 M05/M10 反向设频率 | `01-状态层设计.md` §9.3 | 采用 / 否 |

## 二、其余未决（非"我方主张"，是暂缓或按模块定）

| 编号 | 一句话 | 出处 | 状态 |
| --- | --- | --- | --- |
| A-03 | 各模块的输入必需 / 缺料 / 冲突规则（**M04 已落**，其余模块待补） | `01-模块组合设计.md` §3.2 | 本阶段 |
| A-04 | 哪些上下文常驻缓存 / 按需读取 / 只保留引用 | 同上 | 暂缓（执行图联合设计） |
| A-05 | M04 是单节点 / 子图 / 多受限 Agent | 同上 | 暂缓 |
| A-06 | 推理链的存储位置与格式 | 同上 | 暂缓 |
| A-07 | 模型内部思考（A 层）与推理通道（B 层）是否调研借鉴 | 同上 | 后续调研 |
| A-09 | M09 知识面（派生视图；取值 覆盖 / 部分 / 未知）——**依赖 M08 结构** | 同上 | 依赖 D-007 |
| C-02 | 各模块的动态上下文骨架字段与组合范围 | `02-上下文资产格式.md` §6 | 未决，逐模块补 |

> 上表 A-* 与 C-02 是"暂缓/待补"，不是"我方主张"，列出仅为完整；决定它们不属于本轮。

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-20 | 建立本文：从 `01-模块组合设计.md` §3.1/§3.2、`01-状态层设计.md` §9.3、`02-上下文资产格式.md` §6 抽出 |
