# Harness 待确认项（我方主张，未经产品负责人确认）

> 用途：把 Harness 设计里**由我方提出、尚未经产品负责人确认**的条目集中成一页，供逐条裁决。
> 性质：**索引视图**，内容仍以出处文件为准；本页不新增结论。
> 裁决方式：每条只需「采用 / 否」；采用后回写出处文件并从此表移除。
> 更新：2026-09-20

## 一、我方主张，待确认

| 编号 | 一句话 | 出处 | 待你定 |
| --- | --- | --- | --- |
| D-07 | 梯子不是文档，是 **M08 图上的路线**；基础段复用书本路线，前沿段由外部工具汇总且**全部候选化（带锚点）** | `01-模块组合设计.md` §3.1；**依据核对**：产品 M08 审查单「来源与增长」（2026-09-18 补）逐句同义 | 采用 / 否 |
| D-08 | **不给学习者贴水平标签**（高中/大学/能力评级）；水平只由实际知识面表达 | 同上；**依据核对**：M09 审查单「明确不做：人格类型、能力评级」+ M15 审查单「明确不能由 M15 推断：人格 / 能力 / 稳定学习风格」 | 采用 / 否（与 M09「明确不做」一致） |
| D-09 | **新能力 = 组合实例，不是新模块**；但产出必须有主（目标畅谈 / 网络调研都是组合实例） | 同上；**依据核对**：与 `README.md` 分层（组合层不新增产品结论）同向；「产出必须有主」为组合层治理规则，无产品原文对应 → 需拍板 | 采用 / 否 |
| D-10 | **目标畅谈**：输出 = 目标面（**锚定 M08 对象**）+ 项目目标陈述 → M01；对话循环是组合实例，非新模块 | 同上；**依据核对**：M01 审查单允许记录项目目标；「目标面由谁产出」产品原文未定 → 需拍板 | 采用 / 否 |
| A-10 | **M08/M09 环路默认关闭 + 不对称**：易写 M09、极难升 M08；上升"递归"只作方向、不作承诺 | `01-模块组合设计.md` §3.2；**依据核对**：M08 审查单「候选隔离、审计与晋升」= 默认关闭已有产品原文依据；「不对称 / 极难」是实现取向，其原引实验数字出自已删归档、**不作依据** | 采用 / 否 |
| F-01 | **M15 自调频**：调频信号来自 M15 自身（阶段子模块的波动读数 / 自己的窗口即将过期）；不让 M05/M10 反向设频率 | `01-状态层设计.md` §9.3；**依据核对**：与集中权限表一致（不存在 M05/M10 → M15 的读写边）；「自调频」本身无产品原文对应 → 需拍板 | 采用 / 否 |
| A-03 | **各模块缺料 / 冲突规则口径**：输入分「必需 / 条件」；**缺料检查由运行时做**（不指望模型自查）；冲突优先序 = 现场证据 > M09 历史背景、M15 候选经 M04 裁决、M08 是依据不构成冲突；M04 不读任何派生结论 | 9 模块各一节：`01-节点设计.md` §2.9 / §3.5 / §4.5 / §5.5、`01-容器与路线设计.md`、`01-知识层设计.md` §6.7 / §7.6、`01-状态层设计.md` §9.10 | 采用 / 否 |

## 二、其余未决（非"我方主张"，是暂缓或按模块定）

| 编号 | 一句话 | 出处 | 状态 |
| --- | --- | --- | --- |
| A-03 | 各模块的输入必需 / 缺料 / 冲突规则 | `01-模块组合设计.md` §3.2 | **已落**（2026-09-20，9 模块各一节）；**口径待拍板** → 见 §一 A-03 |
| A-04 | 哪些上下文常驻缓存 / 按需读取 / 只保留引用 | 同上 | 暂缓（执行图联合设计） |
| A-05 | M04 是单节点 / 子图 / 多受限 Agent | 同上 | 暂缓 |
| A-06 | 推理链的存储位置与格式 | 同上 | 暂缓 |
| A-07 | 模型内部思考（A 层）与推理通道（B 层）是否调研借鉴 | 同上 | 后续调研 |
| A-09 | M09 知识面（派生视图；取值 覆盖 / 部分 / 未知）——**依赖 M08 结构** | 同上 | 依赖 D-007 |
| C-02 | 各模块的动态上下文骨架字段与组合范围 | `02-上下文资产格式.md` §6 | 未决，逐模块补 |

> 上表 A-* 与 C-02 是"暂缓/待补"，不是"我方主张"，列出仅为完整；决定它们不属于本轮。

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-20 | 建立本文：从 `01-模块组合设计.md` §3.1/§3.2、`01-状态层设计.md` §9.3、`02-上下文资产格式.md` §6 抽出 |
| 2026-09-20 | A-03 内容落齐 9 模块 → 移入 §一 待拍板；D-07 / D-08 / A-10 补「依据核对」列（前两条已有产品原文依据） |
