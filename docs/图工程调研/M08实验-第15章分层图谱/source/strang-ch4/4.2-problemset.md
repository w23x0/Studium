## Problem Set 4.2

Questions 1-9 ask for projections p onto lines. Also errors $e = b - p$ and matrices P.

1 Project the vector b onto the line through a. Check that e is perpendicular to a:

$$
\text {(a)} \boldsymbol {b} = \left[ \begin{array}{l} 1 \\ 2 \\ 2 \end{array} \right] \quad \text { and } \quad \boldsymbol {a} = \left[ \begin{array}{l} 1 \\ 1 \\ 1 \end{array} \right]
$$

(b)

$$
\boldsymbol {b} = \left[ \begin{array}{l} 1 \\ 3 \\ 1 \end{array} \right] \quad \text { and } \quad \boldsymbol {a} = \left[ \begin{array}{l} - 1 \\ - 3 \\ - 1 \end{array} \right].
$$

2 Draw the projection of b onto a and also compute it from $\pmb { p } = \widehat { \pmb { x } } \pmb { a }$ :

$$
\text {(a)} \quad b = \left[ \begin{array}{l} \cos \theta \\ \sin \theta \end{array} \right] \quad \text { and } \quad a = \left[ \begin{array}{l} 1 \\ 0 \end{array} \right] \quad \text {(b)} \quad b = \left[ \begin{array}{l} 1 \\ 1 \end{array} \right] \quad \text { and } \quad a = \left[ \begin{array}{l} 1 \\ - 1 \end{array} \right].
$$

3 In Problem 1, find the projection matrix $P = { a } { a } ^ { \mathrm { T } } / { a } ^ { \mathrm { T } } { a }$ onto the line through each vector a. Verify in both cases tha $P ^ { 2 } = P$ . Multiply $P b$ in each case to compute the projection $\pmb { p } .$

4 Construct the projection matrices $P _ { 1 }$ and $P _ { 2 }$ onto the lines through the $\pmb { a } ^ { \prime } \pmb { s }$ in Problem 2. Is it true that $( P _ { 1 } + P _ { 2 } ) ^ { 2 } = P _ { 1 } + P _ { 2 } ?$ This <sup>would</sup> be true if $P _ { 1 } P _ { 2 } = 0$

5 Compute the projection matrices ${ a } { a } ^ { \mathrm { T } } / { a } ^ { \mathrm { T } } { a }$ onto the lines through ${ \pmb a } _ { 1 } = ( - 1 , 2 , 2 )$ and ${ \pmb a } _ { 2 } = ( 2 , 2 , - 1 )$ ). Multiply those projection matrices and explain why their prod uct $P _ { 1 } P _ { 2 }$ is what it is.

6 Project $\boldsymbol { b } = ( 1 , 0 , 0 )$ onto the lines through $\mathbf { \delta } _ { a _ { 1 } }$ and $\mathbf { { a } } _ { 2 }$ in Problem 5 and also onto ${ \pmb a } _ { 3 } = ( 2 , - 1 , 2 )$ . Add up the three projections ${ \pmb p } _ { 1 } + { \pmb p } _ { 2 } + { \pmb p } _ { 3 }$

7 Continuing Problems 5-6, find the projection matrix $P _ { 3 }$ onto ${ \pmb a } _ { 3 } = ( 2 , - 1 , 2 )$ ). Verify that $P _ { 1 } + P _ { 2 } + P _ { 3 } = I$ . This is because the basis ${ \mathbf { } a _ { 1 } , \mathbf { } a _ { 2 } , \mathbf { } a _ { 3 } }$ is orthogonal!

![](images/44068b7a6f7b789862212ea0ee9af081ceab2cc8a30d3be08208571b34717aa8.jpg)

![](images/1549e85cec2dbdad5501f8df3dc2d065bd6b195323180c3ffe23a867e0bf9ae2.jpg)

Questions 5-6-7: orthogonal

Questions 8-9-10: not orthogonal

Project the vector $b = ( 1 , 1 )$ onto the lines through ${ \pmb a } _ { 1 } = ( 1 , 0 )$ and $a _ { 2 } = ( 1 , 2 )$ Draw the projections $\pmb { p } _ { 1 }$ and $\pmb { p } _ { 2 }$ and add ${ \pmb p } _ { 1 } + { \pmb p } _ { 2 }$ . The projections do not add to b because the <sup>a's</sup> are not orthogonal.

In Problem $^ { 8 , }$ the projection of b onto the plane of $\mathbf { \delta a } _ { 1 }$ and $\mathbf { \delta } _ { a _ { 2 } }$ will equal b. Find $P = A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \bar { \mathrm { T } } }$ for $\begin{array} { r } { A = \left[ \begin{array} { l l } { \mathbf { a } _ { 1 } } & { \mathbf { a } _ { 2 } } \end{array} \right] = \left[ \begin{array} { l l } { \mathbf { 1 } } & { \mathbf { 1 } } \\ { \mathbf { 0 } } & { \mathbf { 2 } } \end{array} \right] = } \end{array}$ invertible matrix.

0 Project ${ \pmb a } _ { 1 } = ( 1 , 0 )$ onto $a _ { 2 } = ( 1 , 2 )$ ). Then project the result back onto $\pmb { a } _ { 1 }$ <sub>1</sub> Draw these projections and multiply the projection matrices $P _ { 1 } P _ { 2 } \colon$ Is this a projection?

Questions 11-20 ask for projections, and projection matrices, onto subspaces.

11 Project b onto the column space of A by solving $A ^ { \mathrm { T } } A { \widehat { \mathbf { x } } } = A ^ { \mathrm { T } } b$ and $p = A { \widehat { x } }$

$$
\text {(a)} A = \left[ \begin{array}{l l} 1 & 1 \\ 0 & 1 \\ 0 & 0 \end{array} \right] \quad \text {and} \quad b = \left[ \begin{array}{l} 2 \\ 3 \\ 4 \end{array} \right]
$$

(b)

$$
A = \left[ \begin{array}{c c} 1 & 1 \\ 1 & 1 \\ 0 & 1 \end{array} \right] \quad \text { and } \quad \boldsymbol {b} = \left[ \begin{array}{c} 4 \\ 4 \\ 6 \end{array} \right].
$$

Find $e = b - p .$ . It should be perpendicular to the columns of A.

Compute the projection matrices $P _ { 1 }$ and $P _ { 2 }$ onto the column spaces in Problem 11. Verify that $P _ { 1 } \boldsymbol { b }$ gives the first projection $p _ { 1 }$ . Also verify $P _ { 2 } ^ { 2 } = P _ { 2 }$

(Quick and Recommended) Suppose A is the 4 by 4 identity matrix with its last column removed. A is 4 by 3. Project $b = ( 1 , 2 , 3 , 4 )$ onto the column space of A. What shape is the projection matrix $P$ and what is $P ?$

14 Suppose b equals 2 times the first column of A. What is the projection of b onto the column space of $A ?$ Is $P = I$ for sure in this case? Compute $_ p$ and $P$ when $b = ( 0 , 2 , 4 )$ and the columns of A are (0, 1, 2) and $( 1 , 2 , 0 )$

If A is doubled, then $P = 2 A ( 4 A ^ { \mathrm { T } } A ) ^ { - 1 } 2 A ^ { \mathrm { T } }$ \_ This is the same as $A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ The column space of 2A is the same as \_\_ . Is x the same for A and 2A?

16 What linear combination of $( 1 , 2 , - 1 )$ and $( 1 , 0 , 1 )$ ) is closest to $\pmb { b } = ( 2 , 1 , 1 ) \ P$

17 (Important) If $P ^ { 2 } = P$ show that $( I - P ) ^ { 2 } = I - P$ . When $P$ projects onto the column space of $A , I - P$ projects onto the \_

(a) If $P$ is the 2 by 2 projection matrix onto the line through (1, 1), then $I - P$ is the projection matrix onto

(b) If $P$ is the 3 by 3 projection matrix onto the line through $( 1 , 1 , 1 )$ , then $I - P$ is the projection matrix onto

To find the projection matrix onto the plane $x - y - 2 z = 0$ , choose two vectors in that plane and make them the columns of A. The plane will be the column space of A! Then compute $P = A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$

20 To find the projection matrix P onto the same plane $x - y - 2 z = 0$ , write down a vector e that is perpendicular to that plane. Compute the projection $Q = e e ^ { \mathrm { T } } / e ^ { \mathrm { T } } e$ and then $P = I - Q$

Questions21-26show that projection matrices satisfy $P ^ { 2 } { = } P \mathbf { a n d } P ^ { \mathbf { T } } { = } P$

21 Multiply the matrix $P = A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ by itself. Cancel to prove that $P ^ { 2 } = P$ Explain why $P ( P b )$ always equals Pb: The vector $P b$ is in the column space of A so its projection onto that column space is

22 Prove that $P = A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ is symmetric by computing $P ^ { \mathrm { T } }$ \_ Remember that the inverse of a symmetric matrix is symmetric.

23 If A is square and invertible, the warning against splitting $( A ^ { \mathrm { T } } A ) ^ { - 1 }$ does not apply. It is true that $A A ^ { - 1 } ( A ^ { \mathrm { T } } ) ^ { - 1 } A ^ { \mathrm { T } } = I$ . When A is invertible, why is $P = I ?$ What <sub>is</sub> the <sub>error</sub> <sub>e?</sub>

24 The nullspace of $A ^ { \mathrm { T } }$ is \_\_ to the column space $\ b { C } ( \ b { A } )$ . So if $A ^ { \mathrm { T } } b \ = \ \mathbf { 0 }$ , the projection of b onto $\ b { C } ( \ b { A } )$ should be $p = _ { - }$ . Check that $P = A ( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ gives this answer.

25 The projection matrix P onto an n-dimensional subspace of $\mathbb { R } ^ { m }$ has rank $r = n$ Reason: The projections Pb fill the subspace S. So Sis the \_ \_ of P.

26 If an m by m matrix has $A ^ { 2 } = A$ and its rank ism, prove that $A = I$

27 The important fact that ends the section is this: $\Im f A ^ { \mathbf { T } } A x \ = \ 0$ then $A x = 0$ New <sub>Proof:</sub> The vector Ax is in the nullspace of \_\_ . Ax is always in the column space of \_\_ . To be in both of those perpendicular spaces, Ax must be zero.

28 Use $P ^ { \mathrm { T } } = P$ and $P ^ { 2 } = P$ to prove that the length squared of column 2 always equals the diagonal entry $P _ { 2 2 }$ . This number is $\begin{array} { r } { \frac { 2 } { 6 } = \frac { 4 } { 3 6 } + \frac { 4 } { 3 6 } + \frac { 4 } { 3 6 } } \end{array}$ for

$$
P = \frac {1}{6} \left[ \begin{array}{c c c} 5 & 2 & - 1 \\ 2 & 2 & 2 \\ - 1 & 2 & 5 \end{array} \right].
$$

If B has rank m (full row rank, independent rows) show that $B B ^ { \mathrm { T } }$ is invertible

## Challenge Problems

(a) Find the projection matrix $P _ { C }$ onto the column space of A (after looking closely at the matrix!)

$$
A = \left[ \begin{array}{l l l} 3 & 6 & 6 \\ 4 & 8 & 8 \end{array} \right]
$$

(b) Find the 3 by 3 projection matrix $P _ { R }$ onto the row space of A. Multiply $B =$ $P _ { C } A P _ { R }$ . Your answer B should be a little surprising-can you explain it?

In $\mathbb { R } ^ { m }$ , suppose I give you b and also a combination p of $a _ { 1 } , \ldots , a _ { n }$ . How would you test to see if pis the projection of b onto the subspace spanned by the $\pmb { a } ^ { \prime } \mathbf { s } ^ { \prime }$

32 Suppose $P _ { 1 }$ is the projection matrix onto the I-dimensional subspace spanned by the first column of A. Suppose $P _ { 2 }$ is the projection matrix onto the 2-dimensional column space of A. After thinking a little, compute the product $P _ { 2 } P _ { 1 }$

$$
A = \left[ \begin{array}{l l} 1 & 0 \\ 2 & 1 \\ 0 & 1 \end{array} \right].
$$

Suppose you know the average $\widehat { x } _ { \mathrm { o l d } }$ of $b _ { 1 } , b _ { 2 } , \ldots , \ b _ { 9 9 9 }$ . When $b _ { 1 0 0 0 }$ arrives, check that the new average is a combination of $\widehat { x } _ { \mathrm { o l d } }$ and the mismatch $b _ { 1 0 0 0 } - \widehat { x } _ { \mathrm { o l d } } ;$

$$
\widehat {\boldsymbol {x}} _ {\mathbf {n e w}} = \frac {b _ {1} + \cdots + b _ {1 0 0 0}}{1 0 0 0} = \frac {b _ {1} + \cdots + b _ {9 9 9}}{9 9 9} + \frac {1}{1 0 0 0} \left(b _ {1 0 0 0} - \frac {b _ {1} + \cdots + b _ {9 9 9}}{9 9 9}\right)
$$

This is a "Kalman fi.Uer" $\begin{array} { r } { \overline { { x } } _ { \mathrm { n e w } } = \widehat { x } _ { \mathrm { o l d } } + \frac { 1 } { 1 0 0 0 } \left( b _ { 1 0 0 0 } - \widehat { x } _ { \mathrm { o l d } } \right) } \end{array}$ with gain matrix ${ \scriptstyle { \frac { 1 } { 1 0 0 0 } } } .$ The last page of the book extends the Kalman filter to matrix updates.

(2017) Suppose $P _ { 1 }$ and $P _ { 2 }$ are projection matrices $( P _ { i } ^ { 2 } { = } P _ { i } { = } P _ { i } ^ { \mathrm { T } } )$ . Prove this fact: $P _ { 1 } P _ { 2 }$ is a projection matrix if and only if $P _ { 1 } P _ { 2 } = P _ { 2 } P _ { 1 }$

