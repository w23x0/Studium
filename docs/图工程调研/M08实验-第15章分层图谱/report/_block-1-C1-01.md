**裁决：A1 §1 的 21 条待杀 id 清单可采纳，`d:for-function-spaces-closure-is-the-only-real-content` 撤出成立。已核。** `A1-互证塌缩检查.md:35` 的 §1 标题原文为「那 22 条无冲突 KILL 的 id 清单（逐条回显）」，22 − 1 = 21 自洽。B2 的 29 条 KILL 与 `data/` 命中未复算。

**裁决：A1 §2.2 的 NEAR 那条可安全杀，但落盘前须把存活侧路径补成 `data/inherited/nodes-A1.jsonl:15`。已核。** `A1-互证塌缩检查.md:140` 现文为「存活的 `apostol:space-of-continuous-functions`（`nodes-A1.jsonl:15`）锚点」，缺 `inherited/`；该文件实际只存在于 `data/inherited/`。65 / 131 字符数未复核。

**裁决：A3 §1 的 `SPEC.md:53` 与 `:55` 冲突成立，登记为前置裁断项；未裁前 A3 §3/§4 的补锚不得作「必须执行」进单。已核。** `SPEC.md:53`「至少 1 条,最多 3 条」与 `:55`「找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。」逐字命中、行号相符；`A3-零证据节点锚点候选.md:64-65/:68/:74/:81` 明写该冲突、「四个节点全部已经满足 `SPEC.md:55` 第二分支的形式要件」及两分支后果。

**裁决：A3 §6 验收口径可采纳——须读 stdout 的 `nodes with NO anchor`，只应用 §3/§4 则由 4 变 2，带 quote 锚点数 921 → 924。已核（924）。** `A3-零证据节点锚点候选.md:441` 原文「带 quote 的锚点数会从 921 变 924(:42 加 2、:54 加 1)」，同处声明 522/1433 不变；`:440` 提示 §5 裁决乙动拓扑、须同批处理 `edges-D2.jsonl:90`。`verify_anchors.py` 源码结论沿用碎片。

**裁决：A4 §3 疑似第 8 条（`nodes-D4.jsonl:53`）判 PLAUSIBLE、不自动执行，且须按 index 而非「剥第二条」的口头描述执行。已核（PLAUSIBLE 定位）。** `A4-形式B漂移与借用锚点.md:6` 原文「另附 1 条本代理自行发现的**疑似**第 8 条借用锚点，标为 PLAUSIBLE，与被点名的 7 条严格分开」。碎片实测 `:53` 的 `[0]`/`[1]` 与 `:15` 顺序正好相反（未复核）。此为本片最须保留的执行警告。

**裁决：A4 §4 关于 `15.06.md:9`「整句 207 字符」一项不得进单。未复核，且定位失败。** 碎片称该行整行实测 421、207 指行内某句而无从定位；本片在 `A4-形式B漂移与借用锚点.md` 内 Grep `15.06.md:9` 零命中，Grep `207` 只命中 `:258` 的 `edges-D1.jsonl:207、208`（行号，非字符数）。

**裁决：A1 §3 / §4 / §5 全部处置不采纳（无法装配），A1 §0 把「§3.2 的 6 条重指做完」列为 `apply_kills.py` 强制前置一并不采纳。已核。** `A1-互证塌缩检查.md` 的一级节标题只有三个：`:5`「## 0 摘要」、`:35`「## 1 …id 清单」、`:70`「## 2 塌缩对清单」——§3/§4/§5 不存在，故「引文彻底消失 7 条分 3 类处置」「B2 `reason` 须重写 4 条」「6 条重指」均不可核，前置条件本身不可查。

**裁决：A1 §0「引文彻底消失 7 条」与「锚点轴塌缩 1 对」不可引用。已核。冲突待人工裁定。** `A1-互证塌缩检查.md:19` 现文仍为「| 引文从全图彻底消失 | **7 条**（§4），分 3 类处置 |」，指向不存在的 §4；`:144` 自陈「§0 前一版把损失记成「7 条」…按引文数是 9 条。§0 已改。」——改动未落地。7（按节点数）与 9（按引文数）两说并存，本片不替主控选口径。

**裁决：A4 §3 末句「921 降到 913（减 8）」不可引用，验收只能用 918（A+B）或 917（A+B+C）。已核。** `A4-形式B漂移与借用锚点.md:428` 原文「全图锚点从 921 降到 **913**（减 8）」，只算 B 组减 7、C 组减 1，漏掉 A 组净增 4；`:507` 自给「921 + 4 − 7 = **918**。若 C 组也执行，为 **917**」，`:508` 另警告「请按实际采纳的方案重算，不要照抄 918」——该警告须随 918 一同进单。

**裁决：A1 §0 算式「29 − 9 = 22」不得抄进验收单，数字 22 可用。未复核。** 碎片称 9 个争议里 B2 判 KILL 者只有 7 个（`x:dimension-versus-rank`、`x:independence-criterion-and-its-consequences` 在 B2 为 FIX），29 − 7 = 22。来源 `A1-互证塌缩检查.md` §0。

**裁决：A1 §0 零残留三项可采纳，是 `apply_kills.py` 唯一放行前提。未复核。** 碎片称按 21 个 id 做裸串与 `parent` 双扫描实测均为 0。来源 `A1-互证塌缩检查.md` §0。

**裁决：A3 §0 四个零证据节点与 12 条边可采纳。未复核。** 碎片实测 `nodes-D2.jsonl:42/:54/:72/:73` 为 `anchors: []`、`origin: "model"`，全图无锚节点恰这 4 个，`edges-D2.jsonl:90` 为唯一 TD。须带小注：表头写 `type`，字段名实为 `node_type`。

**裁决：A4 §7 B 组 7 条借用锚点剥 `index 1` 可采纳。未复核（范围声明已核）。** 碎片逐行复核 `nodes-D4.jsonl:15`、`:52` 与 `nodes-A2-x2.jsonl:2/:3/:4/:5/:6` 各 2 条锚点、待剥均为 `anchors[1]`、剥后剩 1 不击穿下限；`A4-形式B漂移与借用锚点.md:5` 自述范围为「§6.2 的 7 条借用锚点（死因 0，只摘锚点不动节点）」，已核。

**裁决：A4 §7 A 组三条补锚的目标定位可采纳（仅定位）。未复核。** 碎片实测 `nodes-D1.jsonl:62/:69/:70` 各只 1 条锚点、共用同一条 115 字符锚点即形式 B 共享组本身。

**裁决：A3 §5 对 `:72`/`:73` 移出本轮，无可装配动作。未复核。** A3 只作裁决甲/乙的后果分析、未给单一建议，其倾向的「第三条路」自陈「不作为本提案的建议动作」。

**移交主控另片**（碎片标「采纳」、本片无支撑）：A1 §0 边计数表、A1 §2.1 对 B / 对 C、A1 §2.1 对 A 的十条公理映射表（归 `G2b`）、A4 §4 四处字符更正、A4 §0 边 `evidence` 为 dict。**未回源**：A1 §2.2 FULL/PART、A3 §2-§4、A4 §1–§2/§5。A10 不属本分片。
