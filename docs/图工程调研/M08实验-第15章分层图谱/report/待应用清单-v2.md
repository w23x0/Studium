# 待应用清单 v2

## 〇 本清单的产生方式与它的已知局限

本清单由只读裁决代理产出，**未改 `data/` 下任何文件**，也未运行 `tools/` 下任何脚本。
输入是 `report/待应用-提案/` 下 73 份提案。

**读法（决定了本清单的可信度上限）**：按简报要求，每份提案只读其「〇 结论速览」与明确标为
可执行 / 清单 / 修改 / 条文的小节，逐条推理**未读**。需要判某条张力时才单点回查。
因此：凡本清单标「已核」的，是我本轮在 `tmp_index/` 上重测过的；凡标「采信」的，
是照抄提案自报值、**我没有独立复现**。两者在正文里逐条区分，不要混读。

本轮我独立实测的项（全部在 `tmp_index/` 上，可复算）：
- 三个 L1 分组节点各自的第二条 `part-of` 指向（裁决 9 的承重事实）；
- 公理 2 锚点 = **175** 字符及其 **5 个持有者**；
- `origin` 字段分布：24 `model` + 9 `source` = 33（裁决 8）；
- `d:ten-axioms-listed-in-three-groups` 两条锚点的持有者数，与它作 dst 的 5 条边；
- A4 剥锚目标（`nodes-A2-x2.jsonl:2–6`）与 G1b 唯一载体（`:12`、`:18`）的 id 不相交（裁决 6）；
- `edges-D1.jsonl:106` 的 114 字符 evidence 有 8 个持有者。

### 已知局限（四条，都会影响结论强度）

1. **19 条 KILL 的死因判定本身，本清单未复核。** 与 G1d 一致：本清单做的是装配、排序、
   冲突消解，不重审 B2 的 KILL 判决。若某条 KILL 的死因是错的，本清单会把错误照序执行。
2. **G4 十六份完备性报告的 21 条补建缺口，本清单未逐条裁决**（见 §四）。它们要新建节点，
   与本轮「先改接再删」的执行序列正交，且 G4z 自报严重度 4 占 15 条。
3. **锚点是否**支撑**断言，本清单一条都没有独立验证。** 所有「引文支撑」类判断均采信提案。
   按盲区五型，这正是 grep 查不出的那一层，D 型（覆盖不足）尤其查不出。
4. **执行后 `check_graph` 是否通过，是推断而非实测。** 每步不变量由「悬空 id 只减不增」
   这条结构论证给出，我没有跑过任何脚本。§六 给的是必须重跑的命令，不是已跑过的结果。

## 一 裁决记录

简报点名 8 条，我另加 1 条（裁决 9：G2a × G2b 正面冲突，两者都在执行路径上，不裁决无法排序）。

### 裁决 1 —— A9 的两处反转：一条不成立、一条成立

**裁决：A9 的自我更正全部维持。** 即：`apostol:zero-element` 的「死因 6 → 0」**不成立**，
死因 6 维持；某分片的 **100 字符**锚点计数**是对的**，89 是另一条子串。

- 依据（zero-element）：G2e §1.2/§1.3 与 A9 更正三**结论一致**。G2e 逐条列了该节点的
  池 7 行（`pool.tsv`）与 TD 14 行（`pool-TD-excluded.tsv`），并指出上一轮用来归零的那条边上
  `apostol:zero-element` 是 **dst**，按四层定义属 TD、不入池。G2e 另加一层实测：句 2 的源行
  `15.03.md:19` 的 `n_sentence_end=4`，属多句行，**不得按行号做覆盖比对**（过报通道 b）——
  即便按行号看起来「覆盖了」，池内 15.03 的条数仍是 0。
- 依据（100 vs 89）：G2f §1.2/§1.3 **未沿用 A9**，用 Python `len()` + `in` 独立重测，
  得 A = 100（含 `EXAMPLE 3. `）、B = 89（`edges-D2.jsonl:85` 的边引文）、C = 79（节点锚点），
  100 − 89 = 11 = `EXAMPLE 3. ` 的长度。100 有两个互不依赖的测量来源，89 没有任何来源指向 A。
- 简报的「以有逐字实测的那一方为准」在此**不触发**：G2e、G2f 与 A9 三方一致，无分歧可裁。
- 死因编号：zero-element **6**（针对 statement 句 2）；100/89 一项 **0**（记账错）。
- 建议动作：zero-element **FIX**，且必须按 G2e A 案**同批**落两项——补 `15.03.md:19` 内一条锚点
  **并**补 `sections` 15.03。G2e 的关键耦合已核过并采信：**单补 `sections` 不能消死因 6**
  （sections 是元数据不是证据），单补会制造「sections 声称覆盖 15.03、池里查不到」的新不一致。
- 置信度：高。不确定点：G2e 新构造的那条 15.03.md:19 锚点字符数我**未实测**（采信 G2e）。

### 裁决 2 —— SPEC.md:53 与 :55 的矛盾，P05 是否消解

**裁决：P05 消解了矛盾，但留下两个缺口，本清单在 §三 一并补掉。**

- 矛盾确认（A3 §1 提出）：`:53`「至少 1 条」无条件成立时，`:55` 第二分支「找不到可逐字引用的
  依据 ⇒ … 或标 `origin: model` 并如实说明」的前提永远无法执行，是死条文。
- P05 的消解方式：把「无锚」从「找不到锚点」收紧为「**原理上不可锚**」，新增封闭词表字段
  `unanchorable`（`external-proof` / `negative-claim` / `derived-criterion`），
  并明文写「`origin: model` **不构成**无锚的凭据」。这一句把 A5 的授权与锚点下限**解耦**，
  矛盾消解成立。
- **缺口 A（P05 × P06 正面冲突，必须裁）**：P06 的 `NO_ANCHOR_NO_EXEMPTION` 豁免 2 写的是
  「`origin ∈ {model,mixed}` + 非空 `origin_note`」，**明确拒绝新增字段**；P05 与 A3 都要
  `unanchorable`。**裁决：采 P05/A3 的专用字段，否决 P06 的豁免 2。**
  依据是我本轮实测：`origin=model` 的 24 个节点里 **20 个有锚点、4 个无锚点**
  （`nodes.tsv` 第 7 列 + 第 6 列交叉）。既然 20/24 带锚，`origin` 与「可否无锚」正交，
  用 `origin` 当豁免会一次性授权 24 个节点无锚，而实际只有 4 个需要豁免。
  P06 被否决的理由不是它算错了（它的 4 个节点已带 `origin_note` 这一实测是对的），
  而是它选的判据**射程比它要豁免的对象大 6 倍**。P06 的其余部分（六类 FAIL、stdout 契约、
  「机器不判只能人工判」那一段）全部采纳。
- **缺口 B（P05 未处理）**：A3 §5.3 指出 `:72`/`:73` 的 `sections` 写 `["15.08"]` 而内容不在
  15.08 里。P05 的条文**已覆盖**这一条（「拿委托语所在的节号冒充内容所在的节号判死因 3」），
  但 P05 没有给这两个节点的落盘裁决。**本清单不裁**（见 §五 第 3 条），标为未完成。
- 死因编号：0（SPEC 条文缺陷，非图谱缺陷）。建议动作：**进 SPEC**，见 §三 R2、R5。
- 置信度：高（24/20/4 三个数我实测）。不确定点：`unanchorable` 落盘后 `verify_anchors.py`
  的补丁本清单只给条文、不给代码。

### 裁决 3 —— A7 的前提更正是否被 G3a–G3e 遵守

**裁决：全部遵守，无一回到「整合」口径。已核。**

- 依据（逐份看 §〇 速览的动词）：G3a「保留 4 / 修 7 / 撤回 4 = 15」；G3b「保留 3 / 修 8 /
  撤回 0，节点实际数量 = 11」；G3c「保留 10 / 修 5 / 撤回 4 = 19」；G3d「逐字核验通过 15 /
  失败 0，应撤回 1 条」；G3e「H 层 59 → 50 条，节点 11 个全部保留、其中 8 个需 FIX」。
  五份用的都是保留/修/撤回三分，没有一份用「并入」。
- A7 本身也已自我更正：A7 §0 逐字写「本提案不是'把 H 层并进去'，而是'H 层已在图里，
  哪些该留、哪些该改、哪些该撤'」，并核了 522/1433 含 H1/H2。**A7 与 G3 系列口径一致。**
- 死因编号：0。建议动作：按 G3e 的总装执行（本清单 §二 步骤 6–8 只登记、不展开逐条）。
- 置信度：高（速览动词是逐字读的）。不确定点：G3a–G3d 各自的逐条判定我**未复核**。

### 追加裁决 C1-01 —— A1 / A3 / A4

**裁决：A1 §1 的 21 条待杀 id 清单可采纳，`d:for-function-spaces-closure-is-the-only-real-content` 撤出成立。已核。** `A1-互证塌缩检查.md:35` 的 §1 标题原文为「那 22 条无冲突 KILL 的 id 清单（逐条回显）」，22 − 1 = 21 自洽。B2 的 29 条 KILL 与 `data/` 命中未复算。

**裁决：A1 §2.2 的 NEAR 那条可安全杀，但落盘前须把存活侧路径补成 `data/inherited/nodes-A1.jsonl:15`。已核。** `A1-互证塌缩检查.md:140` 现文为「存活的 `apostol:space-of-continuous-functions`（`nodes-A1.jsonl:15`）锚点」，缺 `inherited/`；该文件实际只存在于 `data/inherited/`。65 / 131 字符数未复核。

**裁决：A3 §1 的 `SPEC.md:53` 与 `:55` 冲突成立，登记为前置裁断项；未裁前 A3 §3/§4 的补锚不得作「必须执行」进单。已核。** `SPEC.md:53`「至少 1 条,最多 3 条」与 `:55`「找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。」逐字命中、行号相符；`A3-零证据节点锚点候选.md:64-65/:68/:74/:81` 明写该冲突、「四个节点全部已经满足 `SPEC.md:55` 第二分支的形式要件」及两分支后果。

**裁决：A3 §6 验收口径可采纳——须读 stdout 的 `nodes with NO anchor`，只应用 §3/§4 则由 4 变 2，带 quote 锚点数 921 → 924。已核（924）。** `A3-零证据节点锚点候选.md:441` 原文「带 quote 的锚点数会从 921 变 924(:42 加 2、:54 加 1)」，同处声明 522/1433 不变；`:440` 提示 §5 裁决乙动拓扑、须同批处理 `edges-D2.jsonl:90`。`verify_anchors.py` 源码结论沿用碎片。

**裁决：A4 §3 疑似第 8 条（`nodes-D4.jsonl:53`）判 PLAUSIBLE、不自动执行，且须按 index 而非「剥第二条」的口头描述执行。已核（PLAUSIBLE 定位）。** `A4-形式B漂移与借用锚点.md:6` 原文「另附 1 条本代理自行发现的**疑似**第 8 条借用锚点，标为 PLAUSIBLE，与被点名的 7 条严格分开」。碎片实测 `:53` 的 `[0]`/`[1]` 与 `:15` 顺序正好相反（未复核）。此为本片最须保留的执行警告。

**裁决：A4 §4 关于 `15.06.md:9`「整句 207 字符」一项不得进单。未复核，且定位失败。** 碎片称该行整行实测 421、207 指行内某句而无从定位；本片在 `A4-形式B漂移与借用锚点.md` 内 Grep `15.06.md:9` 零命中，Grep `207` 只命中 `:258` 的 `edges-D1.jsonl:207、208`（行号，非字符数）。

**裁决：A1 §3 / §4 / §5 全部处置不采纳（无法装配），A1 §0 把「§3.2 的 6 条重指做完」列为 `apply_kills.py` 强制前置一并不采纳。已核。** `A1-互证塌缩检查.md` 的一级节标题只有三个：`:5`「## 0 摘要」、`:35`「## 1 …id 清单」、`:70`「## 2 塌缩对清单」——§3/§4/§5 不存在，故「引文彻底消失 7 条分 3 类处置」「B2 `reason` 须重写 4 条」「6 条重指」均不可核，前置条件本身不可查。

**裁决：A1 §0「引文彻底消失 7 条」与「锚点轴塌缩 1 对」不可引用。已核。冲突待人工裁定。** `A1-互证塌缩检查.md:19` 现文仍为「| 引文从全图彻底消失 | **7 条**（§4），分 3 类处置 |」，指向不存在的 §4；`:144` 自陈「§0 前一版把损失记成「7 条」…按引文数是 9 条。§0 已改。」——改动未落地。7（按节点数）与 9（按引文数）两说并存，本片不替主控选口径。

**裁决：A4 §3 末句「921 降到 913（减 8）」不可引用，验收只能用 918（A+B）或 917（A+B+C）。已核。** `A4-形式B漂移与借用锚点.md:428` 原文「全图锚点从 921 降到 **913**（减 8）」，只算 B 组减 7、C 组减 1，漏掉 A 组净增 4；`:507` 自给「921 + 4 − 7 = **918**。若 C 组也执行，为 **917**」，`:508` 另警告「请按实际采纳的方案重算，不要照抄 918」——该警告须随 918 一同进单。

**裁决：A1 §0 算式「29 − 9 = 22」不得抄进验收单，数字 22 可用。未复核。** 碎片称 9 个争议里 B2 判 KILL 者只有 7 个（`x:dimension-versus-rank`、`x:independence-criterion-and-its-consequences` 在 B2 为 FIX），29 − 7 = 22。来源 `A1-互证塌缩检查.md` §0。

**裁决：A1 §0 零残留三项可采纳，是 `apply_kills.py` 唯一放行前提。未复核。** 碎片称按 21 个 id 做裸串与 `parent` 双扫描实测均为 0。来源 `A1-互证塌缩检查.md` §0。

**裁决：A3 §0 四个零证据节点与 12 条边可采纳。未复核。** 碎片实测 `nodes-D2.jsonl:42/:54/:72/:73` 为 `anchors: []`、`origin: "model"`，全图无锚节点恰这 4 个，`edges-D2.jsonl:90` 为唯一 TD。须带小注：表头写 `type`，字段名实为 `node_type`。

**裁决：A4 §7 B 组 7 条借用锚点剥 `index 1` 可采纳。未复核（范围声明已核）。** 碎片逐行复核 `nodes-D4.jsonl:15`、`:52` 与 `nodes-A2-x2.jsonl:2/:3/:4/:5/:6` 各 2 条锚点、待剥均为 `anchors[1]`、剥后剩 1 不击穿下限；`A4-形式B漂移与借用锚点.md:5` 自述范围为「§6.2 的 7 条借用锚点（死因 0，只摘锚点不动节点）」，已核。

**裁决：A4 §7 A 组三条补锚的目标定位可采纳（仅定位）。未复核。** 碎片实测 `nodes-D1.jsonl:62/:69/:70` 各只 1 条锚点、共用同一条 115 字符锚点即形式 B 共享组本身。

**裁决：A3 §5 对 `:72`/`:73` 移出本轮，无可装配动作。未复核。** A3 只作裁决甲/乙的后果分析、未给单一建议，其倾向的「第三条路」自陈「不作为本提案的建议动作」。

**移交主控另片**（碎片标「采纳」、本片无支撑）：A1 §0 边计数表、A1 §2.1 对 B / 对 C、A1 §2.1 对 A 的十条公理映射表（归 `G2b`）、A4 §4 四处字符更正、A4 §0 边 `evidence` 为 dict。**未回源**：A1 §2.2 FULL/PART、A3 §2-§4、A4 §1–§2/§5。A10 不属本分片。

### 追加裁决 C1-02 —— A5 / A6 / A7

**裁决：A5 §6 的 20 节点全 KEEP、死因 `0`、登记形式 E，采纳。已核。** 依据 `A5-CX2与origin.md` §6。`SPEC.md:100` 亲验逐字为「死因编号:`1` 量词过度断言 / `2` 编造成员归属 / `3` 伪造教材出处 / `4` 数学错误 / `5` 伪抽象·层塌缩。」，止于 5、无 6，故「形式 E 不入现有死因编号」与 SPEC 现状不冲突。§6.1 的 20 行 id 逐条回显未复核。

**裁决：A5 §2.2 的 41 条锚点全通过、`occurrences` 均 1、min 34 / max 167 落在 30–200，采纳。未复核。** 依据 `A5-CX2与origin.md` §2.2，41 条未独立复跑。阈值本身已核：`SPEC.md` 逐字为「- 长度 30–200 字符。不跨行拼接、不改标点、不加省略号、不修正 OCR 噪声。」，但**该行在 `:54`，不在碎片所写的 `:52`**，落笔前改掉这个行号。

**裁决：A5 §3.3 形态 (a)（节点级 `origin`、位置紧跟 `anchors`、取值只用 `"model"`），采纳。已核。** 事实基础机器复核全中：`data/nodes-*.jsonl` 内 `"origin":` 共 **33**,分账 D1 1 / D2 8 / D3 4 / D4 9 / H2 11；`"origin_note":` 共 **39**,分账 CX2 15 / D2 9 / D3 4 / H2 11,均与 `A5-CX2与origin.md` §3.3 逐项吻合。`tools/export_obsidian.py:229-230` 亲验逐字为 `if node.get("origin_note"):` / `body += ["## 来源说明", "", prose("origin_note"), ""]`。`:183-184` 的 `origin` 前言输出、取值 model 24 / source 9 的分项未复核。

**裁决：A5 §3.1 属补文档而非新增能力，采纳。已核。** `SPEC.md:55` 亲验逐字为「- 找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。」，且紧接 `:53`「至少 1 条,最多 3 条」与 `:54` 长度规则，确在锚点规则段内、节点语境授权成立。`SPEC.md:62` 节点 schema 无 `origin`、`:70` 边 schema 有 `"origin":"source|model"` 两项未复核。

**裁决：A5 §3.3 第 2 项（SPEC 补 cause `6`「引文不支撑该断言」并写明「形式 E ≠ 死因 3」），采纳。已核。** 依据 `A5-CX2与origin.md` §3.3。`SPEC.md:100` 亲验止于 `5`，`6` 不与既有编号相撞；该行 `3` 现为「伪造教材出处」，与形式 E 确非同类，补这一句有实义。

**裁决：A5 §5 的数据改动（`nodes-CX2.jsonl` 15 行各加 `"origin": "model",`、`origin_note` 一字不动；`nodes-A2-x2.jsonl` :2–:6 各加 2 字段），采纳。已核（规模项）。** `data/nodes-CX2.jsonl` 现有 `"origin_note":` 恰 **15** 行且无任何 `"origin":`，与 `A5-CX2与origin.md` §0 第 5 点、§5 自报的 15 行改动量一致。:2–:6 的行级定位未复核。

**裁决：A5 §5.1 的 `tmp_apply_a5.py` 改后采纳——插入位置（`anchors` 后、`origin_note` 前）照用，整行重序列化换成定点插入。未复核。** 依据 `A5-CX2与origin.md` §5.1：整行 `json.dumps` 会重写该行全部标量的转义与间隔，兑现不了「每行只多出 `"origin": "model", `」的承诺。脚本本体未读。落笔后以 `git diff --stat` 验只有 15 行变化。

**裁决：A5 §7.1 伞节点 `apostol:cx-inner-product-axiom-diagnosis` 一并加 `origin: "model"` 与 §7.1 给出的逐字 `origin_note`，采纳。未复核。** 依据 `A5-CX2与origin.md` §7.1：被统摄的 5 个都标而伞节点不标属反常状态。该 id 落在 `nodes-A2-x2.jsonl:1` 未亲验。

**裁决：A5 §7.4 剩余同型节点另起一批，改后采纳，开批前先修分账数字。已核（部分）。冲突待人工裁定。** `data/nodes-A2-x2.jsonl` 亲验恰 **34** 行，与 `A5-CX2与origin.md` §7.4 的「CX2 15 + A2-x2 34 = 49」「本批 20、剩 29」自洽。但该文内部矛盾：正文称「**13 个**疑似形式 E」，所列 id 只 **12** 个（`:1 :8 :11 :12 :13 :16 :17 :18 :21 :30 :33 :34`），`:9`/`:10` 另作「混合」单列，16 + 13 = 29 的分账缺 1 个 id 落地。两说并列，人工定「13」或「12」后才能开批。

**裁决：A5 §7.2 / §7.3 两处 statement 覆盖缺口不修、不为 #1/#2 的 "usual" 子句加第 3 条锚点，均采纳。未复核。** 依据 `A5-CX2与origin.md` §7.2/§7.3，与本轮「只加字段、不动 statement」总原则一致。CX2 #10 的公理 8、15.12 #17 的无见证声称两处原文未回查。

**裁决：A6 缺陷 1 主方案（按 §1.4 主方案逐字改 `nodes-D2.jsonl:36` 的 `statement`、新增锚点 A2），采纳。已核（要件）。** `data/nodes-D2.jsonl:36` 的 id 亲验为 `d:exponential-independence-select-the-largest-exponent`，与 `A6-两处数学缺陷.md` §1.4 的回显一致；A2 的判别子串 `are distinct real numbers, the` 在 `source/apostol-ch15/15.07.md` 第 **39** 行、全文 **1** 次、不跨行。78 字符与「现有 `anchors` 恰 1 条、加后 2 条不触上限」未复核。

**裁决：A6 §1.5 / §2.6 的三条「不要做」（不给 `:36` 挂 15.07.md L57、不给 `:51` 挂 L23 与 L9、不加 `:36 --requires--> :33` 边），均采纳。未复核。** 依据 `A6-两处数学缺陷.md` §1.5/§2.6：TD 入边须排除，该依赖已由 `edges-D2.jsonl:39` 承载。该边所在行未亲验。

**裁决：A6 缺陷 2 主方案（按 §2.4 主方案逐字改 `nodes-D4.jsonl:51` 的 `statement`、新增锚点 B3）改后采纳；落笔前删掉或改写 §2.3 那句自评。已核（要件）。冲突待人工裁定。** `data/nodes-D4.jsonl:51` 的 id 亲验为 `d:pythagorean-split-of-the-approximation-error`；B3 判别子串 `Then, for any` 在 `source/apostol-ch15/15.15.md` 第 **11** 行、**1** 次、不跨行，与「34 字符最小跨同在 L11」自洽,故「最小语义单元不可独立成锚」的两难属实。`A6-两处数学缺陷.md` 内部矛盾：§2.3 称改用 `exceeds … by exactly` 搭配显式平方口径后歧义消解，§2.4 定稿保留的正是它刚批评的该结构，t = s 时的严格性歧义并未由文本消解。两说并列。82 字符未复核。

**裁决：A6 §1.7 / §2.7 连带项（`id`、`name_en`、`name_zh`、`sections`、`atomic_reason` 与所有边一律不动），采纳。未复核。** 依据 `A6-两处数学缺陷.md` §1.7/§2.7。`sections` 为 `["15.15"]` 与内容相符只由该文自报的 L3/L9/L11/L17/L20/L23 行级依据支撑，其中仅 L11 经本片亲验。

**裁决：A6 §三 验收提示——「只改 `statement` ⇒ 点数不变」与「必须读 stdout 里 `nodes with NO anchor` 那一行、不能只看退出码」无条件采纳；「带 quote 的锚点 921 → 923」未复核。未复核。** 依据 `A6-两处数学缺陷.md` §三。本片未做全库锚点计数，921 与 923 两个数都不背书。

**裁决：A7 §0 的前提校正数字「节点 522、边 1433」改后采纳，必须补写口径。未复核。冲突待人工裁定。** 依据 `A7-H层整合.md` §0。本片未做全库计数，1433 = 1220 + 213、522 = 397 + 125、以及 H1 边 15 / H2 节点 11 / H2 边 19 三项均未复核。碎片自报 522 **不含 `data/structures-*.jsonl` 的 24 行**（计入则 546），原文未交代该口径，照抄会与按 546 计数的提案对不上。且本条采纳 §0 的数字，与下一条整份撤回 `A7-H层整合.md` 在字面上相抵，需人工划清「撤回 34 项裁决」与「保留 §0 前提数字」的边界。

**裁决：`A7-H层整合.md` 整份不采纳（撤回）。已核。** 亲验该文件第 **13** 行即 `<!--SKELETON: §1 机械核验 / §2 H1 逐条 / §3 H2 节点逐条 / §4 H2 边逐条 / §5 整合提案 / §6 两份审计都漏的 / §7 未做的事-->`,§1–§7 只以这一行注释存在，§0 宣称的「直接并入 9 项、改后并入 5 项、建议丢弃 20 项、另报 3 处漏检」从未写出，无一条可装配，汇总数字在本文件内无支撑。磁盘全长 1284 字符未复核。

**裁决：A7 §0 的机械合规结论（31 条边引文逐字命中、11 节点锚点逐字命中、字符数全落 30–200、无跨行、无悬空 id、无对称关系反向重复边）移出本轮。已核（不可复核性）。** 依据本应在未写出的 §1,而 §1 经亲验只是骨架注释，无从复核。H 层裁决改由在册的 `G3a-H1边逐条.md`、`G3b-H2节点逐条.md`、`G3c-H2边逐条.md`、`G3e-H层总装.md` 四份承载；这四份文件本片未查在册状态。

**裁决：A6 §1.4 缺陷 1 备选方案（不点名 distinctness、不新增锚点）移出本轮。未复核。** 依据 `A6-两处数学缺陷.md` §1.4：仅在主控拒绝新增锚点时启用，与主方案互斥，不并行装配。

**裁决：A6 §2.4 缺陷 2 备选方案（去掉全称量词、不引 L11）移出本轮。未复核。** 依据 `A6-两处数学缺陷.md` §2.4 自承会在 `:51` 留下 D 型射程不足残留，主方案已可用。

**裁决：A6 §2.5 把锚点 B2 换成 96 字符长跨，不采纳。未复核。** 依据 `A6-两处数学缺陷.md` §2.5 自证该 `(file,quote)` 对已被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 占用，提为锚点会形成三方共享并越界吃进 `:50` 的自有内容。两处占用行未亲验。

**裁决：A5 §7.4 里 16 个「非形式 E」节点补 `origin: model`，不采纳。未复核。** 依据 `A5-CX2与origin.md` §7.4：其锚点含 "Prove that / show that" 型题面，定性结论有锚，不满足形式 E 构成要件。16 个 id 的锚点文本未回源。

**裁决：以下余项一律未复核，不得据以落笔。未复核。** `A5-CX2与origin.md` §4.1–§4.20 的 20 张子句分解表未逐张回源（碎片只核 §4.21 汇总自洽性：108 = 21 + 5 + 55 + 27，82/108 = 75.9%,本片亦未复核该算式）；§5.2/§5.3 的 5 条 `origin_note` 逐字文本未与源题面比对；A6「921 → 923」未复核；A5 §2.2 的 41 条锚点未复跑。

### 追加裁决 C1-03 —— A9 / A10

**裁决：A10 §6.1 采纳，清单 :199–200 改为「34 个节点 `origin`/`origin_note` 命中 0/34」。已核。** `A10-批次分层信号.md` 第 215 行逐字给出「真正未披露的批次是 `A2-x2`：34 个节点，`origin`/`origin_note` 命中 0/34」。§4.1 与 15/15 复算未复核。

**裁决：A10 §6.2 采纳，`审查-形式B-共享引文.md:15` 的「41 个不同节点」改「42」。已核。** `A10-批次分层信号.md` 第 375–379 行给出现文 41、改为 42，及「跨两组的节点实测恰好 3 个，45 − 3 = 42」。

**裁决：A10 §6.3 改后采纳，新增 6.5 节须同时保留「34 个」与「已审 6 / 剩余 28」两数。已核（数字侧）。** `A10-批次分层信号.md` 第 395–397 行：34/34 锚点落 `15.12-exercises.md`(25)/`15.16-exercises.md`(9)、两文件 `grep -ci answer` 各 0、「只读过其中 6 个(5 判借用锚 + 1 判合法)，剩余 28 个从未被审查」。

**裁决：A10 §6.4 采纳，SPEC 增补「锚点的出处充分性」条。已核（前提侧）。** `A10-批次分层信号.md` 第 303–306 行实测四习题文件 78/80/152/77 行、`grep -ci answer` 各 0，故该判据是演绎前提非统计外推。§八 二值性实测未复核。

**裁决：A10 §6.5 改后采纳，方法论三句须与 §3.3 三条局限同段落写入。未复核。** 单调率序列 83%/19%/13%/0%、`nbatches==1` 仅 1 组支撑、`intra>=6` 仅 2 组支撑，均未在 `A10-批次分层信号.md` 命中。附歧义：该文件第 176–178 行的 0.24 是 S2 召回 0.50 的 95% CI 下界，碎片却把「0.24→0.50」当前后对比数，**冲突待人工裁定**。

**裁决：A10 §4.2 析取式采纳，`>=3` 须标暂定，review 集不得标为「形式 B 缺陷集」。已核。** `A10-批次分层信号.md` 第 253 行逐字为 `review(v) = exonly(v) or intra(v) >= 3`；第 161 行 S2 单独已 20/25、CX2 列 15/15，故 review 集主体是形式 E。合取的 5/25 未复核。

**裁决：A10 全份降级条款采纳为引用限制，下游引 A10 的比率须带那三句限定。已核（数值侧）。** `A10-批次分层信号.md` 第 163 行 S2 OR S1(`>=3`) 命中 70、24/25、末列 0.34；第 174–178 行召回列分母确为「10 个 POS」，支持「是覆盖率不是召回」的降级。

**裁决：A9 更正一采纳，改 `_D分片-主控核验.md:525`，分片的 100 字符无误。已核。** `A9-记账错更正.md` 第 11 行：「分片的 100 字符是对的（实测 100，逐字子串）。89 是 `edges-D2.jsonl:85` 边引文那个另一条子串的长度」；第 31 行实测 `15.08.md:15` 整行 233 字符含三句。

**裁决：A9 更正二采纳，替换 `审查-形式D-D3-片21-45.md:89` 整行，`:125` 移出「本该引而未引」。已核。** `A9-记账错更正.md` 第 99–100 行：实测 42 字符、`in 15.10.md` 为 True、命中 `15.10.md:125`、「是该节点已有的唯一锚点（T1），不是缺的」。

**裁决：A9 更正三采纳撤回主控原判，`apostol:zero-element` cause 6 维持、6→0 不成立。已核。** `A9-记账错更正.md` 第 13 行：「6 → 0 不成立。支撑它的那条引文只走 TD（该节点是 `dst`），按四层定义 TD 不入池。cause 6 维持」。

**裁决：A9 更正三处置采纳，补 `sections` 与补第三条锚点同批执行，先锚点后死因的顺序约束随文装配。已核（双改侧）。** `A9-记账错更正.md` 第 13 行「A5 给的 fix（补 sections 并 补第三条锚点）本来就是对的，主控只采了一半」；第 157–159 行给出该 66 字符引文与 `edges-A1.jsonl:18`。`15.03.md:19`、整行 359 字符、额度 2/3 未复核。

**裁决：A9 更正三的 SPEC 议题剥离采纳，本节点摘出、`sections` 语义议题待议。已核。** `A9-记账错更正.md` 第 237 行：`sections` 只在 `SPEC.md:62` 与 `:84` 两处 schema 示例出现，全文无一句定义其语义。

**裁决：A9 派生检查改后采纳，只登记为新增待办，不得作结论。已核。** `A9-记账错更正.md` 第 288 行给出该检查（`:450` 之前、理由引了「该节点是 dst 的边」的塌陷判定须重算），并明言「没有做这个全量重扫，这是本提案留下的缺口」。

**裁决：A10 §4.2「S2 AND S3」合取判据落选。未复核。** 命中 34、25 正例仅覆盖 5 个、按定义排除 CX2 整批，三数未在 `A10-批次分层信号.md` 命中。

**裁决：A10 §3.2「S3 `nodecl` 单独作筛子」落选，无选择性。已核。** `A10-批次分层信号.md` 第 162 行 S3 命中 473、末列 0.02。分母 522 与 91% 未复核。

**裁决：A10 §6.5「按批次名分层」落选。未复核。** 「`intra>=3` 的 30 个命中里 15 个来自 D1」这一 FP 分布未在 `A10-批次分层信号.md` 命中。

**裁决：A10 §5.1 第 3 条「`intra / |batch|` 归一化式」移出本轮。未复核。** 金标准无小批次正例可定标、A10 自身不提出该式，两点均未复核。

**裁决：「把 S2 当形式 B 检测器」这一读法不采纳。已核。** `A10-批次分层信号.md` 第 161 行 S2 命中 49、CX2 列 15/15、合计 20/25，与「20 个中 15 个属形式 E」的数字骨架一致。

**裁决：在 §3.3 之上定 `>=3` / `>=4` 硬阈值移出本轮。已核。** `A10-批次分层信号.md` 第 176–177 行两条 95% CI 为 [0.60, 0.98] 与 [0.49, 0.94]，重叠，只够定方向。

**裁决：A9 简报预设第 1 条「分片的 100 字符是错的」不采纳。已核。** `A9-记账错更正.md` 第 11 行明言两数各量不同子串，要改的是主控核验文件。

**裁决：A9 主控原路径「只改 `sections`、不补锚点」不采纳。已核。** `A9-记账错更正.md` 第 13 行「主控只采了一半」，A5 原 fix 两项本就都对。

**裁决：A9「`:321–448` 全量重扫」移出本轮，仅作缺口登记。已核。** 同上，`A9-记账错更正.md` 第 288 行自认未做。

### 追加裁决 C1-04 —— G6d D1 / D3

**裁决：A 行 43 死因 4 成立，动作定 FIX（不 KILL）。已核。** 复数 "closure axioms" 在 `source/apostol-ch15/15.03.md` 实测 3 次命中，与 `G6d-D1-片31-56-摘要.md` 前提「封闭是两条公理、10−2=8」一致；该提案自陈已给改法且倾向 FIX。

**裁决：C L26 改判死因 3（虚构出处），采纳。已核。** `\tag{15.7}` 在 `source/apostol-ch15/` 全目录唯一命中 15.11.md:46，与 `G6d-D3-片21-45-摘要.md` 所记逐字吻合，故「定理 15.8 处引 (15.7)」是虚构出处而非记账错。

**裁决：C L40 的「实空间限定」降为记账位置问题，其余缺口（`:125`、`:22`）保留，采纳。已核。** `data/edges-D3.jsonl:40` 逐字含 "In a real Euclidean space V"，与 `G6d-D3-片21-45-摘要.md` 所记 T2 位置一致。

**裁决：落选 D-5（L61）依据成立，维持不采纳。已核。** `data/edges-D3.jsonl:148` 命中 "neq j"（156 行另有一处），与 `G6d-D3-片46-69-摘要.md` 所述 evidence 位置吻合，该指控确因真 T2 不成立。

**裁决：落选 D-6（L50）依据不成立，退回待裁而非直接不采纳。已核（反证）。** `u_0` 在 `data/edges-D3.jsonl` 全文零命中，`G6d-D3-片46-69-摘要.md` 所称「:49 逐字含 `(u_0,u_0)=\int_0^{2\pi}dx=2\pi`」失去支撑，其 T2 塌陷判定不可用。

**裁决：落选项「A §四 #6（13 个 file,quote 对）」第二半依据错误，须换依据重述。已核（反证）。** `tmp_index/shared-quotes.tsv` 实测 481 行非空，非碎片所记 15 行。「表体只列 11 行」「13 无出处」本片未核，来源 `G6d-D1-片31-56-摘要.md`。

**裁决：B #68 采纳（改 q(t) = -t^n + 1 或注明 n ≥ 2），但「statement 与边 rel_note 各一处」的定位须改。已核。** "both of degree" 只命中 `data/nodes-D1.jsonl:68` 与 `data/nodes-CX2.jsonl:12`，皆节点侧；`edges-D1.jsonl` 无该串，只有 "degree n" 落 176/177/179。两处同改的条数成立，第二处应改指 CX2。数学自证：n=0 时 q 次数为 1，n=1 时 q≡0。来源 `G6d-D1-片57-81-摘要.md`。

**裁决：B #70 改后采纳后者（换锚为零多项式约定句），不按「支撑充分」结案。已核。** `G6d-D1-片57-81-摘要.md`:110/117 把 #70 列入「T2 真 evidence 缓解→撤回或降级」并指明换锚即零多项式约定句；同文件 :69 自陈 #66/#69/#70 减轻情节「未坐实」，未打开目标节点验其锚点。提案自带否证。

**裁决：R-5 所依「edges-D1.jsonl:149 为 #61 的 part-of 边 evidence」未坐实；R-5 本体仍列待裁项，本轮不得当已消解。已核（部分反证）。** `15.03.md:19` 在 `data/edges-D1.jsonl` 零命中，而 `G6d-D1-片57-81-摘要.md`:110 自陈该 evidence 存在。「兄弟已承载不构成减轻」属规则层裁决，不依赖该行号。

**裁决：C 顺带发现的「`origin: model` 只在 edge schema，故 23 个含无锚 claim 的节点无一能标」不成立，须改述后再作 schema 上报。已核（反证）。** `"origin"` 在 `data/nodes-*.jsonl` 共 33 处（H2 11、D4 9、D2 8、D3 4、D1 1）。命中归属未核，但「节点侧无 origin 可写」已推翻；不记生产者不变。来源 `G6d-D3-片21-45-摘要.md`。

**裁决：A 的五项维持采纳——行 43/46 公理分组矛盾（8 vs 9）绑为同一动作、行 31/38/45/53/54 量词由死因 0 统一升 1、§五「T2 新引入源行 1、T3 合计 2，20 条指控无一被免除」、行 40（E-7）判生产者、行 52（D-3）作 B 型干净样本。未复核。** 据 `G6d-D1-片31-56-摘要.md` 自陈；行 40 依赖 15.04.md:9 = 179 字符加「1 锚 2 空位」，行 52 依赖 15.03.md:9 后半 92 字符共用，皆字符计数，Grep 不能证明。

**裁决：B #62 补锚 15.03.md:33 与 :35 采纳，字符数未定。未复核。** 「两空锚位现成」依赖 `n_anchors` 实测，未查；来源 `G6d-D1-片57-81-摘要.md`。

**裁决：C L30 不判生产者且形态标签归 E-2、D 型定 3 例（L25/L33/L43）、L25 补从严备选，及 D 的四条（L51+L69 合并同判死因 1、L66 死因 1 不因边证据免除、L67 判 0 + 强制改写、L64 改述为句粒度未引），全部采纳。未复核。** 来源 `G6d-D3-片21-45-摘要.md`、`G6d-D3-片46-69-摘要.md`；所依 `:123` 实测 29 差 1、`:75` 含 finite-dimensional、`:89` 三句已被 L67/L69 锚用、「两节之内」三次均在 §15.11，皆未命中。L25 与 L33/L43 同型（末句超射程）故从严标准一致施用；L67 属定位性假陈述非数学错误，不升 4。

**裁决：三组计数修正（A 形式 E = 20、B 总账 25、D §5 = 24）与十项字符数修正、`n_sentence_end` = 4，全部未复核。** 字符数与句数不能由 Grep 命中证明。但过报通道 (b) 的操作性结论——禁止把 15.03.md `:19/:25/:33/:35/:37` 与 15.11.md `:5/:7/:29/:67/:89` 当句级证据复用——从严采纳。

**裁决：其余落选项维持碎片原判。未复核。** 含 A 行 46/55 归因偏软、A 把 15.02.md:51 推给行 48 移出本轮、B #66 恢复 E 判、B 以兄弟减轻的 3 处（#60/#81/R-5 八节点）不采纳、B #72/#74/#78 不入 SPEC 计数、B #57/#61 移出本轮、B R-2 改 16 持有者、C §3 组表与 §七 持有者数不采纳、D「L46/L47 下调」不采纳、D §四 应为 24/24 且末句例外两个。所涉数值 Grep 预算已尽。

**裁决：三处冲突待人工裁定。** 一，B #62 的 15.03.md:35 字符数 99 与分片自记 98 并存。二，B #70 在 `G6d-D1-片57-81-摘要.md` 内既记「已因真 T2 缓解」（110/117）又记「减轻未坐实」（69）。三，B #68 缺陷位置自述与实测（两处皆节点侧）不一致。

**裁决：未覆盖项原样上抛，不在此轮结案。未复核。** C 的 20 例 E、D 的 14 处 E- 未过 T2 池；A/C 型、edges 完备性、`atomic_reason` 四份提案皆未核；D §七 Pythagorean 同名冲突待 D4/D5 交叉核。

### 追加裁决 C1-05 —— G6d D4 与 A6 交叉项

**裁决：A6 §1.4 主方案（改 `nodes-D2.jsonl:36` statement + 新增锚点 A2）改后采纳，但锚点 A2 的字符数与取跨须先锁定。冲突待人工裁定。** 被指控的原句 `exponents smaller by a strictly negative amount` 逐字命中于 `G2e-zero-element与两处数学缺陷.md` L105-107，字面自相矛盾这一判据成立。但碎片把 A2 实测为 78 字符，`A9-记账错更正.md` L41-43 把同一处 `15.07.md:39` 实测为 **89 字符**并称「整行即该串」，该串以 `EXAMPLE 7. ` 起头。两数可由是否含此 11 字符前缀调和，但装配前必须指定取整行还是去前缀子跨。

**裁决：A6 §2.4 主方案（改 `nodes-D4.jsonl:51` statement）采纳。已核。** `A6-两处数学缺陷.md` L144 逐字载有现 statement，其中确含 `is the substantive content of the approximation theorem` 这处过度断言，主方案同时修掉它，故射程缺陷与量纲缺陷一并消除。

**裁决：新增锚点 B3（`15.15.md` L11）有 B 型借用风险，不能按碎片所写「使用者 0」直接装配。冲突待人工裁定。** `G6d-D4-片28-54-摘要.md` 裁决表 L46 把节点 `:50` 的锚 2 记为「给『for any t in S』」，而 A6 拟新增的 B3 跨内含 `Then, for any $t$ in $S$ , we have` 同一短语。两者是否为同一 `(file,quote)` 对本片无法判定；若同跨，A6 的「使用者 0」不成立。

**裁决：`15.15.md:17` 的归属两说并存，A6 「L17 全句是 `:50` 自有锚点」与摘要「`:17` 是 `:51` 的锚 2」互斥。冲突待人工裁定。** `A6-两处数学缺陷.md` L215 写 `:50`（`d:x-minus-t-splits-orthogonally`）的锚点为覆盖 L17 全句的 152 字符跨；`G6d-D4-片28-54-摘要.md` 裁决表 L46 记 `:50` 锚 1 = `15.15.md:17` 散文句，L47 记 `:51` 两锚为 `15.15.md:20`、`:17`。按后者 `:17` 已被两节点共享，A6 的「自有」表述失准。

**裁决：改写 `:51` 须连带补 `origin: model`，A6 漏了该字段。已核。** `G6d-D4-片28-54-摘要.md` L66 逐字列出缺该字段的 6 个节点（L30、31、36、45、48、**51**），并记全片仅 L28、40、43、44 有该字段。`:51` 确在其中，故碎片第 3 条的连带项成立。

**裁决：摘要裁决表 L51 的最小改法（末句「the error」两处改「the squared error」）降级，按 A6 §2.4 主方案关闭。已核。** `G6d-D4-片28-54-摘要.md` 裁决表 L47 逐字为「E 末句 + 表述错 | 0（从严可 4） | FIX：末句「the error」两处改「the squared error」」，并自陈末句「严格读为假（量纲不对）」。该改法保留了 `is the substantive content of the approximation theorem`，射程缺陷未消，故不足。

**裁决：17 处缺陷一处也不能归因于 SPEC 3 锚点上限。已核。** `G6d-D4-片28-54-摘要.md` §〇 元观察 (1) 逐字为「27 个节点没有一个用满 3 条锚点（4 个用 1 条、23 个用 2 条）」，4+23=27 自洽。

**裁决：凡还要给 `:51` 补锚的动作必须排在 A6 §2.4 之前。未复核。** `:51` 现有两锚（摘要裁决表 L47），A6 填第三槽后再无空位；该约束是否与上述 `:17`/B3 归属冲突叠加，待人工裁定。

**裁决：`node_type` 出现 `method`（12 个）与 `notation`（L29、L54）超出四词表，立为独立待查项。已核（词表是否封闭仍未复核）。** `G6d-D4-片28-54-摘要.md` L168 逐字记载此发现并注明「分片未察觉此事」；涉及 14 个节点。本片无 SPEC 原文，无法确认 concept/theorem/proof-step/example 是否为封闭词表。

**裁决：片1-27 摘要 §四.1「行 16 槽位已满是记账错」采纳，但拟填入的 `15.08.md:17` 与 A3 的判定相抵。冲突待人工裁定。** `G6d-D4-片1-27-摘要.md` L76 自陈「字符数全部正确，记账错只有两处，均在 §6 一带，且都不改任何裁决方向」，与碎片第 6、7 条同属该带，方向一致。但 `A3-零证据节点锚点候选.md` L313 把 `15.08.md:17` 的 60 字符跨判为「形式 A（宾位错）」并称「拿来支撑 C1」不成立；碎片称该行实测 187 字符、3 个句末标点，两数可由整行与子句跨调和，取跨与适配性须人工定。

**裁决：行 14 `exactly two load-bearing places` 判死因 1 并改写，改后采纳；反例本身与 D3 交叉未独立复核。未复核。** `G6d-D4-片1-27-摘要.md` L11 逐字记「行 14 的 `exactly two load-bearing places`，被 `15.11.md:15` 反例推翻，且图内缺一条对应边」，L10 记它是全片唯一实质死因 1。但本片仅命中摘要自述，未见 `15.11.md:15` 第三处使用点的独立证据，亦无 D3 材料可交叉。

**裁决：「D4 第 1–54 行无一条 KILL、无一条撤回」采纳但限定射程，两条 B 型线索另立。已核。** `G6d-D4-片1-27-摘要.md` L12 逐字为「无节点被判 KILL、无节点被判撤回；分片对全部 27 条的处置建议都是 KEEP + 改写/补锚」，L9 记 FIX 12 条、保留 15 条，合计 27，无 KILL 槽位。该结论只在形式 D/E 视角下成立。

**裁决：落选 §2.6 / 裁决表 L48 的强制换锚（`15.15.md:17 + :23`）本轮不装配。已核。** `A6-两处数学缺陷.md` L213-215 逐字记该 96 字符长跨已被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 两处占用、提为锚点会造成三方共享，且 A6 自陈「但我不推荐」。须先给出不与 `:50`/`:52` 逐字重合的子句跨并实测字符数。

**裁决：A6 §三.1 验收数字（节点 522、边 1433、带 quote 锚点 921→923）与摘要 §3.1.1 的 SPEC 第 55 行删句主张，均移出本轮。未复核。** 前者需回查 `data/`，后者需 SPEC 原文，两者本片皆无。A2 的「全库 `(file,quote)` 使用者 0」同属需回查 `data/` 之列，本片不作装配依据。

## 二 执行序列

总原则（G1d §一）：**加/改在前，删在后**；改接只指向存活节点，删节点放最后；同文件内删除按行号**倒序**（G3e步13/14/15）；记账排在其依赖落盘步后。步1–5为**非落盘裁决**，阻塞下游。

| 步 | 类型 | 目标/ id | 依赖 | 验证 |
| --- | --- | --- | --- | --- |
| 1 | 裁决 | SPEC：`rel_note`能否用于非`other`关系（G3e步1） | — | 判不合法则步19/20走「原样不动」退路 |
| 2 | 裁决 | 自核`edges-H1.jsonl:7`新引文15.14.md:30单行连续+字符数（G3e步2） | — | 单行逐字命中、charlen 30–200 |
| 3 | 裁决 | `sections`须否条条有锚（G3b口径） | — | 未裁前步16的行6/行10**不删sections** |
| 4 | 裁决 | 待应用清单§2.2偏离+是否显式写`Examples 5 to 12`（G2c §11） | — | 主控确认；标**未复核**（未读§2.2原文） |
| 5 | 裁决 | `nodes-D1.jsonl:42`与`:46`双文本冲突（见文末） | — | 每目标只剩一份落盘文本 |
| 6 | 改写 | `report/audit-B2-verdicts.jsonl`：`:46/:49/:52` reason换G1c §1.1/1.2/1.3全文（G1d步骤0）；`:24/:5/:60` reason换G2d F5定稿；`:89` suggested_fix前三项执行、末项挂起（F7） | — | 前三行不点名本批19待删id；verdict/cause未动；node_type仍在SPEC.md:33四值内 |
| 7 | 撤回 | `report/audit-A4a-verdicts.jsonl:24` suggested_fix首项，无替代（G2d F1） | — | 余项不依赖"real number" |
| 8 | 记账 | `_D分片-主控核验.md:525`＋`_转交-新会话-workflow.md:293`同改100/89（G2f 1、2）；`审查-形式D-D2-片59-80.md:440`改「T2塌陷」（4）；`审查-形式D-D3-片21-45.md:89`删`:125`（6） | — | 两处100/89不再分叉；`:72/:74`未被误改（G2f 3）；D3额度仍1/3 |
| 9 | 改写 | `nodes-D1.jsonl:24` ← ADJ3 §3定稿（G2d F4）；`nodes-D2.jsonl:36` statement+atomic_reason（G2e 8）；`nodes-D4.jsonl:51`只改statement、`SQUARED`写小写（G2e 9） | — | `:24`首句不再是"mention real numbers"；后两条anchors/sections/parent未动 |
| 10 | 改写 | `inherited/nodes-A1.jsonl:8`：sections加`"15.03"` +第3锚66字符（G2e 7 A案） | — | 两项**同时**落盘（单加sections不消死因6）；池内现≥1条15.03 |
| 11 | 改写 | `nodes-D1.jsonl:42`整行1376字符、4字段（G2c §9） | 4、5 | json.loads往返、无换行；3锚实测138/160/146 |
| 12 | 改写 | `nodes-D1.jsonl:46`**冲突待人工裁定** | 5 | 裁定后单一文本落盘 |
| 13 | FIX | `nodes-X.jsonl:20`：第2锚换192字符串、增第3锚73字符、sections→`["15.14","15.15","4.1"]`（G2d F2 / G2a 4） | — | 第1锚100字符未动、OCR噪声原样；登记须写ADJ1孤本论证，**不得写成ADJ2悬空边论证** |
| 14 | 改写 | 5条锚点重挂1a–1e（G1d步骤1，强制前置：`apply_kills.py`不重挂） | — | 5条引文持有者≥1且全存活；1c字符数**落盘时实测重取**（G1b记57/190、G1d实测193，三数不得照抄） |
| 15 | 改写 | `nodes-H2.jsonl:1,4,5,6,7,9,10,11`整行替换（G3b §三） | 3 | 18锚json.loads过、逐字命中、30–200；行1锚2保留OCR "and nc on any other"；行9公式锚须由15.13.md直读串化不手打 |
| 16 | 改写 | `edges-H1.jsonl:4/:8/:12`换evidence；`:1/:13` origin→model并删evidence（G3a §三）。`:9` **移出本轮**（与`edges-A2-s2.jsonl:46`抢15.14.md:36，G3e D1） | — | 前三条单行逐字命中，`:8`不再与`d:15-10-nonzero-hypothesis-is-essential`抢15.11.md:7 |
| 17 | 改写 | `edges-H1.jsonl:7`整行（contrasts→requires并反向） | 2 | 步2判跨行则改用G3a E7原方案并登记缺口 |
| 18 | 改写 | `edges-H2.jsonl:1,7`（requires→generalizes）与`:10,13,14`（换ev）（G3c §三） | 15 | 核SPEC.md:29「is-a与generalizes互为反向只写一条」；edges.tsv无反向边；`:10` rel_note原样 |
| 19 | 改写 | `edges-X.jsonl:109,110,111,112,117,120`换quote（origin仍model）、`:112`同时改dst为`strang:projection-onto-a-subspace`（G3d §5.2）；`:115`→contrasts、`:116`→equivalent（§5.4，quote不变，本行不依赖步1） | 1 | 六条实测113/53/38/73/55/93、不跨行；`:112`/`:117`的rel_note落盘；`:116`改后重跑对称边去重 |
| 20 | 改写 | `edges-X.jsonl:108`、`:124`各加`rel_note`（G3e C2/C3，替代G3d的改指与撤回） | 1 | src/dst/rel/quote未动；步1判不合法则原样不动并记入提案 |
| 21 | FIX | `edges-A2-x2.jsonl:47`的contrasts语义并入src `apostol:cx-derivative-pairing-is-blind-to-constants`的statement（G1a §1.1要求，G1d步骤3未承接） | — | src已含该对比；**不做则步26静默丢内容** |
| 22 | FIX | `nodes-A2-x2.jsonl:12`/`:18`降级：留节点与锚点、收缩statement，**本轮不落盘statement文本**（G1d §二） | — | 两节点仍在、4条唯一引文仍有持有者；标`origin=model`（练习12(b)(d)解读未经数学核验） |
| 23 | 重指向 | 改接5条悬空边，rel/origin/ev_quote保原值：`edges-D1.jsonl:166/:191/:210`→`apostol:function-space`；`edges-CX2.jsonl:22`、`edges-D5.jsonl:9`→`apostol:zero-element`（G1d步骤2） | 10、14 | 5条dst全存活、悬空id=0；两新dst无同源同向重复边；`:167/:192/:211`**不改接**，留步26 |
| 24 | 击杀 | `edges-H2.jsonl:19,18,4,3`倒序（G3c §二）；`edges-H1.jsonl:15,14,11,6`倒序（G3a §二） | 16、17、18 | 撤H2的3/4前确认第5/6行仍在（T3上浮落点），edges-H2剩15、edges-H1剩11；`:6`撤后`edges-A2-s1.jsonl:8`的implies仍在 |
| 25 | 击杀 | `edges-X.jsonl:107`整行（G3d §5.1） | 19、20 | 同文件改动已落盘；`apostol:euclidean-space`余21边、`strang:orthogonal-vectors`仍有`:54`/`:121`/`inherited/edges-S1.jsonl:60,61,62`，不悬空 |
| 26 | 击杀 | 18条悬空边：`edges-D1.jsonl:` 104、141、150、151、159、160、163、164、196、197、200、201、204、205＋`edges-A2-x2.jsonl:47`＋`edges-D1.jsonl:` 167、192、211（G1d步骤3） | 21、23 | 悬空id=0；18条evidence无一离开全图（`:141/:159/:160`有存活持有者、`:104`由src自身锚点持有、余ev_charlen=0）。`edges-A2-x2.jsonl:50` **不在本步** |
| 27 | 击杀 | `nodes-D1.jsonl:5`恢复KILL；`edges-D1.jsonl:10/11/12`随节点进墓地、**不改指**（G2a 1、2） | 6 | 三个L1分组节点仍有第二条part-of（`inherited/edges-A1.jsonl:1/2/3`），不产生孤儿、无重复边；54字符锚点记为可接受引文损失。**顺序未定，需先确认**：2/4/4是否先并入`apostol:linear-space`再删（G2a不确定点，未复核） |
| 28 | 击杀 | 19个节点（A1 §1表#1–#15、#17、#18、#19、#21），作src的38条边随节点进墓地（G1d步骤4） | 6、14、23、26 | 悬空id=0（23条已5改接+18删除）；存活节点parent指向待删id 0个、structures-* 0处；括注应为「含**18**条自身part-of」（#21无parent） |
| 29 | 记账 | 悬空边24→**23**（`edges-A2-x2.jsonl:50` dst已降FIX）；A1 §〇「含22条part-of」→**19**；§3.1的8/9行/6条三方不符，以步23的**5条**为准（frag01 7、8）；A1 §1表末「三条维持的KILL」→**四条**（增列`nodes-D1.jsonl:5`），22 id表**不改**（G2a 5、6）；撤回`nodes-D1.jsonl:62`（G1d §三） | 23、26、27、28 | 各数字与diff逐条对齐，22 id表逐行未动 |
| 30 | 记账 | H-cross是**25**非24（`edges-A2-x2.jsonl:21`在X文件外），H层15+19+25=**59**（G3e〇）；`15.07.md:17`合并后4持有者、「新引入源行数=0」（C1）；edges-H1冗余度记**+3行**非+4条；两`ext-uncited-*`成孤点各排FIX | 24、25 | 计数与diff一致，两节点仍在`nodes-H2.jsonl:10,11` |
| 31 | 记账 | `report/audit-B2.md:107`「15.02有21」→「22」且同行删15.06；`:109`「12个未被D1触及」→「11」（G2d F6） | **顺序未定** | 取决于`nodes-D1.jsonl:71`的KILL是否本批：若执行则15.06重回未触及、原文变真，须**先删节点后订正且只做一次**。frag01条目1称「维持3」不在本批、frag02条目7称其可直接执行，均未给批次归属 |
| 32 | 记账 | 重跑`check_graph`核对计数 | 1–31 | **只读态不可执行**。分母**冲突未决**：G3d §5.7称916→930、G3e步17称916→914，基准不同须主控统一后再报数；1433→1424为自报未实跑，不作已核数 |

##冲突待人工裁定

- **`nodes-D1.jsonl:42`两份落盘文本互斥。** G2c §9要整行替换（statement 437→405、6子句→4、锚点1→3、动atomic_reason与audit_fix），依据是死因1与ADJ3的C5.3为假同时消除；G2d F4要statement换`裁定-ADJ3.md` §3定稿，依据是D13（末句假因果）已全核。
- **`nodes-D1.jsonl:46`处置方向互斥（一方删节点、一方改内容）。** G2a §二要保留KILL、十条「公理↔ R算术律」映射表先并入parent `d:example-1-the-real-numbers-form-a-linear-space`（KEEP）后删，依据是该节点冗余、parent存活；G2d F4+R1要替换statement（标未复核）并把映射句留在原节点标`origin: model`，依据是15.03全文grep `commutativ|associativ|distributiv`命中0、补锚无解。

##本片未覆盖

两项待SPEC裁决无法排序：G5-P18「边evidence能否算节点锚点」未裁前L66补锚A（100字符）挂起（G2f 5）；`15.10.md:22`（35字符）补锚撞SPECQ §5.2待议项。G3e §五与顺带发现为空占位，「可疑新增4条」无明细。`edges-X` 161行vs「162条对齐边」只登记待核。各碎片落选项自带理由不重列。

## 三 SPEC.md 的最终修订

按 SPEC.md（现 109 行）章节顺序。落笔须自底向上（`:100` 起、`:29` 末），四片给的局部次序已合并（C3-01 第5项、C3-02 第8项、C3-05 开篇、C3-06 第11项）。C3-04 已声明 P10/P11/P12 行号互撞，各自算出的总行数不可叠加。

#### 关系词表（`:12–30`）

1. `:29` 后新增三段（不扩词表／第二使用点不建边／`rel_note` 不限 `other`），逐字用 S3 §一代码块；再插两小节（词表扩充程序＋「复用不建边」三条分流，照抄 P11 `117–146`）。复用族实测 8 条，非 S3 的 4 条；若 P09 改判语义 B，分流第 1 条依据须重写。
2. 其中「不新增关系词」的论证**换 P11 版**：S3 原论证（`sections` 是数组、57 节点靠它记多节出现）在 P09 语义 A 下失效；P11 改用 L2 `members` 实测（24 个 L2、255 条成员引用、188 个不同下层 id，全部跨 ≥2 节）。取 P11 因其不依赖 `sections` 语义。

#### id 命名空间（`:35–46`）

3. 三处纯增量（`:39` 后插 2 行、`:45` 末合并追加、`:45` 后新增二级前缀小节），单源 P10／P17，无跨提案冲突，逐字来源与实数见未覆盖首条。

#### 锚点规则（`:47–57`）

4. **`:53`／`:53–54` 替换权——冲突待人工裁定（三方）。** P02 §二：整块替换两行为六条（可跨行、禁 `grep -F` 校验跨行 quote、文件内唯一、行间公式连断言引、边 evidence 同受约束）；依据假 quote 在 `15.02.md` 命中 52 行，`grep -F` 恰放行伪造。P04 改动1：`:53` 换 3 行，「最多 3 条」改锚点预算 `3+(sections 条数−1)`；依据 22 节点锚点未覆盖自己声明的 sections、其中 6 个已满额无槽，回溯违规 0。P18 §3.4：`:53` 条数界与 `:54` 长度界**明令不改**；依据保住上限正是其裁定目的（全量搬 T2 会让 86 节点破限）。P02 与 P04 可拼合（P02 第1条条数句换 P04 预算式），P18 与两者互斥。S1 只替换 `:54`，其第 5 条（退化中途起始 6 条）三片一致移出本轮。
5. `:54` 后插 3 行（同节点任两锚点源行集合相等即违规；同节点锚点总字符 ≤600），P04 改动2。源行去重使 **54 节点从合规变违规**（2 锚 41、3 锚 13），删冗余后无一跌破下限；600 界现零违规（有锚节点 max 463）。这 54 个入 FIX 队列。
6. **`:55` 改法——冲突待人工裁定（三方）。** P05 §三：删「或标 `origin: model` 并如实说明」，改「⇒ 不要收录」＋三条替代路径；依据 24 个 `origin: model` 节点里 20 个有锚（37 条），该字段与有无锚点正交，直译会一次性授权 24 节点无锚。P08 改动1：**保留**该分支，只改指为节点级 `origin` 并当场消歧；依据 `:55` 是节点 `origin` 唯一现存依据，指针不能悬空。P12：`:55` **不动**。此争未裁前第 7、11 项不能定稿。
7. `:57` 后新增「无锚豁免(唯一的锚点数量例外)」（P05 §二）：门槛为原理上不可锚（须给否定性检索式与零命中）＋四条必要条件（空 `anchors`／`origin:"model"`／`origin_note` 写明哪句无锚／新字段 `unanchorable` ∈{`external-proof`,`negative-claim`,`derived-criterion`}），缺一判死因 `6`；L2+ 不走；`:56–57` 整行原样搬入其末尾。回溯：`nodes-D2.jsonl:42`、`:54` 须补锚，`:72`/`:73` 只需加字段，现 546 节点 0 个带 `unanchorable`。
8. `:57` 后新增「证据池：四层，前三层入池；边的 `evidence` 不是锚点」。同处两版：S2 项2 与 P18 §3.1。**取 P18 版**——P18 自陈是在 S2 草稿上做的三处加强（T2 与锚点长度/上限规则不互通、未校验 model 引文禁入、冗余度只用「新引入源行数」度量），S2 版是其真子集。实测「0 锚点且 T2>0」=0、「0 锚点且 T3>0」=0，合规下限影响为零；全量搬迁会让 86 节点破上限。
9. **`:57` 后四处插入次序**（C3-02 第8项裁定，非提案原文）：第 7→P04-c→8→`verify_anchors.py` 覆盖范围条（见未覆盖），均须在 `## 节点 schema` 前，每步以内容定位而非原行号定位。P12 句级证据条与第 8 项同处证据池，两片未给相对次序，**待人工裁定**。

#### 节点 schema（`:59–66`）

10. `:65` 整行替换为 `### parent 字段（单义：仅 part-of；且是派生冗余字段）`（P07 修改点1，**取路线甲**：旧句改「仅 `d:` 节点可填，且有 part-of 父时必填」），`:66` 空行保留；四条机器校验入条文（甲下第1条 ⇔ 放宽为 ⇒）。实测 287 个带 `parent` 节点：一致 284、冲突 0、无 part-of 边 3、悬空 0；合规变违规 3、须改边 0。三例外走甲（`parent` 留空＋`atomic_reason` 写明 is-a＋各补 1 条 `is-a` 边）；路线乙不采纳（那三条 part-of 是伪造关系词）。
11. `:65` 后新增「节点的 `origin`」（`source|model|mixed`），逐字用 S2 项3 代码块，**落笔须把外层围栏改四反引号**（内嵌 ```json 会断栏）。**取 P08 的四处更正**：`origin`↔`origin_note` **双向**绑定（S2 只写单向）；须改总数 **60** 而非 S2 的 49（`10+16+49−15`）；补 §5.1 边界款（`origin: source` 只声明每句有原文承载，不声明那段原文即该对象的原始表述）；导出侧可见性款入条文。
12. `:65` 后新增「## sections 语义(出处,非相关性)」（P09，语义 A）：正向硬校验＋归一化＋「修复方向由审计定，不得默认补 `sections`」三句不可分拆（违规 12）；反向规则只作审计项、见证取 T1∪T2 排除 T3（22→20）；溢出条款（非 L2 且 >3 须写 `sections_note`，唯一触发者 `apostol:ext-vn-as-a-prior-vector-space`）。**取 P09 对 S2 的落点更正**：S2 总览表项4 的 `:59–65` 范围内无一行需改，只能新增小节。**未复核**：正向 12 例中 8 例提案自标未逐条读原文；「需补约 10 个」系抽查外推。
13. **`:65` 邻域硬排序**（C3-03 A 项，采纳 P07 §6）：第 10→11→12，P16 的「原子化下延」小节压在 `:67` 前；须同步更新 P08/P09/P16 的旧文对照栏。

#### 边 schema（`:67–76`）

14. `:70` 改字 `"rel_note":"rel=other 时必填;其它 rel 值在选词经过权衡时应填"`（P11，必做）：S3 遗漏此步，不改则与第 1 项「`rel_note` 不限 `other`」段矛盾；11 条非 other 带 note 的边由此从越界变合规。

#### 审计规则（`:89–103`）

15. **`:93–94` 两份合并整段替换**：P01 条文 C（提案 `156–161`：「引文真在书里」只排除 `3` 不排除 `6`；每条锚点须再问「它管到这句断言了吗」；「N/N 锚点已验证」永不得当正确性保证）＋P09 D5 后半（审计 A 续行后追加「并逐句核 `statement`…」）。须一次写完。
16. **`:95` 两份合并、只改一次**：P14 新逐字（审计 B 用删除测试判、对照集恒为**恢复基** `R(n)`、不得临场另选对照；P15 §三(2) 确认此句已含其核心）＋P16 §3.3 补行（向下拆分同归审计 B，按「原子化下延的边界」三条合取判，不得只凭主题判——按主题会误杀 17 个公理分解节点）。
17. **`:98` 整段替换——冲突待人工裁定（三方）。** P13：三档 `KEEP|FIX|KILL`＋三档表＋枢纽禁令＋孤儿检查（约 +40 行），依据 SPEC 原无 FIX 档（`grep` 0 行），P15 的 9 条 KILL 降级靠它落地。S3 §二：亦三档但另一版逐字文本，依据 `audit-L2-A` 的 10 个 FIX 已落盘、孤儿风险面 33。S2 项1 后半：**保留两档**（`KEEP` 配 `1`–`6`）。三者语义重叠不能同时落笔。C3-05 第11项裁「取 P13、否决 S3」；C3-07 第8项建议「取 S3 骨架、删 S2 那句、保留 cause `0` 供 KEEP 用」且自标为本片判断非原文——**P13 与 S3 之争仍未裁**。另 P01 条文 B 是段末**追加**四行（`0` 只配 `KEEP`；`FIX | 0` 是格式错误；`FIX | 6` 与 `KILL | 6` 以是否存在可引依据分界），P01 已让出整行替换权，对任一版本均可原样追加。
18. **`:100` 一次性合并改写，不得两次改同一行**，并入四份：P01 条文 A 全段（提案 `87–140`：新增 `0` 与 `6`、3/6/1 三环互斥表、`6` 的五步判定程序、T2/T3 塌陷分型表、`6-不可修` 标记法）；S2 项1 的 `0`/`6`＋三环判据表（**取 P01 版**，条文更全且已把 15.6% 写进条文）；P13 的 `0` 无死因仅需 FIX（已有 7 条在用）；P16 §3.2 给死因 `5` 加的向下形态括注。**P01 条文 A 须补排除句**「本判定程序不适用于 `L2:` 及以上抽象层节点」，插在第 1 步之前——原文只写在反例 3，照原样落地会让 24 个 `L2:` 节点全判 6（假阳性 24）。量化口径改源行：T1+T2+T3 源行 **263** 为正式口径，TD 误计入只洗掉 41 个＝**15.6%**；S2 的 51.9% 改记 52.3%（273/522）。
19. `:100` 下游：P08 C5、P17 闸门1/3、P18 条文、P12 第4条共四处引用死因 `6`，未入表前均悬空，故第 18 项为其前置；P18 §3.3「由 P18 改 `:100`」不采纳。P01 条文 A 硬依赖第 8 项须同批，否则须自带四层池完整定义表（原文 `173–175` 明写）。

`## 硬性禁止`（`:104–109`）无修订项。

#### 本片未覆盖

篇幅到限，下列已核项只列落点与来源：

- **单源纯增量、无跨提案冲突**：`:47` 标题加「;机器覆盖范围见本节末」(P06)；`:53` 后插「锚点条数与句数无对应关系」(P12)；`:57` 后另起段落 P04-c（三条界皆节点级，报零违规须同报行覆盖 74% 与 209%）；`:57` 后 P12 `101–128` 六条句级证据（无证据句以 `[model]` 开头而非补锚，TD 不入池，硬凑锚点判 6；回溯 188 节点加标记、新增锚点 0、KILL 0）；`:62` 改 `"statement"` 值(P12)；`:65` 后「## 原子化下延的边界」(P16 §二，逐字 `73–102`)；`:73–74` 后 P02 第6条（边 evidence 同受长度与唯一性约束，5 条 FIX 全可修）；`:74` 末追加(P18)；`:75` 尾追加(P07 B2)；`:75` 前插一行(P11)；`:76` 后「## 共享前提的记账」(逐字 P17 `127–165`，须附闸门3 下界标注与「逐句级判重未核」，且 P16 条件3 与 P17 闸门2 触发集重叠须先合并措辞)；`:87` 后 L2 附注(P09 D5)；`:95`–`:96` 间插 `R(n)` 六档表＋三禁令(P14)。
- **`:57` 后「机器校验的覆盖范围(`verify_anchors.py`)」**（P06 §二）三处必改：豁免2 补第三必要条件（否则放行第 7 项判须补锚的两节点；因现 0 节点带 `unanchorable`，脚本先落地会造 4 个失败，须与数据 FIX 同批）；`TOO_MANY_ANCHORS` 改 `>3+(len(sections)−1)`，**跟第 4 项 P04 一侧，若裁给 P18 须回退 `>3`**；补两行覆盖第 5 项两界。
- **审计程序细节**（P15/P13）：作用域恒为 `R(n)`、全图多算 89.9 倍（旧数「167 倍／漏 377 对」须回改 89.9／295 对）；批次前四道检查；枢纽禁令、FIX 门槛、孤儿检查的逐字与实数。
- **P03 两处改挂**：A 段须改挂为替换第 4 项里 P02 的第 5 条而非 `S1:81–83`；B 段须与 P02 第5条 (2)(3) 档合并。附 4 条源行 FIX 与事实纠正（定理 15.9(a) 在 `15.10.md:123`）。
- **`tools/` 与数据侧排序**：`export_obsidian.py:145` 修法 `quote.replace("\n","\n  > ")`；`edges-D1.jsonl:168`/`:212` 翻转须在枢纽豁免与删除测试之前；`:148` 改 `alias-of` 的 KILL 前置三处不一致（`待应用清单.md:28` 零残留／`G2a:153` KEEP／`G1d:288–290` 未核），**待人工裁定**。
- 短公式口径统一取 **35**（S1）无条文落点；`:98` 三方最终合并文本；`:57` 后第 8 项与 P12 句级条的相对次序。
- S1 待议项二～五、S2 项4–7、S3 §三–§六及附录：原文本身截断（仅剩占位注释），逐字文本不存在，一律「未复核」，未装配。
- P05 §5.1 残余漏洞（沾边引文绕过豁免门槛）无堵法；各提案第五、六节边界案例与「顺带发现」只登记不裁；`SPEC.md:33` 节点类型词表与任务书口径冲突，三片均移出本轮。

## 四 未纳入的提案条目

口径：转自 12 份碎片「落选项」小节。标「已核」处已回查 `待应用-提案/` 原文行号；未标处省去数字，报数前须回查原文。下轮：留／否（已否决）／挂（前置未裁，落定后重启）。

#### SPEC 类

| 来源提案 | 落选内容 | 落选理由 | 下轮 |
| --- | --- | --- | --- |
| P01 | 为 `L2:` 引入「TM 层＝members 池并集」 | 会成为伪抽象的掩体，作者自陈留主控 | 留 |
| P01／P16／P17／G3a | 节点类型词表与任务书不一致 | 四处均列「顺带发现」、无人认领；归 S2 字段语义，非 data/ 改动 | 留（冲突 3） |
| P01 | 未校验 model-quote 进证据池 | 本轮不产生错误免除，条文 A 无须加此规则 | 否 |
| P02 | 唯一性改「全语料唯一」；动 30 字符地板 | 前者 `file` 已限范围、改后凭空造 1 条回溯违规；后者抬到 33/35/40 各违规 2/8/32 条（已核 P02:229） | 否 |
| P02 | 回改交接文档 §3.9 及 SPECQ/S1 口径分歧 | 文档维护，非 SPEC 条文 | 留 |
| P03 | A 段触发条件写成正则 `^\([a-z]\)`；四行的 `n_sentence_end` 数据 | 前者自否：会把裸 `(a)` 习题选项一并放进来；后者自标「此项未核」、两表 join 全空 | 否／挂 |
| P04 | 取消 3 锚上限；或一律提到 5；P04-d 另立 `## 机器校验契约` | 取消后只剩单条长度界，可用 170 条锚把 `15.10.md`（7547 字符）整节搬进 `anchors`；提到 5 给 465 个单 section 节点（非 L2 的 89%）白发 2 槽而无一被 3 条卡住（已核 P04:287、290–291）；第三项与 P06 两处记账 | 否 |
| P06 | 「模型来源豁免」只需 `origin`＋`origin_note`；「3 锚上限若提高本条不动」 | 前者与 P05 门槛冲突、会放行须补锚的节点；后者对 P04 逐节点预算式不成立 | 否 |
| P06／P07 | `QUOTE_NOT_UNIQUE` 一行；`is-a` 与 `part-of` 同 (src,dst) 共存 | 前者以 S1 被采纳为前提、该片无 S1 切片（冲突 5）；后者自述属 S3 地界、本身未裁 | 留 |
| P07 | 禁 `parent` 指向 `L2:`；路线乙补 `part-of` 让 `parent` 自洽 | 前者当前无实例、提案明确不写进条文；后者那几条是伪造关系词，且凭空放宽死因 6 门槛 | 否 |
| P08 | `mixed` 粒度判据「按是否引入新数学内容」；L2 的 statement 是否含具体数学断言 | 前者自己不写进正式条文；后者标注未逐个核，无数可装 | 留 |
| P09 | 「同一命题第二实例」记 `sections` 还是记边；`ext:` 的 `sections` 偏语义 B | 前者自标中置信、与 S3 项 2 耦合未裁；后者自认靠 `sections_note` 打补丁「是妥协不是解决」 | 留 |
| P10 | C5 写成强制（既有节点须改名）；`cx-` 的 origin 与准入；`gen_manifest.py` 分桶 | 第一项按字样召回的 8 个节点所指全在章内，牵动 71 条边端点而真须改名 0 个（71 是误读风险量级，非改动量；已核 P10:275–284）；`cx-` 与 `ext-` 零重叠、留 P08／S2 项 3；分桶因 manifest 自动生成，非手改 | 否／留 |
| P11 | 方案 A：新增 `reused-at`/`recurs-in` | 与 `members` 双重记账，受益边少、成本两处 schema | 否 |
| P11 | 方案 B 单用（`other`＋`rel_note` 规范化） | 不解决「复用该不该建边」，语义推给自由文本 | 挂（兜底） |
| P11／S3 | 本轮扩 `violates` | 有数据支持，但 `contrasts` 已覆盖「成对辨析」一半；S3 亦自判列为下一轮 | 留（冲突 4） |
| P12／P13 | 「statement 每句都必须被锚点管到」；枢纽豁免本身 | 前者立刻违规 273/522（52.3%），须新造 338 条锚（921→1259），估 ≈250 条属 D 型硬凑，且与三锚上限不可同时满足（已核 P12:191/221）；后者 SPEC 从无此规定，会让无内容的中间层永久免死 | 否 |
| P13 | 作废《待应用清单》§2.1（ten-axioms 改边） | 原文自述归属不明 | 留（冲突 2） |
| P15 | §三(4) 回退版：`:95` 改窄为「父＋兄弟」 | 161/546 节点变 `NOT-TESTABLE`、51/85 条 node KILL 不合规（已核 P15:213） | 否 |
| S3 | 速查表「全图冗余另立死因」；§二 的 `SPEC.md:98` 版本 | 跨分支重复归 `0` 或 `6`，不新增死因号；`:98` 与 P13 版同处冲突，取 P13 | 否 |
| P16 | B2 例 3／例 4 改判；「锚点含住」「首句含运算词」两候选集入条文 | 前者属审计记账修正非 SPEC 文本、例 3 置信中判据待主控；后者自陈本轮无法复算，只配当信号 | 留 |
| P17 | 为闸门 2 加「`origin=model` 不得充当共享前提」 | 提案写明本条不加，留该待议项 | 留 |
| P18 | 由 P18 改 `:100` 加死因 6；三条短 evidence 的替换引文 | 前者自陈不负责定义 `6`，已改挂为前置动作；后者解法在 P02、提案自标「未核」 | 否／留 |
| S1／P02 | 退化中途起始（S1 逐字第 5 条＝P02「上下都越界维持不可引」同一批行） | 两片同判移出：行首是截断的词或半个 LaTeX 花括号，机器可过而人不可读，且不在任何已确证受损节点里 | 留 |
| S1 | 任务书「低于地板的行多数全局唯一」 | 换任何口径都复现不出，明写不采用 | 否 |
| S2 | 节点 `origin=source` 改必填；复核已有带 `origin` 的节点 | 前者须改节点数由 49 涨到 489（522−33）（已核 S2:449）；后者 S2 明说没有逐个复核、无实测支撑 | 否／留 |
| S3 | `edges-D1.jsonl:148` 改 `alias-of` | 宿主节点已判 KILL，边随节点进墓地 | 否（冲突 1） |

#### 执行类（`data/` 落盘动作）

| 来源提案 | 落选内容 | 落选理由 | 下轮 |
| --- | --- | --- | --- |
| G1b／G1d | 先把锚点重挂到别的节点再杀，以保 KILL 条数 | 接手方 statement 讲概念不讲该题，跨源重挂造 A/D 型盲区；两提案也不推荐 | 否 |
| G1a／G1c／G1d | `edges-A2-x2.jsonl:50` 改接；能否分步调用 `apply_kills.py`；§1.3 死因 6 观察（v:52） | 依次为：目标已降级 FIX 而作废（否）；只读态无法核、禁对该脚本用 `--help`（挂）；不改处置（留） | 三档 |
| G1d | §五 余项：KILL 死因判定与引文是否支撑宿主、「LOST」与 G1b 同名计数对齐、「维持 3」的悬空边耦合、某习题行删前无持有者、步骤 4 后 `check_graph` | 前四项三份输入均未核（两个同名数不得相加），末项只读态无法跑 | 留 |
| G2b | 全篇 FIX 方案（ten-axioms 由 KILL 改 FIX）；及三项附随结论：行长／行数更正、「S3 某处『信息含量为零』已过期」标记、「FIX 后不改指」的记账 | 主案载重前提「唯一汇聚父节点」被双表证伪，T3 塌陷说明内容合法下移；附随前两项随 FIX 落选失效（数值属已作废改写行；KILL 恢复后该表述与裁决一致），第三项动作虽同而理由不同 | 否 |
| G2c | 为 C5.2 另建新节点（`sections` 含 15.02）；顺带发现三条（备用锚位／疑挂错锚／无证据 model 边） | 前者论据为真但未核 15.02 有无可引整句，且删后全图无承担者；后者线索未展开 | 留 |
| G2c | #0＋#1 退路（否掉第三条锚点） | 立刻新造一个 E 型＋一个 D 型，提案自己不推荐 | 否 |
| G2a | 2/4/4 是否先并入 `apostol:linear-space` 再删 | 提案列为不确定点，须读《待应用清单》，未复核 | 留 |
| G2e | §1.4 B 案（`zero-element` statement 截为句 1） | 自荐 A 案，两案不可混用；B 案与「补 sections」指示相反 | 否 |
| G2d | F3：给 `inherited/nodes-A1.jsonl:12` 加第 3 锚 | 覆盖引文已在该节点 T2 池，死因 6 已免除；新引入源行 0（WONTFIX） | 否 |
| G2f | §1.5 给 L66 补锚 A；同改两处复述 D3 的半句 | 前者等 SPEC「边证据能否算锚点」裁决（冲突 5）；后者那两处措辞本身是对的，只需核对不要误改 | 挂／否 |
| G2d | D14（ADJ3 末句规律与分组统计） | 无落盘动作；只核了表内求和自洽，分解本身未重做 | 留 |
| G3a | `:6` 最小修法（保边、`origin→model`、删 evidence）；`:1` 备选「相邻编号例子也算源文并置」；`:13` 退用 15.15.md:39 作 model 边引文 | 依次为：与已有 `implies` 并存使 rel 语义含混；与 origin 尺子 `SPEC.md:73` 冲突；仍单侧，会塞进从不被 `check_graph` 校验那类边 | 否 |
| G3a | 三处新增边：`dependence-inherited-by-supersets requires …`、E14 `equivalent`、E15 `is-a`／`implies` | 前后两处超出「保留／修／撤回」范围、本轮不新增边；E14 另一端无节点，建不出 | 留 |
| G3a | 抽查 H1 之外的 `origin=source` `contrasts`；E2、E10 因「contrasts 需双侧证据」而改 | 前者超出该片范围；后者受「不跨行」限制取不到双侧引文，属 SPEC 议题 | 留 |
| G3b | KILL `ext-vn-as-a-prior-vector-space`；因层归属撤 `ext-…legendre…` | 前者与 `apostol:v-n-space` 不重合（后者记内容，前者记章外来源身份）；后者 H2 承担全部章外依赖、方向只一维，改 `origin_note` 即可 | 否 |
| G3d | L-1 `:108` 改指；L-2 撤回 `:124` 的 `contrasts` | 前者新 src 只覆盖引文一半射程，改后引文不支撑断言（死因 6）；后者是「同一公式两书地位相反」唯一载体，词表无对应单一词，择一必丢一半 | 否 |
| G3d | L-3 evidence 扩数组／新增 `evidence_dst`；L-4 SPEC 加「evidence 须逐字校验」并改 `check_graph`；L-9 顺带发现（疑缺 `is-a`、`node_type` 越词表） | 前两项属 schema／工具改动，非 `data/` 条目（L-4 已全过，追溯成本零）；L-9 属新问题 | 留 |
| G3e | L-5 新增 `orthogonal-to-a-set contrasts …complement` | 会与 `:108` 抢同一引文构成 B 型借用，已并入 `:108` | 否 |
| G3e | L-6 新增 `finite-basis contrasts strang:basis` | 须先裁 evidence 是否扩数组，否则单侧落地重犯同一错 | 挂 |
| G3e | L-7 Gram-Schmidt 补边；L-8 H-back 缺口 1 新建 ext 节点 | L-7 需 manifest 外的 Strang 锚点，属越界取证、只登记缺口；L-8 自倾向补 `origin_note`，判据未回读例 4 | 留 |

#### 碎片间冲突（不调和，待人工裁定）

1. **`edges-D1.jsonl:148` 宿主是否已判 KILL。** C3-07 依 S3 原文当既成事实；C3-04 依 P11 数据侧核查，称该 KILL 前置三处不一致（清单 KILL／G2a KEEP／G1d 未核）。
2. **《待应用清单》§2.1（ten-axioms 改边）。** C3-05 依 P13 自述「归属不明」挂起；C2-02 依 `edges.tsv`＋`partof.tsv` 双表已判其前提证伪、按否决落笔。
3. **词表分歧挂哪行。** C3-01／C3-06 记 `SPEC.md:33`，C2-04 记 `:29`（已核两份原文各自如此写）；须回查现文定其一。
4. **`violates` 条数。** C3-04 转 P11 实测值、C3-07 转 S3 值，两值不等（S3 偏小；已核 P11 §5.4 专为此口径差异设一节）；两边都主张移出本轮，分歧只在记账。
5. **两条的前置可能已消失（非矛盾，是解锁）。** C3-02 的 `QUOTE_NOT_UNIQUE` 以「无 S1 切片」移出，而 C3-07 已装配 S1 该条；C2-03 的 L66 补锚以「边证据未裁」挂起，而 C3-06 已落地「边 `evidence` 不算锚点」。均应重过。

**本片未覆盖**：「留」类未排优先级与依赖；各碎片自身「本片未覆盖」段不属本节。

11 份碎片「## 落选项」共 76 条，合并同型后 36 行。未标「已回查」者一律**未复核**，引数字前须回 `report/待应用-提案/<文件名>` 对应小节。

| 来源提案 | 落选内容 | 落选理由 | 下一轮 |
|---|---|---|---|
| `A7-H层整合.md` | 整份：直接并入 9 + 改后 5 + 丢弃 20 + 3 处新发现 | §1–§7 只以一行 `<!--SKELETON-->` 存在，全文 13 行 1284 字符，§0 汇总数字文内无支撑（已回查） | 否，改由 G3a–G3e 承载 |
| A7 | §0 机械合规结论（31 条边引文逐字命中、字符全落 30–200、无悬空 id） | 依据本应在未写出的 §1（已回查） | 是，转 G3 四份重做 |
| `A1-互证塌缩检查.md` | §3/§4/§5 全部处置：引文消失 7 条分 3 类、B2 `reason` 重写 4 条、§3.2 的 6 条重指 | 文件仅 146 行、止于 §2.2 后的 `<!-- MARK-3 -->`，三节在磁盘上不存在；§0 却把「6 条重指做完」列为 `apply_kills.py` 强制前置（已回查） | 是，重跑后半 |
| `A8-完备性评审.md` | §一 逐节缺口、§五 提案 | 全文 1005 字节，§零–§六 六节全为占位符，仅统计基线有实质内容（已回查） | 是，重跑 |
| A1 | §0「引文彻底消失 7 条」与「锚点轴塌缩 1 对」 | §2.2 标题与表给 9 条，第 144 行自称「§0 已改」而第 19 行仍是 7（已回查两行） | 是，改数后可引 |
| A1 | §0 算式「29 − 9 = 20」 | 9 个争议里 B2 判 KILL 只 7 个；29−7=22 成立，结论 22 可用、算式不得进验收单 | 否 |
| `A4-形式B漂移与借用锚点.md` | §3 末句「全图锚点 921 → 913（减 8）」 | 只算 B 组减 7、C 组减 1，漏 A 组净增 4；§7D 自给 918/917 | 否，验收用 918/917 |
| `A10-批次分层信号.md` | §6 小节编号 | `### 6.5` 出现两次（392、422 行），`6.4`（406 行）排在第一个 6.5 之后；仅形式，两节内容各自可采（已回查） | 是，重排后再引用 |
| A10 | 四种失效筛法：§4.2「S2 AND S3」合取、§3.2「S3 `nodecl` 单独作筛子」、「把 S2 当形式 B 检测器」这一读法、§6.5 按批次名（`D*` vs `A2-*`）分层 | 依次为：命中 34 而 25 个正例只覆盖 5、按定义排除唯一实证整批逃逸 CX2；命中 473/522=91%、正例占比 0.02、A10 自身亦不采；S2 检出的是形式 E（49 命中里已确认 20 个中 15 个登记形式 E 不判死）；D1 是最大批次也贡献最多 FP（`intra>=3` 的 30 命中里 15 来自 D1） | 否 |
| A10 | §5.1 第 3 条 `intra/|batch|` 归一化式 | 无小批次正例可定标，A10 明确不提出该式 | 待裁 |
| A10 | 在 §3.3 之上定 `intra >= 3` / `>= 4` 硬阈值 | Wilson 区间 [0.60,0.98] 与 [0.49,0.94] 重叠，只够定方向 | 是，补样本后定 |
| `A6-两处数学缺陷.md` | §1.4 备选（不点名 distinctness、1 锚不变）、§2.4 备选（去全称量词、不引 L11）；§2.5 把锚点 B2 换成 96 字符长跨 | 两备选与主方案互斥：§2.4 备选留 D 型无锚残留而第三槽本空，§1.4 备选启用前提（主控拒新增锚）未触发；B2 长跨的 `(file,quote)` 已被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 占用，会成三方共享并吃进 `:50` 自有内容 | 否 |
| A6 | §三.1 验收数字（节点 522、边 1433、锚点 921→923） | 需回查 `data/` 才能核，两片均只读提案 | 是，标未复核 |
| `G6d-D4-片28-54-摘要.md` | §2.6 / L48 强制换锚为 `15.15.md:17 + :23` | L17 的 152 字符全句是 `:50` 自有锚、L23 是 `:52` 自有锚，照原样换会造 B 型借用 | 是，先给不重合子句跨 |
| 同上 | §3.1.1：按 SPEC 第 55 行删 L30 末句 | 该片无 SPEC 原文，第 55 行未复核 | 是 |
| `A9-记账错更正.md` | 简报预设第 1 条「分片的 100 字符是错的、实际 89」；主控原路径「只改 `sections`、不补锚点」 | 前者两数各量不同子串、要改的是主控核验文件；后者甲裁下字段自相矛盾、乙裁下 cause 6 照样成立 | 否 |
| A9 | 「`:321–448` 全量重扫」逐条核 verdicts 的 reason 是否引 TD 边 | A9 自认未做 | 是，作缺口登记 |
| `A5-CX2与origin.md` | §7.4 里 16 个「非形式 E」节点补 `origin: model` | 锚点含 "Prove that / show that" 型题面、定性结论有锚，不满足形式 E 要件 | 否 |
| `A3-零证据节点锚点候选.md` | §5 对 `:72`/`:73` 的处置 | 只给甲/乙两条后果分析，未作单一建议；倾向的第三条路（Theorem 12.10 入 `source/`）自陈改变实验边界、不作建议动作 | 待裁 |
| A1 | §2.1 对 A 裁定依赖的「十条公理映射表并入 parent」 | `v:46` 属维持的 3 条、不在本批 21 条内，管辖权在 `G2b-十条公理FIX.md` | 是，G2b 承载 |
| `G6d-D1-片31-56-摘要.md` | §四 #6「13 个 `(file, quote)` 对」 | 表体只列 11 行，13 无出处 | 否 |
| `G6d-D1-片57-81-摘要.md` | R-2「该引文被 6 个节点引用」；#66 由初判 E 改判「支撑充分」；以「兄弟节点已承载」减轻 #60/#81/R-5 八节点；把 #72/#74/#78 计入 SPEC 计数（S-3） | R-2 实测 16 个持有者 = 7 节点 + 9 条边；#66 依据两条边 `rel_note`、不入四层池；兄弟不属 T1/T2/T3 任何一层；S-3 正文自陈成因是源材料范围 | 否 |
| 同上 | #57、#61 归 SPEC | 分片未读 `SPEC.md`，归因依赖未核文本 | 待裁 |
| `G6d-D1-片31-56-摘要.md` | 行 46、55 的「3 锚点上限致证据不足」；把 `15.02.md:51` 推荐给行 48 | 前者 `n_anchors` 均为 2、各剩 1 空位，归因偏软；后者该行 791 字符远超 200 上限 | 行 48 是，先定引哪子句 |
| `G6d-D3-片21-45-摘要.md` | §3 的 E-1/E-2/E-3 组表；§七 第 5 项「3 对片内 + 2 对跨片」 | 前者多算 L37/L42、漏算 L26/L40，20 靠错误抵消凑成；后者实测 `15.11.md:98` 有 8 个持有者、`:133` 7、`:105` 6 | 否 |
| `G6d-D3-片46-69-摘要.md` | 形式 D 指控 D-5(L61)、D-6(L50)；L46 claim 3/4 与 L47 claim 3 降级；§四 #2/#3「23/24 含无锚 claim」「22 个末句无锚、唯一例外 L66」 | D-5/D-6 T2 塌陷（`edges-D3.jsonl:148`/`:49` 的 evidence 逐字含该内容）；L47 两条 T2 evidence 与 T1 相同、去重后源行仍为 1；计数正确为 24/24，末句例外实有两个（L66、L59） | 否 |
| `G4-完备性-15.02/15.03/15.04/15.11/15.14.md` | 低于 30 字符下界、无合法引文的公式行与残片：15.02 四条展示等式 S13/S16/S18/S20（16/18/22/22，S13 = `x + (- 1) x = O.`，已回查 15.02:49「实测 16 字符 < 30」）、15.03 S12（27）、15.04 S32/S33/S34（11/14/28）、15.14 S21（16）、15.11 S10（26）/S27（13） | 结构性不可锚；15.02 一组已由 `SPEC缺陷-锚点规则量化.md` §2.3.1 登记归 `G5-P02`，其余节点已存在、内容不缺 | 随 G5-P02 处理 |
| `G4-完备性-15.01/15.02.md` | 3 锚位已满仍想补锚：`apostol:linear-space` 补 nonempty 句为第 4 锚（15.01 §五抽查 3，charlen 未核）；`apostol:axioms-for-multiplication-by-numbers` D 型命中判保留（15.02 §五抽查 3） | 根因同为 `G5-P04-3 锚点上限是否提高.md` 未裁 | 是，阻塞于 G5-P04 |
| `G4-完备性-15.06/15.07/15.08/15.11/15.12/15.14.md` | L2 节点「声明某节却无该节锚点」：06/07/08 计 3+5+9 个、15.14 §四不一致 2 计 13 个、12 抽查 2 与 11 §四 B−A 计 10 个 | 按设计无锚、靠 `members` 挂载（15.14 侧引 `tmp_index/README.md:21-22`「24 个 L2 节点」）；三处互为重叠子集、口径不同，非矛盾 | 否 |
| `G4-完备性-15.07/15.10/15.11/15.12/15.13/15.14/15.15/15.16.md` | 过渡叙述、纯记号、图注、OCR 残骸类建节点或补锚：15.13 FIX 6 图 15.1 几何解释 L93=84 字符（原文自标「不进 SPEC（可选）」，已回查 15.13:238）；15.14 六句；15.11 S5/S30/S32/S48；15.12 十七处；15.10 S3/S62/S5；15.15 §三 回引句 163 字符；15.16 单元 28；07 §三 (15.1)(15.2) 与 S30 | 无独立数学内容，或本体已由既有锚点/`cx-` 节点承担，另建即复述 parent（死因 5） | 否 |
| `G4-完备性-15.09.md`、`G4z-缺口清单.md` | 09 提案 7 / S4-10 `apostol:cx-three-generators-of-a-shifted-basis-span-only-a-plane`（`15.09-exercises.md:35`，95 字符） | 提案自评「置信度：中……是不是真陷阱有主观成分，若主控认为属过度开采可降为不建」（已回查 15.09:181）；缺口清单亦排末位 | 待裁，两份口径一致 |
| `G4-完备性-15.08/15.10/15.11/15.12/15.15.md` | 死因 0、原文明示保留、无待装配动作：15.15 §五抽查 3（a1 93 字符）；12 抽查 2、11 §四 B−A 跨节引用者、10 §四(甲) 3 节点；08 §四(三) `apostol:spanning-set` 与 `d:finite-dimensionality-supplies-…` | 原文判「倾向保留」，后者是「sections 声明须有该节锚点」豁免的样本 | 否 |
| `G4-完备性-15.04/15.05/15.06/15.08/15.09/15.11.md` | 超出本轮射程：08 §四(二) 3 个 `n_anchors=0` 且 `origin=model` 节点；05 四项（11 个 `cx-` 节点 `node_type` 全 concept、该 11 节点 part-of 全孤立、反例层 §1 称 28 边而 ev_file 落 15.05 仅 1 条、第 32/27 题补锚未给 id）；06 §四 3 条 B 型候选 + 三节顺带发现 10 条；15.04 顺带发现 `node_type` 出词表外 `method`/`notation`；09 抽查 2 的 `origin_note`（字段不在索引内）；11 S30 与 S44 引导句口径不一致 | 属节点自身证据缺口而非覆盖缺口，或只列候选未给动作 | 是，须另立工单 |
| `G4z-缺口清单.md` | 按任务书补建「15.15 例 2 Legendre 节点」 | 前提被推翻：`apostol:legendre-polynomial-approximation`（`data/nodes-A2-s3.jsonl:6`）与 `apostol:best-linear-approximation-to-sine`（`:7`）已存在 | 否，不执行 |
| `G4z-缺口清单.md`、`G4-完备性-15.13.md` | S3-2 / 15.13 A3 原形态：新建 `apostol:normalized-legendre-first-six-explicitly` | 死因 5 风险最高（可由 parent 的 φₙ=yₙ/‖yₙ‖ 机械导出）且 `15.13.md:165` 整行 246 字符超界；两片一致降级为 FIX，两锚补给 `apostol:normalized-legendre-polynomials` | 否，改走 FIX |
| `G4z-审计覆盖形状.md`、`G4z-盲区抽查合并.md` | §一 逐节量化表与 §二 相称性判定；15.14 的 Figure 15.2 几何直观丢失；把 71%–98% 当全章误报率外推；本轮击杀名单 | 前两项属背景口径（原文自述不重判缺口）或真实但不可弥补；后两项原文明确拒绝点估计、抽查 45 条里 KILL 0 条 | 否 |

`_frag-C6.md` 落选项为「无」。碎片间未见须裁定的矛盾。

## 五 未完成或缺失的提案

本轮应补项，括号内为影响层。**理由与逐字锚点文本未抄**，落盘前须回 `待应用-提案/` 对应小节取；字符数系提案实测，标「未复核」者不得据以落盘。

**15.01**（D1）`d:objects-already-met-that-can-be-added-and-scaled` 缺第 3 锚 142。

**15.02**（D1）`d:only-axiom-1-states-that-the-result-is-unique` 第②句「in every example」零支撑（全节不含 example）。

**15.03**（ext）`apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 缺 15.03 锚（140，EXAMPLE 12），或 sections 收缩为 15.08。

**15.04**（D1）`d:thm-15-1-two-candidate-zeros-swap-roles-in-axiom-5` idx=0 锚前伸至 118（S8 全图 0 持有者）；连带 `edges-D5.jsonl:1` 的 144 字符 evidence 不再同文（未核）。`d:axiom-5-…-right-neutral-element` sections→`15.02,15.04`。

**15.05**（新建/D1/反例层）新建五条：R⁺ 乘法 142；数列空间 31+34；级数空间 31+42；deg f≤deg g 103；变系数 ODE 解空间 93+75（**禁跨行拼接**）。`d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 首句「Apostol does not remark…」与 `15.05.md:63` 第 30(a) 逐字冲突：删句 + sections 加 15.05 + 补 65 锚。反例层逐题表第 17 行改「仅覆盖常系数」（statement 逐字写 given constants）。

**15.06**（D1）`d:thm-15-4-identities-inherited-from-the-ambient-space` 增 66 锚与 #0 并存（前移版 207 超上界，**不可用**）。与 15.08 并案：`d:finite-basis-two-clauses-are-logically-independent` 两提案独立同判死因 6，取 08 换 110 定义句锚 + 06 处理第二句，均不主张 KILL。

**15.07**（D2/H1）F1/F2/F3 补 100/75/56；`edges-H1:15` 换 Example 2 引文；OCR「and nc…」入清单。

**15.08**（D2/ext）P1 增 164（现锚 122 把集合列举切在锚外，n_holders=3 故不换锚）；P4 增 131；P2 增第 3 锚（宜 132 自足版）。

**15.09**（新建/CX2/反例层）新建 A1–A7（ex22(c)(d)(e)、24(a)(b)、23(b)(d)、ex12/17）65/56/46/72/65/67/40；A6 独立性结论与 A7 两维数值标 model，A7 statement 限缩 ex12/17。`cx-degree-exactly-k-inside-p-n-still-fails-closure` 第 (ii) 段引文不含：标 model 或补 15.03 锚。更正反例层 §4「30 字符下限漏收 0」：ex11/16/23(f)/23(i) 与 12 的 S42（24）均低于 30，现 SPEC 下无合规锚。

**15.10**（D3）`edges-D3.jsonl:171` dst 改 `d:example-6-all-polynomials-as-a-function-space` + 补 62 锚（例 5 无次数上界，A 型对象错）；同类边未核。

**15.11**（D3/D4）`d:trigonometric-orthogonal-system` 补 a2/a3 51/52（本节唯一实质内容零覆盖，不建节点）。`d:parseval-requires-an-orthonormal-basis` 断言 (iii) 源文无文字、origin 空。`d:normalized-element-has-norm-one` 持 15.11.md 的 a2（75）而 sections=`15.10,15.13`：补 sections 或撤该锚，**不能都不做**。`d:15-10-nonzero-hypothesis-is-essential` 反例 `{O,i,i+j}` 本节无此例（只见 `15.06.md:21`）、origin 空。

**15.12**（新建/CX）新建 A8 sesquilinearity 86+155（习题 15 零覆盖；死因 5 须与审计 B 对齐）、A9 polarization 56、A10 三函数 66（π/6 与 π/3 配对自算、L61 未点名，标 model）。`…convergence-must-be-proved-before-the-axioms` 补第 4 锚 48。`cx-l2-sequence-space-…`「定理 15.11/15.12 假设有限维」分句无 T1 无 T2。`cx-dropping-the-conjugate-destroys-positivity` 的 f≡i 未标 origin。

**15.13**（新建/A2/D4/ext）新建 `apostol:ex-gram-schmidt-in-v4-detects-a-dependence` 156/156/181（掩码 0 六单元含两无归属断言）。(15.15) 本体（E11,L80）全图零锚零边而 `apostol:gram-schmidt-process` statement 逐字写「(formula 15.15)」：补 155 截取版（整行 217 超界）。`…legendre-first-six-explicitly` 降 FIX：两锚 166/95 归 `apostol:normalized-legendre-polynomials`（死因 5 + L165 整行 246 超界）。另四条补锚 72/82/124/51。`apostol:orthonormal-basis` 补 L117=156，**与首条二者择一**（同一引文）。

**15.14**（D4/A2-s1）`apostol:theorem-15-15-orthogonal-decomposition` 补 (15.16) 锚 111（`\tag{15.16}` 在 anchors.tsv 与 edges.tsv 均 0 行）；`d:orthogonal-decomposition-uniqueness` 补 L50=59（须按句截）。`d:orthogonality-to-generators-extends-to-their-span` sections→`15.13,15.14`；同条「不建议 KILL」**未复核**。`apostol:projection-along-an-element` 三锚全在 15.13.md 而末句属 15.14：补 76 + 退化子句标 model 或删。

**15.15**（A2-s3/D4/SPEC）**须先于其他动作**：撤回任务书前提「例 2 Legendre 无节点」——`apostol:legendre-polynomial-approximation`（`nodes-A2-s3.jsonl:6`，3 锚 8 出边）与 `apostol:best-linear-approximation-to-sine`（`:7`，3 锚 3 出边）均存在，不撤回则重复建节点。例 2 投影公式在 921 锚 + 1433 边里零命中而 statement 明文断言：补 L56=155（例 1 同位 (15.21) 有锚，漏做非设计），连带立「逐子句证据覆盖」判据。另五条补锚 66/46/114/117。SPEC 例外：唯一逐字载体短于 30 的独立公式行允许无锚（15.13 E12=26 为第二例）。

**15.16**（CX/新建/SPEC）`…orthonormalizing-a-dependent-list-…` 补 78（四组向量只 1(b) 无锚）；`…normalizing-constant-breaks-at-the-index-zero` 补 81（该行 145、现锚只吃前 36，「A 与 B 不同」只锚 A）。新建 `apostol:cx-exponential-weight-produces-a-laguerre-family` 56/116 + 两条边（四表 `grep -ic laguerre` 皆 0、六对内积验算全 0）。一条 202 字符候选锚超上界（`G4z-审计覆盖形状.md:211`），无法靠延长既有锚解决。

**跨节** **`A8-完备性评审.md` 正文整体缺失**：全文 1005 字节，§零–§六全为占位符（§零/§一 只写「正文见…」，§二–§六 无字），前言却称「全部 16 节走完，无静默截断」；逐节缺口、丢弃 vs 未发现、严重度排序、提案四块全未产出，**须重跑**。全章分母 522 节点 + 24 L2 = 546、边 1433。

**被阻塞/移出的 18 项，只列要件**：15.03 补满 7 锚超上限 3（阻塞于 `G5-P04`）；15.07 两节点删 sections 或补边（**不得单删**）；15.10 短式 21/26/29 待 30 字符例外；跨节 `node_type: example` 未裁（全图无一 example，阻塞 6 条提案）、21 条新建工单（严重度 2/1/3/15）、32 条 FIX 未排序、进 SPEC 四条、九项复核工单；其余十项未列名。

#### 冲突待人工裁定

1. `d:linear-space-is-a-set-together-with-two-operations` sections 三方互斥：15.01 要 `15.01,15.03`（据定位性引用）、15.03 要 `15.01,15.02,15.03`（据本节内有定义引文）、15.02 要删 `15.02`（据锚点 file 归属）。三条 FIX 互覆。
2. `d:axiom-5-…-right-neutral-element` 方向相反：15.02 侧补第 3 锚 76 保「Theorem 15.1 onward」（据 T1 两条 + T2 三条全在 15.02.md）；15.04 侧改「Theorem 15.2 onward」（据逐句读过两定理证明）。同节点均判 D 型轻度却分记死因 6 与 1，边界未校准。
3. `sections` 语义：15.01/15.02 侧按 `S2-SPEC-字段语义.md:35` 项 4 已裁改判；15.09–15.12 侧未定、二择一（10 侧 70/76，11 侧 A−B=1/B−A=12）；15.08 侧要新立「每声明小节须有该节锚点」。三者不能同时成立。
4. 15.16 Laguerre 定位：15.16 侧判「部分回收非新缺口」入 `丢弃与未覆盖清单.md` §一 第 5 题（据六对内积全 0）；缺口清单侧倾向升严重度 1（据「全图零提及」压过「来源是习题」）。
5. 齐次条件族（S4-11）：15.09 判 Pₙ 上齐次条件值得建；15.05 §三 归为 `x:solution-set-of-a-homogeneous-linear-condition`(15.03,3.3) 实例、不建。差别只在载体，并存即双记账。

#### 审查覆盖本身的缺口

1. **「形式 D / D1 片 1–30 无分片文件」不成立——覆盖在 `report/审查-形式D-D1.md` 自身。** 已按文件名核对 `report/`：D1 侧只有主文件与 `片31-56`、`片57-81`，确无 `片1-30` 文件；但主文件 §1 把行 1–30 记为「审在本文件 §2 批次 1–3 + §4 前 11 条」，并给闭合核查：30 个 id 中 29 个逐字出现，未出现的 `d:axiom-7-associative-law-for-multiplication-by-numbers`（行 16）在 D-13 按「节点 16」裁决，账目 22 + 8 = 30 闭合。**真实缺口是口径不是覆盖**：三区间判型互不相同（1–30 用 S/L、31–56 允许兼 D 与 E、57–81 用 D/E 互斥），不可加总，唯一可加总轴是节点级动作。
2. `G4-完备性-15.13.md` 缺三/四/五三节（已核：只有〇/一/二与「顺带发现」，无锚点抽查），故盲区抽查实得 45 条非 48 条，**15.13 抽查须补跑**。
3. **本片未覆盖**：45 条抽查只覆盖 44 节点（546 的 8.1%）与约 4.9% 锚点、非随机，只作形态学证据；按 `anchors.tsv` 随机重抽**本轮未做**。另八类已确认但未成条目：15.04 `d:thm-15-3-…` 第二句 E 型；15.05 二十一道正面例题无节点；15.09/15.12 的 OCR 回填三项；15.15 两条锚点抽查；15.02 的 84.6%、15.15 的 76.7% 系已达上限的满分（要求补齐即违硬禁令 2）；15.10「123 锚点全过机器校验而 3 处选样失配」口径；被阻塞十八项中十项；D 型截断候选 179 条、B 型 `shared-quotes.tsv` 480 组自我共享剔除。四份 §一 逐句覆盖表（05 47/18/29、06 37/34/27/7、07 45/37/8、08 37/34/3）按摘要转述，**未复核**；05 §五抽查 2/3、08 §五三条、06 §四表体未读全；A8 §一/§五 原文为空，无法转条目。

## 六 执行后必须重跑的检查

# _frag-C6：data/ 改完之后必须重跑的检查

取证范围：只做了两步（列 `tools/` 文件名；对 6 个校验类脚本各读开头 40 行）。
40 行以外的内容、`report/待应用-提案/` 下任何提案，本片都没读。凡下文标「未复核」的，
落笔前必须回查提案原文对应小节。


#### 1. `tools/verify_anchors.py`
- 输入：`python verify_anchors.py <nodes.jsonl> <source_dir> [更多 source_dir]`。位置参数，**一次只吃一个 nodes 文件**。
- 判错：每条 anchor 的 `quote` 必须在其 `file` 命名的源文件中**逐字连续**出现；不满足即计入 failures（记 id / file / quote）。另单独统计「无锚点节点」与 `bad_json`。
- 何时重跑：任何 nodes 文件的 `anchors`（quote 或 file 字段）或 `statement` 之外的锚点结构被改动后，对**该文件**重跑；换源文件（`source/` 下 .md）后对全部 14 个 nodes 文件重跑。
- 通过判据：脚本自述「这是本实验唯一的硬校验，只判断这句话是否真的在书里」。ok/total 与退出码的具体口径在 40 行之外，**脚本未声明（首 40 行内）**；`verify_anchors.py` 判失败的范围本身有提案在争（`G5-P06-verify_anchors.py 判失败范围.md`），**未复核**。

#### 2. `tools/check_graph.py`
- 输入：`--nodes data/nodes-*.jsonl --edges data/edges-*.jsonl --source source/apostol-ch15 [source/strang-ch3 ...]`。
- 判错（docstring 四项）：id 唯一性、边端点可解析、边 `evidence` 逐字校验、词表封闭。词表在文件里写死：`REL_VOCAB` 10 个（is-a / part-of / requires / implies / equivalent / contrasts / applies-to / generalizes / alias-of / other），`SYMMETRIC` 3 个（equivalent / contrasts / alias-of），`NODE_TYPES` 4 个（concept / method / theorem / notation）。
- 何时重跑：**改任何 nodes 或 edges 文件之后都要重跑**（它是唯一覆盖边一侧和跨文件全局一致性的检查器）。新增关系词、新增节点 type、删节点（会让边悬空）尤其必跑。
- 通过判据：4 类问题均为 0。退出码语义在 40 行之外，**脚本未声明（首 40 行内）**。

#### 3. `tools/check_layer.py`
- 输入：`--layer 2 --candidates data/structures-L2-*.jsonl --nodes <节点池，含 data/inherited/nodes-A1.jsonl 等> [--lower ...]`。
- 判错（docstring 明列 9 项）：`ID_DUP`、`ID_PREFIX`（id 前缀与 --layer 不符）、`MISSING_FIELD`（9 个必填字段 id/name_zh/name_en/layer/statement/members/boundary/abstraction_gain/sections 缺失或为空）、`LAYER_MISMATCH`、`FEW_MEMBERS`（成员 < 3）、`DANGLING_MEMBER`（成员 id 不在节点池）、`DUP_MEMBER`、`FEW_SECTIONS`（sections < 2）、`SELF_MEMBER`。
- 不算错误、仅报告：单层取材集中度（阈值 0.85）、候选间成员重叠（Jaccard ≥ 0.5）。
- 何时重跑：改 `data/structures-L2-a/b/c.jsonl` 之后；**以及删任何 L1 节点之后**——节点池缩小会把 L2 成员变成 `DANGLING_MEMBER`。
- 通过判据：9 类错误为 0；两项报告项非 0 不算失败。

#### 4. `tools/check_shared_quotes.py`
- 输入：`--nodes ... --edges ...（含 inherited）[--min 2] [--strict] [--out report/shared-quotes.md]`。
- 判错：列出被 >1 个节点或边共用的 (file, quote) 组。脚本自述只覆盖 A3 分类里的**形式 B（借用引文）**，形式 A/C 不可机械化；**非 0 计数是预期的，不算失败，退出码保持 0，除非传 `--strict`**。
- 何时重跑：改动 anchors/evidence 的 quote 文本之后（含把某节点的锚点换成别人的、或新增借用锚点）。
- 通过判据：脚本明说不是 gate；只能人工读输出。A3 手查 11 组、9 合法 2 漂移这一数字来自本脚本 docstring，非提案，可引；若要重述漂移结论仍需回查提案，**未复核**。

#### 5. `tools/check_unanchored_numbers.py`
- 输入：nodes（`statement` 里的数字 token 与该节点自身 anchor quote 比对）。
- 判错：statement 中出现、但在该节点任何锚点引文中都找不到的数值 token。过滤器 `REFWORDS`（axiom/theorem/exercise/example/section/chapter/... 共 20 个词）+ `LOOKBACK 44` 字符用来排除书内编号回指。
- 何时重跑：改动任何 nodes 的 `statement` 数值或其锚点之后。
- 通过判据：**没有通过判据**。脚本明写「Exit code is 0 regardless of findings; this is a report, not a gate」，且「deliberately a triage tool, not a verdict tool」，会过报，输出是给人读的。

#### 6. `tools/audit_coverage.py`
- 输入：**硬编码相对路径**，无 CLI 参数：`data/nodes-*.jsonl`、`data/inherited/nodes-*.jsonl`、`data/edges-*.jsonl`、`data/inherited/edges-*.jsonl`，加 `report/audit-A1-verdicts.jsonl`、`audit-A2-verdicts.jsonl`、`audit-B-verdicts.jsonl`。→ 必须在实验根目录下跑。
- 判错：不判错，只报覆盖形状——节点/边总数、每文件计数、按源文件分的 A/noA、B/noB 覆盖表，以及 orphan（裁定里有、图里没有的 id）。docstring 自述还报 Audit B 的 KILL 爆炸半径。
- 何时重跑：删/加节点或边之后（会改 orphan 与分母）；以及新增或改写任一 verdicts.jsonl 之后。
- 通过判据：**脚本未声明**（是报表）。orphan 非空是最接近「异常」的信号，但脚本未把它定义为失败。

#### 7. 本轮已知会变动的数据文件 → 需重跑的检查器
清单本身**未复核**：具体哪些文件在本轮被改，写在 `report/待应用-提案/` 各提案与 `report/待应用清单-v2.md` 里，本片按硬约束没读。下表是**按文件类别**的映射，依据是上面 6 个脚本自己声明的输入，可直接引用：

| 变动的数据文件（`data/` 下） | 必须重跑 |
|---|---|
| 任一 `nodes-*.jsonl`（12 个）或 `inherited/nodes-A1|S1.jsonl` 的 anchors | verify_anchors（对该文件单跑）、check_graph、check_shared_quotes、check_unanchored_numbers |
| 任一 nodes 的 statement / type / id | check_graph、check_unanchored_numbers、check_layer（id 变会致 DANGLING_MEMBER）、audit_coverage |
| 删节点（击杀，落 `graveyard/killed-nodes-auditB.jsonl`） | check_graph（边悬空）、check_layer（成员悬空）、audit_coverage（orphan/分母） |
| 任一 `edges-*.jsonl`（14 个）或 `inherited/edges-A1|S1.jsonl` | check_graph、check_shared_quotes、audit_coverage |
| `structures-L2-a|b|c.jsonl` | check_layer、（若 L2 带锚点则加 verify_anchors，是否带锚**未复核**） |
| `ID-MANIFEST.md` | 无检查器读它；它是 `gen_manifest.py` 的产物，需重新生成而非重新检查（**未复核**：生成脚本未在本片取证范围内） |

两处从 `ls` 直接看到、会影响重跑计划的事实：`data/` 里有 `edges-H1.jsonl` 但**没有** `nodes-H1.jsonl`（H1 的节点端点落在别的文件里，check_graph 的 `--nodes` 必须凑全否则误报端点不可解析）；`verify_anchors.py` 一次只吃一个 nodes 文件，所以「全量重跑」= 14 次调用。

本片未覆盖：`apply_kills.py`、`repair_dangling.py`、`gen_manifest.py`、`export_obsidian.py`、`graph_stats.py` 未读（非校验用途，属改数据/出报表）；6 个检查器 40 行之外的退出码与阈值细节未读；本轮实际变动文件清单未读。
