# 装配片 C3-04 · P10–P12

行号均为 **SPEC.md 110 行**原始基线；P10 插 `:46`、P11 插 `:30`、P12 插 `:58`，三处互撞（各自算的「变 141 行」「变约 137 行」不可叠加），落笔顺序须主控重算。
长条文不在此重排：下文给出提案内**逐字区间**，落笔时整段照抄，不要转述。

## 主体

**1. P10 C1｜命名空间表增两行（采纳）** 插在 `:39`「`apostol:`」行之后（非表末，令二级前缀紧跟父前缀）。逐字：
`| `apostol:ext-` | 第15章**之外**(其它章 / 其它卷)的对象,但第15章在正文里点名征用了它 | H-back 代理 |`
`| `apostol:cx-` | 由第15章习题(`*-exercises.md`)导出的节点 | H-in / CX 代理 |`
理由：旧表严格读法为闭表，60 个 id（`ext-` 11 + `cx-` 49）现不属任何已登记命名空间。

**2. P10 C2+C3｜`:45` 之后插入小节「### 二级前缀 `apostol:ext-`：章外依赖的记账位」（采纳）** 照抄提案 `102–119`（C2）与 `123–128`（C3）。要点：三条准入 = 所指在章外（回指第12章或前向外包第II卷皆可）／征用点在章内且 ≥1 锚点落 `source/apostol-ch15/` 正文、受 30–200 与逐字连续、**不得用章外原文当锚点**／`statement` 只写「第15章索取了什么」，陈述未持有的章外原文判**死因 3**；`sections` 按引用句所在节填；`ext-` 不是给章内节点造第二个 id 的通道，内容重合判**死因 5**。理由：11/11 通过（16 条锚点 charlen 56–167、`has_nl` 全 0、0 锚点节点 0 个），任务书「ext- 是虚构节点」被数据否掉。

**3. P10 C4｜`ext-` 与 `origin` 正交（采纳，本项实质裁决）** 逐字（提案 `133–143`）：

```
`ext-` 与 `origin` **正交,互不推导**:前缀说的是**所指在哪里**,`origin` 说的是
**这段 statement 的内容从哪来**。故:

- **`ext-` 节点不必标 `origin: model`。** 若第15章正文里有点名出处的引用句
  (「Theorem 12.8」「Section 12.16」「in Volume II」),则「该对象在章外」这一判断
  是第15章自己说的,`origin` 取 `source`,锚点即那句引用语。
- **`origin: model` 只用于第15章未给任何出处的借用**:第15章直接使用了一个它没有证明、
  也没有指向任何出处的事实,而「它来自章外」是我们的推断。此时 `origin: model` 必填,
  且 `origin_note` 须写明:锚点是**断言处/使用处**而非引用语,判其为章外是推断。
- **`origin: model` 的 `ext-` 节点不得指名章外的具体定理编号。** 无原文可查而指名,
  判死因 `3`。
```

理由：与现状 9 `source` / 2 `model` 完全一致 0 变更；反向规则「所有 ext- 必须 origin:model」会让 **9 个节点从合规变违规**，这是否掉任务书猜测的量化理由。

**4. P10 C5 + `:45` 行末追加（采纳）** C5 照抄 `148–152`：`ext-` 是可选声明，不用不违规，既有节点不改名（`:45` 末句「引用已有节点必须用它的原 id」优先）。`:45` 追加最长前缀警告（照抄 `195–199`）：`apostol:ext-x` 属 `apostol:ext-`；按 `apostol:` 分桶的工具会一并收进（现状 `tools/gen_manifest.py:15`）；匹配须带冒号或连字符边界，裸串 `ext` 误命中 4 个 `d:` 节点（如 `d:thm-15-7a-independent-sets-extend-to-bases`）。净计：替换 1 行、表内插 2 行、插 1 小节、删 0 行。

**5. P11｜「不新增关系词」保留、决定性论证替换（采纳）** S3 原论证「`sections` 是数组，57 节点已靠它记多节出现」（`S3:66–68`）在 P09 语义 A 后失效。改用：同型复用已由 L2 `members` 记账——24 个 L2 节点 **255 条成员引用、188 个不同下层 id**，全部跨 ≥2 节。理由：该论证不依赖 `sections` 语义，不受 P09 影响。

**6. P11｜`:29` 之后插入两小节（采纳）** 照抄提案 `117–146`（实测 30 行）：「词表扩充程序」（扩词须附实测：该模式在现有 `other` 中 <10 条不予扩；已由 `members`/`sections` 记账者一律不扩）＋「二次使用点：复用不建边」三条分流（无新断言 ⇒ 不建边不新建节点；有新断言 ⇒ `新断言 --requires--> 原对象`、方向不得反写；论证模式复现 ⇒ 记入 `members`，找不到统摄节点则不记录并在报告写明丢了什么）。理由：复用族真实规模 F 2 + P 6 = **8 条**，非 S3 的 4 条。

**7. P11｜`:70` 改字（采纳，必做）** 旧 `"rel_note":"仅 rel=other 时必填"` → 新 `"rel_note":"rel=other 时必填;其它 rel 值在选词经过权衡时应填"`。理由：否则与「`rel_note` 不限 `other`」段矛盾（S3 遗漏此步）；11 条非 other 带 note 的边（contrasts 7 / requires 3 / generalizes 1）由此从越界变合规，34 条存量列审计待补、不判违规。

**8. P11｜`:75` 之前插入一行（采纳）** 逐字：`- 依赖方向与发现顺序冲突时,依赖方向优先:`requires` 的 src 恒为「后需要」的一方,即使它在教材里出现得更晚。粗→细只约束 `part-of`,不约束 `requires` 的时间序。` 理由：现行 `:75` 把两词绑一起，正是 `edges-D1.jsonl:168`/`:212` 方向反写的字面借口；审计筛子命中 **11/620**。

**9. P12｜`:57` 之后插入「### statement 的句级证据要求」（采纳）** 照抄提案 `101–128` 六条。进 SPEC 的是对偶形式——**无锚句必须显式标记，而非必须补锚**：①不要求每句有锚，句数 > 锚点数本身不是缺陷；②无证据句**必须**以 `[model]` 开头，证据池 = T1 自身 anchors／T2 该节点作 src 的边 evidence／T3 part-of 后代的 anchors 与其边 evidence，**指向该节点的边（TD）不算证据**；条文自带的逐字示例须一并抄入：`"statement":"... S is an orthogonal set. [model] This is the worked instance of Theorem 15.10."`；③`[model]` 是声明不是豁免（含教材出处断言 ⇒ 死因 3；含未经支持的全称/唯一量词 ⇒ 死因 1；都过不了就删句）；④**禁止硬凑锚点**——quote 与所称支撑句非同一对象或只覆盖部分射程，判**死因 6**，比坦白无锚更严重；⑤机械下界 **S(n) ≤ D(n)**，仅必要条件，报告不得写成「句级覆盖已验证」；⑥L2+ 不适用。理由：抽样 10 节点 19 句，三层池全算仍有 4 句（21%）完全无证据、6 句（32%）只有部分射程。回溯量：188 节点需加 `[model]`、上界 243 句、需新增锚点 0、需 KILL 0。

**10. P12｜`:53` 之后插入子句（采纳）** 逐字（缩进两格）：`  锚点条数与 `statement` 句数之间**没有**对应关系,不要为了凑句数而增加锚点。`

**11. P12｜`:62` schema 改字（采纳）** 旧 `"statement":"1–3句英文,说清它是什么/断言什么"` → 新 `"statement":"1–3句英文,说清它是什么/断言什么;无证据支撑的句子须以 [model] 开头"`。仅此一字段值替换；`:55` 不动。

## 落选项

- P12｜「每句都必须被锚点管到」（任务书原议）：不采纳——立刻违规 273 节点（52.3%），22 个句数 > 3 与 `:53` 三锚上限在 SPEC 内不可满足，需新造 338 条锚（921→1259），按抽样 3:4 估 ≈250 条属 D 型硬凑，且全部会通过 `verify_anchors.py`（`:56` 唯一测试 `quote and quote in text`）计入「N/N 已验证」。
- P11｜方案 A 新增 `reused-at`/`recurs-in`：不采纳——与 `members` 双重记账，仅 8 条边受益，成本三处工具两处 schema。
- P11｜方案 B 单用（`other`+`rel_note` 规范化）：仅留作兜底——不解决「复用该不该建边」，把语义分类推给自由文本。
- P11｜本轮扩 `violates`：移出本轮——数据支持（实测 11 条，S3 写 10），但 `contrasts` 已覆盖「成对辨析」一半。
- P10｜C5 写成强制：不采纳——8 个按字样召回的节点所指全在章内，牵动 71 条边端点（4 个在已校验 `inherited/nodes-A1.jsonl`），真正须改名 0 个。
- P10｜`cx-` 的 origin 与准入：移出本轮——104 条锚点全在 exercises、49 个 `origin` 全空，与 `ext-` 零重叠，留 P08 / S2 项 3。
- P10｜`tools/gen_manifest.py` 分桶：降为审计项——manifest 自动生成，不属 `data/` 手改。

## 待装配项（留主控，本片不代裁）

1. 三份提案行号互撞，落笔顺序须统一重算。
2. P08 若判「节点不得带 `origin`」，P10 C4 须整条重写。
3. S2 项 3 若引入 `mixed`，两个 `ext-uncited-*` 可能更该记 `mixed`（C4 现按二值写）。
4. P05 若给 `origin: model` 开无锚豁免，须写明**不覆盖** `ext-`，否则 `ext-` 退化成可凭空新建的命名空间。
5. P05 若判「`origin: model` 允许整节点无锚」，可加简化条款「视为每句已标 `[model]`」，把 188 降到 **173**；若判仍须 ≥1 锚，4 个 0 锚 `origin=model` 节点补不出就该 KILL 不该凑。
6. P01：P12 第 4 条挂死因 6，若 6 不进 SPEC 须改挂 0 或 1。
7. G3b §三：`ext-vn-as-a-prior-vector-space` 的锚点与 `sections`（4 节降 2 节）最终列表须主控统一。
8. P09 若改判 `sections` 语义 B，P11 分流第 1 条依据要重写。

## 本片未覆盖

- **P11 的数据侧 FIX 未转写**（非 SPEC 文本，但须登记）：`edges-D1.jsonl:168`/`:212` 方向翻转、`:148` 改 `alias-of`、P 族 5 条「保留+补 note」。`:148` 的 KILL 前置三处不一致（`待应用清单.md:28` KILL 零残留 / `G2a:153` KEEP / `G1d:288–290` 完全未核）；`:168`/`:212` 翻转须在 B2 §6.1 枢纽豁免与删除测试**之前**完成。
- P10 §5.1–5.4 四个反例/边界的逐字案例（只取其结论约束进条文）。
- P11 §5.1 `edges-D1.jsonl:78`（64 字符引文 7 个持有者，B 型借用信号）、§5.2–5.4。
- P12 §5.2–5.7 六个边界、§6.3–6.6 耦合、§4.2 三口径对比与 243 句差值分布。
- 三份提案的「顺带发现」一节全部未读。

