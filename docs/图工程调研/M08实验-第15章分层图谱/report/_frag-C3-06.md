# 装配片 C3-06 · P16–P18

均已回查原文对应小节。长条文不逐字复抄（超篇幅），改为给出原文精确行段供照搬。

## 主体

**1. P16 主体条文（新增一节）** — 来源 `G5-P16-B2 §4(c) 禁止重复建 operations 节点.md` §二/§3.1。
主张：在 `SPEC.md:65` 之后、`:67`（`## 边 schema`）之前插入「## 原子化下延的边界(死因 5 的向下形态)」，逐字块＝该提案第 73–102 行围栏（正文 74–101 行）。**采纳。**
理由：现行 SPEC 对「向下复述 parent」零约束（`:45` 只管 id、`:65` 只管字段必填、`:95` 只写向上抽象），B2 靠外推判案，条文补授权缺口。

**2. P16 改 `SPEC.md:100`（死因表加指针）** — 同上 §3.2。**采纳。** 新文本逐字：
`死因编号:`1` 量词过度断言 / `2` 编造成员归属 / `3` 伪造教材出处 / `4` 数学错误 / `5` 伪抽象·层塌缩(向上:换名重新包装下层;向下:复述 parent 已写出的内容,判据见「原子化下延的边界」一节)。`
理由：新节不从死因表挂进去，判案时找不到它。

**3. P16 改 `SPEC.md:95`（审计 B 补向下方向）** — 同上 §3.3。**采纳。** 原行不动，其后补一行，逐字：
`  向下拆分的节点同样归审计 B,按「原子化下延的边界」一节的三条合取判,不得只凭主题判。`
理由：把外推变明文，并禁掉按主题判（按主题会误杀 17 个公理分解节点）。

**4. P17 改 `SPEC.md:45`** — 来源 `G5-P17-共享前提能否单独建节点+用边连.md` §3.1。**采纳。** 原句一字不动，其后补一行，逐字：
`同一前提也不得在两个节点里各写一遍文字,见「共享前提的记账(P17)」。`
理由：纯增量，内容层禁令与 id 层禁令同处。

**5. P17 主体条文（新增一节）** — 同上 §二/§3.2。
主张：在 `SPEC.md:75` 之后（`:76` 空行之后、`:77` 之前）插入「## 共享前提的记账(P17)」，逐字块＝该提案第 127–165 行，末补 1 空行，共占 40 行。**改后采纳**（限定见 6、7）。
理由：132 节点 / 572 条 `requires` 边现在只靠「SPEC 沉默」合规，追认收益 704 条、代价 4 条 FIX，无一条 KILL。

**6. 闸门 3 须随条文附下界标注** — 同上 §4.4、§七。**改后采纳**：条文照抄，装配时记「违规数 ≥1，921 条锚点未全量核，上界未知」；已确证 1 条为 `d:thm-15-6-hypothesis-finite-dimensional-space`（锚点 `15.08.md` 110 字符，前提-only 跨度 45 字符、`grep -c -F` 命中 1 次）。
理由：提案自陈「4 条变违规」是下界，不标注会被当闭合数字用。

**7. 主条文判重口径须附未核标注** — 同上 §4.1。**改后采纳**：条文「有一句去空白后完全相同即判重复」照抄，但须记「全文级实测 0 桶（546 节点无 statement 全文重复对）；逐句级未核」。
理由：条文口径严于已实测口径，差额没被量过。

**8. P18 主体条文（新增子节）** — 来源 `G5-P18-边的 evidence 能否算节点的锚点.md` §3.1。
主张：在 `SPEC.md:57` 之后插入「### 证据池:四层,前三层入池;边的 `evidence` 不是锚点」，逐字块＝该提案第 103–150 行围栏（正文 104–149 行）。**采纳。**
理由：反方案实测 89:0（86 节点破 3 锚点上限 + 3 条破 30 字符地板，从违规变合规 0 条）。

**9. P18 改 `SPEC.md:74`（行内追加）** — 同上 §3.2。**采纳。** `:73` 不动，`:74` 末追加，逐字：
`` `origin: model` 若仍带 `quote`,该 quote **不受机器校验**,回查者引用它作证据前必须自己核过——见「证据池」小节。 ``
理由：`unverified-model-quotes.tsv` 的 15 条正钻「可省 evidence」这个空子。

**10. P18 显式不改清单** — 同上 §3.4。**采纳**（记为装配约束）：`:53`「至少 1 条,最多 3 条」不改；`:54` 的 30–200 界不改；`verify_anchors.py` 不改。
理由：保住上限正是本裁定目的；`T1=0 且 T2>0` 的节点实测 0 个，脚本无动因。

**11. 装配顺序（自底向上）** — 三份提案各自登记了行号位移风险，此处合并。**采纳。**
落笔序：`:100`→`:95`→插 `:75` 后→`:74`→插 `:65` 后→插 `:57` 后→`:45`。
理由：P17 自陈其插入使原 `:77–110` 下移 40 行；自底向上则任一步都不需重算前面行号。

**12. P16 条件 3 与 P17 闸门 2 须合并措辞** — 来源 P17 §六第 2 条。**采纳为前置动作**，本片不代拟合并文本。
理由：触发集重叠（`d:function-space-scalar-multiple-is-pointwise` 同时在 P16 §四 6 人名单与 P17 §4.3 的 4 条违规里），否则同一节点被各判一次。

**13. `SPEC.md:100` 的竞争改动** — P16 §3.2 直接改 `:100`；P18 §3.3 要求 P01 先落地死因 6。**采纳为前置动作**：`:100` 一次性合并 P01 的 `6` 与 P16 的死因 5 括注，不得两次改写同一行。
理由：P17 闸门 1/3 与 P18 条文共四处引用死因 6，`6` 未入表前是悬空引用。

## 落选项

- P16 §5.2：B2 例 3、例 4 改判（例 4 真死因为 6）。移出本轮——审计记账修正非 SPEC 文本，且例 3 置信度「中」，需主控明示「componentwise」算写出还是只提到。
- P16 §四：107（锚点含住）与 70（首句含运算词）两个候选集。不采纳入条文——提案自陈本轮无法复算，§5.4 已论证二者只配当定向信号。
- P16/P17 §顺带发现：`SPEC.md:33` 节点类型与任务书口径冲突。移出本轮——归 S2 字段语义。
- P17 §六：为闸门 2 加「origin=model 不得充当共享前提」。不采纳——提案自己写明「本条不加，留给该待议项」。
- P18 §3.3：由 P18 改 `:100` 加死因 6。不采纳——提案自陈不负责定义 `6`，已改挂第 13 条。
- P18 §5.4：`edges-D1.jsonl:10/11/23`（charlen 14/19/14）三条短 evidence 的替换引文。移出本轮——解法在 P02，提案标「未核」。

**本片未覆盖**：三份长条文正文未逐字复抄（篇幅上限），装配时须按第 1、5、8 条的行段从原文照搬。
