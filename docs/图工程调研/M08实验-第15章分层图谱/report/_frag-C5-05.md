# C5-05 缺口条目（A8 + G4z 三份）

## 主体

**G-01｜A8-完备性评审.md：正文整体缺失。** 全文 1005 字节，§零/一/二/三/四/五/六 六节全为占位符（§零 下写「（正文见 §0）」，§一 下写「（正文见 §1.1 … §1.16）」，§二/三/四/五/六 标题下无任何字），而前言仍写「全部 16 节走完，无静默截断」。**采纳（本片最大缺失项）**：A8 承诺的逐节覆盖缺口、有意丢弃 vs 未发现、严重度排序、提案（目标文件/行号/id/逐字文本）四块全部未产出，需重跑。理由：回查原文六节皆空，前言的完备性声明与文件内容互相矛盾。

**G-02｜A8 的统计基线。** `data/nodes-*.jsonl` + `data/inherited/nodes-*.jsonl` = 522 个节点，`data/structures-L2-*.jsonl` = 24 个 L2 结构节点，合计 546 条记录；边 1433 条。**采纳为全章分母口径**（覆盖率按 546 计，需要时分开报）。理由：A8 唯一有实质内容的部分，且与 G4z-审计覆盖形状的节点聚合口径一致。

**G-03｜G4z-缺口清单.md：21 条待补建节点，本轮未装配。** 去重后 21 条，按严重度 1/2/3/4 各 **2 / 1 / 3 / 15** 条；建议顺序第一批（被依赖者）为 S2-1、S3-3、S4-7、S4-8、S3-1，第二批为叶子按严重度。**采纳为本轮未装配的新建节点工单。** 理由：清单已给出逐条 id、源 file:line 与字符数，可直接转 SPEC。

**G-04｜同上：撤回任务书的严重度 1 示例。** 任务书称「15.15 例 2 的 Legendre 完全无节点」，实为两个节点存在：`apostol:legendre-polynomial-approximation`（`data/nodes-A2-s3.jsonl:6`，3 锚点 8 出边）与 `apostol:best-linear-approximation-to-sine`（同文件 :7，3 锚点 3 出边）。**采纳**：不要执行「补建 15.15 例 2 节点」。理由：清单已复核 `nodes.tsv`，两 id 各 1 命中，置信度高。

**G-05｜同上：`example` 类型是否首次引入，未裁。** 全图 node_type 分布 concept 258 / theorem 173 / method 69 / notation 22 / 空 24（L2 层），无一个 `example`，而 6 条提案想用它。**采纳为待裁项（阻塞 S1-1、S1-2、S3-1、S3-2、S4-* 多条）。** 理由：不引入则退化为 `concept`，不影响锚点与 statement，但归属需主控定。

**G-06｜同上：S4-11 跨节相反结论，必须并为一次裁决。** 15.09 判 Pₙ 上齐次条件值得建，15.05 §三 把第 3、4、8、9、10、12、15 题一律归为「齐次条件族，重复：`x:solution-set-of-a-homogeneous-linear-condition`(15.03,3.3) 的实例」。**采纳为待裁项**：二选一，不要两节各建一个。理由：差别只在载体（函数空间 vs Pₙ），同一套路两判并存会双记账。

**G-07｜同上：S4-15 分级争议未裁。** Laguerre 族全图零提及（`grep -ic laguerre` 在 `nodes.tsv` / `statements.tsv` / `anchors.tsv` / `edges.tsv` 四表皆 0），来源是习题。**采纳为待裁项**，清单本身倾向移到严重度 1。理由：「全图零提及」与「来源是习题」两条判据在此相撞。

**G-08｜同上：五项未复核，本轮未完成。** 逐条为：S3-1 的 y₃–y₅ 三式未独立重算；S3-2 的 φ 数值未验算；S4-14 的「u₁ 与 u₃ 夹角 π/6、u₂ 与 u₃ 夹角 π/3」这一具体配对为模型自算（原文 `15.12.md:61` 只说「两个正交、两个成 π/3、两个成 π/6」而未点名）；S4-11 的两个维数（n 与 ⌊n/2⌋+1）为模型自算未核；S4-15 的六对内积未复核；S4-5 的反例层逐题表原文未复核；S2-1 引用的 15.09 ex22(a)(b) 重复判定未核。**采纳为复核工单。** 理由：均系清单自标「我未复核/未重算」，落盘前必须有人做。

**G-09｜同上：S4-5 附带 FIX。** 反例层逐题表第 17 行写「A1 已有 `apostol:homogeneous-de-solution-space`」，而该节点 statement 逐字写「a and b given **constants**」，覆盖不到变系数。**采纳**：不建 S4-5 也要改该行判断为「仅覆盖常系数」。理由：否则构成一次隐性覆盖高报（盲区 D）。

**G-10｜同上：各节 FIX 条目未排序，出本轮射程。** 逐节数照抄：15.01 一条、15.04 一条、15.06 一条、15.07 三条、15.08 四条、15.10 三条、15.11 两条、15.12 两条、15.13 六条、15.14 两条、15.15 五条、15.16 两条（我据此相加得 32 条，原文未给合计）。**采纳为下一轮工单。** 理由：清单只排新建节点顺序，FIX 的依赖关系未处理。

**G-11｜同上：一处事实错误待 FIX。** `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 的 statement 称「Apostol 未提及」，而 `15.05.md:63` 第 30(a) 题逐字要求证明该命题。**采纳。** 理由：statement 与源行直接冲突，非判断分歧。

**G-12｜G4z-审计覆盖形状.md：覆盖率口径约束。** 覆盖率必须与三个量同看：分母里不可建/不该建单元的占比、`exposure.tsv` 的 `T3`、该节是否有 D 层批次参与生产。且 15.02 的 84.6% 与 15.15 的 76.7% **是已达上限的满分**，要求补齐差距等于要求违反硬禁令 2。**采纳为汇总口径约束。** 理由：15.02 四条公理等式实测 16/18/22/22 字符，物理上无合法引文。

**G-13｜同上：可复用的机械预测器。** `nodes.tsv` 的 loc×sections 交叉表可机械预测哪些节会稀疏，不需读任何报告正文——四个习题节（15.05 / 15.09 / 15.12 / 15.16）是全章唯一单批次覆盖、零 D 层、零 L2 结构节点的节。**采纳。** 理由：全量机械聚合，可复算。

**G-14｜同上：主控汇总的抓取缺口。** `G4-完备性-15.05.md` 的 §〇 标题行仍是未填模板占位符「本节 n 句 / 已覆盖 a / 下移 b / 未覆盖 c；其中值得补建 d 个」，真实数字在正文首行；另 15.03、15.04、15.10、15.13、15.14 五份标题行不带数字，只有 11 份可从标题行机械提取。**采纳**：正则抓取方案需改。

**G-15｜同上：15.09 八条候选锚点字符数未核。** 由该报告自己用 Python `len()` 实测，未经索引表核对。**采纳为落盘前必做的复测项。**

**G-16｜同上：一条 202 字符候选锚点超 SPEC 200 上界（15.16）。** **采纳**：该处缺口无法靠延长既有锚点解决，须另立方案。

**G-17｜同上：两项自标未复核。** 15.13 的 31 个下移单元是否全部合法（未逐条核）；15.05 的「21 道正面例题」计数出自该节报告，未独立数源文件。**采纳为复核工单。**

**G-18｜G4z-盲区抽查合并.md：`G4-完备性-15.13.md` 缺三、四、五三节。** 只有〇/一/二三节与「顺带发现」，无第五节、无锚点抽查，该节未计入合并；故实得抽查 **45 条**（不是 48 条）。**采纳为缺失项，需补跑 15.13 的抽查。**

**G-19｜同上：D 型边界截断候选集（全量实测）。** 921 条锚点里尾截断 117 条、首截断 92 条、取并 **179 条 = 19.4%**。**采纳为可机械化候选集**（不等于 179 个缺陷）。理由：这修正了「D 型机器查不出来」的表述，应改为「D 型的语义子形态机器查不出来」。

**G-20｜同上：B 型候选须先剔自我共享。** `shared-quotes.tsv` 共 480 组，≥2 个 `node:` 持有者的仅 158 组，另 322 组是节点 + 自身出边。抽查侧精确度约 21%（33 条碰撞里 7 条有害）。**采纳**：B 型定位为候选生成器，非检测器。

**G-21｜同上：死因 1/6 边界在代理间未校准。** 15.02-2 与 15.04-3 抽的是同一节点 `d:axiom-5-states-the-zero-only-as-a-right-neutral-element` 的两条锚点，两处都判 D 型轻度命中，死因分别记 6 与 1。**采纳为待裁项。**

**G-22｜同上：五型词表漏两个形态。** 15.15-3 的「上下文句冒充步骤句」、15.16-3 的「T2 空转」（边把机制指向 dst，却用答案句当 evidence）。**改后采纳**：扩表，不改既有五型编号。

**G-23｜同上：唯一新的完全可机械化 SPEC 候选。** 15.05-3 建议「题目指令句不得单独充当锚点」，配一份指令句模式表 + grep。**采纳，建议单列裁决。** 理由：本轮唯一零语义依赖的新判据。

**G-24｜同上：抽查结果只能当形态学证据。** 严格口径误报率 32/45 = 71%（死因 6 共 25、死因 1 共 6、死因 4 轻度 1），宽松口径 44/45 = 98%；建议动作 FIX 32 / 保留 13 / KILL 0；45 条只覆盖 44 个不同节点（546 的 8.1%）与不到 45 条锚点（921 的约 4.9%）。**采纳为口径限制 + 未完成项**：按 anchors.tsv 随机抽样、预写死判定阈值重跑一轮，本轮**未做**。

## 落选项

- **G4z-缺口清单.md**：按任务书补建「15.15 例 2 Legendre 节点」。**不采纳**——前提已被推翻（见 G-04），两节点确实存在。
- **G4z-缺口清单.md**：S4-10 `apostol:cx-three-generators-of-a-shifted-basis-span-only-a-plane`（15.09 ex21(d)，`:35`，95 字符）。**移出本轮**——15.09 报告自评「置信度中，是不是真陷阱有主观成分，可降为不建」，清单亦排末位。
- **G4z-缺口清单.md**：S3-2 新建 `apostol:normalized-legendre-first-six-explicitly`。**改后采纳（移出新建清单）**——死因 5 风险最高，按原文退化方案把两条锚点（`15.13.md:165` 截取 166 字符、`:169` 95 字符）直接补给 `apostol:normalized-legendre-polynomials`，走 FIX 不建节点。
- **G4z-审计覆盖形状.md**：§一 逐节量化表、§二 相称性判定。**不采纳为缺口条目**——该文件自述「只谈覆盖形状，不重判任何缺口本身」，属背景与口径，其可装配部分已收在 G-12/G-13。
- **G4z-盲区抽查合并.md**：把 71%–98% 当全章误报率外推。**不采纳**——原文明确拒绝给点估计，样本偏差方向已知向上但幅度未核。
- **G4z-审计覆盖形状.md**：15.14 报告记 Figure 15.2 的几何直观在纯文本图谱里必然丢失。**不采纳**——原文判为真实但不可弥补的缺口，无可执行动作。
- **G4z-盲区抽查合并.md**：本轮击杀名单。**不采纳**——抽查 45 条建议动作里 **KILL 0 条**，抽查侧不提供任何支持。

（本片未覆盖：A8 的 §一 逐节缺口与 §五 提案因原文为空而无法转条目，见 G-01；G4z 三份的〇速览以外小节我已回查，未再展开逐节量化表全表。）
