# 审查报告：形式 D / E 锚点支撑性 — nodes-D3.jsonl 第 46–69 节点

状态：已完成（24/24 节点）

审查员：形式 D/E 专项（片区 46–69，文件行序 1-indexed，至文件末尾）
数据文件：`data/nodes-D3.jsonl`
覆盖范围：15.09–15.12（内积、范数、正交、Gram-Schmidt、投影）

---

## 1. 覆盖声明

**已审完全部 24 个节点**（`data/nodes-D3.jsonl` 第 46–69 行，1-indexed，至文件末尾；文件共 69 行）。全部按"statement 拆原子 claim → 逐条映射锚点或原文行号 → 无锚 claim 独立核真 → 判型"流程人工处理，分 5 批落盘（见 §8）。

节点 id 清单（行号: id）：
46 `d:orthogonal-set-condition-on-distinct-pairs-only`；47 `d:orthonormal-set-adds-unit-norm`；48 `d:normalizing-an-orthogonal-set`；49 `d:trigonometric-orthogonal-system`；50 `d:trigonometric-system-norms`；51 `d:normalized-trigonometric-system`；52 `d:15-10-nonzero-hypothesis-is-essential`；53 `d:15-10-proof-setup-vanishing-finite-combination`；54 `d:15-10-proof-pair-with-a-single-element`；55 `d:15-10-proof-divide-by-nonzero-self-inner-product`；56 `d:15-10-proof-repeat-for-each-index`；57 `d:15-10-n-orthogonal-nonzero-elements-form-a-basis`；58 `d:existence-of-an-orthogonal-basis-deferred`；59 `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis`；60 `d:orthogonal-basis-component-formula`；61 `d:15-11-proof-pair-with-basis-element`；62 `d:15-11-denominator-nonzero-by-positivity`；63 `d:orthonormal-basis-component-is-the-inner-product`；64 `d:orthonormal-expansion-formula`；65 `d:parseval-requires-an-orthonormal-basis`；66 `d:parseval-formula-statement`；67 `d:parseval-proof-pair-expansion-with-y`；68 `d:parseval-conjugate-comes-from-hermitian-symmetry`；69 `d:parseval-norm-form`。

实际落在 15.11 一节（正交性、Thm 15.10/15.11/15.12、Parseval）。任务书所述本片区应覆盖 15.09–15.12，但**本 24 节点的 `sections` 与锚点全部指向 `15.11.md`**（另有 2 条跨文件锚指向 `15.10.md`）；15.09 习题、15.12 习题、Gram-Schmidt（15.13）、投影（15.14/15.15）**不在本片区节点内**，故相应专项检查的适用面见 §4 与 §9。

读过的源文件：`15.10.md` 全文、`15.11.md` 全文、`15.07.md:3,9`、`15.08.md:3,19–25`、`15.13.md:3,5,83,85`、`15.14.md:20–30`、`15.15.md:25–55`。

**不报"N/N 锚点已验证"。** 下列结论按 claim 计，不按锚点计。

## 2. 确认的形式 D 实例（SCOPE-SHORTFALL：射程不足）

按严重度降序。共 **6 处**（4 处主症 + 2 处局部射程不足）。

**D-1（最重）L51 `d:normalized-trigonometric-system` 末句** — claim："它是 Parseval 公式 (15.11) 取得其经典 Fourier 含义的标准系统。"
- 锚点射程：唯一锚点 `15.11.md:38` 只给出 φ_0、φ_(2n-1)、φ_(2n) 的**显式公式**，对 Parseval 一字未及。
- 缺口：(15.11) 出自 Theorem 15.12（`15.11.md:77`），明写 "finite-dimensional Euclidean space of dimension $n$" 且要求 $\{e_1,\ldots,e_n\}$ 是正交规范**基**。C(0,2π) 无限维、标准化三角系为无限集，故 (15.11) 按已证形式**不适用**于该系统；经典 Fourier 版需完备性，本章未证。
- 反面证据（支持"这不是原文的意思"）：Apostol 自己走到 Fourier 系数是在 `15.15.md:25`–`:39`，先截出**有限维子空间 S（dim = 2n+1）**再谈投影与 Fourier 系数，结论是**逼近**定理（`:51`）而非 Parseval 恒等式。
- 建议：删末句，或改为"其经典 Fourier 对应需无限维/完备性论证，超出 (15.11) 的有限维前提"。死因 **1**（`:91` 历史指向读法下可退为 0 + 强制改写）。

**D-2（最重，与 D-1 同型配对）L69 `d:parseval-norm-form` 末句** — claim："特化到标准化三角函数系上，它就是 Fourier 系数的经典 Parseval 恒等式。"
- 锚点射程：`15.11.md:86`（式 15.12）+ `:89`（x=y 代入）**只管住"由 (15.11) 取 x=y 得 (15.12)"**，止于此。
- 缺口：同 D-1 的有限维/完备性缺口。仅取有限段 {φ_0..φ_{2n}} 得到的是子空间内等式或一般 f 的 Bessel 不等式，**都不是**经典 Parseval 恒等式。
- 建议同 D-1。死因 **1**。
- **配对意义**：D-1 与 D-2 分别位于本片区 Parseval 处理的入口（标准化系统）与出口（范数形式），是**同一种越界犯了两次**，宜合并裁定。

**D-3 L66 `d:parseval-formula-statement` claim 3** — claim："在正交规范基下**每个** Euclidean 空间都像 V_n 配点积那样计算。"
- 锚点射程：锚点 2（`15.11.md:75`）原文为 "in a **finite-dimensional** Euclidean space with an orthonormal basis..."。
- 缺口：节点复述时把 finite-dimensional 换成了 **every** —— **锚点里有的限定词在断言里被抹掉**（§4.1 正题）。另有复空间下"点积"不带共轭的失准（§4.2）。
- 部分挽救："in an orthonormal basis" 隐含预设有限基存在。
- 建议：改为"在**有限维**空间的正交规范基下，与 V_n 配（Hermite）点积同形"。死因 **1**（隐含预设读法下 0 + 强制改写）。

**D-4 L57 `d:15-10-n-orthogonal-nonzero-elements-form-a-basis` 末句** — claim："这一分句不增加任何内积内容；它的**全部力量**来自一个更早证明的维数计数定理。"
- 锚点射程：锚点 2（`15.11.md:15`）止于 "Theorem 15.7(b) shows that S is a basis for V"，只管住"用了 Thm 15.7(b)"这一步。
- 缺口："全部力量来自维数计数"不成立 —— 结论仍依赖第一分句"正交非零 ⇒ 独立"（正定性 + 正交性，纯内积内容），没有它 Thm 15.7(b) 无从启动；且与本节点 claim 2 自述的"两步复合"内部矛盾。
- 建议：改为"该分句相对第一分句的**增量**内容不涉及内积，仅为维数计数"。死因 **0 + 强制改写**（字面读 "whole force" 可升 1）。

**D-5（局部）L61 `d:15-11-proof-pair-with-basis-element` claim 1 的理由子句** — 等式链由锚点 2（`:64`）逐字覆盖，但断言里的 "since (e_i, e_j) = 0 for i not j" 位于 `:67`，**未锚**（第 3 槽空置，且 `:67` 合规可引 —— L62 就锚了它）。死因 0 + 补锚。

**D-6（局部）L50 `d:trigonometric-system-norms` claim 1** — 断言 "(u_0,u_0)=∫dx over [0,2pi]=2pi"，但两条锚点分别给 cos²/sin² 积分与**范数结论**，无锚点直接给该值（只能经范数定义反推）。原文 `:29` 有逐字可引句，第 3 槽空置。死因 0 + 补锚。

另有 **L49 claim 3** 介于 D 型与 C 型之间（断言的是正交关系式 `:26`，锚点给的是其结论 "so S is an orthogonal set"，属拿结论当关系式），已在 §8 批次 1 记录，不重复计入。

## 3. 确认的形式 E 实例（无锚点从句）

**形式 E 是本片区的主症：24 个节点中 23 个含至少一条零锚点 claim**（唯一例外为 L66，其末句被锚点覆盖 —— 但 L66 另有 D-3）。逐条明细见 §8，此处按严重度分三层归总。

**E-严重（无锚从句本身为假或失准）——2 处，均在 L67 `d:parseval-proof-pair-expansion-with-y`：**
- **E-1（假）**："这是**两节之内**同一技巧的第三次出现，前有 Theorem 15.10 与 Theorem 15.11。" 三次出现在 `15.11.md:15`、`:61`、`:89`，**全在同一节 §15.11**（§15.10 内是 Theorem 15.8/15.9）。节点把定理编号误当节号，且与自身 `sections = ["15.11"]` 矛盾。改法："the third occurrence within Section 15.11"。
- **E-2（失准）**："这正是正交**规范**性所要促成的技巧。" 该配对技巧在**仅正交**的基上同样奏效（Thm 15.11 证明 `:61` 用的就是非规范正交基）；正交规范性买到的是**分母消失**。L65 自己做了正确区分，故此处属跨节点不一致。
- L67 死因：**0 + 强制改写**（claim 1 完好、锚点无误，缺陷限于两条可删从句）；对无锚假陈述从严可支持 **4**。

**E-中（无锚 claim 为真，且合规原文就在同文件/邻文件、锚点槽空置）——本片区最普遍的一类，计 14 处：**
- L46：claim 3、4、5（可引 `15.11.md:5`、`:7`）
- L47：claim 2、3、4、5（可引 `15.11.md:67`、`15.10.md:123`）
- L48：claim 2（可引 `15.10.md:125`）
- L49：claim 1、6（可引 `15.11.md:17`、`:26`）
- L50：claim 4（可引 `15.11.md:38`）
- L51：claim 2（可引 `15.11.md:35`）
- L52：claim 4、5、6、7（可引 `15.11.md:3`、`:15`、`15.07.md:3`）
- L53：claim 3、4（**关键**：末句的定义依据在 `15.07.md:3`，跨文件锚未设，而节点 `sections` 只写 ["15.11"]）
- L54：claim 2（可引 `15.10.md:27`，68 字符合规）
- L55：claim 3（可引 `15.10.md:22` 正定性式，35 字符合规）
- L58：claim 2（可引 `15.11.md:43`）
- L62：claim 3（可引 `15.08.md:3` 或 `15.07.md:3`，跨文件）
- L63：claim 2 —— **特别一例**：正文以引文口吻**复述**了 Apostol 的从句 "when (e_j,e_j)=1 we obtain (15.9)"，而该句（`:67`）未被锚点收录
- L64：末句 —— **本片区"最该引却没引"的一条**：末句断言的正是 `15.11.md:89` 逐字说的事（"Taking the inner product of both members of Equation (15.10) with y ... we obtain (15.11)"），第 3 槽空置
以上均经核为真，死因一律 **0 = KEEP + 补锚**。

**E-轻（对已锚原文的元评注/直读解释，射程外延很短）——计 11 处：**
L52 claim 2、L53 claim 2、L54 claim 3（另涉 SPEC，见 §6）、L55 claim 2、L56 claim 2、L56 claim 3、L59 claim 2 后半与末句、L60 claim 2、L60 claim 3、L61 claim 2、L61 claim 3、L63 claim 3 与末句、L65 claim 2、L68 claim 1/3/4（推导步，已由 `origin=model` 诚实标注）、L69 claim 2/4。全部经核为真，死因 **0 = KEEP**。

**分布规律（与任务书经验一致并加强）**：24 个节点中 **22 个的 statement 末句为无锚附加内容**（唯一例外 L66 末句被锚点覆盖；L69 末句无锚且越界）。本片区四处最重的缺陷（D-1、D-2、D-3、D-4）**全部落在末句或末句所在的最后一个断言**。

## 4. 四个专项检查

### 4.1 有限维假设的增删

基准（人工核对原文）：`15.11.md:7` Theorem 15.10 **首句无**有限维假设（"In a Euclidean space V, every orthogonal set of nonzero elements is independent."），其 "In particular" **第二分句才有** "in a finite-dimensional Euclidean space with dim V = n"；`:43` Theorem 15.11 **写了** "finite-dimensional ... with dimension $n$"；`:77` Theorem 15.12 **写了** "finite-dimensional ... of dimension $n$"；`:75` 引出句**也写了** "in a finite-dimensional Euclidean space with an orthonormal basis"。

逐节点核查（本片区涉及有限维的 8 个节点）：

| 节点 | 原文有无 | 节点处理 | 结论 |
|---|---|---|---|
| L52（Thm 15.10 首句必要性） | 首句**无** | 锚点只截首句，statement 亦只谈首句，**未添加**有限维 | ✓ 忠实 |
| L53/L54/L55/L56（Thm 15.10 证明四步） | 首句**无** | 四节点均**未引入**有限维 | ✓ 忠实（证明确实不需有限维；`15.13.md:3` 亦称该构造对 "finite or infinite dimensional" 均成立） |
| L57（Thm 15.10 第二分句） | **有** | 锚点与 statement **均逐字保留** dim V = n 与 finite-dimensional | ✓ 忠实 |
| L58（正交基存在性推迟） | **有** | 保留 "every finite-dimensional Euclidean space"；跨文件核实 `15.13.md:3,85` 确在 15.13 证出（Thm 15.14 为标准正交版） | ✓ 忠实 |
| L59（Thm 15.11 前提） | **有** | 锚点 + statement 均保留 | ✓ 忠实 |
| L65（Parseval 需正交规范基） | **有** | 两条锚点**均含** finite-dimensional，statement 未增删 | ✓ 忠实 |
| **L66（Parseval 公式陈述）** | **有**（锚点 2 逐字含 finite-dimensional） | claim 3 复述为 "**every** Euclidean space" | ✗ **抹掉了原文有的**（= D-3） |
| **L51 / L69（Parseval 两端）** | **有**（(15.11)/(15.12) 均出自写明 finite-dimensional 的 Thm 15.12） | 末句把结论"特化/赋义"到**无限维**的 C(0,2π) 与无限三角系 | ✗ **越出原文有限维前提**（= D-1、D-2） |

小结：**"添加原文没有的有限维假设" 0 例；"抹掉/越出原文有的有限维假设" 3 例（L66、L51、L69）**，且三例集中在 Parseval 一带。四步证明节点（L53–L56）正确地没有引入有限维，这一点判断准确。

### 4.2 实 / 复 Euclidean space 分裂

基准：`15.10.md:13` 内积定义以**实**线性空间起手；`:25` "real Euclidean space"；`:29`–`:32` 复空间以 (1') $(x,y)=\overline{(y,x)}$ 替换对称公理；`:35`–`:41` 由齐性 + (1') **派生**共轭齐次性 $(x,cy)=\bar c(x,y)$；`:43` 复 Euclidean space / unitary space；**`:45` 关键**："the theorems of this chapter are valid for complex Euclidean spaces as well ... the space can be real or complex"；`:151` 角度定义**限定** "In a real Euclidean space"。共轭出现处：三角不等式证明 `:136`、Parseval `15.11.md:80`。

本片区结论（4 处相关，其中 1 处确认缺陷、2 处措辞不严、1 处处理正确）：

- **✗ L65 末句"clean **bilinear** form of (15.11)" —— 确认用词缺陷。** (15.11) 在 `15.11.md:80` 逐字带共轭杠 $(\overline{{y , e_{i}}})$，正是为复空间所设（`15.10.md:45` 明言本章定理对复空间有效）；该形式对第二变元是**共轭线性**（sesquilinear），并非双线性。实质数学（正交非规范基下出现 1/(e_i,e_i) 因子）经验算正确，缺陷限于术语。建议改"共轭双线性"或径称"干净形式"。
- **✗ L66 claim 3"像 V_n 配点积那样计算" —— 确认失准。** 实空间下与 `15.10.md:8` 的 dot product $\sum x_i y_i$ 同形 ✓；**复空间下应为 Hermite 点积 $\sum x_i \overline{y_i}$**，而原文定义的 dot product 无共轭。建议补"（Hermite）"。
- **△ L54 末句"a consequence of an axiom plus **symmetry**" —— 措辞不严（轻）。** 复空间中对称公理被 (1') 替换，"公理 + 对称性"的推导会带出共轭；结论（两种放法各缺一条规则）仍成立。同类轻度问题见 L48 claim 3"标量按齐性从内积中提出"（第二槽提标量在复情形带共轭；因涉及的是**范数**这一正实数，结论无碍）。
- **✓ L68 —— 本片区实/复处理最正确的节点。** 明确把共轭归因于 Hermite 对称并**跨文件锚了公理 (1')**（`15.10.md:32`）；经验算，两条理由（第一槽路径上共轭只由 (1') 引入；共轭齐次性本身由 `15.10.md:35`–`:41` 派生自齐性 + (1')）**均成立**。
- 另：L49、L51 说 "real space C(0,2pi)"，与 `15.11.md:17` "In the real linear space $C(0, 2\pi)$" **一致 ✓**。
- 本片区**无**节点涉及角度定义（`15.10.md:151`，实空间限定），故无该处的实/复误用。

### 4.3 反常积分 / 积分内积收敛与正定性条件

**本片区无一节点引用 Example 4（权函数 w，`15.10.md:73`）或 Example 5（$\int_0^\infty e^{-t}fg\,dt$ 反常积分，`:75`–`:81`）。** 机器复核：对第 46–69 行 grep `improper|weight function|Riemann|continuous|e^{-t}`，命中 0；仅 "integral inner product" 命中 1 次（L49）。相关内容应在 D3 前 45 个节点或 D4 片区，不在我的覆盖内（见 §9）。

本片区唯一相邻接触点，经核**无缺陷**：
- L49/L50/L51 使用 C(0,2π) 上的积分内积。L48 claim 2 与 L50/L51 的标准化依赖 ||u_n|| ≠ 0，即依赖**严格正定性**。
- 任务书所指的断裂点（仅 Riemann 可积时 ∫f²=0 不迫使 f=0）**在此不发生**：`15.10.md:59` Example 3 把空间限定为 "real-valued functions **continuous** on an interval $[a, b]$"，而 `15.11.md:17` 的 C(0,2π) 正是连续函数空间，故 ∫f²=0 ⟹ f=0 成立 ✓。且三角系各元素的范数在 `:29`–`:35` 被**显式算出**为 √(2π)、√π，非零性无需依赖抽象正定性 ✓。
- 登记（不算缺陷）：L49 statement 写 "with the integral inner product" 而未复述"连续"限制；但 C(0,2π) 记号本身承载该限制，且 L49 锚点所在段落 `:17` 含 "real linear space $C(0,2\pi)$"。

### 4.4 计算值人工验算

本片区**无**任务书列举的那批符号值（(e−1/e)/2、3/e、e²−1、pi − 2 sin x、x = 1 and y = i）；机器复核对第 46–69 行 grep 该五类模式，命中 0（它们应属 15.12 习题一带，在 D4 或 D3 前段）。本片区模型自算的符号值/构造共 **7 项，全部逐项人工验算，全部正确**：

1. **L50** (u_0,u_0) = ∫_0^{2π} dx = **2π** ✓
2. **L50** ∫_0^{2π} cos²(nx) dx = ∫_0^{2π}(1+cos 2nx)/2 dx = **π**（n ≥ 1 整数，cos 2nx 项整周期积零）✓；∫_0^{2π} sin²(nx) dx = **π** ✓
3. **L50** ||u_0|| = √(2π)、||u_n|| = √π ✓（与 `15.11.md:35` 逐字一致）
4. **L51** φ_0 = 1/√(2π)、φ_(2n−1) = cos nx/√π、φ_(2n) = sin nx/√π ✓（与 `:38` 逐字一致；且与 `15.15.md:29` 的 (15.20) 独立复现一致）
5. **L52 反例 {O, i, i+j}** ✓：配点积，(i, i+j) = 1 ≠ 0 故**非**正交集 ✓；含 O 故相依 ✓；三项断言（含 O、相依、非正交）同时成立 ✓。（措辞的逻辑指向问题见 §7）
6. **L65** 正交非规范基下 (x,y) = Σ (x,e_i)·conj((y,e_i)) / **(e_i,e_i)** ✓：由 c_i = (x,e_i)/(e_i,e_i) 与 (e_i,y) = conj((y,e_i)) 代入即得，**1/(e_i,e_i) 因子确实出现** ✓
7. **L68** 第一槽推导 (x,y) = Σ(x,e_i)(e_i,y) ✓（公理 (3) $c(x,y)=(cx,y)$ 即第一槽齐性，无共轭）；再由 (1') 得 (e_i,y) = conj((y,e_i)) ✓ → 复现 (15.11) ✓

附带自行验算（用于核查节点的反事实断言，非节点自算值）：
- **L55 末句**"正半定形式上定理失败" ✓：取 V = R²、退化形式 (x,y) = x_1y_1，则 e_2 = (0,1) ≠ O 但 (e_2,e_2) = 0；{e_2, 2e_2} 相异对内积为 0、元素皆非零、却相依 ⇒ 定理失败 ✓
- **L57 claim 2** 对 Theorem 15.7(b) 的刻画 ✓：`15.08.md:19,23` "Let $V$ be a finite-dimensional linear space with $\dim V = n$ ... (b) Any set of n independent elements is a basis for V." —— 刻画准确 ✓
- **L69 claim 2** z·conj(z) = |z|² ✓（`15.10.md:94` 有同型原文）

**结论：本片区计算值 0 错。** 但需强调：这 7 项中**只有第 3、4 项有锚点直接覆盖**，其余 5 项（含两个反例构造）为无锚模型内容，机器筛查无从判其真假 —— 与任务书"召回 1/4"的结论一致。

## 5. 判定支撑充分的节点 id 清单

先说清判准，避免与 §3 冲突：**本片区 23/24 个节点含无锚 claim**，若按"零无锚 claim"作标准，清单近乎为空，那不是有用的输出。故此处"支撑充分"指：**核心断言由锚点完整覆盖，且所有无锚 claim 均为（a）对已锚原文的元评注/直读，或（b）已由 `origin=model` 诚实标注的模型内容，且经人工核真为真 —— 即无需改写、只需（可选）补锚。**

判**支撑充分**（9 个）：
- `d:15-10-proof-pair-with-a-single-element`（L54）—— 末句的槽位二难经验算成立；其齐性公理依据被 SPEC 卡死，非 producer 之责
- `d:15-10-proof-divide-by-nonzero-self-inner-product`（L55）
- `d:15-10-proof-repeat-for-each-index`（L56）
- `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis`（L59）—— claim 2 前半由锚点措辞的"缺席"合法支撑
- `d:orthogonal-basis-component-formula`（L60）—— 散文锚 + 公式锚配对得当
- `d:15-11-denominator-nonzero-by-positivity`（L62）—— **本片区最佳之一**：origin 标注 + 跨文件锚正定性公理 + 把 Apostol 的隐含步骤显式化
- `d:orthonormal-basis-component-is-the-inner-product`（L63）
- `d:orthonormal-expansion-formula`（L64）
- `d:parseval-conjugate-comes-from-hermitian-symmetry`（L68）—— **本片区最佳之一**：origin 标注 + 跨文件锚公理 (1')，共轭归因经双路验算成立

判**KEEP 但应补锚**（需补跨文件或同文件锚点，数学无误，8 个）：
`d:orthogonal-set-condition-on-distinct-pairs-only`（L46）、`d:orthonormal-set-adds-unit-norm`（L47）、`d:normalizing-an-orthogonal-set`（L48）、`d:trigonometric-orthogonal-system`（L49）、`d:trigonometric-system-norms`（L50，另 D-6）、`d:15-10-nonzero-hypothesis-is-essential`（L52）、`d:15-10-proof-setup-vanishing-finite-combination`（L53）、`d:existence-of-an-orthogonal-basis-deferred`（L58）、`d:15-11-proof-pair-with-basis-element`（L61，另 D-5）。（按 id 计 9 个条目，其中 L50、L61 兼有局部 D 型。）

判**需改写**（措辞越界/失准/含假从句，5 个）：
- `d:normalized-trigonometric-system`（L51）—— 建议死因 **1**
- `d:parseval-norm-form`（L69）—— 建议死因 **1**
- `d:parseval-formula-statement`（L66）—— 建议死因 **1**（或 0 + 强制改写）
- `d:15-10-n-orthogonal-nonzero-elements-form-a-basis`（L57）—— 建议 **0 + 强制改写**
- `d:parseval-proof-pair-expansion-with-y`（L67）—— 建议 **0 + 强制改写**（含唯一明确假陈述，从严可 **4**）
- `d:parseval-requires-an-orthonormal-basis`（L65）—— 建议 **0 + 术语改写**（"bilinear" → 共轭双线性）

**全片区数学错误（死因 4）候选：0 个实质数学错误。** 唯一可能被判 4 的是 L67 的 "两节之内"（定位性事实错误，非数学错误）。所有模型自算值、反例构造、公理归因经人工验算**全部正确**。

## 6. 因 SPEC 30 字符下限 / 3 锚点上限导致证据不足的实例计数

**3 锚点上限：本片区致证据不足 0 例。** 机器统计 + 人工复核：24 个节点中 9 个用 1 条锚点、15 个用 2 条，**没有任何一个节点用满 3 条**。故本片区所有射程缺口**均不可归因于 3 锚点上限**；可引的合规原文普遍就在同文件邻行（`15.11.md:5/7/17/26/29/38/43/67/89`）或邻文件（`15.07.md:3`、`15.08.md:3,23`、`15.10.md:22/27/94`）。

**30 字符下限：确认卡死 2 条关键公式行，牵连 6 处 claim。** 实测字符数（`15.10.md`）：

| 行 | 内容 | 字符数 | 是否达标 |
|---|---|---|---|
| :19 | `(3) $c(x,y) = (cx,y)$`（**齐性公理**） | **21** | ✗ 卡死 |
| :108 | `\| x \| = (x, x) ^ {1 / 2}`（**范数定义式**） | **26** | ✗ 卡死 |
| :15 | 公理 (1) 对称性 | 49 | ✓ |
| :17 | 公理 (2) 加性 | 63 | ✓ |
| :22 | 正定性 `(x, x) > 0 ... x \neq O` | 35 | ✓ |
| :27 | Note: 取 c = 0 得 (O,y) = 0 | 68 | ✓ |

受牵连的 claim（**计 6 处，不判 producer**）：
1. L48 claim 3（正交性在标准化下存活 ← 齐性公理）
2. L54 claim 2（齐性提出 c_i ← 齐性公理）
3. L54 claim 3（第一/第二槽的公理归属 ← 齐性公理；其加性与对称性部分**有**合规锚点可引，故仅部分归因 SPEC）
4. L61 claim 3（同 L54 的齐性部分）
5. L68 claim 1（第一槽齐性提出 c_i ← 齐性公理）
6. **L69 claim 3（"(x,x) 由范数定义成为 ||x||²" ← 范数定义式 26 字符）** —— 这是任务书所述"该引的公式引不了、只能引旁边的散文"的**教科书样例**：旁边的 `15.10.md:105`（89 字符，合规）是 "DEFINITION. In a Euclidean space V, the nonnegative number $\|x\|$ defined by the equation"，**不含该等式本身**，引了也支撑不住断言。

补充：本片区 producer 在**有**合规锚点可引时的表现分裂 —— 正定性公理（35 字符，达标）被 L62、L55 之一正确引用（L62 引了，L55 未引）；加性公理（63 字符，达标）在 L54/L61/L68 三处需要它时**均未引**。故 §3 的 E-中 14 处**不能**归到 SPEC 头上。

## 7. 顺带发现的其它维度问题（登记，不裁定）

1. **L52 claim 6 逻辑指向糊。** 句子写 "Containing O is not by itself sufficient for orthogonality"，但反例 {O, i, i+j} 实际否掉的是"相依 + 含 O ⇒ 正交"这个**逆向蕴含**；且"含 O 是否足以推出正交"本身不是正文关心的命题。数学为真（§4.4 第 5 项），仅措辞指向不清。
2. **L67 与 L65 跨节点不一致**（已在 §3 E-2 记）：L67 把配对技巧归因于正交**规范**性，L65 正确地把它归因于正交性、把规范性归因于分母消失。
3. **L57 内部张力**（已在 D-4 记）：claim 2 自述"两步复合（含第一分句的独立性）"，末句却称"全部力量来自维数计数"。
4. **L69 claim 4 图内术语冲突。** 节点称 (15.12) 为 "the abstract Pythagorean theorem"（alias 亦含 "Pythagorean identity"）；命名为学界通用，但 **Apostol 在本章把 "Pythagorean formula" 用于正交分解式 (15.17)**（`15.14.md:24`：$\|x\|^2 = \|s\|^2 + \|s^\perp\|^2$），未用于 (15.12)。若 D4/D5 片区为 (15.17) 也建了 Pythagorean 名节点，图内会出现同名不同指。建议交叉核。另 `15.07.md:29` 中 "Pythagorean identity" 指的是 cos²+sin²=1，第三处不同指。
5. **L59 claim 2 后半略松**："其范数不要求已知"作为**前提**为真，但要**使用** (15.8) 必须能算 (e_j,e_j)（即范数平方）。
6. **L56 claim 2 未提相异性**：证明中对 x_j 复用正交性需 x_1..x_k **相异**；相异性来自 `15.07.md:3` 的相依定义。Apostol `:15` 亦只写 "where each $x_{i} \in S$"，属原文简写，**非 producer 引入**。
7. **L58 末句非数学断言**："记录这一点保持依赖诚实……"属图工程编辑性说明，无可核真值。若 SPEC 要求 statement 仅含可核断言，此类句子应移入 `atomic_reason` 或注释字段。同类：L63 claim 3"这是标准化自我回本之处"。
8. **`sections` 字段与实际依赖不符**：L53（依赖 `15.07.md:3` 的独立性定义）、L57（依赖 `15.08.md:23` 的 Thm 15.7(b)）、L62（依赖 `15.08.md:3` 的基定义）三者 `sections` 均只写 ["15.11"]。若下游按 `sections` 做覆盖统计或前置检查，会漏掉跨节依赖。
9. **可复用的正面模式**：L62 与 L68 是唯二带 `origin`/`origin_note` 的节点，也是唯二设了跨文件锚（`15.10.md`）的节点，同时是 §5 中判定最干净的两个。二者的做法（模型补充的推理显式标注 + 把被援引的公理跨文件锚上）若推广到 L46/L47/L52/L53 等，可直接消掉 §3 的 E-中 14 处过半。

## 8. 附录：逐批工作记录

### 批次 1：第 46–50 节点（15.11 正交性）

已读源：`15.10.md` 全文（1–158）、`15.11.md` 全文（1–92）。

**L46 `d:orthogonal-set-condition-on-distinct-pairs-only`** — 锚点 1 条（`15.11.md:3` 定义句）。claim 分解：
1. 相异元素对内积为零的定义 → 锚点覆盖 ✓
2. "distinct 是承重的，豁免了对角线，(x,x) 未被断言" → 是锚点内量词的合法读解，可接受
3. "正交集因此可以含零元" → **无锚**。真（`15.10.md:27` (O,y)=0；`15.11.md:5` "The zero element is orthogonal to every element of V"）
4. "其元素不要求非零" → **无锚**。真
5. 末句 "这正是迫使 Theorem 15.10 加额外假设的那个缺口" → **无锚**。真（Thm 15.10 `15.11.md:7` 写了 "of nonzero elements"）
判定：**形式 E**。3 条 claim 零锚点。非 SPEC 所致：`15.11.md:5`（98 字符）与 `15.11.md:7` 首句（79 字符）均合规可引，producer 只用了 1/3 锚点槽。死因 0=KEEP + 补锚。

**L47 `d:orthonormal-set-adds-unit-norm`** — 锚点 1 条（`15.11.md:3` 标准正交定义句）。claim 分解：
1. 标准正交 = 每元素范数 1 → 锚点覆盖 ✓
2. "不能含 O，因为 ||O||=0" → **无锚**。真（Thm 15.9(a)，`15.10.md:123`）
3. "所以标准正交集由 Theorem 15.10 自动独立" → **无锚**。真
4. "(e_j,e_j)=1" → **无锚**。真（需范数定义 `15.10.md:108`）
5. 末句 "这正是把 (15.8) 塌缩成 (15.9) 的东西" → **无锚**。真，且 `15.11.md:67` 原文 "This implies (15.8), and when $(e_j, e_j) = 1$ , we obtain (15.9)." 直接可引却未引
判定：**形式 E**。4 条 claim 零锚点，2 个锚点槽空置且有合规原文可引。死因 0=KEEP + 补锚。

**L48 `d:normalizing-an-orthogonal-set`** — 锚点 1 条（`15.11.md:35` 标准化句）。claim 分解：
1. 每个 u_n 除以范数得标准正交集，phi_n = u_n/||u_n|| → 锚点覆盖 ✓
2. "除法要求 ||u_n|| 非零，故 u_n 非零，故需正定性公理" → **无锚**。真（Thm 15.9(b)，`15.10.md:125`）
3. "正交性在除法下存活，因为标量按齐性从内积中提出" → **无锚**。真，但**理想锚点被 SPEC 卡死**：内积公理 (3) `(3) $c(x,y) = (cx,y)$` 仅 21 字符，低于 30 字符下限（计入 §6）
判定：**形式 E**，但 claim 3 属 SPEC 致证据不足，不判 producer。另见 §4.2 登记一处复空间措辞不严。死因 0=KEEP。

**L49 `d:trigonometric-orthogonal-system`** — 锚点 2 条（`15.11.md:20` u_n 定义式、`:29` "so S is an orthogonal set. Since no member..."）。claim 分解：
1. "在实空间 C(0,2pi) 配积分内积中" → **无锚**。真（`15.11.md:17` "In the real linear space $C(0, 2\pi)$ with the inner product..."，可引未引）
2. u_0=1, u_(2n-1)=cos nx, u_(2n)=sin nx → 锚点 1 覆盖 ✓
3. "满足 m≠n 时 ∫u_n u_m over [0,2pi] = 0" → **锚点射程不足/取端点**：锚点 2 起点是结论 "so S is an orthogonal set"，正交关系式本身在 `:23`–`:26`（未引，可引，第 3 槽空置）。结论确可反推该关系（配定义），但断言的是关系式而锚给的是结论
4. "所以 S 是正交集" → 锚点 2 覆盖 ✓
5. "Apostol 一步得独立性：没有成员是零元，故独立" → 锚点 2 覆盖 ✓
6. 末句 "这是 Theorem 15.10 的实作，展示非零假设被手工核验而非假定" → **无锚**。真（元观察正确；Apostol 在例中未显式引 Thm 15.10，但确实逐项核了非零）
判定：**形式 E（claim 1、6）+ 形式 D/C 边缘（claim 3）**。死因 0=KEEP + 补锚 `:17` 与 `:26`。

**L50 `d:trigonometric-system-norms`** — 锚点 2 条（`15.11.md:32` cos²/sin² 积分式、`:35` 范数结论）。claim 分解：
1. "(u_0,u_0) = ∫dx over [0,2pi] = 2pi" → **射程不足**：无锚点直接给该值；锚点 2 只给 ||u_0||=√(2π)，配范数定义可反推。原文 `:29` 有 "We have $(u_{0}, u_{0}) = \int_{0}^{2\pi} dx = 2\pi$"，可引未引，第 3 槽空置
2. "n≥1 时 ∫cos²(nx) 与 ∫sin²(nx) over [0,2pi] 均为 pi" → 锚点 1 覆盖 ✓
3. "故 ||u_0||=sqrt(2pi)，||u_n||=sqrt(pi) (n≥1)" → 锚点 2 覆盖 ✓
4. 末句 "u_0 与其余的不对称正是标准化系统有两个不同分母的原因，并且它是正交集一般不标准正交的具体例证" → **无锚**。真（`:38` φ_0=1/√(2π) 对 φ_(2n-1)=cos nx/√π，两个分母；且 S 的范数均 ≠1 故非标准正交）。`:38` 可引未引
数值人工验算（§4.4）：∫_0^{2π}dx = 2π ✓；∫_0^{2π}cos²(nx)dx = ∫(1+cos2nx)/2 = π ✓（整数 n≥1）；∫_0^{2π}sin²(nx)dx = π ✓；开方 √(2π)、√π ✓。全部正确。
判定：**形式 E（claim 4）+ 形式 D（claim 1）**。死因 0=KEEP + 补锚。

批次 1 小结：5/5 节点的 statement 末句均为无锚附加洞见，与任务书"缺陷集中在末句"的经验完全一致。5 节点的派生 claim 全部为真，故均不判死，但锚点射程系统性止于"教材原句"，未覆盖 producer 自己接上的推论。锚点槽普遍空置（46/47/48 用 1/3，49/50 用 2/3），可引原文就在同页——**不是 SPEC 的错**（唯一例外：L48 claim 3 的齐性公理 21 字符，SPEC 卡死）。

### 批次 2：第 51–55 节点（15.11 标准化系统 + Thm 15.10 必要性与证明三步）

补读源：`15.07.md:3,9`（相依/独立定义）、`15.08.md:3`（有限维定义）。

**L51 `d:normalized-trigonometric-system`** — 锚点 1 条（`15.11.md:38` φ_n 显式公式）。claim 分解：
1. φ_0=1/√(2π)、φ_(2n-1)=cos nx/√π、φ_(2n)=sin nx/√π (n≥1) → 锚点覆盖 ✓
2. "它是 C(0,2pi) 中的标准正交集" → **无锚**。真（`15.11.md:35` "we obtain an orthonormal set"，可引未引）
3. 末句 "它是 Parseval 公式 (15.11) 取得其经典 Fourier 含义的标准系统" → **无锚，且与所引公式的假设相冲突**。见下
数值验算（§4.4）：由 ||u_0||=√(2π) 得 φ_0=1/√(2π) ✓；||u_n||=√π 得 cos nx/√π、sin nx/√π ✓。与 `:38` 逐字一致。
**这是本批最重的一处**：公式 (15.11) 所在的 Theorem 15.12（`15.11.md:77`）明写 "finite-dimensional Euclidean space of dimension $n$" 且要求 $\{e_1,\ldots,e_n\}$ 是**正交规范基**。而 C(0,2π) 是无限维（`15.08.md:17` 的同型论证：其中含独立无限集），标准化三角系是无限集，**并非** (15.11) 意义下的基。故 (15.11) 按 Apostol 所证的形式**不适用于**本节点所述系统；经典 Fourier 版 Parseval 是 L² 完备性陈述，本章未证。
两面看：`15.11.md:91` 的 Note 确实说 Parseval "obtained this type of formula in a special function space"，故"经典 Fourier 含义"作为历史指向是可辩护的；但节点写成 "Parseval's formula (15.11) acquires its classical Fourier meaning"，读者极易读成"(15.11) 适用于该无限系统"，那是假的。
判定：**形式 D（锚点射程止于 φ 的显式公式，对 Parseval 一字未及）+ §4.1 有限维假设隐性侵蚀**。建议改法：把末句改为"其经典 Fourier 对应需无限维/完备性论证，超出 (15.11) 的有限维假设"，或删末句。死因：按当前措辞的歧义倾向可判 **1（量词过度断言）**；若裁定员采信 `:91` 的历史指向读法则 0=KEEP + 强制改写。我倾向 1，理由是节点显式引了编号公式 (15.11) 而非泛指 Parseval。

**L52 `d:15-10-nonzero-hypothesis-is-essential`** — 锚点 2 条（`15.11.md:7` Thm 15.10 首句、`:5` 零元句）。注：该节点带 `audit_fix = "audit-A1"` 字段（未读该文件，按硬禁）。claim 分解：
1. Thm 15.10 断言"非零元素正交集独立" → 锚点 1 覆盖 ✓（且锚点只截首句，未含 "In particular" 的有限维分句 —— statement 也只谈首句，**一致，无 §4.1 问题**）
2. "该限制真有必要，非装饰" → 本节点论点，由 3–5 支撑
3. "把 O 并入正交集仍正交，因为条件只约束相异元素对，且 O 与一切正交" → 后半由锚点 2 覆盖 ✓；前半"只约束相异对"**无锚**（定义在 `:3`，本节点未引）→ 局部射程不足
4. "但扩大后的集合相依" → **无锚**。真，但依据在 `15.07.md:3`（含 O 的集合取 c=1 即得非平凡零组合），**跨文件锚点未设**
5. "故若无非零假设定理会假" → **无锚**。真，由 3+4 推出
6. 反例 "{O, i, i+j} 含 O 且相依，却不是正交集" → **无锚**（节点自造，非 Apostol）。人工验算：取 i,j 为 V_2/V_3 标准基配点积，(i, i+j)=1≠0 故非正交 ✓；含 O 故相依 ✓。**数学为真**。但措辞可议：该例实际否掉的是"相依+含 O ⇒ 正交"这个逆向蕴含，而句子写成 "Containing O is not by itself sufficient for orthogonality"，逻辑指向略糊（登记入 §7）
7. 末句 "证明中该假设恰在一点被用到：除以 (x_1,x_1)" → **无锚**。真（`:15`，可引未引，第 3 槽空置）
判定：**形式 E（claim 4、5、6、7 零锚）+ claim 3 前半射程不足**。全部数学为真。死因 0=KEEP + 补锚（`:3`、`15.07.md:3`、`:15`）。

**L53 `d:15-10-proof-setup-vanishing-finite-combination`** — 锚点 2 条（`15.11.md:9` 证明开头、`:12` 求和式）。claim 分解：
1. 取 S 为非零元素正交集，设某有限线性组合为零，Σ_{i=1}^{k} c_i x_i = O → 锚点 1+2 完整覆盖 ✓
2. "有限性关乎论证的形状而非对 S 的限制" → **无锚**（元观察）。真
3. "S 本身可以是无限的，三角系就是" → **无锚**。真（`15.11.md:17`–`:20` 的 $\{u_0,u_1,u_2,\ldots\}$ 为无限集）
4. 末句 "无限集的独立性按定义就是关于其有限子集的陈述" → **无锚**。真，原文依据在 **`15.07.md:3`**："A set S of elements in a linear space V is called dependent if there is a finite set of distinct elements in S..."（跨文件可引，未引，第 3 槽空置）
判定：**形式 E（claim 2、3、4 零锚）**。全部为真，末句的关键定义依据在 15.07 而本节点 `sections` 只写 ["15.11"]。死因 0=KEEP + 补跨文件锚 `15.07.md:3`。

**L54 `d:15-10-proof-pair-with-a-single-element`** — 锚点 1 条（`15.11.md:15` 配对句）。claim 分解：
1. 与 x_1 配对、用 (x_1,x_i)=0 (i≠1) 得 c_1(x_1,x_1)=0 → 锚点覆盖 ✓
2. "这一行消耗三个事实：加性拆和、齐性提取 c_i、右端 (O,x_1)=0（后者是齐性取 c=0 的推论）" → **无锚**。真；`15.10.md:27` "Note: Taking $c = 0$ in (3), we find that $(O, y) = 0$ for all $y$ ." 直接可引（约 66 字符，合规）却未引
3. 末句 "无论把 x_1 放在哪个槽，两条所需规则中必有一条不是公理而是公理加对称性的推论，因为 Apostol 只在第二槽假设加性、只在第一槽假设齐性" → **无锚**。**人工核验：为真且非平凡**。公理 (2) `(x,y+z)=(x,y)+(x,z)` 加性在第二槽（`15.10.md:17`）；公理 (3) `c(x,y)=(cx,y)` 齐性在第一槽（`:19`）。证明取 (Σc_i x_i, x_1)：第一槽加性 —— 非公理，需对称性；第一槽齐性 —— 正是公理 (3)。若改取 (x_1, Σc_i x_i)：第二槽加性 —— 正是公理 (2)；第二槽齐性 —— 非公理，需推导。两种放法各缺一条，**claim 3 的二难成立** ✓
   锚点可得性：公理 (1) 行约 48 字符、公理 (2) 行约 62 字符，**均合规可引**；公理 (3) 行仅约 21 字符，**低于 SPEC 30 字符下限，卡死**（计入 §6）。第 2、3 槽空置。
判定：**形式 E（claim 2、3 零锚）**，其中 claim 3 的齐性公理部分属 SPEC 致证据不足，加性与对称性公理部分不是 SPEC 的错。另见 §4.2 登记复空间措辞一处。死因 0=KEEP + 补锚。

**L55 `d:15-10-proof-divide-by-nonzero-self-inner-product`** — 锚点 1 条（`15.11.md:15` "But $(x_{1}, x_{1}) \neq 0$ since..."）。claim 分解：
1. 由 c_1(x_1,x_1)=0 得 c_1=0，因 (x_1,x_1)≠0，而后者非零恰因 x_1≠O → 锚点完整覆盖 ✓
2. "这是证明中非零假设被花掉的唯一一点" → **无锚**（元观察）。真：原文中 x_1≠O 仅用于导出 (x_1,x_1)≠0，其余为对 x_j 的重复
3. "它是通过援引正定性公理花掉的" → **无锚**。真；`15.10.md:22` 的 `(x, x) > 0 \quad i f \quad x \neq O`（约 35 字符，**过下限，可引**）未引
4. 末句 "在正半定形式上这一步失败，定理随之失败" → **无锚**，典型末句越界洞见。**人工核验：为真**。反例：V=R²，退化形式 (x,y)=x_1y_1，则 e_2=(0,1)≠O 但 (e_2,e_2)=0；集合 {e_2, 2e_2} 相异对内积为 0 故"正交"、元素皆非零、却相依 ⇒ 定理失败 ✓。保留意见：正半定形式下已不是 Apostol 意义的 Euclidean space，故这是对修改后设定的反事实陈述；节点措辞已标明假设语气，可接受
判定：**形式 E（claim 2、3、4 零锚）**。全部为真。死因 0=KEEP + 补锚 `15.10.md:22`（合规，非 SPEC 所致）。

批次 2 小结：末句缺陷继续 5/5 命中。**L51 是本片区第一个实质性问题**（形式 D + 有限维假设隐性侵蚀，建议死因 1）；L52–L55 均为形式 E 但数学全真，且可引的合规原文就在同文件或 `15.07.md`/`15.10.md`，锚点槽大量空置。SPEC 真正卡死的只有内积公理 (3)（齐性，21 字符）。

### 批次 3：第 56–60 节点（Thm 15.10 收尾 + Thm 15.11 前提与公式 (15.8)）

补读源：`15.08.md:19–25`（Theorem 15.7）、`15.13.md:3,5,83,85`（正交/标准正交基存在性）。锚点候选字符数实测（`15.10.md`）：公理(1) 49、公理(2) 63、**公理(3) 21（卡下限）**、正定性式 35、`:27` 注 68、范数定义式 `:108` **26（卡下限）**。

**L56 `d:15-10-proof-repeat-for-each-index`** — 锚点 1 条（`15.11.md:15` 重复句）。claim 分解：
1. 把 x_1 换成 x_j 重复得每个 c_j=0，证明 S 独立 → 锚点完整覆盖 ✓
2. "重复是合法的，因为前两步没用到关于下标 1 的任何特殊之处，只用到 x_j 属于该正交集且非零" → **无锚**（元评注）。真：前两步只用了 x_j∈S 非零与 S 上相异对正交性
3. 末句 "这个下标对称性观察正是把单系数结论变成独立性的东西" → **无锚**（元评注）。真
判定：**形式 E（轻度）**。claim 2、3 是对**已被锚点覆盖的那句原文本身**的评注，射程外延很短，严重度低于 L51/L57。附带登记（§7）：claim 2 未提及 x_i 需**相异**（相异性来自 `15.07.md:3` 的相依定义），Apostol `:15` 亦仅写 "where each $x_{i} \in S$"，属原文简写，非 producer 引入的错误。死因 0=KEEP。

**L57 `d:15-10-n-orthogonal-nonzero-elements-form-a-basis`** — 锚点 2 条（`15.11.md:7` 第二分句、`:15` 证明末句）。claim 分解：
1. 第二分句：有限维、dim V=n，n 个非零元素的正交集是 V 的基 → 锚点 1 逐字覆盖 ✓（**§4.1：原文写了 finite-dimensional，节点保留 ✓，无增无删**）
2. "Apostol 的证明是两步复合：独立性来自第一分句，再用 Theorem 15.7(b) 把 n 维空间中 n 个独立元素升级为基" → 锚点 2 覆盖 Thm 15.7(b) 这一步 ✓；"独立性来自第一分句"隐含未锚；**对 Thm 15.7(b) 内容的刻画需跨文件核**：`15.08.md:19,23` "Let $V$ be a finite-dimensional linear space with $\dim V = n$ ... (b) Any set of n independent elements is a basis for V." → **刻画准确 ✓，非数学错误**。但该依据在 15.08，本节点无跨文件锚点
3. 末句 "所以这一分句不增加任何内积内容；它的全部力量来自一个更早证明的维数计数定理" → **无锚，且措辞过强**。前半（"不增加"= 相对第一分句的**增量**无内积内容）可辩护；后半 "whole force comes from a dimension-counting theorem" **不成立**：该分句的结论仍依赖第一分句"正交非零 ⇒ 独立"，那正是内积内容（正定性 + 正交性）；没有它，Thm 15.7(b) 无从启动。且与本节点 claim 2 自己给出的"两步复合"存在内部张力
判定：**形式 D 确认**。锚点射程止于 `:15` 的 "Theorem 15.7(b) shows that S is a basis"，对"全部力量来自维数计数"这一归因**一字未及**，而该归因越出射程且过强。建议改法：把末句改为"该分句相对第一分句的**增量**内容不涉及内积，仅为维数计数（Thm 15.7(b)）"，并补 `15.08.md:23` 跨文件锚。死因：**0=KEEP + 强制改写**（理由：前半正确、claim 2 已给出正确分解，属措辞过强而非数学错误）；若裁定员按字面读 "whole force" 则可升为 1。

**L58 `d:existence-of-an-orthogonal-basis-deferred`** — 锚点 1 条（`15.11.md:41`）。claim 分解：
1. Apostol 标明"每个有限维 Euclidean 空间有正交基"仅在 15.13 节才证 → 锚点逐字覆盖 ✓（**§4.1：finite-dimensional 保留 ✓**）。跨文件核实：`15.13.md:3` "If the space is Euclidean, we can always construct an orthogonal basis"、`:83`、`:85` Thm 15.14，**确实在 15.13 证出 ✓**
2. "故 Theorem 15.11 实践上是有条件的：它计算的是**相对于**一个正交基的分量，并假设已给定一个" → **无锚**。真（依据在 `:43` Thm 15.11 前提句，本节点未引，第 2、3 槽空置）
3. 末句 "记录这一点保持依赖诚实，否则 15.11 容易被读成正交基的存在性已然已知" → **无锚**，且属**图工程编辑性说明，非数学断言**，不构成可核真值的 claim（登记入 §7）
判定：**形式 E（claim 2 零锚）**。数学为真。死因 0=KEEP + 补锚 `:43`。

**L59 `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis`** — 锚点 2 条（`15.11.md:43` Thm 15.11 前提、`:46` 式 (15.7)）。claim 分解：
1. 假设 V 有限维、dim=n，S={e_1..e_n} 为**正交基**，并从给定表示 x=Σc_i e_i (15.7) 出发 → 锚点 1+2 完整覆盖 ✓（**§4.1：finite-dimensional 与 dimension n 均保留，逐字一致 ✓**）
2. "注意什么没被假设：e_i 不要求标准正交，其范数不要求已知" → 前半由锚点 1 的"缺席"直接支撑（原文写 orthogonal basis 而非 orthonormal）✓，可接受；后半"范数不要求已知"略松（作为**前提**为真；但要**使用** (15.8) 必须能算 (e_j,e_j)，即范数平方）→ 登记 §7
3. 末句 "基假设提供表示的存在性；正交假设使系数可计算" → **无锚**。真且分解干净。细微处：Apostol `:43` 写的是条件式 "If an element $x$ is expressed as a linear combination"（把表示当假设给出），而节点说基假设"供给"存在性 —— 数学上为真（基张成全空间），与原文措辞略异，不构成错误
判定：**形式 E（轻度，仅末句）**。本批最干净的节点之一，claim 2 前半属"由锚点措辞的缺席支撑"的合法读解。死因 0=KEEP。

**L60 `d:orthogonal-basis-component-formula`** — 锚点 2 条（`15.11.md:49` 引出句、`:52` 式 (15.8)）。claim 分解：
1. Thm 15.11 结论：x 相对有序基 (e_1..e_n) 的分量为 c_j=(x,e_j)/(e_j,e_j)，j=1..n → 锚点 1（"components relative to the ordered basis"）+ 锚点 2（公式逐字含 `\tag{15.8}`）**配对良好，完整覆盖 ✓**。这是本片区锚点组合最恰当的一例：散文锚点承接"有序基/分量"的语义，公式锚点承接等式本身
2. "意义在于计算上的解耦：每个分量由一次针对单个基元素的内积得到，无线性系统可解，不牵涉其它分量" → **无锚**（对已锚公式的解释性读解）。真，由 (15.8) 形式直读
3. 末句 "这种解耦恰是正交性买来的" → **无锚**。真，原文依据在 `:67` "since $(e_i, e_j) = 0$ if $i \neq j$"，**可引未引，第 3 槽空置**
判定：**形式 E（轻度，claim 3）**。解释性 claim 严格说超出射程，但均为对已锚公式的直读，严重度低。死因 0=KEEP + 可补锚 `:67`。

批次 3 小结：末句缺陷 4/5 命中（L59 末句为真且干净，L56 末句为轻度元评注）。**新增一个形式 D 确认：L57 末句"全部力量来自维数计数定理"**，越出锚点射程且与本节点 claim 2 内部张力。跨文件依据（`15.08.md:23` Thm 15.7(b)、`15.13.md:85` Thm 15.14）经核**全部为真**，producer 的刻画无数学错误，问题纯在锚点未跨文件设置。§4.1 本批 3 处涉及有限维假设（L57、L58、L59），**全部忠实保留原文，无增删**。

### 批次 4：第 61–65 节点（Thm 15.11 证明、(15.9)、(15.10)、Parseval 前提比较）

**L61 `d:15-11-proof-pair-with-basis-element`** — 锚点 2 条（`15.11.md:61` 证明引出句、`:64` 展开式）。claim 分解：
1. 与 e_j 作内积得 (x,e_j)=Σc_i(e_i,e_j)=c_j(e_j,e_j)，**因为 (e_i,e_j)=0 当 i≠j** → 等式链由锚点 2 逐字覆盖 ✓；但 "since (e_i,e_j)=0 for i not j" 这个**理由子句在 `:67`，本节点未锚**（第 3 槽空置，且 `:67` 确实合规可引 —— L62 就锚了它）→ **局部射程不足**
2. "这与 Theorem 15.10 证明第 2 步是结构相同的一招，只是作用在 x 的表示上而非零组合上" → **无锚**（跨节点比较性元评注）。真（比对 `:15` 与 `:64` 成立）
3. 末句 "并且它同样消耗加性来拆和、齐性来提出 c_i" → **无锚**。真；同 L54，齐性公理 21 字符**被 SPEC 卡死**，加性公理 63 字符可引未引
判定：**形式 E（claim 2、3）+ 形式 D（claim 1 的理由子句）**。死因 0=KEEP + 补锚 `:67`。

**L62 `d:15-11-denominator-nonzero-by-positivity`** — 锚点 2 条（`15.11.md:67`、**`15.10.md:22` 正定性公理**）。**本节点带 `origin = "model"` 与 `origin_note`，明确标注了"Apostol 只写 This implies (15.8)，分母非零是隐含的"**。claim 分解：
1. 从 c_j(e_j,e_j)=(x,e_j) 过到 (15.8) 需要 (e_j,e_j)≠0 → 锚点 1 给出 "This implies (15.8)"，"需要非零"为 model 观察，已由 `origin_note` 诚实标注 ✓
2. "Apostol 未就此评论" → 真（`:67` 原文确实只写 "This implies (15.8)"）✓
3. "基元素不能是 O，因为含 O 的集合相依" → **无锚**。真（基 = 独立 + 张成，`15.08.md:3`；含 O 则取系数 1 得非平凡零组合，`15.07.md:3`）。**跨文件锚未设，第 3 槽空置**
4. "非零元素由正定性公理有 (e_j,e_j)>0" → 锚点 2 逐字覆盖 ✓。**注：正定性式实测 35 字符，过 SPEC 下限，可引且已引 —— 这是本片区唯一一处 producer 主动引用了内积公理的地方**
5. 末句 "所以 (15.8) 依赖严格正定性，尽管其陈述中不出现任何不等式" → **无锚**，但是 claim 3+4 的直接归纳。真
判定：**形式 E（仅 claim 3 跨文件依据零锚）**。本批**构造最扎实的节点**：有 origin 标注、跨文件锚了公理、且把 Apostol 的隐含步骤显式化。死因 0=KEEP + 补 `15.08.md:3` 或 `15.07.md:3`。

**L63 `d:orthonormal-basis-component-is-the-inner-product`** — 锚点 2 条（`15.11.md:55` "In particular, if S is an orthonormal basis..."、`:58` 式 (15.9)）。claim 分解：
1. (15.8) 的特化：S 为正交规范基则 c_j=(x,e_j) → 锚点 1+2 完整覆盖 ✓
2. "机制被 Apostol 用一个从句说出：when (e_j,e_j)=1 we obtain (15.9)" → **该句在正文中被复述却未锚**（原文 `:67`，本节点未引，第 3 槽空置）。这是**引文式复述而无锚点**的一例，比纯洞见更该补锚
3. "单位范数条件只是抹掉了分母" → **无锚**。真（(e_j,e_j)=||e_j||²=1）
4. 末句 "这是标准化自我回本之处，也是使 Parseval 公式可纯用内积表达的东西" → **无锚**。真：(15.11) 中系数即 (x,e_i)，无分母，故纯内积表达 ✓
判定：**形式 E（claim 2、3、4）**。数学全真。死因 0=KEEP + 补锚 `:67`。

**L64 `d:orthonormal-expansion-formula`** — 锚点 2 条（`15.11.md:69` 引出句、`:72` 式 (15.10)）。claim 分解：
1. 把 (15.9) 代入 (15.7)，把任意 x 在正交规范基下写成 x=Σ(x,e_i)e_i → 锚点 1（"Equation (15.7) can be written in the form"）+ 锚点 2（(15.10) 逐字含 tag）完整覆盖 ✓
2. 末句 "这个自再生形式（元素自身对基的内积充当其系数）正是与 y 配对以产生 Parseval 公式的那个恒等式" → **无锚**。真，且**原文 `:89` "Taking the inner product of both members of Equation (15.10) with y and using the linearity property of the inner product, we obtain (15.11)." 直接支撑该断言，合规可引，第 3 槽空置**
判定：**形式 E（末句）**。真。死因 0=KEEP + 补锚 `:89`（这是本片区最"该引却没引"的一条，因为末句断言的正是 `:89` 说的事）。

**L65 `d:parseval-requires-an-orthonormal-basis`** — 锚点 2 条（`15.11.md:77` Thm 15.12 前提、`:43` Thm 15.11 前提）。claim 分解：
1. Thm 15.12 假设正交**规范**基，严格强于 Thm 15.11 的正交基 → **两条锚点并置恰好支撑该比较 ✓**，是本片区锚点使用最贴切的比较型节点。严格性由 L50 的三角系（正交但范数 ≠1）反证 ✓。**§4.1：两条锚点均含 finite-dimensional，节点亦未增删 ✓✓**
2. "理由可追溯：证明把式 (15.10) 与 y 配对，而 (15.10) 本身只在正交规范条件下存在" → **无锚**。真（`:69`、`:89` 均支撑，均合规可引，第 3 槽空置）
3. 末句 "若只有正交基，类似公式会带上 1/(e_i,e_i) 因子，所以 (15.11) 干净的**双线性**形式恰是单位范数买来的" → **无锚**。**人工验算**：正交非规范基下 c_i=(x,e_i)/(e_i,e_i)，故 (x,y)=Σc_i(e_i,y)=Σ(x,e_i)·conj((y,e_i))/(e_i,e_i) —— **1/(e_i,e_i) 因子确实出现 ✓，数值/符号结论正确**。
   **但"bilinear（双线性）"一词在复空间下是错的**：Apostol `15.10.md:45` 明言本章定理对复 Euclidean 空间同样有效，而 (15.11) 在 `:80` 正是为此带了共轭杠 `(\overline{{y , e_{i}}})`，即该形式对第二变元是**共轭线性**（sesquilinear / 共轭双线性），并非双线性。见 §4.2。
判定：**形式 E（claim 2、3）+ §4.2 实复分裂用词缺陷（末句 "bilinear"）**。实质数学（1/(e_i,e_i) 因子）正确；缺陷限于术语。建议把 "clean bilinear form" 改为 "干净的（共轭）双线性形式"或径称"干净形式"。死因 0=KEEP + 术语改写。

批次 4 小结：末句缺陷 5/5 命中。**新增 §4.2 实质发现：L65 末句称 (15.11) 为 "bilinear"，忽略了原文为复空间专门加的共轭杠。** L62 为本片区构造最扎实节点（origin 标注 + 跨文件公理锚 + 显式化隐含步骤）。L64 末句所断言的内容恰有 `:89` 逐字支撑而未引，是"锚点槽空置"最刺眼的一例。第 3 锚点槽在 61–65 中**全部空置**（5/5 只用 2/3），故本批**无一处可归因于 SPEC 的 3 锚点上限**；唯一 SPEC 卡死项仍是齐性公理 21 字符（L61 claim 3）。

### 批次 5：第 66–69 节点（Parseval 公式、证明、共轭来源、范数形式）

补读源：`15.15.md:25–51`（Example 1，Apostol 实际抵达 Fourier 系数的路径）、`15.14.md:24`（Pythagorean formula 的原文用法）、`15.11.md:91`（Parseval 命名注）。

**L66 `d:parseval-formula-statement`** — 锚点 2 条（`15.11.md:80` 式 (15.11)、`:75` 引出句）。claim 分解：
1. (15.11)：(x,y)=Σ_{i=1}^{n}(x,e_i)·conj((y,e_i)) → 锚点 1 逐字覆盖 ✓
2. "它把抽象内积化归为分量标量积的有限和" → 锚点 2 "can be computed in terms of their components" 支撑 ✓
3. "即：在正交规范基下**每个** Euclidean 空间都像 V_n 配点积那样计算" → **无锚，且有两处问题**：
   - **§4.1 有限维假设被抹掉**：锚点 2 原文写的是 "in a **finite-dimensional** Euclidean space with an orthonormal basis"，节点自己的复述改成 "**every** Euclidean space"。无限维 Euclidean 空间（如 C(0,2π)）没有有限正交规范基，(15.11) 的有限和形式不成立。"in an orthonormal basis" 一语隐含预设了基的存在，可部分挽救，但量词 "every" 越界
   - **§4.2 实复分裂**：实空间下 (15.11) 确与点积公式 (15.5) 同形 ✓；但复空间下是 Hermite 点积 Σx_i·conj(y_i)，而 `15.10.md:8` 定义的 dot product 是 Σx_i y_i，**无共轭**。故"像 V_n 配点积"在复情形下不准确，而 Apostol `:45` 明言本章定理对复空间同样有效
4. 末句 "注意共轭的位置：在 y 因子上，即**第二个**变元的分量上" → **锚点 1 直接覆盖 ✓**（公式逐字含 `(\overline {{y , e _ {i}}})`）。**这是本片区 24 个节点中唯一一个末句被锚点覆盖的节点**
判定：**形式 D 确认（claim 3）+ §4.1 + §4.2**。建议改法：把 claim 3 改为"在**有限维**空间的正交规范基下，其计算与 V_n 配（Hermite）点积同形"。死因：字面读 "every" 可判 **1（量词过度断言）**；采信 "in an orthonormal basis" 的隐含预设读法则 0=KEEP + 强制改写。

**L67 `d:parseval-proof-pair-expansion-with-y`** — 锚点 1 条（`15.11.md:89` 证明句）。claim 分解：
1. "Apostol 对 (15.11) 的全部证明就是一招：把 (15.10) 两端与 y 作内积并用线性性" → 锚点逐字覆盖 ✓；"whole proof" 经核为真（`:89` 即全部证明，仅另加 x=y 一句）
2. "这是**两节之内**同一技巧的第三次出现，前有 Theorem 15.10 与 Theorem 15.11" → **无锚，且该从句为假**。三次出现分别在 `:15`（Thm 15.10 证明）、`:61`（Thm 15.11 证明）、`:89`（Thm 15.12 证明）—— **三者全部位于同一节 §15.11**（`15.11.md` 标题为 "## 15.11 Orthogonality in a Euclidean space"，其中含 Theorem 15.10/15.11/15.12；§15.10 内是 Theorem 15.8/15.9）。节点把**定理编号**误当**节号**，故"两节"不成立。节点自身 `sections` 字段亦只写 ["15.11"]，与该从句自相矛盾。建议改为 "the third occurrence within Section 15.11"
3. 末句 "并且这正是正交**规范**性所要促成的技巧" → **无锚，且不准确**：该配对技巧在**仅正交**的基上同样奏效（Thm 15.11 的证明 `:61` 用的就是正交基，非规范），正交规范性买到的是**分母消失**而非技巧本身 —— L65 自己就正确地做了这个区分，故此处与 L65 存在跨节点不一致
判定：**形式 E，且其中两个无锚从句一假一失准**。这是本片区**唯一含明确假陈述**的节点。死因：**0=KEEP + 强制改写**（claim 1 完好、锚点无误，缺陷限于两个可删的附加从句）；若裁定员对无锚假陈述从严，claim 2 可支持 **4**。

**L68 `d:parseval-conjugate-comes-from-hermitian-symmetry`** — 锚点 2 条（`15.11.md:80` 式 (15.11)、**`15.10.md:32` 公理 (1')**）。带 `origin = "model"` 与 `origin_note`（诚实标注 Apostol 未分析共轭位置）。claim 分解：
1. "把 (15.10) 与 y 在第一槽配对得 (x,y)=Σ(x,e_i)(e_i,y)，其中 c_i=(x,e_i) 由**第一槽齐性**提出，不产生共轭" → **无锚**（model 推导）。**人工验算**：x=Σc_i e_i，(x,y)=Σ(c_i e_i, y)，由公理 (3) c(x,y)=(cx,y) 得 =Σc_i(e_i,y) ✓ **第一槽齐性正是公理，无共轭 ✓ 正确**。（旁注：第一槽**加性**仍非公理，需对称性 —— 与 L54 的观察一致，本节点未提，属省略非错误）
2. "共轭只在最后一步出现：由公理 (1') 把 (e_i,y) 改写为 conj((y,e_i))" → 锚点 2 覆盖公理 (1') ✓。验算：(1') 即 (x,y)=conj((y,x))，故 (e_i,y)=conj((y,e_i)) ✓ **正确**
3. "从而公式以 y 的分量而非反序积表达" → **无锚**。真（y 的分量按 (15.9) 即 (y,e_i)）✓
4. 末句 "所以 (15.11) 的共轭是 Hermite 对称的记账后果，而非共轭齐次性的后果" → **无锚**（model 归因，已诚实标注）。**验算：成立**。理由有二：(a) 在 Apostol 与本节点所走的第一槽路径上，共轭确实只由 (1') 引入；(b) 更根本地，共轭齐次性本身是**派生的** —— `15.10.md:35`–`:41` 明写 "From the homogeneity axiom and $(1')$ , we get the companion relation ... $= \bar{c}(x, y)$"，即共轭齐次性 = 齐性 + (1')。故归因于 Hermite 对称更本源 ✓。`:35`/`:40` 可作第 3 锚点强化该归因，未引
判定：**形式 E（claim 1、3、4 的推导步无锚）**，但**全部经人工验算为真**，且有 origin 标注 + 跨文件公理锚。**与 L62 并列为本片区构造最扎实的两个节点**（也正是唯二带 origin=model 标注、唯二设了跨文件锚的节点）。死因 0=KEEP。

**L69 `d:parseval-norm-form`** — 锚点 2 条（`15.11.md:86` 式 (15.12)、`:89` "When x = y, Equation (15.11) reduces to (15.12)."）。claim 分解：
1. "在 (15.11) 中取 x=y 得 ||x||²=Σ|(x,e_i)|²" → 锚点 1+2 **配对完整覆盖 ✓**（锚点 2 给代入动作，锚点 1 给结果公式）
2. "(x,e_i) 乘自身共轭成为模平方" → **无锚**。真（z·conj(z)=|z|²）；`15.10.md:94` "(x, y) (y, x) = (x, y) \overline {{(x , y)}} = | (x, y) | ^ {2}" 可作第 3 锚点，未引
3. "(x,x) 由范数定义成为 ||x||²" → **无锚，且理想锚点被 SPEC 卡死**：范数定义式 `15.10.md:108` 实测 **26 字符 < 30 下限**；旁边的散文 `:105`（89 字符，合规）**不含该等式** —— 这正是任务书描述的"该引的公式引不了、只能引旁边的散文"，计入 §6
4. "这是抽象的 Pythagoras 定理" → **无锚**。命名为学界通用，但**登记一处图内术语冲突**（§7）：Apostol 在本章把 "Pythagorean formula" 用于**正交分解**的 (15.17)（`15.14.md:24`：$\|x\|^2 = \|s\|^2 + \|s^\perp\|^2$），未用于 (15.12)
5. 末句 "并且特化到标准化三角函数系上，它就是 Fourier 系数的经典 Parseval 恒等式" → **无锚，且与 L51 末句同型的有限维越界**。(15.12) 的前提是有限维 + 正交规范**基**；标准化三角系是无限集、C(0,2π) 是无限维，故 (15.12) 无法"特化"得到经典 Parseval 恒等式（后者是需完备性的 L² 无穷级数陈述，本章未证；仅取有限段 {φ_0..φ_{2n}} 只能得到子空间内的等式或一般 x 的 Bessel 不等式，均非经典 Parseval 恒等式）。
   **反证性旁证（跨文件）**：Apostol 自己抵达 Fourier 系数的路径在 `15.15.md:25`–`:39` —— 他先把 φ_0..φ_{2n} 张成的**有限维子空间 S（dimension 2n+1）**取出，再在 S 上谈投影与 "Fourier coefficients"，得到的是**逼近**定理（`15.15.md:51`：范数 ||f − f_n|| 尽可能小），**不是** Parseval 恒等式。这恰恰说明有限维限制在原文中被一路保持
判定：**形式 D 确认（末句）+ §4.1 有限维越界 + §4.3 相关的 SPEC 卡死一处（claim 3）**。建议改法：末句改为"其无限维/Fourier 对应需完备性论证（参 §15.15 的有限维子空间逼近路径），超出 (15.12) 的前提"，或删。死因：**1（量词过度断言）**，与 L51 同判。

批次 5 小结：末句缺陷 3/4 命中（**L66 是 24 节点中唯一末句被锚点覆盖者**）。本批产出本片区最重的三处：**L69 与 L51 构成同型配对**（Parseval 处理的两端各有一次"跨到无限维经典 Fourier"的越界，建议同判 1）；**L67 含唯一明确假陈述**（"两节之内"，把定理号误作节号）；**L66 抹掉了锚点原文中的 finite-dimensional 并改写为 every**。另一面，L68 与 L62 是构造最扎实的两个节点。

**全片区锚点槽统计（机器统计，人工复核）**：24 节点中 9 个用 1 条锚点、15 个用 2 条，**没有任何一个节点用满 3 条**。锚点分布：`15.11.md` 37 条、`15.10.md` 仅 2 条（且这 2 条跨文件锚全部出自唯二带 `origin=model` 标注的 L62、L68）。



## 9. 未覆盖事项声明

**节点覆盖：完整。** 第 46–69 行全部 24 个节点均已逐条拆 claim 审完，无遗漏、无因中断跳过者。

**以下事项本报告未覆盖，不做结论：**

1. **任务书预期的 15.09 / 15.10 / 15.12 节点不在本片区。** 本 24 个节点的 `sections` 与锚点全部指向 15.11（另 2 条跨文件锚指向 `15.10.md`）。故任务书专项 3（反常积分收敛，`15.10.md:73`/`:75`–`:81` 的权函数与 Example 5）在本片区**无适用节点**（§4.3 已给机器复核证据）；任务书列举的符号值（(e−1/e)/2、3/e、e²−1、pi − 2 sin x、x = 1 and y = i）在本片区**全部命中 0**，应在 D3 前 45 个节点或 D4（15.12 习题）中，需由相应审查员处理。
2. **Cauchy–Schwarz（Thm 15.8）与范数性质（Thm 15.9）本身无节点在我的范围内。** 我只在核 L48/L55/L69 的无锚 claim 时把 `15.10.md:83`–`:149` 当依据读过，未审这两条定理的节点（若存在，在片 1–45）。
3. **Gram-Schmidt（15.13）与投影（15.14/15.15）无节点在我的范围内。** 我只把 `15.13.md:3,5,83,85`、`15.14.md:20–30`、`15.15.md:25–55` 当作核 L58、L69、L51 的跨文件依据读过。特别是：§4.1 中用 `15.15.md` Example 1 反证 L51/L69 的越界，该结论**只针对 L51/L69 的措辞**，不构成对 15.15 相关节点（若在 D5）的任何判定。
4. **A 型（变量位置错）与 B 型（借用引文）未系统排查**，非我职责。仅顺带注意到 `15.11.md:67` 被 L62 锚用、同时被 L63 在正文中复述而未锚（§3 E-中），以及 `15.11.md:15` 被 L54、L55、L56 三节点分别截取不同子串、`:43` 被 L59 与 L65 共用、`:80` 被 L66 与 L68 共用 —— **这些共享是否构成 B 型借用，我未判定**，留给 B 型审查员与 `共享引文清单.md` 的口径。
5. **未读任何其它审查员的输出**（`审查-形式D-*.md` 除本文件、`裁定-*.md`、`_捞回-*.md`、`audit-*`），未读任何 `M08实验-Apostol微积分卷1/` 目录。故本报告与其它审查员的编号、判型口径、死因倾向**可能不一致**，一致化由裁定员处理。也因此，**若某节点已被其它审查员或 `audit-A1` 处理过**（如 L52 带 `audit_fix = "audit-A1"` 字段），我的判定是在不知其内容的前提下独立作出的。
6. **`edges-D3.jsonl` 未审。** L46 的 `atomic_reason` 称"关于零元的推论通过 edges 记录而非拆出"、L52 依赖 `parent` 指向的 `apostol:theorem-15-10-...` 节点 —— **这些边是否真的存在、是否承载了 statement 中的无锚 claim，我未核**。若边确实承载了那些推论，§3 中部分 E 型（尤其 L46 claim 3/4、L47 claim 3）的严重度可下调；这是本报告最主要的**可能高估处**，明确标出。
7. **`SPEC.md` 未通读。** §6 的字符数为我实测，30 字符下限 / 3 锚点上限的口径直接采用任务书给定值，未回查 SPEC 原文是否另有例外条款（如公式行豁免）。若 SPEC 对公式行有豁免，§6 的 6 处牵连计数需重算。
8. **死因编码为建议值，非裁定。** 特别是 L51/L66/L69 的 **1 vs 0+改写** 与 L67 的 **0+改写 vs 4**，两种读法我都已在 §2、§3 写明依据，取舍权在裁定员。

---

状态：**已完成**（24/24 节点，5 批全部落盘）。
