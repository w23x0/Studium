# 15.15 覆盖完备性

## 〇 结论速览（本节 43 句 / 已覆盖 33 / 下移 0 / 未覆盖 10；其中值得补建 0 个）

**主控给的前提不成立，这是本轮最重要的一条。**

任务书写「15.15 的例 2（Legendre 多项式）完全没有对应节点」。核实结果：**例 2 有节点，且有两个。**
- `apostol:legendre-polynomial-approximation`（`data/nodes-A2-s3.jsonl:6`，method，sections=15.15，**3 条锚点**，8 条出边）
- `apostol:best-linear-approximation-to-sine`（`data/nodes-A2-s3.jsonl:7`，concept，sections=15.15，**3 条锚点**，3 条出边）——即例 2 里 f(x)=sin πx 的算例

依据：`tmp_index/anchors.tsv` 中 file=15.15.md 的 31 条锚点里，这两个节点占 6 条；
`edges.tsv` 中 ev_file=15.15.md 的 56 条边里，这两个节点作 src 的占 11 条
（`data/edges-A2-bridge.jsonl:4`、`data/edges-A2-s3.jsonl:29–35,38–40`）。
`exposure.tsv`：legendre 节点 T1=3 / T2=8 / T3=0 / 池 11 / 不同源行 6；sine 节点 T1=3 / T2=3 / 池 6 / 不同源行 6。
**建议动作：撤回该前提**，不要按「补建例 2 节点」执行。置信度高。

真实缺口是另一件事，性质轻得多：**5 处已有节点的锚点未覆盖到自己 statement 里已断言的公式**，
其中最严重的一处（例 2 的投影公式，源第 56 行）在全图 921 条锚点 + 1433 条边证据里**一次都没出现过**，
而 `apostol:legendre-polynomial-approximation` 的 statement 明文断言了它。这是死因 6 的合格候选。
全部 5 处都是**补锚点**（FIX），**不需要新建任何节点**，故「值得补建 d=0」。

覆盖口径说明：43 句 = 按句切（不按行切，源第 3 行 nsent=4、第 25 行 nsent=3、第 53 行 nsent=5，
按行号比对会高报，见 `source-lines.tsv`）。33 已覆盖 = 26 由节点锚点直接覆盖 + 5 仅由边 evidence 覆盖（T2）
+ 2 由同义散文锚点覆盖而公式本身无锚。下移 0：本节 `partof.tsv` 只有 2 个父节点有子
（`apostol:theorem-15-16-approximation-theorem` 1 个、`apostol:fourier-coefficients` 1 个），
无一处是「内容从本节句子下移到后代」造成的缺口。

## 一 逐句覆盖表（句号 / 句子摘要 / 覆盖状态 / 覆盖者 id 或 file:line / 判据）

句号按**句**编号（S1–S43），不按行。「源行」列给出该句所在的 `15.15.md` 行号，
凡该行 nsent>1 者已在本表内切开成多句。状态记号：
**锚**=节点锚点逐字覆盖；**边**=仅边 evidence 覆盖（T2，内容在图里但记在边上）；
**散**=同义散文句已锚而该公式本身无锚；**无**=未覆盖。

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 1 | 节标题：用有限维子空间的元素作最佳逼近 | 锚 | `apostol:best-approximation-in-a-finite-dimensional-subspace` a0 | anchors.tsv 102 字符 |
| S2 | 3 | THEOREM 15.16. APPROXIMATION THEOREM.（定理标号） | 锚 | `apostol:theorem-15-16-approximation-theorem` a0 | anchors.tsv 131 字符（含 S3） |
| S3 | 3 | 设 S 是 Euclid 空间 V 的有限维子空间，x∈V（假设） | 锚 | `d:approximation-proof-starts-from-the-orthogonal-decomposition` a1 | anchors.tsv 93 字符；另 4 条边 |
| S4 | 3 | 则 x 在 S 上的投影比 S 中任何其它元素更近（结论） | 锚 | `apostol:theorem-15-16-approximation-theorem` a1 | anchors.tsv 73 字符；shared 3 holders |
| S5 | 3 | 即：若 s 是 x 在 S 上的投影，则有 | 锚 | `x:best-approximation-by-the-projection` a0 | anchors.tsv 124 字符（含 S4+S5） |
| S6 | 6 | 公式 ‖x−s‖ ≤ ‖x−t‖ | 散 | S4 是其散文等价，已锚 | grep 全图 anchors/edges 该式 0 命中；且该式仅 28 字符，低于 SPEC 30 下界，不跨行则无法独立成锚 |
| S7 | 9 | 对一切 t∈S；等号成立当且仅当 t=s | 锚 | `apostol:theorem-15-16-approximation-theorem` a2 等 4 节点 | shared-quotes.tsv 69 字符 5 holders |
| S8 | 11 | 证明：由定理 15.15 可写 x=s+s⊥，s∈S、s⊥∈S⊥ | 锚 | `d:approximation-proof-starts-from-the-orthogonal-decomposition` a0 | anchors.tsv 99 字符；另 3 边 |
| S9 | 11 | 于是对 S 中任意 t 有 | 锚 | `d:x-minus-t-splits-orthogonally` a1 | anchors.tsv 34 字符 |
| S10 | 14 | 公式 x−t=(x−s)+(s−t) | 散 | `d:x-minus-t-splits-orthogonally` statement 明文含该式，两条锚点（第 11、17 行）均不含 | grep 全图 0 命中；该式 26 字符，低于 SPEC 30 下界 |
| S11 | 17 | 因 s−t∈S 且 x−s∈S⊥，这是 x−t 的正交分解，故其范数由勾股公式给出 | 锚 | `d:x-minus-t-splits-orthogonally` a0 | anchors.tsv 152 字符 |
| S12 | 20 | 公式 ‖x−t‖²=‖x−s‖²+‖s−t‖² | 锚 | `d:pythagorean-split-of-the-approximation-error` a0 | anchors.tsv 58 字符 |
| S13 | 23 | 因 ‖s−t‖²≥0 故 ‖x−t‖²≥‖x−s‖²，等号当且仅当 s=t | 锚 | `d:equality-holds-exactly-when-t-equals-s` a0 | anchors.tsv 120 字符 |
| S14 | 23 | 证毕（This completes the proof.） | 无 | —— | grep 全图 0 命中；见 §三 |
| S15 | 25 | EXAMPLE 1. 用三角多项式逼近 [0,2π] 上的连续函数 | 锚 | `apostol:trigonometric-polynomial-approximation` a0 | anchors.tsv 93 字符 |
| S16 | 25 | 取 V=C(0,2π)，并定义内积 (f,g)=∫₀^{2π} fg | 锚+边 | 后半由 `apostol:trigonometric-polynomial-approximation` a1 锚（80 字符）；前半「Let V=C(0,2π)…」仅在边 `edges-A2-s3.jsonl:15,24` | **过报通道 c 实例**：同一源行两半分落节点锚与边证据 |
| S17 | 25 | 15.11 节已给出三角函数正交集 φ₀,φ₁,φ₂,… | 边 | `edges-A2-s3.jsonl:14,19,26`（125 字符） | shared-quotes.tsv 3 holders 全为 edge:，无 node: |
| S18 | 28 | 公式 (15.20)：φ₀=1/√(2π)、φ_{2k−1}=cos kx/√π、φ_{2k}=sin kx/√π | 无 | —— | grep 全图 0 命中。但**同一组公式已锚在 15.11.md** 的 `d:normalized-trigonometric-system` a0（198 字符，仅下标字母 n↔k 之差），故属重复，见 §三 |
| S19 | 31 | 这 2n+1 个元素 φ₀…φ_{2n} 张成 2n+1 维子空间 S | 锚 | `apostol:trigonometric-polynomial` a0 | anchors.tsv 168 字符（含 S19+S20） |
| S20 | 31 | S 的元素称为三角多项式 | 锚 | 同上 | 同上 |
| S21 | 33 | 若 f∈C(0,2π)，记 f_n 为 f 在 S 上的投影。于是有 | 边 | `edges-H1.jsonl:13`（contrasts，97 字符） | 该句唯一持有者是一条边，无节点锚 |
| S22 | 36 | 公式 (15.21)：f_n=Σ_{k=0}^{2n}(f,φ_k)φ_k，(f,φ_k)=∫₀^{2π}fφ_k | 锚 | `apostol:fourier-coefficients` a1 | anchors.tsv 157 字符 |
| S23 | 39 | 数 (f,φ_k) 称为 f 的 Fourier 系数 | 锚 | `apostol:fourier-coefficients` a0 | anchors.tsv 69 字符 |
| S24 | 39 | 用 (15.20) 的公式可把 (15.21) 改写成 | 锚 | `d:fourier-coefficients-in-cosine-and-sine-form` a0 | anchors.tsv 65 字符 |
| S25 | 42 | 公式 (15.22)：f_n(x)=½a₀+Σ_{k=1}^{n}(a_k cos kx+b_k sin kx) | 无 | —— | grep 全图 anchors/edges 0 命中。**两个节点的 statement 都明文断言它**（`d:fourier-coefficients-in-cosine-and-sine-form`「改写成经典形式 (15.22)」、`apostol:trigonometric-polynomial`「具体地是 (1/2)a_0+…」），见 §二 |
| S26 | 45 | where（连接词） | 无 | —— | 见 §三 |
| S27 | 48 | 公式：a_k=(1/π)∫₀^{2π}f cos kx、b_k=(1/π)∫₀^{2π}f sin kx | 锚 | `d:fourier-coefficients-in-cosine-and-sine-form` a1 | anchors.tsv 134 字符 |
| S28 | 51 | 对 k=0,1,2,…,n（指标范围） | 无 | —— | 见 §三 |
| S29 | 51 | 逼近定理告诉我们 (15.22) 的三角多项式比 S 中任何其它三角多项式更好地逼近 f，即 ‖f−f_n‖ 尽可能小 | 锚（截断） | `apostol:trigonometric-polynomial-approximation` a2（146 字符） | 锚点在「…trigonometric polynomial in S」处**截断**，未含「in the sense that the norm ‖f−f_n‖ is as small as possible.」——而该节点 statement 恰断言「making ‖f−f_n‖ as small as possible」。**D 型**，见 §五 |
| S30 | 53 | EXAMPLE 2. 用次数 ≤n 的多项式逼近 [−1,1] 上的连续函数 | 锚 | `apostol:legendre-polynomial-approximation` a0 | anchors.tsv 97 字符 |
| S31 | 53 | 取 V=C(−1,1)，并令 (f,g)=∫_{−1}^{1} fg | 边 | `edges-A2-s3.jsonl:30`（72 字符）+ `:31`（86 字符） | 两半均只在边上，无节点锚 |
| S32 | 53 | 这 n+1 个规范化 Legendre 多项式（见 15.13 节）张成 n+1 维子空间 S，即全体次数 ≤n 的多项式 | 锚+边 | `apostol:legendre-polynomial-approximation` a1（89 字符）+ `edges-A2-bridge.jsonl:4`（135 字符） | shared-quotes.tsv 该 89 字符引文 4 holders |
| S33 | 53 | 若 f∈C(−1,1)，记 f_n 为 f 在 S 上的投影 | 无 | —— | grep 全图 0 命中；见 §二 |
| S34 | 53 | Then we have（连接词） | 无 | —— | 见 §三 |
| S35 | 56 | 公式：f_n=Σ_{k=0}^{n}(f,φ_k)φ_k，(f,φ_k)=∫_{−1}^{1}f(t)φ_k(t)dt | 无 | —— | **grep 全图 anchors.tsv + edges.tsv 0 命中**，而 `apostol:legendre-polynomial-approximation` statement 明文断言该式，且 exposure.tsv 显示其 T3=0（无 part-of 后代），T1 三条 + T2 八条均不含该式 → 该断言在四层池中零证据。见 §二 |
| S36 | 59 | 这就是使 ‖f−f_n‖ 最小的次数 ≤n 的多项式 | 锚 | `apostol:legendre-polynomial-approximation` a2 | shared-quotes.tsv 88 字符 **6 holders**（见 §五 抽查 1） |
| S37 | 59 | 例如当 f(x)=sin πx 时，系数 (f,φ_k) 由下式给出 | 边 | `edges-A2-s3.jsonl:38`（is-a，85 字符） | 唯一持有者是一条边 |
| S38 | 62 | 公式 (f,φ_k)=∫_{−1}^{1} sin πt·φ_k(t)dt | 边 | `edges-A2-s3.jsonl:39`（70 字符） | 唯一持有者是一条边 |
| S39 | 65 | 特别地有 (f,φ₀)=0 | 无 | —— | grep 全图 0 命中，而 `apostol:best-linear-approximation-to-sine` statement 断言「(f, phi_0) = 0」。见 §二 |
| S40 | 68 | 公式 (f,φ₁)=∫_{−1}^{1}√(3/2)·t·sin πt dt=√(3/2)·(2/π) | 无 | —— | grep 全图 0 命中，而同一 statement 断言「(f, phi_1) = sqrt(3/2) * 2/pi」。见 §二 |
| S41 | 71 | 故 [−1,1] 上距 sin πt 最近的线性多项式 f₁(t) 是 | 锚 | `apostol:best-linear-approximation-to-sine` a0 | anchors.tsv 91 字符 |
| S42 | 74 | 公式 f₁(t)=√(3/2)(2/π)φ₁(t)=(3/π)t | 锚 | `apostol:best-linear-approximation-to-sine` a1 | anchors.tsv 86 字符 |
| S43 | 77 | 因 (f,φ₂)=0，这也是最近的二次逼近 | 锚 | `apostol:best-linear-approximation-to-sine` a2 | anchors.tsv 78 字符 |

小计：锚 26（S1–S5,S7–S9,S11–S13,S15,S16,S19,S20,S22–S24,S27,S29,S30,S32,S36,S41–S43）、
边 5（S17,S21,S31,S37,S38）、散 2（S6,S10）、无 10（S14,S18,S25,S26,S28,S33,S34,S35,S39,S40）。
33 = 26+5+2。

## 二 未覆盖且值得补建（建议 id / node_type / statement / 锚点逐字 + 实测字符数）

**本节需要新建的节点：0 个。** 5 处真实缺口全部是「已有节点的 statement 已断言、但四层池里无逐字证据」，
即**补锚点（FIX）**而非补节点。下面每条给出建议挂载的**已有** id、要补的锚点逐字文本、以及我自己实测的字符数。

实测方法：`python` 读 `source/apostol-ch15/15.15.md` 原文，用 `q in src` 判子串、`len(q)` 计长度、
`'\n' in q` 判跨行、`src.count(q)` 判唯一性。下表 5 条全部 **FOUND / count=1 / 不跨行 / 30–200 区间内**。

### FIX-1（最严重，死因 6 候选）例 2 的投影公式在全图零证据
- 挂载到已有节点：`apostol:legendre-polynomial-approximation`（`data/nodes-A2-s3.jsonl:6`）作第 4 条锚点
- 缺口性质：该节点 statement 明文写「the projection f_n = sum_{k=0}^{n} (f, phi_k) phi_k」，
  但 `exposure.tsv` 显示 T1=3 / T2=8 / **T3=0**（`partof.tsv` 中它不是任何行的 parent），
  逐条核过 3 条锚点与 8 条出边，**无一条含该式**；全图 grep 亦 0 命中。
- 锚点逐字（**实测 155 字符**，不跨行，源第 56 行，全文唯一）：
```
f _ {n} = \sum_ {k = 0} ^ {n} (f, \varphi_ {k}) \varphi_ {k}, \quad \text { where } \quad (f, \varphi_ {k}) = \int_ {- 1} ^ {1} f (t) \varphi_ {k} (t) d t.
```
- 死因编号：6（引文不支撑断言——statement 的核心公式子句无任何证据）
- 建议动作：**FIX**（补锚点）。同时建议**进 SPEC**：本例说明「节点存在且 N/N 锚点已验证」与
  「statement 各子句均有证据」是两件事，应把「statement 逐子句证据覆盖」列为独立判据。
- 置信度：高（机器可复核：grep + exposure.tsv 三层全查）。不确定点：无。
- 非对称性佐证：例 1 的同位公式 (15.21) **有**锚点（`apostol:fourier-coefficients` a1，157 字符），
  例 2 的同位公式没有。两例结构相同，处理不同，说明这是漏做而非有意设计。

### FIX-2 例 2 的投影定义句无锚
- 挂载到已有节点：`apostol:legendre-polynomial-approximation` 作第 5 条锚点
- 锚点逐字（**实测 66 字符**，不跨行，源第 53 行，全文唯一）：
```
If $f \in C(-1, 1)$ , let $f_{n}$ denote the projection of f on S.
```
- 死因编号：0（仅需 FIX）。建议动作：**FIX**。置信度：高。
- 备选（若要连带覆盖 S34 的「Then we have」）：**实测 79 字符**，同样 FOUND / count=1：
  `If $f \in C(-1, 1)$ , let $f_{n}$ denote the projection of f on S. Then we have`

### FIX-3 sin πx 算例的两个系数值无锚（(f,φ₀)=0）
- 挂载到已有节点：`apostol:best-linear-approximation-to-sine`（`data/nodes-A2-s3.jsonl:7`）作第 4 条锚点
- 缺口性质：statement 断言「(f, phi_0) = 0 and (f, phi_1) = sqrt(3/2) * 2/pi」，
  但其 3 条锚点分别落在第 71、74、77 行，**都不含这两个系数的来源**；exposure.tsv T3=0。
- 锚点逐字（**实测 46 字符**，不跨行，源第 65 行，全文唯一）：
```
In particular, we have $(f,\varphi_0) = 0$ and
```
- 死因编号：6（同 FIX-1，statement 的数值子句无证据）。建议动作：**FIX**。置信度：高。

### FIX-4 sin πx 算例的 (f,φ₁) 计算式无锚
- 挂载到已有节点：`apostol:best-linear-approximation-to-sine` 作第 5 条锚点
- 锚点逐字（**实测 114 字符**，不跨行，源第 68 行，全文唯一）：
```
(f, \varphi_ {1}) = \int_ {- 1} ^ {1} \sqrt {\frac {3}{2}} t \sin \pi t d t = \sqrt {\frac {3}{2}} \frac {2}{\pi}.
```
- 死因编号：6。建议动作：**FIX**。置信度：高。
- 附带结论（与 `丢弃与未覆盖清单.md` §三「模型自算数值」相关）：√(3/2)·(2/π) 与 (3/π)t
  **都在源文里逐字出现**，故这两个值**不是**自算数值，无需登记进该表。核过第 68、74 行原文。

### FIX-5 经典 Fourier 级数形式 (15.22) 无锚
- 挂载到已有节点：`d:fourier-coefficients-in-cosine-and-sine-form`（`data/nodes-D4.jsonl:54`）作第 3 条锚点
- 缺口性质：该节点 statement 断言「rewrites the whole projection formula (15.21) into the classical form (15.22)」，
  其 2 条锚点为第 39 行散文与第 48 行 a_k/b_k 定义式，**(15.22) 本体从未入图**；
  `apostol:trigonometric-polynomial` 的 statement 也断言「具体地是 (1/2)a_0 + sum(a_k cos kx + b_k sin kx)」，
  而它只有 1 条锚点（第 31 行，168 字符）、T3=0，同样不含该式。**一处缺口影响两个节点。**
- 锚点逐字（**实测 117 字符**，不跨行，源第 42 行，全文唯一。含 `\tag{15.22}` 以便与正文引用对齐）：
```
f _ {n} (x) = \frac {1}{2} a _ {0} + \sum_ {k = 1} ^ {n} \left(a _ {k} \cos k x + b _ {k} \sin k x\right),\tag{15.22}
```
- 若不愿收 `\tag`，去掉后**实测 106 字符**，同样 FOUND / count=1。
- 死因编号：6。建议动作：**FIX**（建议同时补给上述两个节点）。置信度：高。

## 三 未覆盖但不值得建（一行一条 + 理由：过渡叙述 / 重复 / 纯记号）

- **S14**（第 23 行）「This completes the proof.」——证明收束句，纯语法切分，`丢弃与未覆盖清单.md` §五已确立
  「证明的纯语法切分（开场句、收束句）」为审计 B 的 KILL 类型（6 例），故不建。建议动作：**不进 SPEC**（已有先例）。
- **S18**（第 28 行）公式 (15.20) φ₀=1/√(2π)、φ_{2k−1}=cos kx/√π、φ_{2k}=sin kx/√π——**重复**。
  同一组公式已锚在 `15.11.md` 的 `d:normalized-trigonometric-system` a0（198 字符），仅下标字母 n↔k 之差；
  源文本身写「In Section 15.11 we exhibited」，即 Apostol 在此是回引。建议动作：**保留现状**。
  （若追求形式完整，可给 `apostol:trigonometric-polynomial` 补一条第 28 行的锚点，实测 163 字符可用：
  `\varphi_ {0} (x) = \frac {1}{\sqrt {2 \pi}}, \quad \varphi_ {2 k - 1} (x) = ...`——但这不是缺口，优先级最低。）
- **S26**（第 45 行）「where」——纯连接词，5 字符，低于 SPEC 30 下界，无独立数学内容。
- **S28**（第 51 行）「for $k = 0, 1, 2, \ldots, n$ .」——纯指标范围记号，30 字符边界附近但无独立断言，
  且已被 S27 的 a_k/b_k 定义式在语义上吸收。
- **S34**（第 53 行）「Then we have」——纯连接词。若采用 FIX-2 的 79 字符备选锚点即连带覆盖，无需单列。
- **S6**（第 6 行）公式 ‖x−s‖≤‖x−t‖ 与 **S10**（第 14 行）公式 x−t=(x−s)+(s−t)——
  **不是内容缺口而是 SPEC 结构性约束**：两式分别只有 28、26 字符，均低于 SPEC 锚点 30 字符下界，
  而硬禁令 2 要求引文不跨行，故二者**无法合法地单独成锚**。二者的散文等价句（S4、S11）与
  节点 statement（`apostol:theorem-15-16-approximation-theorem`、`d:x-minus-t-splits-orthogonally`）均已覆盖其内容。
  建议动作：**进 SPEC** 记一条例外——「当唯一逐字载体是短于 30 字符的独立公式行时，允许 statement
  以同段散文锚点承担，并在节点上标注 `anchor_short_display: true`」，否则这类式子将永久无法入图。
  置信度：高（字符数为实测）。不确定点：SPEC 是否愿意开这个例外，属编排者裁量。

## 四 锚点集合与 sections 集合的不一致（若有）

**存在不一致，但方向是单侧的，且大部分可解释。**

- 集合 A（`anchors.tsv` 中 file=15.15.md 的锚点所属节点，去重）：**14 个**
- 集合 B（`nodes.tsv` 中 sections 含 15.15 的节点，去重）：**25 个**
- **A \ B = 空**。即凡在 15.15.md 有锚点的节点，其 sections 都标了 15.15。这一侧干净。
- **B \ A = 11 个**，逐个归类：

| id | 锚点 file | 判定 |
|---|---|---|
| `apostol:projection-on-a-subspace` | 15.14.md | **合法**。sections=15.14,15.15，概念定义在 15.14，statement 末句「Theorem 15.16 (in Section 15.15) shows it solves the approximation problem」是跨节引用，故标 15.15 但锚点在 15.14 |
| `d:projection-is-the-s-component-of-the-orthogonal-decomposition` | 15.14.md | **合法**。sections=15.14,15.15，是上一条的 part-of 子节点；其出边 `edges-D4.jsonl:107` 的 evidence 确实取自 15.15.md 第 3 行（T2） |
| `d:strict-minimality-rests-on-positivity-of-the-norm` | 15.10.md | **合法**。sections=15.10,15.15；断言「严格性来自范数的正定性」，正定性定义在 15.10，用在 15.15。锚点合理地留在 15.10 |
| `L2:read-off-a-coefficient-by-one-pairing` 等 8 个 L2 节点 | 无锚点 | **合法**。`tmp_index/README.md` 已载明 L2 层 24 个节点全部无锚点（设计如此，L2 靠 members 挂载），其 sections 由成员节点并集而来 |

结论：**不一致不构成缺陷**。11 个 B\A 节点里 8 个是无锚点的 L2 层（设计使然），
3 个是跨节概念（锚点合法地留在定义所在节，通过 sections 多值或边 evidence 与 15.15 相连）。
建议动作：**保留**。置信度：高。

**但有一处需要提醒综合代理的口径问题**：若用「sections 含 15.15」统计本节节点数会得 25，
用「在 15.15.md 有锚点」会得 14，二者差 11。报本节规模时必须说明用的是哪一个口径，
否则会重演 546 与 522 那类总数混淆。此外 14 个之中 6 个属 `d:` 层（D4 文件），
按 `丢弃与未覆盖清单.md` §六，`data/nodes-D4.jsonl` 是 A、B 两判都过了的文件，
但 `data/nodes-A2-s3.jsonl`（本节 7 个 `apostol:` 节点的出处）**不在该表的任何一行**——
既没列为已审，也没列为未覆盖。这意味着本节主干 7 个节点的审计状态在清单里是空白。见「顺带发现」。

## 五 本节现有锚点抽查（挑 3 条，按盲区五型 A/B/C/D/E 逐型检查，写出结论）

抽查 3 条锚点。每条按 A（变量/对象错）/ B（借用引文）/ C（把端点当中间步骤）/
D（覆盖范围不足）/ E（整句完全无锚）五型逐型判。

### 抽查 1：`apostol:best-approximation-in-a-finite-dimensional-subspace` a1（88 字符）
引文：`This is the polynomial of degree $\leq n$ for which the norm $\| f - f_n\|$ is smallest.`（源第 59 行）
statement：「本节的逼近问题：给定 Euclid 空间 V 中的 x 与有限维子空间 S，求 S 中使 ‖x−t‖ 最小的元素……」

- **A 型：命中。** 断言的对象是一般的 x、t、S；引文的对象是 f、f_n、「次数 ≤n 的多项式」——
  例 2 的具体实例，无一个是断言里的量。以特例句支撑一般性问题陈述。
- **B 型：命中。** `shared-quotes.tsv` 该 88 字符引文 **6 holders**：
  `node:apostol:best-approximation-in-a-finite-dimensional-subspace`、
  `node:apostol:legendre-polynomial-approximation`、
  以及 `edges-A2-s3.jsonl:9,29,35,40`。该句是**例 2 的原生句**（S36），
  对 `legendre-polynomial-approximation` 成立，对本节点是借来的。
- **C 型：不命中。** 引文本身不是「拿终点冒充中间步」，它就是一句结论。
- **D 型：命中（另一方向）。** statement 还含一段实质论断——「内积结构不是陈述该问题的前提，
  该问题在任何赋范线性空间中都有意义，但正是内积结构使投影成为解、并使勾股等号论证可用」。
  该节点 T1=2 / T2=4 / **T3=0**（`exposure.tsv`，`distinct_src_lines=2`），两条锚点为节标题与本句，
  4 条出边 evidence 亦取自这两行——**「在任何赋范线性空间中都有意义」这一断言在池中零证据**，
  且它是一个数学论断（可争议：范数不必来自内积），不是原文内容。疑为模型自加而未标 origin。
- **E 型：不命中**（节点有锚）。
- **结论**：a1 是 A+B 型双命中的借用锚点；a0（节标题，102 字符）才是支撑一般性问题陈述的正确锚点。
  另发现 statement 有一段无证据的自加论断。
- **依据**：`anchors.tsv`（该节点 2 行）、`shared-quotes.tsv` 88 字符行、`exposure.tsv` 该节点行、
  `statements.tsv` 该节点行。
- **死因编号**：6（a1 引文不支撑断言）＋疑似 3（statement 末段自加论断未标 origin，**待综合代理复核**，我未查 origin 字段原文）
- **建议动作**：**FIX**——撤掉 a1，或把 a1 换成定理 15.16 的一般性表述句
  （如源第 3 行「Then the projection of x on S is nearer to x than any other element of S.」73 字符，
  但该句已 3 holders，会新增一次 B 型碰撞；更干净的做法是只留 a0）。
  statement 末段建议独立提案裁决，不在本轮范围内。
- **置信度**：A/B 型高（`shared-quotes.tsv` 机器可复核）；statement 自加论断一项中，
  因我未读 `data/nodes-A2-s3.jsonl:2` 的 origin 字段原文（本轮只读索引），**这一项标为未核**。

### 抽查 2：`apostol:trigonometric-polynomial-approximation` a2（146 字符）
引文：`The approximation theorem tells us that the trigonometric polynomial in (15.22) approximates f better than any other trigonometric polynomial in S`（源第 51 行）
statement 末句：「Rewritten with (15.20) it is the truncated Fourier series (15.22).」，
statement 中段：「…is the trigonometric polynomial of order n making ‖f − f_n‖ as small as possible.」

- **A 型：不命中。** 对象一致（f、S、(15.22) 的三角多项式）。
- **B 型：命中但无害。** `shared-quotes.tsv` 3 holders：本节点 + `edges-A2-s3.jsonl:21,28`，
  两条边的 src 都是本节点自己（T2），非跨节点借用。
- **C 型：不命中。**
- **D 型：命中，这是本条的实质问题。** 引文在「…trigonometric polynomial in S」**截断**，
  丢掉了原句尾「, in the sense that the norm $\|f - f_{n}\|$ is as small as possible.」；
  而 statement 恰以「making ‖f − f_n‖ as small as possible」作为核心断言。
  即断言里的**范数最小性**这一半，在锚点里被切掉了。另：statement 断言的 (15.22) 本体
  在全图无锚（见 §二 FIX-5），故「Rewritten with (15.20) it is (15.22)」也缺逐字支撑。
- **E 型：不命中。**
- **结论**：D 型（覆盖范围不足）。引文支撑「比其它三角多项式更好」，不支撑「范数尽可能小」这一等价改写。
  按 SPEC 这属可修复的记账不足，不是数学错误。
- **依据**：`anchors.tsv` 该节点 a2（146 字符）与 `source-lines.tsv` 第 51 行（charlen=246，nsent=2）之差；
  `statements.tsv` 该节点行。
- **死因编号**：6。**建议动作**：**FIX**——把 a2 延长至含句尾（源第 51 行该句完整版）。
  我未实测延长后的字符数（需含「, in the sense that…as small as possible.」，
  由 146 加约 68 得约 214，**可能超出 SPEC 200 上界**，故建议改为**补一条独立锚点**只取句尾从
  「in the sense that」起的片段。具体字符数**未核**——应由执行方实测。
- **置信度**：D 型判定高（截断处在索引里逐字可见）；修法的字符数一项未核。

### 抽查 3：`d:approximation-proof-starts-from-the-orthogonal-decomposition` a1（93 字符）
引文：`Let S be a finite-dimensional subspace of a Euclidean space V, and let x be any element of V.`（源第 3 行，定理**假设**）
statement：「第一步是引用定理 15.15 写出 x = s + s⊥……这一引用是有限维性假设到达定理 15.16 的一条通道；
另一条是命题自身的短语『x 在 S 上的投影』，因为该概念在 15.14 中只对有有限正交基的子空间定义。」

- **A 型：不命中。** 对象一致（S、V、x）。
- **B 型：命中。** `shared-quotes.tsv` 该 93 字符引文 **5 holders**：本节点 +
  `edges-A2-s3.jsonl:1,2,3,6`。四条边的 src 均为 `apostol:theorem-15-16-approximation-theorem`，
  即该句的**原生持有者是定理节点**（它是定理的假设句），本节点（一个 proof 步骤节点）是借用。
- **C 型：不命中**，但有近亲问题：引文是定理的**假设**，节点讲的是**证明的第一步**。
  引文位置在证明之前而非证明之内。严格说这不是「端点冒充中间步」（C 型的定义），
  而是「上下文句冒充步骤句」——五型未收此形。
- **D 型：不命中（罕见地）。** statement 的核心断言「引用定理 15.15 写出 x=s+s⊥」由 a0
  （99 字符，源第 11 行）逐字支撑；而 statement 里那句实质论断——「有限维性经由两条通道到达定理 15.16，
  其中一条是『投影』这一概念本身在 15.14 只对有有限正交基的子空间定义」——正是靠 a1 这条假设句
  加上跨节的 15.14 定义才立得住。**在这个特定断言下，a1 是恰当的**：断言讲的就是假设如何传导。
- **E 型：不命中。**
- **结论**：**该锚点成立，判「保留」。** B 型碰撞属合法一类（`丢弃与未覆盖清单.md` 已确立
  「概念节点 + 记号节点共用定义句」等合法共享形态；此处是「证明步骤节点引用它所依赖的定理假设」，
  同属合法）。这条抽查的价值在于给出一个**阴性对照**：B 型碰撞本身不构成缺陷，
  必须读 statement 才能判，机器清单只能给候选。
- **依据**：`anchors.tsv` 该节点 2 行、`shared-quotes.tsv` 93 字符行、`statements.tsv` 该节点行。
- **死因编号**：0。**建议动作**：**保留**。**置信度**：中高。
  不确定点：a1 的「上下文句 vs 步骤句」错位在五型里无对应编号，若综合代理认为该形态需独立编号，
  本条应改判为该新型的实例；我不擅自新造分型。

## 顺带发现

- `data/nodes-A2-s3.jsonl` 未出现在 `丢弃与未覆盖清单.md` §六的任何一行——既未列已审也未列未覆盖，本节 7 个主干节点的审计状态在清单里是空白，建议补进该表。
- 源第 17 行有 OCR 缺陷：`$x - s = s^{-} \in S^{\perp}$` 应为 `s^{\perp}`；`d:x-minus-t-splits-orthogonally` a0 与 3 条边逐字复制了该错，行为正确（不得改源），但建议进 §二「已知 OCR 缺陷」表登记。
- `d:fourier-coefficients-in-cosine-and-sine-form` 的 statement 含一个模型自行推出的换算关系「(f, phi_{2k-1}) = sqrt(pi) a_k, 且 k=0 项贡献 a_0/2」，源文未给该换算，疑似死因 3（未标 origin），我未查其 origin 字段，未核。
- `apostol:best-approximation-in-a-finite-dimensional-subspace` 的 statement 断言「该问题在任何赋范线性空间中都有意义」，池中零证据且属可争议的数学论断，见 §五 抽查 1，建议单独裁决。
- `edges-X.jsonl:112`（`theorem-15-16` generalizes `strang:projection-onto-subspace-formula`，origin=model 但带 131 字符 quote）属 README 所列 15 条未校验模型引文之一；我已核该 quote 在 15.15.md 第 3 行逐字存在，引文本身无误，但 generalizes 方向是否成立未核。
- 本节 43 句中有 5 句（S17、S21、S31、S37、S38）**唯一持有者是边**，无任何节点锚点；例 2 的内积定义（S31）即在其中，属 T2 塌陷、记账位置问题而非内容缺失。
