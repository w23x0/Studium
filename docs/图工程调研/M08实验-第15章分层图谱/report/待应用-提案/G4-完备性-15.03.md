# 15.03 覆盖完备性

审查范围：仅 `source/apostol-ch15/15.03.md` 一节的覆盖完备性。全部判据取自
`tmp_index/` 的 anchors.tsv / nodes.tsv / edges.tsv / shared-quotes.tsv / source-lines.tsv /
statements.tsv / pool.tsv，未重算。源文件逐字核对用 `grep -cF`。

## 〇 结论速览

本节 **36 句** / 已覆盖 **35**（节点锚点 34 + 仅边 evidence 1）/ 下移 **0** / 未覆盖 **1**；
其中值得补建 **0** 个。

一句话结论：**15.03 的覆盖完备性没有实质缺口**，唯一未被任何 quote 命中的是 S12 的
独立公式行 `(f + g) (x) = f (x) + g (x)`，而它的内容已由 S11 与 S13 的散文锚点承载
（`apostol:function-space` anchors[0]、`d:function-space-addition-is-pointwise`），
且该行实测仅 27 字符（source-lines.tsv `apostol-ch15/15.03.md` 第 16 行 charlen=27），
**低于 SPEC 锚点下界 30**，物理上无法单独作锚。故本节不提任何新建节点。

真正的问题不在完备性而在**记账正确性**，两处（详见四、五节）：
1 `d:linear-space-is-a-set-together-with-two-operations` 有一条 15.03.md 的锚点，
  但它的 sections 只写 `15.01,15.02` —— sections 漏登 15.03。
2 `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 的 sections 声明
  `15.03,15.08`，但两条锚点全在 15.08.md，15.03 侧零锚。

覆盖率口径声明：本节按**句**切分统计，未按行号。source-lines.tsv 显示 15.03.md
19 个非空行里 13 行的 n_sentence_end ≥ 2（第 3/5/7/9/11/19/21/23/25/27/29/31/33/35/37 行），
最高 4 句/行，按行号比对会严重高报——这正是过报通道 b，已避开。

## 一 逐句覆盖表

句号按**句**编，不按行。括号内为源文件行号（`source/apostol-ch15/15.03.md`）。
标题行 L1 `## 15.3 Examples of linear spaces` 不计句。
覆盖者列的字符数一律取 anchors.tsv / edges.tsv 的 charlen 实测列。

| 句号 | 句子摘要（行号） | 覆盖状态 | 覆盖者 | 判据 |
|---|---|---|---|---|
| S1 | 给出 V 与两种运算即得一个具体线性空间的例子（L3） | 节点锚点 | `d:an-example-is-a-set-plus-two-explicit-operations`[0]、`d:linear-space-is-a-set-together-with-two-operations`[1] | anchors.tsv，138 字符，逐字全句 |
| S2 | 读者可自行验证以下各例满足实线性空间全部公理（L3） | 节点锚点 | `d:examples-are-asserted-to-satisfy-the-axioms-and-left-to-the-reader`[0] | anchors.tsv，114 字符 |
| S3 | 例 1：V=R，加法与数乘为普通实数运算（L5） | 节点锚点 | `d:example-1-the-real-numbers-form-a-linear-space`[0] | anchors.tsv，144 字符，含 "EXAMPLE 1." 全句 |
| S4 | 例 2：V=C，加法为复数加法，ax 为复数乘实数（L7） | 节点锚点（两段拼合） | `d:example-2-the-complex-numbers-with-real-scalars`[0]（111）+ `d:operations-of-example-2-complex-addition-and-real-scaling`[0]（139） | anchors.tsv；前段止于 "complex numbers"，后段承接至句末，合并无缺口 |
| S5 | 元素是复数但标量是实数，故仍是实线性空间（L7） | 节点锚点 | `d:real-in-real-linear-space-refers-to-the-scalars`[1]、`d:example-2-shows-the-scalars-decide-real-or-complex`[0] | anchors.tsv，108 字符 |
| S6 | 例 3：V=V_n，按分量定义运算（L9） | 节点锚点 | `apostol:v-n-space`[0]、`d:example-3-v-n-with-componentwise-operations`[0] | anchors.tsv，171 字符，逐字全句 |
| S7 | 例 4：V_n 中与给定非零向量 N 正交的全部向量（L11） | 节点锚点 | `d:example-4-vectors-orthogonal-to-a-fixed-nonzero-vector`[0] | anchors.tsv，93 字符 |
| S8 | n=2 时是过 O 且以 N 为法向的直线（L11） | 节点锚点 | `d:example-4-is-a-line-or-a-plane-through-the-origin`[0] | anchors.tsv，133 字符，一条锚点含 S8+S9 两句 |
| S9 | n=3 时是过 O 且以 N 为法向的平面（L11） | 节点锚点 | 同上（同一条 133 字符锚点） | 同上 |
| S10 | 以下诸例称为函数空间（L13） | 节点锚点 | `apostol:function-space`[0] | anchors.tsv，160 字符，含 S10+S11 |
| S11 | V 的元素是实值函数，两函数之和按常规定义（L13） | 节点锚点 | `apostol:function-space`[0] 后半 + `d:function-space-addition-is-pointwise`[0]（109） | anchors.tsv；109 字符锚点即 S11 全句 |
| S12 | 独立公式行 `(f + g) (x) = f (x) + g (x)`（L16） | **未覆盖** | 无 | anchors.tsv / edges.tsv 中 `grep -c "f + g"` 与 `grep -c "g) (x)"` 均为 0；source-lines.tsv L16 charlen=27 |
| S13 | 上式对 f、g 定义域交集中的每个实 x 成立（L19） | 节点锚点 | `d:function-space-addition-lives-on-the-intersection-of-domains`[0] | anchors.tsv，63 字符 |
| S14 | af 是在 f 定义域每点取值 af(x) 的函数（L19） | 节点锚点 | `d:function-space-scalar-multiple-is-pointwise`[0]、`apostol:function-space`[1] | anchors.tsv，146 字符 |
| S15 | 零元是处处取零值的函数（L19） | 节点锚点 | `d:function-space-zero-is-the-everywhere-zero-function`[0] | anchors.tsv，66 字符 |
| S16 | 读者可自行验证以下各集是函数空间（L19） | 节点锚点 | `d:examples-are-asserted-to-satisfy-the-axioms-and-left-to-the-reader`[1] | anchors.tsv，81 字符 |
| S17 | 例 5：给定区间上全部函数（L21） | 节点锚点 | `d:example-5-all-functions-on-a-given-interval`[0] | anchors.tsv，64 字符 |
| S18 | 例 6：全部多项式（L23） | 节点锚点 | `d:example-6-all-polynomials-as-a-function-space`[0] | anchors.tsv，38 字符 |
| S19 | 例 7：次数 ≤ n 的全部多项式，n 固定（L25） | 节点锚点 | `apostol:polynomials-of-degree-at-most-n`[0]、`d:example-7-polynomials-of-degree-at-most-n`[0] | anchors.tsv，76 字符 |
| S20 | 约定该集合含零多项式（L25，括号句） | 节点锚点 | `d:zero-polynomial-is-included-by-convention`[0]、`apostol:polynomials-of-degree-at-most-n`[1] | anchors.tsv，91 字符，含括号逐字 |
| S21 | 次数恰为 n 的多项式集不是线性空间，因闭包公理不满足（L25） | 节点锚点 | `d:degree-exactly-n-is-not-a-linear-space`[0] 等 7 个节点共用 | anchors.tsv，115 字符；shared-quotes.tsv 该 quote n_holders=16 |
| S22 | 例如两个 n 次多项式之和不必是 n 次（L25） | 节点锚点 | `d:degree-exactly-n-fails-axiom-1-with-an-explicit-witness`[0]、`apostol:polynomials-of-degree-exactly-n`[1] | anchors.tsv，75 字符 |
| S23 | 例 8：给定区间上全部连续函数（L27） | 节点锚点 | `d:example-8-continuous-functions-on-an-interval`[0] | anchors.tsv，67 字符 |
| S24 | 区间为 [a,b] 时记该空间为 C(a,b)（L27） | 节点锚点 | `d:notation-c-a-b-for-continuous-functions-on-a-b`[0] | anchors.tsv，65 字符 |
| S25 | 例 9：在给定点可微的全部函数（L29） | 节点锚点 | `d:example-9-functions-differentiable-at-a-given-point`[0] | anchors.tsv，68 字符 |
| S26 | 例 10：在给定区间上可积的全部函数（L31） | 节点锚点 | `d:example-10-functions-integrable-on-an-interval`[0] | anchors.tsv，68 字符 |
| S27 | 例 11：在 1 处有定义且 f(1)=0 的全部函数（L33） | 节点锚点 | `d:example-11-functions-vanishing-at-a-point`[0] | anchors.tsv，67 字符 |
| S28 | 本例中数 0 是本质的（L33） | 节点锚点 | `d:example-11-a-nonzero-prescribed-value-breaks-closure`[0] | anchors.tsv，112 字符，含 S28+S29 |
| S29 | 若把 0 换成非零 c 就违反闭包公理（L33） | 节点锚点 + 边 | 同上 112 字符锚点；另 `edges-D1.jsonl:207/208` 用 69 字符单句 | anchors.tsv + edges.tsv |
| S30 | 例 12：齐次线性微分方程 y''+ay'+by=0 的全部解（L35） | 节点锚点 | `apostol:homogeneous-de-solution-space`[0]、`d:example-12-solutions-of-a-homogeneous-second-order-equation`[0] | anchors.tsv，140 字符 |
| S31 | 此处 0 同样是本质的（L35） | 节点锚点 | `d:example-12-nonhomogeneous-solutions-break-closure`[0] | anchors.tsv，126 字符，含 S31+S32 |
| S32 | 非齐次方程的解集不满足闭包公理（L35） | 节点锚点 | 同上；另 `apostol:homogeneous-de-solution-space`[1]、`x:solution-set-of-a-homogeneous-linear-condition`[0] 用 99 字符单句 | anchors.tsv |
| S33 | 这些例子说明线性空间概念渗透代数、几何与分析（L37） | 节点锚点 | `d:linear-space-concept-permeates-algebra-geometry-analysis`[0]、`d:one-proof-from-the-axioms-serves-every-example`[1] | anchors.tsv，113 字符 |
| S34 | 从公理推出的定理一举对每个具体例子都有效（L37） | 节点锚点 | `d:one-proof-from-the-axioms-serves-every-example`[0]、`x:vectors-need-not-be-columns`[0] | anchors.tsv，128 字符 |
| S35 | 如此统一诸例使我们对每一例都获得更深洞察（L37） | **仅边 evidence** | `data/edges-D1.jsonl:219`（`d:knowledge-of-one-example-guides-work-in-the-others` --contrasts--> `d:one-proof-from-the-axioms-serves-every-example`） | edges.tsv ev_charlen=76；anchors.tsv 全表 `grep -c unifying` = 0，无任何节点以此句为锚 |
| S36 | 对某一例的专门知识可预示或解释别例的结果，揭示本会漏掉的联系（L37） | 节点锚点 | `d:knowledge-of-one-example-guides-work-in-the-others`[0] | anchors.tsv，182 字符 |

S35 的性质说明：这是 T2 记账（内容在图里，只挂在边上），按四层证据池定义
`d:knowledge-of-one-example-guides-work-in-the-others` 作为该边的 **src**，此 evidence 入其 T2 池。
不是缺口，属记账位置差异，**不建议动**。

## 二 未覆盖且值得补建

**无。本节 0 个新建提案。**

这不是省事，是判断结论：唯一未覆盖项 S12 是一条公式行，它的数学内容已被 S11/S13/S14
的散文锚点完整承载，且它自身长度不足 SPEC 下界（见下节）。为它建节点只会制造一个
statement 与 `d:function-space-addition-is-pointwise` 同义的重复节点——那正是审计 B
判死 28 个 D 层节点时用的死因 5（伪抽象/层塌缩，"parent 的 statement 已逐字含有该句"），
见 `report/丢弃与未覆盖清单.md` 第五节。

结论 / 依据：anchors.tsv + edges.tsv 全表检索 15.03.md 得 38 条不同锚点 quote、
39 条不同边 evidence quote，覆盖 36 句中 35 句 / 死因 0（无死因）/ 建议动作 **不进 SPEC，不新建** /
置信度 高。不确定点：无。

## 三 未覆盖但不值得建

- **S12**（L16 公式 `(f + g) (x) = f (x) + g (x)`）—— 纯记号：内容已由 S11 散文句
  （`d:function-space-addition-is-pointwise`，109 字符锚点）与 `apostol:function-space`
  的 statement（`with (f + g)(x) = f(x) + g(x)`，statements.tsv 已逐字含该式）承载；
  且该行实测 27 字符（source-lines.tsv `apostol-ch15/15.03.md` L16 charlen=27；我另用
  `printf | wc -m` 复核得 27），**低于锚点下界 30**，无法为它构造合法锚点。
  死因 0 / 建议动作 **不建** / 置信度 高。

顺带记下一条口径：若日后有代理想为 S12 建节点，唯一合法的锚点构造是把 L13 的
`with addition of two functions f and g defined in the usual way:` 与公式行拼接——
但那会跨行，违反硬禁令 2（quote 不得跨行）。所以 S12 在当前 SPEC 下是**结构性不可锚**的，
不是漏掉的。这也意味着任何按「源行是否被 quote 命中」算的机器覆盖率，在 15.03 上
天花板是 18/19 行而非 19/19，不应被当作缺陷计入。

## 四 锚点集合与 sections 集合的不一致

**两个集合确实对不上，共 4 处不一致，逐条报出。**

集合定义与规模：
- A = anchors.tsv 中 file 列为 `15.03.md` 的锚点所属节点，去重后 **52** 个。
- S = nodes.tsv 中 sections 列含 `15.03` 的节点，去重后 **54** 个。
- A∩S = 51；A\S = 1；S\A = 3。

### A\S：有 15.03 锚点，但 sections 未登记 15.03（1 处，建议 FIX）

| 节点 | loc | 现 sections | 15.03 锚点 |
|---|---|---|---|
| `d:linear-space-is-a-set-together-with-two-operations` | `data/nodes-D1.jsonl:1` | `15.01,15.02` | anchors[1]，15.03.md，138 字符，即 S1 全句 |

结论：sections 漏登。该节点 anchors[0] 在 15.01.md（150 字符），anchors[1] 在 15.03.md，
但 sections 只写了 `15.01,15.02` —— 15.02 反而没有对应锚点，15.03 有锚点却没登记。
依据：nodes.tsv 该行 sections 字段 + anchors.tsv 该节点两行。
死因 **0**（记账错，非内容错）/ 建议动作 **FIX：sections 改为 `15.01,15.02,15.03`** /
置信度 高。不确定点：`15.02` 是否另有依据（例如 statement 呼应 15.02 的公理表述）我未核，
所以建议是**追加** 15.03 而非重写整个字段。

### S\A：sections 声明 15.03，但本节零锚点（3 处，2 处合法 / 1 处建议 FIX）

| 节点 | loc | 判定 |
|---|---|---|
| `L2:closure-certificate-for-subspacehood` | `data/structures-L2-a.jsonl:1` | **合法**。L2 层节点设计上不带锚点（README 已列 24 个无锚 L2 节点，本节点在列），sections 由 16 个 members 汇总而来。不动。 |
| `L2:one-independent-constraint-costs-exactly-one-dimension` | `data/structures-L2-b.jsonl:3` | **合法**。同上，10 个 members。不动。 |
| `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` | `data/nodes-H2.jsonl:10` | **建议 FIX**，见下。 |

第三条详述：该节点 sections 写 `15.03,15.08`，`origin=model`，但两条锚点
（anchors.tsv）**全在 15.08.md**：100 字符的 "EXAMPLE 3. The space of solutions … has dimension 2."
与 79 字符的 "One basis consists of the two functions …"。15.03 侧一条锚点都没有。
它声明 15.03 的合理动机是可推的——15.03 例 12（S30）正是齐次二阶方程解集的引入处——
但**动机不等于锚点**，当前状态下"该节点覆盖 15.03"这一说法在索引里无据。
死因 **0**（记账/出处登记问题；注意它不是死因 3 虚构出处，因为 statement 本身就明说
"A borrowing with no citation attached"，是**自陈**无出处）/ 建议动作 **FIX，二选一**：
(a) 补一条 15.03.md 的锚点，可用 S30 的 140 字符句
`EXAMPLE 12. The set of all solutions of a homogeneous linear differential equation $y'' + ay' + by = 0$ , where a and b are given constants.`
（charlen 取 edges.tsv/anchors.tsv 实测 140，在 30–200 界内）；或
(b) 把 sections 收缩为 `15.08`。
我倾向 (a)，因为该节点断言的正是"15.08 用了 15.03 引入的那个解空间却没证维数"，
两节都点到才说得通。/ 置信度 中高。不确定点：H2 层是否有"sections 可只按语义归属登记、
不要求每节都有锚点"的独立约定，我未核 H2 的提案说明，若有该约定则本条应改判为合法。

## 五 本节现有锚点抽查

抽查 3 条，每条按 A/B/C/D/E 五型逐型走。三条锚点的引文我都用 `grep -cF` 对
`source/apostol-ch15/15.03.md` 逐字核过，均返回 1（未篡改）——但下面两条仍有 D/E 型问题，
这正是"引文未被篡改 ≠ 引文支撑断言"的本节实例。

### 抽查 1：`d:function-space-addition-lives-on-the-intersection-of-domains`

锚点：[0] 15.03.md 63 字符 `for every real x in the intersection of the domains of f and g.`；
[1] 15.03.md 68 字符 `EXAMPLE 9. The set of all functions differentiable at a given point.`
statement（statements.tsv）：逐点和只对 f、g 定义域交集中的 x 有定义，故 f+g 的定义域
可能比任一加项更小；除非所考虑的函数集共享一个固定定义域，这种收缩会把 f+g 挤出集合
并破坏 Axiom 1，**这就是例 5、8、9、10、11 各自预先固定一个区间或一个点的原因**。

- A（变量/对象错）：**通过**。f、g、x 的角色与原文一致，交集方向没写反。
- B（借用引文）：**通过但需知情**。shared-quotes.tsv：63 字符句 n_holders=3
  （本节点 + `edges-D1.jsonl:140/141`，都是本节点自发的边，属自持不属借用）；
  68 字符句 n_holders=3，与 `d:example-9-functions-differentiable-at-a-given-point` 共享。
  后者是例 9 的本主，本节点借它当"五例之一的样本"，用途不同但不构成误配。
- C（端点当中间步）：**通过**。不涉及证明链。
- D（覆盖范围不足）：**不通过。这是本条的实质问题。** statement 点名了
  **例 5、8、9、10、11 五个**例子，锚点只给了**例 9 一个**。另外四例
  （S17 例 5 64 字符、S23 例 8 67 字符、S26 例 10 68 字符、S27 例 11 67 字符）
  在源文里都存在、都已被别的节点锚定，但本节点一条都没挂。即"五例都预先固定定义域"
  这个全称成分只有 1/5 的锚点支撑。按盲区分型这是 D 型（机器查不出来），
  按死因编号最接近 **1（量词过度断言）**——注意四层证据池规则明示死因 1
  **不因边证据而免除**，而本节点的 T2 池（edges-D1.jsonl:140/141）恰恰只是同一条 63 字符句的重复，
  没有新引入任何源行，冗余度增量为 0。
- E（整句完全无锚）：**部分不通过**。"break Axiom 1" 这一句在 15.03.md 里**没有出处**——
  原文只说取定义域交集，从未提 Axiom 1 会被破坏（Axiom 1 定义在 15.02）。
  这是模型的正确推理，但在本节点上无锚。对比 `d:example-4-nontrivial-check-is-closure-by-linearity-of-the-dot-product`
  的做法（它给了 15.03.md 82 字符 + **15.02.md 160 字符 Axiom 1 原文**两条锚），
  本节点缺的正是那第二条。

判定：结论 / 依据 anchors.tsv 本节点 2 行 + statements.tsv 本节点行 + shared-quotes.tsv
63/68 字符两行 / 死因 **1**（量词过度断言，D 型；附带 E 型的 Axiom 1 无锚）/
建议动作 **FIX，不 KILL**：补挂例 5、8、10、11 的四条既有锚点（64/67/68/67 字符，
均在 30–200 界内，charlen 取 anchors.tsv 实测），并补 15.02.md 的 Axiom 1 原文锚
（160 字符，`d:example-4-nontrivial-check-…` 已在用，可直接复用同一 quote）/
置信度 高（D 型判定基于 statement 与锚点集合的直接比对，不需要额外推断）。
不确定点：补挂后是否与 `apostol:function-space` 的 T3 池产生新的 B 型碰撞，我未核。

### 抽查 2：`d:degree-exactly-n-has-no-zero-element`

锚点：唯一一条，15.03.md 115 字符
`The set of all polynomials of degree equal to n is not a linear space because the closure axioms are not satisfied.`
statement：pointwise 加法下 O 的唯一候选是零多项式，而它被"次数恰为 n"排除，
故 **Axiom 5 无见证、Axiom 6 失去目标**；该集合因此不仅闭包失败——那是 Apostol 给的理由——
**也违反加法公理**。

- A：**通过**。零多项式确实不属于"次数恰为 n"的集合，n≥1 时无疑；数学本身无误。
- B：**不通过（形式上）**。shared-quotes.tsv 显示这条 115 字符 quote 是**全节最热的引文，
  n_holders=16**：7 个节点（`apostol:polynomials-of-degree-exactly-n`、
  `d:degree-exactly-n-is-not-a-linear-space`、`d:degree-exactly-n-fails-axiom-2-at-the-scalar-zero`、
  `d:degree-exactly-n-has-no-zero-element`、`d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n`、
  `d:example-6-all-polynomials-as-a-function-space`、`d:for-function-spaces-closure-is-the-only-real-content`）
  加 9 条边。本节点是这 16 个持有者里**唯一只有这一条锚点的节点**（n_anchors=1），
  也就是说它的全部证据就是一句被另外 15 处共用的话。
- C：**通过**。不涉及证明链的端点/中间步混淆。
- D：**不通过**。statement 有三个独立成分：(i) 零多项式是 O 的唯一候选；(ii) Axiom 5 无见证、
  Axiom 6 失去目标；(iii) Apostol 给的理由是闭包。锚点只支撑 (iii)。(i) 与 (ii) 零覆盖。
- E：**不通过，且这是最重的一条**。引文里**根本不出现** "zero"、"Axiom 5"、"Axiom 6"，
  只出现 "closure axioms"。而 statement 的主断言恰恰是"闭包之外还差零元"——
  它明确地说"不只是 Apostol 给的那个理由"。**引文说的是断言明确声称自己超出的那部分。**
  这是 E 型（整句完全无锚）叠加死因 6（引文不支撑断言）的干净样本。

判定：结论 / 依据 anchors.tsv 本节点 1 行 + statements.tsv 本节点行 +
shared-quotes.tsv 115 字符行（n_holders=16）/ 死因 **6**（引文不支撑断言；SPEC 尚未收录）/
建议动作 **FIX，不 KILL**——数学是对的（A 型通过），缺的是出处：补 15.02.md 的
Axiom 5 与 Axiom 6 原文各一条锚点（我未逐字取出这两条 quote，故不写字符数，**未核**），
并把"零多项式是 O 的唯一候选"这一步的 origin 标为 model（现 nodes.tsv 该节点 origin 字段为空）/
置信度 高。不确定点：15.02.md 中 Axiom 5/6 的原文断句能否切出 30–200 字符的单行 quote，
我未核 15.02.md（超出本次范围）。

### 抽查 3：`d:example-4-is-a-line-or-a-plane-through-the-origin`

锚点：唯一一条，15.03.md 133 字符
`If n = 2, this linear space is a line through O with N as a normal vector. If n = 3, it is a plane through O with N as normal vector.`
statement：n=2 时是过 O 以 N 为法向的直线，n=3 时是过 O 以 N 为法向的平面；
**两者都必然过 O，这是"线性空间必含零元"这一事实的几何面相**。

- A：**通过**。n=2 对直线、n=3 对平面，与原文一一对应，没有互换。
- B：**通过**。shared-quotes.tsv 该 quote n_holders=2，另一持有者是本节点自发的
  `edges-D1.jsonl:136`（part-of 指向 `d:example-4-vectors-orthogonal-to-a-fixed-nonzero-vector`），
  属自持，非借用。全节最干净的一条。
- C：**通过**。
- D：**通过**。锚点一句同时覆盖 S8、S9 两个 case，statement 的前两个成分被逐字全覆盖，
  n=2/n=3 两种情形都在引文内，无遗漏 case。
- E：**边界性不通过，但不严重**。第三个成分"必然过 O ／ 线性空间必含零元"在 15.03.md 无锚——
  原文只说 "a line through O"，把它归因到零元公理是模型的解读（该归因本身正确）。
  与抽查 2 的差别在于：这里引文里出现了 "through O"，**断言是对引文的解释而非超出**，
  属可接受的最小推断；抽查 2 是引文明确不涉及的另一件事。

判定：结论 / 依据 anchors.tsv 本节点 1 行 + shared-quotes.tsv 133 字符行（n_holders=2）+
statements.tsv 本节点行 / 死因 **0** / 建议动作 **保留**；可选 FIX：若要与
`d:example-4-nontrivial-check-…` 的规范对齐，可补一条 15.02.md Axiom 5（零元）的锚点，
使"必然过 O"的归因落地——但这是加分项，不是缺陷 / 置信度 高。

### 抽查小结（仅限这 3 条，不外推）

3 条里 1 条干净（抽查 3）、2 条有实质问题（抽查 1 死因 1、抽查 2 死因 6）。
两处问题都**不是** A 型（数学错），都是 D/E 型——即 statement 的成分数多于锚点能支撑的成分数。
共同修法也相同：**把断言引用到的 15.02 公理原文补成锚点**。
这两条都是机器校验通过（origin=source，check_graph 逐字核过）却仍不支撑断言的例子。
本节另有一条独立事实值得记账：unverified-model-quotes.tsv 中 **0 条**涉及 15.03.md
（`grep -c "15.03" ` = 0），即 README 提到的 15 条未核 model quote 边**不落在本节**，
本节的引文校验盲区完全来自 D/E 型，不来自工具跳过。

## 顺带发现

- 全节 115 字符那句（"degree equal to n is not a linear space…"）被 16 个持有者共用，是本节 B 型风险的最大聚集点，7 个节点里有 4 个把它当唯一或主要锚点，值得单独一轮 form B 复核。
- `d:example-4-nontrivial-check-is-closure-by-linearity-of-the-dot-product` 是本节唯一同时锚 15.03 与 15.02 公理原文的节点，可当作"断言引用公理就补公理锚点"的样板写进 SPEC。
- source-lines.tsv 显示 15.03.md 有 15 行 n_sentence_end ≥ 2、最高 4 句/行，若全图有按行号计覆盖率的表，本节会被显著高报。
- nodes.tsv 的 origin 列 513 个节点为空、仅 24 个 model / 9 个 source，本节 D 层节点全部为空值，含模型推断成分的 statement（如抽查 1、2）无法从该列区分。
- `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 的 statement 自陈"无引文出处"，这类自陈式无出处节点是否该单独立一类标记，超出本轮范围。
