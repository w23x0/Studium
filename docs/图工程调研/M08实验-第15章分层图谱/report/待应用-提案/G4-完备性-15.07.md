# 15.07 覆盖完备性

## 〇 结论速览（本节 45 句 / 已覆盖 37 / 下移 0 / 未覆盖 8；其中值得补建 0 个）

按**句**切分（不按行）：正文 S2–S46 共 45 句，其中 5 句是整块公式（每块记 1 句）。
- 已覆盖 37 句：节点锚点直接覆盖 36 句，**仅由边 evidence 覆盖 1 句**（S18，函数空间的取定）。
- 下移到 part-of 后代 0 句。本节 part-of 结构是「L1 概念 ← D2 切片」自上而下切分，
  没有出现「父节点断言只靠后代锚点支撑」的 T3 情形（查 partof.tsv 与 pool.tsv）。
- 未覆盖 8 句：叙述句 4 句（S7、S16、S17、S19、S21 中 S16/S17 为过渡），
  公式块 3 句（S25=(15.1)、S30=指数函数定义式、S34=(15.2)；对照 (15.3) 有锚）。
- **值得新建节点的：0 个**。8 句里 2 句是过渡叙述、3 句是纯公式记号、
  3 句（S7、S19、S21）的**断言**已被现有节点的 statement 承载，缺的是**锚点**不是节点。
  所以本节的缺口类型是「FIX 加锚」，不是「补建节点」——这一点与 A8 的框架不同，须让裁决者注意。
- 关键发现：S19（Example 5 里 u₁=cos²t、u₂=sin²t、u₃=1 的定义句）与
  S21（Example 6 里 u_k(t)=t^k 的定义句）**全图无任何锚点或边引用**
  （grep anchors.tsv / edges.tsv 全表，`u_{1}(t)`、`u_{k}(t)` 命中 0）。
  这使两个节点的 statement 里的 cos²/sin²/t^k 这些**对象名**在图内无出处支撑，属盲区 A/D 型。
  置信度高（机械可证：两条 grep 全表零命中）。

## 一 逐句覆盖表

句号按语义句排（源行号在括号里）。**不按行号比对**：line 15/17/19/27/29/31/37/45/51/57/61
的 n_sentence_end 均 ≥2（source-lines.tsv），按行会高报（过报通道 b）。

| 句 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 1 | 节标题 | 不计 | — | 标题非断言 |
| S2 | 3 | DEFINITION：S 依赖 ⟺ 存在有限个互异元与不全为零的标量使组合为 O | 已覆盖 | apostol:dependent-set#0/#1；d:dependence-finiteness…、…distinctness…、…coefficients-not-all-zero | anchors.tsv 四条锚点覆盖该句三个子句 |
| S3 | 6 | 公式 Σc_i x_i = O | 已覆盖 | d:dependence-target-is-the-zero-element#0（40 字符） | anchors.tsv |
| S4 | 9 | S 独立 ⟺ 它不依赖 | 已覆盖 | apostol:independent-set#0（57 字符） | anchors.tsv |
| S5 | 9 | In this case, for all choices of… | 已覆盖 | d:independence-universal-quantifier-over-choices#0（115）；x:linear-independence#0（173） | anchors.tsv |
| S6 | 12 | 公式 Σc_i x_i = O implies c_1=…=c_k=0 | 已覆盖 | d:independence-vanishing-combination-forces-vanishing-coefficients#0（106） | anchors.tsv |
| S7 | 15 | 依赖/独立是集合的性质，但也用于元素本身 | **未覆盖** | — | grep -F "properties of sets"、"apply these terms" 全表 0 命中 |
| S8 | 15 | 例如独立集里的元素叫独立元素 | 已覆盖 | apostol:independent-set#1（80） | anchors.tsv |
| S9 | 17 | 若 S 有限，本定义与第 12 章 V_n 的定义一致 | 已覆盖 | apostol:ext-chapter-12-finite-dependence-definition#0（107） | anchors.tsv；边 edges-H2:13 亦用它 |
| S10 | 17 | 但本定义不限于有限集 | 已覆盖 | apostol:independent-set#2、d:dependence-definition-not-restricted-to-finite-sets#0（65） | anchors.tsv |
| S11 | 19 | EXAMPLE 1：T⊂S 依赖 ⟹ S 依赖 | 已覆盖 | apostol:dependence-inherited-by-supersets#0（77） | anchors.tsv |
| S12 | 19 | 等价说法：独立集的每个子集独立 | 已覆盖 | apostol:dependence-inherited-by-supersets#1（101） | anchors.tsv |
| S13 | 21 | EXAMPLE 2：某元是另一元的标量倍 ⟹ S 依赖 | 已覆盖 | d:dependence-from-one-element-being-a-scalar-multiple-of-another#0（84） | anchors.tsv |
| S14 | 23 | EXAMPLE 3：O∈S ⟹ S 依赖 | 已覆盖 | apostol:zero-element-forces-dependence#0、x:zero-element-destroys-independence#0（48） | anchors.tsv |
| S15 | 25 | EXAMPLE 4：空集独立 | 已覆盖 | d:independence-of-the-empty-set#0、x:zero-element-destroys-independence#1（40） | anchors.tsv |
| S16 | 27 | 第 12 章已讨论过 V_n 中大量依赖/独立的例子 | **未覆盖** | — | grep -F "Many examples of dependent" 全表 0 命中 |
| S17 | 27 | 下面的例子在函数空间中说明这些概念 | **未覆盖** | — | grep -F "following examples illustrate" 全表 0 命中 |
| S18 | 27 | 每个例子里 V 都取实轴上全体实值函数 | 已覆盖（仅边） | 边 edges-D2.jsonl:34、:47 的 evidence（108 字符） | edges.tsv；无任何节点以它为锚点 |
| S19 | 29 | EXAMPLE 5：令 u₁=cos²t、u₂=sin²t、u₃=1（对一切实 t） | **未覆盖** | — | grep -F "u_{1}(t)" anchors.tsv/edges.tsv 均 0 命中 |
| S20 | 29 | 毕达哥拉斯恒等式给出 u₁+u₂−u₃=O，故三函数依赖 | 已覆盖 | d:dependence-witness-must-be-exhibited-not-merely-counted#0（125） | anchors.tsv |
| S21 | 31 | EXAMPLE 6：令 u_k(t)=t^k，k=0,1,2,…，t 实 | **未覆盖** | — | grep -F "u_{k}(t)" 全表 0 命中；该节点锚点从下一句起 |
| S22 | 31 | 集合 S={u₀,u₁,u₂,…} 独立 | 已覆盖 | d:independence-of-an-infinite-set-reduces-to-its-finite-subsets#0（138） | anchors.tsv |
| S23 | 31 | 只需证对每个 n，n+1 个多项式独立 | 已覆盖 | 上锚点（截断）+ 边 edges-D2 evidence（123，整句） | anchors.tsv + edges.tsv |
| S24 | 31 | Σc_k u_k = O 形式的关系意味着 | 已覆盖 | d:zero-element-of-a-function-space-is-a-pointwise-identity#0（56） | anchors.tsv |
| S25 | 34+37 | 公式 (15.1) Σc_k t^k = 0，及其尾句「for all real t」 | **未覆盖** | — | grep "tag{15.1}"、"c _ {k} t \^ {k}" 全表 0 命中 |
| S26 | 37 | t=0 给出 c₀=0 | 已覆盖 | d:powers-independence-by-differentiating-and-evaluating-at-zero#0（175） | anchors.tsv |
| S27 | 37 | 微分 (15.1) 后取 t=0 得 c₁=0 | 已覆盖 | 同上锚点 | anchors.tsv |
| S28 | 37 | 重复此过程，每个 c_k 为零 | 已覆盖 | 同上锚点 | anchors.tsv |
| S29 | 39 | EXAMPLE 7：a₁…a_n 互异实数时，n 个指数函数 | 已覆盖 | apostol:independence-of-exponential-functions#0、d:exponential-independence-hypothesis-distinct-exponents#0（89） | anchors.tsv |
| S30 | 42 | 公式 u₁(x)=e^{a₁x},…,u_n(x)=e^{a_nx} | **未覆盖** | — | grep "e \^ {a _ {1} x}" 全表 0 命中 |
| S31 | 45 | 「are independent」+ 用对 n 归纳证明 | 已覆盖 | apostol:independence-of-exponential-functions#1（98）、d:exponential-independence-proof-is-induction-on-n#0（165） | anchors.tsv |
| S32 | 45 | n=1 时平凡成立 | 已覆盖 | d:exponential-independence-base-case-n-equals-one#0（41） | anchors.tsv |
| S33 | 45 | 假设 n−1 情形成立，考虑标量 c₁…c_n 使 | 已覆盖 | 165 字符锚点（尾部记号 `$c_{1},\ldots ,c_{n}$ such that` 未入锚） | anchors.tsv |
| S34 | 48 | 公式 (15.2) Σc_k e^{a_k x}=0 | **未覆盖** | — | grep "tag{15.2}"、"mathbf {1}" 全表 0 命中；对照 (15.3) 有锚 |
| S35 | 51 | 令 a_M 为 n 个数中最大者 | 已覆盖 | d:exponential-independence-select-the-largest-exponent#0（67） | anchors.tsv |
| S36 | 51 | 用 e^{−a_M x} 乘 (15.2) 两端 | 已覆盖 | d:exponential-independence-multiply-by-a-normalizing-exponential#0（62） | anchors.tsv |
| S37 | 54 | 公式 (15.3) | 已覆盖 | d:exponential-law-used-in-the-shift#0（70） | anchors.tsv |
| S38 | 57 | k≠M 时 a_k−a_M 为负 | 已覆盖 | d:exponential-independence-negativity-of-the-shifted-exponents#0（51） | anchors.tsv |
| S39 | 57 | x→+∞ 时 k≠M 各项趋零，得 c_M=0 | 已覆盖 | d:exponential-independence-limit-argument-isolates-one-coefficient#0（120） | anchors.tsv |
| S40 | 57 | 删去第 M 项并用归纳假设，余 n−1 个系数为零 | 已覆盖 | d:exponential-independence-delete-the-killed-term-and-recurse#0（146） | anchors.tsv |
| S41 | 59 | THEOREM 15.5 前提：S 是 V 中 k 元独立集 | 已覆盖 | apostol:theorem-15-5#0（86）、d:thm-15-5-hypothesis…#0（72） | anchors.tsv |
| S42 | 59 | L(S) 是 S 张成的子空间 | 已覆盖 | apostol:linear-span-notation-l-of-s#1（44） | anchors.tsv |
| S43 | 59 | 结论：L(S) 中任意 k+1 元集依赖 | 已覆盖 | apostol:theorem-15-5#1（58）、d:thm-15-5-conclusion…#0（103）、d:n-plus-one-elements…#0（58） | anchors.tsv |
| S44 | 61 | Proof：V=V_n 时 15.5 归约为 12.8 | 已覆盖 | d:thm-15-5-proof-delegated-to-theorem-12-8#0、apostol:ext-theorem-12-8#0（56） | anchors.tsv |
| S45 | 61 | 检视 12.8 的证明，它只用到 V_n 是线性空间 | 已覆盖 | d:thm-15-5-generality-transfer…#0（122）、d:dimension-theory-uses-no-metric-notions#0（107） | anchors.tsv |
| S46 | 61 | 故 12.8 的证明对任何线性空间 V 有效 | 已覆盖 | apostol:ext-theorem-12-8#1（78） | anchors.tsv |

合计 45 句（S2–S46，S1 标题不计）：已覆盖 37（其中 S18 仅由边覆盖）、下移 0、未覆盖 8
（S7、S16、S17、S19、S21、S25、S30、S34）。

## 二 未覆盖且值得补建

**结论：0 个**。本节没有「教材有内容而图里完全没有该断言」的缺口。8 句未覆盖里，
没有一句携带图中不存在的数学断言。因此不提新建节点，改为提三条**加锚 FIX**——
它们是同一缺口的正确修法（缺的是出处，不是节点）。

死因编号：**0（无死因，仅需 FIX）**。建议动作：**FIX（加锚点，不新建节点）**。
下列 quote 均已在源文件逐字实测（Python `in` 判定 + 定位行号），不跨行、无换行。

| # | 目标节点 | 补锚 quote（逐字） | 实测字符数 | 源行 | 理由 |
|---|---|---|---|---|---|
| F1 | `d:dependence-witness-must-be-exhibited-not-merely-counted` | `EXAMPLE 5. Let $u_{1}(t) = \cos^{2} t$ , $u_{2}(t) = \sin^{2} t$ , $u_{3}(t) = 1$ for all real $t$ .` | **100** | 15.07.md:29 | 该节点 statement 点名「cos squared plus sin squared minus 1」，但唯一锚点（125 字符）只含 u₁+u₂−u₃=O，不含 u 的定义，对象名在图内无出处 |
| F2 | `d:powers-independence-by-differentiating-and-evaluating-at-zero` | `EXAMPLE 6. Let $u_{k}(t) = t^{k}$ for $k = 0, 1, 2, \ldots$ , and $t$ real.` | **75** | 15.07.md:31 | 该节点与 `d:independence-of-an-infinite-set-reduces-to-its-finite-subsets` 的 statement 都以「powers」为对象，但 t^k 的定义句全图无引用 |
| F3 | `d:zero-element-of-a-function-space-is-a-pointwise-identity` | `for all real $t$ . When $t = 0$ , this gives $c_0 = 0$ .` | **56** | 15.07.md:37 | 该节点断言「a relation summing to O is an identity holding for every value of the variable」；现锚点（56 字符，line 31）只到「means that」，「for all real t」这半句才是该断言的出处，它在 line 37 行首 |

均在 SPEC 的 30–200 字符界内。置信度高（字符数与行号都是自测）。
不确定点：F1–F3 是否与其它代理提的锚点改动冲突，我未查其它待应用提案，交裁决者。

## 三 未覆盖但不值得建

- S7（line 15「Although dependence and independence are properties of sets…」）——断言已在
  `apostol:independent-set` 的 statement 里（「The term is also applied to the elements themselves」），
  且下一句 S8 的锚点（80 字符）已是它的具体形式；建节点即为死因 5。**保留，不进 SPEC**。
- S16（line 27「Many examples … were discussed in Chapter 12.」）——过渡叙述兼向第 12 章的指路；
  `apostol:ext-vn-as-a-prior-vector-space` 与 `apostol:ext-chapter-12-finite-dependence-definition`
  已承担外部依赖的记账（各带 15.06/15.03 锚点）。**保留，不进 SPEC**。
- S17（line 27「The following examples illustrate these concepts in function spaces.」）——纯过渡。
- S25 的公式 (15.1)、S34 的公式 (15.2)——纯记号；两者都只在证明步骤的散文句中被指名引用
  （「Differentiating (15.1)」「Multiplying both members of (15.2)」），那些散文句已有锚点。
  注意对照：(15.3) 反而有独立节点 `d:exponential-law-used-in-the-shift`——这是同类公式的
  记账不一致，但我不主张为了一致性去补建 (15.1)(15.2) 节点，只在「顺带发现」记一行。
- S30（line 42 指数函数定义式 u₁(x)=e^{a₁x},…）——纯记号，
  `apostol:independence-of-exponential-functions` 的 statement 已把它写成 u_k(x)=e^{a_k x}。
  唯一顾虑与 F1/F2 同型（对象名无出处），但该节点锚点 #0 的散文句已含「the $n$ exponential functions」，
  比 Example 5/6 的情形轻，判**不必补**。置信度中。

## 四 锚点集合与 sections 集合的不一致

**有不一致，单向。** 集合 A = anchors.tsv 中 file=15.07.md 的持有节点，共 **39** 个；
集合 B = nodes.tsv 中 sections 含 15.07 的节点，共 **47** 个。
A\B = **空**（无「有 15.07 锚点却不声明 15.07」的节点，这一侧干净）。
B\A = **8** 个，即声明覆盖 15.07 却在 15.07.md 无任何锚点：

| 节点 | 实际锚点所在 | 判定 |
|---|---|---|
| `L2:independence-by-killing-all-but-one-coefficient` | 无锚点 | 合法：L2 24 个节点按设计全部无锚点（README 已列） |
| `L2:proof-inheritance-by-auditing-what-the-old-proof-used` | 无锚点 | 同上 |
| `L2:testing-infinite-objects-through-finite-windows` | 无锚点 | 同上 |
| `L2:dimension-as-a-two-sided-count-barrier` | 无锚点 | 同上 |
| `L2:a-basis-locks-the-coordinate-count-not-the-geometry` | 无锚点 | 同上 |
| `apostol:ext-vn-as-a-prior-vector-space` | 15.03.md、15.06.md | **可疑**：sections 写 15.03,15.06,15.07,15.10，但它对 15.07 无锚点也无以 15.07 为 evidence 的自发边（pool.tsv 只有 2 条 T1）。sections 里的 15.07 无凭据 |
| `d:zero-space-declared-finite-dimensional-by-fiat` | 15.08.md、15.06.md | **可疑**：sections 写 15.06,15.07,15.08，池内 4 条证据全在 15.06/15.08。15.07 同样无凭据 |
| `d:exponential-independence-uses-no-inner-product` | 无锚点 | 合法但零证据：origin=model，exposure.tsv 全 0（T1=T2=T3=0），三条自发边均 origin=model 且无 quote。属 README 列的 4 个无锚 d: 节点之一 |

结论：5 个 L2 是设计使然，不算不一致。**真正的不一致是后 2 个**
（`apostol:ext-vn-as-a-prior-vector-space`、`d:zero-space-declared-finite-dependent…-by-fiat`）：
它们把 15.07 写进 sections，但在 15.07 里既无锚点也无出边证据。
依据：nodes.tsv 第 32/60 行左右的 sections 列；anchors.tsv 全表按 node_id grep；pool.tsv 逐条。
死因编号：**0（记账错，仅需 FIX）**——sections 是元数据不是断言，不构成死因 1/3。
建议动作：**FIX**，从这两个节点的 sections 里删去 `15.07`，
或（若提案方本意是「该节点在 15.07 被使用」）补一条以 15.07.md 为 evidence 的边。
（上表末段第二个 id 应为 `d:zero-space-declared-finite-dimensional-by-fiat`。）
置信度中：我只核了「15.07 无锚点/无出边证据」这一事实，未核提案方写入 15.07 的原意。
不确定点：`d:zero-space-declared-finite-dimensional-by-fiat` 的 statement 我未逐字读全，
它是否在论证中实际用到 15.07 的 Theorem 15.5，**未核**。

## 五 本节现有锚点抽查

### 抽查 1 `d:dependence-witness-must-be-exhibited-not-merely-counted`
锚点（anchors.tsv，125 字符，15.07.md:29）：
`The Pythagorean identity shows that $u_{1} + u_{2} - u_{3} = O$ , so the three functions $u_{1}, u_{2}, u_{3}$ are dependent.`
statement：「Example 5 shows the practical shape of a dependence proof: exhibit a single identity
with nonzero coefficients. The Pythagorean identity gives cos squared plus sin squared minus 1
equal to O, so those three functions are dependent.」

- **A 型（变量/对象错）：命中（轻度）**。statement 把断言写成 cos²+sin²−1，
  但锚点只有 u₁+u₂−u₃，**u 到 cos²/sin²/1 的对应关系不在锚点里**，也不在该节点其它证据里
  （exposure.tsv：T1=1、T2=1、T3=0，两条同引这一句）。即 statement 里的对象名在图内无出处。
  这正是 §二 F1 要修的东西。死因 0，动作 FIX。
- **B 型（借用引文）：命中，但合法**。shared-quotes.tsv 该句 n_holders=3：
  本节点、自发边 edges-D2:137、以及边 edges-H1:15
  （`d:dependence-from-one-element-being-a-scalar-multiple-of-another --contrasts--> apostol:independent-set`）。
  第三个持有者用 Example 5 去支撑「标量倍」的对照，**这是可疑的**：Example 5 讲的是
  三函数的恒等式，不是「一元是另一元的标量倍」（Example 2 的内容）。判为 **B 型真命中**，
  该边的 evidence 应换成 line 21 的 Example 2 句（84 字符，已有现成锚点）。死因 6，动作 FIX。
- **C 型（端点当中间步）：未命中**。锚点自身就是结论句，statement 也把它当结论。
- **D 型（覆盖不足）：未命中**。statement 的第一句（「dependence proof 的实操形状」）是元层评论，
  由锚点的「shows that … so … are dependent」形状支撑，范围相称。
- **E 型（整句无锚）：未命中**。

### 抽查 2 `d:dimension-theory-uses-no-metric-notions`
锚点（107 字符，15.07.md:61）：
`it is based only on the fact that $V_{n}$ is a linear space and nc on any other special property of $V_{n}$`
statement：「Theorem 15.5, Theorem 15.6 and Theorem 15.7 use only the linear space axioms,
the finiteness of the counts and the ordering of the integers. No inner product, norm, orthogonality,
distance, coordinates or completeness appears…」

- **D 型（覆盖范围不足）：命中，且是本次抽查里最严重的一处**。锚点只讲 **Theorem 12.8/15.5**
  一处证明「只用到 V_n 是线性空间」；statement 却对 **15.5、15.6、15.7 三条定理**做全称断言，
  并进一步枚举「无内积/范数/正交/距离/坐标/完备性」。池内证据（pool.tsv：T1 1 条、T2 2 条，
  distinct_src_lines=2，exposure.tsv）**全部来自 15.07.md:61 同一句的两个切片**，
  对 15.6 与 15.7 零证据。按四层池规则，T2 塌陷不免除量词过度断言。
  死因 **1（量词过度断言）**。建议动作：**FIX**——把 statement 收缩到 15.5，
  或补 15.08.md 中 15.6/15.7 证明的锚点（我未去 15.08 找，标**未核**）。置信度高。
- **A 型：未命中**（对象是定理编号，锚点里的 V_n 与 statement 一致）。
- **B 型：不算命中**。shared-quotes.tsv 显示 107 字符版 n_holders=2（本节点 + 自发边 edges-D2:183），
  109 字符版（带句末句点）另被 edges-D2:52/:102 两条边持有；同源不同切片，非借用。
- **C 型：轻度**。锚点是 Apostol 对 12.8 证明的**审计结论**，statement 把它当作对整层理论的
  **性质陈述**，中间少了「15.6/15.7 也经过同样审计」这一步。与上面的 D 型是同一处失配的两个面。
- **E 型：未命中**。

### 抽查 3 `d:exponential-independence-uses-no-inner-product`
锚点：**无**。exposure.tsv 全零（后代 0 / T1 0 / T2 0 / T3 0 / 池 0 / 不同源行 0）。
三条自发边（edges-D2:45、:46、:154）origin 均为 model 且 quote 为空。

- **E 型（整句完全无锚）：命中，且是纯粹形态**。statement 的正面部分
  （「uses ordering of the reals, the exponential addition law, nonvanishing of the exponential
  and one limit」）与 Example 7 的实际论证相符，我核过源文 line 51–57，**数学上无误**；
  但整条断言在图内零证据。
- **A/B/C/D 型：不适用**（无引文可错、可借、可错位、可不足）。
- 它已正确标 `origin: model`，未违反禁令 3，**不是死因 3**。
  死因 **0**，动作 **保留 + FIX**：把 line 57 的一句（如
  `If $k \neq M$ , the number $a_k - a_M$ is negative.`，51 字符，已在图中）
  作为其证据来源，或明确接受它是无锚的元层观察。置信度高（全靠索引表机械可证）。

**跨三条的共同结论**：三条锚点在 check_graph 眼里都是「已验证」（第三条根本没有锚点可验），
但按盲区五型逐型看，一条 A+B 命中、一条 D+C 命中（死因 1）、一条 E 命中。
即 15.07 这一节的「N/N 锚点已验证」确实不构成正确性保证。

## 顺带发现

- 15.07.md:61 有 OCR 缺陷「and **nc** on any other special property」（原文应为 not），该错字被 4 条锚点/边逐字继承；`report/丢弃与未覆盖清单.md` 的 OCR 缺陷表未登记这一处。
- 公式记账不一致：(15.3) 有独立节点 `d:exponential-law-used-in-the-shift`，而 (15.1)(15.2) 全图无引用。
- `unverified-model-quotes.tsv` 里的 edges-X.jsonl:113（`apostol:independent-set --generalizes--> strang:linear-independence`，15.07.md，65 字符）的 quote 我已逐字核过，在 line 17 内、不跨行，未被篡改；但它支撑的是「不限于有限集」，与 strang 的独立性定义无直接关系，疑 C 型。
- `d:dependence-from-one-element-being-a-scalar-multiple-of-another` 有一条 contrasts 边（edges-H1:15）用 Example 5 的引文，应换成 Example 2 的引文（详见 §五 抽查 1 B 型）。
- 15.07.md 全节 62 行里 11 行的 n_sentence_end ≥ 2，占有内容行的大半；本节按行号做覆盖统计会显著高报。
