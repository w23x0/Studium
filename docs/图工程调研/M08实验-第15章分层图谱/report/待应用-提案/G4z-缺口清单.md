# 全章覆盖缺口清单

## 〇 结论速览

**缺口共 21 条**（去重后的「值得补建新节点」条目；按严重度 1/2/3/4 各 **2 / 1 / 3 / 15** 条）。

**缺失的节报告：无。** 16 份 `G4-完备性-15.01.md` … `-15.16.md` 全部存在
（`ls` 于 `report/待应用-提案/` 确认），16 节全部计入。

各节贡献的「值得补建节点」数（取自各报告〇速览的自报数，本清单已去重与重分级）：

| 节 | 自报补建数 | 说明 |
| --- | --- | --- |
| 15.01 / 15.02 / 15.03 / 15.04 | 0 / 0 / 0 / 0 | 四节一致：缺的是锚点不是节点 |
| 15.05 | 5 | 全部来自习题（正面例题无人建） |
| 15.06 | 1 | **正文例子**，本清单判严重度 1 |
| 15.07 / 15.08 | 0 / 0 | 只需补锚 |
| 15.09 | 8 | 全部来自习题 |
| 15.10 / 15.11 | 0 / 0 | 只需补锚（15.10 有 3 处受 30 字符下界卡死） |
| 15.12 | 3 | 全部来自习题 |
| 15.13 | 3 | 1 条正文例子（严重度 1）+ 2 条可由 parent 导出 |
| 15.14 | 0 | 无内容缺口 |
| 15.15 | 0 | **任务书前提被推翻，见下** |
| 15.16 | 1 | 习题，但对象全图零提及 |

**三条要先说的判断：**

1 **任务书的严重度 1 示例不成立。** 任务书写「15.15 例 2 的 Legendre 完全无节点」，
  15.15 报告核实为**有两个节点**：`apostol:legendre-polynomial-approximation`
  （`data/nodes-A2-s3.jsonl:6`，3 锚点 8 出边）与 `apostol:best-linear-approximation-to-sine`
  （同文件 :7，3 锚点 3 出边）。我复核 `nodes.tsv`：两 id 均在表内，`grep -c` 各 1 命中。
  **建议动作：撤回该前提**，不要按「补建 15.15 例 2 节点」执行。置信度高。

2 **本章没有「证明步骤缺节点」的缺口。** 16 份报告里所有与证明步骤相关的缺口
  （15.04 S8、15.08 P1–P4、15.13 FIX 1–5、15.14 FIX-1/2）**全部是缺锚点、不是缺节点**。
  故严重度 2 只有 1 条，且它本身是习题命题被现有节点当依据用。

3 **本章缺节点的主体是习题（15 / 21 条）。** 15.05、15.09、15.12、15.16 四节的正面命题
  与正面例题系统性无人建；15.05 报告指出成因是任务分配空档
  （只有反例代理 CX2 扫过，它只建「不是线性空间」的节点）。这不是逐节疏漏，是分工缺口。


## 一 严重度 1（整个例子或定理完全无节点）

判据：整段正文例子在图里没有任何节点承载，且内容不能由现有节点导出。

### S1-1 多项式空间的三组张成集（15.06 正文，例子整体无节点）

- **内容**：次数 ≤ n 的多项式空间同时被 `{1, t, …, tⁿ}`、`{1, t/2, t²/3, …, tⁿ/(n+1)}`、
  `{1, (1+t), …, (1+t)ⁿ}` 三组集合张成。三组元素个数都恰为 n+1，即「张成集不唯一」
  与「元素个数是否变化」无关。
- **源**：`source/apostol-ch15/15.06.md:21`（97 字符可锚）、`:24`（公式块 36 字符）、
  `:27`（128 字符可锚）。来源节：**15.06**（唯一来源，无重复上报）。
- **建议 id**：`d:polynomial-space-has-several-spanning-sets`，node_type `example`，
  part-of → `apostol:spanning-set`。
- **为何最重**：三个句子在全图无任何持有者。我独立复核：
  `grep -c "It is also spanned"` 在 `anchors.tsv` / `edges.tsv` / `statements.tsv` **均 0**；
  `grep -c "(1 + t)\|(1+t)"` 三表**亦均 0**。即第二、第三组张成集这两个数学对象**全图零提及**。
  第一组 `{1, t, …, tⁿ}` 有近邻节点 `d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one`，
  但它 sections=15.08、parent=`apostol:dimension`，**不是** `apostol:spanning-set` 的
  part-of 后代（`exposure.tsv`：`apostol:spanning-set` 的 n_partof_desc=3、T3=7，后代不含它），
  故也不构成过报通道 a 的下移免责。
- **死因编号**：0（缺口，非错误）。**建议动作**：进 SPEC（新建节点）。
- **置信度**：高（三条 grep 全表零命中，机械可复核）。
  **不确定点**：15.06 报告自陈「不确定点 1」——是否会被裁为与 15.08 那个维数节点重复。
  我的判断是不重复：那个节点断言的是**维数 = n+1**，本条断言的是**张成集不唯一**，
  且第二、三组集合在它的 statement 与锚点里都不存在。

### S1-2 例 1：V₄ 中的完整 Gram-Schmidt 计算（15.13 正文，例子整体无节点）

- **内容**：对 V₄ 中 x₁=(1,−1,1,−1)、x₂=(5,1,1,1)、x₃=(−3,−3,1,−3) 施行 Gram-Schmidt，
  得 y₁=(1,−1,1,−1)、y₂=(4,2,0,2)、y₃=(0,0,0,0)。y₃=O 证明三向量相依，y₁,y₂ 非零证明
  x₁,x₂ 独立，故 L(x₁,x₂,x₃) 是 2 维，归一化后 {½(1,−1,1,−1), (1/√6)(2,1,0,1)} 是正交基。
  这是「Gram-Schmidt 过程同时是独立性检验」的唯一算例。
- **源**：`source/apostol-ch15/15.13.md:111`（181 字符）、`:114`（156 字符）、`:117`（156 字符）。
  来源节：**15.13**（唯一来源）。
- **建议 id**：`apostol:ex-gram-schmidt-in-v4-detects-a-dependence`，node_type `example`，
  part-of → `apostol:gram-schmidt-process`。
- **为何最重**：整个算例的三个结论（y₃=O、dim=2、标准正交基）在图里都无持有者。我复核：
  `grep -c "5, 1, 1, 1\|5,1,1,1"` 与 `grep -c "sqrt {6}"` 在 `anchors.tsv` / `edges.tsv` /
  `statements.tsv` **六项全 0**。`grep "dimension 2"` 有命中，但持有者是
  `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space`（锚点在 **15.08.md**）
  与 `d:dim-of-a-second-order-de-solution-space-is-two`——是**二阶 ODE 解空间**的维数 2，
  与本例的 V₄ 无关。即「dim=2」这个字面命中是假阳性，不构成覆盖。
  `nodes.tsv` 中 sections 含 15.13 的 38 个非 L2 节点里，无一个是这个例子。
- **死因编号**：0。**建议动作**：进 SPEC（新建节点）。
- **置信度**：高（三组 grep 全表零命中；「dimension 2」的假阳性已定位到持有者）。
  **不确定点**：15.13 报告称「全图目前 0 个 example 类型节点」。我复核 `nodes.tsv` 的
  node_type 分布为 concept 258 / theorem 173 / method 69 / notation 22 / 空 24（L2 层），
  **确实没有 example 类型**。封闭词表允许 `example`，但本图从未用过——
  是否首次引入 example 类型由主控裁；若不引入，退化为 `concept` 不影响锚点与 statement。
  这一点同时影响 S1-1 与下面 S3-1、S3-2、S4-* 的多条提案。


## 二 严重度 2（证明步骤缺失，导致 requires 链断裂）

判据：某个已有节点的断言把该内容当依据在用，而该内容在图里没有独立节点，
故 requires 链上少一环。**本章只有 1 条**——所有真正的「证明步骤缺失」都是缺锚点，
已由 15.04 / 15.08 / 15.13 / 15.14 四份报告以 FIX 形式提出，不属节点缺口，不列入本清单。

### S2-1 线性包算子保序（15.09 习题 22(d)）

- **内容**：S ⊆ T ⊆ V 蕴涵 L(S) ⊆ L(T)，即线性包算子 L 保序。
- **源**：`source/apostol-ch15/15.09-exercises.md:45`，锚点 65 字符（15.09 报告实测）。
  来源节：**15.09**（提案 5，唯一来源）。
- **建议 id**：`apostol:span-is-monotone-in-the-generating-set`，node_type `concept`，
  `requires` → `apostol:linear-span`。
- **它会断哪条 requires 链**：现有节点
  `apostol:cx-span-does-not-commute-with-intersection` 的 statement 里
  「L 是保序的但不与交相容」这句话**以保序为前提**，而这个前提在图里没有任何节点，
  也没有任何锚点。即该反例节点的一半依据悬空——它断言「保序成立但交换失败」，
  只有失败那一半有出处。补上本节点后，那条 statement 才能连出一条
  `requires` → 本节点 的实边。
- **死因编号**：0（缺口）。**建议动作**：进 SPEC（新建节点）。
- **置信度**：中高。**不确定点**：本条的源是**习题**，按任务书判据 4 本应算最轻；
  我按判据优先级（1→2→3→4，先判 2）把它提到严重度 2，理由是它同时满足判据 2
  且已被现有节点当依据在用。若主控认为「习题来源」应压过「链断裂」，此条降为严重度 4，
  在建议顺序里的位置不变（它仍应先于依赖它的反例节点的 FIX）。
  另：15.09 报告称 ex22(a)(b)(c)(d) 合起来是在证「L 是闭包算子（扩张、保序、幂等）」，
  其中 (a) 判重复不建、(b) 已由 `x:span-of-a-set` 承载、(c) 见 S3-3。
  **我未核** (a)(b) 的重复判定是否成立，只转述。


## 三 严重度 3（概念缺失但内容已被别节的节点间接覆盖）

判据：概念本身在图里无节点，但其内容可由现有节点（通常是 parent）机械导出，
或已被别节的节点以另一形式断言。这一档的共同风险是**死因 5（伪抽象·层塌缩）**——
建了可能过不了删除测试。

### S3-1 Legendre 正交多项式 y₂–y₅ 的显式表（15.13 正文）

- **内容**：对 xₙ(t)=tⁿ 在 (−1,1) 上作 Gram-Schmidt，前六个正交多项式为
  y₀=1、y₁=t、y₂=t²−1/3、y₃=t³−(3/5)t、y₄=t⁴−(6/7)t²+3/35、y₅=t⁵−(10/9)t³+(5/21)t。
- **源**：`15.13.md:141`（177 字符）、`:147`（181 字符）。来源节：**15.13**（提案 A2）。
- **建议 id**：`apostol:legendre-first-six-orthogonalized-polynomials`，
  part-of → `apostol:legendre-polynomials`（该 parent 存在，`nodes.tsv`：
  `data/nodes-A2-s1.jsonl:10`，concept，3 锚点）。
- **为何是 3 而非 1**：parent `apostol:legendre-polynomials` 存在且有 3 条锚点，
  加上 `apostol:gram-schmidt-process` 的公式，y₂–y₅ 原则上可由「对 tⁿ 施行过程」导出，
  内容不算全图零提及。缺的是**显式表**这一具体记账。
- **死因编号**：0。**建议动作**：进 SPEC（新建节点），优先级低于严重度 1、2 各条。
- **置信度**：中高。**不确定点**：15.13 报告明确写「y₃–y₅ 三式我**未独立重算**
  （本轮只核引文逐字，未验数值）」。**本清单也未重算**——若采纳，数值正确性需另派复核，
  或按该报告的建议依赖锚点自带数字（锚点是逐字引文，数字随引文进图，不构成模型自算数值）。

### S3-2 归一化 Legendre 多项式 φ₀–φ₅ 的显式表（15.13 正文）

- **内容**：φ₀=1/√2、φ₁=√(3/2)t、φ₂=½√(5/2)(3t²−1)、φ₃=½√(7/2)(5t³−3t)、
  φ₄=⅛√(9/2)(35t⁴−30t²+3)、φ₅=⅛√(11/2)(63t⁵−70t³+15)。
- **源**：`15.13.md:165`（截取 166 字符，整行 246 超 200 上界）、`:169`（95 字符）。
  来源节：**15.13**（提案 A3）。
- **建议 id**：`apostol:normalized-legendre-first-six-explicitly`，
  part-of → `apostol:normalized-legendre-polynomials`（存在，`data/nodes-A2-s1.jsonl:11`）。
- **为何是 3**：15.13 报告自陈「φ₂–φ₅ 可由 parent 的 φₙ=yₙ/‖yₙ‖ 加 A2 机械导出，
  按删除测试有落入**死因 5** 的风险」。即它的内容被 parent + S3-1 两者合起来覆盖。
- **死因编号**：0（若被判死因 5 则应改为给 parent 补锚，不建节点）。
  **建议动作**：进 SPEC，但**优先级最低**；退化方案是把两条锚点直接补给
  `apostol:normalized-legendre-polynomials`（FIX，不建节点）。
- **置信度**：中（转述 15.13 的自评；本清单未独立验算 φ 的数值）。

### S3-3 子空间性等价于线性包不动（15.09 习题 22(c)）

- **内容**：V 的子集 S 是子空间当且仅当 L(S) = S，即子空间恰是 L 的不动点。
- **源**：`15.09-exercises.md:43`，锚点 72 字符。来源节：**15.09**（提案 4）。
- **建议 id**：`apostol:subspacehood-is-equivalent-to-being-fixed-by-the-span-operator`，
  `requires` → `apostol:linear-span`，`equivalent` → `apostol:subspace`。
- **为何是 3**：两端各自都已有节点——`apostol:linear-span` 定义了 L(S)、
  `apostol:subspace` 与 `apostol:theorem-15-4-subspace-criterion` 给了判据。
  缺的是把两者接起来的等价命题，内容在图里以两半的形式存在。
- **死因编号**：0。**建议动作**：进 SPEC（新建节点）。
- **置信度**：中。**不确定点**：这条也可能被判为死因 5（由两端定义机械合成）。
  15.09 报告称它是「15.06 的 L(S) 定义与 15.04 的子空间判据之间缺失的那道桥」，
  我同意有桥梁价值，但**未核**是否已有边直接连这两个节点从而使桥多余。


## 四 严重度 4（练习题未建节点）

共 15 条，全部来自习题节（15.05 / 15.09 / 15.12 / 15.16）。**这一档整体是待议的**——
「练习节是否该建节点」本身没有定论；但请注意 15.05 报告的结构性发现：
15.05 的 21 道「是线性空间」的正面例题**一个节点都没有**，因为只有反例代理 CX2 扫过该节，
而它的任务书只覆盖反例。所以这一档的多寡反映的是**任务分配**，不是内容重要性。
以下 id 与字符数均取自各节报告的实测值，本清单未逐条复测。

### 4.1 来自 15.05（5 条）

| # | 内容 | 源 file:line | 建议 id | 备注 |
| --- | --- | --- | --- | --- |
| S4-1 | 第 29 题：R⁺ 上以乘积为加法、以 xᶜ 为数乘，零元是 1 | `15.05.md:61`（142 字符） | `apostol:multiplicative-model-of-a-linear-space-on-the-positive-reals` | **本档最高价值**：全章唯一「运算被整体替换仍满足十公理」的例子，证明零元是角色而非记号 0 |
| S4-2 | 第 18、19 题：有界实数列 / 收敛实数列空间 | `15.05.md:35`（31）、`:39`（34） | `apostol:sequence-spaces-bounded-and-convergent` | 两锚点刚过 30 下限；本章首次以无穷序列为向量 |
| S4-3 | 第 20、21 题：收敛级数 / 绝对收敛级数空间 | `15.05.md:37`（31）、`:41`（42） | `apostol:series-spaces-convergent-and-absolutely-convergent` | 15.05 报告倾向与 S4-2 分开建（数学判据不同），承认可争议 |
| S4-4 | 第 2 题：deg f ≤ deg g 的有理函数 | `15.05.md:7`（103 字符） | `apostol:rational-functions-with-numerator-degree-bounded-by-denominator` | 本节唯一封闭性验证不平凡的正面题；与「次数恰为 n 不是线性空间」构成对照 |
| S4-5 | 第 17 题：**变系数**二阶齐次 ODE 的解空间 | `15.05.md:31`（93）、`:33`（75） | `apostol:homogeneous-de-solution-space-variable-coefficients` | 见下方「附带 FIX」 |

**S4-5 附带 FIX（不是节点缺口，但与它同源，需一并裁决）**：反例层逐题表第 17 行写
「A1 已有 `apostol:homogeneous-de-solution-space`」，而那个节点的 statement 逐字写
「a and b given **constants**」，覆盖不到变系数。15.05 报告判这是**盲区 D（覆盖范围不足）**
的一个实例，建议把该行的「已有」判断改为「仅覆盖常系数」。
若不建 S4-5，则现状构成一次**隐性覆盖高报**。**我未复核**该反例层逐题表原文。

### 4.2 来自 15.09（6 条）

| # | 内容 | 源 file:line | 建议 id | 备注 |
| --- | --- | --- | --- | --- |
| S4-6 | ex22(e)：两子空间的交是子空间 | `15.09-exercises.md:47`（65 字符） | `apostol:intersection-of-two-subspaces-is-a-subspace` | 图里已有 22(f)(g) 的「L 不与交交换」反例节点，缺的是对照的正面一半 |
| S4-7 | ex24(a)：有限维空间的子空间有限维且 dim S ≤ dim V | `:73`（56 字符） | `apostol:subspace-of-a-finite-dimensional-space-is-finite-dimensional` | 两断言不可分：不先证有限维，dim S 无定义 |
| S4-8 | ex24(b)：dim S = dim V ⟺ S = V | `:75`（46 字符） | `apostol:equal-dimension-forces-the-subspace-to-be-the-whole-space` | `requires` → S4-7；可作 L2:count-saturation… 的实例（该 L2 现有 0 锚点） |
| S4-9 | ex23(b)(d)：{e^{ax}, xe^{ax}} 等重指数族独立 | `:57`（67 字符） | `apostol:cx-repeated-exponents-need-more-than-the-distinct-exponent-theorem` | 现有指数独立性定理要求指数互异，重指数是其**射程缺口**；结论为模型判定，origin=model |
| S4-10 | ex21(d)：{1+t, (1+t)²} 只张成 2 维 | `:35`（95 字符） | `apostol:cx-three-generators-of-a-shifted-basis-span-only-a-plane` | 15.09 报告自评「置信度中，是不是真陷阱有主观成分，可降为不建」 |
| S4-11 | ex12、ex17：Pₙ 上的齐次条件切出子空间 | `:25`（40 字符） | `apostol:homogeneous-conditions-on-p-n-cut-out-subspaces` | **与 15.05 冲突，见下** |

**S4-11 的跨节冲突（必须裁决）**：15.09 判它值得建；15.05 对同类内容判**不建**——
15.05 §三把第 3、4、8、9、10、12、15 题一律归为「齐次条件族，重复：
`x:solution-set-of-a-homogeneous-linear-condition`(15.03,3.3) 的实例」。
两节对**同一套路**给出相反结论，差别只在载体（15.05 是函数空间，15.09 是 Pₙ）。
**建议动作**：合并为一条裁决，二选一，不要两节各建一个。
另注：15.09 报告为本条自标了 **D 型风险**——ex11/13/14/15/16 同套路，但 ex11 与 ex16 的
题干被 OCR 转成 10 / 13 字符裸公式行，**低于 30 下限无法成锚**，故 statement 必须限缩到
ex12 与 ex17 两题，不得写成「ex11–17 全是子空间」。两个维数值（n 与 ⌊n/2⌋+1）为模型自算，
需登记。**我未复核**这两个维数。

### 4.3 来自 15.12（3 条）

| # | 内容 | 源 file:line | 建议 id | 备注 |
| --- | --- | --- | --- | --- |
| S4-12 | 习题 15：(ax,by)=a·conj(b)(x,y) 与第二槽共轭线性 | `15.12.md:133`（155）、`:135`（86） | `apostol:cx-sesquilinearity-is-the-two-slot-scalar-rule` | 与既有 `d:conjugate-homogeneity-in-second-argument` 是否重复未定，见下 |
| S4-13 | 习题 16(b)：极化恒等式 ‖x+y‖²−‖x−y‖²=2(x,y)+2(y,x) | `15.12.md:141`（56 字符） | `apostol:cx-polarization-recovers-the-inner-product-from-the-norm` | 自然位置是 16(a)/16(c) 那对 part-of 父子下的第二个子；兄弟冗余风险未核 |
| S4-14 | 习题 9：C(−1,1) 上三函数同时给出三种夹角 | `15.12.md:58`（66 字符） | `apostol:cx-three-functions-give-all-three-angle-cases` | **含模型自算配对**，见下 |

**S4-12 的重复风险**：15.12 报告承认图里已有 `d:conjugate-homogeneity-in-second-argument`
（15.10 的 (x,cy)=conj(c)(x,y)）与 `d:complex-homogeneity-allows-complex-scalars`，
若审计 B 认为无抽象增益会判**死因 5**。该报告主张有增益（两槽标量的乘法形式 a·conj(b)
在既有节点里不是逐字内容），但自陈「这属于值不值得的判断，不是硬事实」。

**S4-14 必须登记的自算数值**：statement 里「u₁ 与 u₃ 夹角 π/6、u₂ 与 u₃ 夹角 π/3」这一
**具体配对**是模型算的，原文 `15.12.md:61` 只说「两个正交、两个成 π/3、两个成 π/6」而未点名。
15.12 报告给出了完整计算并要求登记进「丢弃与未覆盖清单」§3。
退化方案：statement 只写「三种夹角同时出现」不点名配对，此时不含自算数值。
**我未复核**该计算。

### 4.4 来自 15.16（1 条）

| # | 内容 | 源 file:line | 建议 id | 备注 |
| --- | --- | --- | --- | --- |
| S4-15 | 习题 5：e^{−t} 权在 [0,∞) 上把幂函数正交化成 Laguerre 族 | `15.16-exercises.md:47`（56）、`:67`（116） | `apostol:cx-exponential-weight-produces-a-laguerre-family` | **可能应升为严重度 1，见下** |

**S4-15 的分级争议（请综合代理裁决）**：按「练习题」判据它属严重度 4，但它同时满足
严重度 1 的实质条件——**Laguerre 族这个数学实体全图零提及**：15.16 报告的
`grep -ic laguerre` 在 `nodes.tsv` / `statements.tsv` / `anchors.tsv` / `edges.tsv`
**四表皆 0**。最近的两个节点（`apostol:cx-exponential-weight-is-what-tames-the-unbounded-interval`
与 `d:exponential-weight-makes-improper-integral-converge`）都只讲**权为什么必要**，
不讲**这个权把幂函数正交化成什么**。两条判据在此相撞：
若主控认为「全图零提及」压过「来源是习题」，本条应移到严重度 1，成为该档第 3 条。
**我倾向移到严重度 1**，理由与 S1-1 一致（判严重度 1 的实质是「实体在图里不存在」）。

另注：本条是对 `report/丢弃与未覆盖清单.md` §一已登记丢弃项（第 5 题 OCR 题干散失）的
**部分回收**，不是新缺口。15.16 报告验算了六对内积全为 0，据此主张公式与内积的对应关系
由数学钉住、不依赖丢失的题干，故不构成死因 3（虚构出处）。它明确**不写**「这四式来自
对 tⁿ 做 Gram-Schmidt」（连接文字确已丢失），也**不猜**原题是否要求归一化。
**我未复核**那六对内积。


## 五 建议补建顺序

排序原则（按任务书）：**先建被别的节点 requires 的，再建叶子。** 在同为叶子时，
按严重度降序。所有条目的 parent 都已存在（我逐条在 `nodes.tsv` 里核过 S1-1、S1-2、
S3-1、S3-2 的 parent），故没有「先建 parent」的额外约束。

**第一批 —— 被依赖的节点（必须先建）**

1. **S2-1 线性包保序**（15.09 ex22(d)）。依赖关系：现有节点
   `apostol:cx-span-does-not-commute-with-intersection` 的 statement 以它为前提。
   它自己只 `requires` → `apostol:linear-span`（已存在）。**全清单唯一被现有节点依赖的条目。**
2. **S3-3 子空间性 ⟺ L(S)=S**（15.09 ex22(c)）。依赖关系：它是
   `apostol:linear-span`（15.06）与 `apostol:subspace` / `apostol:theorem-15-4-subspace-criterion`
   （15.04/15.06）之间的桥。虽无现有节点显式 requires 它，但它是**枢纽型**，
   排在 S2-1 之后、其余之前，可让后面几条子空间类条目挂得上。
3. **S4-7 子空间有限维且 dim S ≤ dim V**（15.09 ex24(a)）。**被 S4-8 requires**，故先于 S4-8。
4. **S4-8 dim S = dim V ⟺ S = V**（15.09 ex24(b)）。`requires` → S4-7。
   建成后可为 `L2:count-saturation-upgrades-a-necessary-condition-to-a-sufficient-one`
   提供一个 15.09 实例（该 L2 现有 0 锚点）——但归属由主控裁，15.09 报告未代决。
5. **S3-1 Legendre y₂–y₅ 显式表**（15.13 A2）。**被 S3-2 requires**（S3-2 由 parent + S3-1 导出），
   故先于 S3-2。数值复核未做，采纳前需另派。

**第二批 —— 叶子，按严重度**

6. **S1-1 多项式空间的三组张成集**（严重度 1，15.06）。叶子，无新依赖。
7. **S1-2 例 1 V₄ 的 Gram-Schmidt 算例**（严重度 1，15.13 A1）。叶子。
8. **S4-15 Laguerre 族**（15.16 ex5）。叶子。**若采纳我的建议升为严重度 1，它排在此位不变**
   （它是叶子，只有两条出边：`applies-to` → `apostol:weighted-integral-inner-product`、
   `contrasts` → `apostol:cx-legendre-family-changes-with-the-interval`，两个 dst 都已存在——
   后者我在 `nodes.tsv` 里核过：`data/nodes-A2-x2.jsonl:31`）。
9. **S3-2 归一化 Legendre φ₀–φ₅**（严重度 3，15.13 A3）。`requires` 前置 S3-1，且死因 5 风险最高，
   故排在 S3-1 之后、并建议**最后再定是否建**（退化为给 parent 补锚）。
10. **S4-1 R⁺ 上的乘法模型**（15.05 ex29）。叶子，本档最高价值，建议在习题类里第一个建。
11. **S4-5 变系数 ODE 解空间**（15.05 ex17）。叶子，但**必须与那条 D 型 FIX 一起处理**
    （改反例层逐题表第 17 行的「已有」判断），否则留下一次隐性覆盖高报。
12. **S4-4 deg f ≤ deg g 的有理函数**（15.05 ex2）。叶子。
13. **S4-2 数列空间** 与 **S4-3 级数空间**（15.05 ex18–21）。叶子；两者是否合并需先裁决。
14. **S4-6 子空间的交是子空间**（15.09 ex22(e)）。叶子，但建议紧随 S2-1/S3-3 之后处理，
    因为它与 22(c)(d) 同属「L 的算子性质」这一族，一次裁决更省。
15. **S4-12 共轭双线性** 与 **S4-13 极化恒等式**（15.12 ex15、ex16(b)）。叶子；
    两者都有兄弟冗余/死因 5 风险，建议与审计 B 的口径对齐后再动。
16. **S4-9 重指数族**（15.09 ex23(b)(d)）。叶子；结论为模型判定，origin 必须标 model。
17. **S4-14 三函数三种夹角**（15.12 ex9）。叶子；含模型自算配对，需登记或退化 statement。
18. **S4-10 位移基的三生成元只张成平面**（15.09 ex21(d)）。叶子；来源报告自评可降为不建，
    建议排在末位。
19. **S4-11 Pₙ 上的齐次条件**（15.09 ex12/ex17）。**排最后，因为它需要先解决 15.05 与 15.09 的
    相反结论**，且它带 D 型 statement 限缩要求与两个未复核的模型自算维数。

**不进本顺序的两件事**（提醒主控别混进来）：
- 15.15 例 2 的「补建」——**前提已被推翻**，不要执行。
- 所有 FIX（补锚点、改边、改 statement）——本清单只排**新建节点**的顺序。
  各节报告里的 FIX 条目（15.01 一条、15.04 一条、15.06 一条、15.07 三条、15.08 四条、
  15.10 三条、15.11 两条、15.12 两条、15.13 六条、15.14 两条、15.15 五条、15.16 两条）
  另有各自的依赖关系，不在本清单射程内。


## 顺带发现

- 任务书举的严重度 1 示例（15.15 例 2 Legendre 无节点）不成立，该节点存在且有 3 锚点 8 出边。
- 全图 `node_type` 分布无一个 `example`（concept 258 / theorem 173 / method 69 / notation 22 / 空 24），而本清单 6 条提案想用 `example`，需先裁决是否首次引入该类型。
- 「dimension 2」在三表里的命中全部属于二阶 ODE 解空间，与 15.13 例 1 的 V₄ 无关——按字面 grep 判覆盖会得到假阳性。
- 30 字符锚点下界与 OCR 逐行切分相撞，至少压死 15.02 四条公理等式、15.04 定理 15.3(a)(b)(c) 三条命题行、15.09 五道习题题干、15.10 三个核心式子（含范数定义式）、15.12 一条、15.15 两式——这是全章最高频的单一结构性障碍，已有五份报告各自建议进 SPEC。
- 15.05 报告指出正面例题无人建的成因是任务分配空档（只有反例代理 CX2 扫过该节），`report/丢弃与未覆盖清单.md` 未记录这一项。
- 15.16 报告实测该节 17 条边 evidence 未引入任何一条新源行，T2 证据增量为 0。
- 15.14 报告记 Figure 15.2 的几何直观在纯文本图谱里必然丢失，属真实但不可弥补的缺口。
- 15.05 报告发现 `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 的 statement 称「Apostol 未提及」，而 `15.05.md:63` 第 30(a) 题逐字要求证明该命题——事实错误，需 FIX。

