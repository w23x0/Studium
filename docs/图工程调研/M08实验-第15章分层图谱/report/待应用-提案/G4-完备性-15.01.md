# 15.01 覆盖完备性

## 〇 结论速览（本节 8 句 / 已覆盖 7 / 下移 0 / 未覆盖 1；其中值得补建 0 个）

源文件只有两个正文段（第 3、5 行）加一个标题行（第 1 行）。按**句**切开共 8 句
（标题行不计句）。source-lines.tsv 显示第 3 行 n_sentence_end=4、第 5 行 n_sentence_end=4，
两行都含 4 句 —— **本节完全落在过报通道 b 里，按行号比对会把覆盖率报成 2/2=100%**，
所以下面一律按句判定。

- 已覆盖 7 句（其中 6 句由节点锚点直接覆盖，1 句由节点锚点与边 evidence 同时覆盖）
- 合法下移到 part-of 后代（T3 才有）0 句
- 未覆盖 1 句：S1（本章开篇的动机句）
- 未覆盖句中值得**新建节点**的 0 个 —— S1 的内容已由现有节点
  `d:objects-already-met-that-can-be-added-and-scaled` 的 statement 承担，
  但该 statement 的核心谓词（"可加、可乘实数"）在它的两条锚点里都找不到。
  所以正确动作是给现有节点**补一条锚点**（FIX），不是建新节点。详见 §二。

## 一 逐句覆盖表（句号 / 句子摘要 / 覆盖状态 / 覆盖者 id 或 file:line / 判据）

句号规则：S1–S4 属源文件第 3 行，S5–S8 属第 5 行。

| 句号 | 句子摘要（英文原句起首） | 覆盖状态 | 覆盖者 | 判据 |
|---|---|---|---|---|
| S1 | "Throughout this book we have encountered many examples of mathematical objects that can be added to each other and multiplied by real numbers." | **未覆盖** | 无 | anchors.tsv / edges.tsv 全表 grep "Throughout this book" 命中 0；grep "multiplied by real numbers" 在 anchors.tsv 与 edges.tsv 各命中 0，只在 source-lines.tsv 命中 |
| S2 | "First of all, the real numbers themselves are such objects." | 节点锚点已覆盖 | `d:objects-already-met-that-can-be-added-and-scaled` 锚点 0 | anchors.tsv：该锚 charlen=188，逐字含 S2+S3 两句 |
| S3 | "Other examples are real-valued functions, the complex numbers, infinite series, vectors in n-space, and vector-valued functions." | 节点锚点已覆盖（两处） | 同上锚点 0；另 `d:example-3-v-n-with-componentwise-operations` 锚点 1（charlen=128） | anchors.tsv 两行；shared-quotes.tsv 未收 128 那条（n_holders=1），故非借用 |
| S4 | "In this chapter we discuss a general mathematical concept, called a linear space, which includes all these examples and many others as special cases." | 节点锚点 + 边 evidence 双覆盖 | `apostol:linear-space` 锚点 2（charlen=149，含句首 "In this chapter"）；`d:objects-already-met-...` 锚点 1（charlen=133，从 "we discuss" 起）；边 `data/edges-D1.jsonl:9` evidence（charlen=133） | anchors.tsv 两行 + edges.tsv 一行。**注意这是过报通道 c 的实例**：同一句的长短两个切片分别落在节点锚点与边证据上，冗余度虚高，新引入源行数为 0 |
| S5 | "Briefly, a linear space is a set of elements of any kind on which certain operations (called addition and multiplication by numbers) can be performed." | 节点锚点已覆盖（两处）+ 边 evidence | `apostol:linear-space` 锚点 0；`d:linear-space-is-a-set-together-with-two-operations` 锚点 0；边 `data/edges-D1.jsonl:1` | shared-quotes.tsv 该条 n_holders=3，holders 为上述两节点 + 该边。三份记账同一句 |
| S6 | "In defining a linear space, we do not specify the nature of the elements nor do we tell how the operations are to be performed on them." | 节点锚点已覆盖 + 边 evidence | `d:nature-of-the-elements-is-left-unspecified` 锚点 0（charlen=135）；边 `data/edges-D1.jsonl:2` | anchors.tsv + edges.tsv；shared-quotes.tsv n_holders=2 |
| S7 | "Instead, we require that the operations have certain properties which we take as axioms for a linear space." | 节点锚点已覆盖 + 边 evidence | `d:axioms-replace-a-construction-with-required-properties` 锚点 0（charlen=107）；边 `data/edges-D1.jsonl:4` | anchors.tsv + edges.tsv；shared-quotes.tsv n_holders=2 |
| S8 | "We turn now to a detailed description of these axioms." | 节点锚点已覆盖（但**支撑力弱**，见 §五） | `d:ten-axioms-listed-in-three-groups` 锚点 1（charlen=54） | anchors.tsv；该节点 sections 只写 15.02，见 §四 |

统计：8 句中 S2–S8 共 7 句被 15.01.md 的 9 条锚点（涉 7 个节点）与 4 条边 evidence 覆盖，
唯 S1 无任何持有者。全部 9 条锚点 has_nl=0、charlen 在 54–188 之间，
其中 S8 那条 54 字符已在 SPEC 30–200 界内但接近下界。

## 二 未覆盖且值得补建（建议 id / node_type / statement / 锚点逐字 + 实测字符数）

**建议新建节点数：0。** S1 是本节唯一未覆盖句，且它是数学内容而非过渡叙述
（它给出了整章抽象的出发点：书中已遇到的对象具有"可相加、可乘实数"这两种运算），
但它不该建新节点 —— 已有节点 `d:objects-already-met-that-can-be-added-and-scaled`
的 statement 正是在陈述这件事：

> "Before any axiom appears, the chapter lists objects encountered earlier
> **that can be added and multiplied by real numbers**: ..."

而该节点的两条锚点（188 和 133 字符）都不含 "added"、不含 "multiplied by real numbers"
（anchors.tsv 全表 grep 该串命中 0）。它锚点 0 开头的 "the real numbers themselves are
**such objects**" 里的 "such objects" 是悬空指代 —— 被指代的先行语正在 S1 里。

结论 / 依据 / 死因 / 建议动作：

- 结论：S1 未被任何锚点或边证据覆盖；同时它是现有节点 `d:objects-already-met-that-can-be-added-and-scaled` statement 主谓词的唯一出处，该节点因此存在 D 型（覆盖范围不足）锚点缺陷。
- 依据：source/apostol-ch15/15.01.md:3 第 1 句；anchors.tsv 该节点两行（charlen 188、133）；anchors.tsv+edges.tsv grep "multiplied by real numbers" 命中 0；exposure.tsv 该节点 n_partof_desc=0、T1=2、T2=1、T3=0，即**无后代可下移**，排除过报通道 a。
- 死因编号：6（引文不支撑断言）—— 仅针对 statement 第一句的谓词部分；节点本身内容正确，非死因 1/2/3/4/5。
- 建议动作：**FIX**（给现有节点补锚，不新建节点、不 KILL）。补锚后该节点锚点数 2→3，新引入源行数 +0（S1 与 S2/S3 同在第 3 行），但**新引入句数 +1**，这是真实的冗余度增益。

拟补锚点（逐字、连续、不跨行，已实测）：

```
Throughout this book we have encountered many examples of mathematical objects that can be added to each other and multiplied by real numbers.
```

- file：`15.01.md`
- 实测字符数：**142**（我用 Python `len()` 对源文件切片实测；`quote in text` 返回 True）
- has_nl：0
- 在 SPEC 30–200 界内：是

置信度：高（"S1 无持有者"由三张表的 grep 共同支持，且该节点 T3=0 排除了下移解释）。
不确定点：本条是否应改为把锚点 0 向前扩展成一条更长的引文（S1+S2+S3 合并，
合并后长度会超过 200 字符上限），还是如上另立一条 142 字符的独立锚点 ——
我按 SPEC 上限选了后者，但这是记账风格判断，主控可另裁。

## 三 未覆盖但不值得建（一行一条 + 理由：过渡叙述 / 重复 / 纯记号）

- 第 1 行 `## 15.1 Introduction`（20 字符）：纯节标题，不是句子，不建节点。
- 本节无其它未覆盖句 —— S1 是唯一一条，且它归到 §二（FIX 现有节点），不进本节。

即：本节没有"未覆盖且不值得建"的句子。这一栏为空是实测结果，不是未核。

## 四 锚点集合与 sections 集合的不一致（若有）

**两个集合确实对不上，明确报出。**

- 集合 A（anchors.tsv 中 file 列 = `15.01.md` 的锚点所属节点）：7 个节点、9 条锚点
  - `d:linear-space-is-a-set-together-with-two-operations`
  - `d:nature-of-the-elements-is-left-unspecified`
  - `d:axioms-replace-a-construction-with-required-properties`
  - `d:objects-already-met-that-can-be-added-and-scaled`（2 条）
  - `apostol:linear-space`（2 条）
  - `d:ten-axioms-listed-in-three-groups`
  - `d:example-3-v-n-with-componentwise-operations`
- 集合 B（nodes.tsv 中 sections 列含 `15.01` 的节点）：5 个
  - 前 5 项，即集合 A 去掉后两项

不一致 1（A 有 B 无，2 处）：

| 节点 id | nodes.tsv 的 sections | 它持有的 15.01.md 锚点 | 判定 |
|---|---|---|---|
| `d:ten-axioms-listed-in-three-groups` | `15.02` | 锚点 1，charlen=54，"We turn now to a detailed description of these axioms." | sections 漏登 15.01 |
| `d:example-3-v-n-with-componentwise-operations` | `15.03` | 锚点 1，charlen=128，"Other examples are real-valued functions, ..." | sections 漏登 15.01 |

依据：nodes.tsv 两行 + anchors.tsv 两行 + data/nodes-D1.jsonl:5 与 :50 原文
（两者的 `sections` 字段分别是 `["15.02"]`、`["15.03"]`，而 `anchors` 数组里都含一条
`"file": "15.01.md"`）。

不一致 2（B 有 A 无，1 处，反方向）：
`d:linear-space-is-a-set-together-with-two-operations` 的 sections 是 `15.01,15.02`，
但它两条锚点的 file 是 `15.01.md` 与 **`15.03.md`** —— 声明了 15.02 却无 15.02 锚点，
持有 15.03 锚点却未登记 15.03。依据：nodes.tsv 该行 + anchors.tsv 该节点两行。

判定汇总：

- 结论：sections 字段与 anchors 的 file 集合在 15.01 上有 3 处不一致（2 处漏登 15.01、1 处 15.02/15.03 互错）。
- 依据：nodes.tsv 与 anchors.tsv 上述各行；data/nodes-D1.jsonl:1、:5、:50。
- 死因编号：0（无死因，仅需 FIX）—— 引文本身逐字为真，statement 也没错，错的只是 sections 这个检索用元数据。check_graph 不校验 sections 与 anchors 的一致性，所以这类错不会被现有工具发现。
- 建议动作：**FIX**，把 sections 改为 anchors 实际覆盖的节的并集（分别改为 `["15.01","15.02"]`、`["15.01","15.03"]`、`["15.01","15.03"]`）。
- 置信度：高（纯机械比对，两张表互证）。不确定点：sections 的语义是否被定义为"锚点所在节"，还是"该断言在教材中的归属节"。若是后者，则第 1、2 处可能是有意为之（引文只是跨节的定位性引用）。这一点我未核到 SPEC 里的 sections 字段定义，主控需先裁定语义再决定是否改。

## 五 本节现有锚点抽查（挑 3 条，按盲区五型 A/B/C/D/E 逐型检查，写出结论）

三条都已用 Python 对源文件做 `quote in text` 逐字复核：全部 True、全部不跨行，
即"引文未被篡改"这一层全过。下面查的是机器查不出的另一层。

### 抽查 1 `d:objects-already-met-that-can-be-added-and-scaled` 锚点 0（charlen=188）

引文：`First of all, the real numbers themselves are such objects. Other examples are real-valued functions, the complex numbers, infinite series, vectors in n-space, and vector-valued functions.`

- A 变量/对象错：无。清单六项与 statement 六项逐项对得上（实数本身、实值函数、复数、无穷级数、n 维空间的向量、向量值函数）。
- B 借用引文：无。shared-quotes.tsv 中 15.01.md 只有 4 条被共享的 (file,quote)，charlen 分别是 150/135/107/133，**不含 188** —— 这条是独占的。
- C 端点当中间步骤：不适用（concept 节点，无推理链）。
- D 覆盖范围不足：**命中**。statement 的谓词"that can be added and multiplied by real numbers"在这条锚点里没有；锚点开头的 "such objects" 是悬空指代，先行语在未被任何人锚定的 S1 里。这正是 §二 那条 FIX 的由来。
- E 整句无锚：statement 第二句"The abstraction is introduced as an after-the-fact common denominator of known cases, not as an invention."**部分成立** —— "after-the-fact"（事后）这层时序含义的唯一源头是 S1 的 "Throughout this book we have encountered"（过去完成体），而该句无锚；锚点 1（S4 的 "includes all these examples ... as special cases"）只支撑"共同分母"，不支撑"事后"。

结论 / 依据 / 死因 6 / **FIX**（补 §二 那条 142 字符锚点，可同时消掉 D 与 E）/ 置信度高。

### 抽查 2 `d:ten-axioms-listed-in-three-groups` 锚点 1（charlen=54）

引文：`We turn now to a detailed description of these axioms.`

- A：无。
- B 借用引文：无。shared-quotes.tsv 无此条（n_holders=1）。
- C：不适用。
- D 覆盖范围不足：**命中，且是本节最严重的一处**。statement 断言"exactly ten axioms, presented in three groups: two closure axioms, four axioms for addition, and four axioms for multiplication by numbers"。这条 54 字符的引文里既没有"ten"，也没有"three groups"，更没有 2/4/4 的分组计数 —— 它只是一句预告性的过渡句。真正支撑"ten / three groups"的是该节点锚点 0（15.02.md，charlen=163，逐字含 "the following ten axioms which we list in three groups"）。
- E 整句无锚：2/4/4 这三个具体计数在该节点**两条锚点里都找不到**。但它不构成死因 6 —— exposure.tsv 显示该节点 n_partof_desc=19、T3=60，partof.tsv 显示它的三个直接子节点正是 `apostol:closure-axioms` / `apostol:axioms-for-addition` / `apostol:axioms-for-multiplication-by-numbers`，而这三个节点的锚点逐字给出 AXIOM 1–2、3/4/6、7/10 的编号文本，2/4/4 可由 T3 恢复。**这是合法下移（T3），只是记账位置在下层**。

结论：锚点 1 对本节点的断言零支撑力，属纯定位性引用；断言本身有 T3 支撑，不判死因 6。
依据：anchors.tsv 该节点两行；partof.tsv `d:ten-axioms-listed-in-three-groups` 行（n_children=3）；exposure.tsv 该节点行（T3=60）。
死因编号：0（仅需 FIX 记账）。建议动作：**保留节点，锚点 1 标注为定位性引用或删除**（删除后该节点在 15.01 的唯一存在理由消失，§四 的 sections 不一致也随之消解）。
置信度：中。不确定点：我核到了三个分组节点的锚点含公理编号，但**未逐一核对每组的公理条数是否恰为 2/4/4**（那要求把 15.02 的十条公理全查一遍，超出本节范围）—— 未核。

### 抽查 3 `apostol:linear-space` 锚点 0（charlen=150）

引文：`Briefly, a linear space is a set of elements of any kind on which certain operations (called addition and multiplication by numbers) can be performed.`

- A：无。
- B 借用引文：**命中（半机械化可查）**。shared-quotes.tsv 该条 n_holders=3，holders 为 `edge:data/edges-D1.jsonl:1`、`node:apostol:linear-space`、`node:d:linear-space-is-a-set-together-with-two-operations`。同一句在图里被记了三次，而这两个节点正是 part-of 父子关系（partof.tsv 中后者是前者的 17 个子节点之一）。父与子共用同一句引文，是"parent 陈述的同义改写"这一死因 5 家族的典型形态特征 —— 但**本处不判死因 5**：子节点 statement 明确加了父节点没有的内容（"putting different operations on the same underlying set produces a different linear space"），删除测试不平凡通过。冗余度层面，这三份记账新引入源行数为 0。
- C：不适用。
- D 覆盖范围不足：**命中**。`apostol:linear-space` 的 statement 断言 "a **nonempty** set V ... subject to **ten axioms listed in three groups**"。该节点三条锚点（15.01.md×2 + 15.02.md 那条 charlen=106）里**没有 "nonempty"** —— 我另核了 15.01.md 全文，也不含该词。"nonempty" 的唯一逐字来源是 15.02.md 的 "Let V denote a nonempty set of objects, called elements."，它是 `d:linear-space-requires-a-nonempty-underlying-set` 与 `d:ten-axioms-listed-in-three-groups` 的锚点 0，二者都是 `apostol:linear-space` 的 part-of 后代。
- E：无整句失锚。

结论：该节点 statement 的 "nonempty" 一词在自身 T1 里无支撑，但由 T3 后代覆盖 —— 合法下移，**不判死因 6**，属记账位置问题。
依据：anchors.tsv `apostol:linear-space` 三行；shared-quotes.tsv 150 那行；partof.tsv `apostol:linear-space` 行；anchors.tsv `d:linear-space-requires-a-nonempty-underlying-set` 行（charlen=163，逐字含 "nonempty"）；exposure.tsv 该节点行（n_partof_desc=35、T1=3、T2=0、T3=104、distinct_src_lines=45）。
死因编号：0。建议动作：**保留**；若要自足，可把 15.02.md 的 "Let V denote a nonempty set of objects, called elements." 补为该节点第 4 条锚点（该串 charlen 我未单独实测，故不写进提案的字符数栏 —— 未核）。
置信度：高（"三条锚点无 nonempty" 与 "15.01 全文无 nonempty" 都是逐字核过的）。



## 顺带发现

- `report/丢弃与未覆盖清单.md` 全文 grep `15\.01` 与 `15\.1 ` 命中 0，故本文件 §二、§四 三处均为该清单之外的新缺口。
- 本节是过报通道 b+c 的干净样本：2 个正文行、8 句，按行号算覆盖率是 2/2，按句算是 7/8；且 S4 的长短切片分落节点锚与边证据（通道 c）。可作为"冗余度须按新引入句数计"的示例，但我未推广到别节。
- nodes.tsv 的 sections 与 anchors 的 file 集合无任何工具校验（check_graph 不查），这类不一致可能不止本节 3 处 —— 未核。
- `d:example-3-v-n-with-componentwise-operations` 的 statement 里写了 "several familiar examples 15.1 cites"，即它自认引用 15.1，却把 sections 登记为 15.03 —— 与 §四 不一致 1 同源，未展开。

