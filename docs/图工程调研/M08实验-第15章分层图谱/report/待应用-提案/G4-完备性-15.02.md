# 15.02 覆盖完备性

## 〇 结论速览（本节 27 句 / 已覆盖 22 / 下移 0 / 未覆盖 4；其中值得补建 0 个）

范围：仅 `source/apostol-ch15/15.02.md`，全文 52 行（21 空行、10 行 `$$` 围栏标记、
21 行真实内容）。按**句**切得 27 个语义单元（S1–S27），其中 S1 是节标题，不计入覆盖判定，
故受判定的是 26 句：**节点锚点覆盖 20 / 仅边 evidence 覆盖 2 / 未覆盖 4**。

- **已覆盖 22**：其中 20 句由节点锚点直接覆盖，2 句（S4「Closure axioms」、
  S7「Axioms for addition」）只由**边 evidence** 覆盖，因为这两行分别只有 14 / 19 字符，
  低于 SPEC 锚点 30 字符下限，做不成锚点。
- **未覆盖 4**：全部是**公理的展示等式行**——S13 `x + (- 1) x = O.`(16)、
  S16 `a (b x) = (a b) x.`(18)、S18 `a (x + y) = a x + a y.`(22)、
  S20 `(a + b) x = a x + b x.`(22)。四行都低于 30 字符下限且 SPEC 禁止跨行拼接，
  因此在现行规则下**不存在任何合法引文**。
- **下移 0**：本节没有一句是「内容合法下移到 part-of 后代」的情形。反过来说，
  本节内容的记账位置非常密集：31 个 sections 含 15.02 的节点、43 条 file=15.02.md
  的锚点、45 条 ev_file=15.02.md 的边，全部落在本节自己的子树里。
- **值得补建 0**：四条未覆盖的等式**都已有对应节点**
  （`d:axiom-6-existence-of-negatives` / `-7-` / `-8-` / `-9-`），
  缺的不是节点而是**引文**。新建节点解决不了这个问题，因此不提任何补建节点提案。
  这四行的不可引已由 `report/SPEC缺陷-锚点规则量化.md` §2.3.1 逐条登记（含相同的
  16/18/22/22 字符实测值），本报告只补一件它没说的事：**这四条等式在图谱里的下游后果是
  「断言存在、引文缺位」**——四个节点的 statement 都写出了规范化后的等式
  （如 `x + (-1)x = O`、`a(bx) = (ab)x`），而这些等式在整张图的 anchors.tsv / edges.tsv
  里**一次都没有作为逐字引文出现过**（已 grep 验证：`(a b) x` / `a x + a y` /
  `a x + b x` / `1) x = O` 四个片段在两表中命中数均为 0）。

## 一 逐句覆盖表

切句口径：一条 `AXIOM n. NAME. <断言>` 计为**一个**语义单元（编号头与题名不单独切句），
展示块内的等式各计一个单元。**不按行号比对**——`source-lines.tsv` 显示本节 21 行真实内容里
有 12 行 `n_sentence_end ≥ 2`（第 51 行独占 6 句），按行号算必然高报（过报通道 b）。

| 句号 | 源行 | 句子摘要 | 覆盖状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 1 | 节标题 `## 15.2 The definition of a linear space` | 不适用 | — | 标题非数学内容；grep `The definition of a linear space` 在 anchors/edges 均 0 命中 |
| S2 | 3 上半 | V 是非空集合，其元素称 elements | 节点锚点 | `d:linear-space-requires-a-nonempty-underlying-set`（163 字符锚点，整行） | anchors.tsv；`apostol:linear-space` statement 亦含 nonempty 与 elements |
| S3 | 3 下半 | 满足以下十条公理（分三组）者称线性空间 | 节点锚点 | `apostol:linear-space`[1]（106）、`d:ten-axioms-listed-in-three-groups` | anchors.tsv 两条同引该分句 |
| S4 | 5 | 组标题 Closure axioms | **仅边 evidence** | edges-D1.jsonl:10（`apostol:closure-axioms` part-of `d:ten-axioms…`）、:23 | edges.tsv ev_charlen=14，低于锚点下限 30，做不成锚点 |
| S5 | 7 | AXIOM 1 加法封闭且和唯一 | 节点锚点 | `d:axiom-1-closure-under-addition`（160，整行） | anchors.tsv |
| S6 | 9 | AXIOM 2 数乘封闭 | 节点锚点 | `d:axiom-2-closure-under-multiplication-by-real-numbers`（175，整行） | anchors.tsv |
| S7 | 11 | 组标题 Axioms for addition | **仅边 evidence** | edges-D1.jsonl:11（`apostol:axioms-for-addition` part-of `d:ten-axioms…`） | edges.tsv ev_charlen=19 < 30 |
| S8 | 13 | AXIOM 3 交换律 | 节点锚点 | `d:axiom-3-commutative-law-for-addition`（73，整行） | anchors.tsv |
| S9 | 15 | AXIOM 4 结合律 | 节点锚点 | `d:axiom-4-associative-law-for-addition`（97，整行） | anchors.tsv |
| S10 | 17 | AXIOM 5 存在零元 O，使得…… | 节点锚点 | `d:axiom-5-existence-of-zero-element`[0]、`apostol:zero-element`[0]、`d:thm-15-1-…`[1]（均 85） | anchors.tsv |
| S11 | 20 | 展示式 `x + O = x \quad f o r a l l x i n V.` | 节点锚点（**唯一被锚到的展示式**） | `d:axiom-5-existence-of-zero-element`[1]、`d:axiom-5-states-the-zero-only-as-a-right-neutral-element`[0]、`d:thm-15-2-…`[2] | anchors.tsv，charlen=36；能过线只因 OCR 把 for all x in V 拆成带空格串 |
| S12 | 23 | AXIOM 6 每个 x 的 (-1)x 具有性质…… | 节点锚点 | `d:axiom-6-existence-of-negatives`、`apostol:negative-of-an-element`、`apostol:axioms-for-addition`[2]、`d:axiom-6-names-…`、`d:axiom-10-is-derivable-…`（均 87） | anchors.tsv；shared-quotes.tsv n_holders=8 |
| S13 | 26 | 展示式 `x + (- 1) x = O.` | **未覆盖** | — | 实测 16 字符 < 30；grep `1) x = O` 在 anchors/edges 命中 0 |
| S14 | 29 | 组标题 Axioms for multiplication by numbers | 节点锚点 | `apostol:axioms-for-multiplication-by-numbers`[0]（36） | anchors.tsv；恰好 36 ≥ 30，与 S4/S7 命运不同纯因字数 |
| S15 | 31 | AXIOM 7 数乘结合律引导句 | 节点锚点 | `d:axiom-7-associative-law-for-multiplication-by-numbers`、`apostol:axioms-for-multiplication-by-numbers`[1]（均 80） | anchors.tsv |
| S16 | 34 | 展示式 `a (b x) = (a b) x.` | **未覆盖** | — | 实测 18 < 30；grep `(a b) x` 命中 0 |
| S17 | 37 | AXIOM 8 V 内加法的分配律引导句 | 节点锚点 | `d:axiom-8-distributive-law-for-addition-in-v`（101） | anchors.tsv |
| S18 | 40 | 展示式 `a (x + y) = a x + a y.` | **未覆盖** | — | 实测 22 < 30；grep `a x + a y` 命中 0 |
| S19 | 43 | AXIOM 9 数的加法的分配律引导句 | 节点锚点 | `d:axiom-9-distributive-law-for-addition-of-numbers`（104） | anchors.tsv |
| S20 | 46 | 展示式 `(a + b) x = a x + b x.` | **未覆盖** | — | 实测 22 < 30；grep `a x + b x` 命中 0 |
| S21 | 49 | AXIOM 10 单位元 1x = x（等式在同一行内） | 节点锚点 | `d:axiom-10-existence-of-identity`、`apostol:axioms-for-multiplication-by-numbers`[2]、`d:axiom-10-is-derivable-…`[1]（均 66） | anchors.tsv；本行含等式，故 AXIOM 10 是**唯一等式被引文覆盖**的展示型公理 |

第 51 行是本节最密的一行：791 字符、`n_sentence_end = 6`。逐句拆开后**六句全部各有锚点**，
这是按句切之后仍然全覆盖的一段，值得记下来——按行号算会给出「1 行已覆盖」这种无信息的结论。

| 句号 | 源行 | 句子摘要 | 覆盖状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S22 | 51 | 上述线性空间有时称 real linear space，以强调乘的是实数 | 节点锚点 | `apostol:real-linear-space`[0]、`d:real-in-real-linear-space-refers-to-the-scalars`[0]（均 153） | anchors.tsv；shared-quotes n_holders=4 |
| S23 | 51 | 把公理 2/7/8/9 的 real 换成 complex 得复线性空间 | 节点锚点 | `apostol:complex-linear-space`[0]、`d:axioms-2-7-8-9-are-the-scalar-dependent-axioms`（均 128） | anchors.tsv；shared-quotes n_holders=10（本节最高） |
| S24 | 51 | 又称 linear vector space / vector space；乘子称 scalars | 节点锚点 | `d:vector-space-and-linear-vector-space-are-aliases`、`apostol:scalar`[0]（均 147） | anchors.tsv |
| S25 | 51 | 实线性空间以实数为标量，复线性空间以复数为标量 | 节点锚点 | `apostol:scalar`[1]（103） | anchors.tsv；另有 3 条边同引（edges-D1:42、A1:11、A1:12） |
| S26 | 51 | 虽主要处理实线性空间，但所有定理对复线性空间同样成立 | 节点锚点 | `d:unqualified-linear-space-may-be-real-or-complex`[1]、`apostol:complex-linear-space`[1]（均 131） | anchors.tsv |
| S27 | 51 | 不加限定说 linear space 时，实/复皆可 | 节点锚点 | `d:unqualified-linear-space-may-be-real-or-complex`[0]（124） | anchors.tsv |

## 二 未覆盖且值得补建

**结论：0 个。** 我给不出任何值得补建的节点，理由不是「内容不重要」，恰恰相反——
四条未覆盖的句子是公理 6/7/8/9 所断言的**那条等式本身**，是本节最硬的数学内容。
但它们**已经各有节点**，且节点的 statement 已写出等式：

| 未覆盖句 | 已有的对应节点 | 该节点 statement 里的等式 |
|---|---|---|
| S13 `x + (- 1) x = O.` | `d:axiom-6-existence-of-negatives` | `x + (-1)x = O` |
| S16 `a (b x) = (a b) x.` | `d:axiom-7-associative-law-for-multiplication-by-numbers` | `a(bx) = (ab)x` |
| S18 `a (x + y) = a x + a y.` | `d:axiom-8-distributive-law-for-addition-in-v` | `a(x + y) = ax + ay` |
| S20 `(a + b) x = a x + b x.` | `d:axiom-9-distributive-law-for-addition-of-numbers` | `(a + b)x = ax + bx` |

所以这是**引文缺位**而不是**节点缺位**。再建节点只会制造死因 5（伪抽象·层塌缩）：
新节点会与既有公理节点断言同一件事，且新节点同样引不到那条等式。

**依据**：`15.02.md:26 / 34 / 40 / 46`，实测字符数 16 / 18 / 22 / 22（python `len()` 实测，
与 `SPEC缺陷-锚点规则量化.md` §2.3.1 表中数值一致）。SPEC.md:54 要求锚点 30–200 字符且
「不跨行拼接」，两条规则合起来使这四行**不可能**成为任何合法锚点或（若照锚点标准要求）
边 evidence。**死因编号：0**（非死因，仅需 FIX/SPEC 修改）。**建议动作：不建节点**。

**可选的记账修补（供主控裁量，我不主张必须做）**：SPEC.md 只对 `anchors` 写了 30 字符下限，
对边 `evidence` **未写任何长度下限**；图里已有先例——edges-D1.jsonl:10/11 分别用
14 字符的 `Closure axioms` 与 19 字符的 `Axioms for addition` 作 evidence 并通过了机器校验。
照此先例，这四条等式**可以**作为某条既有边的 evidence 进入 T2 池，从而让等式的逐字文本
至少在图里出现一次。我不给具体改边方案，因为选哪条边承载是语义判断（改哪条边、
是否会破坏该边原有的证据支撑），超出本任务范围；且这依赖「边 evidence 无长度下限」
这一读法被 SPEC 正式确认（见 §顺带发现第 1 条）。**置信度**：等式不可锚这一事实置信度高
（逐字实测 + grep 双证）；可选修补的可行性中等（取决于 SPEC 对边 evidence 长度的裁定）。

## 三 未覆盖但不值得建

- **S1**（行 1，`## 15.2 The definition of a linear space`，40 字符）：纯节标题，
  无数学断言；不计入未覆盖统计。grep 全表 0 命中，属正常。
- 四条展示等式（S13/S16/S18/S20）**不属于本节**——它们是数学内容而非过渡叙述，
  只是「不值得**新建节点**」，见 §二。为免混淆，本节不重复列出。
- 本节**没有**真正意义上的「过渡叙述」型未覆盖句。第 3 行的引入句、第 51 行的术语段
  都已被节点锚点覆盖（S2/S3、S22–S27），连 `Sometimes a linear space is referred to as…`
  这种纯术语句也建了 `d:vector-space-and-linear-vector-space-are-aliases`（node_type=notation）。
  **判断：本节的覆盖失败模式是单一的**——只在「短展示等式」这一处失败，且失败原因是规则
  而非产出者的取舍。这与 A4a §（`audit-A4a.md:114`）的结论一致，我复核后同意其判断。

## 四 锚点集合与 sections 集合的不一致

**有不一致，两个方向都有。** 集合 A = anchors.tsv 里 `file == 15.02.md` 的节点，|A| = 32；
集合 S = nodes.tsv 里 `sections` 含 15.02 的节点，|S| = 31。两集合交集 29，各有 3 / 2 个独有项。

### A \ S：有 15.02.md 锚点但 sections 不含 15.02（3 个）—— 判**合法，不改**

| 节点 | sections | 引 15.02 的哪一句 | 判定 |
|---|---|---|---|
| `d:example-4-nontrivial-check-is-closure-by-linearity-of-the-dot-product` | 15.03 | S5（AXIOM 1，160） | 合法：断言陈述于 15.03，只是把公理 1 当作**被检验的对象**引来 |
| `d:thm-15-1-two-candidate-zeros-swap-roles-in-axiom-5` | 15.04 | S10（AXIOM 5，85） | 合法：断言是定理 15.1 的证明分析，陈述于 15.04 |
| `d:thm-15-2-left-neutrality-needs-the-commutative-law` | 15.04 | S11（`x + O = x …`，36） | 合法：同上，陈述于 15.04 |

**依据**：`report/待应用-提案/S2-SPEC-字段语义.md:35`（项 4）裁定 `sections` 语义为
**「被定义/陈述于」而非「被用到」**。这三个节点都是「在 15.03/15.04 陈述、引 15.02 的公理作依据」，
按语义 A 正确。**死因 0，建议动作：保留**。置信度高。
**这条不一致本身不是缺陷，而是 `sections` 语义 A 的正常表现**——它说明「file=15.02.md 的锚点数」
不能当作「15.02 有多少节点」的代理指标，反之亦然。报覆盖率时必须说清用的是哪个集合。

### S \ A：sections 含 15.02 但无 15.02.md 锚点（2 个）—— 疑似记账错

| 节点 | sections | 现有锚点 | 判定 |
|---|---|---|---|
| `d:linear-space-is-a-set-together-with-two-operations` | 15.01,15.02 | 15.01.md(150)、15.03.md(138) | **疑 FIX**：statement「线性空间是集合 V 加两条运算规则」陈述于 15.01 与 15.03（其两条锚点所在），15.02 只是**列出了公理**，没有明说「集合 + 两运算」这层。按语义 A，`sections` 里的 15.02 应删。置信度中——15.02 的 S2/S3 合读确实隐含此意，但**隐含不等于陈述于**。 |
| `d:example-1-verification-is-the-field-axioms-of-r` | 15.02,15.03 | 15.03.md(114)、15.03.md(144) | **疑 FIX**：statement 是对 EXAMPLE 1（V = R）的逐公理核对，陈述于 15.03；15.02 是**十条公理的出处**，属「被用到」而非「被陈述于」。`sections` 里的 15.02 应删。置信度中高。 |

**死因编号：0**（记账位置错，非内容错）。**建议动作：FIX（删 sections 里的 15.02），共 2 个节点**。
**不确定点**：这两条与 S2 提案项 4 已认定的「12 个（+1 联动）须改节点」是否重叠，我**未核**
——S2 提案没有列出那 12 个的逐条 id，我不去猜。主控裁决时请先与 S2 的清单对表，避免重复计数。

## 五 本节现有锚点抽查

抽查 3 条，逐条按盲区五型 A（变量/对象错）/ B（借用引文）/ C（把端点当中间步骤）/
D（覆盖范围不足）/ E（整句完全无锚）走一遍。B 型用 shared-quotes.tsv 半机械化，
其余四型靠读 statement 与源文对照。

### 抽查 1 `d:only-axiom-1-states-that-the-result-is-unique`

锚点：`15.02.md` AXIOM 1 整行（160）、AXIOM 2 整行（175）。origin 空（**非** model）。
statement 两句：①「公理 1 说对每一对有**唯一**元素对应，公理 2 只说有元素对应、无唯一性子句」；
②「Both operations are single-valued **in every example**, but the requirement is written into
the text only for addition.」

- **A 型：未命中**。公理 1 谈 pair→sum、公理 2 谈 scalar+element→product，statement 对得上。
- **B 型：未命中（合法共引）**。两条引文分别有 4 / 5 个持有者（shared-quotes.tsv）。
  但本节点的断言**就是在比较公理 1 与公理 2**，同时引两条是断言本身要求的，
  与 `丢弃与未覆盖清单.md:100–101` 已判合法的「公理组节点引用它所含公理」同型。
- **C 型：未命中**。引文即断言所比较的两条公理本身，无端点/中间步之别。
- **D 型：命中——第②句前半的全称量词无任何证据**。「in every example」是**跨节的全章级
  全称断言**（例子在 15.03），而本节点四层证据池**全部落在 15.02.md 之内**：
  T1 两条锚点（160、175），T2 三条自发边 evidence（`Closure axioms` 14、
  `there corresponds a unique element in V called the sum of x and y` 65、
  `there corresponds an element in V called the product of a and x, denoted by ax.` 79），
  T3 无（该节点无 part-of 后代）。**15.02 全节不含任何 example**，所以池里不可能有支撑。
  依据：pool.tsv 该 id 全部 5 行；nodes.tsv 该行 origin 列为空。
  **死因编号：1（量词过度断言）**——且按本轮规则，死因 1 **不因边证据而免除**，这里边证据
  确实也没帮上忙（三条 T2 全在 15.02 内）。
  **建议动作：FIX**。二选一：(a) 把「in every example」改成有据的表述，例如限定为
  「在公理的文字里」；(b) 保留该观察但整句标 `origin: model` 并注明是模型的跨节归纳。
  我倾向 (a)，因为 (b) 会把一个可验证的事实降级成模型意见。
  **置信度：高**（池内无 example 类引文，可机械复核）。**不确定点**：15.03 起的各 example
  是否真的「全部单值」我**未逐例核**——但这不影响判定，判定针对的是**证据缺位**而非事实真假。
- **E 型：未命中**。第①句有锚。

### 抽查 2 `d:axiom-5-states-the-zero-only-as-a-right-neutral-element`

锚点 2 条：`15.02.md` 的 `x + O = x \quad f o r a l l x i n V.`（36）、
`15.04.md` 的 `y _ {2} + (x + y _ {1}) = (y _ {2} + x) + y _ {1} = O + y _ {1} = y _ {1} + O = y _ {1}.`（88）。
statement：「公理 5 只断言 x + O = x，对 O + x 一无所言。两侧性质——**从定理 15.1 起就被自由使用**
——是由公理 3 提供的，所以交换律每次在左边加 O 时都在暗中干活。」

- **A 型：未命中**。x + O = x 确为右中性，statement 的变量与源文一致。
- **B 型：未命中（合法共引）**。36 字符那条 shared-quotes 记 5 个持有者
  （2 边 + `d:axiom-5-existence-of-zero-element` + 本节点 + `d:thm-15-2-…`）。三个节点的断言不同：
  一个陈述公理、一个指出单侧性、一个谈定理 15.2 的用法，**同一等式支撑三种不同观察**是正当的。
- **C 型：未命中**。88 字符那条恰是「用公理 3 把 `O + y_1` 翻成 `y_1 + O` 再消去」的**中间步**，
  正是 statement 所指的机制，不是把结论当机制。这条锚点选得准。
- **D 型：命中（轻）——「从定理 15.1 起」的起点没有引文**。锚点覆盖的是**定理 15.2 的**证明链
  （15.04.md:20），池里没有任何来自定理 15.1 证明（15.04.md:7）的引文。
  T1 两条 + T2 两条（edges-D1:26 引 36 字符式、:27 引 AXIOM 5 行）+ T3 无（无后代），
  依据 pool.tsv 该 id 全 4 行。而定理 15.1 的证明确实用了这一步
  （`15.04.md:7` 含 `But $O_1 + O_2 = O_2 + O_1$ because of the commutative law, so $O_1 = O_2$ .`
  ——我实测该子串 **76 字符**、在 15.04.md:7 内逐字连续、不跨行，合规）。
  **死因编号：6（引文不支撑断言）**，程度轻：断言为真，只是所声明的**起点**无引文。
  **建议动作：FIX**，把上述 76 字符子串补为第 3 条锚点（该节点现有 2 条，SPEC 上限 3，
  有位置）。或退一步把「从定理 15.1 起」改成「在定理 15.2 的证明里」，与现有锚点严格对齐。
  **置信度：中高**。**不确定点**：这条属于「引文射程不足」还是「表述可收窄」的边界，
  按 S2 提案项 1 的三分表，失败点在「断言是否落在引文射程内」这一环，故归 6 而非 3。

### 抽查 3 `apostol:axioms-for-multiplication-by-numbers`

锚点 3 条（已达 SPEC 上限）：`Axioms for multiplication by numbers`（36）、
AXIOM 7 引导行（80）、AXIOM 10 整行（66）。
statement：「第三组公理（公理 7 到 10）：`a(bx) = (ab)x`、`a(x + y) = ax + ay`、
`(a + b)x = ax + bx`，以及对每个 x 与所有实 a、b 有 `1x = x`。」

- **A 型：未命中**。四条等式与源文 34/40/46/49 行逐条对应，变量无错位。
- **B 型：未命中（合法共引）**。三条锚点分别 2 / 4 / 5 个持有者，但都属
  「公理组节点引用它所含的那条公理」，与 `丢弃与未覆盖清单.md:100–101` 已判合法的第三组同型。
- **C 型：未命中**。
- **D 型：命中——statement 写出 4 条等式，锚点只覆盖到其中 1 条**。
  `1x = x` 在 AXIOM 10 锚点内（该行含等式）；另三条等式
  （`a (b x) = (a b) x.` / `a (x + y) = a x + a y.` / `(a + b) x = a x + b x.`）
  **不在任何锚点内**：AXIOM 7 的锚点止于 `we have`，公理 8、9 两行连锚点都没进这三条之列。
  扩到 T3 也救不了——后代 `d:axiom-7/8/9-…` 的锚点同样都是**引导句**，止于 `we have`
  （依据：pool.tsv 该 id 共 19 行，逐行看 quote 均无等式）。
  即：**该节点池内 19 条证据，没有一条含 3/4 的等式**。
- **E 型：未命中**（「公理 7 到 10」这个枚举本身由三条锚点支撑）。
- **判定**：D 型命中，但**根因是 §二 那条 SPEC 规则缺陷，不是产出者的取舍**——
  那三条等式在现行规则下不存在合法引文。因此**不判死因 6**，**建议动作：保留**，
  待 SPEC 放宽展示行下限后补锚点。**置信度：高**（等式不可引已双证）。
  **这条抽查的价值在于给出一个可推广的检查式**：凡 statement 写出等式而池内无等式引文的节点，
  都是同一缺陷的下游。全图有多少个这样的节点我**未查**，本任务范围只到 15.02。

## 顺带发现

- SPEC.md:54 给锚点定了 30 字符下限，但边 `evidence`（SPEC.md:71–76）**没有任何长度规定**；图里已有 14 / 19 字符的 evidence（edges-D1.jsonl:10、:11）通过机器校验，建议 SPEC 明确边 evidence 是否同受 30 下限约束——否则「进不了锚点但能进边」的不对称会被当成绕道用。
- `d:only-axiom-1-states-that-the-result-is-unique` 的 origin 列为空而非 `model`，但其第②句是模型的跨节归纳；同类「D1 层节点做了全章级归纳却未标 origin」的情形我未做全表扫描。
- `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 标了 `origin=model` 且 statement 首句自陈「the derivation below is not in the text」——这是本节唯一标注诚实的模型内容，可作正面样例。
- `apostol:axioms-for-multiplication-by-numbers`[0] 的锚点 `Axioms for multiplication by numbers` 恰 36 字符而过线，同类的 `Closure axioms`(14)、`Axioms for addition`(19) 落线——三个同质组标题因字数被分成两类命运，是 30 下限任意性的一个干净例证。
- 本节 21 行真实内容里 12 行 `n_sentence_end ≥ 2`（第 51 行 6 句），若按行号算覆盖率会得到「20/21」这种高报数；按句切实际为 22/26。
- `unverified-model-quotes.tsv` 里 0 条涉及 15.02.md，本节不受那 15 条未校验边的影响。
