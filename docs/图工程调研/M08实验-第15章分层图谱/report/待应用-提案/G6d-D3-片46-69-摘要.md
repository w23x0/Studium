# D3 片46-69 结构化摘要

来源：`report/审查-形式D-D3-片46-69.md`（唯一输入，只读）。覆盖 `data/nodes-D3.jsonl` 第 46–69 行共 24 节点，全部落在 15.11（另 2 条跨文件锚指向 15.10.md）。本摘要只压缩，不新增裁定。

## 〇 速览

本片共 24 条节点级裁决（按节点计，非按 claim 计）：**KILL 0 / FIX 15 / 保留 9；未决 0 条节点**，但**死因编码二选一未定 4 条**（L51、L66、L69 的 1 vs 0+强制改写；L67 的 0+强制改写 vs 4），分片明确把取舍权交裁定员。

- 死因 1（量词过度断言）建议：3 条 —— L51、L66、L69
- 死因 0 + 强制改写：3 条 —— L57、L67、L65（L65 为术语改写）
- 死因 0 = 保留 + 补锚：9 条 —— L46、L47、L48、L49、L50、L52、L53、L58、L61
- 死因 0 = 保留、无需动作：9 条 —— L54、L55、L56、L59、L60、L62、L63、L64、L68
- 死因 2 / 3 / 4 / 5：**0 条实质实例**。唯一可升 4 的候选是 L67 claim 2 的定位性假陈述（"两节之内"），非数学错误。
- 分片自报计算值 0 错（7 项模型自算值 + 3 项反事实/刻画核验，全部人工验算通过）。

## 一 裁决表

依据列的 file:line 为分片给出的「缺口所在原文行」或「可引未引行」；无前缀者均指 `apostol-ch15/15.11.md`。

| 编号 | 节点 id | 死因 | 动作 | 依据 file:line | 一句话理由 |
|---|---|---|---|---|---|
| L46 | `d:orthogonal-set-condition-on-distinct-pairs-only` | 0 | 保留+补锚 | :5、:7 | claim 3/4/5 零锚但为真，`:5`（170 字符）、`:7`（238 字符）合规可引，只用了 1/3 锚点槽 |
| L47 | `d:orthonormal-set-adds-unit-norm` | 0 | 保留+补锚 | :67、15.10.md:123 | claim 2/3/4/5 零锚为真；末句所断言的正是 `:67` 原文所说，可引未引 |
| L48 | `d:normalizing-an-orthogonal-set` | 0 | 保留+补锚 | 15.10.md:125（claim 2）；claim 3 归 SPEC | claim 2 可引未引；claim 3 的齐性公理 `15.10.md:19` 仅 21 字符被下限卡死，不判 producer |
| L49 | `d:trigonometric-orthogonal-system` | 0 | 保留+补锚 | :17（claim 1）、:26（claim 3） | claim 3 断言的是正交关系式而锚给的是结论句（D/C 边缘）；claim 1/6 零锚为真 |
| L50 | `d:trigonometric-system-norms` | 0 | 保留+补锚 | :29（claim 1）、:38（claim 4） | claim 1 的 2π 值无锚直接覆盖（只能经范数定义反推），`:29` 有逐字可引句；第 3 槽空置 |
| L51 | `d:normalized-trigonometric-system` | **1**（或 0+强制改写） | FIX（改/删末句） | 锚点仅 :38；缺口在 :77 Thm 15.12 的有限维前提 | 末句把 (15.11) 的「经典 Fourier 含义」赋给无限维 C(0,2π) 上的无限三角系，越出 (15.11) 已证形式 |
| L52 | `d:15-10-nonzero-hypothesis-is-essential` | 0 | 保留+补锚 | :3、15.07.md:3、:15 | claim 4/5/6/7 零锚但数学全真；claim 3 前半「只约束相异对」射程不足 |
| L53 | `d:15-10-proof-setup-vanishing-finite-combination` | 0 | 保留+补跨文件锚 | 15.07.md:3 | 末句的独立性定义依据在 15.07，跨文件锚未设且 `sections` 只写 ["15.11"] |
| L54 | `d:15-10-proof-pair-with-a-single-element` | 0 | 保留（可补锚） | 15.10.md:27 可引；claim 3 齐性部分归 SPEC | claim 3 的「两槽各缺一条规则」二难经验算成立；齐性公理 21 字符卡死，非 producer 之责 |
| L55 | `d:15-10-proof-divide-by-nonzero-self-inner-product` | 0 | 保留（可补锚） | 15.10.md:22（35 字符，合规） | claim 2/3/4 零锚全真；正定性公理达标却未引，不能归 SPEC |
| L56 | `d:15-10-proof-repeat-for-each-index` | 0 | 保留 | 锚点 :15 已覆盖 claim 1 | claim 2/3 是对已锚原句的元评注，射程外延极短 |
| L57 | `d:15-10-n-orthogonal-nonzero-elements-form-a-basis` | **0 + 强制改写**（字面读可升 1） | FIX（改末句）+补跨文件锚 | 锚点止于 :15；刻画依据 15.08.md:23 | 末句「全部力量来自维数计数定理」不成立（仍依赖第一分句的正交⇒独立），且与本节点 claim 2 的「两步复合」内部矛盾 |
| L58 | `d:existence-of-an-orthogonal-basis-deferred` | 0 | 保留+补锚 | :43 | claim 2 零锚为真；末句为图工程编辑性说明，无可核真值 |
| L59 | `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis` | 0 | 保留 | 锚点 :43 + :46 完整覆盖 | claim 2 前半由锚点措辞的「缺席」合法支撑；后半「范数不要求已知」略松 |
| L60 | `d:orthogonal-basis-component-formula` | 0 | 保留（可补锚 :67） | 锚点 :49 + :52 | 散文锚 + 公式锚配对得当，claim 2/3 为对已锚公式的直读 |
| L61 | `d:15-11-proof-pair-with-basis-element` | 0 | 保留+补锚 | :67 | claim 1 的理由子句「(e_i,e_j)=0 当 i≠j」在 `:67` 未锚，而 L62 就锚了它；第 3 槽空置 |
| L62 | `d:15-11-denominator-nonzero-by-positivity` | 0 | 保留（可补 15.08.md:3） | 锚点 :67 + 15.10.md:22 | 本片区最扎实之一：origin=model 标注 + 跨文件公理锚 + 把 Apostol 隐含步骤显式化 |
| L63 | `d:orthonormal-basis-component-is-the-inner-product` | 0 | 保留+补锚 | :67 | claim 2 以引文口吻复述了 `:67` 的从句却未锚（引文式复述无锚，比纯洞见更该补） |
| L64 | `d:orthonormal-expansion-formula` | 0 | 保留+补锚 | :89 | 末句断言的正是 `:89` 逐字说的事，合规可引而第 3 槽空置（分片自评「最该引却没引」） |
| L65 | `d:parseval-requires-an-orthonormal-basis` | 0 + 术语改写 | FIX（"bilinear"→共轭双线性） | :80 带共轭杠；15.10.md:45 明言本章定理对复空间有效 | 实质数学（1/(e_i,e_i) 因子）验算正确，缺陷限于术语：该形式对第二变元是共轭线性 |
| L66 | `d:parseval-formula-statement` | **1**（或 0+强制改写） | FIX（claim 3 补「有限维」「Hermite」） | 锚点 2 = :75 逐字含 finite-dimensional | claim 3 把原文的 finite-dimensional 复述成 **every** —— 锚点里有的限定词被抹掉；另有复空间下「点积」不带共轭的失准 |
| L67 | `d:parseval-proof-pair-expansion-with-y` | **0 + 强制改写**（从严可 4） | FIX（删/改两条从句） | 三次出现均在 :15、:61、:89，同属 §15.11 | claim 2「两节之内」为假（把定理编号误当节号，与自身 `sections` 矛盾）；claim 3 把配对技巧归因于正交规范性亦失准，与 L65 不一致 |
| L68 | `d:parseval-conjugate-comes-from-hermitian-symmetry` | 0 | 保留 | 锚点 :80 + 15.10.md:32（公理 1'） | 本片区最扎实之一：共轭归因经双路验算成立（1' 引入 + 共轭齐次性本身派生自 15.10.md:35–:41） |
| L69 | `d:parseval-norm-form` | **1** | FIX（改/删末句） | 锚点止于 :86 + :89；反证在 15.15.md:25–:51 | 末句把 (15.12)「特化」到无限维 C(0,2π) 的无限三角系得经典 Parseval，与 (15.12) 的有限维正交规范基前提冲突；另 claim 3 的范数定义式 `15.10.md:108` 26 字符被 SPEC 卡死 |

## 二 分片自评最重的条目

分片给出的严重度序（形式 D 六处按降序），以及三处「最」字自评：

1. **L51 + L69 并列最重（分片明言「D-1（最重）」「D-2（最重，与 D-1 同型配对）」）** —— 同一种越界犯两次，分别在本片 Parseval 处理的入口（标准化系统）与出口（范数形式）。分片建议**合并裁定**，两条同判死因 1。
2. **L66 第三重（D-3）** —— 唯一一例「锚点里有的限定词在断言里被抹掉」（finite-dimensional → every）。
3. **L57 第四重（D-4）** —— 「全部力量来自维数计数」越出射程且与本节点自述矛盾。
4. **L64 = 分片自评「本片区最该引却没引」的一条**（§3 E-中末条 + §8 批次 4：「因为末句断言的正是 `:89` 说的事」）。**注：主控 §4.11 已单独裁定 L64，本条只作指针，不重复处理。**
5. **L67 = 分片自评「本片区唯一含明确假陈述」的节点**（"两节之内"）。
6. **L62 与 L68 = 分片自评「构造最扎实的两个节点」**，也正是唯二带 `origin=model` 标注、唯二设了跨文件锚的节点。分片建议把二者做法（模型内容显式标注 + 跨文件锚被援引的公理）推广到 L46/L47/L52/L53，可直接消掉 E-中过半。

另两处局部射程不足（分片自列为 D-5、D-6，严重度最低）：L61 claim 1 理由子句未锚 `:67`；L50 claim 1 的 2π 值无锚直接覆盖。

## 三 未决与不确定项

分片 §9 明确声明的未覆盖项与自认不确定处（原文用词：「这是本报告最主要的**可能高估处**」等）：

1. **死因编码为建议值而非裁定，4 条二选一未决**：L51 / L66 / L69 的 **1 vs 0+强制改写**（分歧点：`:91` 的 Parseval 命名注是否可当「历史指向」读法；L66 的 "in an orthonormal basis" 是否隐含预设有限基），L67 的 **0+强制改写 vs 4**（无锚假陈述从严与否）。分片对 L51 自表「我倾向 1，理由是节点显式引了编号公式 (15.11) 而非泛指 Parseval」。
2. **`edges-D3.jsonl` 未审 —— 分片自标的最大可能高估处**。L46 的 `atomic_reason` 称「关于零元的推论通过 edges 记录而非拆出」，L52 依赖 `parent` 指向的 `apostol:theorem-15-10-*`。若边确实承载了那些推论，**L46 claim 3/4、L47 claim 3 的严重度可下调**。
3. **`SPEC.md` 未通读**。§6 的字符数为分片实测，30 字符下限 / 3 锚点上限口径直接采用任务书给定值。若 SPEC 对公式行有豁免条款，§6 的「6 处受牵连 claim」计数需重算。
4. **A 型（变量位置错）与 B 型（借用引文）未系统排查**，分片自认非其职责。仅顺带登记若干共享：`:15` 被 L54/L55/L56 分别截不同子串、`:43` 被 L59 与 L65 共用、`:80` 被 L66 与 L68 共用、`:67` 被 L62 锚用而 L63 正文复述未锚 —— **是否构成 B 型借用，分片未判定**。
5. **未读任何其它审查员输出**（含 `裁定-*.md`、`audit-*`）。L52 带 `audit_fix = "audit-A1"` 字段，分片是在不知其内容的前提下独立作出判定的，编号/判型口径可能与他人不一致。
6. **任务书预期的专项在本片无适用节点**：反常积分/权函数（Example 4/5，`15.10.md:73`、`:75`–`:81`）机器 grep 命中 0；任务书列举的五类符号值（(e−1/e)/2、3/e、e²−1、pi − 2 sin x、x = 1 and y = i）命中 0，应在 D3 前 45 节点或 D4。Cauchy–Schwarz（Thm 15.8）、范数性质（Thm 15.9）、Gram-Schmidt（15.13）、投影（15.14/15.15）**均无节点在本片**。
7. **L69 claim 4 的图内同名冲突需交叉核，分片未核**：Apostol 本章把 "Pythagorean formula" 用于正交分解式 (15.17)（`15.14.md:24`），未用于 (15.12)；若 D4/D5 为 (15.17) 也建了 Pythagorean 名节点，图内会同名不同指（`15.07.md:29` 的 "Pythagorean identity" 指 cos²+sin²=1，是第三处不同指）。
8. **L55 末句的反事实保留意见**：正半定形式下已不是 Apostol 意义的 Euclidean space，故该断言是对修改后设定的反事实陈述；分片认为节点措辞已标明假设语气，可接受。

## 四 分片自身的记账错

以下逐条对过 `tmp_index`（nodes.tsv / anchors.tsv / source-lines.tsv / edges.tsv）。**分片的实质裁决无一因此翻转**，但合并时若照抄这些数字会出错。

1. **§5 小标题数与清单数不符（2 处）**：「判 KEEP 但应补锚（…8 个）」下实际列 9 个 id（分片自己在同段末括号里承认「按 id 计 9 个条目」，标题未回改）；「判需改写（…5 个）」下实际列 6 个（L51、L69、L66、L57、L67、L65）。**正确数：补锚 9、需改写 6，合计 9+9+6 = 24 ✓。**
2. **§3 与 §5 的「23/24 含无锚 claim」低报 1**：分片称「唯一例外为 L66」，但其 §8 批次 5 自己把 L66 claim 3 判为「**无锚**，且有两处问题」。L66 的真实例外性只在「**末句**被锚点覆盖」，不在「无无锚 claim」。**正确数：24/24 个节点均含至少一条无锚 claim。**
3. **§3「22 个节点末句为无锚附加内容，唯一例外 L66」内部不一致**：若只有一个例外，应为 23。分片各批小结实报为 5+5+4+5+3 = 22 命中，即实有两个例外（L66 末句被锚覆盖，另一为批次 3 中未命中的 L59）。**「22」与「唯一例外」二者只能取一。**
4. **§6 与 §8 批次 5 的 `15.10.md:105` 字符数误差 1**：分片两处写 89 字符，`source-lines.tsv` 实测为 **90**。不影响「合规但不含该等式本身」的结论。
5. **§8 批次 1/2 的字符数为约值且与实测有偏差**：批次 1 称 `15.11.md:5` 98 字符、`:7` 首句 79 字符，`source-lines.tsv` 实测整行为 **170** 与 **238**（该两行分别含 2 句、3 句）；批次 2/3 称公理 (1) 约 48/49、公理 (2) 约 62/63、`:27` 约 66/68，实测为 **49 / 63 / 68**。分片是按拟引子串估的、未标明，合并时应改用实测整行值或明确标注子串。
6. **按行号比对导致的过报风险（通道 b），分片未识别**：其大量「可引未引」指认落在**含多句的源行**上 —— `:5`（2 句）、`:7`（3 句）、`:29`（3 句）、`:35`（2 句）、`:67`（2 句）、`:89`（3 句）、`15.11.md:3`（4 句）。其中 `:89` 的三句已分别被 L67 锚（第 1 句）与 L69 锚（第 3 句）占用，故 L64「`:89` 可引未引」应表述为**句粒度**未引，而非整行未被任何节点引用。同理 `:67` 已被 L62 锚用。
7. **§4.4 自称「7 项全部逐项人工验算」，实为 7 项模型自算值 + 3 项附带核验**（L55 反例、L57 对 Thm 15.7(b) 的刻画、L69 的 z·conj(z)），分片自己在同节末尾以「附带自行验算」列出，标题的「7 项」未含这 3 项。不影响「0 错」结论。
8. **锚点槽统计经复核为真，可放心引用**：`nodes.tsv` 第 46–69 行确为 **9 个 1 锚点 + 15 个 2 锚点，无一用满 3 条**；`anchors.tsv` 确为 `15.11.md` 37 条 + `15.10.md` 2 条，且这 2 条跨文件锚确实全部出自 `origin=model` 的 L62、L68。§6 的两条 SPEC 卡死项亦经实测确认：`15.10.md:19` = **21** 字符、`15.10.md:108` = **26** 字符，均 < 30；被判「达标」的正定性式 `:22` = **35**、加性公理 `:17` = **63** 亦确认。
9. **未发现「把有锚说成无锚」类记账错**：抽查 L66 末句（分片称锚点 1 覆盖）—— `anchors.tsv` 中 L66 锚点 #0 确为 (15.11) 全式且逐字含 `(\overline {{y , e _ {i}}})`，判定正确。

## 五 主通道统计

分片本身在**四层证据池定义之前**写成，全程只用「节点自身锚点」（≈ T1）判射程，未查 T2/T3。故本节按 `tmp_index/exposure.tsv` 与 `pool.tsv` 补出分片六条形式 D 指控的实际塌陷层，供合并代理对齐主控 §4.11 的口径。**这不是新裁定，主控 §4.11 已对本区间给出「7 条塌陷、3 条指控成立」的正式结论，本节只标层位。**

| 分片指控 | 节点 exposure（T1/T2/T3/池总/去重源行） | 缺口是否被池内某层补上 | 塌陷层 |
|---|---|---|---|
| D-1 L51 | 1/3/0/4/2 | 否（3 条 T2 全在重复 `:35` 标准化句，无一触及 Parseval 前提） | **不塌陷，指控成立** |
| D-2 L69 | 2/2/0/4/2 | 否（2 条 T2 只重复自身两条 T1 的 `:86`/`:89`，无新源行） | **不塌陷，指控成立** |
| D-3 L66 | 2/2/0/4/3 | 否（T2 新引入的是 `:89` 证明句，与 finite-dimensional→every 的量词缺口无关） | **不塌陷，指控成立** |
| D-4 L57 | 2/3/0/5/2 | 否（3 条 T2 全在重复两条 T1 的同一源行，去重后仍只 2 行） | **不塌陷，指控成立** |
| D-5 L61 | 2/2/0/4/3 | **是** —— `edges-D3.jsonl:148`（src=L61，requires）evidence 逐字含 `:67`「since $(e_i,e_j)=0$ if $i \neq j$」，正是分片说的未锚理由子句 | **T2 塌陷**（记账位置问题：内容在图里，只是挂在边上） |
| D-6 L50 | 2/1/0/3/3 | **是** —— `edges-D3.jsonl:49`（src=L50，part-of）evidence 逐字含 `(u_0,u_0)=\int_0^{2\pi}dx=2\pi` | **T2 塌陷** |

计数：**六条指控中 T2 塌陷 2 条（D-5、D-6，均为分片自列的最轻两条）、T1 层成立 4 条（D-1/D-2/D-3/D-4，即分片自列最重四条）；T3 塌陷 0 条**。

T3 通道在本片几乎不存在：24 节点中只有 3 个有 part-of 后代（L49 后代 2 个、L60 后代 2 个、L63 后代 1 个），其余 21 个 `n_partof_desc = 0`，故「内容已下移到后代」这条过报通道（a）在本片基本不适用。

另与死因规则对齐：分片建议的 3 条死因 1（L51、L66、L69）**不因边证据而免除**（量词过度断言的规则如此），且经上表核实这三条本来也没有边证据可补；分片建议的 6 号候选（引文不支撑断言）在本片以 D-5、D-6 形式出现，二者恰好被 T2 塌陷。

## 顺带发现

- 分片对 L46/L47 的 E 型严重度确实可能高估：`exposure.tsv` 显示 L47 有 2 条 T2，但两条 evidence 与其 T1 完全相同（去重后源行仍为 1），故边并未补入新内容 —— 与分片 §9 第 6 条自标的「可能高估」方向相反，此处边救不了。
- L49 是本片唯一 T3 通道显著的节点（后代 2 个、T3 共 7 条、去重源行 8），若有其它审查员按 T1 判它射程不足，应先查 T3。
- 分片 §2 末尾说 L49 claim 3「已在 §8 批次 1 记录，不重复计入」，但其 §2 开头的「共 6 处」与 §5 的清单都未给 L49 一个独立编号，合并时若按 D-1…D-6 编号统计会漏掉这条 D/C 边缘个案。
- 分片全程未使用四层证据池、未查 `edges-D3.jsonl`（自认），故其「可引未引/第 3 槽空置」类指认在合并时**须一律先过 T2**，本摘要 §五 只补了六条形式 D 指控，E-中 14 处未逐条过池。
- 分片 §7 第 4 条提出的 Pythagorean 同名冲突需 D4/D5 交叉核，本摘要范围外，未核。
