# 综合用 · 70 份提案摘要对照表

本表由 `tmp_extract_summaries.py` 机械抽取，**不是**提案原文的替代品。
每份只取其开头摘要小节，截断到 2600 字符。
需要判某条张力时单点回查 `report/待应用-提案/<文件名>`。

---

## A1-互证塌缩检查.md

*抽自小节：## 0 摘要；原文 8247 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**，未写任何 `data/` 文件。所有改动由主控落盘。

| 项 | 结果 |
|---|---|
| `audit-B2-verdicts.jsonl` 中 `verdict=="KILL"` | 29 条 |
| 减去 `待应用清单.md` §1 的 9 个争议节点 | **22 条无冲突 KILL**，id 全部在 §1 回显 |
| 22 条在数据中全部找到（防按名字错杀） | 是，0 个 id 落空 |
| 已撤出（形式 B 真漂移，重挂锚不得杀） | 1 条 `d:for-function-spaces-closure-is-the-only-real-content` |
| **本批实际待杀** | **21 条** |
| 内容轴塌缩对（reason 点名了同批被杀节点） | **3 对**，与 `待应用清单.md` §3 表格逐条对上，无新增无遗漏 |
| **锚点轴塌缩对（新发现，§3 机械检查看不见）** | **1 对**，比内容轴那 3 对更严重 |
| 存活节点 `parent` 字段指向待删节点 | **0 个** |
| `structures-L2-{a,b,c}.jsonl` 的 `members` 含待删 id | **0 处**（原文逐行扫描，不靠字段解析） |
| 待删 id 出现在任何 structures 文件的任何字段 | **0 处** |
| 引文从全图彻底消失 | **7 条**（§4），分 3 类处置 |
| B2 `reason` 须重写 | **4 条**（§5），含 1 条伪造引文 |

**边计数须分两种口径**（前一版本此处只记了一种，易被读成错数）：

| 口径 | 删除节点数 | 进墓地的边 | A 存活者为 src | B 存活者为 dst | C 两端皆删 |
|---|---|---|---|---|---|
| 仅本批 21 条 | 21 | 71 | **24** | 44 | 3 |
| 本批 21 + 维持的 3 条 KILL | 24 | 82 | **26** | 53 | 3 |

只有 A 类需要处置（存活节点失去一个 requires/contrasts 目标）。B 类是被杀节点自己发出的边，
随节点走，存活端不受损。C 类两端同死。

**执行前置条件（强制）**：§2 的 4 组塌缩必须先裁断、§3.2 的 6 条重指必须先做完，
才可调用 `apply_kills.py`。`apply_kills.py` 把边送进墓地但**不重指**，裸跑会静默丢内容。

## A10-批次分层信号.md

*抽自小节：## 一、信号的可计算定义；原文 17111 字符*

**前言**：**结论先说**：不能替代，但能**互补**，而且互补后的召回率是实测可证的提升。

- 「批次内引文共享」（同一 `nodes-*.jsonl` 内多个节点引同一句）**强预示**形式 B 缺陷；
  「跨批次引文共享」**反向预示**——批次越多，缺陷率越低，单调下降到 0。
- 但批次内共享**仍然是共享检测的子筛**，它只能给共享清单重排序，**救不回 238 个盲区节点**。
- 真正能替代的那一半不是「共享」而是**「批次的源材料类型」**：
  锚点全部落在 `*-exercises.md` 的节点（49 个，四个习题文件实测零答案），
  其诊断**结构性地不可能有原文支撑**。这条规则单独就抓到了共享检测完全静默的整批 CX2（15/15）。
- **与「缺 origin」合并要用析取，不能用合取。**用合取会把召回从 20/25 打到 5/25——
  因为 CX2 那 15 个**已经如实标了 `origin_note`**（实测 15/15），
  合取规则会把唯一的真逃逸批次排除掉。这是本次最反直觉的实测结果。

本文所有数字都是实测（脚本 `tmp_a10_1.py` … `tmp_a10_6.py`，已删）。
图谱 sanity check 通过：节点 522、边 1433、节点锚点 921。

「批次」= 节点所在的 `data/nodes-*.jsonl` 文件名去掉前后缀，共 14 个：
`D1`(81) `D2`(80) `D3`(69) `A1`(63) `S1`(62) `D4`(54) `A2-x2`(34) `X`(22)
`CX2`(15) `A2-s1`(11) `H2`(11) `A2-s2`(10) `A2-s3`(7) `D5`(3)。
（`structures-L2-*` 无锚点，不参与本信号。）

#### 1.1 三个判据，逐条给出计算式

设 `cites[(file, quote)]` = 引用该 (文件, 引文) 对的**节点**集合（**边的 evidence 不计入**，
理由见 §1.2）。对每个节点 `v`，令 `B(v)` 为它的批次。

**判据 S1（批次内共享度）**
```
intra(v) = max over groups g containing v of  |{ u in g : B(u) == B(v) }|
```
即：在 v 参与的所有共享组里，**与 v 同批次**的引用者数目的最大值。
`intra(v) == 1` 表示 v 的每条锚点在自己批次内都无人共用。

**判据 S1'（跨批次广度，反向指标）**
```
cross(v) = max over groups g containing v of  |{ B(u) : u in g }|
```
`cross(v) == 1` 表示 v 所在的最大共享组**完全落在一个批次内**。

**判据 S2（源材料类型，与共享无关）**
```
exonly(v) = (v.anchors 非空) and (v 的每一条 anchor 的 file 都匹配 *-exercises.md)
```

**判据 S3（未披露模型内容）**
```
nodecl(v) = ('origin' not in v) and ('origin_note' not in v)
```

#### 1.2 为什么 evidence 不计入分组

`check_shared_quotes.py` 把边的 evidence 与节点 anchors 混在同一个计数里（源码 :68–81），
`--min` 因此同时受两者影响。这与四层证据池的 TD 排除原则冲突：
边的 evidence 是**边**的记账，不是 dst 节点的引用。
实测差异不小：含边时不同 `(file, quote)` 对 875 个、总引用 1852；
只算节点锚点时 708 对、921 次引用。
`report/_形式B-共享引文候选.md` 头部写的「708 对」证明**那份清单实际是按节点锚点算的**，
即它跑的是 nodes-only。本提案沿用 nodes-only，以便与已标注的金标准可比。

---

## A3-零证据节点锚点候选.md

*抽自小节：## §0 现状核实；原文 21780 字符*

**前言**：只读分析代理产出。**本文是提案,不是执行结果。** 未改动 `data/` 下任何文件。

对象:`data/nodes-D2.jsonl` 第 42、54、72、73 行(全部 `origin: "model"`)。
上游:`report/待应用清单.md` §4.2。

回读 `data/nodes-D2.jsonl` 对应行,四个节点**确实都是 `"anchors": []`**,§4.2 的记录准确。

同时对全图做了无锚点普查(遍历 `data/nodes-*.jsonl` + `data/inherited/nodes-*.jsonl`,
共 522 个节点,不含 `structures-L2-*.jsonl`):

| 文件:行 | id | origin | type |
| --- | --- | --- | --- |
| nodes-D2.jsonl:42 | `d:exponential-independence-uses-no-inner-product` | model | concept |
| nodes-D2.jsonl:54 | `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets` | model | method |
| nodes-D2.jsonl:72 | `d:thm-15-7b-argument-adjoining-an-outside-element-breaks-the-count` | model | method |
| nodes-D2.jsonl:73 | `d:adjoining-an-element-outside-the-span-preserves-independence` | model | theorem |

**全图无锚点节点恰好就是这 4 个,没有第五个。** 也就是说本提案覆盖了该缺陷类的全部实例。

触及四节点的边(共 12 条,全部 `evidence` 为空):

| 边 | 角色 | 关系 |
| --- | --- | --- |
| edges-D2.jsonl:45 | :42 为 src | requires → `d:exponential-independence-limit-argument-isolates-one-coefficient` |
| edges-D2.jsonl:46 | :42 为 src | requires → `d:exponential-law-used-in-the-shift` |
| edges-D2.jsonl:154 | :42 为 src | part-of → `apostol:independence-of-exponential-functions` |
| edges-D2.jsonl:64 | :54 为 src | requires → `apostol:theorem-15-5` |
| edges-D2.jsonl:65 | :54 为 src | requires → `d:infinite-dimensional-operational-criterion-no-finite-spanning-set` |
| edges-D2.jsonl:166 | :54 为 src | part-of → `apostol:infinite-dimensional-space` |
| edges-D2.jsonl:93 | :72 为 src | part-of → `d:thm-15-7b-n-independent-elements-form-a-basis` |
| edges-D2.jsonl:96 | :72 为 src | requires → `d:n-plus-one-elements-in-an-n-dimensional-space-are-dependent` |
| edges-D2.jsonl:97 | :73 为 src | part-of → `d:thm-15-7b-argument-adjoining-an-outside-element-breaks-the-count` |
| edges-D2.jsonl:98 | :73 为 src | requires → `apostol:linear-span` |
| edges-D2.jsonl:99 | :73 为 src | requires → `d:independence-vanishing-combination-forces-vanishing-coefficients` |
| edges-D2.jsonl:90 | :73 为 **dst** | `d:thm-15-7a-independent-sets-extend-to-bases` requires → :73 |

按 §4.6 四层证据池:四个节点 T1 空;T2 全空(11 条自发边无一带 evidence);
T3 —— :42 与 :54 无 part-of 后代,池为空;:72 有一个后代(:73)但 :73 自己也空。
edges-D2.jsonl:90 是 TD,按 §4.6 **必须排除**,不得用来给 :73 补分。
**所以「引文支撑为零」这个描述在四层证据池的意义下是准确的,不是 T2/T3 塌陷造成的过报。**

## A4-形式B漂移与借用锚点.md

*抽自小节：## §0 校验方法与口径；原文 25745 字符*

**前言**：本文件是**提案**，不是执行结果。全部改写由主控自行落到 `data/` 下。本代理为只读代理，未写入 `data/` 下任何文件。

范围：`report/待应用清单.md` §6.1 的 3 条真漂移（死因 6）、§6.2 的 7 条借用锚点（死因 0，只摘锚点不动节点）。
另附 1 条本代理自行发现的**疑似**第 8 条借用锚点，标为 PLAUSIBLE，与被点名的 7 条严格分开。

采用的锚点判准（沿用 `report/审查-形式B-共享引文.md` 第 757–760 行自述的标准）：
锚点须支撑该节点的**主语**（在谈哪个对象）与**核心断言**；围绕核心断言的分析性展开视为 commentary，不单独要求锚点，但若分析中含可核的事实性主张，该主张须为真。

锚点 SPEC 复述（下文每条候选都按此逐项报告）：1–3 条；`quote` 须是 `file` 的逐字连续子串；30–200 字符；不跨行；不改标点、不省略号、不修 OCR 噪声。

字符数一律由本代理用 `len()` 实测，不沿用审查稿数字；`in_text` 与命中行号亦实测。

**四层证据池。** 判断一条死因 6 指控是否成立，取以下三层为权威：T1 节点自身 anchors；T2 以该节点为 `src` 的边上的 evidence；T3 part-of 后代的 anchors 及后代发出的边。**TD（指向该节点的边）被排除**：下游消费者引了某行，不等于上游节点引了某行。T2 塌缩是记账位置问题，T3 塌缩是内容合法下移到子节点，两者补法不同，混为一谈会把修复引错方向。死因 1 对边证据免疫，死因 6 不免疫。

**冗余度口径。** 衡量替换锚点的收益，看它新引入的**源文句子**，不看"池子变大了"。把节点已经引过的行再引一遍，救不了任何断言。

**不用行号做覆盖比较。** 一行可以装好几句（15.10.md:119 是 460 字符 4 句；15.03.md:25 是 360 字符 4 句），行号比较无意义。

**机械信号只定向、不定论。** `check_unanchored_numbers.py` 召回 1/4；`check_shared_quotes.py` 对 238/522 = 46% 的节点是盲的。

**关于 `tools/verify_anchors.py` 的验证力（读源码第 40–80 行确认，未执行该脚本）：** 无锚节点被追加进 `nodes_without_anchor` 后 `continue`，**永不计入 failures**；唯一的检查是 `elif quote and quote in text`，其中 `text` 是**整份文件**，因此 30–200 长度规则与不跨行规则**从未被机器检查过**；`return 1 if failures or bad_json else 0`。所以验收必须去读 stdout 里 `nodes with NO anchor` 那一行，不能只看退出码。

**「N/N 锚点已验证」不得作为正确性保证汇报。** 本文件所有"已实测"仅指：逐字子串成立、长度落在 30–200、不跨行、命中行号唯一。它不保证引文支撑它所挂的断言——后者只能靠逐条人读。

**序列化。** `json.dumps(o, ensure_ascii=False)` 能逐字复现原 JSONL 行（已对 nodes-D4.jsonl:15、nodes-A2-x2.jsonl:2、nodes-D1.jsonl:69 三处校验过），故下文给出的"逐字最终文本"可直接落盘。

**边记录的 `evidence` 是单个对象（dict），不是数组。** 本代理第一版持有者普查按数组解析，漏计了边持有者，已重跑修正；下文所有边持有者数字来自修正后的普查。

## A5-CX2与origin.md

*抽自小节：## §0 结论摘要（先看这一段）；原文 43978 字符*

**前言**：只读代理产出的**提案**，不是执行结果。主控自行改数据。
本文件不写 `data/` 下任何内容；核验用的一次性脚本 `tmp_a5_verify.py` 已在收尾时删除。

1. **20 个节点全部 KEEP，登记形式 E（有子句完全无锚），零 KILL。** 与 §六 6.3 的裁定一致。
2. **锚点核验：41 条锚点全部逐字通过**，`occurrences` 均为 1（无多处歧义命中），
   换行 0 条，实测长度 min 34 / max 167，全部落在 SPEC 的 30–200 区间内。**0 违规。**
3. **锚点确实只覆盖题面**，逐条回源比对已确认（按字符偏移定位，不按行号）。
   41 条锚点里没有一条含失效诊断、见证元素或数值。三份源文件里**不存在**任何答案文本
   （见 §2.3 的强化核验：不只 grep `answer`，还扫了 8 个解答标志词，唯一命中是
   15.05 第 17 题的 "All **solutions** of a linear second-order..."，是题面用词，不是答案）。
4. **字段方案：推荐形态 (a)，即节点带 `origin`。理由是它已经不是待议项——**
   `origin` 已在 33 个节点上实际使用（D1 1 / D2 8 / D3 4 / D4 9 / H2 11），
   `origin_note` 已在 39 个节点上使用，且 `tools/export_obsidian.py` **已经实现了两者的渲染**
   （`:183-184` 写进 frontmatter，`:229-230` 渲染成「## 来源说明」章节）。
   详见 §3——这一项**不是新增 SPEC 能力，而是补写 SPEC 已落后于实现的文档**。
5. 改动量：**15 个 CX2 节点各加 1 个字段**（`origin`，`origin_note` 已有，不动）；
   **5 个 15.12 节点各加 2 个字段**（`origin` + 新写 `origin_note`）。逐字文本见 §5。
6. 未做完的：见 §7。有 1 项须主控裁断（`apostol:cx-inner-product-axiom-diagnosis`
   是否一并处理，它是习题 1 的伞节点但不在本简报点名的 5 个之内）。

## A6-两处数学缺陷.md

*抽自小节：## 〇 结论速览；原文 13774 字符*

**前言**：只读代理产出，**这是提案，未写入 data/**。两处缺陷主控已核实，本文件不复核其存在性，
只给「错在哪一步的数学」+ 逐字改写文本 + 逐条断言的锚点候选（字符数全部实测）。

所有锚点候选均以 raw string 做 `quote in text` 逐字子串测试（脚本 `tmp_a6.py` / `tmp_a6b.py`，
已删除），并额外测了：出现次数、是否跨行、全库 `(file, quote)` 对的已有使用者。

| # | 节点 | 位置 | 缺陷类型 | 改写后锚点数 | 需新增锚点 |
|---|---|---|---|---|---|
| 1 | `d:exponential-independence-select-the-largest-exponent` | `data/nodes-D2.jsonl:36` | 严格性误归因（+ 一处符号词误用） | 2 | 1 条（15.07.md L39，78 字符） |
| 2 | `d:pythagorean-split-of-the-approximation-error` | `data/nodes-D4.jsonl:51` | 范数 / 范数平方混同（+ 一处过度断言） | 3 | 1 条（15.15.md L11，82 字符） |

两处都**只改 `statement`**。`id`、`name_en`、`name_zh`、`node_type`、`sections`、`parent`、
`atomic`、`atomic_reason` 一律不动，边一条不动（理由见各节「连带项」）。

## A7-H层整合.md

*抽自小节：## 0. 一句话结论；原文 633 字符*

**前言**：只读分析代理产出。**本文件是提案，未改动 data/ 下任何文件。**

检查对象：`data/edges-H1.jsonl`（15 条）、`data/nodes-H2.jsonl`（11 个）、`data/edges-H2.jsonl`（19 条），全部逐条，无抽样。

H1/H2 的 schema 与引文机械合规性**全部通过**（31 条边引文逐字命中、11 个节点锚点逐字命中、字符数实测全部落在 30–200、无跨行、无悬空 id、无对称关系反向重复边）。整合的争点不在合规性，在**信息增益**与**已在前序审计中被发现的 4 处断言失真**。本提案给出：直接并入 9 项、改后并入 5 项、建议丢弃 20 项，另报告 3 处两份前序审计都漏掉的问题。

**重要前提校正**：H1/H2 已经在主图里。`data/` 全量点数 = 节点 522（含 nodes-H2 的 11 个）、边 1433（含 edges-H1 的 15 + edges-H2 的 19），与简报给的图谱状态完全吻合。所以本提案不是"把 H 层并进去"，而是"H 层已在图里，哪些该留、哪些该改、哪些该撤"。

<!--SKELETON: §1 机械核验 / §2 H1 逐条 / §3 H2 节点逐条 / §4 H2 边逐条 / §5 整合提案 / §6 两份审计都漏的 / §7 未做的事-->

## A8-完备性评审.md

*抽自小节：## 零 三条前提的核验结果（其中一条不成立）；原文 530 字符*

**前言**：只读代理产出。本文件是**缺口清单 + 提案**，不含任何数据写入。全部 16 节走完，无静默截断。

统计基线（本轮实测，与简报所给一致）：
- `data/nodes-*.jsonl` + `data/inherited/nodes-*.jsonl` = **522** 个节点；
  `data/structures-L2-*.jsonl` = **24** 个 L2 结构节点。两者合计 546 条记录。
- 边 1433 条。
- 简报里的"节点 522"未计入 24 个 L2 结构节点。下文所有覆盖率按 546 计，并在需要时分开报。

（正文见 §0）

---


#### 一 逐节覆盖缺口（15.01 … 15.16）

（正文见 §1.1 … §1.16）

---

## A9-记账错更正.md

*抽自小节：## 摘要（三处更正的实际结论）；原文 12411 字符*

**前言**：只读代理产出，**提案不是执行结果**。本文件不改任何 data/ 文件。

简报给我的三条待更正里，**只有第 2 条与我实测一致**。第 1 条与第 3 条经回源后结论都要改写：

| # | 简报的预设 | 我实测后的结论 |
|---|---|---|
| 1 | D2 片给 L66 的建议锚点「标为 100 字符，实际是 89 字符」 | **分片的 100 字符是对的**（实测 100，逐字子串）。89 是 `edges-D2.jsonl:85` 边引文那个**另一条**子串的长度。要更正的是主控核验文件，不是分片。 |
| 2 | D3 片把 L31 的 `:125` 列为无锚，而节点锚点就是 `:125` | **成立**，实测确认。更正对象是分片报告的表格行。 |
| 3 | `apostol:zero-element`：sections 补 15.03，死因 6 → 0 | **6 → 0 不成立。**支撑它的那条引文只走 TD（该节点是 `dst`），按四层定义 TD 不入池。cause 6 **维持**，且原审查员 A5 给的 fix（补 sections **并** 补第三条锚点）本来就是对的，主控只采了一半。 |

三条的过报通道 (c) 本身（一源行两半分落节点锚点与边）**不受影响，仍然成立**——错的只是主控写这条时引的字符数配错了对象。

---

## G1a-悬空边清算.md

*抽自小节：## 〇 结论速览；原文 11879 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**，未写任何 `data/` 文件。
口径全部取自 `tmp_index/edges.tsv`（1433 行）、`nodes.tsv`、`partof.tsv`、`shared-quotes.tsv`，未重扫 `data/`。

**22 个候选共触及 75 条边：48 条它们作 src（随节点走，不悬空）、24 条存活节点作 src 而 dst 将死（=悬空边）、3 条两端皆死。24 条悬空边里 18 条建议一并删除、6 条必须改接到 `apostol:function-space`；无一条需要阻止 KILL，但有 1 项必须降级为「先改接后杀」的强制前置。**

| 项 | 数 | 说明 |
|---|---|---|
| 22 个 id 作 **src** 的边（含 22 条 part-of 自身） | 48 | B 类，随节点进墓地，存活端不受损 |
| **悬空边**（存活 src → 待删 dst） | **24** | 本文件唯一需处置的对象 |
| 两端皆死（C 类） | 3 | D1:118 / D1:125 / D1:130，无需处置 |
| 悬空边处置：一并删除 | 18 | 全部为「已被 part-of 或存活兄弟覆盖」的冗余 requires/contrasts |
| 悬空边处置：改接到父节点 | **6** | 例 7 / 例 8 / 例 12 各 2 条，新 dst 均为 `apostol:function-space` |
| 因此必须**阻止**的 KILL | **0** | — |
| 必须**降级为「先改接后杀」**的 KILL | **2** | `d:function-space-addition-is-pointwise`、`d:function-space-scalar-multiple-is-pointwise`；裸跑 `apply_kills.py` 会静默丢这 6 条 |

口径与 A1 §0 的差异：A1 记「本批 21 条 → A 类存活者为 src = 24」。本文件对 22 个候选（含 A1 已撤出的 #16）逐 id 数，得同一个 24 —— 因为 #16 `d:for-function-spaces-closure-is-the-only-real-content` 的 dst 边数为 **0**（`edges.tsv` 无任何行以它为 dst），撤不撤出都不改变悬空边总数。这一条可独立执行或独立撤回，与其余 21 条无边耦合。

死因编号：本节所有处置均为 **0（无死因、仅需 FIX/改接）** —— 悬空边本身不是内容错误，是删除操作的副作用。

## G1b-唯一载体检查.md

*抽自小节：## 〇 结论速览；原文 13723 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**，未写任何 `data/` 文件。

口径声明：本节所有数字按 A1 §0 的 **21 条实际待杀**（22 候选减去已撤出的
`d:for-function-spaces-closure-is-the-only-real-content`）计算。另按「21 + 维持的 3 条 = 24 条」
重算过一遍，**结果完全相同**（见 §一 末尾的 K24 复核），所以本轮结论不依赖两种口径的选择。

| 项 | 结果 |
|---|---|
| 22 个候选持有的锚点总数 | 27 条（`anchors.tsv` 按 id 计数；16 个候选各 1 条、6 个各 2 条） |
| 逐字同 (file,quote) 仍有存活持有者 | 15 条（可安全丢弃） |
| 逐字持有者全灭 | **12 条** |
| ├ 被存活侧更长引文完整含住（CONTAINED） | 3 条 |
| ├ 仅差末尾句点即被含住（NEAR） | 1 条 |
| └ **彻底掉出图谱** | **8 条**（其中 1 条为部分损失，见下） |
| 彻底掉出的 8 条涉及的候选 id 数 | **6 个** |
| 因此再无任何持有者的**源行数** | **4 行**（全在 `15.12-exercises.md`） |
| 另有：只挂在将死边上、无锚点对应的引文 | **1 条**（`edges-D1.jsonl:219`，76 字符） |

**与 A1 §2.2 的关系**：A1 报「6 节点、9 条引文 LOST」。本轮独立复算得到的**节点数一致（6 个）**，
条数也是 9，但**构成不同**：本轮把 8 条判给节点锚点、第 9 条判给一条将死边上的独立引文
（`By unifying diverse examples in this way we gain a deeper insight into each.`，76 字符，
`15.03.md`，唯一持有者 `edges-D1.jsonl:219`，该边 src 是候选 `d:knowledge-of-one-example-guides-work-in-the-others`）。
并且本轮认定 8 条里有 1 条只是**部分损失**（`d:operations-of-example-2-...`，见 §二）。
差异不构成矛盾，但两份提案的「9 条」不是同一个 9 条，综合裁决时须按 id 逐条对齐，不要相加。

死因编号：本节全部为 **0（无死因、仅需 FIX 或附加重挂动作）**——唯一载体是**执行安全问题**，
不是节点自身的质量缺陷，不新增死因。

## G1c-B2互证理由改写.md

*抽自小节：## 〇 结论速览（3 条里成功改写几条、撤回几条）；原文 11473 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**，未写任何 `data/` 文件。
范围严格限定在 A1 §2.1 已认定的 3 对内容轴塌缩，不重审其他节点、不重算已有表。

**3 条全部改写成功，0 条撤回。** 三条 KILL 的死因 5（伪抽象·层塌缩）判定本身不依赖被杀节点，
被杀节点只出现在「承载者指认」与「冗余度陈述」两处措辞里，都能改指到存活节点。

| 对 | 被杀节点 | 原 reason 依赖的被杀节点 | 依赖性质 | 改写后的承载者（全部存活） | 结论 |
|---|---|---|---|---|---|
| A | `d:example-1-verification-is-the-field-axioms-of-r`（v:46，属「维持 3」） | `d:example-3-verification-reduces-to-arithmetic-in-each-component` | 先例（同 species 类比） | parent `d:example-1-the-real-numbers-form-a-linear-space`（KEEP） | 改写成功，死因仍 5 |
| B | `d:example-2-shows-the-scalars-decide-real-or-complex`（v:49） | `d:real-in-real-linear-space-refers-to-the-scalars`（v:25） | 冗余度陈述（三份拷贝计数） | parent `d:example-2-the-complex-numbers-with-real-scalars`（KEEP） | 改写成功，死因仍 5 |
| C | `d:example-3-verification-reduces-to-arithmetic-in-each-component`（v:52） | `d:operations-of-example-3-are-componentwise`（v:51） | **承载**（机制句的内容在 v:51 里） | parent `d:example-3-v-n-with-componentwise-operations`（KEEP）+ 祖父 `apostol:v-n-space`（不在击杀名单） | 改写成功，死因仍 5；另附死因 6 观察 |

只有 C 是真正的承载依赖，也只有 C 需要把承载者上移一层；A、B 两条的依赖是措辞层面的，
删掉引用即可，判定不受影响。三条改写用到的证据全部来自各自节点池的 T1/T2 与存活节点的 T1/T2/T3，
未动用 `pool-TD-excluded.tsv` 的任何一行。

**改写不改变 A1 §2.1 的裁定结果**（B 假阳性、C 可杀但承载者上移、A 须先并入 parent），
本文件只提供三条可直接落盘的 reason 全文。

## G1d-安全击杀清单.md

*抽自小节：## 〇 结论速览（最终 KILL n 条 / 降级 FIX m 条 / 撤回 k 条，合计仍是 22）；原文 15016 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**，未写任何 `data/` 文件。
输入：`G1a-悬空边清算.md`、`G1b-唯一载体检查.md`、`G1c-B2互证理由改写.md`（三份均存在，无缺件）
＋ `A1-互证塌缩检查.md` §1 的 22 条候选与 §2 的互证认定。
装配规则优先级按简报：G1c 撤回优先 → G1b 内容损失降级 → G1a 悬空边排序 → 互证对子拆批。

本清单只做装配与执行安全排序，**不重审任何 KILL 的死因判定**。

**最终 KILL 19 条 / 降级 FIX 2 条 / 撤回 1 条 = 22 条，账面闭合。**

| 处置 | 条数 | id |
|---|---|---|
| **KILL** | **19** | A1 §1 表中的 #1–#15、#17、#18、#19、#21（逐条见 §一 步骤 4） |
| **降级 FIX** | **2** | `apostol:cx-law-of-cosines-needs-nonzero-elements`、`apostol:cx-absolute-value-and-product-of-integrals-on-polynomials` |
| **撤回** | **1** | `d:for-function-spaces-closure-is-the-only-real-content`（A1 已撤出，本清单确认） |

三条输入的贡献：

| 规则 | 输入 | 本清单的动作 |
|---|---|---|
| 1 G1c 撤回 → 移出清单 | G1c 报「3 条全部改写成功，0 条撤回」 | **无条目移出**。G1c 的作用是解开互证依赖（见 §四），不减 KILL |
| 2 G1b 内容彻底掉出 → 降级 FIX | G1b §三 第 1 档 2 个候选 | **2 条降级**（见 §二） |
| 2' G1b 第 2 档 → 保持 KILL，重挂为强制前置 | 4 个候选、5 条引文 | 并入 §一 步骤 1，**不降级** |
| 3 G1a 悬空边排序 | 24 条悬空边（18 删 + 6 改接） | 因 #22 降级，**改接降为 5 条、删除仍 18 条**；顺序=先改接再删节点（§一） |
| 4 互证对子拆批 | A1 §2.1 内容轴 3 对、§2.2 + G1b 锚点轴 2 对 | **不需拆批**，两对可同批同死，理由见 §四 |

**边的账（19 条 KILL 口径，取自 `tmp_index/edges.tsv` 逐 id 重数）**：

| 类 | 条数 | 处置 |
|---|---|---|
| 19 个待删 id 作 src（含各自的 part-of） | **38** | 随节点进墓地，存活端不受损 |
| 悬空边（存活 src → 待删 dst） | **23** | 5 改接 + 18 删除 |
| 两端皆死 | **3** | `edges-D1.jsonl:118`/`:125`/`:130`，随节点走 |

悬空边由 G1a 的 24 降为 23：`edges-A2-x2.jsonl:50` 的 dst 是降级为 FIX 的 #22，该节点不删，
**这条边不再是悬空边，保持原样、不改接、不删除**。这是本次装配对 G1a 的唯一数值修正。

## G2a-9节点合并复核.md

*抽自小节：## 〇 结论速览（5→3 是否成立；若不成立，应是 5→几）；原文 10146 字符*

**前言**：范围：只复核 `待应用清单.md` §一 的「维持的 KILL 从 5 降到 3」这一件事。
被放掉的 2 条逐条复核，维持的 3 条只查互证塌缩。范围外的一律不动。

**5→3 不成立。应为 5→4。**

| 被放掉的节点 | 放掉理由 | 复核结论 | 最终建议 |
|---|---|---|---|
| `d:ten-axioms-listed-in-three-groups` | 三个 L1 分组节点会失去**唯一**汇聚父节点，裸删=层塌缩 | **理由的事实前提为假**：三个分组节点各自另有一条 `part-of --> apostol:linear-space` 的边，已在图里，删本节点不产生任何孤儿 | **恢复 KILL**（死因 5），执行时把 `edges-D1.jsonl:10/11/12` 一并送墓地 |
| `x:least-squares-is-a-projection` | ①2 个 L1 成员在 X 层别无落点、5 条入边悬空（ADJ2）②「不可解系统」这一对齐全图无第二承担者（ADJ1） | 理由①**无效**（记账问题，且 5 条入边是 TD，不入池）；理由②**成立且机械可证**：全图 546 节点正则搜 unsolvable/no solution/salvage，本节点外仅 1 处命中且语境无关 | **维持放掉（FIX 不删）**，但须按 G2d F2 修死因 6 |

一句话：**两条放行里一条靠假前提、一条靠真证据。** 5 条维持 KILL 中应恢复 1 条。

死因 1 的特例检查（简报要求）：**两条原指控都是死因 5，不是死因 1**
（`audit-B2-verdicts.jsonl` 逐字："cause": 5 各一条）。故「死因 1 不因边证据免除」这条规则
在本轮**不触发**，两条放行理由都不能靠这条规则一键否掉，必须逐条查前提。这一点如实登记。

## G2b-十条公理FIX.md

*抽自小节：## 〇 结论速览（改什么字段 / 锚点几条 / 实测字符数）；原文 13197 字符*

**判决**：`d:ten-axioms-listed-in-three-groups` 由 KILL 改 **FIX**，节点保留在
`data/nodes-D1.jsonl:5`（就地替换整行）。**不动 data/，本文件只出提案。**

改动只涉及三个字段，其余字段逐字不变（`id` / `name_en` / `name_zh` / `node_type` /
`sections` / `aliases` / `parent` / `atomic` 全部原样）：

| 字段 | 改动 | 理由 |
| --- | --- | --- |
| `statement` | 补出各组的公理编号：`(Axioms 1 and 2)` / `(Axioms 3 to 6)` / `(Axioms 7 to 10)`，实测 **225 字符** | 让 2/4/4 这个分配可逐条对上三个 L1 分组节点的 statement，把「两/四/四」从模型的计数改成教材编号的转写 |
| `anchors` | 由 2 条改 **3 条**；撤下 `15.01.md` 的过渡句，新增 2 条 `15.02.md` 引文 | 原第 2 条锚点（`We turn now to a detailed description of these axioms.`）不支撑 statement 的任何一句，是死因 6 的 E 型；新增两条把「三组」的组名与「十条」的编号上界落到原文 |
| `atomic_reason` / `audit_fix` | 重写 / 追加 | 原 `atomic_reason` 把「分组三节点已在 L1 层存在」写成了 B2 用来判死的把柄；改写成它真正的保留理由（唯一汇聚点）。`audit_fix` 追加本轮来源，保留原有 `audit-A4a` |

**锚点 3 条，实测字符数 163 / 175 / 36**，全部在 `15.02.md`，全部 30–200 界内、不含换行、
`grep -F` 逐条命中（见第四节）。**公理 2 的候选锚点我独立实测为 175 字符**（`awk` 行长与
Python `len()` 两法一致，`tmp_index/anchors.tsv:` 与 `shared-quotes.tsv:39` 的 charlen 列亦为 175），
确认任务书给出的 175 而非 170 是对的。

**边不动。** ADJ2 的前置条件（`data/edges-D1.jsonl:10/11/12` 三条 `part-of` 改指
`apostol:linear-space`）是**为 KILL 服务的**；既然改判 FIX、节点不删，这三条边保持原指向即可，
不需要任何改边动作。这一点与 待应用清单.md §2.1 的推理方向一致（它把改边写成「再删」的前置），
但结论上须明确：**FIX 之后不执行那三条改指**。

## G2c-例子节点重写.md

*抽自小节：## 〇 结论速览（原 statement 几句 / 改写后几句 / 锚点几条 / 是否仍有无锚句）；原文 15612 字符*

| 项 | 原 | 改写后 |
| --- | --- | --- |
| statement 句数 | 2 句（含 6 个断言子句 C5.1–C5.6） | 2 句（含 4 个断言子句 N1–N4） |
| statement 字符数 | 437（实测） | 405（实测） |
| 锚点条数 | 1（138 字符） | 3（138 / 160 / 146 字符，全部 30–200 合规） |
| 是否仍有无锚句 | 有：C5.2、C5.3 全无锚（E 型），C5.4–C5.6 全无锚 | **无 E 型**。4 个子句全部落在锚点射程内 |
| 死因处置 | 死因 1（量词过度断言，A4a 判）+ ADJ3 查出的 C5.3 为假 | 两者均消除：删掉"must"必要性断言与"which is why"因果断言 |
| 建议动作 | **FIX（改写 + 补 2 条锚点），节点保留** | 置信度 0.85 |

节点存活理由与主控裁断一致（待应用清单 §2.2）：ADJ1 认为末句普查可复得、ADJ2 认为不可复得，主控查明例 5–12 无运算节点，第三桶确实无处可复得。但**本提案的做法与 §2.2 的"保留末句普查表"有一处偏离**：主控要求"删掉可复得的前两句、保留末句普查表"，而我把普查表的三桶枚举也删了，只留第三桶（函数空间前言）并给它**上了两条锚点**。理由见 §二末段与 §五。

主要变化：
1. 首句由"one **must** supply three data"（必要性）改为"is enough to produce"（充分性），与锚点 1 的 `If we specify … we get` 方向一致 —— 消除 ADJ3 的 D-2 射程错配。
2. 删 `Omitting either rule leaves the axioms with nothing to speak about`（C5.2，真但全章无锚，E 型）。
3. 删 `which is why the section fixes the operations before the sets`（C5.3，ADJ3 已证**为假**，例 1–4 反驳）。
4. 删例 1–3、例 4 两桶枚举（C5.4/C5.5，可在 `d:operations-of-example-1..4-*` 四节点复得，且借锚会新造 B 型）。
5. 保留第三桶（例 5–12 共用前言），并把该桶从"无锚断言"变成"两条锚点直接支撑"：新增锚点 1（前言的加法定义，160 字符）与锚点 2（前言的数乘定义，146 字符）。

## G2d-裁定员缺陷.md

*抽自小节：## 〇 结论速览（共 14 条：成立 14 / 不成立 0 / 待证 0）；原文 19936 字符*

**前言**：范围：仅处理《待应用清单》§五「裁定员另外查出、须单独处理的正确性缺陷」。
判定手段：tmp_index 各表、源文件 grep、三份裁定员原文。本轮只读，未改 data/。

14 条全部成立，但**严重性分布与 §五 的排序不同**。用四层证据池逐条复核后，
只有 3 条是真的「证据缺口」，其余是记账位置、审计文本错误或统计口径问题。

按必修优先级重排（与 §五 的行序无关）：

| 级别 | 条目 | 理由 |
| --- | --- | --- |
| **必修·真缺口** | D2（least-squares 射程不足） | 池 T1=2 / **T2=0 / T3=0**，后代数 0。断言在池里**没有任何**证据。唯一的真死因 6 |
| **必修·会引入新错** | D1（A4a 修复文本为假） | 一旦落盘就把真话改成假话；ADJ3 的改写文本已顺手解掉它（见 D11） |
| **必修·文本假** | D12 D13（两条假 claim） | 定稿文本已在 ADJ3 §3，可直接替换 |
| 必修·可执行 | D6 D7 D8 D10（B2 reason 须重写 3 处） | 结论不变，只改 reason 文本 |
| **降级** | D3（function-space 无锚从句） | 覆盖它的引文**已在该节点自己的 T2 池里**（`inherited/edges-A1.jsonl:18`）。记账位置问题，死因 **0** 而非 6。ADJ1 只看到 T3 路径，漏了 T2 |
| 记账·须改文不补证 | D4（十条映射无锚） | 池里只有 2 个不同源行，均无 15.02。**且教材从不陈述该映射**——属「缺理由不缺事实」，见第三节 |
| 阻塞·须先扩 schema | D5 | `node_type` 封闭四值，B2 想要的标签无合法取值 |
| 计数订正 | D9 D14 | D9 成立且**另有一处 ADJ2 未发现的同类计数错**；D14 的全部统计我独立复算一致 |
| 元数据 | D11 | 4 条定稿文本，我核了承重的两条，全真 |

一句话：**§五 里唯一「机器和人都补不了」的是 D2，唯一「不修就会造新错」的是 D1，
而被 ADJ1 与 D2 并列的 D3 其实不该修节点、只该挪个位置。**

## G2e-zero-element与两处数学缺陷.md

*抽自小节：## 一 apostol:zero-element；原文 13015 字符*

**前言**：本文只处理三条互不相干的小修，范围外不展开。所有引用的字符数取自
`tmp_index/anchors.tsv` / `edges.tsv` / `source-lines.tsv` 的 charlen 实测列。
本轮只读，不改 data/。

节点：`data/inherited/nodes-A1.jsonl:8`，type=concept，sections=`["15.02","15.04"]`，2 条锚点。
statement 两句，须分开判：

- 句 1「Axiom 5 postulates an element of V, denoted by O, with x + O = x for all x in V.」
- 句 2「In a function space the zero element is the function whose values are everywhere zero.」

#### 1.1 池清单（T1/T2/T3，取自 tmp_index/pool.tsv 该节点 7 行）

| 层 | via | file | charlen | quote |
| --- | --- | --- | --- | --- |
| T1 | 自身锚点 | 15.02.md | 85 | AXIOM 5. EXISTENCE OF ZERO ELEMENT. There is an element in V, denoted by O, such that |
| T1 | 自身锚点 | 15.04.md | 57 | Axiom 5 tells us that there is at least one zero element. |
| T2 | 自发边 `inherited/edges-A1.jsonl:4` | 15.02.md | 85 | （同上 85 字那条） |
| T3 | 后代锚 `d:axiom-5-states-the-zero-only-as-a-right-neutral-element` | 15.02.md | 36 | x + O = x \quad f o r a l l x i n V. |
| T3 | 同上后代锚 | 15.04.md | 88 | y_{2} + (x + y_{1}) = (y_{2} + x) + y_{1} = O + y_{1} = y_{1} + O = y_{1}. |
| T3 | 后代边 `edges-D1.jsonl:26` | 15.02.md | 36 | x + O = x \quad f o r a l l x i n V. |
| T3 | 后代边 `edges-D1.jsonl:27` | 15.02.md | 85 | （同上 85 字那条） |

`exposure.tsv`：后代数 1、T1=2、T2=1、T3=4、池总 7、去重后不同 (file,quote) **仅 4 条**。
池里出现的源文件只有 **15.02.md 与 15.04.md 两个**，没有任何一条 15.03.md 的证据。

#### 1.2 TD 清单（pool-TD-excluded.tsv 该节点 14 行，一律不入池）
TD 共 14 条，涉及 15.02 / 15.06 / 15.07 / 15.08 / 15.11 / 15.13 / 15.14 / 15.03 各文件。
与本次争议直接相关的一条：

- `data/inherited/edges-A1.jsonl:18`，evidence=15.03.md，charlen=66，
  quote「The zero element is the function whose values are everywhere zero.」

**上一轮「死因 6 → 0」的理由无效，本轮独立确认。** 该边上 `apostol:zero-element` 是 **dst**，
按四层证据池定义属 TD，**不入池**。它是句 2 唯一的字面对应物，却恰好是唯一不能用的那一类。
「图里有这条边」不构成句 2 的证据支撑，只说明记账位置错了（内容挂在指向该节点的边上，
而不在该节点自身或其后代上）。

#### 1.3 死因 6 判定：**成立，不归 0**

分句判定：

- **句 1 有支撑。** T1 的 85 字锚点给出 Axiom 5 的存在性与记号 O，但它在 “such that” 处截断，
  不含 `x + O = x` 本身；补上这一半的是 T3 后代锚（15.02.md，36 字，`x + O = x \quad for all x in V.`）。
  两条合起来覆盖句 1 全部内容。注意这属于过报通道 (c) 的镜像情形：句 1 的支撑是
  **两条锚点各覆盖一半**，任一条单独看射程都不足，不能拿 85 字那条单独宣称句 1 已验证。
- **句 2 无支撑。** 池内 4 条不同 (file,quote) 全部来自 15.02 / 15.04，讲的都是一般线性空间里
  Axiom 5 的存在性与唯一性，**没有一条提到函数空间**，更没有「处处取零的函数」。
  句 2 的字面出处只存在于 TD（`inherited/edges-A1.jsonl:18`，15.03.md）。

排除三条过报通道后仍成立：
- 通道 (a) 内容下移？**不成立。** 该节点只有 1 个 part-of 后代
  `d:axiom-5-states-the-zero-only-as-a-right-neutral-element`（`partof.tsv`），讲右单位元，与函数空间无关。
  写着句 2 内容的 `d:function-space-zero-is-the-everywhere-zero-function`（`nodes-D1.jsonl:60`，sections=15.03）
  parent 是 **`apostol:function-space`**，不是本节点，故不是本节点的 T3，下移抗辩不适用。
-

…（截断，全文 13015 字符）

## G2f-记账错复核.md

*抽自小节：## 一 D2 L66（过报判定 / 两个候选子串各自逐字与实测字符数 / 到底 89 还是 100 / 分片该怎么改）；原文 7124 字符*

**前言**：范围声明：只复核 §4.9（`report/_转交-新会话-workflow.md:293`）列出的两条分片记账错。
全部数字为本轮自行实测（Python `len()` + `in` 子串判定 + 逐行定位），未沿用 A9 的结论，
也未沿用 `check_graph` / `verify_anchors.py` 的输出。**本代理只读，未改 data/。**

#### 1.1 被复核的对象（两处，别搞混）

- 分片正文：`report/审查-形式D-D2-片59-80.md:50–80`，节点 `d:dim-of-a-second-order-de-solution-space-is-two`
  （`data/nodes-D2.jsonl:66`，故简称 L66），分片自评为「本节最实的一例」形式 D。
- 主控核验：`report/_D分片-主控核验.md:484`（塌陷表一行）与 `:525`（附带修正那句）。

#### 1.2 源行事实（先摆出来，两个结论都从这里出）

`source/apostol-ch15/15.08.md:15` 实测 **233 字符、n_sentence_end = 4**
（`tmp_index/source-lines.tsv` 中 `apostol-ch15/15.08.md 15 233 4`，本轮另用 `len()` 复核 = 233）。
整行逐字：

```
EXAMPLE 3. The space of solutions of the differential equation $y'' - 2y' - 3y = 0$ has dimension 2. One basis consists of the two functions $u_1(x) = e^{-x}$ , $u_2(x) = e^{3x}$ . Every solution is a linear combination of these two.
```

一行含 4 句，因此**这一处禁止按行号做覆盖比对**（过报通道 b）。

#### 1.3 两个候选子串，各自逐字与实测字符数

| 记号 | 逐字子串 | 实测字符数 | `in` 源文 | 命中行 | 当前在图里的身份 |
| --- | --- | --- | --- | --- | --- |
| **A** | `EXAMPLE 3. The space of solutions of the differential equation $y'' - 2y' - 3y = 0$ has dimension 2.` | **100** | True | `15.08.md:15` | `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 的锚点 0（`anchors.tsv:587` charlen=100） |
| **B** | `The space of solutions of the differential equation $y'' - 2y' - 3y = 0$ has dimension 2.` | **89** | True | `15.08.md:15` | `data/edges-D2.jsonl:85` 的 `evidence.quote`（`edges.tsv` ev_charlen=89） |
| **C** | `EXAMPLE 7. If $a_1, \ldots, a_n$ are distinct real numbers, the $n$ exponential functions` | **89** | True | `15.07.md:39`（整行 89，即该串就是整行） | 尚未被任何持有者引用 |

**结论：分片的两个数都是对的。** A = 100、C = 89，分片 `:72` 写 100、`:74` 写 89，各指各的串，无误。
A − B = 11，正是句首 `EXAMPLE 3. `（含尾随空格）11 字符。**上一轮的更正成立。**
同一段里出现两个来源不同的 89（B 与 C），这是把 B 的 89 记到 A 头上的成因。
额外一重独立佐证：A 作为另一个节点的既有锚点，`anchors.tsv` 独立测得 charlen=100，
与本轮 `len()` 一致——100 有两个互不依赖的测量来源，89 没有任何来源指向 A。

- 依据：`tmp_index/anchors.tsv:587`、`tmp_index/edges.tsv`（`data/edges-D2.jsonl:85` 行）、
  `tmp_index/source-lines.tsv`（`apostol-ch15/15.08.md` 第 15 行）、本轮 `len()` 实测。
- 死因编号：**0**（记账错，非图谱缺陷）
- 建议动作：**改 `report/_D分片-主控核验.md:525`**，把「实际可引子串是 89 字符」改为
  「实测 100 字符，分片无误；89 是同源行另一子串（`edges-D2.jsonl:85` 的边证据）的长度」。
  同时改 `report/_转交-新会话-workflow.md:293` 里复述这句的 `100 字符、真实子串是 89` 半句。
  **分片文件 `审查-形式D-D2-片59-80.md` 的 `:72`/`:74` 不动。**
- 置信度：高。不确定点：无——两个候选串都已逐字 grep 命中并双测长度。

#### 1.4 过报判定

分片 `:57–68` 的指控是「claim 1（维数为 2）与方程本身无锚」。按四层证据池：

| 层 | 条数 | 内容 |
| --- | --- | --- |
| T1 自身锚点 | 1 | 79 字符「One basis consists of the two functions …」 |
| T2 自发边 | 2 | `edges-D2.jsonl:85`（**B，89 字符

…（截断，全文 7124 字符）

## G3a-H1边逐条.md

*抽自小节：## 〇 结论速览（保留 4 / 修 7 / 撤回 4 = 15）；原文 24524 字符*

**前言**：只读分析代理产出。**未改动 data/ 下任何文件。**

| # | src → dst（略写） | 关系词 | evidence 支撑 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 空集独立 → O∈S 则相依 | 真辨析 | 单侧（src） | 修（origin→model） |
| 2 | 15.7(a) → 15.7(b) | 真辨析 | 单侧（dst） | 保留 |
| 3 | 张成集可无穷 → 无有限张成集 | 真辨析 | **双侧** | 保留 |
| 4 | dimension → spanning-set | 真辨析 | 单侧（dst） | 修（换引文） |
| 5 | S⊥对任意子集 → 正交补名号 | 真辨析 | **双侧** | 保留 |
| 6 | 15.13 → 15.14 | 误用（已有 implies） | 单侧（src） | 撤回 |
| 7 | 15.14 → 15.15 | 误用（应为 requires） | 单侧（dst） | 修（改词+反向+换引文） |
| 8 | 15.10 → O∈S 则相依 | 真辨析 | 单侧（src） | 修（换引文） |
| 9 | 沿元素投影 → 在子空间上投影 | 真辨析 | 单侧（dst） | 修（换引文） |
| 10 | a_j 抵消式 → y_j=O 时 a_j 无关 | 真辨析 | 单侧（dst） | 保留 |
| 11 | 归一化 → y=O 检测相依 | 误用 | 单侧（dst） | 撤回 |
| 12 | 15.9 对每个内积成立 → 内积不唯一 | 真辨析 | 单侧（dst） | 修（换引文） |
| 13 | Fourier 系数 → 有序基下的分量 | 真辨析 | **零侧** | 修（origin→model） |
| 14 | 相依被超集继承 → 独立性的全称量词 | 误用（对象错） | 单侧（src） | 撤回 |
| 15 | 标量倍导致相依 → 独立集 | 误用（对象错） | **零侧** | 撤回 |

机械合规：15/15 条 quote 在 `source/apostol-ch15/` 对应文件中逐字命中、单行、
charlen 40–186 全落在 SPEC 的 30–200 界内（自测，见每条正文）。15 条全为 `origin=source`，
**没有一条落在 tmp_index/unverified-model-quotes.tsv 里**（该表 0 条来自 edges-H1），
即这 15 条的引文本身已被 check_graph 逐字核过。本轮的问题全部不在合规性，在支撑力与选词。

## G3b-H2节点逐条.md

*抽自小节：## 〇 结论速览（节点实际数量 / 保留 n / 修 m / 撤回 k）；原文 33353 字符*

**前言**：只读代理产出。**未改动 data/ 下任何文件。**所有字符数取自 tmp_index/anchors.tsv、edges.tsv 的
charlen 列（实测，转义前）；新构造的 quote 由我自己在 source/apostol-ch15/ 下逐字实测并标注。

**节点实际数量 = 11**。文件 12 行，第 12 行为空行（结尾换行），tmp_index/nodes.tsv 里
`grep 'nodes-H2'` 恰好 11 行，loc 从 `data/nodes-H2.jsonl:1` 到 `:11`，无缺号。

- 保留（不改）3：`ext-theorem-12-10-part-b`、`ext-theorem-12-10-part-c`、`ext-vn-c-section-12-16`
- 修 8：其余 8 个（见 §三）
- 撤回 0

三条全局机械结论（可直接引用）：
1. **锚点字符数全部合规**。16 条锚点，charlen 最小 56（`ext-theorem-12-8` 第 0 条）、
   最大 167（`ext-theorem-12-2` 唯一一条），全部落在 30–200 内，`has_nl` 全为 0，无跨行。
2. **证据池极薄**。exposure.tsv 显示 11 个节点 `n_partof_desc` 全为 0 —— H2 节点全都没有
   part-of 后代，**T3 恒为空**；T2 只有 2 条（`ext-theorem-12-3` 经 edges-H2:9、
   `ext-vn-c-section-12-16` 经 edges-H2:15）。池总量 11 个节点合计仅 18 条。
   这意味着 H2 节点的断言**只能靠自身锚点支撑**，没有下移通道可援引 —— 三条过报通道里的
   (a) 对 H2 全体失效，所以下面每处 E/D 型判定都不能用「内容在后代」解释掉。
3. **TD 极厚且极易误用**。19 条 edges-H2 里有 17 条以 H2 节点为 dst（requires/generalizes），
   按四层定义**全部排除**。审计时最大的陷阱是拿 edges-H2:14
   （`apostol:dependent-set --generalizes--> ext-chapter-12-...`，evidence 65 字
   「However, the present definition is not restricted to finite sets.」）去支撑
   `ext-chapter-12-finite-dependence-definition` 的 statement —— 它是 TD，不入池。
   本审查中有 2 处（节点 5、节点 7）正是这种「内容确实在图里，但挂在 TD 边上」的记账错位。

主要缺陷类型分布：E 型无锚句 7 处、D 型覆盖不足 4 处、B 型借用 6 处（其中 3 处判 H2 节点非原主）、
死因 1（量词过度断言）2 处、死因 4（数学/事实错误）1 处（节点 11 的「the only place」）。
**死因 5（伪抽象）0 处** —— 理由见 §一 末尾的统一判定。

## G3c-H2边逐条.md

*抽自小节：## 〇 结论速览（保留 10 / 修 5 / 撤回 4 = 19）；原文 26313 字符*

**前言**：只读分析代理产出。**未改动 data/ 下任何文件。** 范围严格限于 `data/edges-H2.jsonl` 的 19 条边。

先说机械层：16 条 `origin=source` 的 evidence 我全部自己 `grep -F` 复核过，**19/19 无问题**——
16 条全部在声明的 file 里逐字连续命中、无跨行、charlen 30–200（实测 56/78/67/67/67/67/112/68/167/116/141/84/107/65/91/127）；
另 3 条（第 17/18/19 行）`origin=model` 且**根本没有 evidence 字段**，是 SPEC 允许的写法，
也不在 `tmp_index/unverified-model-quotes.tsv` 的 15 条盲区里（我核过：H2 命中数 0）。
所以 H2 的 19 条边不存在死因 3（虚构出处）。

争点全在**关系词方向**与**记账位置**：

| 判定 | 条数 | 边行号 |
| --- | --- | --- |
| 保留 | 10 | 2, 5, 6, 8, 9, 11, 12, 15, 16, 17 |
| 修 | 5 | 1, 7, 10, 13, 14 |
| 撤回 | 4 | 3, 4, 18, 19 |

三类问题的分布：
- **方向反了（第 1、7 行）**：`requires` 写成了「后来的定理需要旧定理」，但引文说的是
  「$V=V_n$ 时新定理**退化成**旧定理」——这正好是 `generalizes` 的定义（src 是 dst 的推广），
  方向相反。死因 6（引文不支撑断言）+ 方向错。
- **记账位置重复（第 3、4 行撤回）**：定理级节点与它的 part-of 子节点用**同一条引文**
  各发一条边到同一 dst，第 5、6 行是更细的配对（(a)→12.10(b)、(b)→12.10(c)），
  严格优于第 3、4 行。撤回第 3、4 行不丢信息（T3 上浮）。
- **边两端锚点同源（第 18、19 行撤回）**：src 的 `d:` 节点与 dst 的 `ext-uncited-*` 节点
  引的是**同一句 15.08 原文**（逐字相同，我比对过 anchors.tsv），这条 `requires` 边的两端
  说的是同一句话，不承载任何依赖信息。死因 5（层塌缩）。注意：**我撤的是边，不是节点**——
  「Apostol 没标出处」这个观察本身是真的，应留在 ext 节点的 statement 里，由 G3b 裁决节点存废。

置信度：机械层高（自己 grep 过）。方向判定（1、7）高。撤回 3、4 中（是图质量判断，不是错误）。
撤回 18、19 中高。第 10 行的修法只有中等置信，见该节。

## G3d-跨教材未校验边.md

*抽自小节：## 〇 结论速览；原文 17177 字符*

**前言**：只读分析代理产出。**本文件是提案，未改动 data/ 下任何文件。**

审查对象：`data/edges-X.jsonl:106–121` 中 15 条 `rel=generalizes` 的 Apostol→Strang 边
（108 行不含 evidence 的 119 行不在本轮范围内）。这 15 条 `origin=model` 却带 `quote`，
`check_graph` 只校验 `origin=source` 的 916 条，从未逐字核过它们。

**逐字核验通过 15 / 失败 0**。15 条 quote 全部是对应 Apostol 源文件里逐字连续、不跨行的
子串，独立实测字符数与 `tmp_index/unverified-model-quotes.tsv` 的 charlen 列**逐条一致**，
全部落在 SPEC 的 30–200 界内。**没有虚构引文，硬禁令 2 无违反。**

**origin 应改 source 的 0 条。** 任务简报给的口径（"quote 来自教材原文 ⇒ origin 应标 source"）
在这里不成立，我不采用：`origin` 记的是**这条边的关系断言**的来源，不是 quote 字符串的来源。
Apostol 第 15 章通篇没有提到 Strang，任何 Apostol→Strang 的跨教材断言在教材里都无原文依据，
所以这 24 条跨教材边（15 generalizes + 6 contrasts + 3 alias-of）**全部只能是 origin=model，
现状是对的**。真正的缺陷不在 `origin` 字段，而在两处：(a) 工具缺口——`check_graph` 不核
`origin=model` 的 quote，导致 15 条引文长期无人验证（这次核完了，全过）；(b) 引文选得差
——**15 条里有 6 条的 quote 只截了定理/定义的假设句或引言半句，不含任何结论**，
即使逐字为真也支撑不了它所挂的断言（死因 6）。

**应撤回 1 条**：`:107 apostol:euclidean-space generalizes strang:orthogonal-vectors`
——源与目标不同类（空间 vs 两向量间的关系），quote 通篇未提正交，且 `:121`
已经承担了正确配对。死因 6 + 5，建议 KILL。

分类汇总（详见 §四、§五）：
- 方向与配对都站得住、quote 也支撑：**4 条**（113、114、116、121）
- 方向对但 quote 只有假设句/半句，需换引文：**6 条**（109、110、111、112、117、120）
- 方向对但"generalizes"过强（Strang 侧本来就是一般表述，差别在证明手段而非一般性）：
  **3 条**（115、116、118）——其中 116 与上一类重叠，建议改 `equivalent`
- 配对本身错、应改指或撤回：**2 条**（107 撤回、112 改指）
- 借用引文（B 型，quote 的主持有者是别的节点）：**6 条**（107、109、113、114、116、120）

置信度：逐字核验与字符数 = 高（可复算，见 §一）；origin 判定 = 高；
generalizes 方向 = 中（依据 statements.tsv 里两侧的 statement 与 Apostol 原文，
未通读 Strang 3.4/4.1/4.2 全文，只读了两侧 statement 与 dst 的锚点清单）。

## G3e-H层总装.md

*抽自小节：## 〇 结论速览（H 层最终：保留 n 条边 m 个节点 / 撤回 / 缺口 k 条）；原文 13563 字符*

**前言**：只读装配代理产出。**未改动 data/ 下任何文件。**
输入：同目录 G3a（H1 边 15 条）、G3b（H2 节点 11 个）、G3c（H2 边 19 条）、G3d（跨教材 model 带 quote 边 15 条）。
四份全部齐备，无缺件。设计文档：report/横向-章内辨析.md、横向-跨章回指.md、横向-跨教材差异.md。

**H 层边的口径先说清**（三个「H」在数据里分居三处，报数时必须指明范围）：

| 范围 | 现有条数 | 依据 |
| --- | --- | --- |
| H-in（章内辨析） | 15 | `data/edges-H1.jsonl` 全部 |
| H-back（跨章回指） | 19 | `data/edges-H2.jsonl` 全部 |
| H-cross（跨教材，apostol↔strang 直连） | **25** | edges.tsv 全表：edges-X.jsonl:106–128 共 23 条 + edges-X.jsonl:119 已含其中 + `edges-A2-x2.jsonl:21` 1 条 + edges-X.jsonl:105 以下的 `x:` 骨架不计 |
| H 层合计 | **59** | 15 + 19 + 25 |

H-cross 的 25 条 = generalizes 16、contrasts 6、alias-of 3。**注意 24 与 25 的差**：
G3d 与横向-跨教材差异.md 都按「跨教材边 24 条」计，那是只数 `edges-X.jsonl` 的结果；
`edges-A2-x2.jsonl:21`（`apostol:cx-metric-criteria-for-orthogonality --contrasts--> strang:pythagorean-law-orthogonality`）
是一条住在 X 文件之外的跨教材边，两处都漏计了。见 §五。

**最终建议（四份提案合并后）**：

- 边：撤回 **9** 条（H1 4 条 + H2 4 条 + H-cross 1 条），改 **21** 条（H1 7 + H2 5 + H-cross 9），
  原样保留 **29** 条。59 → **50** 条。
- 节点：H2 的 11 个**全部保留**（G3b 撤回清单为空），其中 8 个需 FIX。
- 缺口：设计文档有而图谱无 **4** 条（H-in 0、H-back 1、H-cross 3），
  另有 1 处**无法归因的计数差**（设计文档称 edges-X 162 条，实际 161 条）。
- 可疑新增：图谱有而三份设计文档均未提 **4** 条（3 条 alias-of + 1 条住在 X 文件外的跨教材 contrasts）。
- 交叉悬空：**0 条真悬空**（G3b 不撤任何节点，G3c §四 的依赖表在撤回轴上整体失效），
  但有 **1 处孤点后果**必须处置：G3c 撤 edges-H2:18/19 后，两个 `ext-uncited-*` 节点度归零，而 G3b 保留它们。

置信度：条数与端点核算高（全部来自 tmp_index 逐行 awk）；缺口判定中高（依据设计文档正文与实际边比对，
Strang 侧原文我未通读，只读了设计文档里的逐字引用）；162/161 那处差额**未核出归因**。

## G4-完备性-15.01.md

*抽自小节：## 〇 结论速览（本节 8 句 / 已覆盖 7 / 下移 0 / 未覆盖 1；其中值得补建 0 个）；原文 11483 字符*

源文件只有两个正文段（第 3、5 行）加一个标题行（第 1 行）。按**句**切开共 8 句
（标题行不计句）。source-lines.tsv 显示第 3 行 n_sentence_end=4、第 5 行 n_sentence_end=4，
两行都含 4 句 —— **本节完全落在过报通道 b 里，按行号比对会把覆盖率报成 2/2=100%**，
所以下面一律按句判定。

- 已覆盖 7 句（其中 6 句由节点锚点直接覆盖，1 句由节点锚点与边 evidence 同时覆盖）
- 合法下移到 part-of 后代（T3 才有）0 句
- 未覆盖 1 句：S1（本章开篇的动机句）
- 未覆盖句中值得**新建节点**的 0 个 —— S1 的内容已由现有节点
  `d:objects-already-met-that-can-be-added-and-scaled` 的 statement 承担，
  但该 statement 的核心谓词（"可加、可乘实数"）在它的两条锚点里都找不到。
  所以正确动作是给现有节点**补一条锚点**（FIX），不是建新节点。详见 §二。

## G4-完备性-15.02.md

*抽自小节：## 〇 结论速览（本节 27 句 / 已覆盖 22 / 下移 0 / 未覆盖 4；其中值得补建 0 个）；原文 14041 字符*

范围：仅 `source/apostol-ch15/15.02.md`，全文 52 行（21 空行、10 行 `$$` 围栏标记、
21 行真实内容）。按**句**切得 27 个语义单元（S1–S27），其中 S1 是节标题，不计入覆盖判定，
故受判定的是 26 句：**节点锚点覆盖 20 / 仅边 evidence 覆盖 2 / 未覆盖 4**。

- **已覆盖 22**：其中 20 句由节点锚点直接覆盖，2 句（S4「Closure axioms」、
  S7「Axioms for addition」）只由**边 evidence** 覆盖，因为这两行分别只有 14 / 19 字符，
  低于 SPEC 锚点 30 字符下限，做不成锚点。
- **未覆盖 4**：全部是**公理的展示等式行**——S13 `x + (- 1) x = O.`(16)、
  S16 `a (b x) = (a b) x.`(18)、S18 `a (x + y) = a x + a y.`(22)、
  S20 `(a + b) x = a x + b x.`(22)。四行都低于 30 字符下限且 SPEC 禁止跨行拼接，
  因此在现行规则下**不存在任何合法引文**。
- **下移 0**：本节没有一句是「内容合法下移到 part-of 后代」的情形。反过来说，
  本节内容的记账位置非常密集：31 个 sections 含 15.02 的节点、43 条 file=15.02.md
  的锚点、45 条 ev_file=15.02.md 的边，全部落在本节自己的子树里。
- **值得补建 0**：四条未覆盖的等式**都已有对应节点**
  （`d:axiom-6-existence-of-negatives` / `-7-` / `-8-` / `-9-`），
  缺的不是节点而是**引文**。新建节点解决不了这个问题，因此不提任何补建节点提案。
  这四行的不可引已由 `report/SPEC缺陷-锚点规则量化.md` §2.3.1 逐条登记（含相同的
  16/18/22/22 字符实测值），本报告只补一件它没说的事：**这四条等式在图谱里的下游后果是
  「断言存在、引文缺位」**——四个节点的 statement 都写出了规范化后的等式
  （如 `x + (-1)x = O`、`a(bx) = (ab)x`），而这些等式在整张图的 anchors.tsv / edges.tsv
  里**一次都没有作为逐字引文出现过**（已 grep 验证：`(a b) x` / `a x + a y` /
  `a x + b x` / `1) x = O` 四个片段在两表中命中数均为 0）。

## G4-完备性-15.03.md

*抽自小节：## 〇 结论速览；原文 15618 字符*

**前言**：审查范围：仅 `source/apostol-ch15/15.03.md` 一节的覆盖完备性。全部判据取自
`tmp_index/` 的 anchors.tsv / nodes.tsv / edges.tsv / shared-quotes.tsv / source-lines.tsv /
statements.tsv / pool.tsv，未重算。源文件逐字核对用 `grep -cF`。

本节 **36 句** / 已覆盖 **35**（节点锚点 34 + 仅边 evidence 1）/ 下移 **0** / 未覆盖 **1**；
其中值得补建 **0** 个。

一句话结论：**15.03 的覆盖完备性没有实质缺口**，唯一未被任何 quote 命中的是 S12 的
独立公式行 `(f + g) (x) = f (x) + g (x)`，而它的内容已由 S11 与 S13 的散文锚点承载
（`apostol:function-space` anchors[0]、`d:function-space-addition-is-pointwise`），
且该行实测仅 27 字符（source-lines.tsv `apostol-ch15/15.03.md` 第 16 行 charlen=27），
**低于 SPEC 锚点下界 30**，物理上无法单独作锚。故本节不提任何新建节点。

真正的问题不在完备性而在**记账正确性**，两处（详见四、五节）：
1 `d:linear-space-is-a-set-together-with-two-operations` 有一条 15.03.md 的锚点，
  但它的 sections 只写 `15.01,15.02` —— sections 漏登 15.03。
2 `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 的 sections 声明
  `15.03,15.08`，但两条锚点全在 15.08.md，15.03 侧零锚。

覆盖率口径声明：本节按**句**切分统计，未按行号。source-lines.tsv 显示 15.03.md
19 个非空行里 13 行的 n_sentence_end ≥ 2（第 3/5/7/9/11/19/21/23/25/27/29/31/33/35/37 行），
最高 4 句/行，按行号比对会严重高报——这正是过报通道 b，已避开。

## G4-完备性-15.04.md

*抽自小节：## 〇 结论速览；原文 17813 字符*

本节按**句**切分共 **54 句**（含 4 个 `$$` 数学块各计 1 句；不按行号比对，见下）。

| 覆盖状态 | 句数 |
| --- | --- |
| 节点锚点已覆盖（NODE） | 44 |
| 仅边 evidence 覆盖（EDGE） | 3 |
| 已下移到 part-of 后代（T3） | 0（无「只在后代」的句子；后代覆盖的句子其 parent 也覆盖） |
| **未覆盖** | 7 |

未覆盖 7 句中**值得补建新节点的为 0 个**。7 句的处置是：3 句纯结构/连接词（S1 标题、S6/S16「Proof.」中的连接残句已并入 EDGE，见下表）、
1 句节引导性过渡（S2）、1 句证明步骤缺锚（S8，**应 FIX 现有节点而非新建**）、
3 句为定理 15.3 的 (a)(b)(c) 命题行（S32/S33/S34，**节点已存在、内容不缺，但源行无法合法建锚——见 §三 的字数说明**）。

**过报通道 b 已避开**：`source-lines.tsv` 显示 15.04.md 的第 5、7、9、11、25、29、49、57、59、65 行
`n_sentence_end ≥ 2`（第 7 行达 6 句），若按行号比对会把这些行整行判为「已覆盖」。
本报告全部按句判定。**冗余度未按池大小计**：本节 93 条引文（锚点 36 + 边 evidence 57）
去重后只落在 **24 个不同 (file,quote)** 上、覆盖 **20 个不同源行**，重复度约 3.9 倍。

**引文真实性**：本节 93 条引文（`file=15.04.md` 的全部锚点与边 evidence）已用脚本逐条比对源文件，
**93/93 为单行内逐字连续子串，0 条失败**。这只证明未被篡改，不证明支撑断言（见 §五）。

## G4-完备性-15.05.md

*抽自小节：## 〇 结论速览（本节 n 句 / 已覆盖 a / 下移 b / 未覆盖 c；其中值得补建 d 个）；原文 17864 字符*

**本节 47 句 / 已覆盖 18 / 下移 0 / 未覆盖 29；其中值得补建 5 个。**

- 切句依据：源文件 78 行，非空行 39 行；`tmp_index/source-lines.tsv` 里 15.05 的 `n_sentence_end`
  在 line3 为 5、line13/15/17 为 4、line61 为 4、line67 为 4，**多句行占比很高**，
  所以全表按句切，不按行号比对（避开过报通道 b）。
- 「下移 0」是硬事实：`partof.tsv` 里 11 个 `apostol:cx-`（15.05）节点**一个都不出现**，
  `exposure.tsv` 对这 11 个节点的 `n_partof_desc` 全为 0，T3 全为 0。所以本节没有任何
  「内容合法下移到 part-of 后代」的情形，未覆盖就是未覆盖。
- 29 句未覆盖里有 **3 句是「内容在图里但记账在别节」**（S23 部分、S33、S47，另加 S38 的特殊情形），
  它们不是 part-of 后代（T3=0），因此按四层证据池的定义**不入 15.05 任何节点的池**；
  但从全图完备性看不算内容缺失。扣掉这 3 句，**真正的内容缺口是 26 句**。
- 结构性成因（一句话）：15.05 只被反例代理 CX2 覆盖过一遍，
  它只对「不是线性空间」的 8 道题建了节点；**21 道「是线性空间」的正面例题一个节点都没有**。
  这不是 CX2 的失职（它的任务书就是反例层），是**任务分配的空档**——
  `report/丢弃与未覆盖清单.md` 未记录这一项，属于新缺口。
- 另发现一处**事实错误**（不属完备性，但由本次逐句比对暴露）：
  `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 的 statement 首句写
  「Apostol does not remark on this; the derivation below is not in the text.」，
  而 15.05 第 30(a) 题（line63）**逐字要求**"Prove that Axiom 10 can be deduced from the other axioms."
  即 Apostol 本人以习题形式提出了这个命题。详见 §四与 §五之后的 FIX 建议。

## G4-完备性-15.06.md

*抽自小节：## 〇 结论速览（本节 37 项 / 参与判定 34 / 已覆盖 27 / 下移 0 / 未覆盖 7；其中值得补建 1 个）；原文 17217 字符*

**按句切，不按行切。** 源文件 `source/apostol-ch15/15.06.md` 共 30 行，
其中正文行 8 行；`tmp_index/source-lines.tsv` 显示 15.06.md 的 n_sentence_end
在第 3 行为 3、第 5 行为 3、第 7 行为 2、第 9 行为 3、第 11 行为 7、第 13 行为 2、
第 19 行为 4、第 21 行为 2、第 27 行为 2、第 29 行为 6 —— **每一条正文行都 >1 句**，
所以本节完全不能按行号做覆盖比对（过报通道 b 全线生效）。我人工切出 37 句（含 §标题
与 3 个公式块，均单列编号），与机器的句末标点启发式略有差异（如 "Theorem 15.3(a)."
的句内句点），语义切分以本表为准。

- 本节共 37 项；**不判 3 项**（S1 标题、S7 "Proof." 标签、S37 空行）。
- 参与判定 34 项：**已覆盖 27 / 合法下移（仅靠 T3）0 / 未覆盖 7**。
- 未覆盖 7 项：S16（证毕句 "Therefore S is a subspace of V."）、S27+S28+S29
  （次数 ≤ n 多项式空间的三组张成集，含公式块）、S31（"自然产生若干问题"）、
  S33（"最少需要几个元素"）、S34（引入依赖/独立/基/维数的路线图句）。
- **值得补建 1 个**：S27+S28+S29 合并为一个 example 节点（§二）。
- 另有 2 处只需 **锚点级 FIX**、不需新节点：S10（`d:thm-15-4-identities-inherited-…`
  锚点起点截断，D 型）与 S33（可作为 `apostol:spanning-set` 的第 4 条锚点补上）。
- **无一句属于"合法下移到 T3"**：本节所有已覆盖句都由持有者自身的 T1 锚点或其发出边的
  T2 evidence 覆盖，不存在"父节点空、内容在后代"的情形。这一点我逐条核过 anchors.tsv 与
  edges.tsv 的 ev_file 列，未依赖 pool.tsv 的传递闭包。

## G4-完备性-15.07.md

*抽自小节：## 〇 结论速览（本节 45 句 / 已覆盖 37 / 下移 0 / 未覆盖 8；其中值得补建 0 个）；原文 14574 字符*

按**句**切分（不按行）：正文 S2–S46 共 45 句，其中 5 句是整块公式（每块记 1 句）。
- 已覆盖 37 句：节点锚点直接覆盖 36 句，**仅由边 evidence 覆盖 1 句**（S18，函数空间的取定）。
- 下移到 part-of 后代 0 句。本节 part-of 结构是「L1 概念 ← D2 切片」自上而下切分，
  没有出现「父节点断言只靠后代锚点支撑」的 T3 情形（查 partof.tsv 与 pool.tsv）。
- 未覆盖 8 句：叙述句 4 句（S7、S16、S17、S19、S21 中 S16/S17 为过渡），
  公式块 3 句（S25=(15.1)、S30=指数函数定义式、S34=(15.2)；对照 (15.3) 有锚）。
- **值得新建节点的：0 个**。8 句里 2 句是过渡叙述、3 句是纯公式记号、
  3 句（S7、S19、S21）的**断言**已被现有节点的 statement 承载，缺的是**锚点**不是节点。
  所以本节的缺口类型是「FIX 加锚」，不是「补建节点」——这一点与 A8 的框架不同，须让裁决者注意。
- 关键发现：S19（Example 5 里 u₁=cos²t、u₂=sin²t、u₃=1 的定义句）与
  S21（Example 6 里 u_k(t)=t^k 的定义句）**全图无任何锚点或边引用**
  （grep anchors.tsv / edges.tsv 全表，`u_{1}(t)`、`u_{k}(t)` 命中 0）。
  这使两个节点的 statement 里的 cos²/sin²/t^k 这些**对象名**在图内无出处支撑，属盲区 A/D 型。
  置信度高（机械可证：两条 grep 全表零命中）。

## G4-完备性-15.08.md

*抽自小节：## 〇 结论速览（本节 37 句 / 已覆盖 34 / 下移 0 / 未覆盖 3；其中值得补建 0 个）；原文 19371 字符*

切句口径：`source/apostol-ch15/15.08.md` 共 38 行，其中 19 行非空（含 1 行节标题、2 行 `$$` 界定符）。
按**句**切得 37 句（S1–S37）。`DEFINITION.` / `THEOREM 15.6.` / `Proof.` / `EXAMPLE n.` 这类标签不单独计句，
并入其后第一句；第 31–33 行的行间公式块（`$$` 界定符 + 公式本体）只把公式本体计为 1 句（S34），
两个 `$$` 不计。**没有按行号做覆盖比对**：source-lines.tsv 显示本节 19 个非空行里 15 行的
n_sentence_end > 1（第 7 行 8 处、第 35 行 3 处、第 3/9/13/15 行各 4 处），按行号必然高报。

- 已覆盖 34 句。其中 **3 句只是"锚点截断式覆盖"**（S18、S35、S37）：句子的主干在锚点里，
  但各缺一个头部或尾部分句，语义已在图里，算已覆盖并附 FIX（S37 判为良性，不提 FIX）。
- 下移到 part-of 后代 0 句。本节 9 个 L1 父节点的后代（partof.tsv）全部 sections 也含 15.08，
  即父子同在本节，不存在「内容合法地下移到别节后代」的情形，**T3 通道在本节为空**。
- 未覆盖 3 句：S19（例 2 的张成一半）、S22（例 3 的张成一半）、S33（公式 (15.4) 本体）。
- 值得补建节点 **0 个**。三句全是「已有节点的断言已含该内容、只是锚点没圈进去」，
  死因 0，建议动作是给 3 个既有节点各加/换一条锚点（FIX），不是建新节点。
  若照句面建节点，会造出 parent 复述型节点，正撞审计 B 的死因 5。
- 本节没有找到「教材里有、图谱里没有」的**新增内容缺口**。`report/丢弃与未覆盖清单.md`
  未记录 15.08 的任何丢弃项，本报告也不新增丢弃项，只新增 3 条 FIX 与 1 处集合不一致。

## G4-完备性-15.09.md

*抽自小节：## 〇 结论速览（本节 53 句 / 已覆盖 13 / 下移 0 / 未覆盖 40；其中值得补建 8 个）；原文 17451 字符*

**前言**：审查对象：`source/apostol-ch15/15.09-exercises.md`（80 行，练习节）。
只读审查，未改动 `data/` 下任何文件。

本节全部内容只由 4 个节点持有（`nodes.tsv` 中 sections 含 15.09 的节点，全部来自
`data/nodes-CX2.jsonl:12–15`），共 9 条 T1 锚点、1 条 T2 边证据/节点，
`exposure.tsv` 显示这 4 个节点的 part_of 后代数**全为 0**，故本节**T3 恒空**——
「内容合法下移到后代」这条免责通道在 15.09 上不存在，未覆盖就是真未覆盖。

已覆盖的 13 句集中在 4 个反例点（ex19/20、ex22(f)(g)、ex24(c)(d)、ex23(e)(g)(h)）
及 4 条框架句。未覆盖的 40 句里，**8 处是有独立数学内容的正面命题**，其中
ex22(c)(d)(e)、ex24(a)(b) 五条是教材点名要求证明的命题，图谱里全无对应节点
（已按 statement 全文检索确认，非记账位置问题）。

一句话概括：15.09 被当作**反例矿脉**开采，ex22 与 ex24 的**正面命题**（子空间交封闭、
维数单调、维数饱和判据、L 的闭包算子性质）被系统性漏掉，这不是 OCR 缺陷造成的，
源文件里这几句是完整可锚的。

## G4-完备性-15.10.md

*抽自小节：## 〇 结论速览；原文 21433 字符*

**本节 64 句 / 已覆盖 56（其中 21 句合法下移到 part-of 后代）/ 无锚但有节点承载 3 /
真未覆盖 5（含 2 句部分未覆盖）；其中值得补建新节点 0 个。**

切句办法：按句末标点切，独立展示公式块（`$$ … $$` 内的一行）计为一句，
因为图谱把它们当作独立锚点在用。行号仅作定位，覆盖判定全部按句做——
本节 `source-lines.tsv` 里 `n_sentence_end > 1` 的行有 12 行
（L3 有 4 句、L11/L13/L35/L43/L45/L49/L59/L65/L133 各 2 句、L73 有 3 句、L119 有 4 句），
按行号比对会显著高报（过报通道 b）。

核心结论不是「缺内容」而是「缺锚」：**本节没有一句数学内容是图谱完全不知道的**。
5 处真缺口里，3 处是**同一个结构性原因**——该句所在的源行短于 SPEC 的 30 字符下界，
提案方于是拿邻句顶替，导致节点存在、statement 正确，但**没有一条锚点含有该句本身**：

| 缺锚的句子 | 源行:字符数 | 现有节点 | 现有锚点的实际内容 |
|---|---|---|---|
| 公理 (3) `c(x,y) = (cx,y)` | L19:21 | `d:inner-product-homogeneity-axiom` | 邻句「Note: Taking c = 0…」与复数版说明 |
| 范数定义式 `‖x‖ = (x,x)^{1/2}` | L108:26 | `d:norm-definition-as-square-root` / `apostol:norm` | 定义句的前半与后半，**中间的式子本身没有** |
| 性质 (a) `‖x‖ = 0 if x = O` | L123:29 | `d:norm-vanishes-at-the-zero-element` | THEOREM 15.9 表头 + Proof 开场句，两条都是借用 |

这三处 `d:inner-product-homogeneity-axiom` 的 statement 里已自认了
（「Because the display of axiom (3) occupies a line of only 21 characters,
the anchor below is the sentence that names the axiom and uses it」，
`statements.tsv` 该行）——说明这是**已知取舍**而非疏漏，但它同时说明
**SPEC 的 30 字符下界与 OCR 的逐行切分互相冲突**：
教材里最要紧的三个式子恰好都短于 30 字符。这一条建议进 SPEC。

建议动作合计：新建节点 0；**补锚 3 条**（需 SPEC 先给短式子开例外）；
**改错边 1 条**（`data/edges-D3.jsonl:171` 的 dst 指错空间，见第五节抽查②）；
补锚 1 条为可选（S49 类比句，见第三节）。
置信度：句级覆盖判定高（逐句 grep 三张表核过）；
「值得补建 0 个」中等——它依赖「缺锚不等于缺节点」这一判断，若综合代理采用
「一句必须有一条含它的锚点」的更严标准，则上表 3 处会从「补锚」升级为「补锚+FIX statement」。

## G4-完备性-15.11.md

*抽自小节：## 〇 结论速览（本节 52 句 / 已覆盖 44 / 下移 0 / 未覆盖 8；其中值得补建 0 个、值得补锚 1 处）；原文 16548 字符*

切句办法：读 `source/apostol-ch15/15.11.md` 全文（92 行），按句末标点切，公式块整体计一句。
**不按行号比对**——本节 `source-lines.tsv` 里第 3 行 4 句、第 15 行 6 句、第 29 行 3 句、
第 35 行 2 句、第 41 行 2 句、第 43 行 2 句、第 89 行 3 句、第 91 行 3 句，全是过报通道 b。

- 已覆盖 44：其中 41 句被节点锚点逐字含住，3 句仅靠边 evidence（S22/S23 由 edges-D3.jsonl:49
  的 134 字符 evidence 覆盖，S16 由 edges-D3.jsonl:126 的 154 字符 evidence 覆盖）。
- 下移 0：本节所有 D 层节点的 parent 都在本节内（`apostol:orthogonal-elements` 等 6 个 A1 节点
  的 sections 也是 15.11），不存在内容下移到别节的情况，也没有需要靠 T3 才够的父节点。
- 未覆盖 8：S5 S10 S18 S19 S27 S30 S32 S48。其中只有 S18+S19 是数学内容，其余 6 句是过渡叙述、
  引导小句或纯记号。
- **值得补建 0 个新节点**；值得的是给已有节点 `d:trigonometric-orthogonal-system` 补 2 条锚点
  （见第二节）。它的 statement 已经断言了 S19 的正交关系，却没有一条锚点落在 S19 上——这是
  **死因 6**（引文不支撑断言）的一个实例，不是完备性缺口，而是记账缺口。

已知更正已遵守：**没有**把 15.15 的那处数学缺陷算到本节头上，本节未发现任何死因 4。

## G4-完备性-15.12.md

*抽自小节：## 〇 结论速览（本节 70 句 / 已覆盖 45 / 下移 0 / 未覆盖 25；其中值得补建 3 个）；原文 20106 字符*

切句口径：本节 152 行，去掉节标题行与全部空行后，按**句**切得 70 句（同一源行含多句的按句拆，
例如 L3 的 n_sentence_end=4、L85 的 =4、L135 的 =2，见 source-lines.tsv；OCR 留下的 4 个裸公式块
L114 / L118 / L122 各计 1 句，L94 的 `\text{(d)}...` 计 1 句）。**未按行号比对**（过报通道 b）。

- 已覆盖 45：其中 44 句由节点自身锚点（T1）覆盖，1 句（S15＝习题 3）只有边 evidence（T2）覆盖，
  节点侧无锚点。
- 下移 0：本节 25 个节点里只有 2 组 part-of（`cx-metric-criteria-for-orthogonality` →
  `cx-orthogonality-as-a-minimum-condition`，`cx-cross-terms-do-not-collapse-to-twice-the-inner-product`
  → `cx-parallelogram-law-is-forced-by-an-inner-product`，见 partof.tsv），两个子节点的锚点
  本身就在本节内，已计入「已覆盖」，不存在「内容下移到别节后代因而本节看似缺」的情形。
  故本节 T3 不产生任何额外覆盖，**过报通道 (a) 在本节为空**。
- 未覆盖 25：绝大多数是习题的**计算型小问**（8(a)(b)、10(b)(c)、11(c)(d)、13(a)(c)、14(b)(c)）、
  纯记号铺垫句，以及 OCR 残骸；其中**习题 15 整题（3 句）零覆盖**是本节唯一的成体量内容缺口。
- 值得补建 3 个：习题 15 的共轭双线性（sesquilinear）规则、习题 16(b) 的极化恒等式、
  习题 9 的三函数具体角度配置。
- 另有 2 处**不需要新节点、只需给既有节点补锚点**（12(a) 与 13(a)），见第三节末与第五节。

## G4-完备性-15.13.md

*抽自小节：## 〇 结论速览；原文 14748 字符*

**前言**：审查对象：`source/apostol-ch15/15.13.md`（171 行）。方法：把源文件按**句**切开
（散文句 S1–S77，独立公式块 E1–E25，共 **102 个语义单元**），再用 `tmp_index/anchors.tsv`
与 `tmp_index/edges.tsv` 中 file 列为 `15.13.md` 的全部 **74 条不同引文**在源文件上做
字符级掩码，逐单元统计覆盖字符比例。74 条引文全部在源文件中逐字命中（0 条落空），
所以下面的「未覆盖」是真未覆盖，不是引文写错。

**不按行号比对**：本节 36 个非空行中有 18 行 `n_sentence_end > 1`
（`source-lines.tsv`，最多的 L120 有 8 个句末标点），按行号算会严重高报覆盖率。
下表的「覆盖状态」按句/公式块判定，不按行。

本节 **102 个单元**（散文句 77 + 独立公式块 25）：

| 状态 | 个数 | 说明 |
|---|---|---|
| 已覆盖（本节自有节点的 T1 锚点或 T2 边证据） | 36 | 定理陈述、Gram-Schmidt 命名与描述、15.14、投影定义、Legendre 定义 |
| 已下移到 part-of 后代（T3） | 31 | 整段证明（`d:` 层 14 个节点）+ Legendre 前两步计算 |
| 未覆盖 | 35 | 其中 **17 个是纯记账/过渡**，18 个含数学内容 |

**未覆盖中值得补建节点的：3 个**（例 1 的完整计算、Legendre y₂–y₅ 的显式表、
归一化 Legendre φ₀–φ₅ 的显式表）。另有 **6 处只需给现有节点补锚点的 FIX**，
不需要新节点——最重要的一处是：`apostol:gram-schmidt-process` 的 statement 逐字写着
「formula 15.15」，而**公式 15.15 本体（E11, L80）在全图里没有任何锚点或边证据**。

覆盖率的正确读法：本节 74 条引文只引入 **22 条不同源行**（`exposure.tsv` 的
`distinct_src_lines`，以 `apostol:gram-schmidt-process` 一节点计），而本节有 36 个非空行。
按「新引入源行数」度量而非「池条数」度量，冗余度不低。

死因分布：本节**没有死因 1/2/3/4/5** 的新发现。未覆盖属于完备性缺口（不在六个死因编号内），
第五节抽查出 **1 处死因 6**（引文不支撑断言，B+D 型）与 1 处 C 型嫌疑。

## G4-完备性-15.14.md

*抽自小节：## 〇 结论速览；原文 23766 字符*

本节按句切共 **54 句**（含标题 1 句、公式块 6 句、图注 1 句；不按行号，见下）。

- 已覆盖 **48** = 节点自身锚点直接持有 **19** + 只被边 evidence 持有 **6** + 合法下移到
  part-of 后代 **23**
- 未覆盖 **6**（S5 / S17 / S18 / S21 / S22 / S41）
- 未覆盖中**值得补建 0 个新节点**；有 **2 处值得作为 FIX 补锚点**（挂到已有节点，不新建）

判据说明：不按行号比对。source-lines.tsv 里 `apostol-ch15/15.14.md` 有 12 行的
`n_sentence_end ≥ 2`（第 3/5/7/9/11/13/18/30/36/42/50/58 行），其中第 50 行一行含 6 句、
第 36 行含 5 句、第 18 行含 4 句。按行号会严重高报（过报通道 b），故全部按句核。

**本节没有新的内容缺口。** 7 句未覆盖全部是过渡叙述、图注、或公式块被切碎后的
无信息残片（`That is, we have` 之类）。`report/丢弃与未覆盖清单.md` 未记 15.14 的任何
放弃项，本轮也确认无需追加。

## G4-完备性-15.15.md

*抽自小节：## 〇 结论速览（本节 43 句 / 已覆盖 33 / 下移 0 / 未覆盖 10；其中值得补建 0 个）；原文 18711 字符*

**主控给的前提不成立，这是本轮最重要的一条。**

任务书写「15.15 的例 2（Legendre 多项式）完全没有对应节点」。核实结果：**例 2 有节点，且有两个。**
- `apostol:legendre-polynomial-approximation`（`data/nodes-A2-s3.jsonl:6`，method，sections=15.15，**3 条锚点**，8 条出边）
- `apostol:best-linear-approximation-to-sine`（`data/nodes-A2-s3.jsonl:7`，concept，sections=15.15，**3 条锚点**，3 条出边）——即例 2 里 f(x)=sin πx 的算例

依据：`tmp_index/anchors.tsv` 中 file=15.15.md 的 31 条锚点里，这两个节点占 6 条；
`edges.tsv` 中 ev_file=15.15.md 的 56 条边里，这两个节点作 src 的占 11 条
（`data/edges-A2-bridge.jsonl:4`、`data/edges-A2-s3.jsonl:29–35,38–40`）。
`exposure.tsv`：legendre 节点 T1=3 / T2=8 / T3=0 / 池 11 / 不同源行 6；sine 节点 T1=3 / T2=3 / 池 6 / 不同源行 6。
**建议动作：撤回该前提**，不要按「补建例 2 节点」执行。置信度高。

真实缺口是另一件事，性质轻得多：**5 处已有节点的锚点未覆盖到自己 statement 里已断言的公式**，
其中最严重的一处（例 2 的投影公式，源第 56 行）在全图 921 条锚点 + 1433 条边证据里**一次都没出现过**，
而 `apostol:legendre-polynomial-approximation` 的 statement 明文断言了它。这是死因 6 的合格候选。
全部 5 处都是**补锚点**（FIX），**不需要新建任何节点**，故「值得补建 d=0」。

覆盖口径说明：43 句 = 按句切（不按行切，源第 3 行 nsent=4、第 25 行 nsent=3、第 53 行 nsent=5，
按行号比对会高报，见 `source-lines.tsv`）。33 已覆盖 = 26 由节点锚点直接覆盖 + 5 仅由边 evidence 覆盖（T2）
+ 2 由同义散文锚点覆盖而公式本身无锚。下移 0：本节 `partof.tsv` 只有 2 个父节点有子
（`apostol:theorem-15-16-approximation-theorem` 1 个、`apostol:fourier-coefficients` 1 个），
无一处是「内容从本节句子下移到后代」造成的缺口。

## G4-完备性-15.16.md

*抽自小节：## 〇 结论速览（本节 30 单元 / 已覆盖 18 / 下移 0 / 未覆盖 12；其中值得补建 1 个节点 + 2 处补锚）；原文 16966 字符*

切分口径先说清，否则数字无法复核。本节源文件 77 行，其中 `source-lines.tsv` 显示
L3 / L13 / L23 / L30 的 `n_sentence_end` = 2，L70 / L72 / L74 / L76 = 3，**共 8 行含多句**，
所以全节不得按行号比对（过报通道 b 在这一节是全面生效的，不是个例）。
我按**句 + 独立展示公式块**切成 30 个单元：1 个小节标题、13 个散文句、16 个公式块
（其中 Ex5 的 8 个裸公式块）。公式块单独计数的理由是它们各自承载不同的数学对象
（四组不同向量、两组不同正交族），合并计数会掩盖 1(b) 那一组完全无锚这件事。

- 已覆盖 18 个（单元 2、3、5、6、7、8、9、10、12、13、14、23、24、25、26、27、29、30）。
  其中 4 个是**部分覆盖**：单元 8、13、23、25，锚点只取了该句的一段，句首的空间/内积
  设定被切掉了（D 型，逐条见第一节判据栏）。
- 合法下移（T3）**0 个**。`partof.tsv` 里本节只有两对父子
  （`cx-orthonormalizing-…` ← `cx-cyclic-dependence-…`，`cx-cosine-family-…` ←
  `cx-normalizing-constant-…`），但这两个子节点本身 sections 就是 15.16、锚点也在本文件，
  属于直接覆盖，不是「内容下移到别处」。**本节没有任何单元是靠后代才覆盖的。**
- 未覆盖 12 个：小节标题（单元 1）、Ex1(b) 向量组（单元 4）、Ex3 的 y_n(n≥1) 公式
  （单元 11）、Ex5 的 8 个裸公式块（单元 15–22）、Ex8 的 Compute 句（单元 28）。
- 其中值得动的 3 处：**新建 1 个节点**（Ex5 的 Laguerre 族，属对已登记丢弃项的**部分可回收**
  提案，不是新缺口）、**给已有节点补 2 条锚点**（单元 4 与单元 11 —— 这两处是本轮真正的
  新发现：statement 里明确断言了这两处内容，锚点集里却根本没有对应源行，
  是 E 型「整句完全无锚」而非覆盖率问题）。

冗余度按「新引入的源行数」度量：本节 19 条节点锚点 + 17 条边 evidence = 36 条，
去重后只有 **14 个不同 (file,quote)**（`shared-quotes.tsv` 中 file=15.16-exercises.md 恰 14 行，
每行 n_holders ≥ 2）。即 17 条边 evidence **没有引入任何一条新源行**，T2 对本节的
证据增量为 0 —— 边上的引文全部是节点锚点的复制。这是本节最该记下的一个量。

## G4z-审计覆盖形状.md

*抽自小节：## 〇 结论速览；原文 9170 字符*

16 份报告全部存在（15.01–15.16，无缺节）。以下只谈审计的**覆盖形状**，不重判任何缺口本身。

**覆盖最密的 3 节**（按锚点/句）：15.10（1.76）、15.03（1.42）、15.11（1.39）。
**覆盖最稀的 3 节**（同一指标）：15.09（0.14）、15.05（0.30）、15.16（0.73）。

一句话诊断：**疏密的分界线不是内容难度，而是"正文节 vs 习题节"这条生产分工线**——
四个习题节（15.05 / 15.09 / 15.12 / 15.16）的锚点每句全章最低、`T3` 全部趋零，
而它们恰好是**唯一没有任何 D 层批次、也没有任何 L2 结构节点**参与生产的四节
（`nodes.tsv` 的 loc×sections 交叉表）。但这四节的稀疏**成因不同**：15.12 / 15.16 稀疏是
**内容性**的（纯计算小问 + OCR 残骸，本不该逐句建节点），15.05 / 15.09 稀疏是
**分工性**的（生产方只派了反例代理 CX2，正面命题整批无人建）。
15.09 是全章唯一"低覆盖率是正确信号"的节：4 个节点扛 65 句，报告提出 8 个补建，全章最高。

反向的一句：**覆盖率高不等于质量高**。15.03 覆盖率 97.2% 全章第一，但它自己的结论是
"真正的问题不在完备性而在记账正确性"（两处 sections 漏登/错登）；15.10 锚点最密，
却有 3 个全章最要紧的式子（公理 (3)、范数定义式、定理 15.9(a)）**没有一条锚点含它本身**，
现有锚点是邻句借用（B 型）。这两处覆盖率都看不见。

## G4z-盲区抽查合并.md

*抽自小节：## 〇 结论速览（抽查 45 条，五型各命中 A 3 / B 33 / C 11 / D 39 / E 22 条，五型全清的 1 条）；原文 13241 字符*

**前言**：输入：`report/待应用-提案/G4-完备性-15.01.md` … `-15.16.md` 的第「五」节。
**缺 15.13**：`G4-完备性-15.13.md` 只有〇/一/二三节与「顺带发现」，**没有第五节，无锚点抽查，该节未计入本合并**。
其余 15 节各 3 条，实得抽查条目 **45 条**（不是 48 条）。

一条抽查条目可同时命中多型，故五型之和（108）大于 45。

| 型 | 命中 | 其中判为实质缺陷 | 其中判为合法/已豁免/已声明 |
| --- | --- | --- | --- |
| A 变量·对象错 | 3 | 3 | 0 |
| B 借用引文 | 33 | 7 | 26 |
| C 端点当中间步 | 11 | 5 | 6 |
| D 覆盖范围不足 | 39 | 30 | 9（T2/T3 塌陷豁免或根因在 SPEC） |
| E 整句完全无锚 | 22 | 14 | 8（已标 origin=model 或由 T3 覆盖） |

死因分布（按条目主判）：死因 6 共 **25** 条（其中 1 条落在边而非节点上）、
死因 1 共 **6** 条、死因 4 轻度形态 1 条、死因 0（仅需 FIX 或保留）**13** 条。
建议动作：FIX 32 条、保留（含「保留+可选 FIX」）13 条、**KILL 0 条**。

三条结构性事实：

- **D 型是绝对主型**（39/45），且是唯一一个「命中即多半是实质缺陷」的型（30/39）。
  典型形态不是引文错，而是引文停在断言完成之前，或 statement 的成分数多于引文能兑现的成分数。
- **B 型命中率虚高**（33/45），但只有 7 条有害。33 条里有大量是「节点 + 它自己发出的边」
  重复记账（15.16 报告实测该节 14 组共享引文**无一组是跨节点借用**）。
  B 型的机器候选集必须先剔除自我共享，否则精确度约 21%。
- **A 型极少（3/45）**。三条里两条还不是锚点本身错，而是 statement 的对象名在图内无出处
  （15.07-1）与一条边的 dst 指错空间（15.10-2）。
  即本章的问题几乎不是「引文写错了」，而是「引文管不到断言」。

## G4z-缺口清单.md

*抽自小节：## 〇 结论速览；原文 15625 字符*

**缺口共 21 条**（去重后的「值得补建新节点」条目；按严重度 1/2/3/4 各 **2 / 1 / 3 / 15** 条）。

**缺失的节报告：无。** 16 份 `G4-完备性-15.01.md` … `-15.16.md` 全部存在
（`ls` 于 `report/待应用-提案/` 确认），16 节全部计入。

各节贡献的「值得补建节点」数（取自各报告〇速览的自报数，本清单已去重与重分级）：

| 节 | 自报补建数 | 说明 |
| --- | --- | --- |
| 15.01 / 15.02 / 15.03 / 15.04 | 0 / 0 / 0 / 0 | 四节一致：缺的是锚点不是节点 |
| 15.05 | 5 | 全部来自习题（正面例题无人建） |
| 15.06 | 1 | **正文例子**，本清单判严重度 1 |
| 15.07 / 15.08 | 0 / 0 | 只需补锚 |
| 15.09 | 8 | 全部来自习题 |
| 15.10 / 15.11 | 0 / 0 | 只需补锚（15.10 有 3 处受 30 字符下界卡死） |
| 15.12 | 3 | 全部来自习题 |
| 15.13 | 3 | 1 条正文例子（严重度 1）+ 2 条可由 parent 导出 |
| 15.14 | 0 | 无内容缺口 |
| 15.15 | 0 | **任务书前提被推翻，见下** |
| 15.16 | 1 | 习题，但对象全图零提及 |

**三条要先说的判断：**

1 **任务书的严重度 1 示例不成立。** 任务书写「15.15 例 2 的 Legendre 完全无节点」，
  15.15 报告核实为**有两个节点**：`apostol:legendre-polynomial-approximation`
  （`data/nodes-A2-s3.jsonl:6`，3 锚点 8 出边）与 `apostol:best-linear-approximation-to-sine`
  （同文件 :7，3 锚点 3 出边）。我复核 `nodes.tsv`：两 id 均在表内，`grep -c` 各 1 命中。
  **建议动作：撤回该前提**，不要按「补建 15.15 例 2 节点」执行。置信度高。

2 **本章没有「证明步骤缺节点」的缺口。** 16 份报告里所有与证明步骤相关的缺口
  （15.04 S8、15.08 P1–P4、15.13 FIX 1–5、15.14 FIX-1/2）**全部是缺锚点、不是缺节点**。
  故严重度 2 只有 1 条，且它本身是习题命题被现有节点当依据用。

3 **本章缺节点的主体是习题（15 / 21 条）。** 15.05、15.09、15.12、15.16 四节的正面命题
  与正面例题系统性无人建；15.05 报告指出成因是任务分配空档
  （只有反例代理 CX2 扫过，它只建「不是线性空间」的节点）。这不是逐节疏漏，是分工缺口。

## G5-P01-cause6 入 SPEC.md

*抽自小节：## 〇 裁定；原文 13319 字符*

**前言**：只读代理产出。`data/` 与 `tools/` 一个字节未动，未跑 `tools/` 下任何脚本。
所有数字来自 `tmp_index/` 的 12 张表，用一次性 python 复算，复现算法写在第四节。

**有条件进 SPEC。** 死因 6 是唯一一个**必须跑回查程序才能定案**的编号，不定义它等于让
「引文—断言」链条上最常见的失败模式无编号可挂；条件是它的判定程序要挂在四层证据池的定义上，
而那个定义由 S2 项 2 提供，两条必须同批落地，否则 6 的判定程序无处挂靠。


#### 一 现状取证

#### 1.1 SPEC.md 相关行逐字

`SPEC.md:100`（整行，逐字）：

```
死因编号:`1` 量词过度断言 / `2` 编造成员归属 / `3` 伪造教材出处 / `4` 数学错误 / `5` 伪抽象·层塌缩。
```

`SPEC.md:98`（整行，逐字）：

```
每条裁决格式:`<id> | KEEP|KILL | 死因编号 | 一句理由`。
```

`SPEC.md:93–94`（审计 A 的职责，逐字）：

```
- **审计 A(正确性与归属)**:数学是否正确;`members` 是否真的属于它;锚点/引文是否真在书里。
  逐条核对,不放过量词("所有/仅/恰好")。
```

三处合起来构成本项的全部缺口：`:100` 无 6 也无 0；`:98` 只有两档，没有任何一档对应
「引文存在但管不到」的处理动作；`:93` 说的是「锚点/引文**是否真在书里**」——
这句话精确地只覆盖了死因 3，而**死因 6 问的是另一件事**（引文真在书里、但不覆盖被指控的断言），
SPEC 现有文本连这个问法都没有出现过。

#### 1.2 已有提案里的既有结论

`S2-SPEC-字段语义.md` 项 1（第 43–150 行）已把这一项做完：裁决「6 入，0 也入」，
给了 3/6/1 的三环互斥表，给了替换 `:100` 与 `:98` 的逐字文本，并报「须改数据 0 个节点、0 条边」。
`S2` 项 2（第 176–265 行）给出四层证据池 T1/T2/T3/TD 的定义与 TD 排除的理由。
`S1-SPEC-锚点规则.md:107` 从相反方向补了一条：锚点窗口变长会**加重** cause 6 的判读负担。
`S3-SPEC-关系词与审计程序.md:189–215` 独立地也在改 `SPEC.md:98`，把两档改三档 KEEP/FIX/KILL。

#### 1.3 我的复核结论

**S2 项 1 的定性裁决站得住，三环互斥表可用，但它的两处量化表述需要改，且它与 S3 在 `:98` 上撞车。**

**(a) 三环互斥表复核为真，并补一个 S2 没写的判据。** 我用 `apostol:zero-element` 这个 S2
点名的争议例逐条核过：它的 T1 两条锚点是 `15.02.md:85`「AXIOM 5. …There is an element in V,
denoted by O, such that」与 `15.04.md:57`「Axiom 5 tells us that there is at least one zero
element.」；而它的 statement 第二句是「In a function space the zero element is the function
whose values are everywhere zero.」——这句的引文（`15.03.md:19`「The zero element is the
function whose values are everywhere zero.」）只出现在 `data/inherited/edges-A1.jsonl:18`
的 evidence 上，而那条边是 `apostol:function-space --requires--> apostol:zero-element`，
**它是 TD，不是 T2**。所以按四层池，`apostol:zero-element` 的第二句确实无池内引文覆盖，
cause 6 指控成立；S2 转引的「审查员看对了现象、归错了类」说的是当初打的编号不对，不是指控不成立。
这个例子同时证明：**判 6 之前必须先分清一条 evidence 是 T2 还是 TD，光看「图里有没有这句引文」会误判**。

**(b) S2 的 cause 6 暴露面代理量（「statement 句数 > 锚点数」= 271/522 = 51.9%）算法有偏，
应改用源行口径。** 我按同一算法复算得 273/522 = 52.3%、总句数 1149 / 总锚点 921
（S2 报 1142/921），差异来自切句时对 `$…$` 的屏蔽细节，量级一致，不影响它的用途。
但这个代理量有一个更实质的问题：**它拿「锚点条数」当分母，而多条锚点可以落在同一源行上**
（`apostol:orthonormal-set` 的 2 条锚点＋2 条 T2＋5 条 T3 共 9 条池条目，去重后只有 3 条不同
`(file,quote)`、**只覆盖 2 条源行** `15.11.md:3` 与 `15.11.md:35`）。这正是任务书点的过报通道 c。
按「新引入源行数」重算（算法见第四节），546 全口径：**按 T1 源行 327 个候选，按 T1+T2+T3 源行
263 个候选**。S2 的 332 句上界仍成立，可继续用来排优先级。

**(c) S2 说「含 TD 的池子会把几乎所有 cause 6 洗掉」，方向对，量级夸大了。** 实测：
若误把 TD 计入，候选从 263 降到 222，**只洗掉 41 个 = 15.6%**，不是「几乎所有」。
TD 会给 198 个节点新增 496 条不同 `(file,quote)`。所以 TD 排除的理由**不能靠体量论证**，
必须靠语义论证（下游消费者引了某行 ≠ 上游节点引了它），S2 §项2 的 `d:orthonormal-exp

…（截断，全文 13319 字符）

## G5-P02-30 字符地板豁免短行间公式.md

*抽自小节：## 〇 裁定；原文 10692 字符*

**前言**：> 产出性质：**提案**，供主控串行改 `SPEC.md`。本代理只读，未改 `data/` 与 `tools/`。
> 本文所有数字均为本轮独立实测（脚本写在 `tmp/`，已保留 `tmp/g5_*.py`）。

**有条件进 SPEC，但不是任务书与交接文档主张的那个方案。**
一句话理由：**保留 30–200 字符界，改掉「不跨行拼接」这一条，把唯一性作为附加闸门而非替代判据**——
实测「改用唯一性判据」会让 174 条短行立刻合法（其中 138 条是标题/散文/HTML，只有 36 条是数学），
唯一性在本语料上根本不构成约束（apostol-ch15 里长度 10 的文件内唯一子串就有 38,866 条）。

## G5-P03-定理 15.9(a) 这个反例.md

*抽自小节：## 〇 裁定；原文 12542 字符*

**有条件进 SPEC**：不设豁免条款、也不改用唯一性判据，而是把 S1（P02）已裁定的「向上扩窗 + 唯一性闸门」的**触发条件从「行间公式」放宽到「断言独占一行的短行」**，并补一条向上越界时的向下退让规则——因为 S1 现行条文的字面触发条件**并不覆盖** 15.9(a)，该反例在 S1 落地后仍然无解。

**先纠正任务书的一处事实错误**：定理 15.9(a) **不在** `15.09-exercises.md`，而在 `15.10.md:123`。`15.09-exercises.md` 是「§15.9 习题」，与「定理 15.9」是两个不同的东西（定理 15.9 出现在 §15.10 正文里）。全语料 `grep -n "15\.9"` 只有 4 处命中，`15.09-exercises.md` 里的那处是标题 `## 15.9 Exercises`。本条依据全部取自 `15.10.md`。

## G5-P04-3 锚点上限是否提高.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 21960 字符*

**有条件进 SPEC：数字上限维持 3 不动，改为「3 条 + 按 `sections` 计的追加槽位」的软上限，
同时新增一条真正咬得住「抄整节」的规则——同节点内两条锚点不得落在完全相同的源行集合。**

一句话理由：实测**没有一个节点因 3 条上限而被迫留下无锚断言**——81 个满额节点里，
只有 11 个的 `statement` 句数 ≥3 且无 part-of 后代（T3 救不了），
而这 11 个的无锚句里逐一核过后，**没有一句是「书里有原文、只是锚点槽用光了」**：
它们的无锚句全部是模型自己的对比判断（书上根本没有可引的字），提高上限对它们无效。
真正被 3 条卡住的是另一类——**跨 section / 跨教材节点**：22 个节点的锚点没覆盖到它自己声明的
全部 `sections`，其中 6 个已在 3 条满额，扩不了。所以要动的不是「3」这个数，
而是「3 是按节点算还是按 section 算」。

## G5-P05-origin-model 后是否允许无锚.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 17393 字符*

**前言**：只读分析代理产出。**本文是提案，不是执行结果。** 未改动 `data/` 下任何文件，未运行任何校验脚本。

**有条件进 SPEC。** `SPEC.md:53` 与 `:55` 确实互相矛盾（:55 第二分支的前提「找不到可逐字引用的
依据」在 :53 无条件成立时永远无法执行，是死条文），消解方案是：**把「无锚」从「找不到锚点」
收紧为「原理上不可锚」，用一个封闭词表的新字段 `unanchorable` 显式登记，并给
`tools/verify_anchors.py` 补一条对无该字段的无锚节点判失败的检查**——不这样做，豁免默认对全部
522 个节点生效，等于把唯一硬校验作废。

配套的关键更正：`origin: model` **不是**豁免锚点的凭据。现有 24 个 `origin: model` 节点里
**20 个有锚点**（共持 37 条锚点），说明这个字段在既有用法里只表示「statement 的内容出自模型」，
与「有没有锚点」正交。把 :55 读成「标了 model 就可以无锚」会一次性授权全部 24 个节点无锚。

## G5-P06-verify_anchors.py 判失败范围.md

*抽自小节：## 〇 裁定；原文 13777 字符*

**前言**：> 产出性质：**提案**。本代理只读，未修改 `data/`、`tools/`、`SPEC.md` 中任何文件。
> 本文所有数字为本轮实测（脚本写在 `tmp/g5_p06_*.py`，只读 `data/` 与 `source/`），
> 并与 `tmp_index/` 的 anchors.tsv / nodes.tsv / edges.tsv / exposure.tsv 交叉核对一致。

**有条件进 SPEC。** 长度违规**应该**判失败（今天 0 新增失败项，零成本，纯防回归）；
无锚点节点**不能一律判失败**，必须改成「无锚点 **且** 无豁免字段」才判失败——
今天的 28 个无锚点节点全部落在两条既有豁免里（24 个 L2 有 `layer`+`members`，
4 个 `d:` 有 `origin=model`+`origin_note`），一律判失败会立刻造出 28 个假失败。

一句话理由：**判失败的对象不是「没锚点」，是「没锚点也没交代为什么没锚点」**；
而长度界与「不跨行」这两条 SPEC 明文规则至今零机器覆盖，是纯粹的实现遗漏，补上不花钱。

## G5-P07-parent 是否仅指分解关系.md

*抽自小节：## 〇 裁定；原文 14810 字符*

**前言**：只读分析代理产出。**这是提案，不是执行结果**——`data/` 与 `tools/` 一个字节都没动。
所有实数均出自 `tmp_index/` 的预算表（nodes.tsv / partof.tsv / edges.tsv / pool.tsv / exposure.tsv），
交叉方式写在第四节的算法里，可复算。

**有条件进 SPEC：`parent` 单义化为 part-of，且降格为「派生冗余字段」——语义以边为准，
`parent` 只是挂载点的缓存；两者都保留，但必须加一条机器可查的一致性约束。**

一句话理由：实测 287 个带 `parent` 的节点里 **284 个（98.96%）的 `parent` 恰等于它唯一的 part-of 父**，
剩下 3 个的真实关系是 is-a/实例而非成分——即生产者事实上已经把 `parent` 当 part-of 用了，
SPEC 只需把这个既成惯例写成规则并处理那 3 个例外，不必引入多义。

## G5-P08-节点是否可带 origin.md

*抽自小节：## 〇 裁定；原文 12726 字符*

**有条件进 SPEC**：节点可带 `origin`，取值 `source` / `model` / `mixed`，且必须与边的 `origin`
分开定义（同名不同义）；条件是同时补上 S2 漏掉的两条——`origin_note` 与 `origin` 的双向绑定
（S2 只写了单向）与导出侧的可见性要求（S2 完全没碰）。


#### 一 现状取证

#### 1.1 SPEC.md 里与节点 origin 相关的全部文字（逐字）

`SPEC.md:55`（在「锚点规则」小节内，而锚点是**节点**字段）：

```
- 找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。
```

`SPEC.md:70`（边 schema，`origin` 唯一被正式定义的位置）：

```
{"src":"<id>","rel":"requires","dst":"<id>","origin":"source|model","evidence":{...},"rel_note":"..."}
```

`SPEC.md:73–74`：

```
- `origin: source` 必须给逐字 `evidence`(机器校验)。`origin: model` 可省 `evidence`,
  但**必须如实标注,不要把推断伪装成原文**。
```

`SPEC.md:59–65` 的节点 schema **不含** `origin`，也不含 `origin_note`。
即：SPEC 在 `:55` 已经默许节点带 `origin`，却只在边上定义了它。这是本待议项的全部由来。

#### 1.2 已有提案的结论

`S2-SPEC-字段语义.md` 的**项 3 就是 P08**（该文件 `:34` 摘要行、`:296–460` 正文），
已给出裁决「可以，且必须与边的 origin 分开定义」、三值取值、`origin != source` 时 `origin_note`
必填，并附了可并入文本与影响面表。**我不重做分析，只复核依据。**

#### 1.3 我的复核：S2 站得住的部分

逐条实测（脚本只读 `data/`，未改任何文件）：

| S2 的断言 | 我的实测 | 判 |
| --- | --- | --- |
| 522 节点中 `origin` 33 个（model 24 / source 9） | 33（model 24 / source 9） | ✅ |
| `origin_note` 39 个 | 39 | ✅ |
| 有 `origin_note` 无 `origin` 的 16 个（CX2 15 + `nodes-D2.jsonl:9`） | 16，名单完全一致 | ✅ |
| 锚点全在 `*-exercises.md` 且无 `origin` 的 49 个（A2-x2 34 + CX2 15） | 49，分布一致 | ✅ |
| 须改的**边** 0 条 | 边侧规则不动，0 | ✅ |
| 若 `source` 也必填则须改 489 个 | 489（522 − 33）；含 24 个 L2 则 513 | ✅ |
| `nodes-A2-x2.jsonl:32` 是确定的 `mixed` | 逐字核过 `15.16-exercises.md:70` 与 `:72`，题面确实写出了答案 `g = \frac{1}{2} \log 3` 与 `g = \frac{1}{2}(e^2 - 1)`，而「Both are the average value of f over the interval」不在题面 | ✅ |

`mixed` 这个取值的必要性我也认了：`nodes-A2-x2.jsonl:32` 一个 statement 里同时含题面给出的两个值
与模型加的解释，二值下标 `source` 或 `model` 都是假标注。

#### 1.4 我的复核：S2 错了或漏了的三处

**(1) 影响面漏算 10 个节点，49 应为 60。** S2 影响面表写「已有 `origin` 33 个 → **不动**」。
但按 S2 自己提出的规则「`origin` 为 `model` 或 `mixed` 时 `origin_note` 必填」，
实测 **`origin=model` 而无 `origin_note` 的节点有 10 个**，它们恰在那 33 个里，
按新规立刻违规，必须改：

```
data/nodes-D1.jsonl:23  d:axiom-10-is-derivable-from-apostols-form-of-axiom-6
data/nodes-D4.jsonl:15  d:positivity-axiom-forbids-nonzero-self-orthogonal-elements
data/nodes-D4.jsonl:19  d:normalized-element-has-norm-one
data/nodes-D4.jsonl:20  d:rescaling-preserves-orthogonality
data/nodes-D4.jsonl:23  d:vanishing-projection-terms-in-the-legendre-computation
data/nodes-D4.jsonl:27  d:s-perp-closure-follows-from-linearity-in-the-first-argument
data/nodes-D4.jsonl:28  d:orthogonality-to-a-set-equals-orthogonality-to-its-span
data/nodes-D4.jsonl:40  d:pythagorean-expansion-of-the-squared-norm
data/nodes-D4.jsonl:43  d:projection-is-the-s-component-of-the-orthogonal-d

…（截断，全文 12726 字符）

## G5-P09-sections 语义.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 12015 字符*

**有条件进 SPEC。** 取语义 A（`sections` = 该节点内容的**出处**，不是「与哪些节相关」），
但必须拆成一条硬机器规则（正向：每条锚点的 `file` 所对应的节**必须**在 `sections` 里）
加一条审计规则（反向：每个声明的节应有见证，见证放宽到 T1∪T2，且不对无锚 L2 生效）——
S2 总览表只给了「语义 A / 12(+1)」这个结论与数，正文未写到项 4；我复核后确认 **12 这个数正确**，
但它只覆盖正向，反向另有 **22 个**节点违规，S2 的表漏了这一半。

## G5-P10-apostol-ext- 命名空间是否合法.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 14488 字符*

**有条件进 SPEC。** `apostol:ext-` 合法，但它表示的**不是**任务书猜测的「教材没有、为组织
图谱而加的扩展节点」，而是「**所指对象在第15章之外（第12章 / 第II卷），但第15章在正文里
点名征用了它**」——11 个节点的锚点全部落在第15章正文的引用句上，没有一条来自章外，
所以它们不是模型虚构，而是**章外依赖的记账位**；因此它与 `origin` **正交**，
「所有 ext- 必须标 origin:model」**是错的**（现有 9 个正确地标着 `source`），必须写成
反向规则：**ext- 节点的 origin 由「第15章有没有点名出处」决定，而不是由 ext- 前缀决定**。

一句话的判据差别：`ext-` 说的是**所指在哪里**，`origin` 说的是**statement 的内容从哪来**。

## G5-P11-需不需要「二次使用点复用」关系词.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 16007 字符*

**不新增关系词，但 S3 的决定性论证不成立，须换一条论证再进 SPEC。**
「复用」在数据里实际分裂成两个已有栏位分别承担的东西——前向引用由 `requires`（方向反写即可）承担，
论证模式的重复出现由 L2 的 `members` 承担（255 条成员引用、188 个不同下层 id 记的正是「同一模式在多节复现」）；
新增关系词会与 `members` 开第二套账，这才是不扩词的决定性理由。

S3 用的理由是「`sections` 是数组，57 个节点已靠它记多节出现」（`S3:66–68`）。
这条理由在 P09 裁定 `sections` = 语义 A（出处，**不是**被用到）之后失效：
语义 A 下 `sections` 恰恰**不**记第二使用点。裁决方向对，论证须替换。

## G5-P12-每个 statement 句子是否都必须被锚点管到.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 14955 字符*

**有条件进 SPEC，但条件是「不得要求每句都有锚」。**

一句话理由：抽样实测 10 节点 19 句，即使把 T1/T2/T3 三层证据池全算上仍有 4 句（21%）
完全无证据、6 句（32%）只有部分射程；这些句子多数是**模型的元评论与量词化概括**，
它们要么本来就不该由锚点承担，要么根本不可能在原文找到逐字依据 —— 强制「每句必有锚」
只会把这 10 句里的绝大多数逼成 D 型（硬凑一条邻近 quote，宣称覆盖而实际管不到），
把一个 grep 查不出但**人一眼能看出无锚**的缺陷，转译成一个**人也查不出**的假合规。
因此进 SPEC 的是它的对偶形式：**无锚句必须显式标记，而不是必须补锚。**

## G5-P13-B2 §6.1 枢纽节点豁免 FIX.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 16461 字符*

**前言**：只读代理产出。**本文件是提案，不是执行结果。**未改 `data/` 与 `tools/` 下任何文件。
本文所有计数均在 `tmp_index/` 的 12 张 TSV 上实测，算法逐条写在 §四。

**豁免本身不进 SPEC；「枢纽性不得作为裁决理由」这条禁令进 SPEC。**理由：枢纽性量的是图拓扑、
删除测试量的是信息含量，用「它下面挂了东西」去抵消「它没有增量」是把工程成本记进了正确性账；
正确做法是裁决照旧判 KILL，把拓扑损伤挪到执行期的一次孤儿检查里处理。

方向上我与 S3 §二一致（它已裁「进 SPEC，但须先补 FIX 层」），但**S3 的两处实测数字是错的**，
其中一处错在它自己选的旗舰例证上（详见 §一 复核 1、复核 2）。本条文以复核后的数字重写，
并补上 S3 没写的一条：**5 个待杀枢纽没有可改指的祖父节点**，S3 的孤儿检查在这 5 处无法执行。

## G5-P14-没有 parent 的层如何做删除测试.md

*抽自小节：## 〇 裁定；原文 13664 字符*

**前言**：只读代理产出。**本文件是提案，不是执行结果。**未改 `data/` 与 `tools/` 下任何文件。
所有计数均在本次会话实测（脚本写在 `/tmp/`，用完即弃，算法逐条写在第四节）。

**有条件进 SPEC**：把删除测试的对照从「父 + 兄弟」改写成按优先级唯一确定的
**恢复基 R(n)**（六档，首档命中即止），`members` 只是其中第二档、仅覆盖 24/546 个节点；
条件是同时写入「同层同批次节点不得入基」这条禁令——实测 6/24 条 L2 判决违反它，其中 4 条构成互证塌缩。

任务书提示的「用 `members`」方向正确但**射程不足**：无 `parent` 的节点共 **259 个**，
`members` 只覆盖 24 个（9.3%），剩下 235 个仍无对照。所以本条必须给的是一张全覆盖的优先级表，
不是一条 L2 专用规则。

## G5-P15-删除测试作用域：父+兄弟 还是全图.md

*抽自小节：## 〇 裁定；原文 15097 字符*

**前言**：只读代理产出。**本文件是提案，不是执行结果。**未改 `data/`、`tools/`、`SPEC.md` 下任何文件。
本文所有计数均由 `tmp_index/` 的 TSV 与 `report/audit-*-verdicts.jsonl` 现场算出，
脚本写在系统临时目录、用完即弃，算法逐条写在 §四。

口径声明：节点 546（= check_graph 的 522 + `structures-L2-*.jsonl` 的 24 个 L2）。
「作用域大小」一律指**去重后的无序节点对数**，不是有向计数。
`shared-quotes.tsv` 的 holder 含 `edge:`；本文除特别注明处，一律把边 holder 归到该边的 **src**
（依四层证据池：边证据入 src 的 T2，不入 dst 的池）。

**有条件进 SPEC，分级方案。**
删除测试（死因 5 的判据）的作用域**写死为 P14 的恢复基 `R(n)`，不得是全图**；
跨分支重复另设一道**批次级的全图引文重复扫描**，但那道扫描的产物**只能是 FIX / 合并 / 补边建议，
永远不得当作 KILL 依据**。一句话理由：全图作用域要多算 **89.9 倍**对数（148785 对 vs 1655 对），
而它多捞到的 295 对跨基共引里 statement 词面重合 ≥0.35 的仅 3 对、<0.20 的 143 对（89%），
绝大多数是记账问题不是信息含量问题；且全图作用域把互证塌缩风险从 4 条抬到 11 条（B2 一批 1 → 3）。

## G5-P16-B2 §4(c) 禁止重复建 operations 节点.md

*抽自小节：## 〇 裁定；原文 11400 字符*

**前言**：只读代理产出。**本文件是提案,不是执行结果。**未改 `data/` 下任何文件。
计数口径：节点 546（含 `structures-L2-*.jsonl` 的 24 个 L2）；`check_graph` 口径 522 = 546 − 24。
本条只用 522 口径内的节点（L2 无 `parent`、无 `atomic_reason`，与本条无关）。

**有条件进 SPEC。** B2 §4(c) 的原话（「禁止把 parent 已写出的运算再单立一个节点」）作为规则
是对的，但它按**主题**（运算）划界，因此同时打中了合法原子化；正确的界不在主题而在
**「parent 是否逐字写出了该运算规则」加「子节点是否给出 parent 与兄弟推不出的一条判据」**
这两个条件的合取。按主题写会误杀 17 个公理分解节点；按这两个条件写，
它们一个不损。

**本轮对 §五 的复核修正了本节初稿的一处过头话**：B2 那四支并非「全部落网」，而是
**落网 2 支（例 1、例 2）、改判 2 支（例 3、例 4）**；例 4 的真死因是 6 不是 5（§5.2）。
同时本条文抓到一个 B2 从未审过的违规（`d:subspace-inherits-the-operations-of-the-ambient-space`，§5.3）。
置信度：例 1/2/4 与 5.3 那一个为高（依据均为逐字对读）；例 3 为中
（「componentwise」算写出还是只提到，处在 §二 分界的临界处，建议主控明示）。

## G5-P17-共享前提能否单独建节点+用边连.md

*抽自小节：## 〇 裁定；原文 14126 字符*

**前言**：只读代理产出。**本文件是提案，不是执行结果。**未改 `data/` `tools/` `SPEC.md` 下任何文件。
一切计数均在本次会话用 `tmp_index/` 的 12 张 TSV 实测，算法写在第四节，脚本用完即删。

Sanity check：节点 546（= check_graph 报的 522 + `structures-L2-*.jsonl` 的 24 个 L2 节点）、
边 1433、`requires` 边 707。本文出现的所有分母都注明是 546 还是 522 口径。

**有条件进 SPEC：追认「单独建节点 + `requires` 边」为唯一合法记账形式（禁止两处各写一遍），
但给建节点加一道可机械执行的闸门——该节点必须自带一条逐字陈述该前提的锚点。**

一句话理由：现状已经在 132 个节点上、由 572 条 `requires` 边一致地采用「单独建节点 + 边」，
而「两处各写一遍」的实例是 **0** 条（546 个节点里没有任何一对 `statement` 全文相同）；
SPEC 已在第 45 行禁止「造新 id 重复表达同一对象」，本条只是把这条禁令从 id 层落到内容层。
真正需要新增的不是选择哪种做法，而是防止「造出教材里没有独立地位的节点」这一具体滥用。

## G5-P18-边的 evidence 能否算节点的锚点.md

*抽自小节：## 〇 裁定（进 SPEC / 不进 SPEC / 有条件进；一句话理由）；原文 15564 字符*

**有条件进 SPEC：二分入条，不合一。** 边的 `evidence` **不算**节点的锚点（`anchors` 的
「至少 1 条」下限只认 T1），但**算**审计死因 6 时的证据（证据池 = T1+T2+T3，TD 排除）。

一句话理由：这道题被问错了——它看起来是合规性问题，实测下来是**审计判定问题**。
`exposure.tsv` 全 546 行里 **T1=0 且 T2>0 的节点是 0 个**（28 个零锚点节点的 T2 全为 0），
所以「T2 算不算锚点」对锚点下限的影响**恰好是零**；而它对死因 6 的影响覆盖 **205/522 个节点**。
承认 T2 等价于锚点没有任何合规收益，代价却是 86 个节点破 3 锚点上限、共享引文组 +60%。

**因此 `verify_anchors.py` 不需要为本条改动。** 简报里「要么承认 T2 等价于锚点（则
verify_anchors.py 要改）」这个二选一的前一支，其前提（存在靠 T2 过下限的节点）实测不成立。

## G6d-D1-片31-56-摘要.md

*抽自小节：## 〇 速览；原文 10120 字符*

**前言**：来源：`report/审查-形式D-D1-片31-56.md`（只读压缩，未做新裁定）。
分片编号约定：该片不用「L 几」，用 `data/nodes-D1.jsonl` 的**文件行号**（31–56）+ 自编 `D-n` / `E-n` 标签，本摘要沿用两者。

本片共 26 条裁决（26 个节点，1 节点 1 裁决，无遗漏无抽样）：
**KILL 0 / FIX 20 / 保留 6；未决 5 组**（均为分片声明的「不判型」，非条目级悬置）。

- 唯一带 KILL 字样的是行 43，分片写「KILL 或强改」，实际给出了具体改法，倾向 FIX；合并时需裁一次。
- 死因分布（按分片自报）：`4` × 1（行 43）、`1` × 3（行 43 第 2 claim、35、38）、`6` × 2（行 52、40 若不补锚）、`0` × 需改的其余 14 条、无死因保留 6 条。
- 分片自报形态计数：形式 D 4 例（行 42/50/52/55，四者同时另有形式 E 从句）、形式 E 20 例；20 个缺陷节点的缺陷 100% 落在或延伸到 statement 末句。

## G6d-D1-片57-81-摘要.md

*抽自小节：## 〇 速览；原文 11093 字符*

**前言**：来源：`report/审查-形式D-D1-片57-81.md`（唯一输入，只读）。覆盖 `data/nodes-D1.jsonl` 第 57–81 行共 25 个节点，分片自称 25/25 全审完。
本摘要只做压缩与记账核对，不作新裁定。

本片共 25 条裁决：**KILL 0 / FIX 13 / 保留 10 / 进 SPEC 2**；分片自认未决或悬置 4 条。

- 无一条建议 KILL。分片全部缺陷都判为可补锚或可改写。
- 形式 D 确认 6 条（#58、#62、#67、#69、#75、#80），全部落 **死因 6**，其中 3 条兼另一死因（#75 兼 4、#80 兼 1、#58 兼 1）。
- 形式 E 确认 4 条：#60 单发（判死因 6），#72/#74/#78 一组（**归因源材料范围，明确不判生产者**，死因 0）。
- 另有 #68 一条纯数学缺陷（死因 4/1，与 D/E 无关），#70、#64 各一条锚点/引用错指（分片未编死因号）。
- #57、#61 归 SPEC（30 字符下限 + 3 锚点上限），死因 0。
- 分片强调的正面对照：#71（两锚协同推出结论，本片锚点质量最好）、#77（见证无隐含限制，范例）、#79（本片唯一末句落在锚点射程内者）。

## G6d-D3-片21-45-摘要.md

*抽自小节：## 〇 速览（本片共 25 条裁决：KILL 0 / FIX 23 / 保留 2；未决 10 项 + 3 处死因待定）；原文 15020 字符*

**前言**：来源：`report/审查-形式D-D3-片21-45.md`（仅读该一份）。本文件只做压缩，不做新裁定；
凡我用 `tmp_index/` 复核过的数字，标注表名。

- 覆盖：`data/nodes-D3.jsonl` 行 21–45，25 个节点全审，无抽样。sections 实测：15.10 共 23 个、15.11 共 2 个（nodes.tsv 已复核一致）。
- 形态分类：形式 D（射程不足）3 例＝行 25、33、43；形式 E（无锚从句）20 例；支撑充分 2 例＝行 37、42。行 30 兼 D/E 两型，分片归入 E-2。
- 死因：分片对全 25 条一律给 `0`（KEEP + 定点补锚/改写），**无 KILL、无 `4`（数学错误）**。三处给了从严备选：行 33 claim 3 → `6`；行 43 末句 → `1`；行 41 claim 2 → `1` 低度候选。
- 锚点额度：25 节点共 37 条锚点，无一节点用满 3 条，13 个节点只用 1 条（nodes.tsv 逐条复核，n_anchors 与分片表完全一致）。
- 分片核心判断：缺陷模式是证据纪律，不是数学能力——20 例形式 E 的无锚 claim 经人工核真**全部为真**，唯一例外是行 41 一处边界量词。

## G6d-D3-片46-69-摘要.md

*抽自小节：## 〇 速览；原文 10119 字符*

**前言**：来源：`report/审查-形式D-D3-片46-69.md`（唯一输入，只读）。覆盖 `data/nodes-D3.jsonl` 第 46–69 行共 24 节点，全部落在 15.11（另 2 条跨文件锚指向 15.10.md）。本摘要只压缩，不新增裁定。

本片共 24 条节点级裁决（按节点计，非按 claim 计）：**KILL 0 / FIX 15 / 保留 9；未决 0 条节点**，但**死因编码二选一未定 4 条**（L51、L66、L69 的 1 vs 0+强制改写；L67 的 0+强制改写 vs 4），分片明确把取舍权交裁定员。

- 死因 1（量词过度断言）建议：3 条 —— L51、L66、L69
- 死因 0 + 强制改写：3 条 —— L57、L67、L65（L65 为术语改写）
- 死因 0 = 保留 + 补锚：9 条 —— L46、L47、L48、L49、L50、L52、L53、L58、L61
- 死因 0 = 保留、无需动作：9 条 —— L54、L55、L56、L59、L60、L62、L63、L64、L68
- 死因 2 / 3 / 4 / 5：**0 条实质实例**。唯一可升 4 的候选是 L67 claim 2 的定位性假陈述（"两节之内"），非数学错误。
- 分片自报计算值 0 错（7 项模型自算值 + 3 项反事实/刻画核验，全部人工验算通过）。

## G6d-D4-片1-27-摘要.md

*抽自小节：## 〇 速览（本片共 27 条裁决：KILL 0 / FIX 12 / 保留 15；未决 0 条节点级，另有 7 项声明性未覆盖）；原文 12305 字符*

**前言**：来源：`report/审查-形式D-D4-片1-27.md`（只读该一份）。对象：`data/nodes-D4.jsonl` 第 1–27 行，主题 15.13（Gram-Schmidt / Legendre）+ 15.14（S⊥）。
本摘要只压缩，不作新裁定；新发现一律进「顺带发现」。

- 分片自己的三分法：形式 D **8** 条（行 4、10、14、16、20、22、23、27）／形式 E **6** 条（行 3、7、9、15、21、25，内容全经核为真）／支撑充分 **13** 条（行 1、2、5、6、8、11、12、13、17、18、19、24、26）。8+6+13=27，核对无误。
- 折成动作：**KILL 0 条**。**FIX 12 条** = 8 条形式 D 全部（改 statement 或补/换锚）+ 4 条形式 E（行 3、9 补 `origin: model`；行 7 改「exactly why」；行 21 补锚 `15.13.md:77`）。**保留 15 条** = 13 条支撑充分 + 形式 E 中的行 15、25（已标 model 或已写对限定，无需动）。
- 死因：分片给出的**唯一实质死因是死因 1**（量词过度断言），落在行 14；行 22、4、10 分片写「0（改写）；严办为 1」，属边界档。行 27 写「改写后 0；若严办为 6」。其余 22 条死因 0。
- 分片自称「最严重一条」：行 14 的 `exactly two load-bearing places`，被 `15.11.md:15` 反例推翻，且图内缺一条对应边。
- 无节点被判 KILL、无节点被判撤回；分片对全部 27 条的处置建议都是 KEEP + 改写/补锚。

## G6d-D4-片28-54-摘要.md

*抽自小节：## 〇 速览（本片共 27 条裁决：KILL 0 / FIX 17 / 保留 10；未决 2 条）；原文 11350 字符*

**前言**：来源：`report/审查-形式D-D4-片28-54.md`（状态：已完成）。覆盖 `data/nodes-D4.jsonl` 第 28–54 行，
共 27 个节点，分片自称无遗漏。本摘要只压缩，不新增裁定。

- 分片对 27 个节点全部给出裁决，**无一条 KILL、无一条撤回**：17 条判 FIX（死因基本为 0，
  即断言为真、只需补锚/换锚/补 `origin: model`/改措辞），10 条判支撑充分即保留。
- 缺陷分型：形式 D（射程不足）9 例——29、36、42、43、44、45、46、48、49；
  形式 E（无锚从句）8 例——28、30、31、34、35、38、51、54（36 兼有 E 从句，计入 D）。
- 两条全片区元观察：(1) 27 个节点**没有一个用满 3 条锚点**（4 个用 1 条、23 个用 2 条），
  故 17 处缺陷一处也不能归因于 SPEC 3 锚点上限；(2) 缺口高度集中在 statement 末句。
- 真正被 SPEC 30 字符下限阻挡的只有 2 条公式行，影响 3 处 claim，均不判生产者。

## S1-SPEC-锚点规则.md

*抽自小节：## 0. 基线核对（本轮独立实测）；原文 5267 字符*

**前言**：> 产出性质：**提案**，不是执行结果。主控据此自行改 `SPEC.md` 与 `tools/`。
> 本代理为只读，未修改 `data/` 与 `tools/` 下任何文件。本轮所有数字均实测，脚本已用完删除。

| 项 | 任务书 | 本轮实测 | 一致 |
| --- | --- | --- | --- |
| 节点（不含 L2） | 522 | 522 | ✓ |
| 边 | 1433 | 1433 | ✓ |
| 节点锚点（带 quote） | 921 | 921 | ✓ |
| 带 `evidence.quote` 的边 | 931 | 931 | ✓ |
| 源文件 | 34 | 34 | ✓ |
| 锚点长度 min/median/mean/max | 32/89/94.8/198 | 32/89/94.8/198 | ✓ |
| 锚点含换行 | 0 | 0 | ✓ |
| `part-of` 后代暴露面 0/1–2/3–9/10+ | 406/63/44/9 | 406/**64**/43/9 | 差 1 |

L2 结构节点 24 个，`members`/`boundary`/`abstraction_gain`/`layer`/`sections` 五栏 24/24 齐全，全部无锚点，按 SPEC 第 56 行豁免，不计入分母。

后代暴露面差 1 个（1–2 档 64 而非 63，3–9 档 43 而非 44）是分档边界上的一个节点，不影响任何结论，此处如实记下不追。

#### 0.1 与任务书基线不符的一处：短公式的口径

任务书给的是「60 个低于地板的行里有 51 个是全局唯一的」。本轮无论怎样定义「数学承载行」都复现不出 60/51 这一对数：

| 口径 | N | 文件内唯一 | 全局唯一 |
| --- | --- | --- | --- |
| 内容行（去空行、去 `$$` 标记行）<30 | 215 | 174 | 140 |
| 其中 `display_block` + `math_only_line`（SPECQ 的「数学承载行」） | **35** | **35** | 33 |
| 其中 `display_block` 单独 | 21 | 21 | 19 |
| 含 `$` 或 `\cmd` 标记的短内容行 | 16 | 16 | 16 |
| 含数学标记或关系符的短内容行 | 55 | 50 | 44 |

SPECQ 自己报的是 35（§2.3.3 末「21 个展示块 + 14 条裸公式行 = 35」），§4.3 又写「全部 36 条」。本轮实测 **35**，与 SPECQ §2.3 一致，与 §4.3 的 36 差 1。**下文一律用 35。** 任务书的 60/51 我无法复现，也不采用；若主控知道它的口径，请以主控的为准，本提案的结论不依赖这一对数。

## S2-SPEC-字段语义.md

*抽自小节：## 1 七条裁决总览；原文 16102 字符*

**前言**：只读分析代理产出。**这是提案,不是执行结果**——`data/` 与 `tools/` 一个字节都没动。
所有数字都是我自己写一次性脚本实测的,脚本已删,复现命令见文末附录 B。

| # | 待议项 | 裁决 | 须改数据的节点/边 | 须改 SPEC 的位置 |
| --- | --- | --- | --- | --- |
| 1 | cause 6 入 SPEC | **入**,并补 cause 0 | 0 | `SPEC.md:100`、`:98` |
| 2 | 边 evidence 能否当锚点(T2) | **两分**:schema 下限只算 T1;审计证据池算 T1+T2+T3 | **0** | `SPEC.md:47–57` 新增小节 |
| 3 | 节点可否带 `origin` | **可以**,且必须与边的 `origin` 分开定义 | 65 | `SPEC.md:59–65` |
| 4 | `sections` 语义 | **「被定义/陈述于」**(语义 A),不是「被用到」 | 12(+1 联动) | `SPEC.md:59–65` |
| 5 | `parent` 是否仅指 part-of 父 | **允许 part-of 或 is-a 两种,但证据池只沿 part-of** | 0 | `SPEC.md:59–65` |
| 6 | `apostol:cx-` / `apostol:ext-` 是否合法 | **合法**,补进命名空间表并加最长前缀匹配警告 | 0 | `SPEC.md:35–45` |
| 7 | 无锚证明重构是否允许 | **允许,但须 `omission_site` 而非无条件豁免** | 4 | `SPEC.md:47–57` |

净影响:**须改 SPEC 五处;须改数据 81 个节点、0 条边**。
项 2 与项 5 的裁决是「把既有实践写成规则」,数据改动为零——这不是巧合,详见各项。

## S3-SPEC-关系词与审计程序.md

*抽自小节：## 〇 裁决速查表；原文 9640 字符*

**前言**：只读代理产出。**本文件是提案,不是执行结果。**未改 `data/` 与 `tools/` 下任何文件。
所有计数均在本次会话实测,脚本用完即删,方法记在各条的「影响面」里。

Sanity check 通过：节点 522(实测 546,含 24 个 L2 结构节点,522 = 546 − 24)、边 **1433**(实测,
`data/edges-*.jsonl` 1220 + `data/inherited/edges-*.jsonl` 213)。

| # | 待议项 | 裁决 | SPEC 动作 |
| --- | --- | --- | --- |
| 1 | 「第二使用点复用」关系词 | **不新增关系词** | 改为约束 `other` 的 `rel_note`,登记 6 条方向/选词缺陷 |
| 2 | B2 §6.1 枢纽豁免 FIX | **进 SPEC,但须先补 FIX 层本身** | 新增「裁决三档」节 + 枢纽豁免条 |
| 3 | 无 `parent` 层的删除测试 | **进 SPEC,以「恢复基」替代「父+兄弟」** | 新增「恢复基」定义表 |
| 4 | 删除测试作用域 | **父+兄弟(即恢复基),不是全图** | 明文写死作用域 + 全图冗余另立死因 |
| 5 | B2 §4(c) operations 禁令 | **进 SPEC,但须收窄边界** | 新增「已写出」的可判定定义 |
| 6 | 共享前提单独建节点 + 边 | **允许,且已在大规模使用** | 明文追认 + 加一条防滥用闸门 |
| A | 批量击杀前的互证检查 | **进 SPEC,强制** | 新增「批量执行」节 |
| B | 裁决回显 id 的表述强度 | **现表述不够强** | 加来源与自检要求 |
| C | 停止规则对 L2-B 可判定? | **不可判定,已实测证明** | 重写停止规则 |
