# 追加裁决 · C1-03（A10 / A9）

回查范围：`待应用-提案/A10-批次分层信号.md` 全文、`待应用-提案/A9-记账错更正.md` 全文。
未跑任何脚本、未读 data/ 与被改目标文件，故凡「目标文件行号」一律按提案自报照抄，标注为未独立定位。

## 主体

1. **A10-批次分层信号.md §6.1**｜改 `report/待应用清单.md:199–200`，把「这 15 个节点需要的是 `origin` 字段如实标注,不是删除」换成「已经如实标注……真正未披露的是 `nodes-A2-x2.jsonl`:34 个节点 `origin`/`origin_note` 命中 0/34」。**采纳**。§4.1 给出三条逐字 `origin_note`（如 `The witnesses f(x) = x and 2f are chosen by the model; Apostol states only the exercise.`），§八 独立复算得 15/15 且 15/15 无 `origin` 字段。

2. **A10 §6.2**｜改 `report/审查-形式B-共享引文.md:15`，`45 处引用、41 个不同节点` → `42 个不同节点`。**采纳**。跨两组节点逐字列出恰 3 个（`apostol:inner-product-axioms`、`d:positivity-axiom-forbids-nonzero-self-orthogonal-elements`、`d:strict-minimality-rests-on-positivity-of-the-norm`），45−3=42，§八 第 1 点独立复得 42。

3. **A10 §6.3**｜在 `待应用清单.md` §六 下新增 6.5 节，逐字文本以提案 §6.3 代码块为准。**改后采纳**：标题写「整批 34 个节点的出处审计」而正文写「剩余 28 个从未被审查」，装配时两数须同时保留（已审 6 个 = 5 判借用锚 + 1 判合法），否则会被读成 34 个全未审。数字侧成立：34/34 锚点落 `15.12-exercises.md`(25)/`15.16-exercises.md`(9)，两文件 `grep -ci answer` 各 0。

4. **A10 §6.4**｜SPEC 增补「锚点的出处充分性」条，逐字文本以提案 §6.4 代码块为准。**采纳**。前提是演绎而非统计：四习题文件 78/80/152/77 行、`grep -ci answer` 各 0；且 §八 第 3 点实测该判据完全二值（`A2-x2` 73/73、`CX2` 31/31、其余 12 批次 0/n），阈值取 0.1/0.5/0.9 同解，规则不依赖调参。

5. **A10 §6.5**｜方法论三句写进实验报告。**改后采纳**：文本内数字与 §3.1/§3.2 表自洽（0.24→0.50、20/25、24/25、复审集 70），但单调率序列（83%/19%/13%/0%）必须与 §3.3 三条局限同段落写入——该表 `nbatches==1` 只由 1 个组支撑（组 2，`15.12-exercises.md` 的 6 个 A2-x2 节点），`intra>=6` 只由 2 个组支撑。单独摘出会被当阈值用。

6. **A10 §4.2 合并形态**｜`review(v) = exonly(v) or intra(v) >= 3`，origin 作严重度标签不作过滤。**改后采纳**：析取方向采纳（合取实测 20/25→5/25），但 `>=3` 这个常数按 §3.3 自述「区间都很宽，不足以在 >=3 与 >=4 之间定阈值」，装配时须标暂定。另据 §5.2 第 3 条，49 个命中里已确认的 20 个中 15 个（CX2）登记的是形式 E 不判死，故 review 集不得标为「形式 B 缺陷集」。

7. **A10 全份·降级条款（追加裁决）**｜A10 表内所有「召回」按 §3.3 第 3 条自述严格是「对已知正例的覆盖率」（CX2 那 15 个是形式 B 审查员抽样偶然撞到，真实分母未知）；「命中中正例占比 0.34」按 §7 第 4 条是下界，不得当精确率引用；§八 复算只证计算无误、不证标签正确（两次用同一 10 个标签，均未独立复核）。**采纳为引用限制**：凡下游引 A10 的比率，须带这三句，否则该引用不可信。

8. **A9-记账错更正.md 更正一**｜改 `report/_D分片-主控核验.md:525`：分片的 100 字符无误，89 属 `edges-D2.jsonl:85` 边引文。**采纳**。`15.08.md:15` 整行 233 字符，A=100、B=89、C=79（`nodes-D2.jsonl:66` 唯一 anchors 项），A−B=`EXAMPLE 3. ` 11 字符；分片第二锚点 `15.07.md:39` 实测 89 亦无误。连带：`:524` 引导语改为「**附带修正一处分片记账错误，并撤回一处主控自身的记账错误：**」；`:518–522` 过报通道 (c) 整段不动。

9. **A9 更正二**｜替换 `report/审查-形式D-D3-片21-45.md:89` 整行，把 `:125` 移出「本该引而未引」，改注「（`:125` 已是该节点唯一锚点，42 字符，不在缺失之列）」。**采纳**。`nodes-D3.jsonl:31` = `d:norm-positivity-property`，`sections: ['15.10']`，anchors 恰 1 条 `(b) $\| x\| >0$ if $x\neq O$ (positivity).`，42 字符，命中 `15.10.md:125`。E-1 判定与 `1/3` 额度维持，缺失清单 4→3 项，严重度不降级。连带待办：该 42 字符锚点与 `_D分片-主控核验.md:293` 两处记账须对齐。

10. **A9 更正三·撤回主控原判**｜`apostol:zero-element` 死因 6 → 0 **不成立**，cause 6 维持。**采纳（对主控 `_D分片-主控核验.md:394–409` 整节的撤回）**。`edges-A1.jsonl:18` 该节点是 `dst` ⇒ 按 4.6 表为 TD、不入池；按 T1/T2/T3 实算池共 7 条，无一条来自 `15.03.md`，目标句在池中命中 0。

11. **A9 更正三·处置**｜`data/inherited/nodes-A1.jsonl:8` 同时改 `"sections": ["15.02", "15.03", "15.04"]` 并补第三条锚点 `{"file": "15.03.md", "quote": "The zero element is the function whose values are everywhere zero."}`；`audit-A5-A1-verdicts.jsonl:8` 的 `cause` 维持 6，两项写入后同批改 0。**采纳，顺序约束必须随文装配**（先改死因会让 verdicts 处于「声称无缺陷而缺陷仍在」的中间态）。新锚点合规实测：66 字符（30–200）、`15.03.md:19`、整行 359 字符多句故为句级子串、全 16 文件遍历仅出现于 15.03.md、额度 2/3 有空位。

12. **A9 更正三·SPEC 议题剥离**｜`sections` 语义在 `SPEC.md` 只出现于 `:62`、`:84` 两处 schema 示例，全文无定义；但补锚后甲乙两种裁决都要求含 15.03，故把本节点从该议题摘出，议题本身保留待议。**采纳**。

13. **A9 派生检查**｜「凡在 `:450` 之前做出、且理由里引用了该节点是 `dst` 的边的塌陷判定，都要重算」。**改后采纳**：作为新增待办登记，不得作为结论——A9 §边界 第 1 条自认未做 `:321–448` 的全量重扫，本节点只是已找到的一例。

## 落选项

- A10 §4.2「S2 **AND** S3」合取判据｜以「习题锚点且缺 origin」选人工复审集｜实测命中 34、覆盖 25 个正例中仅 5 个，按定义排除掉唯一实证的整批逃逸 CX2。
- A10 §3.2「S3 `nodecl` 单独作筛子」｜以缺 origin 选集｜命中 473/522=91%，命中中正例占比 0.02，无选择性；A10 自身亦不采。
- A10 §6.5「按批次名（`D*` vs `A2-*`）分层」｜把功劳记在批次分组本身｜无效，D1 是最大批次也贡献最多 FP（`intra>=3` 的 30 个命中里 15 个来自 D1）。
- A10 §5.1 第 3 条「`intra / |batch|` 归一化式」｜按批次大小归一以救小批次门面引文｜移出本轮，金标准无小批次正例可定标，A10 明确不提出该式。
- A10「把 S2 当形式 B 检测器」这一读法｜用 S2 命中数论证 B 型检出能力｜不采纳，S2 检出的是形式 E（出处缺陷），49 命中里已确认 20 个中 15 个登记为形式 E 不判死。
- A10 §3.3 之上定阈值（`>=3` 与 `>=4` 择一）｜选定 `intra` 硬阈值｜移出本轮，Wilson 区间 [0.60,0.98] 与 [0.49,0.94] 重叠，A10 自述只够定方向。
- A9 简报预设第 1 条「分片的 100 字符是错的、实际 89」｜要求改分片｜不采纳，两个数各量不同子串，要改的是主控核验文件。
- A9 主控原路径「只改 sections、不补锚点」｜以 sections 记账替代锚点｜不采纳，甲裁下字段自相矛盾、乙裁下 cause 6 照样成立。
- A9「`:321–448` 全量重扫」｜逐条核 verdicts 的 reason 是否引了 TD 边｜移出本轮，A9 自认未做，只能作缺口登记。

**本片未覆盖**：A10 §二/§三 的比率表我只核了表内自洽与 §八 复算行的对应关系，未独立复算（禁跑脚本）；A9 两段回源验证代码未执行，字符数按提案自报接受；两份提案所引目标文件的行号（`待应用清单.md:199–200`、`审查-形式B-共享引文.md:15`、`_D分片-主控核验.md:524/525/293/394–409/400–407`、`审查-形式D-D3-片21-45.md:89`）均未独立定位。
