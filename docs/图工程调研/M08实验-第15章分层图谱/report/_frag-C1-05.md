# 追加裁决 · C1-05（A6 + G6d D4 片1-27 / 片28-54）

## 主体

1. **来源 `A6-两处数学缺陷.md`**（§1.4 主方案）。主张：把 `data/nodes-D2.jsonl:36`（`d:exponential-independence-select-the-largest-exponent`）statement 换成 §1.4 主方案逐字文本，并新增锚点 A2 = `If $a_1, \ldots, a_n$ are distinct real numbers, the $n$ exponential functions`（`15.07.md` L39，实测 **78** 字符，文件内出现 1 次，全库 `(file,quote)` 使用者 **0**）。判：**改后采纳**（连带项见第 3 条）。理由：两处错都成立——最大性按定义只给 `a_k - a_M <= 0`，严格性来自 Example 7 的互异前提，且原句 `smaller by a strictly negative amount` 字面自相矛盾——而 `:34`/`:39` 已把归因写对，改 `:36` 即自洽。

2. **来源 `A6-两处数学缺陷.md`**（§2.4 主方案）。主张：把 `data/nodes-D4.jsonl:51` statement 换成 §2.4 主方案逐字文本，新增锚点 B3 = `where $s \in S$ and $s^{\perp} \in S^{\perp}$ . Then, for any $t$ in $S$ , we have`（`15.15.md` L11，实测 **82** 字符，使用者 **0**）。判：**采纳**。理由：末句字面为假有数值见证（x=(4,3)、s=(4,0)、t=(0,0)：`||x - t|| - ||x - s||` = 2 而 `||s - t||^2` = 16，且量纲一次 vs 二次），主方案同时修掉 `is the substantive content of the approximation theorem` 这处过度断言。

3. **来源 `A6-两处数学缺陷.md`**（§〇 与 §1.7 / §2.7）。主张：两处「只改 `statement`」，`id`、`name_en`、`name_zh`、`node_type`、`sections`、`parent`、`atomic`、`atomic_reason` 一律不动，边一条不动。判：**改后采纳**（须补一项）。理由：A6 自陈 `:36` 的 C2'、C3、C4b 三条无锚、`:51` 的 C1b 落在 T2，正是 `G6d-D4-片28-54-摘要.md` §7.4 要求补 `origin: model` 的形态，而该摘要实测缺 origin 的 6 个节点（30、31、36、45、48、**51**）含 `:51`，故改写须连带补 `origin: model`；A6 未提此字段。

4. **来源 `G6d-D4-片28-54-摘要.md`**（裁决表 L51 + §3.1.2）。主张：L51 判「死因 0（从严可 4）」，动作仅「末句『the error』两处改『the squared error』」，是否按死因 4 列为未决。判：**改后采纳**（降级该动作，未决按第 2 条关闭）。理由：A6 的数值见证把「字面为假」从条件句变为确证；且此最小改法保留了 `is the substantive content of the approximation theorem`，射程缺陷未消。

5. **来源 `G6d-D4-片28-54-摘要.md`**（§〇 元观察 (1) / §6.3）。主张：27 个节点无一用满 3 条锚点（4 个用 1 条、23 个用 2 条），故 17 处缺陷一处也不能归因于 SPEC 3 锚点上限。判：**采纳，附装配顺序约束**。理由：4+23=27 自洽，与 A6 实测 `:51` 现有 B1+B2 两锚一致；但 A6 主方案填满 `:51` 第三槽后该节点再无空位，凡后续还要给 `:51` 补锚的动作必须排在 A6 之前，或改取 A6 §2.4 备选。

6. **来源 `G6d-D4-片1-27-摘要.md`**（§四.1）。主张：分片对行 16 写「槽位已满」是记账错——`nodes.tsv` 该节点 `n_anchors=2`、`anchors.tsv` 只有 idx 0、1，第三槽是空的，`15.08.md:17`（实测 187 字符、3 个句末标点）可直接占空槽。判：**采纳**。理由：与同摘要行 20「第三槽空着故不能推给 SPEC」是同一核查口径，纠正后行 16 的「换锚 vs 削减 statement」两难消失。

7. **来源 `G6d-D4-片1-27-摘要.md`**（§四.2）。主张：分片 §6 抬头「合计 6 个节点受 SPEC 约束（其中 1 个同时受两条）」应为「5 个节点，其中 2 个（行 20、27）同时受两条」；并集为下限 {19,20,27}、上限 {16,20,27}、跨行 {10}。判：**采纳**。理由：并集逐条可算，不改任何单条裁决。

8. **来源 `G6d-D4-片1-27-摘要.md`**（行 14 / §二.1）。主张：行 14 `exactly two load-bearing places` 为全片唯一死因 1，被 `15.11.md:15` 的第三处使用点推翻，且 `edges.tsv` 中 `d:15-10-proof-divide-by-nonzero-self-inner-product` 只有 4 条边、确无指向本节点的边。判：**改后采纳**。理由：推翻路径（换成内积语言再查）与缺边指控均已核；但同摘要自记 `15.11.md:15` 属 D3 片区领地并建议交叉核，本片无 D3 材料，「与 D3 结论不冲突」标**未复核**；该行实测 463 字符、6 个句末标点，补锚/补边须取子句。

9. **来源 `G6d-D4-片28-54-摘要.md`**（顺带发现第 1 条）。主张：本片 27 节点的 `node_type` 出现 `method`（12 个）与 `notation`（L29、L54），均不在封闭词表 concept/theorem/proof-step/example 内，分片未察觉。判：**采纳为独立待查项**（不并入 17 条 FIX）。理由：若该词表确为 SPEC 所定，涉及 14 个节点，量级高于本片全部 FIX；本片无 SPEC 原文，「词表是否封闭」标**未复核**。

10. **来源 两份 G6d 摘要**（§〇）。主张：D4 第 1–54 行无一条 KILL、无一条撤回。判：**改后采纳（限定射程）**。理由：两片均自陈未系统审边（rel 方向与 evidence 支撑性）、未做 A/C 型专项排查，故该结论只在形式 D/E 视角下成立；已有两条 B 型线索需另立：片1-27 行 15 锚点 2 与行 19 复用同一引文；片28-54 L44 锚 1 与行 36 共用（分片称「本片唯一真借用」）。

**本片未覆盖**：两份摘要裁决表中其余逐行动作（片1-27 的 12 条 FIX、片28-54 的 17 条 FIX 的逐条补锚目标与字符数）未逐条回查原文，本片不对其作装配裁决。

## 落选项

- 来源 `A6-两处数学缺陷.md` §2.4 备选方案（2 锚、不引 `15.15.md` L11）。内容：删掉「for any t in S」以免新增锚点。落选：A6 自陈会在 `:51` 留下「恒等式对一切 t 成立」的 D 型无锚残留，而第三槽本就空着，无成本理由保留缺陷。
- 来源 `A6-两处数学缺陷.md` §1.4 备选方案（不点名 distinctness、1 锚不变）。内容：只撤回错误归因。落选（移出本轮）：仅在主控拒绝新增锚点时才需要，而 A2 的 `(file,quote)` 对全库 0 使用者，该前提未触发。
- 来源 `G6d-D4-片28-54-摘要.md` §2.6 / 裁决表 L48。内容：给 L48 强制换锚为 `15.15.md:17 + :23`（保留 `:9`）。落选（本轮不照原样装配）：A6 实测 L17 的 152 字符全句是 `:50` 的自有锚点、96 字符长跨已被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 逐字占用，L23 是 `:52` 的自有锚点，照原样换锚会造成 B 型借用引文；须先给出与 `:50`/`:52` 不逐字重合的子句跨并实测字符数。
- 来源 `A6-两处数学缺陷.md` §三.1。内容：验收数字（节点 522、边 1433、带 quote 锚点 921→923）。落选（移出本轮）：需回查 `data/` 才能核，本片只读提案，标**未复核**，不作为装配依据。
- 来源 `G6d-D4-片28-54-摘要.md` §3.1.1。内容：若严格执行 SPEC 第 55 行则 L30 末句（claim 8）应删。落选（移出本轮）：本片无 SPEC 原文，第 55 行内容未复核，删句不能凭摘要落地。
