# C1-02 追加裁决（A5 / A6 / A7）

回查范围：`待应用-提案/A5-CX2与origin.md`（§0/§2/§3/§4.21/§5/§6/§7）、`A6-两处数学缺陷.md`（全文）、`A7-H层整合.md`（全文）。机器复核在实验根 `M08实验-第15章分层图谱/` 下只读进行（未改 `data/`，未跑 `tools/`）。

## 主体

1. **A5 §6**：20 个节点全 KEEP、死因 `0`、登记形式 E。**采纳。**§6.1 实为 20 行、id 逐条回显；`SPEC.md:100` 现只写到 `5` 死因、无 6，A5「形式 E 不入死因编号」的记账口径与 SPEC 现状不冲突。
2. **A5 §2.2**：41 条锚点 verbatim 全通过、`occurrences` 均 1、实测 min 34 / max 167 落在 30–200。**采纳（未独立复跑 41 条）。**表内极值与该结论自洽；`SPEC.md:52` 逐字为「长度 30–200 字符」。
3. **A5 §3.3**：形态 (a)，节点带 `origin`，位置紧跟 `anchors`，取值只用 `"model"`。**采纳。**事实基础已机器复核全部属实：节点级 `origin` = **33**（D1 1 / D2 8 / D3 4 / D4 9 / H2 11），取值 `model` 24 / `source` 9；`origin_note` = **39**（CX2 15 / D2 9 / D3 4 / H2 11）。`tools/export_obsidian.py:183-184` 逐字为 `if node.get("origin"):` / `fm.append(f"origin: {yaml_scalar(node['origin'])}")`，`:229-230` 逐字为 `if node.get("origin_note"):` / `body += ["## 来源说明", "", prose("origin_note"), ""]`。
4. **A5 §3.1**：`SPEC.md:55` 已在节点语境授权 `origin: model`，故本项是补文档而非新增能力。**采纳。**该行逐字为「- 找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。」，确在 `## 锚点规则` 下、紧跟长度规则；`SPEC.md:62` 的节点 schema JSON 内无 `origin`，`SPEC.md:70` 的边 schema 有 `"origin":"source|model"`。
5. **A5 §3.3 第 2 项**：SPEC 补 cause `6`（引文不支撑该断言）与「形式 E ≠ 死因 3」一句。**采纳。**已复核 `SPEC.md:100` 逐字止于 `5` 伪抽象·层塌缩。
6. **A5 §5**：`data/nodes-CX2.jsonl` 15 行各加 `"origin": "model",`（`origin_note` 一字不动）；`data/nodes-A2-x2.jsonl` :2–:6 各加 2 字段。**采纳**，改动量与 §0 第 5 点一致。
7. **A5 §5.1 的 `tmp_apply_a5.py`**：**改后采纳。**插入位置（`anchors` 之后、`origin_note` 之前）照用，但整行 `json.dumps` 重序列化不能兑现原文承诺的「每行只多出 `"origin": "model", `」——它重写该行全部标量的转义与间隔。改为定点插入，再 `git diff --stat` 验只有 15 行变化。
8. **A5 §7.1**：伞节点 `apostol:cx-inner-product-axiom-diagnosis`（`data/nodes-A2-x2.jsonl:1`）一并加 `origin: "model"` 与 §7.1 给出的逐字 `origin_note`。**采纳**——被统摄的 5 个都标而伞节点不标是反常状态。
9. **A5 §7.4**：剩余同型节点另起一批。**改后采纳（数字须先修）。**「全库锚到 `*-exercises.md` 的节点共 49」「CX2 15 + A2-x2 34」「本批 20、剩 29」已机器复核属实（`nodes-A2-x2.jsonl` 恰 34 行）；但「**13 个**疑似形式 E」与所列 id 数不符——原文只列 12 个（`:1`、`:8`、`:11`、`:12`、`:13`、`:16`、`:17`、`:18`、`:21`、`:30`、`:33`、`:34`），`:9`/`:10` 另作「混合」单列，16 + 13 = 29 的分账缺 1 个 id。补齐后才能据此开批。
10. **A5 §7.2 / §7.3**：两处 statement 覆盖缺口不修（CX2 #10 的公理 8、15.12 #17 的无见证声称），不为 #1/#2 的 "usual" 子句加第 3 条锚点。**均采纳**，与「只加字段、不动 statement」总原则一致。
11. **A6 缺陷 1 主方案**：按 §1.4 主方案逐字文本改 `data/nodes-D2.jsonl:36` 的 `statement`，新增锚点 A2。**采纳。**该行 id 已回显核对为 `d:exponential-independence-select-the-largest-exponent`；A2 逐字 `If $a_1, \ldots, a_n$ are distinct real numbers, the $n$ exponential functions` 实测 **78** 字符、`15.07.md` 第 **39** 行、文件内出现 **1** 次、不跨行；该节点现有 `anchors` 恰 1 条（A1），加后 2 条，未触 3 条上限。
12. **A6 §1.5 / §2.6 的三条「不要做」**：不给 `:36` 挂 15.07.md L57、不给 `:51` 挂 L23 与 L9、不加 `:36 --requires--> :33` 边。**均采纳**——TD 入边须排除，依赖已由 `edges-D2.jsonl:39` 承载。
13. **A6 缺陷 2 主方案**：按 §2.4 主方案逐字文本改 `data/nodes-D4.jsonl:51` 的 `statement`，新增锚点 B3。**改后采纳。**id 回显核对为 `d:pythagorean-split-of-the-approximation-error`；B3 逐字 `where $s \in S$ and $s^{\perp} \in S^{\perp}$ . Then, for any $t$ in $S$ , we have` 实测 **82** 字符、`15.15.md` 第 **11** 行、出现 1 次、不跨行，原文自报的 34 字符最小跨 `Then, for any $t$ in $S$ , we have` 亦复核为 34 字符、同在 L11，故「最小语义单元不可独立成锚」的两难属实。**须改一点**：§2.3 称「改写用 exceeds … by exactly 搭配显式平方口径后此歧义消解」，但主方案定稿保留的正是它刚批评的 `exceeds … by exactly` 结构，t = s 时的严格性歧义并未由文本消解。落笔前改掉该措辞或删去这句自评（不涉数据改动）。
14. **A6 §1.7 / §2.7 连带项**：`id`、`name_en`、`name_zh`、`sections`、`atomic_reason` 与所有边一律不动。**采纳**——`sections` 为 `["15.15"]` 与内容相符已由原文的 L3/L9/L11/L17/L20/L23 行级依据支撑。
15. **A6 §三 验收提示**：只改 `statement` ⇒ 点数不变；带 quote 的锚点 921 → **923**。**采纳，但「923」标「未复核」**（本片未做全库锚点计数）。「必须读 stdout 里 `nodes with NO anchor` 那一行、不能只看退出码」无条件采纳。
16. **A7 §0 的前提校正数字**：「节点 522、边 1433」。**改后采纳（口径须补写）。**机器复核：边 **1433** = `data/edges-*.jsonl` 1220 + `data/inherited/edges-*.jsonl` 213，吻合；节点 **522** = `data/nodes-*.jsonl` 397 + `inherited/nodes-*.jsonl` 125，**不含 `data/structures-*.jsonl` 的 24 行**（计入则为 546）。原文未交代这个口径，照抄会与按 546 计数的提案对不上。H1 边 15 条 / H2 节点 11 个 / H2 边 19 条三项独立复核属实。

## 落选项

- **A7-H层整合.md（整份）**：§0 宣称「直接并入 9 项、改后并入 5 项、建议丢弃 20 项，另报告 3 处两份前序审计都漏掉的问题」。**不采纳（撤回）**——该文件磁盘全长仅 1284 字符、13 行，§1–§7 只以 `<!--SKELETON: §1 机械核验 / §2 H1 逐条 / §3 H2 节点逐条 / §4 H2 边逐条 / §5 整合提案 / §6 两份审计都漏的 / §7 未做的事-->` 一行注释存在，34 项裁决与 3 处新发现从未写出，无一条可装配，§0 的汇总数字在本文件内无任何支撑。
- **A7 §0 的机械合规结论**（31 条边引文逐字命中、11 个节点锚点逐字命中、字符数全落 30–200、无跨行、无悬空 id、无对称关系反向重复边）：**移出本轮**——依据本应在未写出的 §1，不可复核；同一对象另有 `G3a-H1边逐条.md`、`G3b-H2节点逐条.md`、`G3c-H2边逐条.md`、`G3e-H层总装.md` 四份逐条提案在册，H 层裁决应改由这四份承载。
- **A6 §1.4 缺陷 1 备选方案**（不点名 distinctness、不新增锚点）：**移出本轮**——仅在主控拒绝新增锚点时启用，与主方案互斥，不并行装配。
- **A6 §2.4 缺陷 2 备选方案**（去掉全称量词、不引 L11）：**移出本轮**——原文自承会在 `:51` 留下 D 型射程不足残留，主方案已可用。
- **A6 §2.5 把锚点 B2 换成 96 字符长跨**：**不采纳**——原文自证该 `(file,quote)` 对已被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 占用，提为锚点会形成三方共享并越界吃进 `:50` 的自有内容。
- **A5 §7.4 里 16 个「非形式 E」节点补 `origin: model`**：**不采纳**——锚点含 "Prove that / show that" 型题面，定性结论有锚，不满足形式 E 构成要件。

本片未覆盖：A5 §4.1–§4.20 的 20 张子句分解表未逐张回源（只核了 §4.21 汇总的自洽性：108 = 21 + 5 + 55 + 27，82/108 = 75.9%）；A5 §5.2/§5.3 的 5 条 `origin_note` 逐字文本未与源题面比对；A6 的「921 → 923」未复核。
