# C1-01 追加裁决 · A1 / A10 / A3 / A4

回查：A1 全文；A10 §1.1/1.2/3.2/4.1/4.2/6.1–6.3；A3 §0/1/5.4/6/7；A4 §0/3/4/5/7。另复算 `data/`（只读）与两个 `tools/` 脚本源码、`SPEC.md:53-56`。

## 主体

1. **A1 §1｜21 条待杀 id（已撤出 `d:for-function-spaces-closure-is-the-only-real-content`，`nodes-D1.jsonl:62`）** → **采纳**。
   复算 `audit-B2-verdicts.jsonl` 中 `KILL` **29** 条（行 2/4/5/20/21/24/25/26/38/42/45/46/48/49/51/52/54/57/59/60/62/71/73/80/81/101/115/119/121）；
   §1 表 21 个 id 在 `data/` 全部命中、无重复。撤出理由与 `待应用清单.md` §6.1 第三条「重挂锚点,不得丢弃」一致。

2. **A1 §0｜边计数两口径表** → **采纳**。逐栏复算全中：仅本批 21 条 → 墓地 **71**、A **24**、B **44**、C **3**；
   加维持的 3 条（`nodes-D1.jsonl:46/:60/:71`）→ **82 / 26 / 53 / 3**。「只有 A 类需处置」分类正确。

3. **A1 §0｜算式「29 − §1 的 9 个争议 = 22」** → **改后采纳（结论对、推导错）**。那 9 个里在 B2 判 KILL 的只有 **7** 个
   （:5 `d:ten-axioms-listed-in-three-groups`、:24、:42、:46、:60、:71、:101 `x:least-squares-is-a-projection`）；
   `x:dimension-versus-rank` 与 `x:independence-criterion-and-its-consequences` 在 B2 本就是 FIX，不在 29 内。
   29 − 7 = 22 成立，29 − 9 = 20。数字 22 可用，**该算式不得抄进验收单**。

4. **A1 §0｜零残留三项（存活 `parent` 指向待删 0 个、`structures-L2-{a,b,c}` 含待删 id 0 处）** → **采纳**。
   按 21 个 id 做裸串扫描 + `parent` 扫描，均实测 **0**。这是目前唯一已核的 `apply_kills.py` 放行前提。

5. **A1 §2.2｜NEAR 那条可安全杀** → **采纳（须补路径）**。`d:notation-c-a-b-...` 锚点实测 **65** 字符，
   存活侧 `apostol:space-of-continuous-functions` 锚点 **131** 字符、整句含住只差末句点，核实。
   但后者在 **`data/inherited/nodes-A1.jsonl:15`**，A1 写作 `nodes-A1.jsonl:15`，落盘前须补 `inherited/`。

6. **A1 §2.1 对 B / 对 C 裁定（两个都可杀）** → **采纳**。对 B：承载者 `nodes-D1.jsonl:47` statement 与其第二条锚点 **108** 字符实测相符，不随本批删除。
   对 C：`:51` 与 `:52` 实测共用同一条 **92** 字符锚点（`15.03.md`，`with addition and multiplication by scalars defined in the usual way in terms of components.`）。

7. **A10 §1.1/1.2｜四判据 + evidence 不入分组（nodes-only）** → **采纳，且推断有机械证明**。复算 nodes-only **708** 对 / **921** 次；
   含边 **875** 对 / **1852** 次，与 A10 一致；`_形式B-共享引文候选.md` 头部确为 708。A10 由此推断该清单跑的是 nodes-only——
   `check_shared_quotes.py:88` 的 `ap.add_argument('--edges', nargs='*', default=[])` **默认空**，推断成立，不止是间接证据。
   「该脚本把边 evidence 与节点 anchors 混进同一计数」经源码 `:67-81` 确认。

8. **A10 §3.2/4.1/4.2｜合并须用析取、不得用合取** → **采纳**。逐项复算全中：`exonly` **49**、`nodecl` **473**（91%）、无锚 **4**、
   CX2 `origin_note` **15/15** 且 `exonly` **15/15**、A2-x2 **0/34** 且 `exonly` **34/34**；`S2 AND S3` 命中集恰为 A2-x2 那 34 个
   （按定义排除 CX2），故「合取把召回从 20/25 打到 5/25」的机制成立。14 批次名与节点数、522/1433/921 全中。

9. **A10 §6.1｜改写 `待应用清单.md` 第 199–200 行** → **采纳**。该两行现文与 A10 所引逐字一致，登记为待办的事确已完成。

10. **A10 §6.2｜`审查-形式B-共享引文.md:15` 的 41 → 42** → **改后采纳（须子串替换）**。该行实际全文为
    `共涉及 **45 处引用、41 个不同节点**（\`d:positivity-axiom-forbids-nonzero-self-orthogonal-elements\`、`，
    A10 的「现文」只截到 `（`；照其块整行覆盖会截掉行尾三个 id。算式 45 − 3 = 42 与该行括注自洽。

11. **A10 §6.3｜A2-x2 有 28 个节点从未受形式 E 审查** → **采纳**。实测该批 34 节点锚点：触及 `15.12-exercises.md` **25** 个、
    `15.16-exercises.md` **9** 个，无交叉，合 34；34 − 已审 6 = 28，自洽。

12. **A3 §0｜四个零证据节点与 12 条边** → **采纳**。`nodes-D2.jsonl:42/:54/:72/:73` 实测 `anchors: []`、`origin: "model"`；
    **全图无锚点节点恰好这 4 个**（522 节点复算）。12 条边的行号、src/dst 角色、关系逐条无误，12/12 `evidence` 空；
    `edges-D2.jsonl:90` 确为唯一 TD，按四层证据池排除正确。小注：表头写 `type`，字段名实为 `node_type`，值与表一致。

13. **A3 §1｜`SPEC.md:53` 与 `:55` 冲突，须先裁再决定补锚** → **采纳为前置裁断项**。SPEC 三行逐字复核
    （`:53`「至少 1 条,最多 3 条」、`:55`「找不到可逐字引用的依据 ⇒ 不要收录,或标 `origin: model` 并如实说明。」、`:56` 只免 L2+）；
    四节点确已满足 :55 第二分支形式要件。**后果：A3 §3/§4 补锚是有条件的，未裁前不得作「必须执行」进单。**

14. **A3 §6｜验收口径（须读 stdout `nodes with NO anchor`；只应用 §3/§4 则由 4 变 2；921 → 924）** → **采纳**。
    `verify_anchors.py` 源码复核：无锚节点 append 后 `continue`、永不入 failures；唯一检查 `elif quote and quote in text`（`text` 是整份文件）；
    `return 1 if failures or bad_json else 0`。921 基数已实测。A3 自陈这些是未跑脚本的预期值，如实。

15. **A4 §7 B 组｜7 条借用锚点剥 `index 1`** → **采纳**。七个目标逐行复核全中：`nodes-D4.jsonl:15`、`:52`，`nodes-A2-x2.jsonl:2/:3/:4/:5/:6`；
    各现有 2 条锚点、待剥均为 `anchors[1]`、剥后 **1** 条、不击穿下限。A2-x2 那五条的 `[1]` 实测是同一条 **117** 字符题干指令，即借用源。

16. **A4 §3｜疑似第 8 条（`nodes-D4.jsonl:53`）判 PLAUSIBLE、不自动执行、须按 index 剥** → **采纳**。实测其 `[0]` = 42 字符范数正定性、
    `[1]` = 35 字符内积正定性，与 `:15` 顺序**正好相反**（:15 的 `[0]` 是 35 字符那条）。「不要按『剥第二条』口头描述套用」是本片最该保留的执行警告。

17. **A4 §4｜对审查稿四处字符更正** → **采纳**。独立 `len()` 复测全中：`15.02.md:9` 整行 **175**（审查稿 170）、
    `The reader can easily verify...function space.` **81**、`(Whenever we consider...included.)` **91**、`AXIOM 5. ...such that` **85**，
    三条 quote 均为源文逐字子串。§6 的 `x + (- 1) x = O.` **16** 字符（低于 30 下限，SPEC 归因成立）亦通过。
    **未复核**：§4 说 `15.06.md:9`「整句 207 字符」——该行整行实测 **421**，207 指行内某句，本片无从定位。

18. **A4 §7 A 组｜三条补锚的目标定位** → **采纳（仅定位）**。`nodes-D1.jsonl:62/:69/:70` 实测各只 **1** 条锚点，且三者共用同一条 **115** 字符锚点，
    正是形式 B 共享组本身，重挂对象无误。**未复核**：§1.1–§1.3 替换块本片未读，故 §7 D「921 + 4 − 7 = 918」只按内部一致性接受。

19. **A4 §0｜边 `evidence` 是单个 dict 不是数组** → **采纳**。`check_shared_quotes.py:71-73` 以 `ev.get('file')` 取值，dict 口径确认。
    另 `15.10.md:119` **460** 字符、`15.03.md:25` **360** 字符实测一致，「不用行号做覆盖比较」论据成立。

20. **A1 §0 的执行前置条件（「§3.2 的 6 条重指必须先做完」等）** → **不采纳**，见落选项 1。

## 落选项

- **A1 §3 / §4 / §5 全部处置（§0 表里的「引文彻底消失 7 条分 3 类处置」「B2 `reason` 须重写 4 条含 1 条伪造引文」「§3.2 的 6 条重指」）**
  → 落选·无法装配：`A1-互证塌缩检查.md` 实际只有 146 行、止于 §2 之后的 `<!-- MARK-3 -->`，§3/§4/§5 **在文件里不存在**，
  这些处置无一可核；而 §0 把「§3.2 的 6 条重指做完」列为 `apply_kills.py` 的**强制**前置，即前置条件本身不可查。
  第 1/2/4/5/6 条仍可独立装配。
- **A1 §0「引文彻底消失 7 条」与「锚点轴塌缩 1 对」** → 落选·数字不可引用：同文件 §2.2 表给 LOST **9** 条、标题写「6 个节点、9 条引文」，
  且第 144 行自称「§0 前一版记成 7 条…按引文数是 9 条。**§0 已改**」——但 §0 第 19 行现文仍是 7，改动没落地。
- **A1 §2.1 对 A 裁定依赖的「十条公理映射表并入 parent」** → 移出本轮：`v:46` 属维持的 3 条、不在本批 21 条内，
  A1 自陈不重复裁断，管辖权在 `G2b-十条公理FIX.md`。
- **A3 §5 对 `:72` / `:73` 的处置** → 移出本轮：只给裁决甲/乙两条后果分析，**未作单一建议**；其倾向的「第三条路」（把 Theorem 12.10 加进 `source/`）
  A3 自陈「改变实验边界…不作为本提案的建议动作」。无可装配动作。
- **A4 §3 末句「全图锚点从 921 降到 913（减 8）」** → 落选·数字不可引用：只算 B 组减 7、C 组减 1，漏掉 A 组净增 4；
  §7 D 自己给 918（A+B）与 917（A+B+C）。验收只能用 918/917。
- **A10 §6 小节编号** → 落选（仅形式）：`### 6.5` 出现两次（第 392、422 行），`### 6.4`（第 406 行）排在第一个 6.5 之后。
  两节内容各自可采，编号须主控重排后再引用。

**本片未覆盖**：A1 §2.2 的 FULL 27 / PART 0 未逐条回源（只核了 NEAR）；A10 §2/3.1/3.3/5/8 未读，重排序能力与 Wilson 区间未复核；
A3 §2/3/4 的锚点候选逐字文本与射程分析未读；A4 §1.1–1.3 替换块、§2 七条的逐条论证、§5 的 15.03 持有者普查未回源。
