# 装配片 C3-05（P13 / P14 / P15）

装配顺序 **P14 → P15 → P13**，同落 `SPEC.md` `## 审计规则`（`:89–102`）。原文已回查。

## 主体

1｜**P14：`R(n)` 六档表**（采纳）。R1 `parent` / R2 `members` / R3 `apostol_ids`·`strang_ids` / R4 `part-of` 父 / R5 `part-of` 子 / R6 距离 1 边邻居，**从上往下取、首档命中即停止**。理由：覆盖 287/546 → 546/546；无 `parent` 者 **259 个**，`members` 只覆盖 24 个（9.3%），L2 专用规则射程不足。命中分布 287/24/22/26/70/117，基均值 4.75、最大 17、空基 0。

2｜**P14：三条禁令**（采纳）。①`sections` 同节共现不得入基（同节最多 198 节点）；②同层同批次节点不得入基；③空基记 `verdict: "NOT-TESTABLE"`、不得判 KILL。②的实测：`audit-L2-B-verdicts.jsonl` 20 条 KILL 中 6 条引另一个 L2，而 24 个 L2 的 188 个去重 member 里 `L2:` 计数为 **0** ⇒ 6 条全越界，4 条真塌缩，含一条长度 3 的链、三节点全在 KILL 名单（逐条见 P14 §4.4 表）。

3｜**P14：`SPEC.md:95` 替换**（采纳）。新逐字 `- **审计 B(抽象增益)**:用下文的**删除测试**判定,对照集恒为该节点的**恢复基** `R(n)`,不得临场另选对照。杀掉伪抽象。` `R(n)` 三小节插在 `:95` 与 `:96`（空行）之间；P15 §三(2) 确认此句已含它的核心，`:95` 只改一次。

4｜**P14：扩基 → 缺边建议**（采纳）。基外 id 出现在 `reason` 即为一条缺边建议，执行前逐条裁决。债务：R6 档 5 条越界（`apostol:cx-*` 横向引用，应补边）、15 条 `reason` 未引 id 属**未核**。

5｜**P15：作用域恒等于 `R(n)`，非全图**（采纳）。三推论：KILL 的 `reason` 须点名 ≥1 个基内 id、否则降 FIX；「图里别处也讲了」不构成死因 5；`sections` 与共享引文不入作用域。理由：全图多算 **89.9 倍**对数（148785 vs 1655），多捞 295 对里 statement Jaccard ≥0.35 仅 3 对、<0.20 有 143 对（88.8%）；全图还把互证塌缩从 4 抬到 11（B2 一批 1 → 3）。

6｜**P15：批次级引文重复扫描 M1–M4**（采纳）。M1 合并记 `0`/M2 补边 `0`/M3 换锚补锚 `6`/M4 无动作，不得留空；跨基共引**永不产生 KILL**、不新增死因号、落 M1 须指定 `keep_id`。理由：重合当 KILL 信号会首先打死 `x:` 对齐层（22 个 `x:` 中 19 个与别处共引）。

7｜**批次前检查并成一张表**（改后采纳）：**①互证 → ②空基 → ③引文重复扫描 → ④孤儿检查**。P14 原文「批量执行前的两道检查」须改为四道。

8｜**P15：报告义务 + 9 条降级**（采纳）。须报本批作用域为 `R(n)`、跨基共引四档计数、被降级的 KILL 条数，「不得只报「N/N 已判定」」。85 条 node KILL 中 76 条合规、**9 条**只点名基外 id，形态均为「该在基里、只是缺那条 `part-of` 边」：降 FIX + 补边重跑。

9｜**P15 §1.5 口径更正**（采纳）。「167 倍」→ **89.9 倍**、「漏 377 对」→ **295 对**；`shared-quotes.tsv` 480 行中 322 行归并后只剩 <2 个不同节点，把 480 当借用条数**高报约 2.7 倍**。凡引旧数字处一律回改。

10｜**P13：枢纽禁令进 SPEC**（采纳），逐字：「**枢纽性不是裁决理由，不构成对任何死因的豁免。**一个节点若无增量，即使有子节点以 `part-of` 挂在它下面，裁决仍为 `KILL`，死因照填。」理由：枢纽性量拓扑、删除测试量信息含量，拓扑不得抵消内容。

11｜**P13：`SPEC.md:98` 整段替换**（采纳）。新首行 `每条裁决格式：`<id> | KEEP|FIX|KILL | 死因编号 | 一句理由`。`（旧为 `KEEP|KILL`），后接三档表 + 枢纽禁令 + 孤儿检查，约 +40 行。SPEC 原无 FIX 档（`grep` 命中 0 行），第 8 项降级靠它落地。**冲突**：与 `S3-SPEC-关系词与审计程序.md` §二 同处两版，取 P13 版。

12｜**P13：FIX 门槛**（采纳）。须有非空 `suggested_fix`、内容为定稿文本或可执行改法。债务：`audit-B-verdicts.jsonl:2/41/143/145` + `audit-B3-A1-verdicts.jsonl:5/6/8/12/25/43` 共 **10 条**无改法字段转违规；**7 条** `cause: 0` 转合规（重叠 6 条）；**4 条**须改判 KILL（`audit-B:41/143/145`、`audit-B2:67`）。FIX 总量 99 条；S3 `:248`「13 个 FIX 全部过门槛」实测不成立。

13｜**P13：孤儿检查**（采纳）。独子集按「`c --part-of--> k` **或** c 的 `parent` 等于 k」取并集；改指优先级 ①上溯至第一个不在 K 中的祖先 ②改指 `sections` 本节主概念节点并写明人工指定 ③允许留孤但逐条登记 `orphaned: <c> (was part-of <k>)`、该批次不得声明「悬空 id = 0」。理由：8 个待杀枢纽（全出自 `audit-B5-A2s`、全 cause=5）中 **5 个无祖父可指**、`parent` 皆空，S3 只写第一档在这 5 处无解；逐条 id 见 P13 §四 实数 3 的表。G1d 步骤 4 的 19 个待删节点 ∩ 枢纽集 = 0。

14｜**P13：`:100` 死因行**（改后采纳）。`0` 无死因仅需 FIX（已有 7 条在用）采纳；`6` 只占位、归 P01。与 P15 §三(3)「`:100` 不改」不冲突——P15 只否决另立**新**死因号。

**本片未覆盖**：三份 §六 耦合未逐条转写，两项交综合代理——(a) P07 为最强依赖（P13 与 P14 R1 都把 `parent` 与 part-of 边取并集）；(b) 审计债务（CX2 15 节点从未被 B 系判过；反向 46 条判决被追认）。

## 落选项

- P13：枢纽豁免本身——不采纳，SPEC 从无此规定，且会让「无内容的中间层」永久免死。
- P13：`待应用清单 §2.1`（ten-axioms 改边）作废——移出本轮，原文自述归属不明。
- P15 §三(4) 回退版（`:95` 改窄口径「父+兄弟」）——不采纳，代价 161/546 变 `NOT-TESTABLE`、51/85 条 KILL 不合规。
- S3 速查表第 16 行「全图冗余另立死因」——不采纳，跨分支重复归 `0` 或 `6`。
- S3 §二 的 `SPEC.md:98` 版本——不采纳，与第 11 项同处冲突。
