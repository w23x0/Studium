**裁决：`nodes-D1.jsonl` 行 43 判死因 4、动作 FIX（不 KILL），并与行 46 绑为同一动作。已核。**`G6d-D1-片31-56-摘要.md` 裁决表行 43 逐字给出「封闭是两条公理，10−2=8；原文三处用复数 "closure axioms"」，§〇 记「唯一带 KILL 字样的是行 43，分片写『KILL 或强改』，实际给出了具体改法，倾向 FIX」；「顺带发现」记行 43 与行 46 分组互相矛盾（8 vs 9 条机械）、同属本片，修一处必须同时改另一处，否则留下 8 与 9 并存。

**裁决：行 40（E-7）与行 52（D-3）的死因 6 均成立，判生产者、不推 SPEC。已核。**同摘要裁决表行 40 + §二.1：唯一存在理由「这一步就是 Theorem 15.2」全无锚，`15.04.md:9` 现成可引（§四末段实测 **179** 字符），节点只用 1 锚、剩 2 空位；§二.3：行 52 与 #51 逐字共用 `15.03.md:9` 后半同一条引文（**92** 字符），#51 够用、#52 不够用，分片称其为「grep 通过率无法区分」的干净 B 型样本。两条都有现成空位。

**裁决：行 31、38、45、53、54 的 "only / the only / the first / every" 由死因 0 升死因 1。改后采纳。**同摘要「顺带发现」第 2 条自陈「分片一律记死因 0，但按封闭词表这些属量词过度断言，倾向死因 1；口径差异留给裁定员，我不改判」——本片行使该裁定权。连带：按本轮规则死因 1 不因边证据免除，这 5 条不得再用「兄弟/边已承载」减轻。

**裁决：#68 的数学缺陷成立，动作「改 `q(t) = -t^n + 1` 或注明 n ≥ 2」，statement 与边 `rel_note` 两处同改。已核。**`G6d-D1-片57-81-摘要.md` 裁决表 #68（死因 4 或 1，依据 `15.03.md:25` 末句）：见证 `p=t^n, q=-t^n+t` 声明 "both of degree n" 在 n∈{0,1} 上为假，结论仍成立、错的是普适性；§二.2 记该缺陷「在边 rel_note 中原样复制，是两处而非一处」，并称其为「本片段最重的非 D/E 问题」。本片采死因 4。

**裁决：#62 判死因 6，补 `15.03.md:33`（**69** 字符）与 `:35`（**99**，分片记 98）两句为第 2、3 锚点。已核。**同摘要 §二.1 称 #62 为「本片段最典型的 D」并强调「这不是 SPEC 的错」——两个空锚位现成、两句原文合法且长度合规；§四.5 给 `:35` 那句实测 99，§四末段把 `:33` 的 c≠0 句 69 列入「无误可放心复用」。

**裁决：#70 的两处定性择「B 型锚点错配 + 换锚为零多项式约定句」，不取「支撑充分」。改后采纳。**同摘要「顺带发现」记「#70 被分片同时列入支撑充分表和 B 型锚点错配 + 建议换锚，两处定性不一致，须择一」；裁决表给择后者的实质理由——「Axiom 5、6 都不是封闭公理，锚点在逻辑上不可能支撑该断言」。逻辑上不可能支撑者不得记为支撑充分。

**裁决：R-5 八节点（#63/64/65/72/74/75/76/78）列为待裁项，本轮不得记作已消解；「兄弟节点已承载」一律不构成减轻。改后采纳。**同摘要 §三.1（最大悬置）：主句谓语「…is a function space」普遍未锚，承载句 `15.03.md:19` 末句（§四.3 实测 **81** 字符，非 89）已被第 43 行兄弟节点占用，且同时是 #61 的 part-of 边 evidence（§四.9：`edges-D1.jsonl:149`）；§五末段按四层池判兄弟不属 T1/T2/T3 任何一层（除非恰为 part-of 后代），涉 #60、#81 与这 8 个节点。**与既存裁决 1 不冲突，但给它加一条约束**：裁决 1 要在 `15.03.md:19` 内补一条锚点，而该行 `n_sentence_end=4`，新锚须取非末句子句，否则与第 43 行节点及 `edges-D1.jsonl:149` 逐字重合，制造新的 B 型借用。

**裁决：#66 恢复初判 E，不采纳「支撑充分」改判。已核。**同摘要「顺带发现」：该改判「依据是两条边的 rel_note，而 rel_note 不是 evidence、不入四层池；按池定义该节点 T2 为空，E 判可能应恢复」；同处指出 #69 同属 rel_note 却照判 D，「与 #66 自相矛盾」。取 #69 的口径为准。

**裁决：`nodes-D3.jsonl` L26 的方程编号错引由死因 0 改判死因 3（虚构出处）。改后采纳。**`G6d-D3-片21-45-摘要.md` 裁决表 L26：statement 写 "taking square roots in (15.7)" 为假——`\tag{15.7}` 全书唯一在 `15.11.md:46`（内容是 x=Σcᵢeᵢ），定理 15.8 的显示行 `15.10.md:86` 根本没有编号，两点已用 source-lines.tsv 复核；「顺带发现」末段自陈「形态上更接近死因 3 而非 0，建议裁定方复核该死因归属」。分片核过行 39 的 (15.6)、行 31 的 (15.8) 均正确，属孤立笔误。

**裁决：L30 成因归 SPEC 30 字符下限、不判生产者；形态统一归 E-2，D 型仍定 3 例（L25、L33、L43）；并给 L25 补从严备选。改后采纳。**同摘要 §二.8：`:123` 实测 **29** 字符（差 1），分片明确不判生产者的死；§四.2 记 L30 形态标签在同一报告内三处不一致（§8 判形式 D、§1 总账 D 型 3 例不含 30、§3 放进 E-2），须择一否则 D 型计数在 3 与 4 之间摆动；「顺带发现」记 L25 与 L33/L43 同型（末句超射程）而只有后两者有从严备选，「从严标准在分片内未一致施用」。

**裁决：L40 的「实空间限定」那半降为记账位置问题，其余缺口指控保留。改后采纳。**同摘要 §5.3 实测 T2 新增 `edges-D3.jsonl:40` 引的 `:151` **完整 183 字符**（含 "In a real Euclidean space V"），故实空间限定确已在图里、只是记在边上；同处确认分片指认的另两处缺口 `:125`（定理 15.9(b)）与 `:22`（公理(4)）**未被命中**，指控不动。

**裁决：L51 与 L69 合并同判死因 1，L66 亦判死因 1，三条均不因边证据免除。已核。**`G6d-D3-片46-69-摘要.md` §二.1：L51、L69 是「同一种越界犯两次」（把 (15.11)/(15.12) 的有限维正交规范基结论外推到无限维 C(0,2π) 的无限三角系），分片建议合并裁定；裁决表 L66「锚点 2 = `:75` 逐字含 finite-dimensional，claim 3 把原文复述成 **every**」；§五表实测三条 T2 均不塌陷（L51 的 3 条全重复 `:35`、L69 的 2 条只重复 `:86`/`:89`、L66 新引 `:89` 与量词缺口无关），§五末段记死因 1 不因边证据免除。

**裁决：L67 取「死因 0 + 强制改写」，不升 4；L64 改述为句粒度未引，本轮只留指针。改后采纳。**同摘要 §〇 记「唯一可升 4 的候选是 L67 claim 2 的定位性假陈述（"两节之内"），非数学错误」，裁决表记三次出现均在 `:15`、`:61`、`:89` 且同属 §15.11；§四.6 实测 `:89` 三句已分别被 L67（第 1 句）与 L69（第 3 句）锚用，故 L64「可引未引」应表述为句粒度未引；§二.4 注明主控 §4.11 已单独裁定 L64。

**裁决：D-5（L61）、D-6（L50）两条指控撤回；但 L46/L47 严重度不下调。已核。**同摘要 §五表：`edges-D3.jsonl:148`（src=L61，requires）evidence 逐字含 `:67` 的「since $(e_i,e_j)=0$ if $i \neq j$」，`edges-D3.jsonl:49`（src=L50，part-of）逐字含 `(u_0,u_0)=\int_0^{2\pi}dx=2\pi`，两条 T2 塌陷、属记账位置问题。「顺带发现」反向实测 L47 的 2 条 T2 evidence 与 T1 完全相同、去重后源行仍为 1（与分片 §9 第 6 条自标方向相反），故塌陷不得推广到 L46/L47。

**裁决：SPEC 3 锚点上限不能作任何一条缺陷的成因，凡以此归因者一律驳回。已核。**三份合证：`G6d-D1-片31-56-摘要.md` §四.7 实测行 46、55 的 `n_anchors` 均为 **2**、各剩 1 空位，「3 锚点上限致证据不足」归因偏软；`G6d-D3-片46-69-摘要.md` §四.8 实测第 46–69 行为 9 个 1 锚点 + 15 个 2 锚点、**无一用满 3 条**；`G6d-D4-片28-54-摘要.md` §〇 元观察 (1) / §6.3 实测 27 节点无一用满（4 个用 1 条、23 个用 2 条），1 锚的 4 个为 L29/35/38/45、与 nodes.tsv `n_anchors` 列一致，其 17 处缺陷一处也不能归因 SPEC。（`nodes-D4.jsonl:51` 填满第三槽后的顺序约束已由既存追加裁决 丙 处理，不重复。）

**裁决：`G6d-D4-片1-27-摘要.md` §四 两处记账错采纳——行 16 的第三槽是空的，§6 抬头受约束节点数应为 5。已核。**§四.1 实测 `nodes-D4.jsonl:16` 的 `n_anchors=2`、`anchors.tsv` 只有 idx 0/1（105 + 79 字符），「槽位已满」写反；纠正后行 16 的「换锚 vs 削减 statement」两难消失——`15.08.md:17` 可直接占空槽（§四末段实测 **187** 字符、3 个句末标点，须取子句）。§四.2 按 §6.1–6.3 取并集：30 字符下限 {19,20,27}、3 锚点上限 {16,20,27}、跨行禁止 {10}，并集 **5** 个节点、其中行 20 与 27 各受两条，抬头的「6 个、1 个受两条」错。

**裁决：D4 行 14 的死因 1（`exactly two load-bearing places`）与派生缺边指控成立；「与 D3 结论不冲突」未复核。**同摘要 §二.1 给推翻路径（换成内积语言后出现第三个使用点 `15.11.md:15`，即定理 15.10 证明里「But $(x_1,x_1)\neq O$ since $x_1\neq O$ so $c_1=0$」），并指该节点自己的 part-of 后代行 15 明说该事实「carries no content beyond positivity」，按节点自身口径必须计入；§四末段实测 `edges.tsv` 中 `d:15-10-proof-divide-by-nonzero-self-inner-product` 只有 4 条边、确无指向 `d:zero-is-the-only-element-orthogonal-to-itself` 的边。「顺带发现」自陈 `15.11.md:15` 属 D3 片区领地、建议交叉核，本片无 D3 对应节点材料；该行实测 **463** 字符、6 个句末标点，补锚补边须取子句。

**裁决：`node_type` 越出封闭词表列为独立待查项，不并入任何 FIX 批。已核（词表是否为 SPEC 所定 未复核）。**`G6d-D3-片21-45-摘要.md`「顺带发现」实测「全 nodes-D3.jsonl 计 method 12 个、notation 5 个」，分片只登记了 L35/36/37 间 method 与 theorem 的不一致、未指出两词越表；`G6d-D4-片28-54-摘要.md`「顺带发现」实测该片 27 节点出现 method 12 个、notation 2 个（L29、L54），涉 14 个节点，量级高于该片全部 FIX。两份均称词表为 concept/theorem/proof-step/example，但本片无 SPEC 原文，封闭性未复核。

**裁决：与既存裁决 2 冲突（不改写裁决 2）——「`origin: model` 只在 edge schema、故 23 个含无锚 claim 的节点无一能标」不成立；该项「作 schema 议题上报、不记生产者」的那半采纳。已核。**`G6d-D3-片21-45-摘要.md`「顺带发现」第 6 项仅转述分片 §7 的说法；而 `G6d-D4-片1-27-摘要.md` §四末段实测「§8 声称的 `origin: model` 五节点（行 15、19、20、23、27）与 `nodes.tsv` 的 origin 列完全一致」，`G6d-D4-片28-54-摘要.md`「顺带发现」实测缺 origin 的 6 个节点（30、31、36、45、48、51）origin 列为空——两处都以**节点级** origin 列存在为前提；既存裁决 2 亦实测 `origin=model` 的 24 节点中 20 有锚、4 无锚。故 D3 那 23 个节点的成因须另找，归口 `G5-P08-节点是否可带 origin.md`（本片未读）。

**裁决：`G6d-D4-片28-54-摘要.md` 裁决表 L48 的「强制换锚为 `15.15.md:17` + `:23`（保留 `:9`）」本轮不照原样装配，指控本身不撤。已核。**既存追加裁决 丙 已核 L17 的 96 字符长跨被 `edges-A2-s3.jsonl:4` 与 `edges-D4.jsonl:126` 占用；本片补一层：该摘要裁决表 L48 的指控（两锚 `15.15.md:3`、`:9` 全在定理陈述，claim 2–7 即 statement 全部实质内容无锚）成立，故须先给出与 `:50`/`:52` 自有锚点不逐字重合的子句跨并实测字符数，再落换锚。

**裁决：L51「是否按死因 4」这条未决关闭为不升 4，但其最小动作（末句「the error」两处改「the squared error」）降级、不单独采纳。已核。**该摘要 §3.1 第 2 条把从严选项写为条件句、裁决表 L51 动作仅此一项；既存追加裁决 丙 已定 `nodes-D4.jsonl:51` 按 A6 §2.4 整句替换 statement，最小改法保留的 `is the substantive content of the approximation theorem` 射程缺陷随之消解，故不需要也不应再单独执行该最小动作。

**裁决：计数与字符数一律以摘要实测值为准。已核。**`G6d-D1-片31-56-摘要.md` §四.1：形式 E = **20** 例（§3 标题的 17 错）；`G6d-D1-片57-81-摘要.md` §四.1/2：总账 **25 = 6 D + 4 E + 15**（原「5 例 D」漏 #80，表实为 15 行）；`G6d-D3-片46-69-摘要.md` §四.1/2/3：补锚 9 + 需改写 6 = **24**，24/24 节点均含无锚 claim、末句例外实有两个（L66、L59），「22 个 + 唯一例外」二者只能取一。字符数（括号为分片错值）：`15.03.md:19` 末句 **81**（89）、"for every real x in the intersection of the domains of f and g." **63**（61）、`15.02.md:23` **87**（88）、`15.10.md:105` **90**（89）、`15.11.md:5` **170**、`:7` **238**（分片按拟引子串估 98/79）；无误可复用：`15.02.md:9`=175、`15.10.md:19`=21、`:108`=26、`:123`=29。

**裁决：多句行行号禁作句级证据复用（扩展既存裁决 1 的过报通道 b，不改写该裁决）。已核。**`G6d-D1-片57-81-摘要.md` §四.13 实测 `15.03.md` 的 `:19/:25/:33/:35/:37` 的 `n_sentence_end` 均为 **4**；`G6d-D3-片46-69-摘要.md` §四.6 实测 `15.11.md` 的 `:5`(2 句)、`:7`(3)、`:29`(3)、`:35`(2)、`:67`(2)、`:89`(3)、`15.11.md:3`(4)。这些行号在本轮任何覆盖比对里都须回落到具体句子。

**本片未覆盖**（逐条落点与来源）：(1) `G6d-D1-片31-56-摘要.md` §四.6 的「13 个 (file, quote) 对」不采纳（表体只 11 行、shared-quotes.tsv 实为 15 行，13 无出处）；把 `15.02.md:51` 推荐给行 48 移出本轮（该行 791 字符远超 SPEC 200 上限，须先定引哪个子句）。(2) `G6d-D1-片57-81-摘要.md`：#57、#61 归 SPEC 依赖未读的 SPEC.md，移出本轮；#72/#74/#78 计入 SPEC 计数（S-3）不采纳（正文自陈成因是源材料范围）；§四.10 实测 R-2 的共享引文共 **16 个持有者 = 7 节点 + 9 条边**，B 型扇出须重算。(3) `G6d-D3-片21-45-摘要.md` §3 的 E-1/E-2/E-3 组表不采纳（§四实测多算 L37/L42、漏算 L26/L40，20 靠错误抵消凑成）；§七第 5 项「3 对片区内 + 2 对跨片区」不采纳（实测 `15.11.md:98` 有 8 个持有者、`:133` 有 7、`:105` 有 6）；该片 20 例 E 未逐条过 T2 池。(4) `G6d-D3-片46-69-摘要.md` 的 14 处 E-中未逐条过 T2 池；§三.7 的 Pythagorean 同名冲突（(15.17) 在 `15.14.md:24`、`15.07.md:29` 的 "Pythagorean identity" 指 cos²+sin²=1）待 D4/D5 交叉核。(5) 两份 D4 摘要裁决表其余逐行动作（片1-27 的 12 条 FIX、片28-54 的 17 条 FIX）的补锚目标与字符数未回查原文。(6) `G6d-D4-片28-54-摘要.md` §3.1.1（严格执行 SPEC 第 55 行则 L30 末句应删）无 SPEC 原文，未复核。(7) 四份 D1/D3 摘要的 A 型、C 型、edges 完备性、`atomic_reason` 正确性皆未核；两份 D4 摘要自陈未系统审边与 A/C 型，故其「D4 第 1–54 行无 KILL、无撤回」只在形式 D/E 视角下成立。(8) 两条 B 型线索另立：D4 片1-27 行 15 锚点 2 与行 19 复用同一引文；片28-54 L44 锚 1 与行 36 共用（分片称「本片唯一真借用」）。
