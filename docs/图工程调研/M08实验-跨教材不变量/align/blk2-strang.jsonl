{"tok": "s200", "type": "theorem", "statement": "The vectors v1,...,vn are a basis for R^n exactly when they are the columns of an n by n invertible matrix. Then Ax = 0 forces x = 0 (independence) and Ax = b is always solvable by x = A^{-1}b (spanning), so R^n has infinitely many different bases.", "quotes": ["The vectors $v _ { 1 } , \\ldots , v _ { n }$ are a basis for $\\mathbf { R } ^ { n }$ exactly when they are the columns of an n by n invertible matrix.", "The columns of every invertible n by n matrix give a basis for $\\mathbf { R } ^ { n }$", "The only solution to $A x = \\mathbf { 0 }$ is $\\pmb { x } = \\pmb { A } ^ { - 1 } \\mathbf { 0 } = \\mathbf { 0 }$ . The columns are independent."]}
{"tok": "s201", "type": "concept", "statement": "A sequence of vectors v1,...,vn is linearly independent if the only combination x1v1+...+xnvn that gives the zero vector is the one with all x's equal to zero. Otherwise the vectors are dependent. This definition applies to any sequence of vectors in any vector space.", "quotes": ["DEFINITION The sequence of vectors $v _ { 1 } , \\ldots , v _ { n }$ is linearly independent if the only combination that gives the zero vector is", "Correct language: \"The sequence of vectors is linearly independent.\" Acceptable shortcut: \"The vectors are independent.\""]}
{"tok": "s202", "type": "concept", "statement": "The columns of A are linearly independent when the only solution to Ax = 0 is x = 0; no other combination Ax of the columns gives the zero vector. Equivalently the nullspace of A contains only the zero vector.", "quotes": ["DEFINITION The columns of A are linearly independent when the only solution to Ax = 0 is x = 0. No other combination Ax of the columns gives the zero vector.", "The columns are independent when the nullspace $N ( A )$ contains only the zero vector."]}
{"tok": "s203", "type": "concept", "statement": "Vectors are dependent when some combination with x's not all zero gives the zero vector; equivalently one vector is a combination of the other vectors. A sequence of vectors is either dependent or independent.", "quotes": ["If a combination gives 0, when the x's are not all zero, the vectors are dependent.", "Note Another way to describe linear dependence is this: \"One vector is a combination of the other vectors.\"", "A sequence of vectors is either dependent or independent."]}
{"tok": "s204", "type": "concept", "statement": "Independence, basis and dimension are not restricted to column vectors. The space of all 2 by 2 matrices has dimension 4 and the n by n matrix space has dimension n^2; the solution space of y'' = -y has the two basis functions sin x and cos x, so its dimension is 2.", "quotes": ["The words \"independence\" and \"basis\" and \"dimension\" are not at all restricted to column vectors.", "Matrix spaces The vector space M contains all 2 by 2 matrices. Its dimension is 4.", "That solution space for $y ^ { \\prime \\prime } = - y$ has two basis functions: sin x and cos x."]}
{"tok": "s205", "type": "theorem", "statement": "If a matrix has more columns than rows (n > m), its columns are certainly dependent: Ax = 0 has at least n - m free variables and therefore a nonzero solution. Any seven vectors from R^5 are dependent.", "quotes": ["The columns are certainly dependent if $n > m$ , because $A x = \\mathbf { 0 }$ has a nonzero solution.", "Then the columns must be dependent. Any seven vectors from $\\mathbf { R } ^ { 5 }$ are dependent."]}
{"tok": "s206", "type": "method", "statement": "To find a basis for the space spanned by given vectors: either make them the rows of A and eliminate, keeping the nonzero rows of R, or put them in the columns of A and eliminate, keeping the pivot columns of A (not of R).", "quotes": ["Question Given five vectors in ${ \\bf R } ^ { 7 }$ , how do you find a basis for the space they span?", "First answer Make them the rows of A, and eliminate to find the nonzero rows of R.", "Eliminate to find the pivot columns (of A not R). Those pivot columns are a basis for the column space."]}
{"tok": "s207", "type": "concept", "statement": "A basis for a vector space is a sequence of vectors that are linearly independent and that span the space: not too many and not too few. A space has infinitely many different bases.", "quotes": ["DEFINITION A basis for a vector space is a sequence of vectors with two properties:", "The basis vectors are linearly independent and they span the space.", "Two vectors $\\mathrm { c a n ' t }$ span all of $\\mathbf { R } ^ { 3 }$ even if they are independent."]}
{"tok": "s208", "type": "theorem", "statement": "Every vector v in the space is a combination of the basis vectors because they span the space, and there is one and only one such combination because the basis vectors are independent.", "quotes": ["There is one and only one way to write v as a combination of the basis vectors.", "A basis consists of linearly independent vectors that span the space. Every vector in the space is a unique combination of the basis vectors.", "Every vector v in the space is a combination of the basis vectors, because they span the space."]}
{"tok": "s209", "type": "theorem", "statement": "If v1,...,vm and w1,...,wn are both bases for the same vector space, then m = n. The proof assumes n > m, writes W = VA with A short and wide, and gets a nonzero solution of Ax = 0, so a combination of the w's would be zero.", "quotes": ["If $\\pmb { v } _ { 1 } , \\ldots , \\pmb { v } _ { m }$ and $w _ { 1 } , \\ldots , w _ { n }$ are both bases for the same vector space, then $m = n$", "All bases for a space have the same number of vectors. This number of vectors in a basis is the dimension of the space."]}
{"tok": "s210", "type": "concept", "statement": "The dimension of a space is the number of vectors in every basis for that space. It counts the degrees of freedom in the space; the dimension of R^n is n. We never speak of the rank of a space or the dimension of a basis.", "quotes": ["DEFINITION The dimension of a space is the number of vectors in every basis", "The number of vectors, in any and every basis, is the \"dimension\" of the space."]}
{"tok": "s211", "type": "theorem", "statement": "The columns of A are independent exactly when the rank is r = n: there are n pivots and no free variables, so only x = 0 is in the nullspace. Independent columns produce full column rank.", "quotes": ["Full column rank The columns of A are independent exactly when the rank is $r = n .$ There are n pivots and no free variables.", "The rank is only $r = 2$ . Independent columns produce full column rank $r = n = 3$"]}
{"tok": "s212", "type": "concept", "statement": "The columns of the n by n identity matrix give the standard basis for R^n: i and j in R^2, and i, j, k in R^3. They are independent and they span the space.", "quotes": ["The columns of the 3 by 3 identity matrix are the standard basis $i , j ,$ , k.", "The columns of the n by n identity matrix give the \"standard basis\" for $\\mathbf { R } ^ { n }$"]}
