## Problem Set 4.1

Questions 1-12 grow out of Figures 4.2 and 4.3 with four subspaces

1 Construct any 2 by 3 matrix of rank one. Copy Figure 4.2 and put one vector in each subspace (and put two in the nullspace). Which vectors are orthogonal?

2 Redraw Figure 4.3 for a 3 by 2 matrix of rank $r = 2$ . Which subspace is $z$ (zero vector only)? The nullspace part of any vector x in $\mathbf { R } ^ { 2 }$ is ${ \boldsymbol { x } } _ { n } =$

3 Construct a matrix with the required property or say why that is impossible:

(a) Column space contains $\left[ { \begin{array} { r } { 1 } \\ { 2 } \\ { - 3 } \end{array} } \right]$ and $\left[ - { 3 \atop 5 } \right]$ , nullspace contains $\begin{array} { r } { \left[ \begin{array} { l } { \mathbf { 1 } } \\ { \mathbf { 1 } } \\ { \mathbf { 1 } } \end{array} \right] } \end{array}$

(b) Row space contains ${ \left[ \begin{array} { l } { 1 } \\ { 2 } \\ { - 3 } \end{array} \right] } { \mathrm { ~ a n d ~ } } { \left[ \begin{array} { l } { 2 } \\ { - 3 } \\ { 5 } \end{array} \right] }$ , nullspace contains $\begin{array} { r } { \left[ \begin{array} { l } { \mathbf { 1 } } \\ { \mathbf { 1 } } \\ { \mathbf { 1 } } \end{array} \right] } \end{array}$

( c) $\begin{array} { r } { A x = \left[ \begin{array} { l } { \mathbf { 1 } } \\ { \mathbf { 1 } } \\ { \mathbf { 1 } } \end{array} \right] } \end{array}$ has a solution and $A ^ { \mathrm { T } } \left[ { \bf \frac { 1 } { 0 } } \right] = \left[ \bf { 0 } \right]$

(d) Every row is orthogonal to every column (A is not the zero matrix)

( e) Columns add up to a column of zeros, rows add to a row of ${ 1 3 }$

If $A B = 0$ then the columns of B are in the of A. The rows of A are in the of B. With $A B = 0$ , why can't A and B be 3 by 3 matrices of rank 2?

(a) If $A x = b$ has a solution and $A ^ { \mathrm { T } } y = \mathbf { 0 } , i s \left( y ^ { \mathrm { T } } x = 0 \right) o r ( y ^ { \mathrm { T } } b = 0 ) \}$

(b) If $A ^ { \mathrm { T } } y = ( 1 , 1 , 1 )$ has a solution and $A x = \mathbf { 0 }$ , then \_\_ .

6 This system of equations $A x = b$ has no solution (they lead to $0 = 1 )$ ):

$$
\begin{array}{r c l} x + 2 y + 2 z & = & 5 \\ 2 x + 2 y + 3 z & = & 5 \\ 3 x + 4 y + 5 z & = & 9 \end{array}
$$

Find numbers $y _ { 1 } , y _ { 2 } , y _ { 3 }$ to multiply the equations so they add to $0 = 1$ . You have found a vector yin which subspace? Its dot product $_ { y ^ { \mathrm { T } } b }$ is 1, so no solution x.

7 Every system with no solution is like the one in Problem 6. There are numbers $y _ { 1 } , \ldots , y _ { m }$ that multiply the m equations so they add up to $0 = 1$ . This is called Fredholm's Alternative:

Exactly one of these problems has a solution

$$
A \boldsymbol {x} = \boldsymbol {b} \quad \mathbf {O R} \quad A ^ {\mathrm{T}} \boldsymbol {y} = \boldsymbol {0} \quad \text { with } \quad \boldsymbol {y} ^ {\mathrm{T}} \boldsymbol {b} = 1.
$$

If b is not in the column space of A, it is not orthogonal to the nullspace of $A ^ { \mathrm { T } }$ Multiply the equations $x _ { 1 } - x _ { 2 } = 1$ and $x _ { 2 } - x _ { 3 } = 1$ and $x _ { 1 } - x _ { 3 } = 1$ by numbers $y _ { 1 } , y _ { 2 } , y _ { 3 }$ chosen so that the equations add up to $0 = 1$

8 In Figure 4.3, how do we know that $A x _ { r }$ is equal to Ax? How do we know that this vector is in the column space? If $A = \left[ \begin{array} { l l } { \mathbf { 1 } } & { \mathbf { 1 } } \\ { \mathbf { 1 } } & { \mathbf { 1 } } \end{array} \right]$ and $x = [ \mathbf { \vec { 0 } } ]$ what is $\boldsymbol { x } _ { r } ?$

9 If $A ^ { \mathrm { T } } A x = 0$ then $A x = 0$ . Reason: Ax is in the nullspace of $A ^ { \mathrm { T } }$ and also in the of A and those spaces are \_\_ . Conclusion: $A ^ { \hat { \mathrm { T } } } A$ has the same nullspace as A. This key fact is repeated in the next section.

10 Suppose A is a symmetric matrix $( A ^ { \mathrm { T } } = A )$

(a) Why is its column space perpendicular to its nullspace?

(b) If $A x = \mathbf { 0 }$ and $A z = 5 z$ , which subspaces contain these "eigenvectors" x and $z ?$ Symmetric matrices have perpendicular eigenvectors $x ^ { \mathrm { T } } z = 0$

11 (Recommended) Draw Figure 4.2 to show each subspace correctly for

$$
A = \left[ \begin{array}{c c} 1 & 2 \\ 3 & 6 \end{array} \right] \quad \text {and} \quad B = \left[ \begin{array}{c c} 1 & 0 \\ 3 & 0 \end{array} \right].
$$

12 Find the pieces ${ \mathbf { \mathcal { x } } } _ { r }$ and $\scriptstyle { \mathbf { \mathscr { x } } } _ { n }$ and draw Figure 4.3 properly if

$$
A = \left[ \begin{array}{c c} 1 & - 1 \\ 0 & 0 \\ 0 & 0 \end{array} \right] \quad \text { and } \quad x = \left[ \begin{array}{c} 2 \\ 0 \end{array} \right].
$$

Questions 13-23 are about orthogonal subspaces.

13 Put bases for the subspaces V and W into the columns of matrices V and $W$ . Explain why the test for orthogonal subspaces can be written $V ^ { \mathrm { T } } W =$ zero matrix. This matches ${ \pmb v } ^ { \mathrm { T } } { \pmb w } = 0$ for orthogonal vectors.

14 The floor V and the wall W are not orthogonal subspaces, because they share a nonzero vector (along the line where they meet). No planes $V$ and $W$ in $\dot { \mathbf { R } } ^ { 3 }$ can be orthogonal! Find a vector in the column spaces of both matrices:

$$
A = \left[ \begin{array}{c c} 1 & 2 \\ 1 & 3 \\ 1 & 2 \end{array} \right] \qquad \text {and} \qquad B = \left[ \begin{array}{c c} 5 & 4 \\ 6 & 3 \\ 5 & 1 \end{array} \right]
$$

This will be a vector Ax and also $B \widehat { \mathbfit { x } }$ . Think 3 by 4 with the matrix $\left[ A \ B \right]$

Extend Problem 14 to a p-dimensional subspace V and a q-dimensional subspace $W$ of $\mathbf { R } ^ { n }$ . What inequality on $p + q$ guarantees that $V$ intersects $W$ in a nonzero vector? These subspaces cannot be orthogonal.

Prove that every y in $N ( A ^ { \mathrm { T } } )$ is perpendicular to every Ax in the column space, using the matrix shorthand of equation (2). Start from $A ^ { \mathrm { { \hat { T } } } } y = 0$

11 If S is the subspace of $\mathbb { R } ^ { 3 }$ containing only the zero vector, what is $S ^ { \perp } ?$ If S is spanned by $( 1 , 1 , 1 )$ , what is $S ^ { \perp } ?$ If Sis spanned by $( 1 , 1 , 1 )$ and $( 1 , 1 , - 1 )$ ), what is a basis for $S ^ { \perp } ?$

18 Suppose Sonly contains two vectors (1, 5, 1) and (2, 2, 2) (not a subspace). Then $S ^ { \perp }$ is the nullspace of the matrix $A = \_ { s ^ { \perp } }$ is a subspace even if S is not.

19 Suppose L is a one-dimensional subspace (a line) in $\mathbb { R } ^ { 3 }$ . Its orthogonal complement $L ^ { \perp }$ is the \_\_ perpendicular to L. Then $( { L } ^ { \perp } ) ^ { \perp }$ is a \_\_ perpendicular to $L ^ { \perp }$ In fact $( { L } ^ { \perp } ) ^ { \perp }$ is the same as

20 Suppose V is the whole space $\mathbb { R } ^ { 4 }$ . Then $V ^ { \perp }$ contains only the vector \_\_ . Then $( V ^ { \perp } ) ^ { \perp } \mathrm { i s } \_ \mathrm { \phantom { | } } \_ \mathrm { S 0 } ( V ^ { \perp } ) ^ { \perp }$ is the same as \_

21 Suppose Sis spanned by the vectors $( 1 , 2 , 2 , 3 )$ and (1, 3, 3, 2). Find two vectors that span $S ^ { \perp }$ . This is the same as solving $A x = \mathbf { 0 }$ for which A?

22 If P is the plane of vectors in $\mathbb { R } ^ { 4 }$ satisfying $x _ { 1 } + x _ { 2 } + x _ { 3 } + x _ { 4 } = 0$ , write a basis for $\pmb { P } ^ { \perp }$ . Construct a matrix that has P as its nullspace.

23 If a subspace Sis contained in a subspace V, prove that $S ^ { \perp }$ contains $V ^ { \perp }$ .

Questions 24-30 are about perpendicular columns and rows.

24 Suppose an n by n matrix is invertible: $A A ^ { - 1 } = I$ . Then the first column of $A ^ { - 1 }$ is orthogonal to the space spanned by which rows of A?

25 Find $A ^ { \mathrm { T } } A$ if the columns of A are unit vectors, all mutually perpendicular.

26 Construct a 3 by 3 matrix A with no zero entries whose columns are mutually per pendicular. Compute $A ^ { \mathrm { T } } A$ . Why is it a diagonal matrix?

27 The lines $3 x + y = b _ { 1 }$ and $6 x + 2 y = b _ { 2 }$ are \_\_ . They are the same line if In that case $( b _ { 1 } , b _ { 2 } )$ is perpendicular to the vector \_\_ . The nullspace of the matrix is the line $3 x + y = -$ . One particular vector in that nullspace is

28 Why is each of these statements false?

(a) $( 1 , 1 , 1 )$ is perpendicular to $( 1 , 1 , - 2 )$ so the planes $x + y + z = 0$ and $x + y -$ $2 z = 0$ are orthogonal subspaces

(b) The subspace spanned by $( 1 , 1 , 0 , 0 , 0 )$ and $( 0 , 0 , 0 , 1 , 1 )$ is the orthogonal com plement of the subspace spanned by $( 1 , - 1 , 0 , 0 , 0 )$ and $( 2 , - 2 , 3 , 4 , - 4 )$ ).

(c) Two subspaces that meet only in the zero vector are orthogonal.

Find a matrix with $\pmb { v } = ( 1 , 2 , 3 )$ in the row space and column space. Find another matrix with v in the nullspace and column space. Which pairs of subspaces can v notbe in?

## Challenge Problems

Suppose A is 3 by 4 and B is 4 by 5 and $A B = 0$ . So $N ( A )$ contains $C ( B )$ Prove from the dimensions of $N ( A )$ and $C ( B )$ that $\operatorname { r a n k } ( A ) + \operatorname { r a n k } ( B ) \leq 4$

The command $N = { \mathsf { n u l l } } ( A )$ will produce a basis for the nullspace of A. Then the command $B = \mathsf { n u l l } ( N ^ { \prime } )$ will produce a basis for the \_\_ of A.

32 Suppose I give you four nonzero vectors r, n, c, l in $\mathbb { R } ^ { 2 }$ .

(a) What are the conditions for those to be bases for the four fundamental subspaces $C ( A ^ { \mathrm { T } } ) , N ( A ) , C ( A ) , N ( A ^ { \mathrm { T } } )$ of a 2 by 2 matrix?

(b) What is one possible matrix A?

Suppose I give you eight vectors $r _ { 1 } , r _ { 2 } , n _ { 1 } , n _ { 2 } , c _ { 1 } , c _ { 2 } , l _ { 1 } , l _ { 2 }$ in $\mathbb { R } ^ { 4 }$

(a) What are the conditions for those pairs to be bases for the four fundamenta subspaces of a 4 by 4 matrix?

(b) What is one possible matrix A?

