## Problem Set 3.3

1 (Recommended) Execute the six steps of Worked Example 3.3 A to describe the column space and nullspace of A and the complete solution to $A x = b ;$

$$
A = \left[ \begin{array}{l l l l} 2 & 4 & 6 & 4 \\ 2 & 5 & 7 & 6 \\ 2 & 3 & 5 & 2 \end{array} \right] \qquad \boldsymbol {b} = \left[ \begin{array}{l} b _ {1} \\ b _ {2} \\ b _ {3} \end{array} \right] = \left[ \begin{array}{l} 4 \\ 3 \\ 5 \end{array} \right]
$$

2 Carry out the same six steps for this matrix A with rank one. You will find two conditions on $b _ { 1 } , b _ { 2 } , b _ { 3 }$ for $A x = b$ to be solvable. Together these two conditions put b into the \_\_ space (two planes give a line):

$$
A = \left[ \begin{array}{l} 1 \\ 3 \\ 2 \end{array} \right] \quad \left[ \begin{array}{l l l} 2 & 1 & 3 \end{array} \right] = \left[ \begin{array}{l l l} 2 & 1 & 3 \\ 6 & 3 & 9 \\ 4 & 2 & 6 \end{array} \right] \qquad \boldsymbol {b} = \left[ \begin{array}{l} b _ {1} \\ b _ {2} \\ b _ {3} \end{array} \right] = \left[ \begin{array}{l} 1 0 \\ 3 0 \\ 2 0 \end{array} \right]
$$

Questions 3-15 are about the solution of $A x = b $ . Follow the steps in the text to $x _ { p }$ and ${ \boldsymbol { x } } _ { n }$ Start from the augmented matrix with last column b.

3 Write the complete solution as $\scriptstyle { \boldsymbol { \mathbf { { x } } } } _ { p }$ plus any multiple of s in the nullspace:

$$
\begin{array}{c} {x + 3 y + 3 z = 1} \\ {2 x + 6 y + 9 z = 5} \\ {- x - 3 y + 3 z = 5.} \end{array}
$$

3.3. The Complete Solution to $A x = b$

4 Find the complete solution (also called the general solution) to

$$
{\left[ \begin{array}{l l l l} 1 & 3 & 1 & 2 \\ 2 & 6 & 4 & 8 \\ 0 & 0 & 2 & 4 \end{array} \right]} {\left[ \begin{array}{l} x \\ y \\ z \\ t \end{array} \right]} = {\left[ \begin{array}{l} 1 \\ 3 \\ 1 \end{array} \right]}.
$$

Under what condition on $b _ { 1 } , b _ { 2 } , b _ { 3 }$ is this system solvable? Include b as a fourth column in elimination. Find all solutions when that condition holds:

$$
\begin{array}{c} x + 2 y - 2 z = b _ {1} \\ 2 x + 5 y - 4 z = b _ {2} \\ 4 x + 9 y - 8 z = b _ {3}. \end{array}
$$

What conditions on $b _ { 1 } , b _ { 2 } , b _ { 3 } , b _ { 4 }$ make each system solvable? Find x in that case:

$$
\left[ \begin{array}{c c} 1 & 2 \\ 2 & 4 \\ 2 & 5 \\ 3 & 9 \end{array} \right] \left[ \begin{array}{c} x _ {1} \\ x _ {2} \end{array} \right] = \left[ \begin{array}{c} b _ {1} \\ b _ {2} \\ b _ {3} \\ b _ {4} \end{array} \right]
$$

$$
{\left[ \begin{array}{l l l} 1 & 2 & 3 \\ 2 & 4 & 6 \\ 2 & 5 & 7 \\ 3 & 9 & 1 2 \end{array} \right]} {\left[ \begin{array}{l} x _ {1} \\ x _ {2} \\ x _ {3} \end{array} \right]} = {\left[ \begin{array}{l} b _ {1} \\ b _ {2} \\ b _ {3} \\ b _ {4} \end{array} \right]}.
$$

Show by elimination that $\left( { b _ { 1 } , b _ { 2 } , b _ { 3 } } \right)$ is in the column space if $b _ { 3 } - 2 b _ { 2 } + 4 b _ { 1 } = 0$

$$
A = \left[ \begin{array}{c c c} 1 & 3 & 1 \\ 3 & 8 & 2 \\ 2 & 4 & 0 \end{array} \right].
$$

What combination of the rows of A gives the zero row?

Which vectors $\left( { b _ { 1 } , b _ { 2 } , b _ { 3 } } \right)$ are in the column space of A? Which combinations of the rows of A give zero?

(a)

$$
A = \left[ \begin{array}{c c c} 1 & 2 & 1 \\ 2 & 6 & 3 \\ 0 & 2 & 5 \end{array} \right]
$$

(h)

$$
A = \left[ \begin{array}{c c c} 1 & 1 & 1 \\ 1 & 2 & 4 \\ 2 & 4 & 8 \end{array} \right].
$$

(a) The Worked Example 3.3 A reached $\textbf { \textit { [ U \textit { c ] } } }$ from $\left[ \begin{array} { l l } { A } & { b } \end{array} \right]$ . Put the multipliers into L and verify that LU equals A and Le equals b.

(b) Combine the pivot columns of A with the numbers -9 and 3 in the particula solution $\scriptstyle { \mathbf { { \vec { x } } } } _ { p }$ . What is that linear combination and why?

Construct a 2 by 3 system Ax = b with particular solution $\mathbf { \mathit { x } } _ { p } ~ = ~ ( 2 , 4 , 0 )$ and homogeneous solution $x _ { n } = \mathrm { a n y }$ multiple of (1, 1, 1).

Why can't a 1 by 3 system have $\pmb { x } _ { p } = ( 2 , 4 , 0 )$ and $x _ { n } = \mathrm { a n y }$ multiple of $( 1 , 1 , 1 ) ?$

(a) If $A x = b$ has two solutions $\scriptstyle { \mathbf { \mathscr { x } } } _ { 1 }$ and $\mathbf { \delta x } _ { 2 }$ , find two solutions to $A x = \mathbf { 0 }$

(b) Then find another solution to $A x = 0$ and another solution to $A x = b$

13 Explain why these are all false:

(a) The complete solution is any linear combination of $\scriptstyle { \boldsymbol { x } } _ { p }$ and ${ \pmb x } _ { n }$ .

(b) A system $A x = b$ has at most one particular solution.

(c) The solution $\scriptstyle { \boldsymbol { \mathbf { { x } } } } _ { p }$ with all free variables zero is the shortest solution (minimum length $\lVert { \boldsymbol { x } } \rVert )$ . Find a 2 by 2 counterexample.

(d) If A is invertible there is no solution ${ \pmb x } _ { n }$ in the nullspace.

Suppose column 5 of Uhas no pivot. Then x5 is a \_\_ variable. The zero vector (is) (is not) the only solution to $A x = \mathbf { 0 }$ . If Ax b has a solution, then it has solutions.

15 Suppose row 3 of U has no pivot. Then that row is \_\_ . The equation $U x = c$ is only solvable provided \_\_ . The equation Ax b (is) (is not) (might not be) solvable.

## Questions 16-20 are about matrices of "full rank" r m or r n.

16 The largest possible rank of a 3 by 5 matrix is \_\_ . Then there is a pivot in every of U and R. The solution to Ax= b (always exists) (is unique). The column space of A is \_ \_ . An example is A =

17 The largest possible rank of a 6 by 4 matrix is \_\_ . Then there is a pivot in every of U and R. The solution to Ax = b (always exists) (is unique). The nullspace of A is \_\_ . An example is A =

18 Find by elimination the rank of A and also the rank of $A ^ { \mathrm { T } }$ :

$$
A = \left[ \begin{array}{c c c} 1 & 4 & 0 \\ 2 & 1 1 & 5 \\ - 1 & 2 & 1 0 \end{array} \right] \quad \text { and } \quad A = \left[ \begin{array}{c c c} 1 & 0 & 1 \\ 1 & 1 & 2 \\ 1 & 1 & q \end{array} \right] (\text { rank   depends   on } q).
$$

19 Find the rank of A and also of $A ^ { \mathrm { T } } A$ and also of $A A ^ { \mathrm { T } }$

$$
A = \left[ \begin{array}{c c c} 1 & 1 & 5 \\ 1 & 0 & 1 \end{array} \right] \quad \text { and } \quad A = \left[ \begin{array}{c c} 2 & 0 \\ 1 & 1 \\ 1 & 2 \end{array} \right].
$$

20 Reduce A to its echelon form U. Then find a triangular L so that $A = L U$

$$
A = \left[ \begin{array}{c c c c} 3 & 4 & 1 & 0 \\ 6 & 5 & 2 & 1 \end{array} \right] \quad \text { and } \quad A = \left[ \begin{array}{c c c c} 1 & 0 & 1 & 0 \\ 2 & 2 & 0 & 3 \\ 0 & 6 & 5 & 4 \end{array} \right].
$$

3.3. The Complete Solution to $A x = b$

21 Find the complete solution in the form ${ \pmb x } _ { p } + { \pmb x } _ { n }$ to these full rank systems:

$$
(a) x + y + z = 4
$$

(b)

$$
\begin{array}{l} {x + y + z = 4} \\ {x - y + z = 4.} \end{array}
$$

If $A x = b$ has infinitely many solutions, why is it impossible for $A x = B$ (new right side) to have only one solution? Could $A x = B$ have no solution?

23 Choose the number q so that (if possible) the ranks are ( a) 1, (b) 2, ( c) 3:

$$
A = \left[ \begin{array}{c c c} 6 & 4 & 2 \\ - 3 & - 2 & - 1 \\ 9 & 6 & q \end{array} \right] \quad \text {and} \quad B = \left[ \begin{array}{c c c} 3 & 1 & 3 \\ q & 2 & q \end{array} \right].
$$

24 Give examples of matrices A for which the number of solutions to $A x = b$ is

(a) 0 or 1, depending on b

(b) o, regardless of b

(c) 0 or o, depending on b

(d) 1, regardless of b.

25 Write down all known relations between r and m and n if $A x = b$ has

(a) no solution for some b

(b) infinitely many solutions for every b

(c) exactly one solution for some $^ { b , }$ no solution for other b

(d) exactly one solution for every b.

Questions 26-33 are about Gauss-Jordan elimination (upwards as well as downwards) and the reduced echelon matrix R.

26 Continue elimination from U to R. Divide rows by pivots so the new pivots are all 1. Then produce zeros above those pivots to reach $R \colon$

$$
U = \left[ \begin{array}{c c c} 2 & 4 & 4 \\ 0 & 3 & 6 \\ 0 & 0 & 0 \end{array} \right] \quad \text { and } \quad U = \left[ \begin{array}{c c c} 2 & 4 & 4 \\ 0 & 3 & 6 \\ 0 & 0 & 5 \end{array} \right].
$$

If A is a triangular matrix, when is $R = { \mathsf { r r e f } } ( A )$ equal to I?

Apply Gauss-Jordan elimination to $U x = \mathbf { 0 }$ and $U x = { \pmb { c } }$ . Reach $R x = \mathbf { 0 }$ and $R x = d \colon$

$$
\left[ \begin{array}{c c} U & \mathbf {0} \end{array} \right] = \left[ \begin{array}{c c c c} 1 & 2 & 3 & \mathbf {0} \\ 0 & 0 & 4 & \mathbf {0} \end{array} \right] \quad \text { and } \quad \left[ \begin{array}{c c} U & \boldsymbol {c} \end{array} \right] = \left[ \begin{array}{c c c c} 1 & 2 & 3 & \mathbf {5} \\ 0 & 0 & 4 & \mathbf {8} \end{array} \right].
$$

Solve $R x = \mathbf { 0 }$ to find ${ \mathbf { \mathcal { x } } } _ { n }$ (its free variable is $x _ { 2 } = 1 )$ . Solve $R x = d$ to find $\scriptstyle { \boldsymbol { x } } _ { p }$ (its free variable is $x _ { 2 } = 0 )$

Apply Gauss-Jordan elimination to reduce to $R x = 0$ and $R x = d \cdot$

$$
\left[ \begin{array}{c c} U & 0 \end{array} \right] = \left[ \begin{array}{c c c c} 3 & 0 & 6 & \mathbf {0} \\ 0 & 0 & 2 & \mathbf {0} \\ 0 & 0 & 0 & \mathbf {0} \end{array} \right] \quad \text {and} \quad \left[ \begin{array}{c c} U & \boldsymbol {c c} \end{array} \right] = \left[ \begin{array}{c c c c} 3 & 0 & 6 & \mathbf {9} \\ 0 & 0 & 2 & \mathbf {4} \\ 0 & 0 & 0 & \mathbf {5} \end{array} \right].
$$

Solve $U { \pmb x } = { \bf 0 }$ or $R x = 0$ to find ${ \boldsymbol { \mathbf { \mathit { x } } } } _ { n }$ (free variable = 1). What are the solutions to $R x = d ?$

Reduce to $U { \pmb x } = { \pmb c }$ (Gaussian elimination) and then = (Gauss-Jordan):

$$
A \boldsymbol {x} = \left[ \begin{array}{c c c c} 1 & 0 & 2 & 3 \\ 1 & 3 & 2 & 0 \\ 2 & 0 & 4 & 9 \end{array} \right] \left[ \begin{array}{c} x _ {1} \\ x _ {2} \\ x _ {3} \\ x _ {4} \end{array} \right] = \left[ \begin{array}{c} 2 \\ 5 \\ 1 0 \end{array} \right] = \boldsymbol {b}.
$$

Find a particular solution $x _ { p }$ and all homogeneous solutions ${ \boldsymbol { \mathbf { \mathit { x } } } } _ { n }$

Find matrices and with the given property or explain why you can't:

(a) The only solution of $A { \pmb x } = { \left[ \begin{array} { l } { \mathbf { 1 } } \\ { \mathbf { 2 } } \\ { \mathbf { 3 } } \end{array} \right] } { \mathrm { ~ i s ~ } } { \pmb x } = { \left[ \begin{array} { l } { \mathbf { 0 } } \\ { \mathbf { 1 } } \end{array} \right] } .$

(b) The only soJ,tion of $B { \pmb x } = { \left[ \begin{array} { l } { \mathbf { 0 } } \\ { \mathbf { 1 } } \end{array} \right] } { \mathrm { ~ i s ~ } } x = { \left[ \begin{array} { l } { \mathbf { 1 } } \\ { \mathbf { 2 } } \\ { \mathbf { 3 } } \end{array} \right] } .$

Find the factorization of and the complete solution to $A x = b ;$

$$
A = \left[ \begin{array}{c c c} 1 & 3 & 1 \\ 1 & 2 & 3 \\ 2 & 4 & 6 \\ 1 & 1 & 5 \end{array} \right] \quad \text {and} \quad \boldsymbol {b} = \left[ \begin{array}{c} 1 \\ 3 \\ 6 \\ 5 \end{array} \right] \quad \text {and then} \quad \boldsymbol {b} = \left[ \begin{array}{c} 1 \\ 0 \\ 0 \\ 0 \end{array} \right].
$$

The complete solution to $A x = { \left[ \begin{array} { l } { 1 } \\ { 3 } \end{array} \right] } { \mathrm { ~ i s ~ } } x = { \left[ \begin{array} { l } { 1 } \\ { 0 } \end{array} \right] } + c { \left[ \begin{array} { l } { 0 } \\ { 1 } \end{array} \right] }$ . Find

## Challenge Problems

(Recommended!) Suppose you know that the 3 by 4 matrix A has the vector $s =$ $( 2 , 3 , 1 , 0 )$ as the only special solution to $A x = \mathbf { 0 }$

(a) What is the rank of A and the complete solution to $A x = \mathbf { 0 } ?$

(b) What is the exact row reduced echelon form R of A?

(c) How do you know that $A x = b$ can be solved for all b?

Suppose K is the 9 by 9 second diference matrix (2's on the diagonal, -1 's on the diagonal above and also below). Solve the equation $K \pmb { x } = b = ( 1 0 , . . . , 1 0 )$ If you graph $x _ { 1 } , \ldots , x _ { 9 }$ above the points $1 , \ldots , 9$ on the x axis, I think the nine points fall on a parabola.

Suppose $A x = b$ and $C x = b$ have the same (complete) solutions for every b. Is it true that A equals C ?

Describe the column space of a reduced row echelon matrix R.

