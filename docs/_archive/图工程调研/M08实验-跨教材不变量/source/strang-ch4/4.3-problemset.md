## Problem Set 4.3

Problems 1-11 use four data points $b = ( 0 , 8 , 8 , 2 0 )$ to bring out the key ideas.

![](images/69c527c8212d6f888325a07caf1d861551a1b2dfbf38c4086f8046e8221cd23c.jpg)  
Figure 4.9: Problems 1-11: The closest line $C + D t$ matches $C a _ { 1 } + D a _ { 2 }$ in $ { \mathbf { R } } ^ { 4 }$

1 With $b ~ = ~ 0 , 8 , 8 , 2 0$ at $t \ = \ 0 , 1 , 3 , 4 ,$ , set up and solve the normal equations $A ^ { \mathrm { T } } A { \widehat { \mathbf { x } } } = A ^ { \mathrm { T } } \mathbf { b }$ . For the best straight line in Figure $4 . 9 \mathrm { a }$ , find its four heights $p _ { i }$ and four errors $e _ { i }$ . What is the minimum value $E \overline { { = e _ { 1 } ^ { 2 } + e _ { 2 } ^ { 2 } + e _ { 3 } ^ { 2 } + e _ { 4 } ^ { 2 } \ b { \tau } } }$

2 (Line $C + D t$ does go through $p ^ { \prime } s )$ With $b = \ 0 , 8 , 8 , 2 0$ at times $t = 0 , 1 , 3 , 4 ,$ write down the four equations Ax = b (unsolvable). Change the measurements to $p = 1 , 5 , 1 3 , 1 7$ and find an exact solution to $A { \widehat { \mathbf { x } } } = p$

3 Check that $\pmb { e } = \pmb { b } - \pmb { p } = ( - 1 , 3 , - 5 , 3 )$ is perpendicular to both columns of th same matrix A. What is the shortest distance $\lVert e \rVert$ from b to the column space of $A ?$

4 (By calculus) Write down $E = \| A x - b \| ^ { 2 }$ as a sum of four squares-the last one is $( C + 4 D - 2 0 ) ^ { 2 }$ Find the derivative equations $\partial E / \partial C = 0$ and $\partial E / \partial D = 0$ Divide by 2 to obtain the normal equations $A ^ { \mathrm { T } } A { \widehat { \mathbf { x } } } = A ^ { \mathrm { T } } b$

5 Find the height C of the best horizantal line to fit $\pmb { b } = ( 0 , 8 , 8 , 2 0 )$ ). An exact fit would solve the unsolvable equations $C = 0 , C = 8 , C = 8 , C = 2 0$ . Find the 4 by 1 matrix A in these equations and solve $A ^ { \mathrm { T } } A { \widehat { x } } = A ^ { \mathrm { T } } b$ . Draw the horizonta line at height $\widehat { x } = C$ and the four errors in e.

Project $b = ( 0 , 8 , 8 , 2 0 )$ ) onto the line through ${ \pmb a } = ( 1 , 1 , 1 , 1 )$ . Find ${ \widehat { x } } = a ^ { \mathrm { T } } b / a ^ { \mathrm { T } } a$ and the projection $\pmb { p } = \widehat { x } \pmb { a }$ . Check that $e = \boldsymbol { b } - \boldsymbol { p }$ is perpendicular to $^ { a , }$ and find the shortest distance $\lVert e \rVert$ from b to the line through a.

7 Find the closest line $b = D t ,$ through the origin, to the same four points. An exact fit would solve $D \cdot 0 = 0 , D \cdot 1 = 8 , D \cdot 3 = 8 , D \cdot 4 = 2 0$ . Find the 4 by 1 matrix and solve $A ^ { \mathrm { T } } A { \widehat { x } } = A ^ { \mathrm { T } } b$ . Redraw Figure 4.9a showing the best line $b = D t$ and the $e \mathbf { \hat { s } }$

Project $ { b } = ( 0 , 8 , 8 , 2 0 )$ ) onto the line through $\pmb { a } = ( 0 , 1 , 3 , 4 )$ . Find $\widehat { x } = D$ and $\pmb { p } = \widehat { x } \pmb { a }$ . The best C in Problems 5-6 and the best Din Problems 7-8 do not agree with the best $( C , D )$ in Problems 1-4. That is because $( 1 , 1 , 1 , 1 )$ ) and $( 0 , 1 , 3 , 4 )$ are \_ perpendicular.

For the closest parabola $b = C + D t + E t ^ { 2 }$ to the same four points, write down the unsolvable equations $A x = b$ in three unknowns $\pmb { x } = ( C , D , E )$ . Set up the three normal equations $A ^ { \mathrm { T } } A { \widehat { \mathbf { x } } } = A ^ { \mathrm { T } } b$ (solution not required). In Figure 4.9a you are now fitting a parabola to 4 points-what is happening in Figure 4.9b?

For the closest cubic $b = C + D t + E t ^ { 2 } + F t ^ { 3 }$ to the same four points, write down the four equations $A x = b $ . Solve them by elimination. In Figure 4.9a this cubic now goes exactly through the points. What are p and $e ?$

11 The average of the four times is $\widehat { t } = \textstyle { \frac { 1 } { 4 } } ( 0 + 1 + 3 + 4 ) = 2$ . The average of the four b's is $\widehat { b } = \textstyle { \frac { 1 } { 4 } } ( 0 + 8 + 8 + 2 0 ) = 9$

(a) Verify that the best line goes through the center poin $( \widehat { t } , \widehat { b } ) = ( 2 , 9 )$

(b) Explain why $C + D \widehat { t } = \widehat { b }$ comes from the first equation in $A ^ { \mathrm { T } } A { \widehat { \mathbf { x } } } = A ^ { \mathrm { T } } b$

Questions 12-16 introduce basic ideas of statistics-the foundation for least squares.

12 (Recommended) This problem projects $\pmb { b } = ( b _ { 1 } , \dots , b _ { m } )$ onto the line through ${ \pmb a } =$ $( 1 , \ldots , 1 )$ . We solve m equations $\mathbf { \delta } \mathbf { a } \mathbf { x } = \mathbf { b }$ in 1 unknown (by least squares).

(a) Solve $\pmb { a } ^ { \mathrm { T } } \pmb { a } \widehat { x } = \pmb { a } ^ { \mathrm { T } } b$ to show that $\widehat { x }$ is the mean (the average) of the $\boldsymbol { b ^ { \prime } s }$

(b) Find $e = b - a \widehat { x }$ and the variance $\| e \| ^ { 2 }$ and the standard deviation

(c) The horizontal line $\widehat { \pmb { b } } = 3$ is closest to $\pmb { b } = ( 1 , 2 , 6 )$ . Check that $\pmb { p } = ( 3 , 3 , 3 )$ is perpendicular to e and find the 3 by 3 projection matrix P.

First assumption behind least squares: $A x = b -$ (noise e with mean zero). Multiply the error vectors $e \ : = \ : b \ : - \ : A x$ by $( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ to get ${ \widehat { \mathbf { x } } } - { \boldsymbol { x } }$ on the right. The estimation errors ${ \hat { \mathbf { x } } } - { \mathbf { \nabla } } { \hat { \mathbf { x } } }$ also average to zero. The estimate $\widehat { \mathbf { x } }$ is unbiased.

14 Second assumption behind least squares: Them errors $e _ { i }$ are independent with vari ance $\sigma ^ { 2 }$ , so the average of $( \pmb { b } - \overset { \cdot } { A } \pmb { x } ) ( \pmb { b } - \pmb { A } \pmb { x } ) ^ { \operatorname { T } }$ is $\sigma ^ { 2 } I$ . Multiply on the left by $( A ^ { \mathrm { T } } A ) ^ { - 1 } A ^ { \mathrm { T } }$ and on the right by ${ \overset { \cdot } { A } } ( A ^ { \mathrm { T } } A ) ^ { - 1 }$ to show that the average matrix $( { \widehat { \pmb x } } - { \pmb x } ) ( { \widehat { \pmb x } } - { \pmb x } ) ^ { \mathrm { T } }$ is $\sigma ^ { 2 } ( A ^ { \mathrm { T } } \bar { A } ) ^ { - 1 }$ . This is the covariance matrix $W$ in Section 10.2.

15 A doctor takes 4 readings of your heart rate. The best solution to $x = b _ { 1 } , \ldots , x = b _ { 4 }$ is the average $\widehat { x }$ of $b _ { 1 } , \ldots , b _ { 4 }$ . The matrix A is a column of $1 \mathit { \ ' } _ { \mathsf { S } }$ . Problem 14 gives the expected error $( { \widehat { x } } - x ) ^ { 2 }$ as $\sigma ^ { 2 } ( A ^ { \mathrm { T } } A ) ^ { - 1 } = -$ . By averaging, the vmiance drops from $\sigma ^ { 2 }$ to $\sigma ^ { 2 } / 4$

16 If you know the average $\widehat { x } _ { 9 }$ of 9 numbers $b _ { 1 } , \ldots , b _ { 9 }$ , how can you quickly find the average $\widehat { x } _ { 1 0 }$ with one more number $b _ { 1 0 } ?$ The idea of recursive least squares is to avoid adding 10 numbers. What number multiplies $\widehat { x } _ { 9 }$ in computing $\widehat { x } _ { 1 0 } : \mathbb { Z }$

$\begin{array} { r } { \widehat { x } _ { 1 0 } = \frac { 1 } { 1 0 } b _ { 1 0 } + \widecheck { \widecheck { x } } _ { 9 } = \frac { 1 } { 1 0 } ( b _ { 1 } + \cdot \cdot \cdot + b _ { 1 0 } ) } \end{array}$ as in Worked Example 4.2 C

Questions 17-24 give more practice with x and p and e.

17 Write down three equations for the line $b = C + D t$ to go through $b = 7 \mathrm { a t } t = - 1$ L $b = 7$ at $t = 1$ , and $b = 2 1$ at $t = 2$ . Find the least squares solution ${ \widehat { \pmb x } } = ( C , D )$ and draw the closest line.

18 Find the projection $p = A { \widehat { x } }$ in Problem 17. This gives the three heights of the closest line. Show that the error vector is $\boldsymbol { e } = \left( 2 , - 6 , 4 \right)$ . Why is $P e = \mathbf { 0 } ?$

19 Suppose the measurements at $t = - 1 , 1 , 2$ are the errors $2 , - 6 , 4$ in Problem 18. Compute $\widehat { \mathbf { x } }$ and the closest line to these new measurements. Explain the answer: $b = ( 2 , - 6 , 4 )$ is perpendicular to \_ \_ so the projection is $\mathbf { \nabla } p = \mathbf { 0 }$

20 Suppose the measurements at $t = - 1 , 1 , 2$ are $b = ( 5 , 1 3 , 1 7 )$ . Compute $\widehat { \mathbfit { x } }$ and the closest line and e. The error is $e = { \bf 0 }$ because this b is

21 Which of the four subspaces contains the error vector $e ?$ Which contains $p \mathrm { ? }$ Which contains $\hat { \mathbf { x } } ?$ What is the nullspace of A?

22 Find the best line $C + D t$ to fit $b = 4 , 2 , - 1 , 0 , 0$ at times $t = - 2 , - 1 , 0 , 1 , 2$

23 Is the error vector e orthogonal to b or pore or $\hat { \mathbf { x } } ?$ Show that $\| e \| ^ { 2 }$ equals $e ^ { \mathrm { T } } l$ b which equals $b ^ { \mathrm { T } } b - p ^ { \mathrm { T } } b$ . This is the smallest total error $E$

The partial derivatives of $\| A x \| ^ { 2 }$ with respect to $x _ { 1 } , \ldots , x _ { n }$ fill the vector $2 A ^ { \mathrm { T } } A x$ The derivatives of $2  { b ^ { \mathrm { T } } }$ Ax fill the vector $2 \bar { A } ^ { \mathrm { T } } b$ . So the derivatives of $\| A x - b \| ^ { 2 }$ are zero when

## Challenge Problems

What condition on $( t _ { 1 } , b _ { 1 } ) , ( t _ { 2 } , b _ { 2 } ) , ( t _ { 3 } , b _ { 3 } )$ puts those three points onto a straigh line? A column space answer is: $( b _ { 1 } , b _ { 2 } , b _ { 3 } )$ must be a combination of $( 1 , 1 , 1 )$ and $( t _ { 1 } , t _ { 2 } , t _ { 3 } )$ . Try to reach a specific equation connecting the $t \mathrm { \bar { s } }$ and $b ^ { \prime } \mathbf { s } .$ . I should have thought of this question sooner!

Find the plane that gives the best fit to the 4 values $b = ( 0 , 1 , 3 , 4 )$ at the corners $( 1 , 0 )$ and $( 0 , 1 )$ and $( - 1 , 0 )$ and $( 0 , - 1 )$ of a square. The equations $C + D x + E y =$ b at those 4 points are Ax = b with 3 unknowns $\pmb { x } = ( C , D , E )$ . What is A? At the center (0, 0) of the square, show that $C + D x + E y = \mathrm { a v e r a g e }$ of the $b ^ { \prime } s$

(Distance between lines) The points $P = ( x , x , x )$ and $Q = ( y , 3 y , - 1 )$ are on two lines in space that don't meet. Choose x and y to minimize the squared distance $\| \boldsymbol { P } - \boldsymbol { Q } \| ^ { \bar { 2 } }$ . The line connecting the closest P and $Q$ is perpendicular to

Suppose the columns of A are not independent. How could you find a matrix $B$ so that ${ \bf \dot { \boldsymbol { P } } } = B ( B ^ { \mathrm { T } } { \boldsymbol { B } } ) ^ { - 1 } B ^ { \mathrm { T } } $ does give the projection onto the column space of A? (The usual formula will fail when $A ^ { \hat { \mathrm { T } } } A$ is not invertible.)

Usually there will be exactly one hyperplane in $\mathbb { R } ^ { n }$ that contains the n given points $\pmb { x } = \mathbf { 0 } , a _ { 1 } , \dots , a _ { n - 1 }$ · (Example for $n = 3 { : }$ There will be one plane containing $0 , a _ { 1 } , a _ { 2 }$ unless \_\_ .) What is the test to have exactly one plane in $\mathbb { R } ^ { n } ?$

Example 2 shifted the times $t _ { i }$ to make them add to zero. We subtracted away the average time ${ \widehat t } = ( t _ { 1 } + \cdot \cdot \cdot + t _ { m } ) / m$ to get $T _ { i } = t _ { i } - { \widehat { t } } .$ Those $T _ { i }$ add to zero.

With the columns $( 1 , \ldots , 1 )$ and $\left( T _ { 1 } , \ldots , T _ { m } \right)$ now orthogonal, $A ^ { \mathrm { T } } A$ is diagonal. Its entries are m and $T _ { 1 } ^ { 2 } + \cdots + T _ { m } ^ { 2 }$ . Show that the best C and D have direct formulas:

$$
\boldsymbol {T} \text {is} \boldsymbol {t} - \widehat {\boldsymbol {t}} \quad C = \frac {b _ {1} + \cdots + b _ {m}}{m} \quad \text {and} \quad D = \frac {b _ {1} T _ {1} + \cdots + b _ {m} T _ {m}}{T _ {1} ^ {2} + \cdots + T _ {m} ^ {2}}.
$$

The best line is $C + D T o r C + D ( t - \widehat { t } )$ . The time shift that makes $A ^ { \mathrm { T } } A$ diagonal is an example of the Gram-Schmidt process: orthogonalize the columns of A in advance.

