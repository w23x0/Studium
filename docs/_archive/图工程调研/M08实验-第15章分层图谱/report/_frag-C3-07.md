# 片 C3-07 装配稿（S1 / S2 / S3）

已回查三份原文。**原文本身截断**：S1 只有待议项一，S2 有项 1–3（项 4–7 及附录 B 仅剩 `<!-- ITEM-4 -->` 等占位注释），S3 只有 §一 §二。速查表有裁决而正文缺失者列入末节，不据摘要落笔。`SPEC.md` 的 `:29`/`:54`/`:57`/`:65`/`:98`/`:100` 现文已核，与三份所引一致。

## 主体

**1. S1 ｜ 替换 `SPEC.md:54` 整行为五条 ｜ 改后采纳**
现文核实为 `- 长度 30–200 字符。不跨行拼接、不改标点、不加省略号、不修正 OCR 噪声。**复制粘贴,不要凭记忆重写。**`；逐字替换文本用 S1「可直接并入 SPEC 的逐字文本」代码块。
改动：**第 5 条（退化中途起始）本轮不并入**（见落选项 1）。前四条采纳——S1 实测现有 921 条锚点需改写 **0**、新增 schema 字段 **0**、`verify_anchors.py:56` 需改动 **0**，短公式 35/35 可解中首选形态 29 条。
配套两项：(a) 短公式口径一律用 **35**（`display_block` + `math_only_line`，文件内唯一 35、全局唯一 33；与 SPECQ §2.3.3 一致、与 §4.3 的 36 差 1）；(b) 同批改 `export_obsidian.py:145`（现为 `lines.append(f"  > {quote}")`，修法 `quote.replace("\n", "\n  > ")`）——S1 判它是唯一确定会被跨行 quote 破坏的下游消费者。

**2. S2 项 1 ｜ 替换 `SPEC.md:100` 整行（补死因 `0` 与 `6` + 1/3/6 三环判据表）｜ 采纳**
现文只列 1–5，与 S2 所述一致；逐字文本用 S2 项 1 代码块。理由：两份被点名报告的 125 条裁决里 cause 0 占 **117 条（93.6%）**，主分支未定义；须改数据 0 节点 0 边。

**3. S2 项 2 ｜ `SPEC.md:57` 之后新增「证据池：四层，前三层入池」｜ 采纳**
逐字文本用 S2 项 2 代码块（T1/T2/T3 入池、TD 排除、T2 塌陷判 `0` 数据不动、禁批量搬迁、T3 只沿 `part-of`）。实测「0 锚点且 T2>0」= **0**、「0 锚点且 T3>0」= **0**，故对合规下限影响为零、数据改动 0；全量搬迁会让 **86** 个节点破 3 条上限、共享 `(file, quote)` 组由 **158 → 253**（+60%）。

**4. S2 项 3 ｜ `SPEC.md:65` 之后新增「节点的 `origin`」，取值 `source|model|mixed` ｜ 采纳（落笔须修围栏）**
逐字文本用 S2 项 3 代码块。**该块内嵌一个 ```json 子围栏，照抄会断栏，落笔时外层改四反引号。**
影响面：须改节点 **49**（A2-x2 34 + CX2 15），其中「有 `origin_note` 无 `origin`」**16** 个（CX2 15 + `nodes-D2.jsonl:9`）；须改边 **0**。

**5. S3 §一 ｜ `SPEC.md:29` 之后新增三段（不扩词表 / 第二使用点不建边 / `rel_note` 不限 `other`）｜ 采纳**
`:29` 现文核实为 `` `is-a` 与 `generalizes` 互为反向,**只写一条**。对称关系只写一个方向。 ``，即 S3 指定的插入锚；逐字文本用 S3 §一代码块。理由：`other` 40 条全部带 `rel_note`（0 缺失），仅 4 条属复用模式；`sections` 多值已记同一事实 **57** 个节点，扩词等于开第二套账。

**6. S3 §一 数据 ｜ 翻转 `data/edges-D1.jsonl:168` 与 `:212` 为 `requires` 并删 `rel_note` ｜ 采纳**
`:168` → `src=d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one`、`rel=requires`、`dst=d:example-7-polynomials-of-degree-at-most-n`；`:212` → `src=d:dim-of-a-second-order-de-solution-space-is-two`、`rel=requires`、`dst=d:example-12-solutions-of-a-homogeneous-second-order-equation`。
排序约束（照抄 S3 风险）：**必须在跑枢纽豁免（§二）与删除测试作用域（§四）之前完成翻转**，否则同一节点因边向不同得两种裁决。两条 `origin` 为 `model`、无 evidence，翻转不引锚点债务。

**7. S3 §二 ｜ 替换 `SPEC.md:98`：裁决三档 `KEEP|FIX|KILL` + 孤儿检查 ｜ 改后采纳（见第 8 条）**
逐字文本用 S3 §二代码块。实测：`audit-L2-A` 的 10 个 FIX **已落盘**，24 个 L2 节点中恰好 10 个带 `audit_fix: "audit-L2-A"`，与该报告名单（a1–a8、b1、b4）逐字一致；孤儿检查暴露面 522 里 **115** 个，L1 不击杀后实际风险面 **33**（`d:` 32 + `x:` 1）；唯一有 L1 子节点的 `d:` 节点是 `d:ten-axioms-listed-in-three-groups`。

**8. 冲突登记（S2 项 1 后半 × S3 §二）｜ 两份都要替换 `SPEC.md:98`，须主控裁一次**
S2 保留两档、写「`KEEP` 配 `1`–`6` 表示保留但该缺陷必修」；S3 改三档，把它独立为 `FIX`。语义重叠，不能同时落笔。
建议（本片判断，非原文）：取 S3 三档表作骨架，删 S2 那句，保留 S2 的 cause `0` 供 `KEEP` 用——S3 一侧有已落盘数据（10 个 `audit_fix` + 13 个既有 FIX 全过门槛，无回改债务），S2 一侧无数据依赖。另须对齐 S3 表里「死因填 1–6」与 S2 的 `0`（`KEEP` 恒为 `0`）。

## 落选项

- **S1 逐字文本第 5 条（退化中途起始，6 条）｜ 移出本轮**：S1 风险 4 自述这 6 条（`15.13.md:46` 与 `15.09-exercises.md` 5 条习题项，相邻实质行 224/227/308 字符）不在任何已确证受损节点里。
- **任务书「60 个低于地板的行里 51 个全局唯一」｜ 不采纳**：S1 换任何口径都复现不出，明写不采用。
- **`data/edges-D1.jsonl:148` 改 `alias-of` ｜ 移出本轮**：S3 指出 `d:function-space-zero-is-the-everywhere-zero-function` 已在 `待应用清单` §一判 KILL（零残留），该边随节点进墓地；原文「两处待办在此合流,请勿重复处理」。
- **扩词 `violates`（`other` 最大一族 10 条）｜ 移出本轮**：S3 自判「列为下一轮议题,本轮不动」。
- **节点 `origin=source` 改必填 ｜ 不采纳**：S2 实测须改节点数由 49 涨到 **489**（522 − 33）。
- **复核已有 33 个 `origin` 节点 ｜ 移出本轮**：S2 明说「我**没有**逐个复核这 33 个」，无实测支撑。

### 本片未覆盖（原文缺失，非漏做）

S1 待议项二～五（含其影响面表引用的「待议项五」3 条 <30 字符边 evidence）；S2 项 4 `sections`、项 5 `parent`、项 6 `apostol:cx-`/`ext-` 命名空间、项 7 `omission_site`；S3 §三 恢复基、§四 删除测试作用域、§五 operations 禁令、§六 共享前提、§A 批量互证、§B 裁决回显 id、§C 停止规则对 L2-B。以上**逐字文本不存在**，一律「未复核」，不装配。
