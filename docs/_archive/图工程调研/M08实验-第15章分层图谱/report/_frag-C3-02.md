# 装配片 C3-02（P04 / P05 / P06）

三份提案的改动全部落在 `SPEC.md` 的 `## 锚点规则(唯一硬校验)`（47–57 行）。已逐字核对
SPEC.md:53–57 与三份提案引的现文一致。**三者都往 `:57` 之后插内容，且 P05 要搬走 :56–57，
必须按 §主体 第 8 条的次序落，否则行号互相错位。**

## 主体

**1. 替换 `SPEC.md:53`（来源：G5-P04 §三 改动 1）**
主张：把「至少 1 条,最多 3 条」改成按 `sections` 计的**锚点预算** `3 + (sections 条数 - 1)`，
并把 `grep -F` 那半句拆成独立一行。**采纳（须与第 4 条合并落笔）。**
理由：P04 §1.5 实测 22 个节点的锚点未覆盖自己声明的全部 `sections`，其中 6 个已 3 条满额无槽可补
（`apostol:projection-along-an-element`、`apostol:spanning-set`、
`apostol:cx-convergence-must-be-proved-before-the-axioms`、
`apostol:cx-l2-sequence-space-is-an-infinite-dimensional-euclidean-space`、
`d:normalized-element-has-norm-one`、`x:right-count-upgrades-one-basis-property-to-both`），
预算式恰好覆盖这 6 个，回溯违规 0（现无节点超 3 条）。
落笔文本用 P04 §三 改动 1 的「新」5 行（不用 §二 P04-a 的缩进公式版，行内式更贴 SPEC 现有风格）。

**2. 在 `SPEC.md:54` 之后插入 3 行（来源：G5-P04 §三 改动 2 = 条文 P04-b）**
主张：同节点任两条锚点的源行集合相等即违规；同节点锚点总字符数 ≤ 600。**采纳。**
理由：这是本项唯一咬得住「抄整节」的闸门。**但它不是零成本**——P04 §四 汇总表实数：
同源行去重 **54 个节点从合规变违规**（2 锚 41 个、3 锚 13 个），删冗余那条后无一节点跌破「至少 1 条」；
总字符 600 界现状零违规（518 个有锚节点总字符 max 463）。装配时必须把这 54 个列入 FIX 队列。
`:54` 本身不动（S1 的地盘）。

**3. 在锚点规则小节末尾插入条文 P04-c（来源：G5-P04 §三 改动 3）**
主张：明写三条界都是**节点级**的，不约束同一段原文被多节点分别引用；报「锚点零违规」时必须同时报
行覆盖 74% 与个别文件锚点字符量达 209% 两个数。**采纳。**
理由：P04 §1.6 实测否证了「3 条上限抑制抄整节」——15.02.md 的 209% 是在零违规前提下达到的。
不写这段，零违规会被读成防抄有效。

**4. 改写 `SPEC.md:55`（来源：G5-P05 §三 表格第 55 行）**
主张：删掉「或标 `origin: model` 并如实说明」这半句，改为「⇒ **不要收录**。改写 statement 使它落回
可锚的范围,或把不可锚的那一句下移到一条边的 `rel_note` / 上移到抽象层,都优先于收录一个无锚节点。」
**采纳，但须与 S2 项 3 同批。**
理由：:53 与 :55 互为死条文（:53 无条件成立时 :55 第二分支永不可执行）。P05 §1.3 实测 24 个
`origin: model` 节点里 **20 个有锚点（共 37 条）**，证明该字段与「有没有锚点」正交；照直译 :55
会一次性授权 24 个节点无锚。P05 §三.1 明示：只应用本条会让 33 个在用 `origin` 的节点暂时无 SPEC 依据。

**5. 新增小节「无锚豁免(唯一的锚点数量例外)」（来源：G5-P05 §二）**
主张：豁免门槛是**原理上不可锚**（须给否定性检索式与零命中）；申请豁免须同时满足四条，缺一判死因 `6`：
`"anchors": []` 保留空数组 / `origin:"model"` / `origin_note` 非空且写明哪一句无锚 /
新字段 `unanchorable` 取自封闭词表 {`external-proof`, `negative-claim`, `derived-criterion`}；
`external-proof` 类的 `sections` 须指向委托语所在节，冒充判死因 `3`；L2+ 不走本豁免。**采纳。**
理由：P05 §4.2 逐类实测——甲类 24 个 L2 走 :56，影响 0（其键集连 `anchors` 都不存在）；
乙类 `data/nodes-D2.jsonl:42`、`:54` **不满足门槛必须补锚**（P05 已逐字核出可用锚点，
如 `:42` 的 68 字符句 `The following examples illustrate these concepts in function spaces.`，
在 `15.07.md:27`，文件内出现 1 次，零持有者）；丙类 `:72`、`:73` 符合 `external-proof`，
只需加字段（`:73` 的 `origin_note` 需小改）。净：2 个变违规、2 个变合规、堵住 20 节点/37 锚点的漏洞。
配套：`:56–57` 两行**整行原样搬到本小节末尾**，文字一字不改。
`unanchorable` 是新字段，现有 546 个节点 0 个有它（P05 §三.3 已遍历核实）。

**6. 新增小节「机器校验的覆盖范围(`tools/verify_anchors.py`)」（来源：G5-P06 §二）+ 两处改后落笔**
主张：六类判 FAIL 的表（`QUOTE_NOT_FOUND` / `FILE_NOT_FOUND` / `LEN_TOO_SHORT` / `LEN_TOO_LONG` /
`TOO_MANY_ANCHORS` / `NO_ANCHOR_NO_EXEMPTION`）、`len()` 按 JSON 解码后 Python 字符数计、
两条无锚豁免、机器不判只能人工判的清单、stdout 四行契约。**改后采纳，两处必改：**

- (a) **豁免 2 必须收紧。** P06 原文写「模型来源豁免 —— `origin` ∈ {`model`,`mixed`} 且非空
  `origin_note`」，这正是 P05 §二 判定**不构成**豁免凭据的东西。按 P05 加上第三个必要条件
  `unanchorable` 取值在封闭词表内。不改就会放行 P05 判「必须补锚」的 `:42`、`:54`。
  **连带后果（P06 未预见）**：P06 §四 断言 `NO_ANCHOR_NO_EXEMPTION` 新增失败 **0**，那是按宽版算的；
  按严版，因现有 0 个节点带 `unanchorable`，**脚本先落地会立刻造出 4 个失败**。
  故脚本补丁必须与第 5 条的数据 FIX 同批提交。
- (b) **`TOO_MANY_ANCHORS` 的判据必须跟第 1 条。** P06 原文写 `len(anchors) > 3` 并在 §六 声称
  「上限若提高,本条不动」——对 P04-a 这种**逐节点变动的**上限不成立，须改成
  `len(anchors) > 3 + (len(sections) - 1)`（与 P04-d 第 (1) 项一致，L2+ 除外）。
- (c) 补两行覆盖第 2 条的新界：同节点源行集合相等（P04-d 第 (3) 项给了实现法：按 `\n` 拆 `quote`，
  每个非空片段在 `file` 里逐行 `in` 测试取命中行号并集）、同节点锚点总字符 > 600。

理由：P06 §5.1/§5.2 的两条反例是决定性的（否掉「无锚一律判失败」与「豁免靠文件名或前缀」）；
长度界与「不跨行」至今零机器覆盖，补上今天 0 新增失败，纯防回归。

**7. `SPEC.md:47` 标题加指针（来源：G5-P06 §三 第二行，标「可选但建议」）**
主张：`## 锚点规则(唯一硬校验)` → `## 锚点规则(唯一硬校验;机器覆盖范围见本节末)`。**采纳。**
理由：P06 自己说此项可略、不影响 §二 效力；但它成本为零，且「唯一硬校验」被反复误读成
「凡 `verify_anchors.py` 通过即合规」，加半句指针比在别处重复警告便宜。

**8. `:57` 之后三处插入的落笔次序（本片裁定，非任一提案原文）**
三份提案都说「插在 `:57` 之后、`## 节点 schema` 之前」，且 P05 还要把 :56–57 搬进自己的小节。
按此序落，每步以**内容**定位而非以原行号定位：先第 5 条（建「无锚豁免」小节并把 :56–57 搬到其末尾），
再第 3 条（P04-c 另起一段），最后第 6 条（「机器校验的覆盖范围」小节，须在 `## 节点 schema` 之前）。
P04-c 不得插在 53–54 之间（P04 §三 改动 3 明示：它是元说明，放在硬规则中间会被误读成又一条硬规则）。

## 落选项

- G5-P04 §二：**取消 3 条上限** —— 不采纳。取消后节点级只剩 30–200 单条长度界，一个节点可合法用
  170 条锚点把 15.10.md（7547 字符）整节搬进 `anchors`。
- G5-P04 §二：**一律把上限提到 5** —— 不采纳。给 465 个单 section 节点（占非 L2 的 89%）白发 2 槽，
  而这 465 个无一被 3 条卡住；且 `apostol:ext-vn-as-a-prior-vector-space`（4 节、现 2 锚）在预算式下
  得 6 槽、在「一律 5」下只得 5 槽，按 `sections` 计覆盖更准。
- G5-P04 §三 改动 4：**把 P04-d 单独落成 `## 机器校验契约` 小节（落法甲）或接在 `:98` 之后（落法乙）**
  —— 移出本轮。P04 自己声明「不替 S1/S3 决定采甲还是乙」；两处落法都会与 P06 §二的覆盖表构成
  **两处记账**（P06 §三 明确反对复述数值）。裁定：P04-d 三项断言的内容全部并入第 6 条，不另开小节。
- G5-P06 §二：**原版「模型来源豁免」只要 `origin`+`origin_note`** —— 不采纳，按第 6 条 (a) 收紧。
  理由同上：与 P05 的裁定直接冲突，会放行 `:42`、`:54`。
- G5-P06 §六：**「3 锚点上限若提高,本条不动」** —— 不采纳该判断。对 P04-a 的逐节点预算式不成立，
  须按第 6 条 (b) 改判据。
- G5-P06 §四.4 的 `QUOTE_NOT_UNIQUE` 一行 —— 移出本轮。它以 S1 被采纳为前提，本片无 S1 切片。
  只记下 P06 的边界实数以备 S1 装配时用：唯一性须写成「在其 `file` 内唯一」，写成「全语料唯一」会凭空
  造出 1 个失败（`d:exponential-weight-makes-improper-integral-converge` 的 56 字符 quote 在
  `15.10.md` 与 `15.12-exercises.md` 各出现 1 次，在自己声明的 `15.10.md` 内唯一）。

**本片未覆盖**：`:54` 的 30–200 长度界（S1 / P02 的地盘，三份提案一致声明不碰）；
节点 `origin` 字段的 schema 位置（S2 项 3 承接，第 4 条依赖它同批落）；
P05 §5.1 指出的「沾边引文绕过豁免门槛」这一残余漏洞（`:72` 自身即现成绕法），未在本片给出堵法。
