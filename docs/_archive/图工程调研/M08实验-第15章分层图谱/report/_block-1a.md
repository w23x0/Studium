**裁决：A3 §0 的四个零证据节点定位准确，且「全图无锚点节点恰好这 4 个」已独立证成，该缺陷类闭合。已核。**来源 `A3-零证据节点锚点候选.md` §0 表。我在实验根只读遍历 `data/nodes-*.jsonl` + `data/inherited/nodes-*.jsonl` 共 **522** 个节点（不含 `structures-*.jsonl`），`anchors` 为空者得且仅得 4 个，与该表逐行吻合：`data/nodes-D2.jsonl:42` `d:exponential-independence-uses-no-inner-product`（concept）、`:54` `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets`（method）、`:72` `d:thm-15-7b-argument-adjoining-an-outside-element-breaks-the-count`（method）、`:73` `d:adjoining-an-element-outside-the-span-preserves-independence`（theorem），四者 `origin` 均为 `"model"`。表头写 `type` 而字段名实为 `node_type`，值与表一致，仅表头措辞须校。此项对**既存裁决 2 是独立支撑**：裁决 2 缺口 A 的承重实测「`origin=model` 的 24 个里 20 个有锚、4 个无锚」由此闭合——我复算 `origin` 取值为 `model` **24** / `source` **9**，而无锚的 4 个全部落在那 24 个之内。

**裁决：A3 §1 要求的前置裁断已由既存裁决 2 完成，故 A3 §3/§4 从「有条件的证据增强」转为必须执行。已核。**来源 `A3-零证据节点锚点候选.md` §1，与本清单 §一 裁决 2、§三 R7。A3 §1 把 §3/§4 的执行力系于「`SPEC.md:53` 与 `:55` 谁优先」这一未裁项，并自陈若 `:55` 第二分支有效则「本提案的 §3/§4 也就从『必须执行的补锚』降级为『可选的证据增强』」。SPEC 三行逐字复核与 A3 所引一致：`:53`「至少 1 条,最多 3 条。`quote` 必须在 `file` 中**逐字连续出现**,用 `grep -F` 机器校验。」；`:55`「找不到可逐字引用的依据 ⇒ **不要收录**,或标 `origin: model` 并如实说明。」；`:56` 只对 L2+ 明文豁免——矛盾属实。而 A3 §1 末段自荐的解法（「须另填一个可审计字段说明不可锚的**类型**」）与裁决 2 采纳的 P05 `unanchorable`（`external-proof`/`negative-claim`/`derived-criterion`）是同一构造，裁决 2 又明写「`origin: model` **不构成**无锚的凭据」，即否掉了 §1 的第二分支读法；§三 R7 回溯句逐字把 `nodes-D2.jsonl:42`、`:54` 判为须补锚、`:72`/`:73` 判为只需加字段，与 A3 的 §3/§4（管 :42/:54）× §5（管 :72/:73）分界完全对齐。**与裁决 2 不冲突，是其下游后果**，本条只取消 §3/§4 的「可选」状态。

**裁决：A3 §3.2 方案 A 的两条节点锚点与 §4.3 的一条节点锚点，机器合规全部实测通过，可落盘。已核。**来源 `A3-零证据节点锚点候选.md` §3.2 表、§4.3 表。我用 `q in open(...).read()` 独立复测（未跑 `tools/` 下任何脚本）：`:42` 的 a1 `The following examples illustrate these concepts in function spaces.` 实测 **68** 字符、`15.07.md` 第 **27** 行、全文 **1** 次、不含换行；a2 `In each case the underlying linear space V is the set of all real-valued functions defined on the real line.` 实测 **108** 字符、同在 L27、1 次、不跨行；`:54` 的 `Since S is independent and spans V, Theorem 15.5 tells us that every set of $k + 1$ elements in V is dependent.` 实测 **111** 字符、`15.08.md` 第 **7** 行、1 次、不跨行。三条全落 30–200，与提案自报的 68/108/111 逐字相符。§4.3 自报的「与 `nodes-D2.jsonl:56` `d:thm-15-6-apply-theorem-15-5-to-the-first-basis` 精确共享」经我全库持有者普查确认：该 `(file,quote)` 对现有**节点**持有者恰 `:56` 一个，补后成两方共享，即 §6.3 预告的形式 B 计数 +1 属实、不是新缺陷。**未复核**：§3 A-2 两条边 evidence 的 `\\sum` / `\\tag` 转义（提案自标为唯一落地风险点，须落盘后 `json.loads` 回读）、§3.2 A-3 的 statement 改写文本与源文射程的逐句比对。

**裁决：A3 §6 验收口径采纳；`nodes with NO anchor` 由 4 变 2 这一判据在裁决 2 落地后仍有效，但须与 P06 的脚本补丁同批确认。已核（924 未复核）。**来源 `A3-零证据节点锚点候选.md` §6 第 1、5 项。我实测全库带 `quote` 的锚点基数为 **921**，故 §6.5 的「921 → 924」（`:42` +2、`:54` +1）算术自洽，但 924 本身**未实测**。§6.1 要求「必须读 stdout 里 `nodes with NO anchor:` 那一行，不能只看退出码」，其理由（无锚节点进 `nodes_without_anchor` 后 `continue`、永不入 failures；唯一检查是 `quote and quote in text`）与 C1-01 对 `verify_anchors.py` 的源码复核一致，无条件采纳。**一处执行提示**：裁决 2 采 `unanchorable` 后，`:72`/`:73` 会带该字段但 `anchors` 仍空，按现逻辑仍计入那一行，故「应从 4 变 2」判据不受影响；但 §三「未覆盖」首条第二项要给该脚本打 P06 补丁（豁免 2 补第三必要条件），须确认补丁不改这一行的语义，否则此判据失效。§6.1 另给的反向哨兵（「如果它变成 `0`，说明有人顺手给 :72/:73 挂了锚——那违反本提案 §5，请回退」）一并采纳。

**裁决：A4 §7 A 组三条补锚的替换块逐字合规，净增 4、验收 918 成立。已核。**来源 `A4-形式B漂移与借用锚点.md` §7 A 组表与 §1.1（第 124 行 2 条块）、§1.2（第 198 行 2 条块）、§1.3（第 281 行 3 条块）。我对三块共 7 条 quote 逐条实测：`nodes-D1.jsonl:62` 得 **81**（`15.03.md` L19）/ **115**（L25）/ **69**（L33）；`:69` 得 115（L25）/ **120**（`15.02.md` L9）；`:70` 得 115（L25）/ **85**（`15.02.md` L17）——七条全部逐字命中、各出现 1 次、单行不跨行、全落 30–200。三节点现各 **1** 条锚点（实测），替换后 3 / 2 / 2，故 §7 D 的「3 → 7、净增 4」与 `921 + 4 − 7 = 918` 成立（基数 921 已实测）。**一处比两份碎片都更强的发现**：那条 **115** 字符共享引文的节点持有者实为 **7** 个（`nodes-D1.jsonl:62/:64/:67/:69/:70/:71` 与 `data/inherited/nodes-A1.jsonl:14`），不止 C1-01 所说的三者；且三个替换块**都保留了这条 115 引文**，故 §7A 不解散该形式 B 组、只在其旁补锚，组内持有者数不变、不产生孤引。与**既存裁决 乙 相容**：乙 已把 `nodes-D1.jsonl:62` 撤出待杀 21 条，§7A 才有落点，且补锚须排在 `apply_kills.py` 之前。

**裁决：A4 §7 B 组 7 条剥 `anchors[1]` 采纳；§3 第 8 条 `nodes-D4.jsonl:53` 判 PLAUSIBLE、不自动执行、必须按 index 而非口头描述剥离。已核。**来源 `A4-形式B漂移与借用锚点.md` §7 B 组表、C 组表与 §3。七个目标逐行复核全中：`data/nodes-D4.jsonl:15`（`[0]`=35 / `[1]`=42）、`:52`（120 / 69）、`data/nodes-A2-x2.jsonl:2`（49 / 117）、`:3`（80 / 117）、`:4`（60 / 117）、`:5`（127 / 117）、`:6`（54 / 117）——七者现均 **2** 条锚点，待剥均为 `anchors[1]`，剥后 1 条，不击穿 `:53` 的下限 1。A2-x2 那五条的 `[1]` 实测确为同一条 **117** 字符题干指令，即借用源。§3 的次序警告经实测证实：`:53`（`d:strict-minimality-rests-on-positivity-of-the-norm`）的 `[0]` = 42 字符范数正定性、`[1]` = 35 字符内积正定性，与 `:15` **正好相反**（`:15` 的 `[0]` 是 35 那条），两节点是同一对引文次序互换，故「剥第二条」这一口头描述套到 `:53` 会剥错对象。C 组若执行则验收 917（§7 D 自给）；§3 末句「921 → 913」已由既存清单 §四 判不可引用，本条不重复。

**裁决：A4 §4 四处字符更正采纳；C1-01 标为「未复核、无从定位」的 15.06.md 207 字符一项已定位，A4 结论成立。已核。**来源 `A4-形式B漂移与借用锚点.md` §4 表与其后「另外两处实质更正」第 2 项。四处独立 `len()` 复测全中：`15.02.md:9` 整行实测 **175**（审查稿记 170）；`The reader can easily verify that each of the following sets is a function space.` 实测 **81**（附录记 78）、`15.03.md` 内 1 次；另两条 **91** / **85** 与表一致。C1-01 第 17 条把「§4 说 `15.06.md:9` 整句 207 字符」标为未复核、称「该行整行实测 421，207 指行内某句，本片无从定位」——我已定位：该行整行确为 **421** 字符，按句末标点切分后其中一句实测恰 **207** 字符，超 200 上限，故 A4「实测 207 字符，超 200 上限，SPEC 不合规，本提案仍不采用」成立，**该项不必再标未复核**。这不推翻 C1-01，只补掉它自陈的缺口。

**裁决：A5 §3.3 形态 (a)（节点带 `origin`、位置紧跟 `anchors`、取值只用 `"model"`）采纳，§3.1 的「补文档而非新增能力」定性亦采纳。已核。**来源 `A5-CX2与origin.md` §3.1、§3.2、§3.3。事实基础全部机器复核属实：节点级 `origin` = **33**（D1 1 / D2 8 / D3 4 / D4 9 / H2 11），取值 `model` 24 / `source` 9；`origin_note` = **39**（CX2 15 / D2 9 / D3 4 / H2 11）。渲染已实现：`tools/export_obsidian.py:183-184` 逐字为 `if node.get("origin"):` / `fm.append(f"origin: {yaml_scalar(node['origin'])}")`，`:229-230` 逐字为 `if node.get("origin_note"):` / `body += ["## 来源说明", "", prose("origin_note"), ""]`。SPEC 侧落差已核：`:55` 确在 `## 锚点规则` 块内、紧跟 `:54` 长度规则，已在节点语境授权 `origin: model`；而 `:62` 的节点 schema 示例内**无** `origin` 键，`:70` 的边 schema 有 `"origin":"source|model"`——故落点是 `:65` 后新增小节而非改 `:62`，与 §三 R11 一致，无落点冲突。**顺带纠 C1-02 一处引用**：长度规则「30–200 字符」在 `SPEC.md:54`（逐字「- 长度 30–200 字符。不跨行拼接、不改标点、不加省略号、不修正 OCR 噪声。**复制粘贴,不要凭记忆重写。**」）而非其所写的 `:52`（`:52` 是空行）。

**裁决：A5 落盘会使既存裁决 2 所依赖的「24」过期，须在裁决 2、§三 R6/R7 处加时点标注；方向对裁决 2 有利，不构成冲突。已核。**来源 `A5-CX2与origin.md` §0 第 5 点、§5.1、§5.2、§7.1，与本清单裁决 2 缺口 A、§三 R6/R7。裁决 2 否决 P06 豁免 2 的**唯一**依据是射程比：「`origin=model` 的 24 个节点里 20 个有锚点、4 个无锚点……它选的判据射程比它要豁免的对象大 6 倍」。A5 要给 15 个 CX2 节点 + 5 个 A2-x2 节点（含 §7.1 伞节点则 21 个）新增 `origin: "model"`，落盘后 `origin=model` 由 **24** 升至 **44**（含伞节点 45），须豁免者仍是 4 个，射程比由 6 倍变为约 **11 倍**，裁决 2 的结论只会更强。**故不改写裁决 2**，只要求两点：①裁决 2 与 §三 R6（P05 论证里的「24 个 `origin: model` 节点里 20 个有锚（37 条）」）、R7（「现 546 节点 0 个带 `unanchorable`」）中的基数须注明「A5 落盘前口径」，否则落盘后复算对不上；②可用的落盘前基线（我实测）：全库锚到 `*-exercises.md` 的节点 **49** 个（`nodes-CX2.jsonl` 15 + `nodes-A2-x2.jsonl` 34），其中带节点级 `origin` 者 **0**，与 §7.4 自报一致。

**裁决：A5 §5.1 的 `tmp_apply_a5.py` 改后采纳——插入位置照用，整行 `json.dumps` 重序列化须改定点插入；§7.1 伞节点一并加字段。已核。**来源 `A5-CX2与origin.md` §5.1 代码块与其后「注意」段、§7.1。该脚本以 `object_pairs_hook=OrderedDict` 保序、在 `if k == 'anchors':` 后插 `new['origin'] = 'model'`（插在 `anchors` 之后、`origin_note` 之前，此位置与 §3.3 形态 (a) 一致，照用），但收尾用 `out.append(json.dumps(new, ensure_ascii=False))` 重建整行，与紧随其后的验收承诺「确认只有 15 行变化，且**每行只多出 `"origin": "model", `**」不可同时成立——重序列化会重写该行全部标量的转义与间隔。改为定点插入后再 `git diff --stat` 验只有 15 行变化。前提相符已核：`data/nodes-CX2.jsonl` 实测 **15** 行、全带 `origin_note`、全无 `origin`，故「`origin_note` 一字不动」可兑现。§7.1 伞节点 `apostol:cx-inner-product-axiom-diagnosis` 实测在 `data/nodes-A2-x2.jsonl:1`、`origin` 与 `origin_note` **均无**、现有 **3** 条锚点（117 / 78 / 76 字符，全在 `15.12-exercises.md`）——已触 `SPEC.md:53` 的 3 条上限，故它**只能加字段、不能补锚**，A5 的建议动作恰好落在唯一可行区间内；被统摄的 `:2`–`:6` 都标而伞节点不标属反常状态，采纳一并加。

**裁决：A5 §7.4 的「13 个疑似形式 E」与所列 id 数不符，开批前须补齐一个 id；「16 个非形式 E 也补 origin」不采纳。已核。**来源 `A5-CX2与origin.md` §7.4。三个计数机器复核属实：全库锚到 `*-exercises.md` 的节点 **49**、分布 CX2 15 + A2-x2 34（`nodes-A2-x2.jsonl` 恰 **34** 行）、本批 20、剩 29。但原文写「**13 个**疑似形式 E」，其后只列出 `:1`、`:8`、`:11`、`:12`、`:13`、`:16`、`:17`、`:18`、`:21`、`:30`、`:33`、`:34` = **12** 个，`:9`/`:10` 另起一句判为「属**混合**，须单独判」，两读都与 16 + 13 = 29 的分账矛盾（若 `:9`/`:10` 计入则 14，不计入则 12）。补齐后才能据此开批。同小节「16 个不是形式 E」的不采纳理由用原文自给的判据即足：锚点含 "Prove that…" / "show that…" 型题面，定性结论有锚，不满足形式 E 构成要件——此项与既存清单 §四 已登记的落选项同向，本条不另立。

**裁决：A5 §6 的 20 条全 KEEP、死因 `0`、登记形式 E 采纳；但 §三 R18 新增死因 `6` 时必须显式写入「形式 E ≠ 死因 6/3」一句，否则会误杀这 20 个。已核（923 未复核）。**来源 `A5-CX2与origin.md` §6.1、§6.2，与本清单 §三 R18。§6.1 实测恰 **20** 行 `| KEEP | 0 |` 判决行、id 逐条回显。`SPEC.md:100` 逐字为「死因编号:`1` 量词过度断言 / `2` 编造成员归属 / `3` 伪造教材出处 / `4` 数学错误 / `5` 伪抽象·层塌缩。」——现文止于 `5`、无 `6`，故 A5「形式 E 不入现有死因编号、记 `0`」的记账口径与 SPEC 现状不冲突。**风险点**：R18 要一次性把死因 `6`（引文不支撑该断言）写进 `:100`，而这 20 个正是「锚点支撑题面、无锚的是另一条子句」的形态，按 `6` 的定义仍不判死；A5 §6.2 自陈「主控若把形式 E 记成死因 3，20 个节点会被误杀」，同一风险在 `6` 上重现。故须在 R18 的条文里显式划界。§2.2 表自洽性已核：**41** 行、`verbatim`/`occurrences`/`newline` 三栏一致、长度 min **34** / max **167**、41/41 全落 30–200，与 §0 第 2 点吻合（**但我未独立复跑这 41 条 quote**，只核了表内自洽）。§4.21 汇总自洽亦核：108 = 21 + 5 + 55 + 27，82/108 = 75.9%。A6 侧「921 → 923」未复核，我只实测基数 **921**。

**本片未覆盖**（有裁决意见但未装配，逐条列落点与来源）：①`A3-零证据节点锚点候选.md` §2（锚点供给枯竭的量化）、§3.1 与 §4.1 的逐条断言分解、§4.2 的「Equivalently 属过度断言（死因 1）」这一独立缺陷（不依赖补锚，可单独装配）、§4.4 两条边 evidence 位（`edges-D2.jsonl:64` 的 176 字符 / `:65` 建议留空）、§4.3「明确不推荐的三个候选」（43 / 60 / 63 字符）均未回源；§5 已由既存清单 §四 判「待裁」，不重列。②`A4-形式B漂移与借用锚点.md` §1.1–§1.3 的**断言级论证**——我只核了三个替换块的机器合规（逐字、唯一、单行、长度），**未核每条新锚是否真支撑其所挂断言**，按盲区五型这正是 grep 查不出的那层；另 §2 七条的逐条论证、§5「15.03 已被挖满」持有者普查、§6「如实声明剩余无法上锚的断言」未读。③`A5-CX2与origin.md` §4.1–§4.20 的 20 张子句分解表未逐张回源（只核 §4.21 汇总自洽）、§2.2 的 41 条 quote 未独立复跑、§2.3 的 10 标志词强化核验、§2.4 两项顺带发现、§3.4 形态 (b) 三条退路、§5.2/§5.3 的 5 条 `origin_note` 逐字文本未与源题面比对。④A1 / A6 / A7 三份的可装配部分已分别由既存追加裁决 乙 / 丙 / 甲 承载，本片不重复，只在 A4 §7A 条内补了与 乙 的顺序相容性确认；A1 §2.1 对 A / 对 B / 对 C 三处裁定与 §2.2 的 FULL 27 / PART 0 未回读。⑤全库锚点落盘后计数（A3 的 924、A4 的 918 / 917、A6 的 923）一个都未实测，只核了基数 **921**、节点 **522**、`origin` **33**、`origin_note` **39**、锚到 exercises 的节点 **49**。
