**裁决：`report/待应用清单.md:199–200` 必改——CX2 那 15 个节点的 `origin` 待办已完成，真正未披露的是 `nodes-A2-x2.jsonl` 的 34 个节点。已核。**
来源提案 `A10-批次分层信号.md`。核过的落点：§6.1（第 350–371 行）给出「现文」与「改为」两个代码块，替换文本须照抄第 359–366 行整块、不得转述。事实依据在 §4.1（第 203–215 行）：`nodes-CX2.jsonl` **15/15** 全部带 `origin_note` 且逐条点名模型自供的见证元，逐字例如 `The witnesses f(x) = x and 2f are chosen by the model; Apostol states only the exercise.`（第 207 行）；第 230 行批次表给 `A2-x2` 的 `origin`/`origin_note` 命中 **0/34**、锚点全在习题文件 **34/34**。§八 第 495–496 行由独立第二次运行复得 15/15（并加测「15/15 无 `origin` 字段」）与 0/34。改必要性不止记账：第 368–371 行指出照原描述会写出「习题锚点 AND 缺 origin」的合取判据，纸面命中 CX2、实跑覆盖由 20/25 掉到 5/25。与既存裁决 1/2/3 无交集——裁决 2 处理的是 SPEC `:55` 对 `origin: model` 的授权，本条只改一处报告记账、不动 SPEC 条文。**未复核**：`待应用清单.md:199–200` 两行未独立定位，行号按 A10 自报。

**裁决：`report/_D分片-主控核验.md:525` 按 A9 定稿替换，同时 `:524` 引导语由「两处」改「一处」、`:518–522` 整段不动。已核（提案侧）。**
来源 `A9-记账错更正.md` §更正一。错误表述逐字在第 27 行「D2 59–80 片给 L66 的建议锚点标为 100 字符，实际可引子串是 89 字符。」；`:525` 替换文本照抄第 48 行整段；`:524` 新引导语照抄第 52 行「**附带修正一处分片记账错误，并撤回一处主控自身的记账错误：**」；不得连带改的范围在第 56 行（`:518–522` 过报通道 (c) 论述全部为真：行 233 字符、节点锚点 79 字符、`edges-D2.jsonl:85` 89 字符）。三串归属表在第 33–37 行：A=**100**（分片第一锚点）、B=**89**（`edges-D2.jsonl:85` 的 `evidence.quote`）、C=**79**（`nodes-D2.jsonl:66` 唯一 `anchors` 项），A−B=`EXAMPLE 3. ` 11 字符。**对裁决 1 的增量而非重复**：裁决 1 只裁了「100 对、89 是另一条子串」这个判断本身，未给 `:524`/`:518–522` 的处置。另第 41–43 行给出分片**第二**锚点 `15.07.md:39`（`EXAMPLE 7. If $a_1, \ldots, a_n$ are distinct real numbers, the $n$ exponential functions`）实测 89、且整行即该串——这是同段落里第二个 89 的来源，装配时**不得与 B 的 89 合并记账**。与既存 §二 步骤 8 的关系：步骤 8 只登记 `:525` 与 `_转交-新会话-workflow.md:293` 同改 100/89（依 G2f），A9 全篇未提及后一文件，两者是不同目标、不可互相替代。行号未独立定位。

**裁决：`report/审查-形式D-D3-片21-45.md:89` 整行替换，`:125` 移出「本该引而未引」并当场加注，不是裸删。已核（提案侧）。**
来源 `A9-记账错更正.md` §更正二。错误行逐字在第 87 行；替换定稿照抄第 114 行整行，其第 4 列为 「`:22`、`:151`、`15.11.md:52`（`:125` 已是该节点唯一锚点，42 字符，不在缺失之列）」。实测依据第 93–99 行：`data/nodes-D3.jsonl:31` = `d:norm-positivity-property`，`sections: ['15.10']`，`anchors` 恰 1 条 `{"file": "15.10.md", "quote": "(b) $\\| x\\| >0$ if $x\\neq O$ (positivity)."}`，**42 字符**，命中 `15.10.md:125`。其余三项经核都在（第 106–108 行：`15.10.md:22` 35 字符、`:151` 整行 195、`15.11.md:52` 整行 101，均 30–200 且不跨行）。判决影响在第 134 行：**E-1 判定维持、`1/3` 额度维持、严重度不降级**，缺失清单 4 → 3 项。连带待办（第 136 行）：同一条 42 字符锚点在 `_D分片-主控核验.md:293` 被当作已有锚点实测过，同一行在两个分片里一次算已有、一次算缺失，两处记账须对齐。**对既存 §二 步骤 8 的一处收紧**：步骤 8 写的是「删 `:125`」，A9 定稿要求删的同时把那句注写入，否则读者无从知道 `:125` 为何不在列——按 A9 定稿执行，不按裸删执行。行号未独立定位。

**裁决：`data/inherited/nodes-A1.jsonl:8` 的 `sections` 与第三条锚点同批落盘，逐字文本取 A9 定稿；`report/audit-A5-A1-verdicts.jsonl:8` 的 `cause` 维持 6，两项写入后同批改 0。已核（提案侧）。**
来源 `A9-记账错更正.md` §更正三。`sections` 照抄第 214 行 `"sections": ["15.02", "15.03", "15.04"],`；`anchors` 整个数组照抄第 218 行（三条：`15.02.md` 的 `AXIOM 5. EXISTENCE OF ZERO ELEMENT. There is an element in V, denoted by O, such that`、`15.04.md` 的 `Axiom 5 tells us that there is at least one zero element.`、新增 `15.03.md` 的 `The zero element is the function whose values are everywhere zero.`）。新锚点合规实测在第 221–223 行：**66 字符**（落 30–200）、位于 `15.03.md:19`、该行整行 **359 字符含多句**故为**句级子串**、不跨行、未改标点未用省略号、经全 16 文件遍历**只出现在 15.03.md**、原 2 锚而上限 3 恰有 1 空位。verdicts 侧在第 225–226 行：cause 维持 6 直到锚点真写进 `nodes-A1.jsonl`，先改死因会让 verdicts 停在「声称无缺陷而缺陷仍在」的中间态。**这是对既存裁决 1 不确定点的直接补齐、不是冲突**：裁决 1 自述「G2e 新构造的那条 15.03.md:19 锚点字符数我未实测（采信 G2e）」，A9 独立测得 66，与既存 §二 步骤 10 所记「第 3 锚 66 字符」相符，两个来源互相印证。A9 另给 `_D分片-主控核验.md:400–407` 的替换段（第 230–231 行，逐字含「按四层定义 TD 不入池」「池共 7 条，无一条来自 15.03.md」），须与步骤 10 同批。行号未独立定位。

**裁决：`apostol:zero-element` 从「`sections` 语义未裁」这个议题里摘出，议题本身保留待议。已核（提案侧）。**
来源 `A9-记账错更正.md` §更正三末节（第 235–251 行）。实测前提在第 237 行：`sections` 在 `SPEC.md` **只出现于 `:62`（节点 schema）与 `:84`（L2+ schema）两处示例，全文没有一句定义它的语义**。剥离依据是补锚后两种候选裁决同向（第 241–242 行）：甲（sections = 锚点所在小节）下，补了 `15.03.md` 锚点后 15.03 即成为锚点所在小节 ⇒ 必须含 15.03；乙（sections = 内容归属小节）下，句 2 内容出自 15.03 ⇒ 必须含 15.03。只有「不补锚、只改 sections」这条已被否决的主控原路径才依赖该裁决，且甲下字段自相矛盾、乙下 cause 6 照样成立（第 248–249 行）。**与既存 §五 冲突 3（`sections` 语义三方互斥）不冲突**：本条不裁语义，只把这一个节点移出该议题的作用域；A9 明写语义议题对**没有**现成可引句的节点仍有作用力，不该拿这条当它的实例。与裁决 2 亦无交集——裁决 2 的 `unanchorable` 管无锚豁免，本节点补锚后有锚、不走豁免。

**裁决：「凡在 `:450` 之前做出、且理由里引用了该节点是 `dst` 的边的塌陷判定都要重算」只登记为待办，不得作为本轮结论。已核（提案侧）。**
来源 `A9-记账错更正.md` 第 288 行给出这条可推广检查，同行即自陈「我这轮只被指派了这三处更正，**没有做这个全量重扫，这是本提案留下的缺口**」，第 296 行「本提案的边界」第 1 项重申。该节点为何漏在重算范围外，依据第 199–201 行：`_D分片-主控核验.md:468–471` 只对三条走 T3 的塌陷做过重算（点名 `theorem-elementary-algebraic-properties`、`theorem-15-11-…`、`parsevals-formula`），而 `apostol:zero-element` 在 `:345` 的表里被单列为「指控的**理由**不成立、但**缺陷是真的**」这个中间类别，四层定义是 `:450` 起才引入的，故它的理由塌陷**从未按四层定义重算过**。TD 判据实测在第 168–192 行：`edges-A1.jsonl:18`（`apostol:function-space -[requires]-> apostol:zero-element`）里该节点是 `dst` ⇒ 按 4.6 表属 TD、不入池；按 T1/T2/T3 实算池共 **7** 条（85/57/85/36/88/36/85），无一条来自 `15.03.md`，目标句空白归一化后在池中命中 **0**，只出现在 TD 那 12 条里。**与既存裁决 1 同向**（裁决 1 依 G2e 得同一结论），本条只补「一次理由塌陷判定的作废机制」这一层与它的待办边界，不重述死因结论。

**本片未覆盖**（有落点但本片未装配，须另片承接）：①A10 §二/§三 的比率表我只核了表内自洽与 §八 复算行的逐行对应，**未独立复算**（禁跑脚本）；②A10 §6.3/§6.4/§6.5 与 §4.2 的定稿代码块由既存追加裁决 丁 承接，本片不重复，其中 §6.5 新节标题「整批 34 个节点」与正文「剩余 28 个从未被审查」须同时保留这一点已在 丁 内；③A9 两段回源验证 Python 代码未执行，字符数一律按提案自报接受（100/89/79/42/66/35/195/101/233/359）；④所有目标文件行号**未独立定位**：`待应用清单.md:199–200`、`_D分片-主控核验.md:293/345/394–409/400–407/468–471/518–522/524/525`、`审查-形式D-D3-片21-45.md:89`、`audit-A5-A1-verdicts.jsonl:8`、`inherited/nodes-A1.jsonl:8`；⑤A9 边界第 2、3 项无落点、须另立工单——`d:norm-positivity-property` 那三条缺失引文 A9 只量了长度与合规性、**未核它们是否真能支撑对应 claim**（按 4.1 长度合规不等于支撑成立），`d:dim-of-a-second-order-de-solution-space-is-two` 的 claim 3（「Apostol 在此没有证明关于二阶齐次方程的任何一般结论」）是**章级否定存在断言**、A9 未核、与 D2 36–58 片 line 55 同型、任何单条引文都无法见证；⑥A10 六条与 A9 三条落选项已由既存 §四 表收录（A10 在该表第 318–320 行、A9 在第 325–326 行），本片不重列；⑦A9 第 299 行的执行后要求（写入后须自跑 `verify_anchors.py`，并按 4.2 读 stdout 里 `nodes with NO anchor` 那一行、不要只看退出码）属既存 §六 地界，本片未并入。
