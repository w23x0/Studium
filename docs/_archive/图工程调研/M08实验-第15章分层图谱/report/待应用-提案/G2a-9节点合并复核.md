# G2a 9 节点合并裁决复核

范围：只复核 `待应用清单.md` §一 的「维持的 KILL 从 5 降到 3」这一件事。
被放掉的 2 条逐条复核，维持的 3 条只查互证塌缩。范围外的一律不动。

## 〇 结论速览（5→3 是否成立；若不成立，应是 5→几）

**5→3 不成立。应为 5→4。**

| 被放掉的节点 | 放掉理由 | 复核结论 | 最终建议 |
|---|---|---|---|
| `d:ten-axioms-listed-in-three-groups` | 三个 L1 分组节点会失去**唯一**汇聚父节点，裸删=层塌缩 | **理由的事实前提为假**：三个分组节点各自另有一条 `part-of --> apostol:linear-space` 的边，已在图里，删本节点不产生任何孤儿 | **恢复 KILL**（死因 5），执行时把 `edges-D1.jsonl:10/11/12` 一并送墓地 |
| `x:least-squares-is-a-projection` | ①2 个 L1 成员在 X 层别无落点、5 条入边悬空（ADJ2）②「不可解系统」这一对齐全图无第二承担者（ADJ1） | 理由①**无效**（记账问题，且 5 条入边是 TD，不入池）；理由②**成立且机械可证**：全图 546 节点正则搜 unsolvable/no solution/salvage，本节点外仅 1 处命中且语境无关 | **维持放掉（FIX 不删）**，但须按 G2d F2 修死因 6 |

一句话：**两条放行里一条靠假前提、一条靠真证据。** 5 条维持 KILL 中应恢复 1 条。

死因 1 的特例检查（简报要求）：**两条原指控都是死因 5，不是死因 1**
（`audit-B2-verdicts.jsonl` 逐字："cause": 5 各一条）。故「死因 1 不因边证据免除」这条规则
在本轮**不触发**，两条放行理由都不能靠这条规则一键否掉，必须逐条查前提。这一点如实登记。

## 一 被放掉的 2 条逐条复核

### 1.1 `d:ten-axioms-listed-in-three-groups`（`data/nodes-D1.jsonl:5`）

**原指控（`report/audit-B2-verdicts.jsonl:5`，死因 5）逐字**：
"复述 parent apostol:linear-space 的「subject to ten axioms listed in three groups」。
三个分组本身已是 L1 兄弟节点 …… 剩下的「分组只是叙述性的」是无后果的元评论。"

**放掉理由逐字**（`待应用清单.md` §2.1）：
"5 条入边,其中 3 条是三个 L1 分组节点的 `part-of`。**`apply_kills.py` 把边送进墓地,不改指向**,
所以裸删会让三个分组节点失去它们唯一的汇聚父节点。ADJ2 的前置条件是对的:
**先把 :10/:11/:12 改指 `apostol:linear-space`,再删。**"
G2b §五 把它推到更强的形式："它是三个 L1 分组节点唯一的汇聚父节点，删它就是层塌缩本身……
判 5（层塌缩）却用一个会造成层塌缩的动作执行，是自相矛盾的。"

**池内证据清单**（`pool.tsv`，TD 已排除；`exposure.tsv`：后代 19 / T1=2 / T2=1 / T3=60 / 池 63 / 去重源行 27）：
- T1 `15.02.md` 163 字符 "Let V denote a nonempty set of objects…which we list in three groups."
- T1 `15.01.md` 54 字符 "We turn now to a detailed description of these axioms."
- T2 `15.02.md` 106 字符（自发边 `edges-D1.jsonl:5`，即 163 那条去掉首句）
- T3 60 条，全部来自三个分组节点及其 19 个后代的逐条公理锚点。

**理由是否成立：不成立，前提为假。**
`tmp_index/edges.tsv` 里以三个分组节点为 src 的 `part-of` 边**各有两条，不是一条**：

```
data/edges-D1.jsonl:10            apostol:closure-axioms                        part-of  d:ten-axioms-listed-in-three-groups  source
data/edges-D1.jsonl:11            apostol:axioms-for-addition                   part-of  d:ten-axioms-listed-in-three-groups  source
data/edges-D1.jsonl:12            apostol:axioms-for-multiplication-by-numbers  part-of  d:ten-axioms-listed-in-three-groups  source
data/inherited/edges-A1.jsonl:1   apostol:closure-axioms                        part-of  apostol:linear-space                 source
data/inherited/edges-A1.jsonl:2   apostol:axioms-for-addition                   part-of  apostol:linear-space                 source
data/inherited/edges-A1.jsonl:3   apostol:axioms-for-multiplication-by-numbers  part-of  apostol:linear-space                 source
```

`partof.tsv` 两行独立印证：`apostol:linear-space` 的 17 个子节点里**已经包含**这三个分组节点。
三节点的 `parent` 字段本身为空（`nodes.tsv`），它们的父子关系全靠边表达，而**指向
`apostol:linear-space`（B2=KEEP）的那 3 条边不随本节点删除**。
所以「失去唯一汇聚父节点」为假，「删它就是层塌缩本身」为假。
ADJ2 与主控核的「5 条入边」计数没错，错在把 `:10/:11/:12` 读成了这三个节点的**唯一**父边——
只查了本节点的入边表，没查这三个节点的**全部出边**。

G2b 的另一条依据（"S2/S3/S4 的 2/4/4 分配 parent 一个字未给，故有增量"）也不足以归零：
2/4/4 可从三个分组节点的锚点一步数出，这正是死因 5 的定义域；
G2b 与 ADJ2 自己都写了"虽可从三个 L1 兄弟重新导出——ADJ2 与我都承认这点翻不动"。
按四层池口径，这些证据全在 T3 —— **T3 塌陷说明内容合法地下移，父节点不需要存在来承担它**。
续跑注记已查实：163 字符那条锚点是 parent 所持 106 字符锚点的**超集**，
即本节点未引入 parent 之外的任何新源行——这在机械层面**支持**死因 5，不是推翻它。

**死因**：5（伪抽象·层塌缩），维持。
**建议动作：恢复 KILL。** 执行前置条件只剩一条（且是纯记账）：
`edges-D1.jsonl:10/11/12` 随节点一并送墓地，**不需要改指**——改指会造出与
`inherited/edges-A1.jsonl:1/2/3` 重复的三条边。
**引文残留（如实登记）**：`15.01.md` 那条 54 字符锚点本节点是**唯一持有者**
（`shared-quotes.tsv` 无此行，`anchors.tsv` 全表仅 1 次），删则该源句离开全图。
但 G2b §五 自己已认定这条锚点"**一句都不管**"（零支撑），且其 FIX 方案本就要撤下它——
故它不构成存活理由，只是一处可接受的引文损失。
163 字符那条有 3 个持有者（含 KEEP 的 `d:linear-space-requires-a-nonempty-underlying-set`），不损失。
**置信度**：高。两张表（edges.tsv 与 partof.tsv）互相独立印证同一结论，纯机械。
**不确定点**：未核 2/4/4 这句是否值得并入 parent statement。B2 未要求并入；若主控要保守处理，
可按 `d:example-1-verification` 的模式先并入 `apostol:linear-space` 再删，不影响本条裁定。

### 1.2 `x:least-squares-is-a-projection`（`data/nodes-X.jsonl:20`）

**原指控（`audit-B2-verdicts.jsonl`，死因 5）逐字要点**：
"复述兄弟 `x:best-approximation-by-the-projection` 的 divergence 末段……
statement……则是 `x:orthogonality-of-the-residual` 与 `x:projection-onto-a-subspace`
的不变量换一种说法；所谓两侧动机相反（逼近论 vs 不可解方程）正是上述末段已给的同一件事。"

**放掉理由逐字，两条独立**：
- ADJ2（`裁定-ADJ2.md:27` / §2.7 / :432）："部分翻案：2 个 L1 成员在 X 层别无落点,5 条入边悬空"；
  "statement 两句与两个兄弟的不变量重合,这一半我**翻不动**。翻动的是「直接删」这个动作"。
- ADJ1（`裁定-ADJ1.md:20`）："「不可解的精确问题被换成可解的正交条件」这一 invariant 及其
  「动机相反」的负向对齐**全图无承担者**：22 个 x: 节点里只有本节点出现 no solution / unsolvable /
  salvage，且它是 `strang:solvability-condition` 的**唯一**持有者。"

**池内证据清单**（`exposure.tsv`：后代 0 / **T1=2 / T2=0 / T3=0** / 池 2 / 去重源行 2）：
- T1 `15.14.md` 100 字符 "Given an element x in V, to determine an element in S whose distance from x is as small as possible."
- T1 `4.1.md` 140 字符 "It contains the error $e = b - A x$ in the \"least-squares\" solution. Least squares is the key application of linear algebra in this chapter."

池里就这 2 条，别无它物。它自发的 3 条边（`edges-X.jsonl:156/157/161`）**evidence 全空**
（`edges.tsv` ev_charlen=0），所以 T2=0；无 part-of 后代，所以 T3=0。

**理由①（ADJ2：成员无落点 + 5 条入边悬空）不成立。**
`edges-X.jsonl:93–97` 五条 `is-a` 全部以本节点为 **dst**，即 TD，按四层池定义**排除，不入池**；
且五条 origin 全为 `model`、evidence 全空，删除不损失任何引文。
悬空入边是 `apply_kills.py` 的记账后果，属**执行前置条件**，不是「断言无证据」的反驳。
成员侧我逐个核了 `nodes-X.jsonl` 里的出现次数：`strang:solvability-condition` 1 次、
`apostol:distance-between-elements` 1 次、`d:trivial-case-when-x-already-lies-in-s` 1 次
（**三个唯一持有，不是 ADJ2 说的两个**），其余 5 个成员在兄弟节点里另有落点。
这一项确认「删则 X 层成员覆盖有缺口」，但缺口可以靠把这 3 个成员移挂到
`x:best-approximation-by-the-projection` 补上——**所以它也只是前置条件，不救命。**

**理由②（ADJ1：全图无第二承担者）成立，且我机械复核通过。**
对 `data/*.jsonl` + `data/inherited/*.jsonl` 全部节点文件的 `statement`/`divergence`/`name_en`
跑正则 `no solution|unsolvable|not solvable|salvage|outside the column space|overdetermined`，
命中只有两处：本节点（5 次），以及 `data/nodes-D3.jsonl:43`
`d:angle-defined-only-in-real-euclidean-space`（"the equation has no solution in general"，
讲复内积下 cos θ 无解，**语境完全无关**）。
再逐字读兄弟节点 `nodes-X.jsonl:19` 的 divergence 全文：它的末段是
"Apostol's theorem instantiates to Fourier partial sums and Legendre approximation…;
Strang's flows into least squares fitting of data, and he defers function approximation out of the chapter."
——**只说应用分流，没有「b 在列空间外 ⇒ Ax=b 无解 ⇒ 最小二乘是抢救操作」这条因果链。**
故 B2 的"正是上述末段已给的同一件事"为过强：statement 两句确与兄弟的不变量重合（ADJ2 也认），
但 divergence 里的不可解系统动机在全图**确为孤本**。死因 5 要求「相对存活侧无增量」，此处有增量。

**死因**：5 撤回；但**另立死因 6，成立**。
两条锚点都支撑不住 statement 的核心断言：`15.14.md` 那条只给「最佳逼近问题」的提法，
不含 projection 与 orthogonality；`4.1.md` 那条逐字始于 `It contains the error`，
起点落在动机句之后（源行 `strang-ch4/4.1.md:31` charlen=413、**n_sentence_end=4**，
动机句在同一行前半段——**过报通道 (b) 的活实例**，按行号比对会误判为已覆盖）。
D 型射程不足。G2d §D2 已独立登记同一结论，两方一致，我不重复裁断修法。

**建议动作：维持放掉（FIX，不删）**，但记录里必须写明**放行靠的是 ADJ1 的孤本论证，
不是 ADJ2 的悬空边/成员落点论证**——后者两条都只是执行前置条件。
配套：按 `G2d-裁定员缺陷.md` §F2 换 1 锚 + 增 1 锚 + 补 `sections` 到 `["15.14","15.15","4.1"]`，
把死因 6 归零。若上层仍决定删，则 ADJ2 :432 那条前置（为 3 个唯一成员另找落点 + 处理
`edges-X.jsonl:93–97`）必须先做。
**置信度**：高（孤本论证是全库正则，可复现）。
**不确定点**：ADJ2 说「2 个 L1 成员」，我数出 3 个唯一持有成员，多出的
`d:trivial-case-when-x-already-lies-in-s` 是 d: 层不是 L1 层——若严格按 L1 计则 ADJ2 的 2 正确，
这处差异不影响裁定。另：statement 首句与 `x:projection-onto-a-subspace` 的同义程度我按 ADJ1/ADJ2
两人的一致认定采信，未再独立逐句判。

## 二 维持的 3 条的互证检查

只查一件事：杀因是否引用了同批被杀的节点（互证塌缩）。
方法：取每条 KILL 的 `reason` 全文，提出其中点名的 id，再查该 id 在
`report/*-verdicts.jsonl` 里的判决。

| 被杀节点 | reason 点名的节点 | 后者判决 | 关系性质 | 塌缩？ |
|---|---|---|---|---|
| `d:example-1-verification-is-the-field-axioms-of-r`（`nodes-D1.jsonl:46`） | `d:example-3-verification-reduces-to-arithmetic-in-each-component` | **KILL**（本批） | 先例（"与被杀的…属同一 species"） | **否** |
| 同上 | `d:example-1-the-real-numbers-form-a-linear-space`（parent） | KEEP | 承载 | 否 |
| `d:function-space-zero-is-the-everywhere-zero-function`（`nodes-D1.jsonl:60`） | `apostol:function-space`（parent） | KEEP | 承载 | 否 |
| 同上 | `d:axiom-5-existence-of-zero-element` | KEEP | 承载 | 否 |
| 同上 | `d:degree-exactly-n-has-no-zero-element` | FIX / KEEP | 承载 | 否 |
| `d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n`（`nodes-D1.jsonl:71`） | `d:degree-exactly-n-is-not-a-linear-space`（parent） | KEEP / FIX | 承载 | 否 |
| 同上 | `apostol:subspace` | KEEP | 承载 | 否 |

**结论：3 条维持的 KILL 里只有 1 处点名了同批被杀节点，且是先例关系不是承载关系，不塌缩。**
`d:example-1-verification` 引 `example-3-verification` 的方式是"属同一 species"——
后者死亡不改变前者是否冗余；A1 提案 §2.1 对 A 已作同一裁定，我逐字复核 reason 后同意。
但该条的**实质前置条件另有其事**且已定为强制：那十条「公理 ↔ R 的算术律」映射表
须先并入 parent `d:example-1-the-real-numbers-form-a-linear-space`（KEEP），不可裸删。
其余 2 条的杀因点名者**全部存活**，零残留，可直接执行。

**依据**：`report/audit-B2-verdicts.jsonl` 三条 reason 逐字；
各点名 id 的判决用 `grep '"id": "<id>",' report/*-verdicts.jsonl` 逐个查得（见上表第 3 列）。
**死因**：三条均为 5，维持。**建议动作**：三条保留 KILL。
**置信度**：中高。reason 文本是我逐字读的；id 提取靠人工读而非全库正则匹配，
可能漏掉 reason 里以自然语言描述而未写 id 的承载者——这一类 A1 提案 §2.2（锚点轴）已另行覆盖，
本节不重复。

## 三 与 A1 提案的交叉点

**A1 的 22 id 表本身不改。** 该表按定义是「B2 的 29 条 KILL 减去 §一 的 9 个争议节点」，
`d:ten-axioms-listed-in-three-groups` 与 `x:least-squares-is-a-projection` 都在那 9 个里，
从来不在 22 条内（A1 §1 表逐条回显可核）。恢复前者的 KILL 不会把它挪进 22 条表。

**受影响的是 A1 §1 表末尾那段「三条维持的 KILL」的登记**，须改为**四条**：
在 `d:example-1-verification-is-the-field-axioms-of-r`（`nodes-D1.jsonl:46`）、
`d:function-space-zero-is-the-everywhere-zero-function`（`:60`）、
`d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n`（`:71`）之外
**增列 `d:ten-axioms-listed-in-three-groups`（`data/nodes-D1.jsonl:5`）**，
并把它纳入 A1 §2.1 的互证扫描范围（我已代查：它的 reason 点名
`apostol:closure-axioms` / `apostol:axioms-for-addition` /
`apostol:axioms-for-multiplication-by-numbers` / `apostol:linear-space`，**四个全 KEEP，不塌缩**）。

**与 G2b 提案直接冲突。** `G2b-十条公理FIX.md` 整篇建立在「本节点是三个 L1 分组节点唯一汇聚父节点」
之上（§五 依据三），该前提经 `edges.tsv` 与 `partof.tsv` 双表证伪。
若采纳本提案，**G2b 全篇作废**（它的锚点方案与 SPEC 归因发现可另行保留供 S1 参考）。
这一处冲突请综合代理裁决，我不单方面判 G2b 无效。

## 顺带发现

- `pool.tsv` 的 T3 口径把 `apostol:linear-space` 一路的公理锚点全部计入 `d:ten-axioms-…` 的池（T3=60），使一个零增量节点的 `pool_total` 高达 63，**池总数不能当重要性指标用**。
- `x:least-squares-is-a-projection` 的 3 条自发边（`edges-X.jsonl:156/157/161`）evidence 全空且 origin=model，X 层「边不带证据」可能是批次性现象，未逐批核。
- `apostol:closure-axioms` / `axioms-for-addition` / `axioms-for-multiplication-by-numbers` 的 `parent` 字段皆空、父子关系只靠边，与 d: 层节点写 `parent` 字段的做法不一致（记账双轨）。
- `strang:solvability-condition` 的两条锚点都出自 `3.3.md`，而本节点 sections 只登 `15.14,4.1`，跨小节成员未反映在 sections 里（与 G2d F2 指出的 15.15 缺席同型）。

