# 15.10 覆盖完备性

## 〇 结论速览

**本节 64 句 / 已覆盖 56（其中 21 句合法下移到 part-of 后代）/ 无锚但有节点承载 3 /
真未覆盖 5（含 2 句部分未覆盖）；其中值得补建新节点 0 个。**

切句办法：按句末标点切，独立展示公式块（`$$ … $$` 内的一行）计为一句，
因为图谱把它们当作独立锚点在用。行号仅作定位，覆盖判定全部按句做——
本节 `source-lines.tsv` 里 `n_sentence_end > 1` 的行有 12 行
（L3 有 4 句、L11/L13/L35/L43/L45/L49/L59/L65/L133 各 2 句、L73 有 3 句、L119 有 4 句），
按行号比对会显著高报（过报通道 b）。

核心结论不是「缺内容」而是「缺锚」：**本节没有一句数学内容是图谱完全不知道的**。
5 处真缺口里，3 处是**同一个结构性原因**——该句所在的源行短于 SPEC 的 30 字符下界，
提案方于是拿邻句顶替，导致节点存在、statement 正确，但**没有一条锚点含有该句本身**：

| 缺锚的句子 | 源行:字符数 | 现有节点 | 现有锚点的实际内容 |
|---|---|---|---|
| 公理 (3) `c(x,y) = (cx,y)` | L19:21 | `d:inner-product-homogeneity-axiom` | 邻句「Note: Taking c = 0…」与复数版说明 |
| 范数定义式 `‖x‖ = (x,x)^{1/2}` | L108:26 | `d:norm-definition-as-square-root` / `apostol:norm` | 定义句的前半与后半，**中间的式子本身没有** |
| 性质 (a) `‖x‖ = 0 if x = O` | L123:29 | `d:norm-vanishes-at-the-zero-element` | THEOREM 15.9 表头 + Proof 开场句，两条都是借用 |

这三处 `d:inner-product-homogeneity-axiom` 的 statement 里已自认了
（「Because the display of axiom (3) occupies a line of only 21 characters,
the anchor below is the sentence that names the axiom and uses it」，
`statements.tsv` 该行）——说明这是**已知取舍**而非疏漏，但它同时说明
**SPEC 的 30 字符下界与 OCR 的逐行切分互相冲突**：
教材里最要紧的三个式子恰好都短于 30 字符。这一条建议进 SPEC。

建议动作合计：新建节点 0；**补锚 3 条**（需 SPEC 先给短式子开例外）；
**改错边 1 条**（`data/edges-D3.jsonl:171` 的 dst 指错空间，见第五节抽查②）；
补锚 1 条为可选（S49 类比句，见第三节）。
置信度：句级覆盖判定高（逐句 grep 三张表核过）；
「值得补建 0 个」中等——它依赖「缺锚不等于缺节点」这一判断，若综合代理采用
「一句必须有一条含它的锚点」的更严标准，则上表 3 处会从「补锚」升级为「补锚+FIX statement」。

## 一 逐句覆盖表

覆盖状态四值：**节点锚**=有节点锚点含该句；**下移**=该句内容不在其自然归属的 L1 节点上，
而在其 part-of 后代的锚点上（合法，不算缺口，后代 id 写在覆盖者列）；
**边 ev**=只由边的 evidence 承载；**未覆盖 / 无锚**。
「下移」一列标注的后代即为承载者。判据列写核过的表。

| 句 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | L3 | 依赖测长度与角的性质叫 metric properties | 下移 | `d:metric-properties-motivate-the-inner-product` a0(135) | anchors.tsv |
| S2 | L3 | V_n 中用点积定义了长度与角 | 节点锚 | `apostol:dot-product-in-vn` a0(84) | anchors.tsv |
| S3 | L3 | 现在要把这些想法推广到一般线性空间 | **未覆盖** | 无 | grep `Now we wish to extend` / `extend these ideas` = 0（三表） |
| S4 | L3 | 先推广点积为内积，再由内积定义长度与角 | 下移 | `d:metric-properties-motivate-the-inner-product` a2(157) | anchors.tsv |
| S5 | L5 | 点积 x·y 在第 12 章由公式定义 | **部分未覆盖** | `d:dot-product-component-sum-formula`（只有公式） | grep `was defined in Chapter 12` = 0；出处子句无锚 |
| S6 | L8 | `x·y = Σ xᵢyᵢ` (15.5) | 节点锚 | `d:dot-product-component-sum-formula` a0(58) | anchors.tsv |
| S7 | L11 | 一般空间里写 (x,y) 而不写 x·y | 节点锚 | `apostol:inner-product-notation` a0(86) | anchors.tsv |
| S8 | L11 | 公理化定义而非给定公式 | 节点锚 | `d:inner-product-axiomatic-not-formula` a0(69)、`apostol:inner-product` a1 | anchors.tsv |
| S9 | L11 | 陈述若干希望内积满足的性质并视为公理 | 节点锚 | `apostol:inner-product-axioms` a0(116) | anchors.tsv |
| S10 | L13 | DEFINITION 实线性空间有内积：唯一实数 + 全称量词 | 节点锚 | `apostol:inner-product` a0(176)、`d:inner-product-real-valued-and-quantifiers` a0+a1 | anchors.tsv |
| S11 | L15 | 公理 (1) 对称性 | 下移 | `d:inner-product-symmetry-axiom` a0(49) | anchors.tsv |
| S12 | L17 | 公理 (2) 分配性/线性 | 下移 | `d:inner-product-additivity-axiom` a0(63) | anchors.tsv |
| S13 | L19 | 公理 (3) `c(x,y) = (cx,y)` | **无锚** | 节点 `d:inner-product-homogeneity-axiom` 在，但两锚均为邻句 | 行长 21 < SPEC 30；grep `c(x,y)` 仅命中 statements.tsv 1 处 |
| S14 | L22 | 公理 (4) `(x,x) > 0 if x ≠ O` | 下移 | `d:inner-product-positivity-axiom` a0(35) | anchors.tsv |
| S15 | L25 | 带内积的实线性空间叫实欧氏空间 | 节点锚 | `apostol:real-euclidean-space` a0(75) | anchors.tsv |
| S16 | L27 | Note: 取 c=0 得 (O,y)=0 | 节点锚 | `d:zero-element-has-zero-inner-product` a0(68) | anchors.tsv |
| S17 | L29 | 复空间内积为复数、同公理但对称性被换掉 | 节点锚 | `apostol:complex-inner-product` a0(193) | anchors.tsv |
| S18 | L32 | `(x,y) = conj(y,x)` (1′) | 下移 | `d:hermitian-symmetry-axiom` a1(39) | anchors.tsv |
| S19 | L35 | (y,x) 指 (y,x) 的共轭 | 下移 | `d:hermitian-symmetry-axiom` a2(58) | anchors.tsv |
| S20 | L35 | 齐性公理中 c 可为任意复数 | 节点锚 | `apostol:complex-inner-product` a1(76) | anchors.tsv |
| S21 | L35+L37 | 由齐性与 (1′) 得伴随关系 | 下移 | `d:conjugate-homogeneity-in-second-argument` a0(46) | anchors.tsv |
| S22 | L40 | `(x,cy) = c̄(x,y)` | 下移 | `d:conjugate-homogeneity-in-second-argument` a1(86) | anchors.tsv |
| S23 | L43 | 带内积的复线性空间叫复欧氏空间 | 节点锚 | `apostol:complex-euclidean-space` a0(130) | anchors.tsv |
| S24 | L43 | (有时也叫 unitary space) | 节点锚 | 同上 a0 尾部含该括号 | grep `unitary` anchors=2 |
| S25 | L43 | 例：12.16 节的 V_n(C) | 节点锚 | `apostol:ext-vn-c-section-12-16` a0(91) | anchors.tsv |
| S26 | L45 | 虽主要关心实的，本章定理对复的同样有效 | 边 ev | `apostol:real-euclidean-space --contrasts--> apostol:complex-euclidean-space`(151) | edges.tsv；`d:euclidean-space-unqualified…` a1(72) 只含后半 |
| S27 | L45 | 不加限定说 Euclidean space 即可实可复 | 节点锚 | `apostol:euclidean-space` a0(127) | anchors.tsv |
| S28 | L47 | 读者应自行验证下列各例满足全部公理 | 节点锚 | `d:inner-product-axiomatic-not-formula` a1(98) | anchors.tsv |
| S29 | L49 | EXAMPLE 1：V_n 中取 (x,y)=x·y | 节点锚 | `d:dot-product-satisfies-the-inner-product-axioms` a0(87) | anchors.tsv |
| S30 | L51 | EXAMPLE 2：V_2 中两向量，按公式定义 (x,y) | 边 ev | `d:inner-product-not-unique-on-a-given-space --requires--> apostol:v-n-space`(123) | edges.tsv |
| S31 | L54 | `2x₁y₁ + x₁y₂ + x₂y₁ + x₂y₂` | 节点锚 | `d:inner-product-not-unique-on-a-given-space` a0(81) | anchors.tsv |
| S32 | L57 | 该例说明同一空间可有多个内积 | 节点锚 | 同上 a1(89) | anchors.tsv |
| S33 | L59 | EXAMPLE 3：C(a,b) 为 [a,b] 上连续实函数空间 | 节点锚 | `apostol:integral-inner-product-on-continuous-functions` a0(182) | anchors.tsv |
| S34 | L59 | 由公式定义 f,g 的内积 | 边 ev | `…on-continuous-functions --is-a--> apostol:inner-product`(67) | edges.tsv |
| S35 | L62 | `(f,g) = ∫ₐᵇ f g dt` | 下移 | `d:integral-inner-product-formula` a1(41) | anchors.tsv |
| S36 | L65 | 该式与 (15.5) 类比 | 下移 | `d:integral-inner-product-analogy-with-component-sum` a0(102) | anchors.tsv |
| S37 | L65 | 函数值扮演分量、积分取代求和 | 节点锚 | 同上 a1(137)、`apostol:integral-inner-product-on-continuous-functions` a1 | anchors.tsv |
| S38 | L67 | EXAMPLE 4：C(a,b) 中定义 | 节点锚 | `apostol:weighted-integral-inner-product` a0(42) | anchors.tsv |
| S39 | L70 | `(f,g) = ∫ₐᵇ w f g dt` | 下移 | `d:weight-function-must-be-positive` a0(47) | anchors.tsv |
| S40 | L73 | w 是 C(a,b) 中固定的正函数 | 节点锚 | `apostol:weighted-integral-inner-product` a1(95) | anchors.tsv |
| S41 | L73 | w 叫权函数 | 节点锚 | 同上 a1 含该句 | anchors.tsv |
| S42 | L73 | 例 3 中 w(t)=1 | 下移 | `d:weight-function-must-be-positive` a1(138) 含该句 | anchors.tsv |
| S43 | L75 | EXAMPLE 5：全体实多项式空间中定义 | **未覆盖** | 无（见第五节抽查②：现有边把它挂错到「次数 ≤ n」） | grep `real polynomials, define` 在 15.10.md 命中 0（命中的是 15.12-exercises.md） |
| S44 | L78 | `(f,g) = ∫₀^∞ e^{-t} f g dt` | 下移 | `d:exponential-weight-makes-improper-integral-converge` a0(56) | anchors.tsv |
| S45 | L81 | 指数因子使该反常积分对任意多项式收敛 | 节点锚 | 同上 a1(108) | anchors.tsv |
| S46 | L83 | THEOREM 15.8 内积满足 Cauchy–Schwarz | 节点锚 | `apostol:cauchy-schwarz-inequality` a0(98) | anchors.tsv |
| S47 | L86 | `|(x,y)|² ≤ (x,x)(y,y)` | 下移 | `d:cauchy-schwarz-inequality-statement` a1(70) | anchors.tsv |
| S48 | L89 | 等号成立当且仅当 x,y 相依 | 节点锚 | `apostol:cauchy-schwarz-inequality` a1(71) | anchors.tsv |
| S49 | L91 | 证：V_n 中(定理 12.3)的证明只用了定理 12.2 列的性质 | 节点锚 | `apostol:ext-theorem-12-3` a0(112)、`apostol:ext-theorem-12-2` a0(167) | anchors.tsv |
| S50 | L91 | 因此同一证明在任何实欧氏空间成立 | 节点锚 | `d:cauchy-schwarz-proof-inherited-from-theorem-12-3` a1(68) | anchors.tsv |
| S51 | L91 | 用于复空间时得 (x,y)(y,x) ≤ … 与 C-S 等价 | 下移 | `d:complex-cauchy-schwarz-reduces-to-modulus-squared` a1(171) | anchors.tsv |
| S52 | L94 | `(x,y)(y,x) = |(x,y)|²` | 下移 | 同上 a0(64) | anchors.tsv |
| S53 | L97 | EXAMPLE：把 15.8 用到 C(a,b) 上 | 下移 | `d:cauchy-schwarz-for-integrals` a0(166) | anchors.tsv |
| S54 | L100 | 积分形式的 C-S 不等式 | 下移 | 同上 a1(146) | anchors.tsv |
| S55 | L103 | 内积可用来在任何欧氏空间引入长度这一度量概念 | 节点锚 | `apostol:euclidean-space` a1(95) | anchors.tsv |
| S56 | L105 | DEFINITION 欧氏空间中非负数 ‖x‖ 由下式定义 | 节点锚 | `apostol:norm` a0(90) | anchors.tsv |
| S57 | L108 | `‖x‖ = (x,x)^{1/2}` | **无锚** | 节点 `d:norm-definition-as-square-root` / `apostol:norm` 在，锚只有定义句前后半 | 行长 26 < SPEC 30；grep `(x, x) ^ {1 / 2}` 在三表 = 0 |
| S58 | L111 | 称为 x 的范数 | 节点锚 | `apostol:norm` a1(39) | anchors.tsv |
| S59 | L113 | 用范数表述 C-S 时它变成 | 节点锚 | `apostol:norm-notation` a1(77)、`d:cauchy-schwarz-in-norm-form` a0 | anchors.tsv |
| S60 | L116 | `|(x,y)| ≤ ‖x‖‖y‖` | 节点锚 | `apostol:cauchy-schwarz-inequality` a2(32) | anchors.tsv |
| S61 | L119 | 内积可有多种定义，故范数依赖内积的选取 | 节点锚 | `apostol:norm` a2(142) | anchors.tsv |
| S62 | L119 | 这种不唯一是预期之中的 | **未覆盖** | 无 | grep `This lack of uniqueness` 三表 = 0 |
| S63 | L119 | 类比：量同一线段可用不同刻度/单位 | **未覆盖** | 无 | grep `analogous to the fact` / `scale or unit` 三表 = 0 |
| S64 | L119 | 下一定理给出不依赖内积选取的范数基本性质 | 节点锚 | `apostol:theorem-15-9-properties-of-norms` a2(105) | anchors.tsv |
| S65 | L121 | THEOREM 15.9 表头（全称于 x,y,c） | 节点锚 | `apostol:theorem-15-9-properties-of-norms` a0(126) | anchors.tsv |
| S66 | L123 | (a) `‖x‖ = 0 if x = O` | **无锚** | 节点 `d:norm-vanishes-at-the-zero-element` 在，两锚均借用 | 行长 29 < SPEC 30；见第五节抽查① |
| S67 | L125 | (b) 正性 | 下移 | `d:norm-positivity-property` a0(42) | anchors.tsv |
| S68 | L127 | (c) 齐性 | 下移 | `d:norm-homogeneity-property` a0(40) | anchors.tsv |
| S69 | L129 | (d) 三角不等式 | 节点锚 | `apostol:triangle-inequality` a0(57) | anchors.tsv |
| S70 | L131 | (d) 中等号成立的三种充分条件 | 节点锚 | `apostol:triangle-inequality` a1(90) | anchors.tsv |
| S71 | L133 | 证：(a)(b)(c) 由公理立得 | 下移 | `d:norm-properties-abc-follow-at-once-from-the-axioms` a0(114) | anchors.tsv |
| S72 | L133 | 为证 (d)，注意到 | 下移 | 同上 a0 含该分句 | anchors.tsv |
| S73 | L136 | `‖x+y‖² = …` 展开式 | 下移 | `d:triangle-inequality-proof-expand-norm-squared` a0(168) | anchors.tsv |
| S74 | L139 | 交叉项之和为实 | 下移 | `d:cross-term-sum-is-real` a0(42) | anchors.tsv |
| S75 | L139 | C-S 给出两个界，于是 | 下移 | `d:triangle-inequality-proof-bound-cross-terms-by-cauchy-schwarz` a0(170) | anchors.tsv |
| S76 | L142 | `‖x+y‖² ≤ (‖x‖+‖y‖)²` | 下移 | 同上 a1(101) | anchors.tsv |
| S77 | L145 | 这就证明了 (d)；当 y=cx, c>0 时 | 边 ev | `d:triangle-inequality-statement --requires--> …bound-cross-terms…`(56) | edges.tsv |
| S78 | L148 | 等号链 `‖x+y‖ = … = ‖x‖+‖y‖` | 下移 | `d:triangle-inequality-equality-sufficient-conditions` a1(88) | anchors.tsv |
| S79 | L151 | DEFINITION 实欧氏空间中两非零元的夹角 | 节点锚 | `apostol:angle-between-two-elements` a0(195) | anchors.tsv |
| S80 | L154 | `cos θ = (x,y)/(‖x‖‖y‖)` (15.6) | 节点锚 | 同上 a1(57) | anchors.tsv |
| S81 | L157 | Note: C-S 保证商落在 [-1,1]，故 θ 唯一 | 下移 | `d:angle-well-defined-because-quotient-lies-in-minus-one-to-one` a0(115)+a1(86) | anchors.tsv |

表中 81 行是**分句后的条目数**（含 17 个独立公式块）；
速览里的「64 句」按**散文句**计，不含纯公式块。两个数都给出，避免歧义。
按条目算：真未覆盖 4 条（S3/S43/S62/S63）+ 部分未覆盖 1 条（S5）+ 无锚 3 条（S13/S57/S66）。

## 二 未覆盖且值得补建

**新建节点 0 个。** 逐条给出为什么：4 条真未覆盖里，S3/S62/S63 是过渡与教学类比
（见第三节），S43 是**已有节点挂错了空间**而非缺节点（见第五节抽查②）。
3 条无锚（S13/S57/S66）都已有正确的节点与 statement，缺的是锚点，
建节点会造成兄弟冗余（死因 5，与审计 B 判死 28 个 D 层节点的同一判据）。

因此本节的建议动作全部是 **FIX（补锚 / 改边）**，不是新建。三条补锚候选逐字给出，
字符数为我自己实测（Python `len()`，源文件 UTF-8 原样、未转义）：

**FIX-1 → `d:inner-product-homogeneity-axiom` 增锚**
- 锚点逐字：`(3) $c(x,y) = (cx,y)$`
- 实测 **21 字符**，不含换行，是 `15.10.md` L19 整行、逐字连续
- 障碍：低于 SPEC 30 字符下界。**建议进 SPEC**：公理/定义的独立展示式若整行短于 30 字符，
  允许作为锚点例外，理由是拼接邻句会引入 A 型（变量/对象错）与 D 型（覆盖不足）风险
- 死因编号：0（仅需 FIX）；置信度高

**FIX-2 → `d:norm-definition-as-square-root` 与 `apostol:norm` 增锚**
- 锚点逐字：`\| x \| = (x, x) ^ {1 / 2}`
- 实测 **26 字符**（源文件里的 `\|` 是两个字符，未转义），不含换行，`15.10.md` L108 整行
- 同样低于 30 界。这一条比 FIX-1 更要紧：**范数的定义式本身目前全图无锚**，
  而 `apostol:norm` 的 statement 明确断言了 `||x|| = (x, x)^(1/2)`（`statements.tsv` 该行），
  即 statement 有断言、池里无引文 —— 这正是**死因 6（引文不支撑断言）**的形态
- 死因编号：6（对 `apostol:norm` 与 `d:norm-definition-as-square-root` 各一处）；置信度高

**FIX-3 → `d:norm-vanishes-at-the-zero-element` 增锚**
- 锚点逐字：`(a) $\| x\| = 0$ if $x = O$ .`
- 实测 **29 字符**，不含换行，`15.10.md` L123 整行
- 差 1 字符触界。该节点现有两条锚点**都是借用**（见第五节抽查①）
- 死因编号：6；置信度高

三条的共同前提是 SPEC 放开短式子。若 SPEC 不放开，替代方案是允许锚点跨相邻行拼接，
但那会破坏「不跨行」硬禁令 2，我不建议。第三种方案是把式子与紧邻的散文句合成一条锚点
（如 `(a) $\| x\| = 0$ if $x = O$ .` 无可合的邻句，L121/L125 分别属于别的节点），
本节三处都合不出来 —— 这本身就是「30 字符下界与 OCR 逐行切分冲突」的证据。

## 三 未覆盖但不值得建

- **S3**（L3「Now we wish to extend these ideas to more general linear spaces.」，实测 64 字符）——过渡叙述。它宣告推广动作，而推广的**内容**已由 S4 承载在 `d:metric-properties-motivate-the-inner-property` 的 a2(157) 上（正确 id 见逐句表：`d:metric-properties-motivate-the-inner-product`）。建节点即复述 parent，死因 5。**不进 SPEC，不建。**
- **S62**（L119「This lack of uniqueness is to be expected.」）——纯过渡。它只是把 S61（已由 `apostol:norm` a2 覆盖）的结论重述为一句评价，无新数学内容。**不建。**
- **S63**（L119 的刻度类比，实测 168 字符）——教学类比，非数学内容。判定为**不值得建节点**，但**值得作为 `apostol:norm` 的第四条锚点补上**（可选、优先级低）：它是 Apostol 对「范数不唯一」给出的唯一解释性材料，而 `apostol:norm` 的 statement 已断言了范数依赖内积选取。逐字锚点：`It is analogous to the fact that we can assign different numbers to measure the length of a given line segment, depending on the choice of scale or unit of measurement.`，我实测 **168 字符**，在 30–200 界内，`15.10.md` L119 内逐字连续、不跨行。若综合代理认为教学类比不入图，直接丢弃即可，不影响任何断言。
- **S5 的出处子句**（L5「…in $V_n$ was defined in Chapter 12 by the formula」）——纯出处引用。公式本体已由 `d:dot-product-component-sum-formula` a0(58) 覆盖；跨章出处这一类信息本节已有 `apostol:ext-theorem-12-2/12-3`、`apostol:ext-vn-c-section-12-16` 三个 ext- 节点在承载同类内容，但**没有一个对应第 12 章的点积定义本身**。判定：**不建新节点**（会与 `apostol:ext-vn-as-a-prior-vector-space` 语义重叠），但记为一处**轻度不对称**——ext- 机制覆盖了定理 12.2/12.3 与 12.16 节，独漏点积定义的出处。置信度中等，因为我未核 15.06/15.03 的 ext- 节点是否已在别处兜住它。

## 四 锚点集合与 sections 集合的不一致

**两个集合确实对不上，双向都有差集。** 依据：`anchors.tsv` 取 `file == 15.10.md` 的
唯一 node_id 得 **70** 个；`nodes.tsv` 取 `sections` 含 `15.10` 得 **76** 个。
差集用 `comm` 算出如下。

### (甲) 有 15.10.md 锚点，但 sections 不含 15.10 —— 3 个

| 节点 | 它的 sections | 它在 15.10.md 上的锚点 |
|---|---|---|
| `d:15-11-denominator-nonzero-by-positivity` | `15.11` | idx1(35) 正性公理式 |
| `d:parseval-conjugate-comes-from-hermitian-symmetry` | `15.11` | idx1(39) `(x,y)=conj(y,x)` |
| `d:s-perp-closure-follows-from-linearity-in-the-first-argument` | `15.14` | idx1(63) 公理(2)、idx2(49) 公理(1) |

三个都是 `origin=model`、都是「本节点属于后面某节，但引用了 15.10 的公理作为依据」。
判定：**这不是错误，是 sections 字段的语义选择**——它记的是节点**所属**的节，
不是它**引用**的节。但它使「按 sections 统计某节覆盖」系统性漏掉这类跨节引用者。
死因编号：0；建议动作：**保留**，并**进 SPEC** 一条澄清：
`sections` 是归属而非引用范围；要统计一节的证据流出，必须走 `anchors.tsv` 的 file 列。
对照：这 3 个节点的锚点里，公理(1)(2) 与正性式**同时**被本节的 D 层公理节点持有，
是 `shared-quotes.tsv` 里的合法共享（公理被多处引用），非 B 型借用——
例如公理(2) 那条 63 字符引文有 **10 个持有者**（4 节点 + 6 边），全部是「用这条公理」。

### (乙) sections 含 15.10，但在 15.10.md 上无锚点 —— 9 个

| 节点 | 无锚原因 | 判定 |
|---|---|---|
| `L2:proof-inheritance-by-auditing-what-the-old-proof-used` | L2 层按设计无锚点 | 正常 |
| `L2:expand-the-squared-norm-and-classify-the-cross-terms` | 同上 | 正常 |
| `L2:a-basis-locks-the-coordinate-count-not-the-geometry` | 同上 | 正常 |
| `L2:orthogonalization-as-a-dimension-counter` | 同上 | 正常 |
| `L2:counts-survive-the-procedures-that-destroy-the-objects` | 同上 | 正常 |
| `L2:self-orthogonality-forces-zero-the-uniqueness-engine` | 同上 | 正常 |
| `apostol:cx-convergence-must-be-proved-before-the-axioms` | 3 条锚点全在 `15.12-exercises.md` | **可疑** |
| `apostol:cx-l2-sequence-space-is-an-infinite-dimensional-euclidean-space` | 3 条锚点全在 `15.12-exercises.md` | **可疑** |
| `apostol:ext-vn-as-a-prior-vector-space` | 2 条锚点在 `15.03.md` / `15.06.md` | **可疑** |

前 6 个是 L2 层设计使然（README 已列 24 个无锚 L2 节点），不构成问题。
后 3 个的 `sections` 声明含 15.10，但**证据一条都不落在 15.10.md 上**：
两个 `cx-` 节点的锚点全在习题文件，`ext-vn` 的锚点在 15.03/15.06。
判定：**sections 字段过度声明**（over-claim）。
死因编号：0（不是量词过度断言，statement 本身未越界；是元数据不准）。
建议动作：**FIX 元数据**——把这 3 个节点 sections 中的 `15.10` 改为由边指向本节的节点，
或在 SPEC 里明确 sections 允许记「概念所涉之节」而非「证据所在之节」，二者择一，
但**必须择一**，否则任何按 sections 做的覆盖统计都不可比。置信度：
「不一致存在」高（comm 直接算出）；「该改哪一侧」低——这是 SPEC 未定的口径问题，
应由综合代理裁决，我不单独判。

**给综合代理的直接提示**：任何声称「15.10 有 N 个节点」的表述都必须说明 N 取自哪一侧
（锚点侧 70 / sections 侧 76），二者相差 6，且差异不是随机噪声而是上述两类系统性原因。

## 五 本节现有锚点抽查

抽查 3 条，逐型（A/B/C/D/E）过。选样理由：一条是我在逐句表里标为「无锚」的
（检验我的判断是否成立），一条锚点数最多的复合节点，一条 `origin=model` 的推论节点。

### 抽查① `d:norm-vanishes-at-the-zero-element`（`data/nodes-D3.jsonl:30`）
statement 断言：定理 15.9(a) `||x|| = 0 if x = O`；并指出 Apostol 只给了单向蕴含，
逆向是 (b) 的逆否；证明是把 y=O 代入 (O,y)=0，故只依赖齐性公理、不依赖正性。
两条锚点：a0 = `Proof. Properties (a), (b) and (c) follow at once from the axioms for an inner product. To prove (d), we note that`(114)；a1 = `THEOREM 15.9. In a Euclidean space, every norm has the following properties…`(126)。

- **A 型（变量/对象错）**：未见。statement 里的 x、O 与源文一致。
- **B 型（借用引文）**：**命中，且两条锚点都是借的**。`shared-quotes.tsv`：a1 那条 126 字符引文有 **5 个持有者**（`apostol:theorem-15-9-properties-of-norms`、`d:theorem-15-9-holds-for-every-inner-product`、本节点，另 2 条边）；a0 那条 114 字符引文有 **7 个持有者**（含 `d:norm-properties-abc-follow-at-once-from-the-axioms` 与 5 条边）。两条引文都不是为本节点写的。
- **C 型（把端点当中间步骤）**：未见。
- **D 型（覆盖不足）**：**命中，且是主症**。statement 的核心断言是性质 (a) 本身与「只给单向」这一观察，而 (a) 的文本 `(a) $\| x\| = 0$ if $x = O$ .` **不在任何一条锚点里**（第二节 FIX-3 已实测该行 29 字符）。a1 只说「有下列性质」、a0 只说「(a)(b)(c) 由公理立得」，两条**合起来也推不出 (a) 说的是什么**。
- **E 型（整句完全无锚）**：**命中**，即 (a) 这一整句无锚。

结论：statement 数学正确（我独立核过：`(O,O) = 0` 由 c=0 代入齐性得，
取根得 `||O|| = 0`，且确实不需正性），但**证据池里没有支撑它的引文**。
依据：`anchors.tsv` 本节点 2 行、`shared-quotes.tsv` 对应 2 行、`exposure.tsv` 显示
T1=2 / T2=0 / T3=0 / 池总 2 / 去重源行 2（即两条锚点分属两行，无第三行进池）。
死因编号：**6**（引文不支撑断言）。建议动作：**FIX（补锚 FIX-3），不 KILL**。
置信度：高。不确定点：是否 KILL 取决于 SPEC 对短式子的裁决——若 SPEC 坚持 30 字符下界
且不许拼接，本节点将永远无法自证，那时该考虑把它并入
`d:norm-properties-abc-follow-at-once-from-the-axioms`；但那是层级决定，超出我的范围。

### 抽查② `d:exponential-weight-makes-improper-integral-converge`（`data/nodes-D3.jsonl:22`）
statement 断言：例 5 在**全体实多项式**空间上定义 `(f,g) = ∫₀^∞ e^{-t} f g dt`；
指数因子使反常积分对任意多项式收敛；权在此做双重工作（正性给公理(4)、衰减给良定义性）。
两条锚点：a0 = 公式(56)；a1 = `Because of the exponential factor, this improper integral converges for every choice of polynomials f and g.`(108)。

- **A 型**：**命中，但在边上而非锚点上**。`data/edges-D3.jsonl:171` 写
  `d:exponential-weight-makes-improper-integral-converge --applies-to--> apostol:polynomials-of-degree-at-most-n`，
  而 `apostol:polynomials-of-degree-at-most-n` 的 statement 是「例 7：全体次数 ≤ n 的多项式，n 固定」
  （`statements.tsv` 该行，`sections=15.03`）。**例 5 用的是全体实多项式、无次数上界**
  （源文 L75 逐字：`EXAMPLE 5. In the linear space of all real polynomials, define`）。
  这条边把对象换成了一个更小的空间——正是 A 型（对象错）。
  图里存在正确的目标：`d:example-6-all-polynomials-as-a-function-space`
  （`sections=15.03`，parent=`apostol:function-space`，statement 为「全体多项式，任意次数」）。
- **B 型**：a1 那条 108 字符引文有 3 个持有者（本节点 + 2 条边 `edges-D3.jsonl:21` 与 `:171`），
  但持有者都是本节点自身及其发出的边，属自持，**不是借用**。a0 未进 `shared-quotes.tsv`，独有。
- **C 型**：未见。
- **D 型**：轻度——S43（例 5 的题干句，62 字符）无锚，故「这个内积定义在全体实多项式上」
  这一断言的**空间部分**在池里无引文，只有公式与收敛性有引文。这与 A 型同源。
- **E 型**：S43 整句无锚，命中（已在逐句表记为未覆盖）。

结论：**边 `data/edges-D3.jsonl:171` 的 dst 指错空间**。
依据：`edges.tsv` 该行 + `statements.tsv` 两个多项式空间节点 + 源文 `15.10.md:75`。
死因编号：**1 的边版本 / 实质为 A 型对象错**（statement 本身没错，错在边；
若按死因表取最近者，这是**数学错误 4 的轻度形态**——把「全体多项式」缩小为「次数 ≤ n」，
使 applies-to 的适用范围失真）。建议动作：**FIX**——
`dst` 改为 `d:example-6-all-polynomials-as-a-function-space`，
并补 S43 为本节点第三条锚点（`EXAMPLE 5. In the linear space of all real polynomials, define`，
我实测 **62 字符**，在 30–200 界内，逐字连续不跨行）。置信度：高
（两个空间的 statement 我逐字读过，次数上界的差别不容混淆）。
不确定点：`apostol:polynomials-of-degree-at-most-n` 是否被有意当作「多项式空间」的通用桩，
若是，则全图还有同类边——我未扫，未核。

### 抽查③ `d:additivity-in-first-argument-derived`（`data/nodes-D3.jsonl:8`，`origin=model`）
statement 断言：`(x+y,z) = (x,z)+(y,z)` **不是** Apostol 的公理，而是由对称性(1)
换到第二格、用(2)、再换回来得到；并指出这正是对称性公理在证明中承重的原因，
举了三角不等式证明开头的展开与定理 15.10 中「与 x₁ 配对」两处为例。
两条锚点：a0 = 公理(2) 那条 63 字符引文；a1 = 三角不等式展开式那条 168 字符 array。

- **A 型**：未见。变量与源文一致。
- **B 型**：**命中（a0）**。公理(2) 的 63 字符引文在 `shared-quotes.tsv` 里有 **10 个持有者**
  （`apostol:inner-product-axioms`、`d:inner-product-additivity-axiom`、
  `d:s-perp-closure-follows-from-linearity-in-the-first-argument`、本节点，另 6 条边）。
  本节点是这条引文的第 4 个节点持有者。但判读后认为**合法**：statement 明确说
  「(2) 只许拆第二格」，引用(2) 正是为了指出它**不够用**——引文支撑的是断言的前提部分。
  这与「丢弃与未覆盖清单」记录的那处 X 层 C 型误判不同，那里引文给的是终点。
- **C 型（把端点当中间步骤）**：**需要注意，但判为不命中**。a1 是三角不等式证明的**展开式**，
  statement 用它当「首格拆分实际被用到」的证据。展开式里确有 `(x+y, x+y)` 拆成四项——
  这个拆分**同时**用了首格与次格加性，所以引文确实含有本节点所述的那一步，
  不是「只给了结论」。判为不命中，但置信度中等：严格说来 array 里看不到「先用对称性换格」
  这个中间动作，它是模型补出来的推理（这也正是 `origin=model` 的正当用法）。
- **D 型**：statement 提到的第二个例证——定理 15.10 中「pair the vanishing combination with x₁」
  ——**在池里没有引文**。`exposure.tsv`：本节点 T1=2 / T2=0 / T3=0 / 池总 2 / 去重源行 2，
  两条锚点都在 15.10.md，**15.10.md 之外一条都没有**，而定理 15.10 在 `15.11.md`。
  即 statement 引用了一处它无证据的地方。判为 **D 型命中（轻度）**。
- **E 型**：未见整句无锚。

结论：节点数学正确（我独立核过：由(1)(2) 推首格加性的三步换格无误），
锚点整体成立，但 statement 里对定理 15.10 的那一处引证**在池内无支撑**。
依据：`exposure.tsv` 本节点行（T2=T3=0）、`anchors.tsv` 本节点 2 行均为 15.10.md。
死因编号：**6（轻度，仅限 statement 的第二个例证）**。
建议动作：**FIX**——二选一：删掉 statement 里对定理 15.10 的引证，
或补一条 `15.11.md` 的锚点把它兜住。我倾向后者，因为该引证是这个节点存在价值的一半
（它证明首格加性不止在一处承重）。置信度：中等偏高。
不确定点：我未读 `15.11.md`（超出本轮范围），所以**未核**「pair the vanishing combination
with x₁」是否为该文件里的逐字表述——若不是逐字，补锚需要另选引文。

### 抽查小结
3 条里 **3 条都有 B 型或 D 型命中**，其中抽查①的 D 型是主症（整句无锚），
抽查②发现一条边的对象错，抽查③是 statement 越出证据池。
这与 SPEC 已知的判断一致：**「N/N 锚点已验证」只证明引文未被篡改**。
本节 123 条锚点（`anchors.tsv` 中 file=15.10.md 的行数）全部通过机器逐字校验，
但抽查 3 条即命中 3 处失配，**外推需谨慎**——3 条不是随机样本
（我有意挑了 1 条我预判有问题的、1 条锚点最多的、1 条 model 来源的），
所以**不能**由此推出全节失配率。这个抽样偏差必须写明，不得被综合代理当作比例证据。

## 顺带发现

- SPEC 的 30 字符锚点下界与 OCR 逐行切分冲突：本节三个最要紧的式子（公理(3) 21 字符、范数定义式 26 字符、性质(a) 29 字符）全部触界而无法建锚，这是结构性问题、不止本节。
- `sections` 字段口径未定：既被用作「归属」（3 个 15.11/15.14 节点引用本节公理却不列 15.10）又被用作「概念所涉」（3 个节点列了 15.10 但证据全在别的文件），两种用法并存使按 sections 的统计不可比。
- `data/edges-D3.jsonl:171` 的 dst 指向 `apostol:polynomials-of-degree-at-most-n`，而例 5 用的是全体实多项式；`d:example-6-all-polynomials-as-a-function-space` 才是正确目标。
- `apostol:norm` 的 statement 断言了 `||x|| = (x,x)^(1/2)`，但该式全图无锚——statement 有断言、池内无引文，是死因 6 的干净样本，可作 SPEC 的示例。
- 本节 123 条锚点 100% 过机器校验，抽查 3 条命中 3 处失配；样本非随机（有意选样），不得当作比例证据外推。
- `report/丢弃与未覆盖清单.md` 未记录本节任何缺口，本报告 5 处均为新发现，不与旧清单重复。
- 未核项：`15.11.md` 中「pair the vanishing combination with x₁」是否逐字存在（抽查③补锚需要它）；`apostol:polynomials-of-degree-at-most-n` 是否在全图别处也被当作通用多项式空间桩使用。
