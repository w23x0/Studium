# 15.08 覆盖完备性

## 〇 结论速览（本节 37 句 / 已覆盖 34 / 下移 0 / 未覆盖 3；其中值得补建 0 个）

切句口径：`source/apostol-ch15/15.08.md` 共 38 行，其中 19 行非空（含 1 行节标题、2 行 `$$` 界定符）。
按**句**切得 37 句（S1–S37）。`DEFINITION.` / `THEOREM 15.6.` / `Proof.` / `EXAMPLE n.` 这类标签不单独计句，
并入其后第一句；第 31–33 行的行间公式块（`$$` 界定符 + 公式本体）只把公式本体计为 1 句（S34），
两个 `$$` 不计。**没有按行号做覆盖比对**：source-lines.tsv 显示本节 19 个非空行里 15 行的
n_sentence_end > 1（第 7 行 8 处、第 35 行 3 处、第 3/9/13/15 行各 4 处），按行号必然高报。

- 已覆盖 34 句。其中 **3 句只是"锚点截断式覆盖"**（S18、S35、S37）：句子的主干在锚点里，
  但各缺一个头部或尾部分句，语义已在图里，算已覆盖并附 FIX（S37 判为良性，不提 FIX）。
- 下移到 part-of 后代 0 句。本节 9 个 L1 父节点的后代（partof.tsv）全部 sections 也含 15.08，
  即父子同在本节，不存在「内容合法地下移到别节后代」的情形，**T3 通道在本节为空**。
- 未覆盖 3 句：S19（例 2 的张成一半）、S22（例 3 的张成一半）、S33（公式 (15.4) 本体）。
- 值得补建节点 **0 个**。三句全是「已有节点的断言已含该内容、只是锚点没圈进去」，
  死因 0，建议动作是给 3 个既有节点各加/换一条锚点（FIX），不是建新节点。
  若照句面建节点，会造出 parent 复述型节点，正撞审计 B 的死因 5。
- 本节没有找到「教材里有、图谱里没有」的**新增内容缺口**。`report/丢弃与未覆盖清单.md`
  未记录 15.08 的任何丢弃项，本报告也不新增丢弃项，只新增 3 条 FIX 与 1 处集合不一致。

## 一 逐句覆盖表（句号 / 句子摘要 / 覆盖状态 / 覆盖者 id 或 file:line / 判据）

覆盖状态记号：**A** = 节点锚点已覆盖；**E** = 仅边 evidence 覆盖；**A/E** = 两者皆有；
**部分** = 锚点截断，头部分句未圈进任何一条锚点；**无** = 未覆盖。
判据列写的是 anchors.tsv / edges.tsv 里那条证据的**实测字符数**。

| 句 | 行 | 摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 3 | 有限基定义：有限集 S 独立且张成 V | A/E | `apostol:finite-basis`(122)、`x:basis`(122)、`d:finite-basis-independence-clause`(110)、`-spanning-clause`(62)、`-finiteness-clause`(77) | anchors.tsv 5 条同句锚点，切分覆盖三个从句 |
| S2 | 3 | V 有有限基或 V={O} 则称有限维 | A/E | `apostol:finite-dimensional-space`#0(95)、`d:finite-dimensional-defined-by-existence-of-a-finite-basis`(95) | 逐字整句 |
| S3 | 3 | 否则称无限维 | A | `apostol:infinite-dimensional-space`#0(43)、`d:zero-space-declared-finite-dimensional-by-fiat`(72，跨 S2 尾+S3) | 43 字符恰为整句 |
| S4 | 5 | THEOREM 15.6：有限维空间的任两有限基元素个数相同 | A/E | `x:all-bases-are-equinumerous`(124)、`d:thm-15-6-hypothesis-...`(110)、`apostol:theorem-15-6`#0(64) | 124 含 `THEOREM 15.6.` 标签在内的整句 |
| S5 | 7 | 设 S、T 为 V 的两个有限基 | A/E | `apostol:theorem-15-6`#1(101，含 S5+S6)；edge `apostol:theorem-15-6 requires apostol:finite-basis` ev(38) | 38 字符恰为 S5 整句 |
| S6 | 7 | 设 S 有 k 个、T 有 m 个元素 | A | `apostol:theorem-15-6`#1(101) | 101 字符跨 S5+S6，`Suppose S consists of k elements and T consists of m elements.` 在其内 |
| S7 | 7 | S 独立且张成，故由 Thm 15.5 知 k+1 个元素必相依 | A/E | `d:thm-15-6-apply-theorem-15-5-to-the-first-basis`(111) | 逐字整句；同引文另有 4 条边 |
| S8 | 7 | 故多于 k 个元素的集合必相依 | A/E | `d:thm-15-6-upgrade-from-k-plus-one-to-more-than-k`(63) | 逐字整句 |
| S9 | 7 | T 独立，故 m ≤ k | A/E | `d:thm-15-6-first-inequality-m-at-most-k`(56) | 逐字整句 |
| S10 | 7 | 交换 S、T 同法得 k ≤ m | A/E | `d:thm-15-6-symmetry-step-requires-t-to-span`(67) | 逐字整句（该锚点的支撑力另见第五节抽查①） |
| S11 | 7 | 故 k = m | A/E | `d:thm-15-6-conclusion-by-antisymmetry`(84，含 S10+S11) | `Therefore k = m.` 在 84 字符内 |
| S12 | 9 | 维数定义：基有 n 个元素则 dim V = n | A/E | `apostol:dimension`#0(102)、`x:dimension-versus-rank`(102)、`d:dimension-defined-as-the-element-count-of-a-basis`(90) | 逐字 |
| S13 | 9 | 记作 n = dim V | A/E | `apostol:dim-notation`#0(67) | 67 字符含 `We write $n = \dim V$ .` |
| S14 | 9 | V={O} 时 dim V = 0 | A/E | `apostol:dimension`#1(42)、`d:dimension-of-the-zero-space-is-zero-by-convention`(42) | 42 字符恰为整句 |
| S15 | 11 | 例 1：V_n 的维数为 n | A/E | `apostol:dimension`#2(101)、`d:dim-of-vn-is-n-via-unit-coordinate-vectors`(101)、`apostol:ext-uncited-unit-coordinate-basis-of-vn`(101) | 101 字符跨 S15+S16 |
| S16 | 11 | 一组基是 n 个单位坐标向量 | A/E | 同上 101 字符锚点 | `One basis is the set of $n$ unit coordinate vectors.` 在其内 |
| S17 | 13 | 例 2：次数 ≤ n 的多项式空间维数为 n+1 | A/E | `d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one`(122) | 122 字符含整句 |
| S18 | 13 | 一组基是 n+1 个多项式 {1,t,…,tⁿ} | 部分 | 同上 122 字符锚点，止于 `polynomials` | 集合列举本体 `$\{1, t, t^2, \ldots, t^n\}$` 未进任何锚点；见第二节 P1 |
| S19 | 13 | 每个次数 ≤ n 的多项式是这 n+1 个的线性组合 | **无** | — | 全表 grep `Every polynomial of degree` 在 anchors.tsv 与 edges.tsv 均 0 命中 |
| S20 | 15 | 例 3：y''−2y'−3y=0 的解空间维数为 2 | A/E | `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space`#0(100)；edge ev(89) | 逐字 |
| S21 | 15 | 一组基是 u₁=e⁻ˣ、u₂=e³ˣ | A/E | `d:dim-of-a-second-order-de-solution-space-is-two`(79)、同 ext 节点#1(79) | 逐字整句 |
| S22 | 15 | 每个解都是这两个的线性组合 | **无** | — | grep `Every solution is a linear` 在两表均 0 命中；但该内容在 ext 节点 statement 里有（见第三节） |
| S23 | 17 | 例 4：全体多项式空间是无限维的 | A/E | `apostol:infinite-dimensional-space`#1(71) | 逐字整句 |
| S24 | 17 | 虽然 {1,t,t²,…} 张成它，但无有限集张成 | A/E | `d:infinite-dimensional-operational-criterion-no-finite-spanning-set`(115)、`apostol:infinite-dimensional-space`#2(63) | 逐字整句 |
| S25 | 19 | THEOREM 15.7：dim V = n 的有限维空间有以下性质 | A/E | `apostol:theorem-15-7`#2(106)、`d:thm-15-7-hypothesis-dim-v-equals-n`(92)、`apostol:dim-notation`#1(64) | 逐字 |
| S26 | 21 | (a) 独立集是某个基的子集 | A/E | `apostol:theorem-15-7`#0(78)、`d:thm-15-7a-independent-sets-extend-to-bases`(78) | 逐字整句 |
| S27 | 23 | (b) n 个独立元素构成基 | A/E | `apostol:theorem-15-7`#1(55)、`d:thm-15-7b-...`(55)、`x:right-count-upgrades-...`(55) | 逐字整句 |
| S28 | 25 | (a) 的证明同 Thm 12.10(b) | A/E | `apostol:ext-theorem-12-10-part-b`(67)、`d:thm-15-7-proofs-delegated-to-theorem-12-10`(135) | 逐字整句 |
| S29 | 25 | (b) 的证明同 Thm 12.10(c) | A/E | `apostol:ext-theorem-12-10-part-c`(67)、同上 135 字符锚点 | 逐字整句 |
| S30 | 27 | 取维数 n 的空间与按给定次序排列的基 | A/E | `apostol:ordered-basis`#0(125)、`d:ordered-basis-adds-an-ordering-to-a-basis`(150) | 逐字 |
| S31 | 27 | 把这样的有序基记作 n 元组 (e₁,…,eₙ) | A/E | `apostol:ordered-basis`#1(69)、同上 150 字符锚点 | 逐字 |
| S32 | 29 | 若 x ∈ V，可把 x 写成基元素的线性组合 | A/E | `d:basis-expansion-of-an-element-exists-by-spanning`(82)、`apostol:components-relative-to-ordered-basis`#1(82) | 逐字整句（末尾冒号） |
| S33 | 32 | 公式 (15.4)：x = Σᵢ₌₁ⁿ cᵢeᵢ | **无** | — | grep `tag{15.4}` 在 anchors.tsv 与 edges.tsv 均 0 命中；见第二节 P3 |
| S34 | 35 | 这些系数确定一个由 x 唯一决定的 n 元组 | A/E | `apostol:uniqueness-of-components`#0(122)、`d:uniqueness-of-components-conclusion-tuple-determined-by-x`(122)、`x:unique-coordinates-in-a-basis`(122) | 逐字整句 |
| S35 | 35 | 若另有表示 x=Σdᵢeᵢ，相减得 Σ(cᵢ−dᵢ)eᵢ=O | 部分 | `d:uniqueness-of-components-subtract-to-get-a-vanishing-relation`(84) | 84 字符只从 `then by subtraction` 起，「另有一个表示」这一前提假设未进锚点；见第二节 P4 |
| S36 | 35 | 由基元素独立得 cᵢ=dᵢ，故两元组相等 | A/E | `d:uniqueness-of-components-apply-independence`(83)、`apostol:uniqueness-of-components`#1(83)；edge ev(141) 含尾句 | 83 字符覆盖前半，141 字符边证据覆盖 `so we have $(c_1,\ldots)=(d_1,\ldots)$ .` |
| S37 | 37 | 由 (15.4) 定的 n 元组分量称为 x 相对有序基的分量 | 部分 | `apostol:components-relative-to-ordered-basis`#0(90)、`d:components-depend-on-the-ordering-not-only-on-the-basis`(85) | 两条锚点合起来覆盖 `determined by Equation (15.4) … ordered basis $(e_1,\ldots,e_n)$ .`；句首名词短语 `The components of the ordered n-tuple $(c_{1},\ldots,c_{n})$` 未进锚点，属良性截断，不提 FIX |
补记：`DEFINITION.` / `THEOREM 15.6.` / `Proof.` / `EXAMPLE 1.–4.` 共 7 个标签按第〇节口径
并入其后首句，均已被相应锚点圈在内（`x:basis`(122)、`x:all-bases-are-equinumerous`(124)、
`d:dim-of-vn-is-n-via-unit-coordinate-vectors`(101) 等），不单列。

小计：A/E 34 句（含 S18/S35/S37 三处截断式覆盖）；无 3 句（S19、S22、S33）。
注：S18 计入「已覆盖但截断」，故第〇节的「2 句截断」指语义完全落地的 S35/S37；
S18 的截断落在集合列举本体上，与 S19 同因，合并为第二节 P1 一条 FIX。

## 二 未覆盖且值得补建（建议 id / node_type / statement / 锚点逐字 + 实测字符数）

**建议新建节点数 0**。三句未覆盖句的内容都已在既有节点的 statement 里，缺的只是锚点，
所以本节给出的是 **4 条锚点级 FIX**（含 S35 的截断修正），不是节点提案。理由与逐条如下。

判断依据：对每句问「这句是新的数学内容，还是既有节点断言的一部分」。
S19/S22 都是「例子里基的**张成**那一半」，S33 是公式记号本体。三者都落在既有节点的
statement 覆盖范围内（下面逐条给出 statements.tsv 原文片段），单独建节点会得到
parent 复述型节点——正是审计 B 判死 28 个 D 层节点时用的死因 5 的形状。

### P1 —— 给 `d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one` 换锚点（覆盖 S18 + S19）
- 现锚点（anchors.tsv，122 字符）止于 `One basis is the set of $n + 1$ polynomials`，
  基的集合列举本体和 S19 整句都在锚点外。
- 该节点 statement 原文：「Example 2 counts the basis of powers from the constant through
  the n-th power, giving n+1 elements.」——「数 n+1 个」这个断言正需要集合列举本体作证，
  现锚点却把它切掉了，属盲区 **D 型（覆盖范围不足）**。
- 建议新锚点（逐字连续、不跨行，**实测 164 字符**，超 SPEC 上界 200 之下、下界 30 之上）：

  `One basis is the set of $n + 1$ polynomials $\{1, t, t^2, \ldots, t^n\}$ . Every polynomial of degree $\leq n$ is a linear combination of these $n + 1$ polynomials.`

- 死因 0（仅 FIX）/ 建议动作 **FIX**（换锚点，或在原 122 字符锚点旁**增列**这条，两者皆可；
  倾向增列，因为原锚点是 3 个持有者共享的引文，shared-quotes.tsv 记 n_holders=3，改动会牵连边）。
- 置信度高。不确定点：SPEC 的 30–200 界我按 README 说明理解为对单条锚点，164 合规；
  若 SPEC 另有「锚点不得跨句」的规定，则需拆成 74 + 89 两条（两值我都实测过，见第三节）。

### P2 —— 给 `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 增锚点（覆盖 S22）
- 该节点 statement 已逐字包含这层意思：「…and that **every solution is a combination of
  these two**. Chapter 15 proves none of this…」，但它的两条锚点（100 / 79 字符）都没圈到
  S22 那句。这正是该节点的核心指控（Apostol 未证张成那一半）所依据的句子却无锚，属 **D 型**。
- 建议新锚点（逐字连续、不跨行，**实测 52 字符**）：

  `Every solution is a linear combination of these two.`

- 死因 0 / 建议动作 **FIX**（增列第 3 条锚点）。置信度高。
- 不确定点：52 字符虽在 30–200 界内，但句子短且无变量，单看它认不出是哪个方程；
  若主控偏好自足锚点，可改用 **实测 132 字符** 的
  `One basis consists of the two functions $u_1(x) = e^{-x}$ , $u_2(x) = e^{3x}$ . Every solution is a linear combination of these two.`
  （已 grep 实测为源文件逐字子串、不跨行），代价是与现 79 字符锚点重叠。

### P3 —— 给 `d:basis-expansion-of-an-element-exists-by-spanning` 增锚点（覆盖 S33）
- 公式 (15.4) 本体在全图无任何锚点或边证据（grep `tag{15.4}` 两表均 0）。而 S34 的锚点
  （122 字符）以 `The coefficients in this equation` 开头——**「this equation」指的就是这条
  从未被锚定的公式**。锚点链在这里断了一环，属 **C 型（把端点当中间步骤）的镜像**：
  结论句有锚，被它指代的对象没有。
- 建议新锚点（源文件第 32 行整行，逐字、不跨行，**实测 50 字符**）：

  `x = \sum_ {i = 1} ^ {n} c _ {i} e _ {i}.\tag{15.4}`

- 死因 0 / 建议动作 **FIX**（增列第 2 条锚点）。置信度中高。
- 不确定点：这条 quote 含 OCR 保留的 `\tag{15.4}` 与不规范空格（`\sum_ {i = 1}`），
  逐字性没问题（已实测），但它是否算「有意义的数学引文」由主控定；若判为纯记号不予收录，
  则 S33 应移入第三节，本节 FIX 数降为 3。

### P4 —— 给 `d:uniqueness-of-components-subtract-to-get-a-vanishing-relation` 换锚点（补 S35 前提）
- 现锚点 84 字符从 `then by subtraction from (15.4)` 起，而该节点 statement 说的是
  「Subtracting **the two expansions** yields…」——「两个展开」里的第二个（`x=\sum d_i e_i`）
  从未进锚点。断言里的一个必要前提无引文支撑，**D 型**。
- 建议新锚点（逐字连续、不跨行，**实测 131 字符**）：

  `In fact, if we have another representation of x as a linear combination of $e_{1},\ldots,e_{n}$ , say $x=\sum_{i=1}^{n}d_{i}e_{i}$ `

- 死因 0 / 建议动作 **FIX**（增列，保留原 84 字符锚点——它是「相减」这一步的正确引文）。
- 置信度高。不确定点：该 quote 末尾含一个尾随空格（源文件 `$ , then by` 处的空格），
  实测 131 含该空格；若 SPEC 要求 quote 去尾空格，则应取前 130 字符，我未另行实测该变体。

## 三 未覆盖但不值得建（一行一条 + 理由：过渡叙述 / 重复 / 纯记号）

这里列的是「不值得**建节点**」的句子，与第二节的锚点 FIX 不冲突——同一句可以既不值得建节点、
又值得补一条锚点。

- **S19**（例 2 每个多项式是这 n+1 个的线性组合）：重复。`d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one` 已断言「基有 n+1 个元素」，而「张成」是基定义的一半（`d:finite-basis-spanning-clause` 已单列）。建节点 = parent + 定义的机械合成，死因 5。**只需 P1 锚点**。
- **S22**（例 3 每个解是这两个的线性组合）：重复。同上；且 `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space` 已把「Apostol 没证张成那一半」立成显式指控，比再建一个复述句节点信息量更大。**只需 P2 锚点**。
- **S33**（公式 (15.4) 本体 `x = Σᵢ₌₁ⁿ cᵢeᵢ`）：纯记号。它是 S32 那句话的符号化写法，S32 已有 82 字符锚点与 `apostol:finite-linear-combination` 的 requires 边。**只需 P3 锚点**。
- **S37 句首名词短语**（`The components of the ordered n-tuple $(c_1,\ldots,c_n)$`）：纯记号 + 良性截断。两条既有锚点（90 / 85）已把该句的定义主干与被定义术语都圈进去了，句首只是重述 S34 已锚定的 n 元组。不提 FIX。
- **7 个结构标签**（`DEFINITION.` ×2、`THEOREM 15.6.`、`THEOREM 15.7.`、`Proof.` ×2、`EXAMPLE 1.–4.`）：过渡叙述／排版标签。已被相应锚点连带圈入，无需独立处理。
- **S25 的 `Then we have the following:`**：过渡叙述。作为 S25 的一部分已进 106 / 92 字符锚点。

本节**没有**判「值得建节点但本轮放弃」的项，故不向 `report/丢弃与未覆盖清单.md` 追加行。

## 四 锚点集合与 sections 集合的不一致（若有）

**两个集合确实对不上，明确报出。**

- 集合 A = nodes.tsv 中 `sections` 含 `15.08` 的节点：**65 个**。
- 集合 B = anchors.tsv 中 `file` 为 `15.08.md` 的锚点所属节点：**48 个**。
- B ⊆ A（`comm -13` 为空）：**没有**「有 15.08.md 锚点却不声明 15.08」的节点。方向单侧。
- A \ B = **17 个节点声明属于 15.08，却在 15.08.md 里没有任何一条锚点**。

17 个按性质分三类：

**（一）L2 结构节点 9 个 —— 设计如此，不算异常**
`L2:a-basis-locks-the-coordinate-count-not-the-geometry`、
`L2:count-saturation-upgrades-a-necessary-condition-to-a-sufficient-one`、
`L2:counts-survive-the-procedures-that-destroy-the-objects`、
`L2:dimension-as-a-two-sided-count-barrier`、
`L2:one-independent-constraint-costs-exactly-one-dimension`、
`L2:orthogonalizing-first-decouples-the-coefficients`、
`L2:proof-inheritance-by-auditing-what-the-old-proof-used`、
`L2:testing-infinite-objects-through-finite-windows`、
`L2:uniqueness-by-forcing-the-difference-to-zero`。
依据：README 已声明 24 个 L2 节点全部无锚点，靠 `members` 挂载。判定**保留**，不是缺口。

**（二）无任何锚点的 model 节点 3 个 —— 与 README 的 28 个无锚点清单一致**
`d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets`、
`d:thm-15-7b-argument-adjoining-an-outside-element-breaks-the-count`、
`d:adjoining-an-element-outside-the-span-preserves-independence`。
三者 `n_anchors=0`、`origin=model`。判定：不是 15.08 的**覆盖**缺口（教材相应内容另有节点），
而是这三个节点自身的证据缺口，超出本轮范围，只登记不展开。

**（三）声明 15.08 但锚点全在别的文件 —— 5 个，这一类才是真正的记账不一致**

| 节点 | sections | 唯一/全部锚点所在 | 判定 |
|---|---|---|---|
| `apostol:spanning-set` | 15.06,15.08 | 3 条全在 15.06.md | 15.08 里张成概念只被引用不被定义，声明 15.08 属**过宽**；但 15.08.md 确实反复用它。倾向**保留**，置信度中。 |
| `d:dimension-theory-uses-no-metric-notions` | 15.07,15.08 | 1 条在 15.07.md（107 字符） | 断言横跨两节而引文只在 15.07。**FIX**：或删 15.08、或补一条 15.08 锚点。 |
| `d:finite-basis-two-clauses-are-logically-independent` | 15.06,15.08 | 1 条在 15.06.md（51 字符，是 `$\{i,j\}$ , $\{i,j,i+j\}$ , $\{O,i,-i,j,-j,i+j\}$ .`） | **最可疑的一条**。节点断言「有限基定义的两个从句逻辑上互相独立」，而定义句在 15.08.md 第 3 行；它却拿 15.06 一串**张成集例子**当唯一引文。这串例子说明的是「张成集不唯一」，与「两从句逻辑独立」没有支撑关系。判**死因 6（引文不支撑断言）**、盲区 **B 型 + D 型**。建议动作 **FIX**：把锚点换成 15.08.md 的定义句（`d:finite-basis-independence-clause` 已在用的 110 字符引文），置信度中高；不确定点是我没读该节点在 15.06 语境下的完整提案说明。 |
| `d:finite-dimensionality-supplies-a-finite-basis-to-orthogonalize` | 15.08,15.13 | 1 条在 15.13.md（129 字符） | 该节点讲的是 15.13 正交化对有限维的依赖，声明 15.08 是**向上引用**。倾向**保留**，置信度中。 |
| `d:n-plus-one-elements-in-an-n-dimensional-space-are-dependent` | 15.07,15.08 | 1 条在 15.07.md（58 字符，`Then every set of $k + 1$ elements in $L(S)$ is dependent.`） | 15.07 的引文说的是 `L(S)` 里的 k+1 个元素，节点标题说的是 **n 维空间**里的 n+1 个。从 `L(S)` 到「n 维空间」这一步正是 15.08 Thm 15.6 之后才成立的改写，引文停在改写之前。判 **A 型（对象错：L(S) vs n 维 V）叠 D 型**，`origin=model` 已标注故不构成死因 3。建议动作 **FIX**：补一条 15.08.md 锚点或改标题回 `L(S)`，置信度中。 |

**这一不一致的机器可检性**：A \ B 只需两次 `awk` + `comm` 即可复现（本报告用的就是这个），
建议进 SPEC 作为常规校验项——`check_graph` 现在不做这项比对，所以上面 5 条从未被任何工具报出。
建议动作：**进 SPEC**（新增一条「sections 声明的每个小节，节点应至少有一条该小节的锚点，
L2 层与显式声明为向上/向下引用者豁免」）。置信度中：豁免规则怎么写我没有定论，
第（三）类里 3 个我判「保留」的正是需要豁免的形状。

## 五 本节现有锚点抽查（挑 3 条，按盲区五型 A/B/C/D/E 逐型检查，写出结论）

挑选原则：一条证明步骤节点、一条例子节点、一条模型推断节点，覆盖三种不同的失配风险形状。
每条都先读 statements.tsv 的断言全文，再逐型问「这条引文能不能证这句话」。

### 抽查① `d:thm-15-6-symmetry-step-requires-t-to-span`（证明步骤）
锚点（67 字符）：`The same argument with S and T interchanged shows that $k \leq m$ .`
statement：「Interchanging S and T gives k at most m, but this reuse is legitimate only
because **T also spans V**, so that Theorem 15.5 can be applied with T in the role of S.
The phrase the same argument hides the second, and only, use of T being a spanning set.」

- **A 型（变量/对象错）**：无。S、T、k、m 的角色与源文一致。
- **B 型（借用引文）**：**是，但合法**。shared-quotes.tsv 记该 67 字符引文 n_holders=2，
  另一持有者是 `edges-D2.jsonl:171`（即本节点自己发出的边）。同一节点的锚点与出边共用引文，
  不是借用他人。
- **C 型（把端点当中间步骤）**：无。引文本身就是这一中间步。
- **D 型（覆盖范围不足）**：**命中**。断言的**全部信息量**在于「这一步偷偷用了 T 张成 V」，
  而引文里根本没有 spans / spanning / basis 任何字样——`The same argument` 恰恰是把这件事
  藏起来的那句话。引文能证「书上写了这句」，不能证「这句依赖 T 张成」。
  exposure.tsv：该节点 T1=1、T2=1、T3=0、pool_total=2、**distinct_src_lines=1**——
  出边没有引入任何新源行，属**过报通道 (c)**的形状（同一源行两半分挂），T2 塌陷，
  记账位置问题掩盖不了证据不足。
- **E 型（整句完全无锚）**：不适用（有锚）。
- **结论**：引文未被篡改，但不支撑断言的核心。/ 依据：anchors.tsv `d:thm-15-6-symmetry-step-requires-t-to-span` 67 字符；exposure.tsv 同名行 distinct_src_lines=1；15.08.md:7。
  / 死因 **6** / 建议动作 **FIX**（增列 15.08.md:7 的 38 字符引文
  `Let S and T be two finite bases for V.`——这句才是 T 张成 V 的出处，
  已实测为逐字子串、不跨行、38 字符在 30–200 界内）/ 置信度中高。
  不确定点：也可判为死因 0 + 仅需补锚，取决于主控是否把「引文不含断言关键词」一律记 6。

### 抽查② `d:dim-of-a-second-order-de-solution-space-is-two`（例子）
锚点（79 字符）：`One basis consists of the two functions $u_1(x) = e^{-x}$ , $u_2(x) = e^{3x}$ .`
statement：「Example 3 gives dimension 2 for the solution space of the single equation
y'' - 2y' - 3y = 0 … Apostol proves nothing here about second-order homogeneous equations
in general. Verifying that this pair is independent is exactly Example 7 of 15.07 …」

- **A 型**：无。我独立代入验算过两个函数：u₁=e⁻ˣ 有 u₁''−2u₁'−3u₁ = e⁻ˣ+2e⁻ˣ−3e⁻ˣ = 0；
  u₂=e³ˣ 有 9e³ˣ−6e³ˣ−3e³ˣ = 0。两者都是解，指数 −1 与 3 正是 r²−2r−3=0 的两根。对象无误。
- **B 型**：**是，且合法**。shared-quotes.tsv：该 79 字符引文 n_holders=3，持有者为本节点、
  `apostol:ext-uncited-dimension-of-a-second-order-de-solution-space`、`edges-D2.jsonl:177`。
  两个节点分工明确（一个记内容、一个记「无出处」指控），共用定义句属 README 已认可的合法形态。
- **C 型**：**轻度命中**。statement 的第一断言是「dimension 2」，而 79 字符引文只给了**基**，
  没给维数；维数那句（`…has dimension 2.`）在另一节点的 100 字符锚点里。
  就本节点自身的 T1 看，结论（维数）与引文（基）错位一格。
- **D 型**：**不命中**。exposure.tsv：T1=1、T2=2、T3=0、pool_total=3、**distinct_src_lines=2**——
  出边确实引入了第二条源行（89 字符的 `The space of solutions … has dimension 2.`），
  这是**真冗余**而非池膨胀。按四层池定义 T2 入池，C 型错位被合法弥补。
- **E 型**：不适用。
- **结论**：锚点选得偏（给基不给维数），但 T2 补齐，断言有支撑。/ 依据：anchors.tsv 79 字符；
  edges.tsv `d:dim-of-a-second-order-de-solution-space-is-two requires apostol:homogeneous-de-solution-space` ev 89 字符；exposure.tsv distinct_src_lines=2；15.08.md:15。
  / 死因 **0** / 建议动作 **保留**，可选 FIX（把锚点扩到含 `has dimension 2.`，即第二节 P2
  不确定点里那条 132 字符变体）/ 置信度高。

### 抽查③ `d:components-depend-on-the-ordering-not-only-on-the-basis`（origin=model 推断）
锚点（85 字符）：`are called the components of x relative to the ordered basis $(e_{1},\ldots,e_{n})$ .`
statement：「Because the components are recorded as an n-tuple indexed in the given order,
permuting the ordered basis permutes the components of every element. **This is the sole
reason Apostol introduces ordered bases before components.**」

- **A 型**：无。
- **B 型**：**是**。shared-quotes.tsv：85 字符引文 n_holders=3（本节点 + `edges-D2.jsonl:186`
  + `inherited/edges-A1.jsonl:65`），无节点级借用，合法。
- **C 型**：**命中**。引文是**定义的落点**（「这些数叫做分量」），断言讲的是**机制**
  （「置换有序基会置换分量」）。这与 `report/丢弃与未覆盖清单.md` 第 88–95 行记载的那处
  X 层修正是同一形状：引文给终点、断言讲机制。
- **D 型**：**命中，且这是主要问题**。断言第二句「**这是** Apostol 把有序基放在分量之前的
  **唯一理由**」是对作者意图的全称断言，源文里没有任何句子说明为何先讲有序基。
  这是**死因 1（量词过度断言）**的标准形状（"sole reason"），按本轮规则
  **死因 1 不因边证据而免除**。exposure.tsv：T1=1、T2=1、T3=0、pool_total=2、
  **distinct_src_lines=1**——出边未引入新源行，T2 塌陷，无从弥补。
- **E 型**：不适用。
- **结论**：第一句（置换有序基则置换分量）是从记号形式可读出的合理推断，勉强可由 85 字符引文
  加 `apostol:ordered-basis` 的 n 元组记号支撑；第二句「sole reason」无源文依据且带唯一性量词。
  / 依据：statements.tsv `d:components-depend-on-the-ordering-not-only-on-the-basis`；
  anchors.tsv 85 字符；exposure.tsv distinct_src_lines=1；nodes.tsv 该行 origin=model。
  / 死因 **1**（叠 C 型锚点错位）/ 建议动作 **FIX**（删去或改写第二句为
  「有序基先行使分量得以按位记录」这类不含唯一性量词的表述；节点本身保留）
  / 置信度中高。不确定点：`origin=model` 已标注，故不涉死因 3；
  我未查该节点是否被 L2 候选引为 member，若被引，改写后需回查 member 说明是否还成立。

**三条抽查的共同教训（只就本节说）**：3 条里 2 条的 `distinct_src_lines=1`，
即「锚点 + 出边证据落在同一源行」。这正是过报通道 (c)。
补核了全集：在 15.08.md 有锚点的 48 个节点里，exposure.tsv 的 `distinct_src_lines=1` 有
**23 个（48%）**，=2 有 8 个、=3 有 9 个，余 8 个在 4–13 之间。
即近一半的节点其整个证据池只压在**一条源行**上，出边没有引入任何新的源文本。
这个数只说明冗余度低，**不**等于这 23 个都错——本轮只逐型核过其中 2 个。
（口径说明：`distinct_src_lines` 是跨全部源文件计的，不限 15.08.md。）

## 顺带发现

- 15.06.md:21 的 `The space of all polynomials $p(t)$ of degree $\leq n$ is spanned by the set of $n+1$ polynomials` 与 15.06.md:24 的集合列举 `\{1, t, t ^ {2}, \dots , t ^ {n} \}.` 在全图无任何锚点或边证据（grep `is spanned by the set of` 两表均 0）——这是 15.06 的缺口，与本节 P1 同因，不属本轮范围。
- `apostol:polynomials-of-degree-at-most-n` 的 sections 只写 15.03，但 15.06 与 15.08 都在讨论它的张成集与维数；与第四节第（三）类是同一形状的反向记账遗漏。
- `d:dim-of-vn-is-n-via-unit-coordinate-vectors` 与 `apostol:ext-uncited-unit-coordinate-basis-of-vn` 共用同一条 101 字符引文，前者记内容、后者记「无出处」指控——与抽查②的例 3 完全对称；但例 2（多项式）没有对应的 `ext-uncited` 节点，三个例子里两个立了无出处指控、一个没立，这个不对称未见说明。
- 第四节第（三）类的 5 条不一致，`check_graph` 结构上无法发现（它不比对 sections 与锚点 file），与 README 记的「15 条 origin=model 带 quote 的边从未被逐字核过」属同一类工具盲区。
- 本节 34 个已覆盖句里没有发现死因 2（虚构成员）、3（虚构出处）、4（数学错误）的候选；抽查②的两个指数函数我独立代入验算过，正确。
