# 15.04 覆盖完备性

## 〇 结论速览

本节按**句**切分共 **54 句**（含 4 个 `$$` 数学块各计 1 句；不按行号比对，见下）。

| 覆盖状态 | 句数 |
| --- | --- |
| 节点锚点已覆盖（NODE） | 44 |
| 仅边 evidence 覆盖（EDGE） | 3 |
| 已下移到 part-of 后代（T3） | 0（无「只在后代」的句子；后代覆盖的句子其 parent 也覆盖） |
| **未覆盖** | 7 |

未覆盖 7 句中**值得补建新节点的为 0 个**。7 句的处置是：3 句纯结构/连接词（S1 标题、S6/S16「Proof.」中的连接残句已并入 EDGE，见下表）、
1 句节引导性过渡（S2）、1 句证明步骤缺锚（S8，**应 FIX 现有节点而非新建**）、
3 句为定理 15.3 的 (a)(b)(c) 命题行（S32/S33/S34，**节点已存在、内容不缺，但源行无法合法建锚——见 §三 的字数说明**）。

**过报通道 b 已避开**：`source-lines.tsv` 显示 15.04.md 的第 5、7、9、11、25、29、49、57、59、65 行
`n_sentence_end ≥ 2`（第 7 行达 6 句），若按行号比对会把这些行整行判为「已覆盖」。
本报告全部按句判定。**冗余度未按池大小计**：本节 93 条引文（锚点 36 + 边 evidence 57）
去重后只落在 **24 个不同 (file,quote)** 上、覆盖 **20 个不同源行**，重复度约 3.9 倍。

**引文真实性**：本节 93 条引文（`file=15.04.md` 的全部锚点与边 evidence）已用脚本逐条比对源文件，
**93/93 为单行内逐字连续子串，0 条失败**。这只证明未被篡改，不证明支撑断言（见 §五）。

## 一 逐句覆盖表

「判据」列的 `A:` 指 `anchors.tsv` 命中，`E:` 指 `edges.tsv` 的 evidence 命中。
覆盖判定为严格包含（句 ⊂ 引文 或 引文 ⊂ 句），不使用模糊匹配。

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
| --- | --- | --- | --- | --- | --- |
| S1 | L1 | `## 15.4 Elementary consequences of the axioms`（节标题） | **未覆盖** | — | 全图无任何引文含 "Elementary consequences of the axioms" |
| S2 | L3 | The following theorems are easily deduced from the axioms | **未覆盖** | — | 全图 0 持有者（脚本全表 grep） |
| S3 | L5 | THEOREM 15.1. | NODE | apostol:theorem-uniqueness-of-zero-element | A: 105 字符锚整行含之 |
| S4 | L5 | UNIQUENESS OF THE ZERO ELEMENT.（定理名） | NODE | 同上 | A: 同一 105 字符锚 |
| S5 | L5 | 任一线性空间中零元有且只有一个 | NODE | 同上 | A: 同一 105 字符锚 |
| S6 | L7 | Proof.（证明起始词） | EDGE | apostol:theorem-uniqueness-of-zero-element -requires-> d:axiom-5-existence-of-zero-element | E: edges-D1:43 的 64 字符 evidence 以 "Proof." 起头 |
| S7 | L7 | Axiom 5 给出至少一个零元 | NODE | apostol:zero-element | A: anchors.tsv 57 字符 |
| S8 | L7 | Suppose there were two, say O_1 and O_2. | **未覆盖** | — | 全图 0 持有者；详见 §二 |
| S9 | L7 | 取 x=O_1, O=O_2 代入 Axiom 5 得 O_1+O_2=O_1 | NODE | d:thm-15-1-two-candidate-zeros-swap-roles-in-axiom-5 | A: 144 字符锚含 S9+S10 |
| S10 | L7 | 对称地取 x=O_2, O=O_1 得 O_2+O_1=O_2 | NODE | 同上 | A: 同一 144 字符锚 |
| S11 | L7 | 由交换律 O_1+O_2=O_2+O_1，故 O_1=O_2 | EDGE | apostol:theorem-uniqueness-of-zero-element -requires-> d:axiom-3-…；及 d:thm-15-1-… -requires-> 同 | E: edges-D1:44 / edges-D5:5，76 字符；**无节点锚点持有此句** |
| S12 | L9 | THEOREM 15.2. | NODE | apostol:theorem-uniqueness-of-negatives | A: 177 字符锚 |
| S13 | L9 | UNIQUENESS OF NEGATIVE ELEMENTS. | NODE | 同上 | A: 同一 177 字符锚 |
| S14 | L9 | 每个元素恰有一个负元 | NODE | 同上 | A: 同一 177 字符锚 |
| S15 | L9 | 即对每个 x 有且只有一个 y 使 x+y=O | NODE | 同上 | A: 177 字符锚含之（脚本全表核，1 持有者） |
| S16 | L11 | Proof. | EDGE | apostol:theorem-uniqueness-of-negatives -requires-> d:axiom-6-existence-of-negatives | E: edges-D1:45 的 79 字符 evidence 以 "Proof." 起头 |
| S17 | L11 | Axiom 6 给出至少一个负元 (-1)x | NODE | apostol:negative-of-an-element | A: 70 字符锚 |
| S18 | L11 | Suppose x has two negatives, y_1 and y_2. | NODE | d:thm-15-2-same-sum-evaluated-in-two-groupings | A: 97 字符锚含 S18+S19 |
| S19 | L11 | 则 x+y_1=O 且 x+y_2=O | NODE | 同上 | A: 同一 97 字符锚 |
| S20 | L11 | 把 y_2 加到第一式两边、用 Axioms 5,4,3 | NODE | d:thm-15-2-left-neutrality-needs-the-commutative-law | A: 95 字符锚 |
| S21 | L13–15 | `$$ y_2+(x+y_1)=y_2+O=y_2, $$` | NODE | d:thm-15-2-same-sum-evaluated-in-two-groupings | A: 48 字符锚 |
| S22 | L17 | and（两式之间的连接词） | **未覆盖** | — | 3 字符，纯连接词，无引文含之 |
| S23 | L19–21 | `$$ y_2+(x+y_1)=(y_2+x)+y_1=O+y_1=y_1+O=y_1. $$` | NODE | d:axiom-5-states-…-right-neutral-element；d:thm-15-2-left-neutrality-…；d:thm-15-2-same-sum-… | A: 88 字符锚，3 个节点共享（见 shared-quotes.tsv n_holders=4） |
| S24 | L23 | 故 y_1=y_2，x 恰有一个负元 (-1)x | NODE | apostol:theorem-uniqueness-of-negatives；d:axiom-6-names-the-negative-as-minus-one-times-x | A: 82 字符锚，n_holders=3 |
| S25 | L25 | Notation.（记号段起始词） | NODE | apostol:negative-and-difference-notation | A: 109 字符锚以 "Notation." 起头 |
| S26 | L25 | x 的负元记作 -x | NODE | 同上 | A: 同一 109 字符锚 |
| S27 | L25 | 差 y-x 定义为 y+(-x) | NODE | 同上 | A: 同一 109 字符锚（脚本全表核，2 持有者） |
| S28 | L27 | 下一定理描述一批支配初等代数演算的性质 | NODE | apostol:theorem-elementary-algebraic-properties | A: 116 字符锚（该节点 anchor idx=1） |
| S29 | L29 | THEOREM 15.3. | NODE | apostol:theorem-elementary-…；d:thm-15-3-quantifiers-… | A: 165 / 126 字符锚 |
| S30 | L29 | 设 x,y 为任意元素、a,b 为任意标量 | NODE | 同上两者 | A: 同上；另 E: inherited/edges-A1:31,32 |
| S31 | L29 | Then we have the following properties: | NODE | apostol:theorem-elementary-algebraic-properties | A: 165 字符锚含之（126 字符锚**不含**此句） |
| S32 | L31 | **(a) 0x = O.** | **未覆盖** | — | 全图 0 持有者；源行仅 11 字符，详见 §三 |
| S33 | L33 | **(b) aO = O.** | **未覆盖** | — | 全图 0 持有者；源行仅 14 字符，详见 §三 |
| S34 | L35 | **(c) (-a)x = -(ax) = a(-x).** | **未覆盖** | — | 全图 0 持有者；源行仅 28 字符，详见 §三 |
| S35 | L37 | (d) ax=O ⇒ a=0 或 x=O | NODE | d:thm-15-3d-a-vanishing-product-forces-a-vanishing-factor | A: 50 字符锚（整行） |
| S36 | L39 | (e) ax=ay, a≠0 ⇒ x=y | NODE | d:thm-15-3e-a-nonzero-scalar-may-be-cancelled | A: 48 字符锚（整行） |
| S37 | L41 | (f) ax=bx, x≠O ⇒ a=b | NODE | d:thm-15-3f-a-nonzero-element-may-be-cancelled | A: 48 字符锚（整行） |
| S38 | L43 | (g) -(x+y)=(-x)+(-y)=-x-y | NODE | d:thm-15-3g-the-negative-of-a-sum | A: 38 字符锚（整行） |
| S39 | L45 | (h) x+x=2x, …, Σ_{i=1}^{n} x = nx | NODE | d:thm-15-3h-repeated-addition-is-an-integer-multiple | A: 76 字符锚（整行） |
| S40 | L47 | 只证 (a)(b)(c)，其余留作习题 | NODE | apostol:theorem-elementary-…（idx=2）；d:thm-15-3-only-a-b-c-are-proved-in-the-text | A: 91 字符锚，n_holders=8 |
| S41 | L49 | Proof of (a). | NODE | d:proof-a-set-z-equal-to-0x-and-double-it；d:thm-15-3a-… | A: 113 字符锚以 "Proof of (a)." 起头 |
| S42 | L49 | Let z = 0x. | NODE | 同上两者 | A: 同一 113 字符锚 |
| S43 | L49 | 欲证 z = O | NODE | 同上两者 | A: 同一 113 字符锚 |
| S44 | L49 | 把 z 与自身相加、用 Axiom 9 | NODE | 同上两者 | A: 同一 113 字符锚；另 E: edges-D1:65（52 字符） |
| S45 | L51–53 | `$$ z+z = 0x+0x = (0+0)x = 0x = z. $$` | NODE | d:proof-a-set-z-…；d:thm-15-3a-… | A: 40 字符锚 |
| S46 | L55 | Now add -z to both members to get z = O. | NODE | d:proof-a-cancel-by-adding-the-negative-of-z | A: 40 字符锚（整行） |
| S47 | L57 | Proof of (b). | NODE | d:proof-b-mirrors-proof-a-with-axiom-8；d:thm-15-3b-… | A: 64 字符锚以之起头 |
| S48 | L57 | 令 z=aO，与自身相加，用 Axiom 8 | NODE | 同上两者 | A: 同一 64 字符锚，n_holders=7 |
| S49 | L59 | Proof of (c). | NODE | d:proof-c-first-half-…；d:thm-15-3c-… | A: 82 字符锚以之起头 |
| S50 | L59 | Let z = (-a)x. | NODE | 同上两者 | A: 同一 82 字符锚 |
| S51 | L59 | 把 z 加到 ax、用 Axiom 9 | NODE | 同上两者 | A: 同一 82 字符锚；另 E: edges-D1:79（50 字符） |
| S52 | L61–63 | `$$ z+ax = (-a)x+ax = (-a+a)x = 0x = O, $$` | NODE | d:proof-c-first-half-…；d:thm-15-3c-… | A: 48 字符锚 |
| S53 | L65 | 故 z 是 ax 的负元，z = -(ax) | NODE | d:proof-c-conclusion-rests-on-uniqueness-of-the-negative；d:proof-c-second-half-… | A: 141 字符锚含 S53+S54（两节点共享，n_holders=2） |
| S54 | L65 | 类似地把 a(-x) 加到 ax、用 Axiom 8 与性质 (b)，得 a(-x) = -(ax) | NODE | 同上两者 | A: 同一 141 字符锚；另 E: edges-D1:70,71,82,83 |

## 二 未覆盖且值得补建

**建议新建节点数 = 0。** 本节唯一有数学内容的未覆盖句（S8）落在一个**已存在节点**的断言范围内，
正确动作是给那个节点补锚，不是新建节点。下列为该 FIX 提案。

### FIX-1：S8 缺锚 —— d:thm-15-1-two-candidate-zeros-swap-roles-in-axiom-5 的 D 型覆盖不足

- **结论**：定理 15.1 证明的第一步「假设有两个零元 O_1、O_2」在全图**无任何引文**持有
  （脚本对 anchors.tsv + edges.tsv 全表 grep `Suppose there were two, say $O_1$ and $O_2$`，持有者 0）。
  该步骤是节点 `d:thm-15-1-two-candidate-zeros-swap-roles-in-axiom-5` 断言的前提：
  其 statement 说「取 x = O_1 和 O = O_2」，两个候选零元的引入是这句话的先决条件，
  但它的 144 字符锚点从 `Taking $x = O_1$ …` 开始，恰好切掉了候选的引入。
- **依据**：`anchors.tsv` 该节点 idx=0，charlen=144，quote 起于 "Taking"；
  `source-lines.tsv` `apostol-ch15/15.04.md` L7 charlen=332 / n_sentence_end=6（故不得按行号判为已覆盖）。
  对照：定理 15.2 的平行步骤 S18「Suppose x has two negatives」**有**锚点
  （`d:thm-15-2-same-sum-evaluated-in-two-groupings` 97 字符锚），两节处理不对称。
- **死因编号**：**0**（无死因，仅需 FIX）。不是死因 6：现有锚点确实支撑「代入两次、角色互换」这一断言，
  只是覆盖范围没有前伸到候选引入句，属盲区 **D 型（覆盖范围不足）**。
- **建议动作**：**FIX** —— 把该节点 idx=0 的锚点前伸，或增设一条锚点。建议引文（**已实测**）：

```
Suppose there were two, say $O_1$ and $O_2$ . Taking $x = O_1$ and $O = O_2$ in Axiom 5, we obtain $O_1 + O_2 = O_1$ .
```

  **实测字符数 118**（自测；`in lines[6] == True`，单行内逐字连续，不跨行，落在 SPEC 30–200 界内）。
  若只想补最小片段：`Suppose there were two, say $O_1$ and $O_2$ .` **实测 45 字符**，同样合界。
  推荐 118 字符的那条：它同时保住原锚点的内容，一条锚即可，且把「候选引入 + 双向代入」连成一体。
- **置信度**：高（引文与字数均脚本实测）。**不确定点**：改锚会使该节点与
  `edges-D5.jsonl:1` 的 evidence（同为 144 字符那句）不再同文，是否需要连带改边由主控决定；
  本轮只读，未核该边改动的连带影响。

### 顺带影响：L2:uniqueness-by-forcing-the-difference-to-zero 的 15.04 侧第一步无源锚

- **结论**：该 L2 节点 sections 含 15.04，其 statement 的第一步是「假设同一个位置上有两个对象」。
  它在 15.04 侧的成员是 `apostol:theorem-uniqueness-of-zero-element` 与
  `apostol:theorem-uniqueness-of-negatives`。定理 15.2 侧第一步有锚（S18），
  **定理 15.1 侧第一步（S8）无锚**，即该套路在 15.04 的两个实例里只有一个能出示第一步的源引文。
- **依据**：`data/structures-L2-a.jsonl` 的 members 列表；本节 S8/S18 覆盖判定；
  该 L2 节点 `n_anchors=0`（L2 层设计如此，不算缺陷）。
- **死因编号**：0 —— 这不是 L2 节点自身的错，FIX-1 落地后自动消解。
- **建议动作**：**保留** L2 节点，随 FIX-1 一并解决。置信度中（L2 侧只核了 15.04 的两个成员，未跨节核 15.08/15.13/15.14）。

## 三 未覆盖但不值得建

- **S1**（L1，节标题 `## 15.4 Elementary consequences of the axioms`）：纯结构标记，不是断言。**不建**。
- **S2**（L3，"The following theorems are easily deduced from the axioms for a linear space."）：
  节引导性过渡叙述。它确实含一个方法论断言——本节三条定理只用公理、不用额外假设——
  但该断言已由 `d:thm-15-3-quantifiers-arbitrary-elements-and-arbitrary-scalars`
  （「no finiteness, dimension or metric hypothesis」）在定理 15.3 上表达，
  为定理 15.1/15.2 再建一个同义节点会撞上审计 B 的删除测试（死因 5 风险）。**不建**，
  仅记为过渡叙述。置信度中：我未核审计 B 对该类节点的既有判例，只按其公开判据推断。
- **S22**（L17，"and"）：3 字符纯连接词，连接定理 15.2 证明中的两个公式块。**不建**。
- **S32 / S33 / S34**（L31/L33/L35，即定理 15.3 的命题 (a)(b)(c)）：**内容不缺，源行无法合法建锚**。
  - 三个节点 `d:thm-15-3a-the-scalar-zero-annihilates-every-element`、
    `d:thm-15-3b-every-scalar-annihilates-the-zero-element`、
    `d:thm-15-3c-negation-passes-through-scalar-multiplication` **已存在**（`nodes-D1.jsonl:27/28/29`），
    statement 逐条正确复述了 (a)(b)(c)；parent
    `apostol:theorem-elementary-algebraic-properties` 的 statement 也逐字含
    「0x = O; aO = O; (-a)x = -(ax) = a(-x)」。所以这**不是内容缺口**。
  - 但这三条源行**在全图无任何引文持有**（脚本全表 grep `(a) 0x = O.` / `$aO = O$` / `(c) $(-a)x = -(ax) = a(-x)`，
    持有者各为 0）。三个节点的锚点全部落在**证明行**（L49/L57/L59），不在命题行。
  - **原因是字数硬约束，不是作者疏忽**：实测 L31=**11 字符**、L33=**14 字符**、L35=**28 字符**，
    三者**全部低于 SPEC 的 30 字符下界**；而硬禁令 2 要求引文不跨行。
    对照 (d)–(h) 各行实测为 50 / 48 / 48 / 38 / 76 字符，全部 ≥30，因此它们都拿到了命题行锚点。
    **(a)(b)(c) 与 (d)–(h) 的锚点位置不一致，根源是 SPEC 的 30 字符下界与 OCR 的分行方式相撞。**
  - **死因编号**：**0**。**建议动作**：**不建新节点**；三个节点**保留**。
    这一条**建议进 SPEC 记录为已知限制**（「短公式行低于 30 字符下界时，命题行不可建锚，
    退回证明行锚点」），而不是当成覆盖缺口反复上报。
  - 置信度高（字数与持有者数均脚本实测）。**不确定点**：是否可以把 L29–L35 视作一个逻辑单元
    而允许一条跨行锚点，取决于禁令 2 是否有例外条款——我未核 SPEC 原文，未做此判断。
- **S6 / S16 / S11**（不属未覆盖，列此备核）：这三句只有**边 evidence** 持有、无节点锚点。
  S11「But O_1 + O_2 = O_2 + O_1 because of the commutative law, so O_1 = O_2.」是定理 15.1 的**收束句**，
  按四层证据池它入 `apostol:theorem-uniqueness-of-zero-element` 的 T2（自发边），
  也入 `d:thm-15-1-…` 的 T2，属**记账位置问题而非内容缺失**（T2 塌陷）。
  **建议动作：保留**，不因此新建节点。死因 0。

## 四 锚点集合与 sections 集合的不一致

两个集合**对不上，共 3 处差异**。集合定义：
A = `anchors.tsv` 中 `file == 15.04.md` 的节点集（**25 个**）；
B = `nodes.tsv` 中 `sections` 含 `15.04` 的节点集（**28 个**）。
A\B = 1 个，B\A = 3 个（其中 1 个为 L2 层设计使然）。

### 不一致 1（A\B）：有 15.04 锚点但 sections 不含 15.04

- `d:axiom-5-states-the-zero-only-as-a-right-neutral-element`
  —— `nodes.tsv` `sections = 15.02`（单一），但 `anchors.tsv` idx=1 的锚点 `file = 15.04.md`
  （88 字符，即 S23 那个公式块）。
- **结论**：sections 漏了 15.04。该节点的 statement 明确说这条一侧性「used freely from Theorem 15.1 onward」，
  而它的 15.04 锚点正是定理 15.2 证明里 `O + y_1 = y_1 + O` 这一步——内容确实横跨 15.02 与 15.04。
- **依据**：`nodes.tsv`（`data/nodes-D1.jsonl:21`，sections 列）；`anchors.tsv` 该节点两行；
  `pool.tsv` 该节点 T1 两条分属 15.02.md / 15.04.md。
- **死因编号**：**0**（元数据漏项，非内容错）。
- **建议动作**：**FIX** —— `sections` 改为 `15.02,15.04`。置信度高。

### 不一致 2（B\A）：sections 含 15.04 但无 15.04 锚点

- `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6`
  —— `sections = 15.02,15.04`，但两条锚点 `file` **均为 15.02.md**（87 / 66 字符）。
- **结论**：这里的 15.04 标的不是锚点位置而是**依赖位置**：该节点有一条
  `requires -> d:thm-15-3a-the-scalar-zero-annihilates-every-element`
  （`edges-D1.jsonl:35`，**origin = model，evidence 为空**），定理 15.3(a) 在 15.04。
  它的 statement 自己声明「Apostol does not remark on this; the derivation below is not in the text」，
  所以 15.04 侧本来就不该有源锚点——**这不是缺陷**，但 sections 的语义在此从「有锚点的节」
  滑向了「所依赖内容所在的节」，两种用法混在同一列里。
- **依据**：`anchors.tsv` 两行 file 均 15.02.md；`edges.tsv` 该节点 4 条出边 origin 全为 model、ev_file 为空；
  `exposure.tsv`：T1=2 / T2=1 / T3=0 / 池 2 个不同源行，**全部来自 15.02.md**。
- **死因编号**：**0**（非死因；但 sections 语义不统一是一处 FIX 候选）。
- **建议动作**：**保留**节点内容；建议主控统一 sections 语义（要么只记锚点所在节、
  要么显式区分「锚点节」与「依赖节」）。置信度高（数据实测），
  **不确定点**：sections 列的官方语义我未在 SPEC 里核到，故不判它错，只报不一致。

### 不一致 3（B\A，设计使然，不算缺陷）

- `L2:uniqueness-by-forcing-the-difference-to-zero` —— `sections = 15.04,15.08,15.13,15.14`，`n_anchors = 0`。
  L2 层节点按设计无锚点（`tmp_index/README.md` 列的 28 个无锚节点里含全部 24 个 L2）。**不需动作**。

## 五 本节现有锚点抽查（3 条，按盲区五型逐型检查）

### 抽查 1：`d:thm-15-3-quantifiers-arbitrary-elements-and-arbitrary-scalars`（唯一锚点，126 字符）

引文：`THEOREM 15.3. In a given linear space, let $x$ and $y$ denote arbitrary elements and let $a$ and $b$ denote arbitrary scalars.`

- **A 型（变量/对象错）**：通过。引文点名的正是 x, y, a, b 四个符号，与 statement 第一句一致。
- **B 型（借用引文）**：`shared-quotes.tsv` 该 (file,quote) `n_holders = 3`
  —— 本节点锚点 + `edges-D1:49`（本节点自己发出的 part-of 边）+ `inherited/edges-A1:31`
  （`apostol:theorem-elementary-algebraic-properties -requires-> apostol:linear-space`）。
  前两者同源合法；第三者用同句证明「定理 15.3 需要线性空间概念」，也对。**通过**。
- **C 型（端点冒充中间步）**：不适用（引文本身就是量词声明，不是推导步）。**通过**。
- **D 型（覆盖范围不足）**：**不通过。这是本抽查的实质发现。**
  statement 第二句是「Consequently every property holds verbatim in each of the **twelve examples of Section 15.3**
  and in the **complex case** as well」。`exposure.tsv` 该节点：后代 0 / T1=1 / T2=1 / 池总 2 /
  **去重后不同源行 = 1**，且这 1 条就是上面那句 126 字符引文，全部来自 15.04.md。
  **池里没有任何来自 15.03.md 的引文，也没有任何关于复数情形的引文**——
  「十二个例子逐条成立」与「复数情形亦然」两个断言在四层证据池里零支撑。
  我另行核过 `source/apostol-ch15/15.03.md`，`EXAMPLE 1`–`EXAMPLE 12` 确实存在（计数 12 正确，不是死因 2/3），
  所以问题不是虚构，而是**断言范围远超引文范围**。
- **E 型（整句完全无锚）**：**不通过**——statement 第二句整句无锚，与 D 型同因。
- **结论**：引文支撑 statement 第一句，不支撑第二句。
- **死因编号**：**1**（量词过度断言：「every property … in each of the twelve examples」）
  兼 **6**（引文不支撑断言）。按四层池规则，死因 1 **不因边证据而免除**，且此处连边证据也没有。
- **建议动作**：**FIX** —— 二选一：(i) 把第二句降格为不带全称量词的观察
  （如「该定理不设有限性、维数或度量假设，故对 15.3 的诸例均适用」并补一条 15.03.md 锚点或一条
  `applies-to` 边指向具体例子节点）；(ii) 删去第二句只留第一句。**不建议原样保留**。
- **置信度**：高（exposure/pool 实测，15.03 例数实测）。
  **不确定点**：我未逐个验算十二个例子是否真的逐条成立——很可能都成立，但**未核**，
  故只报「无证据支撑」，不报「数学错误」。复数情形出现在 15.13 之后，也未核。

### 抽查 2：`apostol:zero-element` 的 15.04 锚点（57 字符）

引文：`Axiom 5 tells us that there is at least one zero element.`

- **A 型**：通过（对象是零元，与节点一致）。
- **B 型**：`shared-quotes.tsv` n_holders=3 —— 本节点锚点 + `edges-D5:4` + `inherited/edges-A1:25`，
  两条边都是「定理 15.1 / 15.1 的证明步 requires 零元存在」，用法一致。**通过，不是借用**。
- **C 型**：**技术性不通过（轻微）**。这句是 15.04 里对 Axiom 5 的**回指**，不是 Axiom 5 的定义原文；
  作为「零元存在」的引文可用，但它证明的是「Apostol 在此复述了该公理」。
  好在该节点 idx=0 的锚点是 15.02.md 的 85 字符公理原文，定义本体有正锚，故影响很小。
- **D 型**：**不通过**。statement 有两句：「Axiom 5 postulates an element … with x + O = x for all x in V」
  与「In a function space the zero element is the function whose values are everywhere zero」。
  查 `pool.tsv` 该节点全部 7 条（T1 2 / T2 1 / T3 4，去重 4 个源行）：
  x + O = x 由 T3 后代锚（15.02.md 36 字符 `x + O = x \quad f o r a l l x i n V.`）支撑，**合法下移，不是缺口**；
  但**函数空间那句在池里零支撑**——7 条引文没有一条提到函数空间或处处为零的函数。
- **E 型**：函数空间那一句整句无锚。**不通过**。
- **结论**：15.04 这条锚点本身无误；节点 statement 的第二句缺证据。
- **死因编号**：**6**（引文不支撑断言）。不判死因 2/3：函数空间的零元确实是零函数，
  且 Apostol 在 15.3 的例子里讲过函数空间，内容不虚构，是记账缺失。
- **建议动作**：**FIX** —— 给函数空间那句补一条来自 15.02.md 或 15.03.md 的锚点，
  或把它移到相应的函数空间例子节点上。若补不到逐字引文，则删去该句。
- **置信度**：高（池实测）。**不确定点**：我**未**扫 15.02.md / 15.03.md 去找候选引文
  （超出本任务范围），故未给出具体建议引文与字数。

### 抽查 3：`d:axiom-5-states-the-zero-only-as-a-right-neutral-element` 的 15.04 锚点（88 字符）

引文：`y _ {2} + (x + y _ {1}) = (y _ {2} + x) + y _ {1} = O + y _ {1} = y _ {1} + O = y _ {1}.`

- **A 型**：通过。断言讲的是「Axiom 5 只给 x + O = x，不给 O + x」，引文里恰好出现
  `O + y _ {1} = y _ {1} + O`——把左侧的 O 换到右侧这一步，正是该断言的**用例**。
- **B 型**：`shared-quotes.tsv` 该 (file,quote) `n_holders = 4` ——
  本节点 + `d:thm-15-2-left-neutrality-needs-the-commutative-law` + `d:thm-15-2-same-sum-evaluated-in-two-groupings` + `edges-D5:2`。
  三个节点共享同一公式块。逐条读 statement 后判定：**合法，但存在一处近同义**。
  `d:thm-15-2-left-neutrality-needs-the-commutative-law` 的 statement
  （「Axiom 5 asserts neutrality only in the form x + O = x, with the zero on the right」）
  与本节点的 statement（「Axiom 5 asserts x + O = x and says nothing about O + x」）**讲的是同一件事**，
  只是前者挂在定理 15.2 下、后者挂在 `apostol:zero-element` 下，parent 不同故非兄弟冗余。
  **不判 B 型失配**，但这一对是审计 B 删除测试（死因 5）的候选，**超出本任务范围，未判**。
- **C 型**：通过。引文是中间步（`O + y_1 = y_1 + O` 这一次换序），不是终点 `= y_1`。
- **D 型**：**轻微不通过**。statement 说这条一侧性「used freely **from Theorem 15.1 onward**」。
  我核了定理 15.1 的证明（S7–S11）：它只用 `x + O = x` 的右侧形式加交换律得 O_1 = O_2，
  **没有出现 O 在左侧的 O + y 形式**；左侧中立性第一次真正出现是**定理 15.2** 的 S23 公式块，
  即本锚点所在处。所以「from Theorem 15.1 onward」的起点偏早一节。
- **E 型**：通过（两句都有锚：15.02 锚给公理原文，15.04 锚给使用现场）。
- **结论**：引文支撑核心断言；「from Theorem 15.1 onward」的起点不准，且这是一个未核到底的全章范围断言。
- **死因编号**：**1**（量词过度断言，程度轻——起点错一节，且「onward」覆盖 15.06 以后我**未核**）。
- **建议动作**：**FIX** —— 把 statement 里的「from Theorem 15.1 onward」改为「from Theorem 15.2 onward」
  或改为「in the proof of Theorem 15.2」这类可由现锚点直接兑现的表述。锚点本身**保留**，无需改。
- **置信度**：对 15.04 内的判定高（逐句读过定理 15.1、15.2 的证明全文）；
  对 15.06 及以后是否「freely used」——**未核**，不做预测。

## 顺带发现

- `anchors.tsv` / `edges.tsv` 的 quote 列存在**双重转义**：源文件里的 `\neq`、`\sum` 在表里写成 `\\neq`、`\\sum`，naive 反转义会误判 3 条锚点「引文找不到」；反转义须处理 `\\` → `\`，否则会产出假阳性。
- 本节 3 条边的 evidence（`edges-D1:65` 52 字符、`edges-D1:79` 50 字符、`edges-D1:82` 60 字符）是句子的**中段截取**（不含句首句尾），机器按整句比对会漏判它们已覆盖；覆盖判定必须做双向包含。
- `nodes.tsv` 的 `node_type` 列在本节出现 `method` 与 `notation` 两个值（如 `d:proof-a-*` 为 method、`apostol:negative-and-difference-notation` 为 notation），二者均**不在封闭词表** concept/theorem/proof-step/example 之内；疑为词表与数据不同步，未判。
- `d:axiom-10-is-derivable-from-apostols-form-of-axiom-6` 的 4 条出边全为 `origin=model` 且 evidence 为空，故不在「15 条 origin=model 却带 quote」清单里；本节未发现属于那 15 条的边。
- 定理 15.3 的 (a)(b)(c) 三条命题行低于 30 字符这个现象，很可能在其它节的短公式行上重复出现（本节未跨节核）。
