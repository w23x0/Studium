# 15.11 覆盖完备性

## 〇 结论速览（本节 52 句 / 已覆盖 44 / 下移 0 / 未覆盖 8；其中值得补建 0 个、值得补锚 1 处）

切句办法：读 `source/apostol-ch15/15.11.md` 全文（92 行），按句末标点切，公式块整体计一句。
**不按行号比对**——本节 `source-lines.tsv` 里第 3 行 4 句、第 15 行 6 句、第 29 行 3 句、
第 35 行 2 句、第 41 行 2 句、第 43 行 2 句、第 89 行 3 句、第 91 行 3 句，全是过报通道 b。

- 已覆盖 44：其中 41 句被节点锚点逐字含住，3 句仅靠边 evidence（S22/S23 由 edges-D3.jsonl:49
  的 134 字符 evidence 覆盖，S16 由 edges-D3.jsonl:126 的 154 字符 evidence 覆盖）。
- 下移 0：本节所有 D 层节点的 parent 都在本节内（`apostol:orthogonal-elements` 等 6 个 A1 节点
  的 sections 也是 15.11），不存在内容下移到别节的情况，也没有需要靠 T3 才够的父节点。
- 未覆盖 8：S5 S10 S18 S19 S27 S30 S32 S48。其中只有 S18+S19 是数学内容，其余 6 句是过渡叙述、
  引导小句或纯记号。
- **值得补建 0 个新节点**；值得的是给已有节点 `d:trigonometric-orthogonal-system` 补 2 条锚点
  （见第二节）。它的 statement 已经断言了 S19 的正交关系，却没有一条锚点落在 S19 上——这是
  **死因 6**（引文不支撑断言）的一个实例，不是完备性缺口，而是记账缺口。

已知更正已遵守：**没有**把 15.15 的那处数学缺陷算到本节头上，本节未发现任何死因 4。

## 一 逐句覆盖表（句号 / 句子摘要 / 覆盖状态 / 覆盖者 id 或 file:line / 判据）

覆盖状态记号：**N** = 节点锚点已覆盖；**E** = 仅边 evidence 已覆盖；**—** = 未覆盖。
「覆盖者」列给节点 id 时，判据是 `anchors.tsv` 里该节点的某条锚点逐字含住本句；
给 `edges-*.jsonl:行` 时，判据是 `edges.tsv` 的 ev_quote 逐字含住本句。S0 是节标题，不计句。

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S0 | 1 | 节标题 `## 15.11 Orthogonality in a Euclidean space` | — | — | 标题，不计入 52 句 |
| S1 | 3 | DEFINITION：内积为零即正交 | N | `apostol:orthogonal-elements` a0；`d:orthogonality-is-vanishing-of-the-inner-product` a0 | 110 字符锚点整句 |
| S2 | 3 | 子集 S 的每对**相异**元素内积为 0 则 S 是正交集 | N | `apostol:orthogonal-set` a0；`d:orthogonal-set-condition-on-distinct-pairs-only` a0 | 109 字符 |
| S3 | 3 | 正交集若每元素范数为 1 则称正交规范集 | N | `apostol:orthonormal-set` a0；`d:orthonormal-set-adds-unit-norm` a0 | 75 字符 |
| S4 | 5 | 零元与一切元素正交，且是唯一自正交元 | N | `d:zero-element-orthogonal-to-every-element` a0 等 4 个持有者 | 98 字符，shared-quotes n_holders=8 |
| S5 | 5 | 「下一定理揭示正交性与相依性的关系」 | — | — | 71 字符，过渡叙述，见第三节 |
| S6 | 7 | THEOREM 15.10：非零元的正交集必独立 | N | `apostol:theorem-15-10-orthogonal-sets-are-independent` a0 | 95 字符 |
| S7 | 7 | 特例：dim V = n 时 n 个非零正交元构成基 | N | `d:15-10-n-orthogonal-nonzero-elements-form-a-basis` a0 | 142 字符 |
| S8 | 9 | Proof 起手：设 S 正交非零，某有限线性组合为零 | N | `d:15-10-proof-setup-vanishing-finite-combination` a0 | 132 字符 |
| S9 | 11–13 | 公式块 $\sum c_i x_i = O$ | N | `d:15-10-proof-setup-vanishing-finite-combination` a1 | 40 字符 |
| S10 | 15 | 「其中每个 $x_i \in S$」 | — | — | 26 字符，**低于 SPEC 30 下界**，见第三节 |
| S11 | 15 | 与 $x_1$ 作内积，用 $i \neq 1$ 时配对为零，得 $c_1(x_1,x_1)=0$ | N | `d:15-10-proof-pair-with-a-single-element` a0 | 152 字符 |
| S12 | 15 | $(x_1,x_1) \neq 0$ 因 $x_1 \neq O$，故 $c_1 = 0$ | N | `d:15-10-proof-divide-by-nonzero-self-inner-product` a0 | 65 字符 |
| S13 | 15 | 把 $x_1$ 换成 $x_j$ 重复，得每个 $c_j = 0$ | N | `d:15-10-proof-repeat-for-each-index` a0 | 124 字符（含 S14） |
| S14 | 15 | 「这证明 S 独立」 | N | 同上 a0 | 与 S13 同一 124 字符锚点 |
| S15 | 15 | dim V = n 且 S 有 n 元时由 15.7(b) 得 S 是基 | N | `d:15-10-n-orthogonal-nonzero-elements-form-a-basis` a1 | 92 字符 |
| S16 | 17 | EXAMPLE：$C(0,2\pi)$ 配积分内积，取三角函数集 S | E | `data/edges-D3.jsonl:126`（`d:trigonometric-orthogonal-system requires d:integral-inner-product-formula`） | ev 154 字符含住本句主干；节点锚点里无此句 |
| S17 | 19–21 | 公式块：$u_0 = 1$、$u_{2n-1} = \cos nx$、$u_{2n} = \sin nx$ | N | `d:trigonometric-orthogonal-system` a0 | 126 字符 |
| S18 | 23 | 「若 $m \neq n$，我们有正交关系」（S19 的前提小句） | — | — | 51 字符，**数学前提**，见第二节 |
| S19 | 25–27 | 公式块 $\int_0^{2\pi} u_n u_m \,dx = 0$ | — | — | 52 字符，**正交关系本体，全节最实质的未覆盖句**，见第二节 |
| S20 | 29 | 「所以 S 是正交集」 | N | `apostol:orthogonal-set` a1；`d:trigonometric-orthogonal-system` a1 | 86 字符（含 S21） |
| S21 | 29 | 「S 无零元，故 S 独立」 | N | 同上 a1 | 与 S20 同一 86 字符锚点 |
| S22 | 29 | 「每个元素的范数容易算出」 | E | `data/edges-D3.jsonl:49`（`d:trigonometric-system-norms part-of d:trigonometric-orthogonal-system`） | ev 134 字符含 S22+S23；节点锚点起点在下一句 |
| S23 | 29 | $(u_0,u_0) = \int_0^{2\pi} dx = 2\pi$，且 $n \geq 1$ 时 | E | 同上 | 同一条 134 字符 evidence |
| S24 | 31–33 | 公式块 $(u_{2n-1},u_{2n-1}) = (u_{2n},u_{2n}) = \pi$ | N | `d:trigonometric-system-norms` a0 | 155 字符 |
| S25 | 35 | 因此 $\|u_0\| = \sqrt{2\pi}$、$\|u_n\| = \sqrt{\pi}$ | N | `d:trigonometric-system-norms` a1 | 79 字符 |
| S26 | 35 | 各 $u_n$ 除以范数得正交规范集 $\{\varphi_n\}$ | N | `d:normalizing-an-orthogonal-set` a0 等 3 个节点持有者 | 150 字符，shared-quotes n_holders=7 |
| S27 | 35 | 「Thus, we have」 | — | — | 13 字符，纯引导词，见第三节 |
| S28 | 37–39 | 公式块 $\varphi_0 = 1/\sqrt{2\pi}$、$\varphi_{2n-1}$、$\varphi_{2n}$ | N | `d:normalized-trigonometric-system` a0 | 198 字符 |
| S29 | 41 | 15.13 将证明有限维欧氏空间必有正交基 | N | `d:existence-of-an-orthogonal-basis-deferred` a0 | 102 字符 |
| S30 | 41 | 「下一定理给出相对这种基的分量算法」 | — | — | 92 字符，过渡叙述；但见第三节里的不对称提示 |
| S31 | 43 | THEOREM 15.11 前提：有限维、维数 n、S 是正交基 | N | `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis` a0；`apostol:theorem-15-11-...` a0 | 159 字符 |
| S32 | 43 | 「若 x 表成基元素的线性组合，即」 | — | — | 81 字符，公式引导小句，见第三节 |
| S33 | 45–47 | 公式 (15.7) $x = \sum c_i e_i$ | N | `d:15-11-hypotheses-finite-dimension-and-orthogonal-basis` a1 | 50 字符 |
| S34 | 49 | 则相对有序基的分量由下式给出 | N | `apostol:theorem-15-11-...` a1；`d:orthogonal-basis-component-formula` a0 | 96 字符 |
| S35 | 51–53 | 公式 (15.8) $c_j = (x,e_j)/(e_j,e_j)$ | N | `d:orthogonal-basis-component-formula` a1 | 101 字符 |
| S36 | 55 | 特例：S 正交规范时每个 $c_j$ 由下式给出 | N | `apostol:theorem-15-11-...` a2；`d:orthonormal-basis-component-is-the-inner-product` a0 | 71 字符 |
| S37 | 57–59 | 公式 (15.9) $c_j = (x,e_j)$ | N | `d:orthonormal-basis-component-is-the-inner-product` a1 | 33 字符 |
| S38 | 61 | Proof：把 (15.7) 与 $e_j$ 作内积 | N | `d:15-11-proof-pair-with-basis-element` a0 | 81 字符 |
| S39 | 63–65 | 公式 $(x,e_j) = \sum c_i(e_i,e_j) = c_j(e_j,e_j)$ | N | `d:15-11-proof-pair-with-basis-element` a1 | 90 字符 |
| S40 | 67 | 因 $i \neq j$ 时 $(e_i,e_j) = 0$ | N | `d:15-11-denominator-nonzero-by-positivity` a0 | 105 字符（含 S41） |
| S41 | 67 | 这给出 (15.8)；$(e_j,e_j)=1$ 时给出 (15.9) | N | 同上 a0 | 同一 105 字符锚点 |
| S42 | 69 | 若基正交规范，(15.7) 可写成下式 | N | `d:orthonormal-expansion-formula` a0 | 93 字符 |
| S43 | 71–73 | 公式 (15.10) $x = \sum (x,e_i)e_i$ | N | `d:orthonormal-expansion-formula` a1 | 56 字符 |
| S44 | 75 | 「下一定理：正交规范基下内积可由分量算出」 | N | `apostol:parsevals-formula` a0；`d:parseval-formula-statement` a1 | 173 字符 |
| S45 | 77 | THEOREM 15.12 前提：有限维、维数 n、正交规范基 | N | `d:parseval-requires-an-orthonormal-basis` a0 | 154 字符 |
| S46 | 77 | 「则对 V 中每对元素 x, y 有」 | N | `apostol:parsevals-formula` a1 | 129 字符（跨 S45 尾与 S46） |
| S47 | 79–81 | 公式 (15.11) Parseval 公式 | N | `d:parseval-formula-statement` a0；`d:parseval-conjugate-comes-from-hermitian-symmetry` a0 | 116 字符 |
| S48 | 83 | 「特别地，当 $x = y$ 时」 | — | — | 37 字符，与 S51 重复，见第三节 |
| S49 | 85–87 | 公式 (15.12) $\|x\|^2 = \sum |(x,e_i)|^2$ | N | `d:parseval-norm-form` a0 | 70 字符 |
| S50 | 89 | Proof：把 (15.10) 与 y 作内积、用线性性得 (15.11) | N | `d:parseval-proof-pair-expansion-with-y` a0 | 148 字符 |
| S51 | 89 | 当 x = y 时 (15.11) 退化为 (15.12) | N | `d:parseval-norm-form` a1 | 48 字符 |
| S52 | 91 | Note：(15.11) 以 Parseval（约 1776–1836）命名 | N | `apostol:parsevals-formula` a2 | 140 字符 |

## 二 未覆盖且值得补建（建议 id / node_type / statement / 锚点逐字 + 实测字符数）

### 结论：不需要新节点，需要给已有节点补锚（S18 + S19）

**结论**：S19（$\int_0^{2\pi} u_n(x)u_m(x)\,dx = 0$）与其前提小句 S18（$m \neq n$）是本节唯一
一处**实质数学内容零覆盖**。但它不该建新节点——`d:trigonometric-orthogonal-system` 的 statement
已经逐字断言了这件事（"satisfy the integral of u_n u_m over [0, 2pi] equal to 0 whenever m is not n"），
缺的只是锚点。建新节点会与该节点重复，撞上审计 B 的删除测试（死因 5）。

**依据**：
- `statements.tsv` 中 `d:trigonometric-orthogonal-system` 的 statement 含上述断言；
- `anchors.tsv` 该节点仅 2 条锚点：a0 = 126 字符的 $u_0/u_{2n-1}/u_{2n}$ 定义式（源第 20 行）、
  a1 = 86 字符的 "so S is an orthogonal set..."（源第 29 行）。两条都不含 S18/S19；
- 全表检索：`grep -F "u _ {m}"` 在 anchors.tsv / edges.tsv / pool.tsv / statements.tsv 全部为 0 命中；
  `grep -ci "orthogonality relation"` 三表均 0。即**全图没有任何一条引文落在源第 23 行或第 26 行**。

**死因编号**：6（引文不支撑断言）。该节点断言「积分为零」，而它的两条锚点分别给的是
「函数的定义式」与「所以 S 是正交集这个结论」——正是盲区 **C 型**（把端点当中间步骤）叠加
**D 型**（覆盖范围不足）：定义式 + 结论句之间那一步「为什么正交」正好被跳过。

**建议动作**：FIX —— 给 `d:trigonometric-orthogonal-system` 追加两条锚点，均 file = `15.11.md`：

| 拟加锚点 | 逐字内容 | 实测字符数 | 校验 |
|---|---|---|---|
| a2 | `If $m \neq n$ , we have the orthogonality relations` | **51** | 源第 23 行整行，不跨行 |
| a3 | `\int_ {0} ^ {2 \pi} u _ {n} (x) u _ {m} (x) d x = 0,` | **52** | 源第 26 行整行，不跨行 |

字符数是我用 Python `len()` 对 `source/apostol-ch15/15.11.md` 第 23、26 行实测的（未转义前），
两者都在 SPEC 的 30–200 界内。两行的 `n_sentence_end` 均为 0（`source-lines.tsv`），
所以它们各自是完整一行、不含多句，不构成过报通道 b。

**置信度**：高（0.9）。不确定点：两条锚点该挂在 `d:trigonometric-orthogonal-system` 还是新开一个
「三角系统的正交关系」子节点，取决于主控对 D3 层粒度的口径；我倾向前者，因为该节点的
statement 已经把定义式、正交关系、独立性三步写在一起了，拆分会造成兄弟冗余。
另：a2 单独看是残句（"we have the orthogonality relations" 后面接公式块），
若 SPEC 要求锚点自足可只加 a3，那一条已足以支撑「积分为零」的断言。

## 三 未覆盖但不值得建（一行一条 + 理由：过渡叙述 / 重复 / 纯记号）

- **S5**（71 字符，源 5 行）"The next theorem shows a relation between orthogonality and dependence." —— 过渡叙述：它只宣告下一条定理要讲什么，全部内容已在 S6/S7 的定理本体里。不进 SPEC。
- **S10**（26 字符，源 15 行）"where each $x_{i} \in S$ ." —— 纯记号约束，且 **26 字符低于 SPEC 的 30 下界**，无法独立作锚点。内容已被 S8（"orthogonal set of nonzero elements in V" + "elements of S"）含住。不进 SPEC。
- **S27**（13 字符，源 35 行）"Thus, we have" —— 纯引导词，远低于 30 下界。不进 SPEC。
- **S30**（92 字符，源 41 行）"The next theorem shows how to compute the components of an element relative to such a basis." —— 过渡叙述，内容已在 S34/S35（分量公式本体）。**但记一个不对称**：与它同型的 S44（15.12 的引导句，173 字符）却被 `apostol:parsevals-formula` a0 与 `d:parseval-formula-statement` a1 两处收作锚点。两句功能完全相同，一句收一句不收，说明 D3 层对「引导句是否算锚点」没有统一口径。我建议统一为**不收**（见第五节抽查三），而不是为 S30 补锚。
- **S32**（81 字符，源 43 行）"If an element $x$ is expressed as a linear combination of the basis elements, say" —— 公式引导小句，内容即 S33 的 (15.7)，已有锚点。不进 SPEC。
- **S48**（37 字符，源 83 行）"In particular, when $x = y$ , we have" —— 与 S51（"When x = y, Equation (15.11) reduces to (15.12)."，48 字符，已被 `d:parseval-norm-form` a1 收作锚点）重复；S51 是同一件事的完整表述且更长。不进 SPEC。

以上 6 句合计：过渡叙述 3 句（S5 S30 S32）、纯记号／引导词 2 句（S10 S27）、重复 1 句（S48）。
死因编号 0（无死因），建议动作一律「不进 SPEC」。置信度高（0.9）。
不确定点：S30 的不对称是否要反过来处理（即给 S30 补锚以与 S44 对齐）由主控裁定；
我的判据是「引导句不携带数学内容」，按这条判据 S44 那两条锚点才是该撤的一方。

## 四 锚点集合与 sections 集合的不一致（若有）

两个集合确实对不上，**明确报出**：

- 集合 A =「`anchors.tsv` 中 file 列为 `15.11.md` 的锚点所属节点」，去重后 **36 个**；
- 集合 B =「`nodes.tsv` 中 sections 含 `15.11` 的节点」，**47 个**；
- A − B = 1 个，B − A = 12 个。

### A − B：有 15.11 锚点、但 sections 不含 15.11（**1 个，是真错，需 FIX**）

**结论**：`d:normalized-element-has-norm-one` 的 sections 是 `15.10,15.13`，却持有一条
file = `15.11.md` 的锚点。
**依据**：`nodes.tsv`（`data/nodes-D4.jsonl:19`，sections=`15.10,15.13`）与 `anchors.tsv` 第 500 行
（a2，75 字符，`An orthogonal set is called orthonormal if each of its elements has norm 1.`，file=15.11.md）。
**死因编号**：0（无死因，仅记账错）。
**建议动作**：FIX —— sections 补 `15.11`。
**置信度**：高（0.95）。不确定点：也可反向 FIX（该 a2 锚点其实是 15.11 的正交规范集定义，
而该节点讲的是「$y/\|y\|$ 范数为 1」，靠的是 15.10 的齐性与正定性两条锚点；a2 更像**借来的**
定义句 —— 见第五节口径，B 型）。两种修法主控择一，但**不能两者都不做**。

### B − A：sections 含 15.11、但无 15.11 锚点（**12 个，其中 10 个合法**）

- 合法 10 个：`L2:read-off-a-coefficient-by-one-pairing`、`L2:independence-by-killing-all-but-one-coefficient`、`L2:testing-infinite-objects-through-finite-windows`、`L2:count-saturation-upgrades-a-necessary-condition-to-a-sufficient-one`、`L2:orthogonalization-as-a-dimension-counter`、`L2:counts-survive-the-procedures-that-destroy-the-objects`、`L2:orthogonalizing-first-decouples-the-coefficients`、`L2:self-orthogonality-forces-zero-the-uniqueness-engine`、`L2:orthogonal-splitting-makes-the-norm-additive`、`L2:orthonormal-systems-make-approximation-incremental`。依据：`README.md` 第 21–22 行明列 L2 的 24 个节点**按设计无锚点**，sections 由成员汇总而来。死因 0，建议动作「保留」。
- `apostol:cx-l2-sequence-space-is-an-infinite-dimensional-euclidean-space`：sections=`15.12,15.10,15.11`，3 条锚点全在 `15.12-exercises.md`。其 statement 明确说「Theorem 15.10 of 15.11 apply with no finite-dimension hypothesis, while Theorems 15.11 and 15.12 of that section do assume finite dimension」——它是**引用**本节定理而非取材本节，sections 列 15.11 合理。死因 0，建议动作「保留」。
- `d:positivity-axiom-forbids-nonzero-self-orthogonal-elements`：sections=`15.10,15.11`，2 条锚点都在 `15.10.md`（35 行的 $(x,x)>0$、42 行的正定性）。它是 `d:zero-is-the-only-element-orthogonal-to-itself` 的子节点，后者才带 15.11 锚点（第 488 行，98 字符）。这属于**内容合法留在 15.10、只在 15.11 被用到**，不是缺口。死因 0，建议动作「保留」，置信度中（0.8），不确定点：sections 里列 15.11 是「被用到」还是「取材」，全图口径未定，我未跨节核。

## 五 本节现有锚点抽查（挑 3 条，按盲区五型 A/B/C/D/E 逐型检查）

### 抽查一：`d:parseval-requires-an-orthonormal-basis`（2 条锚点，`anchors.tsv` 455–456 行）

statement 断言三件事：(i) 15.12 要求正交**规范**基，严格强于 15.11 的正交基；
(ii) 原因可追溯——证明把 (15.10) 与 y 配对，而 (15.10) 本身只在正交规范下成立；
(iii) **若只用正交基，类比公式会带 $1/(e_i,e_i)$ 因子**。

- **A 型（变量/对象错）**：无。a0 引 THEOREM 15.12 的前提（154 字符），a1 引 THEOREM 15.11 的前提（159 字符），对象与 (i) 的两个比较项精确对应。
- **B 型（借用引文）**：a1 是共享引文——`shared-quotes.tsv` 该 159 字符 quote 的 n_holders=5，含 `node:apostol:theorem-15-11-components-...`。但此处**合法**：该节点的断言本身就是「15.12 vs 15.11 的前提对比」，引对照方的前提句是必需的，不是搭便车。
- **C 型（端点冒充中间步）**：无明显问题，两条锚点给的都是前提而非结论。
- **D 型（覆盖不足）—— 命中**。断言 (iii) 那个反事实（"would carry factors 1/(e_i, e_i)"）在两条锚点里毫无对应文字，源文中也没有这句话；它是模型从 (15.8) 推出来的（推理正确：由 $c_j = (x,e_j)/(e_j,e_j)$ 代入 (15.7) 确实得出带 $1/(e_i,e_i)$ 的形式）。而 `nodes.tsv` 中该节点 **origin 列为空**，不是 `model`。对照同层的 `d:15-11-denominator-nonzero-by-positivity` 与 `d:parseval-conjugate-comes-from-hermitian-symmetry` 都标了 `origin: model`，这一个没标。
- **E 型（整句无锚）**：断言 (ii) 的「(15.10) 只在正交规范下成立」也无本节点锚点支撑，但它由 T2 覆盖——该节点发出的 `contrasts` 边（`edges-D3.jsonl:167`）与 `d:orthonormal-expansion-formula` 的锚点（93 字符，源 69 行）合起来说明了这点，属记账位置问题而非缺内容。

**结论**：锚点未被篡改、数学无误，但断言 (iii) 越出了锚点覆盖范围且未标 origin。
**依据**：`anchors.tsv` 455–456 行；`nodes.tsv`（`data/nodes-D3.jsonl:65`，origin 空）；`statements.tsv` 同 id。
**死因编号**：6（引文不支撑断言，D 型）；同时触碰硬禁令 3 的边缘（模型内容未标 origin）。
**建议动作**：FIX —— 给该节点补 `origin: model`，或把 (iii) 移出 statement。**不必 KILL**：(i)(ii) 都有据，节点本身有价值。
**置信度**：中高（0.85）。不确定点：D3 层是否允许「statement 含模型推论但 origin 留空」，我没查 SPEC 原文，只做了同层对照。

### 抽查二：`d:15-11-denominator-nonzero-by-positivity`（2 条锚点，449–450 行，origin=model）

statement：从 $c_j(e_j,e_j) = (x,e_j)$ 到 (15.8) 需要 $(e_j,e_j) \neq 0$；两段论证——基元素不能是 $O$（含 $O$ 的集合相依），非零元由正定性得 $(e_j,e_j) > 0$。

- **A 型**：无。变量 $e_j$、$(e_j,e_j)$ 与源文一致。
- **B 型**：a0（105 字符，源 67 行）n_holders=3，另两个持有者是 `edges-D3.jsonl:148/156`，都是该节点自己发出的边，非跨节点借用。合法。
- **C 型 —— 命中（轻）**。a0 是 "since $(e_i, e_j) = 0$ if $i \neq j$ . This implies (15.8), and when $(e_j, e_j) = 1$ , we obtain (15.9)."，给的是**结论**（这推出 15.8），而断言讲的是**为什么分母不为零这个机制**。Apostol 原文对此确实沉默（statement 自己也说 "Apostol does not remark on it"），所以锚点只能落在结论句上。
- **D 型 —— 命中（已缓解）**。两段论证的第一段「含 $O$ 的集合相依」在本节点两条锚点里都没有；a1（35 字符）来自 `15.10.md` 的 $(x,x)>0$，只覆盖第二段。查 `pool.tsv` 与该节点发出的边，未见指向「零元使集合相依」的 requires 边，故第一段在池内无 T2/T3 支撑。**但该节点 origin=model，把推理标成了模型内容，这就把 D 型降级为「已声明的模型推论」而非虚假归属。**
- **E 型**：无整句完全无锚。

**结论**：C 型 + D 型都成立，但因 origin=model 已如实声明，不构成死因。
**依据**：`anchors.tsv` 449–450 行；`nodes.tsv`（`data/nodes-D3.jsonl:62`，origin=model）。
**死因编号**：0（仅需 FIX）。
**建议动作**：FIX（可选）—— 补一条 `requires` 边指向 15.07/15.08 层的「含零元的集合相依」节点，把第一段论证接进池。或直接「保留」。
**置信度**：中（0.75）。不确定点：我未跨节去找是否已存在「含 $O$ 的集合相依」这个节点，任务范围禁止跨节比较；主控若要 FIX 需自行定位 dst。

### 抽查三：`d:parseval-formula-statement`（2 条锚点，457–458 行）

statement 断言：(15.11) 把抽象内积化为分量的有限标量和，即正交规范基下任何欧氏空间都像 $V_n$ 那样算；**并特别指出共轭落在 y 因子上，即第二个自变量的分量上**。

- **A 型**：无。a0（116 字符，公式 (15.11) 本体）里 OCR 渲染为 `(\overline {{y , e _ {i}}})`，共轭确实盖在 $(y,e_i)$ 上，与断言的「共轭在第二自变量的分量上」一致。
- **B 型 —— 命中，且这次是有害的**。a1 是 173 字符的 "The next theorem shows that in a finite-dimensional Euclidean space with an orthonormal basis the inner product of two elements can be computed in terms of their components."，`shared-quotes.tsv` 显示 n_holders=2，另一持有者是父节点 `apostol:parsevals-formula`（a0）。对**父节点**它是自然的引导句；对**本节点**它只重复了断言的前半（化为分量），对断言的重点——共轭位置——零贡献。这是典型的借用：子节点从父节点那里搬了一条引导句来凑锚点数。
- **C 型**：a1 兼具 C 型味道（引导句是「预告」，断言是「公式的性质」），但主要问题仍归 B 型。
- **D 型**：断言的后半（共轭位置）由 a0 独力支撑，且支撑成立，故 D 型不成立。
- **E 型**：无。

**结论**：a1 是无效锚点（B 型借用），删掉它断言仍然被 a0 完整支撑；留着它会让「2/2 锚点已验证」看起来比实际更充分。
**依据**：`anchors.tsv` 457–458 行；`shared-quotes.tsv` 中 173 字符 quote 一行，holders = `node:apostol:parsevals-formula,node:d:parseval-formula-statement`。
**死因编号**：6（引文不支撑断言，B 型）。
**建议动作**：FIX —— 撤掉 a1，或换成真正点名共轭的引文。本节能点名共轭的逐字文字**只有公式本体本身**（源第 80 行），源文正文从未用文字讨论共轭位置，所以更干净的修法是**只撤不换**，让该节点保留 1 条锚点。
**置信度**：高（0.9）。不确定点：SPEC 是否要求每节点 ≥2 锚点；若要求，则只能保留 a1 并在 statement 里降低共轭那句的份量。

## 顺带发现

- `unverified-model-quotes.tsv` 里唯一涉及 15.11 的一条是 `edges-X.jsonl:121`（`apostol:orthogonal-elements generalizes strang:orthogonal-vectors`，ev 110 字符）；我逐字核过，该 quote 与源第 3 行首句完全一致，未被篡改。
- `d:15-10-nonzero-hypothesis-is-essential` 的 statement 用了反例 `{O, i, i + j}`；本节源文无此例，源文中 `i+j` 只出现在 `15.06.md:21`。该节点 origin 列为空，未标 model —— 疑似未登记的模型自算内容，属「丢弃与未覆盖清单」§三残留风险的同类。
- `d:trigonometric-system-norms` 的 S22/S23 只靠边 evidence 覆盖，而 `d:trigonometric-orthogonal-system` 的锚点起点恰好跳过这两句 —— 同一源行的两半分别落在节点锚点与边证据上，是过报通道 c 的一个现成实例。
- 本节 6 个 A1 节点（`apostol:orthogonal-elements` 等）落在 `data/inherited/nodes-A1.jsonl`，按清单 §六 该文件 A、B 两判全空；本轮我只核了锚点与断言的对应，未做审计 A/B 的判据。
