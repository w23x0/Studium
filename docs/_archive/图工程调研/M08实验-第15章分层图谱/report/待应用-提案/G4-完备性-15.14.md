# 15.14 覆盖完备性

## 〇 结论速览

本节按句切共 **54 句**（含标题 1 句、公式块 6 句、图注 1 句；不按行号，见下）。

- 已覆盖 **48** = 节点自身锚点直接持有 **19** + 只被边 evidence 持有 **6** + 合法下移到
  part-of 后代 **23**
- 未覆盖 **6**（S5 / S17 / S18 / S21 / S22 / S41）
- 未覆盖中**值得补建 0 个新节点**；有 **2 处值得作为 FIX 补锚点**（挂到已有节点，不新建）

判据说明：不按行号比对。source-lines.tsv 里 `apostol-ch15/15.14.md` 有 12 行的
`n_sentence_end ≥ 2`（第 3/5/7/9/11/13/18/30/36/42/50/58 行），其中第 50 行一行含 6 句、
第 36 行含 5 句、第 18 行含 4 句。按行号会严重高报（过报通道 b），故全部按句核。

**本节没有新的内容缺口。** 7 句未覆盖全部是过渡叙述、图注、或公式块被切碎后的
无信息残片（`That is, we have` 之类）。`report/丢弃与未覆盖清单.md` 未记 15.14 的任何
放弃项，本轮也确认无需追加。

## 一 逐句覆盖表

状态记法：**锚点** = 某节点自身 anchors 逐字含该句；**边** = 只被某条边的 evidence 含；
**下移** = 只被 part-of 后代（D 层）的锚点持有，父节点不直接持有；**未覆盖** = 三者皆无。
「源行」列给出该句所在的源文件行号（仅供定位，不作覆盖判据）。

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 1 | 节标题 Orthogonal complements. Projections | 边 | 无节点锚点；标题词见多条 evidence | anchors.tsv 无 file=15.14.md 且含 "Orthogonal complements" 的行 |
| S2 | 3 | Let V be a Euclidean space and let S be a finite-dimensional subspace. | 边 | `apostol:best-approximation-problem` 的 3 条 requires 边（ev 70 字符） | edges.tsv ev_file=15.14.md, ev_charlen=70 |
| S3 | 3 | We wish to consider … approximation problem: … distance from x as small as possible. | 锚点 | `apostol:best-approximation-problem` anchor#0（165） | anchors.tsv |
| S4 | 3 | The distance between two elements x and y is defined to be the norm ‖x−y‖. | 锚点 | `apostol:distance-between-elements` anchor#0（79）；后代 `d:distance-is-the-norm-of-the-difference` anchor#0（81） | anchors.tsv 两行 |
| S5 | 5 | Before discussing this problem in its general form, we consider a special case, illustrated in Figure 15.2. | **未覆盖** | — | 全表 grep `Before discussing this problem`：anchors 0 行、edges 0 行 |
| S6 | 5 | Here V is the vector space V₃ and S is a two-dimensional subspace, a plane through the origin. | 边 | `apostol:best-approximation-problem` requires `apostol:v-n-space`（ev 99） | edges.tsv；anchors 无此句 |
| S7 | 5 | Given x in V, the problem is to find, in the plane S, that point s nearest to x. | 下移 | `d:trivial-case-when-x-already-lies-in-s` anchor#1（80） | anchors.tsv；parent `apostol:best-approximation-problem` 不持有 |
| S8 | 7 | If x∈S, then clearly s=x is the solution. | 下移 | `d:trivial-case-when-x-already-lies-in-s` anchor#0（52） | anchors.tsv |
| S9 | 7 | If x not in S, the nearest point s is obtained by dropping a perpendicular. | 下移 | `d:nearest-point-by-dropping-a-perpendicular` anchor#0（112） | anchors.tsv |
| S10 | 7 | This simple example suggests an approach … and motivates the discussion that follows. | 下移 | `d:nearest-point-by-dropping-a-perpendicular` anchor#1（120） | anchors.tsv |
| S11 | 9 | DEFINITION. Let S be a subset of a Euclidean space V. | 锚点 | `apostol:orthogonal-to-a-set` anchor#1（41）；`d:s-perp-is-defined-for-an-arbitrary-subset` anchor#0（53，带 DEFINITION 前缀） | anchors.tsv |
| S12 | 9 | An element in V is said to be orthogonal to S if it is orthogonal to every element of S. | 锚点 | `apostol:orthogonal-to-a-set` anchor#0（88）；另 2 持有者 | shared-quotes.tsv 15.14.md/88/4 |
| S13 | 9 | The set of all elements orthogonal to S is denoted by S⊥ and is called "S perpendicular." | 锚点（**截断**） | `apostol:s-perp-notation` anchor#0（79）只到 `and is called` | 见第五节抽查一：应延到 98 字符 |

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S14 | 11 | It is a simple exercise to verify that S⊥ is a subspace of V, whether or not S itself is one. | 锚点 | `apostol:orthogonal-complement-is-a-subspace` anchor#0（102）；共 10 持有者 | shared-quotes.tsv 15.14.md/102/10 |
| S15 | 11 | In case S is a subspace, then S⊥ is called the orthogonal complement of S. | 锚点 | `apostol:orthogonal-complement` anchor#0（83）；共 6 持有者 | shared-quotes.tsv 15.14.md/83/6 |
| S16 | 13 | EXAMPLE. If S is a plane through the origin …, then S⊥ is a line through the origin perpendicular to this plane. | 锚点 | `apostol:orthogonal-complement` anchor#1（135） | anchors.tsv |
| S17 | 13 | This example also gives a geometric interpretation for the next theorem. | **未覆盖** | — | grep `This example also gives a geometric`：anchors 0、edges 0 |
| S18 | 16 | FIGURE 15.2 Geometric interpretation of the orthogonal decomposition theorem in V₃. | **未覆盖** | — | grep `FIGURE 15.2`：anchors 0、edges 0 |
| S19 | 18 | THEOREM 15.15. ORTHOGONAL DECOMPOSITION THEOREM. Let V be a Euclidean space and let S be a finite-dimensional subspace of V. | 锚点 | `apostol:theorem-15-15-orthogonal-decomposition` anchor#0（124） | anchors.tsv |
| S20 | 18 | Then every element x in V can be represented uniquely as a sum of two elements, one in S and one in S⊥. | 锚点 | 同上 anchor#1（111，无末点）；`x:orthogonal-decomposition-of-the-ambient-space` anchor#0（113，含末点） | anchors.tsv 两行 |
| S21 | 18 | That is, we have | **未覆盖** | — | grep `That is, we have`：anchors 0、edges 0 |
| S22 | 21 | 公式 (15.16)：x = s + s⊥, where s∈S and s⊥∈S⊥ | **未覆盖** | — | grep `\tag{15.16}`：anchors 0、edges 0；全表含 `x = s + s` 的 quote 只有 (15.19) 与 15.15.md 的复述 |
| S23 | 24 | Moreover, the norm of x is given by the Pythagorean formula | 锚点 | `apostol:pythagorean-formula-for-orthogonal-decomposition` anchor#0（61） | anchors.tsv |
| S24 | 27 | 公式 (15.17)：‖x‖² = ‖s‖² + ‖s⊥‖² | 下移 | `d:pythagorean-cross-terms-vanish-by-orthogonality` anchor#1（67） | anchors.tsv |
| S25 | 30 | Proof. First we prove that an orthogonal decomposition (15.16) actually exists. | 锚点 | `apostol:orthogonal-decomposition` anchor#0（72）；`d:orthogonal-decomposition-existence` anchor#0 同句 | shared-quotes.tsv 15.14.md/72/4 |
| S26 | 30 | Since S is finite-dimensional, it has a finite orthonormal basis, say {e₁,…,eₙ}. | 下移 | `d:finite-dimensionality-supplies-a-finite-orthonormal-basis` anchor#0（96） | anchors.tsv；共 8 持有者 |
| S27 | 30 | Given x, define the elements s and s⊥ as follows: | 下移 | `d:orthogonal-decomposition-existence` anchor#1（58） | anchors.tsv |
| S28 | 33 | 公式 (15.18)：s = Σ(x,eᵢ)eᵢ, s⊥ = x − s | 下移 | `d:s-defined-as-the-sum-of-projections-along-the-basis-elements` anchor#0（83） | anchors.tsv |

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S29 | 36 | Note that each term (x,eᵢ)eᵢ is the projection of x along eᵢ. | 下移 | `d:s-defined-as-the-sum-of-projections-along-the-basis-elements` anchor#1（151，含 S30） | anchors.tsv；另有一条 requires 边 ev 76 字符 |
| S30 | 36 | The element s is the sum of the projections of x along each basis element. | 下移 | 同上 anchor#1 的后半；另 `apostol:projection-along-an-element` part-of 边 ev（74） | anchors.tsv + edges.tsv |
| S31 | 36 | Since s is a linear combination of the basis elements, s lies in S. | 下移 | `d:s-lies-in-s-because-it-is-a-linear-combination-of-the-basis` anchor#0（67） | anchors.tsv；共 4 持有者 |
| S32 | 36 | The definition of s⊥ shows that Equation (15.16) holds. | 下移 | 同上 anchor#1（64） | anchors.tsv |
| S33 | 36 | To prove that s⊥ lies in S⊥, we consider the inner product of s⊥ and any basis element eⱼ. | 边 | `d:s-perp-is-orthogonal-to-each-basis-element` part-of 边 ev（124） | edges.tsv；anchors.tsv 无此句 |
| S34 | 39 | 公式：(s⊥,eⱼ) = (x−s,eⱼ) = (x,eⱼ) − (s,eⱼ). | 下移 | `d:s-perp-is-orthogonal-to-each-basis-element` anchor#0（72） | anchors.tsv |
| S35 | 42 | But from (15.18), we find that (s,eⱼ)=(x,eⱼ), so s⊥ is orthogonal to eⱼ. | 下移 | 同上 anchor#1（92） | anchors.tsv |
| S36 | 42 | Therefore s⊥ is orthogonal to every element in S, which means s⊥∈S⊥. | 下移 | `d:orthogonality-to-the-basis-extends-to-all-of-s` anchor#0（100） | anchors.tsv；共 4 持有者（含 `x:orthogonality-of-the-residual`） |
| S37 | 44 | Next we prove that the orthogonal decomposition (15.16) is unique. | 锚点 | `apostol:orthogonal-decomposition` anchor#1（66）；`d:orthogonal-decomposition-uniqueness` anchor#0 同句 | shared-quotes.tsv 15.14.md/66/5 |
| S38 | 44 | Suppose that x has two such representations, say | 下移 | `d:orthogonal-decomposition-uniqueness` anchor#1（50） | anchors.tsv |
| S39 | 47 | 公式 (15.19)：x = s + s⊥ and x = t + t⊥ | 下移 | `d:uniqueness-reduces-to-a-single-vanishing-difference` anchor#1（77） | anchors.tsv |
| S40 | 50 | where s and t are in S, and s⊥ and t⊥ are in S⊥. | 边 | `apostol:orthogonal-decomposition` requires `apostol:subspace` 的 ev（83） | edges.tsv；anchors.tsv 无此句 |
| S41 | 50 | We wish to prove that s = t and s⊥ = t⊥. | **未覆盖** | — | grep `We wish to prove that $s = t$`：anchors 0、edges 0 |
| S42 | 50 | From (15.19), we have s−t = t⊥−s⊥, so we need only prove that s−t = O. | 下移 | `d:uniqueness-reduces-to-a-single-vanishing-difference` anchor#0（96） | anchors.tsv |
| S43 | 50 | But s−t∈S and t⊥−s⊥∈S⊥ so s−t is both orthogonal to and equal to t⊥−s⊥. | 下移 | `d:the-difference-lies-in-both-s-and-s-perp` anchor#0（155） | anchors.tsv |

| 句号 | 源行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S44 | 50 | Since the zero element is the only element orthogonal to itself, we must have s−t = O. | 下移 | `d:uniqueness-concludes-by-self-orthogonality` anchor#0（91）；另 `d:zero-is-the-only-element-orthogonal-to-itself` anchor#1 同句 | shared-quotes.tsv 15.14.md/91/5 |
| S45 | 50 | This shows that the decomposition is unique. | 下移 | `d:uniqueness-concludes-by-self-orthogonality` anchor#1（44） | anchors.tsv |
| S46 | 52 | Finally, we prove that the norm of x is given by the Pythagorean formula. We have | 下移 | `d:pythagorean-expansion-of-the-squared-norm` anchor#1（83，含 "We have"） | anchors.tsv |
| S47 | 55 | 公式：‖x‖² = (x,x) = (s+s⊥,s+s⊥) = (s,s)+(s⊥,s⊥) | 下移 | `d:pythagorean-expansion-of-the-squared-norm` anchor#0（98） | anchors.tsv |
| S48 | 58 | the remaining terms being zero since s and s⊥ are orthogonal. | 锚点 | `apostol:pythagorean-formula-for-orthogonal-decomposition` anchor#1（93，含 S49） | anchors.tsv；共 4 持有者 |
| S49 | 58 | This proves (15.17). | 锚点 | 同上 anchor#1 的后半 | anchors.tsv |
| S50 | 60 | DEFINITION. Let S be a finite-dimensional subspace of a Euclidean space V, and let {e₁,…,eₙ} be an orthonormal basis for S. | 锚点 | `apostol:projection-on-a-subspace` anchor#1（132） | anchors.tsv；共 3 持有者 |
| S51 | 60 | If x∈V, the element s defined by the equation | 锚点 | `x:projection-onto-a-subspace` anchor#0（119，跨 S50/S51） | anchors.tsv |
| S52 | 63 | 公式：s = Σ(x,eᵢ)eᵢ | 边 | `apostol:projection-on-a-subspace` requires `apostol:inner-product` ev（44） | edges.tsv；同 quote 另有 1 条 D4 边 |
| S53 | 66 | is called the projection of x on the subspace S. | 锚点 | `apostol:projection-on-a-subspace` anchor#0（51，无末点）；`d:projection-on-a-subspace-is-defined-through-an-orthonormal-basis` anchor#1（53，含末点） | anchors.tsv 两行 |
| S54 | 68 | We prove next that the projection of x on S is the solution to the approximation problem stated at the beginning of this section. | 锚点 | `apostol:best-approximation-problem` anchor#1（133） | anchors.tsv；共 4 持有者 |
| S55 | — | （文末空行，不计） | — | — | — |

S55 是文末空行，不计入。有效计数为 S1–S54，共 54 句，与速览一致。

## 二 未覆盖且值得补建

**建议新建节点数：0。** 6 句未覆盖里没有一句承载新的数学内容——它们的数学内容
都已由别的句子（且已建节点）说过，缺的只是锚点这一记账动作。

因此本节给出的是 **2 条 FIX（补锚点到已有节点）**，不是新节点提案。

### FIX-1 给 `apostol:theorem-15-15-orthogonal-decomposition` 补公式 (15.16) 的锚点

- 结论：定理 15.15 的**结论公式本身**（S22）在全图 921 条锚点、1433 条边的 quote 里
  一次都没出现。已有的两条锚点只覆盖假设句（S19）与散文式结论句（S20）；
  形式化的 `x = s + s⊥, s∈S, s⊥∈S⊥` 无任何持有者。
- 依据：`grep '\tag{15.16}'` 在 anchors.tsv 与 edges.tsv 均 0 行；含 `x = s + s` 的
  quote 只有 (15.19)（`d:uniqueness-reduces-to-a-single-vanishing-difference`）与
  15.15.md 里的复述句，均不是 (15.16)。
- 死因编号：**0**（无死因，仅需 FIX）。不是死因 6：现有锚点确实支撑现有断言，
  只是公式形式未被记账。
- 建议动作：**FIX**。给该节点追加 anchor#2，file = `15.14.md`，逐字 quote（去掉 `\tag`
  以避开 200 界无必要，这里未超界，两版都可用；建议用不含 tag 的 111 字符版）：

  `x = s + s ^ {\perp}, \quad \text { where } \quad s \in S \quad \text { and } \quad s ^ {\perp} \in S ^ {\perp}.`

  **自测字符数 = 111**（含 `\tag{15.16}` 的完整行为 122）。已用 Python 逐字确认两版
  都是 15.14.md 第 21 行的连续子串、不跨行。
- 置信度：高（机械可判）。不确定点：主控可能认为「公式块不必单独建锚」是本实验的
  既定约定——本节 6 个公式块里有 5 个（15.17/15.18/15.19 及两个无编号式）都有锚点，
  只有 (15.16) 没有，所以我判它是遗漏而非约定。

### FIX-2 给 `d:orthogonal-decomposition-uniqueness` 补证明目标句（S41）的锚点

- 结论：唯一性证明的**目标陈述**「We wish to prove that s = t and s⊥ = t⊥.」无任何持有者。
  该节点现有两条锚点是「Next we prove … is unique.」（宣告）与「Suppose that x has two
  such representations, say」（设定），恰好跳过了「要证什么」。
- 依据：`grep 'We wish to prove that $s = t$'` 在 anchors.tsv 与 edges.tsv 均 0 行；
  该节点 anchors.tsv 两行分别为 66 与 50 字符。
- 死因编号：**0**（仅 FIX）。
- 建议动作：**FIX**。追加 anchor，file = `15.14.md`，逐字：

  `We wish to prove that $s = t$ and $s^{\perp} = t^{\perp}$ .`

  **自测字符数 = 59**（在 30–200 界内）。已 Python 确认是第 50 行的连续子串、不跨行。
  注意第 50 行 `n_sentence_end = 6`，必须按句截取，不能整行入锚。
- 置信度：高。不确定点：也可挂到 `d:uniqueness-reduces-to-a-single-vanishing-difference`
  （它的断言就是「只需证 s−t=O」，这句是它的前一步）；我倾向挂 parent，因为
  「要证 s=t 且 s⊥=t⊥」是唯一性命题的目标，不是化归动作本身。二选一由主控裁。

## 三 未覆盖但不值得建

- **S5**（第 5 行）「Before discussing this problem in its general form, we consider a special case, illustrated in Figure 15.2.」——纯过渡叙述（宣告下文要举特例），无数学内容；它引出的 V₃ 特例本身已由 S6–S10 覆盖（`apostol:best-approximation-problem` 及其两个 D 层后代）。建议动作：不进 SPEC。
- **S17**（第 13 行）「This example also gives a geometric interpretation for the next theorem.」——纯过渡叙述（预告定理 15.15），无独立数学内容；「平面的正交补是垂线」这一几何内容已由 S16 落在 `apostol:orthogonal-complement` anchor#1（135 字符）。建议动作：不进 SPEC。
- **S18**（第 16 行）图注「FIGURE 15.2 Geometric interpretation of the orthogonal decomposition theorem in V₃.」——图注，且第 15 行是无法读取的图片链接（`images/308ee897….jpg`）；建节点会得到一个内容为空、只指向不可读图像的节点。建议动作：不进 SPEC（**这是一处真实但不可弥补的缺口**：Figure 15.2 承载的几何直观在纯文本图谱里必然丢失，与 15.16-exercises 第 5 题的 OCR 缺陷同类，建议由主控记入 `丢弃与未覆盖清单.md` 第二节）。
- **S21**（第 18 行）「That is, we have」——纯衔接短语（16 字符），低于 SPEC 的 30 字符下界，无法单独构成合法锚点。建议动作：不进 SPEC。
- **S22**（第 21 行）公式 (15.16)——**内容值得记账，但不值得建新节点**：它就是 `apostol:orthogonal-decomposition` 与 `apostol:theorem-15-15-orthogonal-decomposition` 的断言的形式写法，另建节点会触发死因 5（层塌缩／同义改写）。改为 FIX-1 补锚点。
- **S41**（第 50 行）「We wish to prove that s = t and s⊥ = t⊥.」——同上：证明目标句，独立建节点会与 `d:orthogonal-decomposition-uniqueness` 复述（正是审计 B 判死的 28 个 D 层节点里「证明的纯语法切分」那一类，6 个）。改为 FIX-2 补锚点。

## 四 锚点集合与 sections 集合的不一致

两个集合确实对不上，**明确报出**。

- 集合 A =「anchors.tsv 里 file 列为 `15.14.md` 的锚点持有者」：**41 个节点**（65 条锚点）
- 集合 B =「nodes.tsv 里 sections 含 `15.14` 的节点」：**54 个节点**
- 交集 40；A 独有 1；B 独有 14

依据：`awk -F'\t' '$3=="15.14.md"{print $1}' anchors.tsv | sort -u` 对
`awk -F'\t' '$4 ~ /15\.14/{print $1}' nodes.tsv | sort -u` 做 comm。

### 不一致 1：A 独有 1 个 —— 有 15.14 锚点但 sections 不含 15.14

`d:orthogonality-to-generators-extends-to-their-span`（`data/nodes-D4.jsonl:9`）
sections = `15.13`，parent = `d:orthogonalization-conclusion-a-orthogonal-to-earlier-span`，
但它的 anchor#1 是 15.14.md 的 100 字符句
「Therefore $s^\perp$ is orthogonal to every element in $S$ , which means that $s^\perp \in S^\perp$ .」。

- 结论：**sections 字段漏填 15.14**。该节点的 statement 明写「Apostol uses this bridge
  twice: here, and again in Theorem 15.15 when passing from the basis elements e_j to all
  of S.」——即它自己声明跨 15.13 与 15.14 两节，锚点也确实各取一句，唯独 sections 只写了
  15.13。
- 依据：nodes.tsv 该行 sections 列；anchors.tsv 该节点两行（15.13.md/60、15.14.md/100）；
  statements.tsv 该行。
- 死因编号：**0**（元数据错，非内容错）。
- 建议动作：**FIX**，sections 改为 `15.13,15.14`。
- 副作用提示：该 100 字符句在 shared-quotes.tsv 里有 4 个持有者
  （本节点、`d:orthogonality-to-the-basis-extends-to-all-of-s`、
  `x:orthogonality-of-the-residual`、1 条 D4 边）。前两者是**同一句被两个 D 层节点共享**，
  一个讲「一般的张成桥」、一个讲「本证明里从 eⱼ 推到全 S」。我读过两条 statement，
  判定这是合法的 form B（抽象层级不同，不是复述），**不建议 KILL**。置信度中：
  真要定论需要连 15.13 的 parent 一起看，超出本任务范围。

### 不一致 2：B 独有 14 个 —— sections 含 15.14 但无 15.14 锚点

- 其中 **13 个是 L2 节点**（`L2:closure-certificate-for-subspacehood`、
  `L2:uniqueness-by-forcing-the-difference-to-zero`、
  `L2:read-off-a-coefficient-by-one-pairing`、
  `L2:construct-the-orthogonal-part-by-subtracting-projections`、
  `L2:expand-the-squared-norm-and-classify-the-cross-terms`、
  `L2:checking-against-a-set-compresses-to-checking-against-a-count`、
  `L2:the-size-of-the-best-approximation-is-fixed-by-the-subspace-dimension`、
  `L2:existence-costs-a-representation-uniqueness-is-free`、
  `L2:orthogonality-as-a-certificate-of-optimality`、
  `L2:peel-off-the-projection-to-manufacture-a-new-direction`、
  `L2:self-orthogonality-forces-zero-the-uniqueness-engine`、
  `L2:the-split-belongs-to-the-subspace-not-to-the-basis`、
  `L2:orthogonal-splitting-makes-the-norm-additive`）。
  按 tmp_index/README.md 第 21–22 行，24 个 L2 节点**设计上就无锚点**（靠 members 引用）。
  结论：**不是不一致，是设计如此**。建议动作：保留，不需处理。
- 剩下 **1 个是 `apostol:projection-along-an-element`**（`data/nodes-A2-s1.jsonl:7`），
  sections = `15.13,15.14`，但它的 3 条锚点**全在 15.13.md**（79 / 38 / 187 字符）。
  它的 statement 末句写「In 15.14 the same notion is applied to an orthonormal basis
  element, where (y, y) = 1 reduces the coefficient to (x, e_i).」——这句 **15.14 的内容
  没有 15.14 的锚点**，属盲区 E 型（整句无锚）。
  - 但它并非无据：该节点作 dst 收到
    `d:s-defined-as-the-sum-of-projections-along-the-basis-elements -requires->` 的边，
    evidence 是 15.14.md 的 76 字符「Note that each term $(x, e_{i})e_{i}$ is the
    projection of x along $e_{i}$ .」——**这是 TD，按四层证据池定义排除，不入它的池**。
    它自己发出的 part-of 边（→`apostol:projection-on-a-subspace`）evidence 是 15.14.md 的
    74 字符「The element s is the sum of the projections of x along each basis element.」，
    **这条是 T2，入池**。
  - 结论：sections 含 15.14 有 T2 支撑，**不是虚构出处**；但「(y,y)=1 使系数化为 (x,eᵢ)」
    这一具体化断言，T2 的那句只说「s 是各投影之和」，没说系数如何退化。
  - 死因编号：**6**（引文不支撑断言）——限于 statement 末句的后半。
  - 建议动作：**FIX**，给它补一条 15.14.md 锚点，用上面那条 76 字符句
    （`Note that each term $(x, e_{i})e_{i}$ is the projection of x along $e_{i}$ .`，
    **自测字符数 = 76**，已确认为第 36 行连续子串、不跨行）；同时把 statement 末句里
    「where (y, y) = 1 reduces the coefficient to (x, e_i)」改为标 `origin: model` 的推论，
    或直接删去——Apostol 在 15.14 从未写出这一步。
  - 置信度：中高。不确定点：15.13 是否另有一句写了这个退化（本任务不跨节核，**未核**）。

## 五 本节现有锚点抽查

抽查 3 条，每条按 A/B/C/D/E 五型逐型过。

### 抽查一：`apostol:s-perp-notation` anchor#0（15.14.md，79 字符）

引文：`The set of all elements orthogonal to S is denoted by $S^{\perp}$ and is called`
statement（两句）：①「所有正交于子集 S 的元素之集记作 S 加垂直上标，读作 'S perpendicular'。」
②「当 S 是子空间时，同一符号用于正交补。」

- **A 型（变量/对象错）**：无。S、S⊥ 对应正确。
- **B 型（借用引文）**：命中但**合法**。shared-quotes.tsv `15.14.md / 79 / 5`，持有者含
  `d:s-perp-is-defined-for-an-arbitrary-subset` 与 3 条边。该 D 层节点讲的是「S⊥ 对任意
  子集都有定义」，本节点讲的是记号本身——与已知清单里 `apostol:norm` / `apostol:norm-notation`
  同型（概念节点 + 记号节点共用定义句），判合法。
- **C 型（端点当中间步）**：无。
- **D 型（覆盖不足）**：**命中**。引文在 `and is called` 处截断，恰好把要读的那个名字
  「"S perpendicular."」切掉了。statement 第①句的后半（"读作 S perpendicular"）因此**无引文支撑**——
  这正是 D 型最典型的形态：引文看着对，但停在断言完成之前。
- **E 型（整句无锚）**：**命中**。statement 第②句（同一符号用于正交补）在本节点的
  1 条锚点里完全没有对应文本。支撑它的是 S15（`In case S is a subspace, then $S^{\perp}$
  is called the orthogonal complement of S.`），那句是 `apostol:orthogonal-complement`
  的锚点，不是本节点的。查 exposure.tsv：本节点 T1=1、T2=1、T3=0、池总 2、去重源行 1
  ——池里那条 T2 是 `requires apostol:orthogonal-to-a-set` 的 79 字符 evidence，与 anchor#0
  同一句，**不引入新源行**，所以第②句在四层池里也无据。

- 结论：D + E 双命中，两处都是「锚点少了一截」，不是内容错误。
- 依据：anchors.tsv 该行（79）；shared-quotes.tsv 15.14.md/79/5；exposure.tsv 该行；
  源文件 `source/apostol-ch15/15.14.md:9`。
- 死因编号：**6**（引文不支撑断言），程度轻。
- 建议动作：**FIX**。把 anchor#0 延长到整句（98 字符，含中文引号 U+201C/U+201D）：

  `The set of all elements orthogonal to S is denoted by $S^{\perp}$ and is called “S perpendicular.”`

  **自测字符数 = 98**，已 Python 确认为第 9 行连续子串、不跨行。再补 anchor#1 = S15 那
  83 字符句以支撑第②句（这会使 15.14.md/83 的持有者从 6 增到 7，属合法 form B）。
- 置信度：高。不确定点：延长后的 quote 含两个非 ASCII 弯引号，写回 JSONL 时须确认
  编码不被转成 ASCII 直引号，否则 check_graph 会判不匹配——**这一点我未核**（只读轮次）。

### 抽查二：`d:projection-does-not-depend-on-the-chosen-orthonormal-basis` 的 2 条锚点

`origin = model`，parent = `apostol:projection-on-a-subspace`，sections = 15.14。
statement：「不同的正交规范基给出同一个 s，因为定理 15.15 的唯一性把 x 的 S 分量钉死了。
Apostol 未评论此点，但没有它这个定义就是基依赖的。」
锚点：#0 = `Next we prove that the orthogonal decomposition (15.16) is unique.`（66）；
#1 = `Let $S$ be a finite-dimensional subspace of a Euclidean space $V$ , and let $\{e_1, \ldots, e_n\}$ be an orthonormal basis for $S$ .`（132）。

- **A 型**：无。
- **B 型**：**两条全部命中**。shared-quotes.tsv：15.14.md/66/5（另 4 个持有者含
  `apostol:orthogonal-decomposition`、`d:orthogonal-decomposition-uniqueness`）、
  15.14.md/132/3（另 2 个持有者 `apostol:projection-on-a-subspace`、
  `d:projection-on-a-subspace-is-defined-through-an-orthonormal-basis`）。
  即该节点**没有一条属于自己的引文**，两条都是从兄弟/父节点借来的。
- **C 型**：**命中**。anchor#0 是唯一性证明的**宣告句**，本节点的断言是「唯一性⇒
  投影与基无关」这一**推论**。引文给的是被引用的定理的标题式提法，不是推论的依据链。
- **D 型**：**命中**（最重的一处）。statement 的核心是「不同基给出同一个 s」；
  两条锚点里没有任何文字提到「不同的基」或「与基无关」——Apostol 全节确实没写。
  statement 自己也承认「Apostol does not remark on it」。
- **E 型**：等价于 D 型在此处的表现：核心断言整句无锚。

- 结论：这是一个**正确的模型推论，但两条锚点都在假装它有教材出处**。数学上无误
  （唯一性确实蕴含 S 分量与基无关），且 `origin=model` 已标注，故不触死因 3。
  真正的问题是锚点选择制造了虚假的出处印象。查 exposure.tsv：T1=2、T2=1、T3=0、
  池总 3、**去重后不同源行仅 2** ——那条 T2（`requires d:orthogonal-decomposition-uniqueness`，
  evidence 就是同一句 66 字符）不引入新源行，冗余度按「新引入源行数」计为 0。
- 依据：nodes.tsv `data/nodes-D4.jsonl:44`；anchors.tsv 两行；shared-quotes.tsv
  15.14.md/66/5 与 15.14.md/132/3；exposure.tsv 该行；statements.tsv 该行。
- 死因编号：**6**（引文不支撑断言）。**不是死因 1**：statement 没有量词过度断言；
  也不是死因 5：删掉它 parent 推不出「与基无关」，抽象增益成立。
- 建议动作：**FIX**（不 KILL）。二选一：
  (i) 清空 anchors，只保留 `requires d:orthogonal-decomposition-uniqueness` 这条边承担
  依据（该节点 origin 已是 model，无锚点是诚实的表示）；
  (ii) 保留 anchor#1（它至少给出「取定一个正交规范基」这一前提），删掉 anchor#0。
  我倾向 (i)：本图已有 28 个无锚点节点的先例（README 第 21 行），model 推论无锚不违规。
- 置信度：中高。不确定点：主控是否要求所有 D 层节点至少 1 条锚点——**该约定我未核**。

### 抽查三：`x:least-squares-is-a-projection` anchor#0（15.14.md，100 字符）

X 层跨教材节点，sections = `15.14,4.1`。
引文：`Given an element x in V, to determine an element in S whose distance from x is as small as possible.`
statement（两句）：①「当元素无法被现有生成元精确表示时，配对诱导的范数下的最佳表示由
投影到它们张成的子空间得到。」②「不可解的精确问题因此被换成一个可解的正交性条件。」

- **A 型**：无。x、S 对应正确。
- **B 型**：**不命中**。该 100 字符 quote 不在 shared-quotes.tsv 的 15.14.md 分组里
  （该分组无 charlen=100 且以 `Given an element x in V` 开头的行；charlen=100 的那条是
  `Therefore $s^\perp$ …`）。即本条锚点是独占的，非借用。
- **C 型**：**命中**。引文是**问题的提法**（approximation problem 的定义），
  statement 第①句断言的是**解**（最佳表示由投影给出）。把问题当成答案的引文——
  这正是 C 型的反向形态。Apostol 在 15.14 只在 S54 里**预告**了这个解
  （「We prove next that the projection … is the solution」），真正的证明在 15.15（定理 15.16）。
  而 S54 是 `apostol:best-approximation-problem` 与
  `d:projection-is-the-s-component-of-the-orthogonal-decomposition` 的锚点，不是本节点的。
- **D 型**：**命中**。引文只覆盖「求最近元素」，未覆盖「由投影得到」这一半。
- **E 型**：**命中**。statement 第②句（把精确问题换成正交性条件）在两条锚点里
  完全无对应文本：15.14.md 那条讲问题提法，4.1.md 那条（140 字符，
  `It contains the error $e = b - A x$ in the "least-squares" solution. …`）讲 Strang 侧的
  最小二乘背景，都没有「正交性条件」。Apostol 侧的正交性条件（Theorem 15.16 的证明）
  在 15.15.md。
- 四层池核查：exposure.tsv 该行 = `0 / 2 / 0 / 0 / 2 / 2`，即 **T2 = 0、T3 = 0**，
  池里只有那 2 条 T1 自身锚点。它发出的 3 条边（`is-a`→`x:best-approximation-by-the-projection`、
  `requires`→`x:orthogonality-of-the-residual`、`contrasts`→`x:solution-set-of-a-homogeneous-linear-condition`）
  全部 `origin=model` 且 **ev 为空**（edges.tsv ev_file 空、ev_charlen=0），
  所以 T2 塌陷不是「记账挂在边上」，是**确实没有证据**。

- 结论：C + D + E 三型命中，且四层池里无补救证据。断言在数学上正确，但**这个节点的
  引文完全不支撑它**。
- 依据：anchors.tsv 该节点 2 行；shared-quotes.tsv 15.14.md 分组无此 quote；
  exposure.tsv 该行（T2=0、T3=0）；edges.tsv 该节点 3 条出边 ev 均空；
  源文件 `15.14.md:3`（问题提法）与 `15.14.md:68`（预告解）。
- 死因编号：**6**（引文不支撑断言）。
- 建议动作：**FIX**，两处都要动：
  (a) Apostol 侧锚点改为或追加 S54 那 133 字符句
  （`We prove next that the projection of $x$ on $S$ is the solution to the approximation problem stated at the beginning of this section.`，
  charlen 133 取自 anchors.tsv 已有实测值），它才点名「投影就是解」；
  (b) statement 第②句关于「正交性条件」的部分，其 Apostol 侧依据在 15.15.md（定理 15.16），
  本节点 sections 只写 `15.14,4.1`——要么把 sections 扩到含 15.15 并补 15.15.md 锚点，
  要么删去第②句。**我不建议在本轮扩 sections**（会跨节，超本任务范围）。
- 置信度：中高。不确定点：Strang 4.1/4.2 侧是否有一句直接写「正交性条件」从而支撑
  第②句——**未核**（本任务不跨教材核 Strang 源文件）。若有，第②句的 E 型可降为轻微。

## 顺带发现

- Figure 15.2 及其图注在纯文本图谱里必然丢失，属与「15.16 第 5 题 OCR」同类的不可弥补缺口，建议主控记入 `丢弃与未覆盖清单.md`。
- 本节 4 条 `origin=model` 却带 quote 的边（`apostol:orthogonal-to-a-set`/`theorem-15-15`/`projection-on-a-subspace`/`pythagorean-formula` → strang:*），我按句核过其 quote 均为 15.14.md 逐字子串，未篡改；属 README 所指的 15 条校验盲区之一部分。
- `apostol:orthogonal-complement` 同时有 `is-a apostol:subspace`（source，ev 102）与 `requires apostol:subspace`（source，ev 83）两条指向同一 dst 的边，语义可能重复，未展开。
- 抽查二、抽查三两处死因 6 都表现为「模型推论借教材引文」，与 D4/X 两个文件相关；若这是系统性模式，值得单独一轮机器扫描（凡 origin=model 而锚点全部命中 shared-quotes 的节点），本轮未做。
