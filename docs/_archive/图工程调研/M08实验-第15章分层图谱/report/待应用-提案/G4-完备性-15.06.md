# 15.06 覆盖完备性

## 〇 结论速览（本节 37 项 / 参与判定 34 / 已覆盖 27 / 下移 0 / 未覆盖 7；其中值得补建 1 个）

**按句切，不按行切。** 源文件 `source/apostol-ch15/15.06.md` 共 30 行，
其中正文行 8 行；`tmp_index/source-lines.tsv` 显示 15.06.md 的 n_sentence_end
在第 3 行为 3、第 5 行为 3、第 7 行为 2、第 9 行为 3、第 11 行为 7、第 13 行为 2、
第 19 行为 4、第 21 行为 2、第 27 行为 2、第 29 行为 6 —— **每一条正文行都 >1 句**，
所以本节完全不能按行号做覆盖比对（过报通道 b 全线生效）。我人工切出 37 句（含 §标题
与 3 个公式块，均单列编号），与机器的句末标点启发式略有差异（如 "Theorem 15.3(a)."
的句内句点），语义切分以本表为准。

- 本节共 37 项；**不判 3 项**（S1 标题、S7 "Proof." 标签、S37 空行）。
- 参与判定 34 项：**已覆盖 27 / 合法下移（仅靠 T3）0 / 未覆盖 7**。
- 未覆盖 7 项：S16（证毕句 "Therefore S is a subspace of V."）、S27+S28+S29
  （次数 ≤ n 多项式空间的三组张成集，含公式块）、S31（"自然产生若干问题"）、
  S33（"最少需要几个元素"）、S34（引入依赖/独立/基/维数的路线图句）。
- **值得补建 1 个**：S27+S28+S29 合并为一个 example 节点（§二）。
- 另有 2 处只需 **锚点级 FIX**、不需新节点：S10（`d:thm-15-4-identities-inherited-…`
  锚点起点截断，D 型）与 S33（可作为 `apostol:spanning-set` 的第 4 条锚点补上）。
- **无一句属于"合法下移到 T3"**：本节所有已覆盖句都由持有者自身的 T1 锚点或其发出边的
  T2 evidence 覆盖，不存在"父节点空、内容在后代"的情形。这一点我逐条核过 anchors.tsv 与
  edges.tsv 的 ev_file 列，未依赖 pool.tsv 的传递闭包。

## 一 逐句覆盖表（句号 / 句子摘要 / 覆盖状态 / 覆盖者 id 或 file:line / 判据）

覆盖状态取值：**T1**=某节点自身锚点逐字含该句（或该句主干）；**T2**=某边 evidence 含该句；
**未覆盖**=anchors.tsv / edges.tsv 中 file=15.06.md 的全部 quote 里都不含该句主干（已用 `grep -F` 实测）。

| 句号 | 行 | 句子摘要 | 状态 | 覆盖者 | 判据 |
|---|---|---|---|---|---|
| S1 | 3(标题) | §15.6 标题 "Subspaces of a linear space" | 未覆盖 | — | 标题，不判 |
| S2 | 3 | Given a linear space V, let S be a nonempty subset of V. | T1 | `apostol:subspace`#1(56)、`d:subspace-nonempty-requirement`#0(56) | anchors.tsv 同 quote |
| S3 | 3 | 若 S 也是线性空间且运算相同，则 S 叫 V 的子空间 | T1 | `apostol:subspace`#0(130)、`d:subspace-inherits-…`#0(130) | anchors.tsv |
| S4 | 3 | 下一定理给出判定子集是否为子空间的简单判据 | T2 | edge `apostol:theorem-15-4-…` requires `apostol:subspace`，ev 114 字符 | edges.tsv；**无任何节点以它作 T1** |
| S5 | 5 | THEOREM 15.4 前半：Let S be a nonempty subset… | T1 | `apostol:theorem-15-4-…`#0(129)、`d:thm-15-4-hypothesis-nonempty-subset`#0(47) | anchors.tsv |
| S6 | 5 | …S 是子空间当且仅当 S 满足闭合公理 | T1 | 同上 #0(129) 的后半分句 | anchors.tsv |
| S7 | 7 | "Proof." | 未覆盖 | — | 纯标签，不值得建 |
| S8 | 7 | 若 S 是子空间，则满足全部公理，特别地满足闭合公理 | T1 | `apostol:theorem-15-4-…`#1(126)、`d:subspace-satisfies-all-…`#0(66) | anchors.tsv |
| S9 | 9 | 现在证明：满足闭合公理即满足其余公理 | T1 | `apostol:theorem-15-4-…`#2(83)、`d:thm-15-4-sufficiency-direction`#0(83) | anchors.tsv |
| S10 | 9 | 交换/结合律与标量乘公理在 S 中自动满足（因对 V 全体成立） | T1（**锚点起点截断**） | `d:thm-15-4-identities-inherited-…`#0(175) | 见 §五第 1 条：quote 从 "laws for addition" 起，丢了主语 |
| S11 | 9 | 剩下要验证 Axiom 5、6：零元与负元的存在 | T1 | `d:thm-15-4-remaining-obligations-zero-and-negative`#0(129) | anchors.tsv |
| S12 | 11 | 取 S 中任一元素 x（S 非空故至少有一个） | T1 | `d:thm-15-4-witness-element-supplied-by-nonemptiness`#0(84) | anchors.tsv |
| S13 | 11 | 由 Axiom 2，对每个标量 a，ax 在 S 中 | T1 | `d:thm-15-4-closure-under-scalar-multiplication-…`#0(49) | anchors.tsv |
| S14 | 11 | 取 a=0 得 0x∈S；由 Thm 15.3(a) 0x=O，故 O∈S，Axiom 5 满足 | T1 | `d:thm-15-4-zero-element-obtained-by-taking-the-scalar-zero`#0(125) | anchors.tsv |
| S15 | 11 | 取 a=−1 得 (−1)x∈S；x+(−1)x=O，故 Axiom 6 在 S 中满足 | T1 | `d:thm-15-4-negative-obtained-by-taking-the-scalar-minus-one`#0(142) | anchors.tsv |
| S16 | 11 | **Therefore S is a subspace of V.**（证毕句） | 未覆盖 | — | `grep -F 'Therefore $S$ is a subspace'` 在 anchors/edges/pool 全为 0；见 §三 |
| S17 | 13 | DEFINITION. Let S be a nonempty subset of a linear space V. | T1 | `apostol:finite-linear-combination`#0(98) | anchors.tsv |
| S18 | 13 | V 中形如（下式）的元素 x | T1 | 同上 #0(98) 尾部 "An element $x$ in $V$ of the form" | anchors.tsv |
| S19 | 16 | 公式块 x = Σ_{i=1}^{k} c_i x_i | T1 | `d:linear-combination-finiteness-of-the-index-set`#0(40) | anchors.tsv |
| S20 | 19 | 其中 x_i 都在 S 中、c_i 是标量，称为 S 元素的有限线性组合 | T1 | `d:linear-combination-summands-drawn-from-s`#0(133)、`…coefficients-are-arbitrary-scalars`#0(88)、`apostol:finite-linear-combination`#1(173) | anchors.tsv |
| S21 | 19 | 全部有限线性组合之集满足闭合公理，故是 V 的子空间 | T1 | `apostol:linear-span`#0(117)、`d:span-is-a-subspace-via-the-closure-test`#0(122) | anchors.tsv |
| S22 | 19 | 称之为 S 张成的子空间 / S 的线性张成，记作 L(S) | T1 | `apostol:linear-span`#1(88)、`apostol:linear-span-notation-l-of-s`#0(88)、`x:span-of-a-set`#0(90) | anchors.tsv |
| S23 | 19 | 若 S 空，定义 L(S)={O} | T1 | `d:span-of-the-empty-set-is-the-zero-subspace`#0(93) | anchors.tsv |
| S24 | 21 | Different sets may span the same subspace. | T1 | `apostol:spanning-set`#0(125)、`d:spanning-set-is-not-unique`#0(126)、`x:span-of-a-set`#1(42) | anchors.tsv |
| S25 | 21 | 例如 V₂ 由下列各组向量分别张成 | T1 | 同上 #0(125/126) 后半 | anchors.tsv |
| S26 | 21 | {i,j}, {i,j,i+j}, {O,i,−i,j,−j,i+j}（三组具体集合） | T1 | `d:spanning-set-need-not-be-independent`#0(51)、`d:finite-basis-two-clauses-…`#0(51) | anchors.tsv；另有 `apostol:dimension` contrasts 边 ev 178 字符含之 |
| S27 | 21 | 次数 ≤ n 的多项式空间由 n+1 个多项式张成 | **未覆盖** | — | 见 §二 |
| S28 | 24 | 公式块 {1, t, t², …, tⁿ} | **未覆盖** | — | `grep -F 't ^ {2}, \dots , t ^ {n}'` 在 anchors/edges 为 0；随 S27 一并补 |
| S29 | 27 | 它也由 {1,t/2,…} 与 {1,(1+t),…} 张成 | **未覆盖** | — | 见 §二 |
| S30 | 27 | 全体多项式空间由无穷集 {1,t,t²,…} 张成 | T1 | `d:spanning-set-may-be-infinite`#0(102)、`apostol:spanning-set`#2(74) | anchors.tsv |
| S31 | 29 | 此处自然产生若干问题 | 未覆盖 | — | 过渡叙述，见 §三 |
| S32 | 29 | 哪些空间可由有限集张成？ | T1 | `apostol:spanning-set`#1(69) | anchors.tsv |
| S33 | 29 | 若可由有限集张成，最少需要几个元素？ | **未覆盖** | — | `grep -F 'smallest number'` 全表为 0；见 §二（仅需补锚点） |
| S34 | 29 | 为讨论这些问题，引入依赖、独立、基、维数诸概念 | 未覆盖 | — | 章节路线图，见 §三 |
| S35 | 29 | 这些概念在第 12 章研究 Vₙ 时已遇到 | T1 | `apostol:ext-vn-as-a-prior-vector-space`#1(130)、`apostol:ext-chapter-12-finite-dependence-definition`#1(130) | anchors.tsv |
| S36 | 29 | 现在把它们推广到一般线性空间 | T1 | 同上 #1(130) 后半 | anchors.tsv |
| S37 | 30 | 空行 | — | — | 不判 |

口径：共 37 项。**不判 3 项**（S1 标题、S7 "Proof." 标签、S37 空行）。
**参与判定 34 项：已覆盖 27、仅靠 T3 下移 0、未覆盖 7**（S16、S27、S28、S29、S31、S33、S34）。
7 项未覆盖中，S27+S28+S29 合并为 1 个值得补建的节点；S33 只需给既有节点补第 4 条锚点；
S16、S31、S34 判为不值得建（§三）。另 S10 虽已覆盖，但锚点起点截断，需锚点级 FIX（§五第 1 条）。

## 二 未覆盖且值得补建（建议 id / node_type / statement / 锚点逐字 + 实测字符数）

### 提案 1（S27+S28+S29）：多项式空间的多组张成集

- **建议 id**：`d:polynomial-space-has-several-spanning-sets`
- **node_type**：`example`
- **parent / rel**：`part-of` → `apostol:spanning-set`
- **sections**：`15.06`
- **statement**（中文；不含任何模型自算数值）：
  次数 ≤ n 的多项式空间同时被 {1, t, …, tⁿ}、{1, t/2, t²/3, …, tⁿ/(n+1)}、
  {1, (1+t), …, (1+t)ⁿ} 三个集合张成。它与 V₂ 的三组向量是同一现象的第二个实例，
  但载体是函数而非几何向量，且三组集合的元素个数都恰为 n+1 —— 说明"张成集不唯一"
  这件事与元素个数是否变化无关。
- **锚点 1（逐字，取自 15.06.md 第 21 行）**：
  `The space of all polynomials $p(t)$ of degree $\leq n$ is spanned by the set of $n+1$ polynomials`
  **实测 97 字符**（本人用 Python `len()` 实测，非索引表引用；已用 `in` 验证为源文件逐字连续子串、不跨行）。
- **锚点 2（逐字，取自 15.06.md 第 27 行）**：
  `It is also spanned by the set $\{1, t/2, t^{2}/3, \ldots, t^{n}/(n+1)\}$ , and by $\{1, (1+t), (1+t)^{2}, \ldots, (1+t)^{n}\}$ .`
  **实测 128 字符**（同上实测方式；不跨行）。
- 两条锚点均落在 30–200 界内。第 24 行的公式块 `{1, t, t^{2}, \dots , t^{n}}` 我**不**单列为
  第三条锚点：它只有 36 字符但纯记号，且已被锚点 1 的 "the set of $n+1$ polynomials" 指称。
- **死因编号**：无（这是缺口，非错误）。按本实验编号体系，缺口本身不配死因；
  若一定要归口，属"内容确实缺"而非"挂在别处"。
- **建议动作**：**进 SPEC**（作为新增节点提案交主控串行写入 data/）。
- **置信度**：高（0.9）。两条锚点均已实测为逐字子串；`grep -F` 证实
  `'It is also spanned by the set'`、`'n+1$ polynomials'`（在 anchors/edges/pool 三表）
  与第 24 行公式块字串全部命中 0 次。
- **不确定点 1**：`d:dim-of-polynomials-of-degree-at-most-n-is-n-plus-one` 存在（锚点在
  **15.08.md**，122 字符，"…has dimension $n + 1$ . One basis is the set of $n + 1$ polynomials"），
  内容与本提案高度邻接。但它 sections=15.08、parent=`apostol:dimension`，**不是**
  `apostol:spanning-set` 的 part-of 后代（exposure.tsv: `apostol:spanning-set` 的
  n_partof_desc=3、T3=7，其后代不含该节点）。所以这**不是** T3 下移，是 Apostol 在
  15.06 与 15.08 两处分别讲了张成与维数两件事。是否会被裁为"与 15.08 节点重复"，
  取决于综合代理如何看待跨节重复——**我按规则不跨节比较，只报本节缺口**。
- **不确定点 2**：node_type 选 `example` 而非 `concept`，理由是它是 Apostol 的举例而非
  独立断言；若综合代理认为 D 层惯例是 `concept`，改之不影响锚点与 statement。

### 附：S33 只需补锚点，不需新节点

- `apostol:spanning-set` 的 statement 已写"Apostol asks which spaces can be spanned by a
  finite set **and with how few elements**"，即断言已含 S33 之意，但它只有 3 条锚点
  （125 / 69 / 74 字符），其中 #1(69) 只锚了两问中的**第一问**
  （"which spaces can be spanned by a finite set of elements?"），第二问无锚。
- **建议动作**：**FIX**，给 `apostol:spanning-set` 补第 4 条锚点：
  `If a space can be spanned by a finite set of elements, what is the smallest number of elements required?`
  **实测 104 字符**（Python `len()` 实测；已验证为 15.06.md 第 29 行的逐字连续子串、不跨行）。
- **死因编号**：0（无死因，仅需 FIX）。若不修，该节点 statement 的后半分句属 **E 型**
  （整句无锚）——断言里有、锚点里没有。
- **置信度**：高（0.95）。`grep -F 'smallest number'` 在 anchors.tsv / edges.tsv /
  pool.tsv / statements.tsv 四表全部命中 0。

## 三 未覆盖但不值得建（一行一条 + 理由：过渡叙述 / 重复 / 纯记号）

- **S1**（"## 15.6 Subspaces of a linear space"）：节标题，纯结构记号。建议动作：保留现状。
- **S7**（"Proof."）：证明起始标签，无数学内容。建议动作：保留现状。
- **S16**（"Therefore $S$ is a subspace of $V$ ."，实测 36 字符）：证毕收束句。数学上它只是
  把 S9 声明的目标与 S11–S15 已验证的义务对接，不引入新依据；且 THEOREM 15.4 的双向结论
  已由 `apostol:theorem-15-4-subspace-criterion`#0(129) 完整持有。审计 B 判死的 28 个 D 层节点里
  已有"证明的纯语法切分（开场句、收束句）"一类共 6 个（见 `report/丢弃与未覆盖清单.md` §五），
  新建它会重蹈同一死因 5。建议动作：**不进 SPEC**。置信度 0.85；不确定点：若综合代理认为
  `d:thm-15-4-sufficiency-direction` 缺一个显式终点会让 C 型（把端点当中间步骤）更难查，
  可考虑把这 36 字符并入该节点的第 2 条锚点，而不是新建节点。
- **S31**（"A number of questions arise naturally at this point."，实测 52 字符）：过渡叙述，
  下一句 S32 才是实际问题且已有锚。建议动作：**不进 SPEC**。置信度 0.9。
- **S34**（"To discuss these and related questions, we introduce the concepts of dependence,
  independence, bases, and dimension."，实测 116 字符）：章节路线图句，它宣告后文将引入的
  四个概念，而这四个概念在 15.07/15.08 各有自己的节点。建单独节点等于把目录当内容。
  建议动作：**不进 SPEC**。置信度 0.85；不确定点见 §顺带发现第 2 条——
  `apostol:ext-chapter-12-finite-dependence-definition` 的 statement 已经**复述**了这句
  （"15.06 announces that dependence, independence, bases and dimension were encountered in
  Chapter 12 …"），但它的两条锚点都是 130 字符那句 S35+S36，**不含** S34 原文，
  `grep -F 'dependence, independence'` 在 anchors.tsv 命中 0、statements.tsv 命中 1。
  这是该节点的 E 型问题，不是本节的缺口；若要修，给它补 S34 这 116 字符即可。
- **S28**（公式块 `{1, t, t ^ {2}, \dots , t ^ {n} \}.`，该行 36 字符、n_sentence_end=1）：
  纯记号，不独立建节点，随 §二 提案 1 的锚点 1 一并覆盖。建议动作：**不进 SPEC**（作为独立节点）。

## 四 锚点集合与 sections 集合的不一致（若有）

两个集合**确实对不上**，报出如下。

- 集合 A：anchors.tsv 中 file 列含 `15.06.md` 的锚点 —— **39 条锚点，分属 31 个节点**。
- 集合 B：nodes.tsv 中 sections 列含 `15.06` 的节点 —— **34 个节点**。
- `comm` 逐行比对结果：
  - A ⊄ B 的差集：**空**。即没有"锚在 15.06 却不声明 15.06"的节点，方向一侧干净。
  - B ⊄ A 的差集：**3 个**，全部是 L2：
    `L2:closure-certificate-for-subspacehood`（sections 15.03,15.06,15.14）、
    `L2:testing-infinite-objects-through-finite-windows`（15.06,15.07,15.08,15.11,15.13）、
    `L2:dimension-as-a-two-sided-count-barrier`（15.06,15.07,15.08,3.4）。
- **判定**：这 3 个不是错误。tmp_index/README.md 已列明 24 个 L2 节点全部 n_anchors=0
  （L2 层按设计不持锚点，靠 members 指向下层节点），三者的 n_members 均为 2。
  它们的 sections 是成员节点 sections 的并集。**死因编号：0**（无死因）。
  **建议动作：保留**。置信度 0.9。
- 由此得出一个对报覆盖率有用的口径：本节"有 15.06 锚点的节点"是 31 个，
  "声明 15.06 的节点"是 34 个，差额 3 全部由 L2 无锚设计造成。
  引用节点数时必须说明是哪一个 —— 这与 README 里 546 vs 522 的提醒同源。

另一类需要报出的**方向性差异**（不是上述两集合的差集，而是 sections 多值带来的）：
下列 3 个节点的**主锚点不在 15.06**，但因带了一条 15.06 的借用锚点而落入集合 A：

| 节点 | sections | 15.06 锚点 | 主锚点所在 |
|---|---|---|---|
| `d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n` | 15.03,15.06 | #1(129) THEOREM 15.4 全句 | #0(115) 在 15.03.md |
| `d:finite-basis-two-clauses-are-logically-independent` | 15.06,15.08 | #0(51) 三组向量 | 无其他锚点（仅此 1 条） |
| `d:zero-space-declared-finite-dimensional-by-fiat` | 15.06,15.07,15.08 | #1(93) L(S)={O} 约定 | #0 未落在 15.06 |

这三条都是 **B 型（借用引文）** 的候选，其中第 2 个另有更重的问题，见 §五第 2 条。

## 五 本节现有锚点抽查（挑 3 条，按盲区五型 A/B/C/D/E 逐型检查，写出结论）

### 抽查 1：`d:thm-15-4-identities-inherited-from-the-ambient-space` #0（175 字符）

statement：「交换律与结合律以及四条标量乘公理都是全称量化的恒等式。任何子集都自动满足它们，
所以这里除了 S ⊆ V 之外没有用到 S 的任何性质。」
锚点 quote：`laws for addition (Axioms 3 and 4) and the axioms for multiplication by scalars (Axioms 7 through 10) are automatically satisfied in S because they hold for all elements of V.`

- **A 型（变量/对象错）**：未发现。statement 说"四条标量乘公理"，quote 说 Axioms 7 through 10 —— 恰四条，与 Apostol 编号一致。
- **B 型（借用引文）**：**否**。shared-quotes.tsv 中 15.06.md 名下无这条 175 字符 quote（同段只有一条 136 字符的 part-of 边 evidence，起点更靠后）；该节点是唯一持有者。
- **C 型（把端点当中间步骤）**：未发现。这一步本身就是证明的中间步，quote 与 statement 都停在"自动满足"，没有冒充结论。
- **D 型（覆盖范围不足）**：**命中**。quote 起点落在 `laws for addition` 上，把主语 `The commutative and associative` 截掉了。单读 quote 无法知道是哪两条律；statement 的"交换律与结合律"这半句因此**无锚**。全图 `grep -F 'The commutative and associative laws'` 在 anchors.tsv / edges.tsv / pool.tsv 三表命中 **0 次**，说明这七个词从未进入任何证据。
- **E 型（整句完全无锚）**：statement 第二句"任何子集都自动满足…没有用到 S 的任何性质"是对 quote 的**解释**（模型的抽象增益），非教材原文。这属于 D/E 边界；我不判 E，因为 quote 的 "because they hold for all elements of V" 直接支撑它。
- **结论**：D 型确认。**死因编号 0**（无死因，仅需 FIX）——内容对、断言对、只是锚点起点截断。
- **建议动作**：**FIX**。把 #0 起点前移 33 字符，改为
  `The commutative and associative laws for addition (Axioms 3 and 4) and the axioms for multiplication by scalars (Axioms 7 through 10) are automatically satisfied in S because they hold for all elements of V.`
  **实测 207 字符** —— **超出 SPEC 200 上界 7 字符**。故建议改为**新增一条短锚点**
  `The commutative and associative laws for addition (Axioms 3 and 4)`，**实测 66 字符**，
  与原 #0 并存。两个字符数均为本人 Python `len()` 实测并验证为逐字连续子串、不跨行。
- **置信度**：高（0.9）。不确定点：SPEC 的 30–200 界是否对"修锚点"与"建锚点"同样硬性，我未核 SPEC 原文。

### 抽查 2：`d:finite-basis-two-clauses-are-logically-independent` #0（51 字符）

statement：「两个子句互不蕴含。含 O, i, −i, j, −j, i+j 的集合张成 V₂ 却不独立；
V₂ 的单个非零向量独立却不张成 V₂。所以基确实是两个独立要求的合取。」
锚点 quote：`$\{i,j\}$ , $\{i,j,i+j\}$ , $\{O,i,-i,j,-j,i+j\}$ .`

- **A 型**：未发现。三组集合与 statement 引用的那一组字面一致。
- **B 型（借用引文）**：**命中**。shared-quotes.tsv：这条 51 字符 quote **n_holders=4**，
  持有者 `edge:data/edges-D2.jsonl:131`、`edge:data/edges-D2.jsonl:161`、
  `node:d:finite-basis-two-clauses-are-logically-independent`、
  `node:d:spanning-set-need-not-be-independent`。两个节点同引一句。
  单看 B 型可以判合法（前者讲"两子句逻辑独立"，后者讲"张成不含独立性要求"，
  断言不同），但顺着 B 型碰撞查下去暴露了下面的 D 型。
- **C 型**：未发现。
- **D 型（覆盖范围不足）**：**命中，且是本次抽查里最重的一处**。statement 是**两个**互不蕴含的
  见证：(i) 张成而不独立 —— 由 quote 的第三组集合支撑；(ii) **独立而不张成（V₂ 的单个非零向量）**
  —— 15.06.md 全文**没有**这句话。我在 15.06.md 与 15.08.md 里 grep
  `nonzero` / `non-zero` / `single element` / `one element`，15.08.md **命中 0**；
  该节点 sections 声明了 15.08 却在 15.08 无任何锚点（它总共只有 1 条锚点）。
  exposure.tsv：该节点 T1=1、T2=1、T3=0、pool_total=2、**distinct_src_lines=1** ——
  整个证据池只落在 15.06.md 第 21 行**同一源行**上。这正是过报通道 (c)：
  同一源行的两半分别落在节点锚点与边证据上，池变大了但**新引入的源行数为 0**。
- **E 型**：见证 (ii) 整句无锚，可同时归 E 型。
- **结论**：**死因 6（引文不支撑断言）**——statement 的一半断言在池里找不到任何引文支撑；
  该节点 nodes.tsv 的 origin 列为 `model`，这一点减轻了"把模型内容当教材内容"的指控
  （硬禁令 3 未违反），但不解除引文覆盖不足。**注意：死因 6 会因边证据免除，
  而此处 T2 的那条边 evidence 与 T1 锚点是同一源行的两半，未引入新内容，故不构成免除。**
- **建议动作**：**FIX**（不建议 KILL）。二选一：(a) 给见证 (ii) 补一条真正点名"独立但不张成"
  的锚点——需在 15.07/15.08 里找，我**未核到**，故不给字符数；
  (b) 把 statement 的第二句改写为明确标注为模型构造的推论，或直接删去该句，
  只保留由 quote 支撑的见证 (i)。
- **置信度**：中高（0.8）。**不确定点**：我只 grep 了 15.06.md 与 15.08.md 的四个关键词，
  没有通读 15.07.md；若 15.07/15.08 某处确有"单个非零向量独立"的原文，则本条降为 B 型合法 + 锚点待补。
  这一点**未核**，不予预测。

### 抽查 3：`d:span-of-the-empty-set-is-the-zero-subspace` #0（93 字符）

statement：「S 为空时没有元素可组合，故 L(S) 由一条单独的约定钉为只含 O 的单元素集。
没有这条约定，L(S) 会是空集、从而不是子空间。」
锚点 quote：`If S is empty, we define $L(S)$ to be $\{O\}$ , the set consisting of the zero element alone.`

- **A 型**：未发现。L(S)、{O}、O 三个对象与 statement 一致。
- **B 型**：**命中但合法**。shared-quotes.tsv：n_holders=4，持有者
  `edge:data/edges-D2.jsonl:129`、`edge:data/edges-D2.jsonl:16`、
  `node:d:span-of-the-empty-set-is-the-zero-subspace`、
  `node:d:zero-space-declared-finite-dimensional-by-fiat`。
  后者用它是为了论证"空集已经张成 {O}、故零空间的有限维性其实冗余"，是把这句当**前提**用；
  前者是把它当**自身断言**。两种用法都指向这句的字面内容，判**合法**（与
  `report/丢弃与未覆盖清单.md` §六记录的 A1 底座三组合法共享同型）。
- **C 型**：未发现。quote 是定义本身，不是某个推理的终点。
- **D 型**：statement 第一句"没有元素可组合"是对 `If S is empty` 的展开，覆盖充分。
- **E 型（整句完全无锚）**：**命中，轻度**。statement 第二句「没有这条约定，L(S) 会是空集、
  从而不是子空间」是**反事实推理**，15.06.md 没有任何对应文字（Apostol 只给约定、不给理由）。
  该节点 nodes.tsv 的 origin 列**为空**（不是 `model`）。按硬禁令 3（不得把模型自己的内容
  当教材内容而不标 origin），一个 origin 空缺的节点里夹一句纯模型推论，是记账缺陷。
- **结论**：**死因 0**（无死因，仅需 FIX）。数学上第二句完全正确（空集若不特设则 L(∅)=∅，
  而子空间要求非空且含 O），不构成死因 4；但它需要 origin 标注。
- **建议动作**：**FIX**。给该节点的第二句补 origin 标注（或把该句移到一条 origin=model 的
  rel_note / 单独的模型节点上）。**不建议**因此 KILL —— 这句话正是该约定的抽象增益所在，
  删了会踩死因 5 的反面。
- **置信度**：中高（0.8）。不确定点：本实验对"节点内混合 source 与 model 句"的记账规范
  我未核到成文条款，只依硬禁令 3 推断；若规范允许 statement 层自由改写、
  只要求 quote 逐字，则本条降为"无需处理"。

**三条抽查的共同教训**：三条锚点在 check_graph 眼里都是"已验证"（quote 逐字、不跨行），
但逐型检查后 3/3 都有实质问题（1 个 D 型截断、1 个 D+E 型半断言无凭、1 个 E 型未标 origin）。
其中只有第 2 条的 B 型碰撞是机器（shared-quotes.tsv）提示出来的，D 型与 E 型都靠通读
statement 与 quote 的差集发现 —— 与 `report/丢弃与未覆盖清单.md` 文末"机器只能证明引文
未被篡改"的判断一致。**"39/39 锚点已验证"对 15.06 这一节不构成正确性保证。**

## 顺带发现

- `x:span-of-a-set` 的 statement 写「the smallest one containing S」，但 15.06.md 全文没有"最小子空间"这一表述，其 15.06 两条锚点（90 / 42 字符）都不支撑该分句；疑 E 型，未展开。
- `apostol:ext-chapter-12-finite-dependence-definition` 的 statement 复述了 S34（"15.06 announces that dependence, independence, bases and dimension…"），但两条锚点都是 S35+S36 那 130 字符，不含 S34 原文；疑 E 型，未展开。
- `apostol:linear-span` generalizes `strang:spanning-a-space` 是 origin=model 却带 quote 的边之一（ev 117 字符，15.06.md），属 README 所列 15 条 check_graph 从不校验的边；我已 grep 确认该 quote 在 15.06.md 第 19 行逐字存在，但 quote 只讲 Apostol 侧、不含任何 Strang 侧内容，无法支撑 generalizes 这个跨教材关系。未展开。
- 15.06.md 的 8 个正文行 n_sentence_end 全部 ≥1 且多数 ≥2（第 11 行为 7、第 29 行为 6），本节是过报通道 b 的典型样本；若全图覆盖率报表里有按行号算的数字，15.06 会被显著高报。未展开。
- `apostol:subspace` 的 sections 只写 15.06，但 `d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n` 把 15.03 的反例挂到了 THEOREM 15.4 上，跨节引用未在 sections 侧留痕。未展开。
