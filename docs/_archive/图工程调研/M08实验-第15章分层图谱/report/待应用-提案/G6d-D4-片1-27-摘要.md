# D4 片1-27 结构化摘要

来源：`report/审查-形式D-D4-片1-27.md`（只读该一份）。对象：`data/nodes-D4.jsonl` 第 1–27 行，主题 15.13（Gram-Schmidt / Legendre）+ 15.14（S⊥）。
本摘要只压缩，不作新裁定；新发现一律进「顺带发现」。

## 〇 速览（本片共 27 条裁决：KILL 0 / FIX 12 / 保留 15；未决 0 条节点级，另有 7 项声明性未覆盖）

- 分片自己的三分法：形式 D **8** 条（行 4、10、14、16、20、22、23、27）／形式 E **6** 条（行 3、7、9、15、21、25，内容全经核为真）／支撑充分 **13** 条（行 1、2、5、6、8、11、12、13、17、18、19、24、26）。8+6+13=27，核对无误。
- 折成动作：**KILL 0 条**。**FIX 12 条** = 8 条形式 D 全部（改 statement 或补/换锚）+ 4 条形式 E（行 3、9 补 `origin: model`；行 7 改「exactly why」；行 21 补锚 `15.13.md:77`）。**保留 15 条** = 13 条支撑充分 + 形式 E 中的行 15、25（已标 model 或已写对限定，无需动）。
- 死因：分片给出的**唯一实质死因是死因 1**（量词过度断言），落在行 14；行 22、4、10 分片写「0（改写）；严办为 1」，属边界档。行 27 写「改写后 0；若严办为 6」。其余 22 条死因 0。
- 分片自称「最严重一条」：行 14 的 `exactly two load-bearing places`，被 `15.11.md:15` 反例推翻，且图内缺一条对应边。
- 无节点被判 KILL、无节点被判撤回；分片对全部 27 条的处置建议都是 KEEP + 改写/补锚。

## 一 裁决表（分片编号 / 节点 id / 死因 / 动作 / 依据 file:line / 一句话理由）

「编号」= `nodes-D4.jsonl` 文件行序（分片内即以行号为编号），括号内为分片自编的 D-n / E-n 标签。
源文件路径均为 `source/apostol-ch15/`，下表省略前缀。

| 编号 | 节点 id | 判型 | 死因 | 动作 | 依据 file:line | 一句话理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 行 1 | `d:orthogonalization-conclusion-a-orthogonal-to-earlier-span` | 支撑充分 | 0 | 保留 | 15.13.md:7 | 「不只正交于 y_1..y_{k-1} 而是正交于整个 L(...)」逐字对应锚点；末句「orthogonality half」措辞小瑕。 |
| 行 2 | `d:orthogonalization-conclusion-b-spans-agree` | 支撑充分 | 0 | 保留 | 15.13.md:9 | 两锚分别覆盖 (b) 陈述与归纳步目标式，末句为无风险解释。 |
| 行 3 (E-1) | `d:gram-schmidt-base-step-y1-equals-x1` | 形式 E | 0 | FIX（补 `origin: model`） | 锚 15.13.md:17；核 15.11.md:5 | claim 2、3（k=1 时 (a) 空洞成立、(b) 是实质等式）无锚，经核为真，属合理补白但未标 model。 |
| 行 4 (D-4) | `d:gram-schmidt-inductive-step-defines-y-r-plus-one` | 形式 D | 0（严办 1） | FIX（改写末句） | 锚 15.13.md:17/:20/:23；缺口 15.13.md:63–75 | 末句「Everything else in the proof」漏掉定理 15.13 的 (c) 唯一性，本片行 12、13 正是给 (c) 建的节点，图自证其伪。 |
| 行 5 | `d:inner-product-of-the-new-element-with-an-earlier-one` | 支撑充分 | 0 | 保留 | 三锚；15.10.md:19、15.13.md:26 | 展开式无共轭与第一变元齐次公理一致，正确。 |
| 行 6 | `d:coefficient-that-cancels-the-projection-component` | 支撑充分 | 0 | 保留 | 两锚；`requires`→`apostol:inner-product-axioms` | 「(y_j,y_j)≠0」一步无锚但由边接住且为真。 |
| 行 7 (E-2) | `d:zero-earlier-element-makes-the-coefficient-irrelevant` | 形式 E | 0 | FIX（「exactly why」降级） | 锚 15.13.md:35；核 15.13.md:5、:77 | 末句因果断言无锚，方向成立但「exactly why」偏强：无独立性假设还依赖 (a) 在 y_k=O 时平凡成立。 |
| 行 8 | `d:new-element-is-well-defined-and-orthogonal-to-all-earlier-ones` | 支撑充分 | 0 | 保留 | 15.13.md:35 | 锚点逐字含「well defined and is orthogonal to each of the earlier elements」；「subtraction of projections」措辞小瑕。 |
| 行 9 (E-3) | `d:orthogonality-to-generators-extends-to-their-span` | 形式 E | 0 | FIX（补 `origin: model`） | 锚 15.13.md:35 + 15.14.md:42；核 15.10.md:17/:19/:40 | 三条并列理由从句全无锚但推理正确；「Apostol 用了两次」两处**都锚了**，是本片处理该形态最规范的一例。 |
| 行 10 (D-5) | `d:span-inclusion-y-inside-x` | 形式 D | 0（严办 1） | FIX（删 the only + 补锚） | 唯一锚 15.13.md:49；缺 15.13.md:43–:49 | 末句「Closure under differences is the only tool used」少算张成单调性与线性组合封闭性两件工具。 |
| 行 11 | `d:span-inclusion-x-inside-y` | 支撑充分 | 0 | 保留 | 锚覆盖两方向包含 | 末句对称性观察为真（(15.13) 移项即得）。 |
| 行 12 | `d:uniqueness-splits-the-competitor-into-span-plus-multiple` | 支撑充分 | 0 | 保留 | 三锚（归纳前提／拆分式／目标 z_r=O） | 逐条覆盖。 |
| 行 13 | `d:z-r-is-orthogonal-to-itself` | 支撑充分 | 0 | 保留 | 两锚，含 15.13.md:75 | 逐字覆盖「orthogonal to itself, so z_r=O」；隐含跳步源自教材（§7.8）。 |
| 行 14 (D-1) | `d:zero-is-the-only-element-orthogonal-to-itself` | **形式 D（最严重）** | **1** | FIX（改写 + 补 `requires` 边） | 锚 15.11.md:5 / 15.14.md:50 / 15.13.md:75；反例 **15.11.md:15** | 「exactly two load-bearing places」是全章穷尽性断言，被定理 15.10 证明里的第三处使用点推翻；图内亦缺 `d:15-10-proof-divide-by-nonzero-self-inner-product` → 本节点的边。 |
| 行 15 (E-4) | `d:positivity-axiom-forbids-nonzero-self-orthogonal-elements` | 形式 E | 0 | 保留 | 锚 15.10.md:22（35 字符）；锚 2 = 15.10.md:125 | 末句「蕴涵严格弱于公理」无锚，分片以 V=R、(x,y):=−xy 构造反例验为真，是有价值的正确补白；已标 `origin: model`。 |
| 行 16 (D-8) | `d:gram-schmidt-works-in-infinite-dimensional-spaces` | 形式 D | 0 | FIX（换锚） | 锚 15.13.md:3 + :120；缺口 **15.08.md:17** | 「The Legendre example is an actual infinite-dimensional instance」的锚只证**序列**无限、不证**空间**无限维；补口的原文在另一文件 15.08.md:17。 |
| 行 17 | `d:projection-along-an-element-needs-a-nonzero-direction` | 支撑充分 | 0 | 保留 | 锚含「with $y \neq O$」；15.13.md:29 | 末句「exactly the hypothesis」偏强但方向成立，登记 §7 未判 D。 |
| 行 18 | `d:normalizing-converts-an-orthogonal-basis-into-an-orthonormal-one` | 支撑充分 | 0 | 保留 | 15.13.md:83 | 末句「silently combines two separate facts」准确，两半即行 19、20，图内以 `part-of` 落实。 |
| 行 19 | `d:normalized-element-has-norm-one` | 支撑充分 | 0 | 保留 | 锚 15.10.md:127（定理 15.9(c)）+ :125 | 已标 `origin: model` 并如实说明教材未写此步；验算 ‖y/‖y‖‖=1 通过。 |
| 行 20 (D-7) | `d:rescaling-preserves-orthogonality` | 形式 D | 0 | FIX（补锚 15.13.md:83 + 改量词） | 锚 15.10.md:40 + 15.11.md:35；缺口 **15.13.md:83** | 「both in 15.11 and in 15.13」只锚了 15.11，而**第三锚点槽位空着**且该句合规可引，故不能推给 SPEC；「whenever he normalizes」是全章量词。 |
| 行 21 (E-5) | `d:gram-schmidt-turns-a-basis-into-an-orthogonal-basis` | 形式 E | 0 | FIX（补锚 15.13.md:77） | 唯一锚 15.13.md:83；核 15.13.md:9、:77、15.11.md:7 | 冒号后整段独立性理由无锚，三条依据都在原文且**锚点槽位空着两个**，本可锚而未锚。 |
| 行 22 (D-3) | `d:finite-dimensionality-supplies-a-finite-basis-to-orthogonalize` | 形式 D | 0（严办 1） | FIX（「The only role」降级） | 唯一锚 15.13.md:3；反例方向 **15.08.md:3** | 「the only role」「the single point at which the hypothesis is consumed」是排他性断言，锚点只到「有限维 ⇒ 存在有限基」；且 V={O} 的退化情形不提供可喂入构造的有限基。 |
| 行 23 (D-6) | `d:vanishing-projection-terms-in-the-legendre-computation` | 形式 D | 0 | FIX（补锚 15.13.md:135 或改单数） | 锚 15.13.md:123 + :129；未锚项 **15.13.md:135** | 「these terms / several projection terms」是复数量词，两锚只涉及 y_1 一项；断言为真但射程只被单例支撑。 |
| 行 24 | `d:legendre-normalizing-constant-from-y-n-to-p-n` | 支撑充分 | 0 | 保留 | 锚覆盖 P_n 双表达式；15.13.md:153、:159 | 常数恒等式 (2n)!/(2^n (n!)²)×n!/(2n)! = 1/(2^n n!) 已验算自洽。 |
| 行 25 (E-6) | `d:normalized-legendre-polynomials-are-y-n-over-its-norm` | 形式 E | 0 | 保留 | 两锚 15.13.md:162 | 末句无锚但经验算为真；常数对一切 n≥0 为正，生产者写了「positive multiple」这个**必要且正确**的限定。 |
| 行 26 | `d:s-perp-is-defined-for-an-arbitrary-subset` | 支撑充分 | 0 | 保留 | 两锚 15.14.md:9 一带 | 逐字覆盖「Let S be a subset of a Euclidean space V」与 S⊥ 命名，正是断言要点。 |
| 行 27 (D-2) | `d:s-perp-closure-follows-from-linearity-in-the-first-argument` | 形式 D | 0（严办 **6**） | FIX（statement 加实/复限定） | 锚 15.14.md:11 + 15.10.md:17 + 15.10.md:15；反证 **15.10.md:29**、:45 | 用**实**线性空间定义段的对称公理 (1) 去证含复空间的一般 Euclidean space 结论；结论在复空间仍真（共轭有加性），但写出的论证路径在复空间断裂——射程不足而非数学错。 |

## 二 分片自评最重的条目

分片明确排了严重度降序，并声明「前三条不止改锚点，statement 本身要动」。按其自评取前三加一条方法论意义最重的：

1. **行 14（D-1，分片标「最严重」，全片唯一死因 1）**。`exactly two load-bearing places` 被 `15.11.md:15` 推翻。分片的推翻路径是**换语言再查**：`grep -n "orthogonal to itself\|only element orthogonal" *.md` 只命中断言所说的三行（15.11.md:5 / 15.13.md:75 / 15.14.md:50，与断言相符），但把同一事实换成内积语言后出现第三个使用点 `15.11.md:15`（定理 15.10 证明里「But $(x_1,x_1)\neq O$ since $x_1\neq O$ so $c_1=0$」）。分片强调该处比另两处更 load-bearing，且**本节点自己的子节点行 15** 明说该事实「carries no content beyond positivity」，即按节点自身口径必须计入。附带图内后果：`d:15-10-proof-divide-by-nonzero-self-inner-product` 与本节点之间无 `requires` 边（缺边与错断言同源）。
2. **行 27（D-2）**，分片列第二重，并给了唯一一处「严办为死因 6」的备选。实/复分裂是本片专项 4.2 唯一命中的缺陷。
3. **行 22（D-3）**，分片列第三重，附一个不作死因的退化反例方向（`15.08.md:3`，V={O} 也算 finite dimensional）。
4. **行 20（D-7）的「不能推给 SPEC」判断**：分片专门核了锚点预算，确认第三槽空着且待引句合规（行 18 就引了它），并做了**同生产者标准不一致**的对照——行 9 做同类「用了两次」断言时把两处都锚了。这条对合并层判「记账位置问题 vs 实质缺证」有直接用处。

## 三 未决与不确定项

节点级未决 **0**（27/27 全部给了判型与处置）。分片自陈的未覆盖／不确定共 7 项 + 2 处措辞级两难：

1. **专项 4.3（反常积分收敛 / 积分内积正定性）在本片零命中**。`15.10.md:73`（权函数 w）、`:75–81`（Example 5 的 improper integral converges）、`:59`（`C(a,b)` 连续性）在本片无节点引用。须由覆盖 15.10、15.12 习题的片区完成。
2. **专项 4.2 的多数检查点无对象**：`15.10.md:13` 内积定义、`:105` 范数定义、`:129`/`:136` 三角不等式与其共轭、`:151` 实空间角度定义、`15.11.md:80` Parseval 共轭——本片只核到 3 个真正涉及实/复分裂的节点（行 27 命中，行 20、9 正确）。
3. **专项 4.1 中定理 15.11（`15.11.md:43`）、15.12（`15.11.md:77`）的 finite-dimensional 假设本片无节点涉及**。
4. **专项 4.4 点名的符号值全部不在本片**（`(e−1/e)/2`、`3/e`、`e^2−1`、`pi−2 sin x`、`x = 1 and y = i`）；分片只验算了本片实际出现的 4 处量，并明确写「零缺陷不能据此推广到其它片区」。
5. **未审 `nodes-D4.jsonl` 第 28 行及以后**，故不对该文件作整体结论。
6. **未读任何其它审查员输出**，判定完全独立，自陈「可能与他人重复或冲突，需裁定层合并」。
7. **未系统审边**：只为核 atomic_reason 自述读了 `edges-D4/D3/H1/H2/A2-s1.jsonl` 中与本片 27 节点相关的行；**边的 rel 方向与 evidence 支撑性未系统审查**。
8. **A / B / C 三型未系统排查**，仅顺带记一处疑似 B 型轻度形态（行 15 锚点 2 与行 19 复用同一引文承担两种角色）。
9. **锚点逐字性未重验**（依赖既有 `grep -F` 结论）；但分片声明引用的原文行号都是直接读 `source/apostol-ch15/` 得来，故射程判断不依赖那个通过率。
10. **行 27 的复空间结论只做了正向验算**（共轭的加性使结论仍真），未穷举复空间下 S⊥ 的全部性质；判定只针对「论证路径在复空间断裂」。
11. 两处措辞级两难（分片给了并列方案未定一者）：行 16「换锚 vs 削减 statement」；行 27「补锚会挤掉 3 锚点上限 vs 只在 statement 加限定（不花锚点预算）」——分片倾向后者，故认为 D-2 判定成立。

## 四 分片自身的记账错

我核了分片写下的每个可机械校验的数字（`tmp_index/nodes.tsv`、`anchors.tsv`、`edges.tsv`、`source-lines.tsv`）。**字符数全部正确**，记账错只有两处，均在 §6 一带，且都不改任何裁决方向。

1. **行 16 的锚点槽位写反（§2 D-8 claim 1 与 claim 3）**。分片写 `无锚（原文 15.13.md:5 可逐字引，槽位已满，见 §6）`，但 `nodes.tsv` 该节点 `n_anchors=2`（`data/nodes-D4.jsonl:16`），`anchors.tsv` 亦只有 idx 0、1 两条（`15.13.md` 105 字符 + 79 字符）。**第三槽是空的，不是满的。** 这与分片在行 20 上做对的那种核查（明确指出第三槽空着、故不能推给 SPEC）是同一件事，此处漏做。影响：行 16 的「换锚 vs 削减 statement」两难被夸大了——至少 `15.08.md:17` 可直接占空槽而不必挤掉任何现有锚点（该行实测 187 字符、3 个句末标点，属多句行，取子句引用合规）。§6.2 里「需 4–5 条，上限 3」的表述本身不错，错的是把「已用 2」说成「已满」。
2. **§6 抬头的受约束节点计数不自洽**。抬头写「30 字符下限 3 条规则实例，影响本片 3 个节点；3 锚点上限 3 个节点；跨行禁止 1 个节点。合计 6 个节点受 SPEC 约束（其中 1 个同时受两条约束）」。按 §6.1–6.3 正文逐条取并集：30 字符下限影响 {行 19、20、27}，3 锚点上限 {行 16、20、27}，跨行禁止 {行 10}——并集 = **5 个节点**，其中**行 20、27 两个**同时受两条约束。应为「合计 5 个节点，其中 2 个同时受两条约束」。

**核过而确认无误的数字**（供合并层免于重算）：`15.10.md:19` = 21 字符、`:108` = 26 字符、`:123` = 29 字符（确差 1 字符）、公理 (1) 49 字符、公理 (2) 63 字符、`(x,x)>0` 35 字符、行 15 锚点 1 = 35 字符、`15.13.md:49` 句首片段所在行 247 字符 2 句（分片估该片段约 66 字符，未逐字复核但量级合理）、`15.13.md:135` = 200 字符（正好卡 SPEC 上限，须取子句）、`15.08.md:17` = 187 字符。§8 声称的 `origin: model` 五节点（行 15、19、20、23、27）与 `nodes.tsv` 的 origin 列**完全一致**；行 3、7、9、21 的 origin 确为空，分片「标注不齐」的指控成立。D-1 的缺边指控也成立：`edges.tsv` 中 `d:15-10-proof-divide-by-nonzero-self-inner-product` 只有 4 条边（`part-of`→定理 15.10、`requires`→`d:15-10-proof-pair-with-a-single-element` / `d:inner-product-positivity-axiom` / `d:15-10-nonzero-hypothesis-is-essential`），确无指向 `d:zero-is-the-only-element-orthogonal-to-itself` 的边。

**未见**下列类型的记账错：把有锚说成无锚、锚点 file 张冠李戴、claim 数与 statement 不符、汇总数不加总（8+6+13=27 对）。

## 五 主通道统计（这一片的指控主要靠 T1 / T2 / T3 哪一层塌陷，给出计数）

前置说明：本分片成文时用的是「锚点射程」口径，不是四层证据池口径。下面把它的语言映射到 T1/T2/T3，并附 `exposure.tsv` 实测池量；这是**转译，不是新裁定**。

**主通道：T1（节点自身锚点）射程不足，14/14 条指控全部由此发起。** 没有任何一条指控是以 T2 或 T3 塌陷为前提提出的。分片的判型定义即「有锚点管着该 claim 但射程只覆盖一部分」（形式 D，8 条）与「该 claim 完全无锚点覆盖」（形式 E，6 条），两者都只看 T1。

分层计数（14 条指控）：

- **纯 T1 射程不足、且任何证据层都救不了：4 条**——行 14、4、10、22。这四条的缺口是**排他性/穷尽性量词**（`exactly two` / `Everything else in the proof` / `the only tool used` / `the only role`+`the single point`），按本实验口径死因 1 不因边证据免除，故 T2/T3 无论多厚都不构成豁免。分片自己也是这样处理的（对行 22 明说图内 `requires` 接住了 claim 3，但排他性缺陷照判）。
- **纯 T1 射程不足、可用补锚/换锚消解：4 条**——行 16（补口在另一文件 `15.08.md:17`）、行 20（第三槽空着）、行 23（`15.13.md:135` 未锚）、行 21（两槽空着）。这四条属**记账位置问题**：内容在教材里、槽位也有，只是没挂上。
- **T1 全空但内容为真的补白：6 条形式 E**（行 3、7、9、15、21、25）。其中行 15、25 的关键从句是模型自提且经独立验算为真（行 15 的 V=R、(x,y)=−xy 反例；行 25 的常数正性），分片判保留。
- **T2 明确救下无锚 claim 的：3 处**——行 6（`requires`→`apostol:inner-product-axioms` 接住「(y_j,y_j)≠0」）、行 22 claim 3（`requires`→`d:gram-schmidt-works-in-infinite-dimensional-spaces` 接住「construction is dimension-free」）、行 27 claim 4（`requires`→`apostol:theorem-15-4-subspace-criterion` 接住「subspace criterion applies」）。分片对这三处均写「图内已接住」，不据此判死因。
- **T3 反向使用（后代反驳父节点）：2 处**，是本片方法上的特点——行 4 的缺陷正由其后代行 12、13 的存在证成（「图自己反驳了这句话」）；行 14 的「exactly two」由其唯一 part-of 后代行 15 的口径证成。这两处 T3 不是救援而是证伪材料。
- **仅 TD 个案：0 条。** 本片没有出现 §4.11 里 L64 那种「证据只存在于指向本节点的边上」的形态。

`exposure.tsv` 实测池量（8 条形式 D 节点，字段序 后代/T1/T2/T3/池总/去重源行）：行 14 = 1/3/2/3/8/5；行 4 = 3/3/2/15/20/14（池最厚，缺陷仍成立，是典型的「池大 ≠ 覆盖」）；行 27 = 0/3/3/0/6/4；行 22 = 0/1/3/0/4/2；行 16 = 0/2/1/0/3/2；行 20 = 0/2/1/0/3/3；行 23 = 0/2/0/0/2/2（**池里只有 T1，无任何边证据**）；行 10 = 0/1/1/0/2/**1**（去重后只 1 条不同源行，全片最薄）。

## 顺带发现

- 分片 §7 另登记 9 条不裁定的其它维度问题（措辞类 5 条：行 1「half」对三条结论、行 8「subtraction of projections」在 y_j=O 分支不存在投影且与本片行 17 互冲、行 22 name_en「15.14」定理/节次歧义、行 24「normalization convention」与行 25 词根撞车、行 9「conjugate of 0」表述绕；结构类 4 条：行 15 锚点 2 主题错位（范数 vs 内积正定性，兼疑似 B 型）、`origin: model` 标注不齐、行 13 隐含跳步归教材、D-1 派生的图内缺边）。合并层若要收措辞类 FIX，这 5 条是现成清单。
- 我未发现分片有明显错判的裁决；两处记账错（§四）不改任何裁决方向。
- 分片自称的「首审」与本轮 §4.11 已完成的 D3/D2 句粒度裁定无重叠对象（本片全在 nodes-D4.jsonl 1–27），但行 14 的反例点 `15.11.md:15` 属 D3 片区节点 `d:15-10-proof-divide-by-nonzero-self-inner-product` 的领地，建议合并层交叉核一下是否与 D3 分片的结论冲突。
- `15.11.md:15` 实测 463 字符、6 个句末标点，属重度多句行；任何按行号做的覆盖比对在此行会高报（过报通道 b），补边或补锚时须取子句并实测字符数。
- `15.13.md:135` 实测正好 200 字符（SPEC 上限），行 23 若按分片建议补锚，必须截取子句而不能整行引用。
