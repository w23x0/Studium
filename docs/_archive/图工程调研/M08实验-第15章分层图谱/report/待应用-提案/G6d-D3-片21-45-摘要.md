# D3 片21-45 结构化摘要

来源：`report/审查-形式D-D3-片21-45.md`（仅读该一份）。本文件只做压缩，不做新裁定；
凡我用 `tmp_index/` 复核过的数字，标注表名。

## 〇 速览（本片共 25 条裁决：KILL 0 / FIX 23 / 保留 2；未决 10 项 + 3 处死因待定）

- 覆盖：`data/nodes-D3.jsonl` 行 21–45，25 个节点全审，无抽样。sections 实测：15.10 共 23 个、15.11 共 2 个（nodes.tsv 已复核一致）。
- 形态分类：形式 D（射程不足）3 例＝行 25、33、43；形式 E（无锚从句）20 例；支撑充分 2 例＝行 37、42。行 30 兼 D/E 两型，分片归入 E-2。
- 死因：分片对全 25 条一律给 `0`（KEEP + 定点补锚/改写），**无 KILL、无 `4`（数学错误）**。三处给了从严备选：行 33 claim 3 → `6`；行 43 末句 → `1`；行 41 claim 2 → `1` 低度候选。
- 锚点额度：25 节点共 37 条锚点，无一节点用满 3 条，13 个节点只用 1 条（nodes.tsv 逐条复核，n_anchors 与分片表完全一致）。
- 分片核心判断：缺陷模式是证据纪律，不是数学能力——20 例形式 E 的无锚 claim 经人工核真**全部为真**，唯一例外是行 41 一处边界量词。

## 一 裁决表

格式：L / 节点 id / 形态 / 死因 / 动作 / 依据 file:line / 一句话理由。
「依据」列写的是分片指认的**缺口锚点或反驳行**，不是已有锚点。

| L | 节点 id | 形态 | 死因 | 动作 | 依据 file:line | 一句话理由 |
| --- | --- | --- | --- | --- | --- | --- |
| 21 | `d:weight-function-must-be-positive` | E-2 | 0 | FIX（补锚/标 origin:model） | 15.10.md:47 | 反事实整句（w 变号或子区间为零则公理(4)失效）无锚，原文 :47 只说"读者自行验证"、从未论证正性必要性；claim 1 的 "only because" 必要性模态也超射程。 |
| 22 | `d:exponential-weight-makes-improper-integral-converge` | E-2 | 0 | FIX（补锚 :75） | 15.10.md:75 | 末句"权做双重工作"整句无锚；载体空间"全体实多项式"在 :75 可引未引。 |
| 23 | `d:cauchy-schwarz-inequality-statement` | E-2 | 0 | FIX（标 origin:model 或删后两句） | 15.10.md:13, 15.10.md:22 | 后两句把结论外推到"半正定内积"这一原文不存在的对象类（Apostol 的 Euclidean space 定义内建严格正性），且论证漏 (y,y)=0 分支。 |
| 24 | `d:cauchy-schwarz-equality-condition` | **E-1（分片列为本批最严重）** | 0 | FIX（必须补锚 :151/:157 并限定实空间） | 15.10.md:151, 15.10.md:157 | 6 条 claim 只有 1 条被锚点管住、仅用 1/3 额度；claim 4 的 "c>0/c<0 与夹角" 只在实空间有意义，而定理 15.8 实复皆成立，节点未作限定。 |
| 25 | `d:cauchy-schwarz-proof-inherited-from-theorem-12-3` | **D-1** | 0（未给从严备选） | FIX（强制改写末句 + 补锚 :94） | 15.10.md:91 后半, 15.10.md:94 | 末句 "every theorem transfers for free" 被它自己所在段落下半句反驳——复情形须补 (x,y)(y,x)=\|(x,y)\|² 一步；锚点射程止于 :91 前半的"实空间"。 |
| 26 | `d:cauchy-schwarz-in-norm-form` | E + **方程编号错引** | 0（分片判；见「顺带发现」） | FIX（删/改 "(15.7)"，补锚 :139 或 :157） | 15.11.md:46, 15.10.md:86 | statement 写 "taking square roots in (15.7)" 为假：`\tag{15.7}` 全书唯一在 15.11.md:46（内容是 x=Σcᵢeᵢ），而定理 15.8 的显示行 15.10.md:86 根本没有编号（我已用 source-lines.tsv 复核两点均确实）。 |
| 27 | `d:cauchy-schwarz-for-integrals` | E-3 | 0 | FIX（末句改无主语或标 origin:model） | 15.10.md:97 | 末句"Apostol offers it as the pay-off…"给作者塞话，:97 原文只有 "EXAMPLE. Applying Theorem 15.8…"、零评价性表述。 |
| 28 | `d:norm-definition-as-square-root` | E-2（最轻） | 0 | FIX（补锚 :22） | 15.10.md:22, 15.10.md:27 | "根号下非负故实平方根存在"依赖公理(4) 与 (O,y)=0，二者皆可引未引；定义式本体 :108 因 26 字符受 SPEC 下限所限不可引。 |
| 29 | `d:theorem-15-9-holds-for-every-inner-product` | **E-1** | 0 | FIX（补锚 :119） | 15.10.md:119 末句 | 该节点全部增益（"(a)–(d) 在任何内积选择下都存活"）的逐字依据就在被引 :121 正上方一行，却完全未引，仅用 1/3 额度。 |
| 30 | `d:norm-vanishes-at-the-zero-element` | D+E（分片归 E-2） | 0 | FIX（**必须**补锚 :27） | 15.10.md:27 | 锚 1（:133）只说"由 the axioms 立即得出"、不指名，末句收紧为"依赖齐性公理经 c=0、完全不依赖正定性"，该具体归属超出射程；且中心 claim 因 :123 实测 29 字符不可引而两锚均不含。 |
| 31 | `d:norm-positivity-property` | **E-1** | 0 | FIX（补锚 :22、:151） | 15.10.md:22, 15.10.md:125, 15.10.md:151, 15.11.md:52 | 唯一性断言、半正定下最先失效、角公式分母非零性三条全无锚，仅用 1/3 额度；末句还跨文件指向 15.11.md:52 而节点无任何 15.11 锚点。分片核真：唯一性成立，**非** `1`；(15.8) 交叉引用正确。 |
| 32 | `d:norm-homogeneity-property` | **E-1** | 0 | FIX（补锚 :40、:15） | 15.10.md:40, 15.10.md:15 | 整个 (c) 的证明重构（两次提取、c·conj(c)=\|c\|²）全无锚，而 Apostol 对 (c) 根本未给证明；实/复处理显式且正确（正面案例）。 |
| 33 | `d:norm-properties-abc-follow-at-once-from-the-axioms` | **D-2（分片称本批最实质）** | 0，从严可 **6** | FIX（强制改写 claim 3） | 15.10.md:133 | 原文 "follow at once from the axioms" 复数不指名，claim 3 收紧为"每条都是单条公理的一步翻译"**为假**：(c) 需齐性+对称两条（行 32 自己写出），行 31 亦直接反驳；31/32/33 三兄弟对同一事实互相冲突。 |
| 34 | `d:triangle-inequality-statement` | E-2 | 0 | FIX（补锚 :17、:139，末句补 "(1')"） | 15.10.md:17, 15.10.md:139 | 末句"证明默默需要第一分量可加性"无锚（核真为真）；且只写 symmetry (1)、未提复情形须换 (1')，与同胞行 35 的处理不一致。 |
| 35 | `d:triangle-inequality-proof-expand-norm-squared` | E-2 | 0 | FIX（补锚 :32 或 :17） | 15.10.md:32, 15.10.md:17 | 三条分析性 claim（隐藏两次公理使用、第一分量拆分靠可加性+对称、(y,x)→conj(x,y) 是 (1')）全无锚，仅用 1/3 额度；核真全部为真，"两次"计数偏松但自洽。实/复处理显式正确（正面案例）。 |
| 36 | `d:cross-term-sum-is-real` | E-2 | 0 | FIX（末句去定冠词最高级或标 origin:model，补锚 :32） | 15.10.md:32, 15.10.md:45 | 后三句无锚；"为实的原因又是 (1')"因果指派要绕一道（直接原因是 z+conj(z)=2Re(z)）；末句 "**the** step that makes the real proof and the complex proof one proof" 为编者最高级判断，且实情形下 (x,y)+(y,x)=2(x,y) 本就为实、无需此步。 |
| 37 | `d:triangle-inequality-proof-bound-cross-terms-by-cauchy-schwarz` | **支撑充分** | 0 | 保留 | 15.10.md:142→:145 | 双锚把 CS 两次施用与结果显示式逐字覆盖，数学主干完整落在射程内；唯一瑕疵是 "then takes square roots" 的极轻微归因外推，且 atomic_reason 已自陈。 |
| 38 | `d:triangle-inequality-equality-sufficient-conditions` | E-3（轻） | 0 | FIX（补锚 :89） | 15.10.md:89 | 仅末句"c>0（而非 c≠0）的正性要求是它与 CS 相依条件的区别"无锚（核真为真，反例 y=−x）；其余部分是本片区量词处理最规范的一处（claim"只是充分条件"由锚 1 自身的 "if" 支撑）。 |
| 39 | `d:angle-cosine-equation` | **E-1** | 0 | FIX（补锚 :151、:157） | 15.10.md:151, 15.10.md:157 | "角定义为该方程的解"与"必须分别检查两个良定义事实"两条依据都存在、都合规、都被跳过，仅用 1/3 额度；claim 3 隐含商为实数，只在实空间成立而节点无实限定。(15.6) 编号已核为正确。 |
| 40 | `d:angle-requires-nonzero-elements` | E（轻；未列入 E-1/2/3 表） | 0 | FIX（补锚 :125） | 15.10.md:125, 15.10.md:22 | "分母非零恰因定理 15.9(b)、归根到底因正定性公理"无锚，:125 与 :22 均合规可引未引；另锚 1 从 :151 切走了句首 "In a real Euclidean space V, "，前移起点即可同时覆盖实空间限定，是被浪费的机会。 |
| 41 | `d:angle-confined-to-the-interval-zero-to-pi` | E-3 + **量词瑕疵** | 0，低度 **1** 候选 | FIX（改写 claim 2 + 补锚 :157） | 15.10.md:157 | claim 2 "even over [0,2π] 该方程有多于一解" 在**商恰为 −1** 时为假（[0,2π] 内唯一解 θ=π，节点自备的 "θ 与 2π−θ" 机制在此退化）；"在全体 R 上"那半永真。本片区唯一实质量词瑕疵。 |
| 42 | `d:angle-well-defined-because-quotient-lies-in-minus-one-to-one` | **支撑充分** | 0 | 保留（建议标 origin:model） | 15.10.md:157 | 双锚把 :157 整条 Note 两半都逐字覆盖，为本片区最完整的锚点策略；余项"余弦在 [0,π] 严格递减"在本实验两源文件内确无可引依据（属第 2/6 章），是正当不可锚项。另 "vacuous" 用词不确。 |
| 43 | `d:angle-defined-only-in-real-euclidean-space` | **D-3** | 0，从严可 **1** | FIX（强制改写末句 + 补锚 :45） | 15.10.md:29–41（反例）, 15.10.md:45 | 末句 "the one place in 15.10 where…" 是全域断言而两锚都是单点事实、零覆盖；反例：:29 复情形替换对称公理为 (1')、:32 给 (1') 本体、:40 给只在复情形存在的伴随关系。窄读为"唯一被限制到实空间的**度量**概念"方成立。 |
| 44 | `d:orthogonality-is-vanishing-of-the-inner-product` | **E-1** | 0 | FIX（补锚 15.10.md:45、:32） | 15.10.md:45, 15.10.md:32 | 定义之外的全部实质内容（正交在复空间同样可用、(x,y)=0⟺(y,x)=0 由 Hermitian 对称、关系仍对称）无锚；核真全部正确。分片称其为本片区**唯一跨文件论证却零外部锚点**的节点（只挂一条 15.11 锚点）。 |
| 45 | `d:zero-element-orthogonal-to-every-element` | **E-1** | 0 | FIX（补锚 15.10.md:27、15.11.md:7） | 15.10.md:27, 15.11.md:7 | 两条推理 claim 全无锚而所需锚点全部合规可引；末句"这是定理 15.10 必须加非零假设的原因"正确且有洞见；有限维检查通过——只取 15.11.md:7 首句的非零假设，未擅自加有限维。 |

## 二 分片自评最重的条目

分片的自评分层有两套：形态上「D 型 3 例」为最重，形式 E 内部另分 E-1/E-2/E-3。
下列是分片明确加重语的条目，按分片给的强度排序。

**A. 分片明说「最该引却没引」类（缺口锚点存在、合规、有空额度，却被跳过）**

1. **L29** —— 分片小标题即「完美锚点就在被引行上方两行却被跳过」。缺的是 `15.10.md:119` 末句 "fundamental properties of norms that do not depend on the choice of inner product"，就在被引的 `:121` 正上方，而这条正是该节点全部增益所在。仅用 1/3 额度。
2. **L39** —— 分片小标题「两处所需锚点均存在、合规、且有 2 个空额度却未引」。缺 `:151` 与 `:157`。
3. **L24** —— 分片称「本批最严重」。6 条 claim 只 1 条有锚，仅 1/3 额度；缺 `:151`、`:157`。另带 `audit_fix: audit-A1` 标记，说明前次审计动过它但没碰锚点覆盖。
4. **L44** —— 分片称「本片区**唯一**跨文件论证却一条外部锚点都没有的节点」。缺 `15.10.md:45`、`:32`。
5. **L45** —— 两条推理 claim 全无锚，而所需锚点（`15.10.md:27` 68 字符、`15.11.md:7` 可截 79 字符）**全部合规可引**。

**B. 分片明说「本批/本片区最实质」类（内容与锚点相抵触）**

6. **L33** —— 分片称「本批最实质的缺陷」：D 型 + 与同胞节点 31/32 自相矛盾，且冲突的一方正是超出射程的一方。分片给了从严备选死因 `6`。
7. **L26** —— 分片称「**本片区最硬的事实性错误**」，且专门指出这与射程不足不同类：数学内容正确、锚点也覆盖重述本身，但对教材出处的陈述为假。分片同时核实行 39 的 (15.6)、行 31 的 (15.8) 均正确，据此判为孤立笔误而非系统性问题。
8. **L30** —— 分片称「本片区**唯一一个中心断言零逐字覆盖**的节点」，且成因完全是 SPEC 30 字符下限（`:123` 实测 29 字符，差 1 字符）。分片明确**不判生产者的死**。
9. **L43** —— 全域量词断言被同节内容反证，分片给了从严备选死因 `1`。
10. **L25** —— 末句被它自己所在段落的下半句反驳（`:91` 后半 + `:94`）。分片**未**给从严备选死因。

**C. 分片主动登记的正面条目（合并时可用来对照严重度基线）**

- L37、L42 判**支撑充分**；L42 的双锚覆盖被分片称为「本片区锚点策略最完整的一例」，L43 的对比两端双向锚定被称为「本片区最正确/最规范的锚点策略」。
- L38 的量词处理被称为「本片区做得最规范的一处」。
- L32、L35 的实/复分裂处理被登记为正面案例（显式且经人工验算正确）。
- 分片专门正面登记 `atomic_reason` 质量：L28、L30 自报精确字符数并与实测一致，L37 自陈开方步骤，L25 自陈第 12 章内容 "deliberately left unexpanded"。

## 三 未决与不确定项

### 3.1 分片自己列的 10 条未覆盖事项（§9，原文自陈，我逐条压缩）

| # | 未做/做不到 | 对合并的影响 |
| --- | --- | --- |
| 1 | 专项检查「有限维假设增删」在本片区**基本落空**（行 21–45 全落在 15.10 与 15.11 开头，那里无有限维定理）。只核了两处跨引（L45→Thm 15.10 首句、L31→(15.8)），均正确。 | 对 Thm 15.11/15.12 正面节点（含 Parseval）**无任何结论**。 |
| 2 | 任务书点名的那批符号值（`(e−1/e)/2`、`3/e`、`e²−1`、`pi − 2 sin x`、`x = 1 and y = i`）**不在本片区**，属 15.12 习题类。 | 对那批数值**无结论**，须由覆盖 15.12 的片区负责。 |
| 3 | Gram-Schmidt 与投影（15.13+）完全未涉及；尽管任务书把 D3/D4 描述为覆盖 15.09–15.12，本片区 sections 实测只有 15.10（23 个）与 15.11（2 个）。 | 任务书的片区范围描述与实测不符，须裁定方知悉。 |
| 4 | **未审查 A 型（变量/对象错）与 C 型（端点当中间步骤）**。审查中未偶遇明显实例，但分片明确声明这**不构成**"本片区无 A/C 型"的结论。 | A/C 型在本片区处于未查状态。 |
| 5 | B 型（借用引文）只做了**片区内 + 同文件**（nodes-D3.jsonl）范围的机器共享检测，未与 nodes-D1/D2/D4/X/A2-* 等做全局 (file,quote) 交叉比对。 | 跨文件借用引文**可能存在而未被发现**。§7 第 5 项的 5 对共享只是这个有限范围内的结果。 |
| 6 | **未读 `data/edges-D3.jsonl`，未审查任何边**。节点 `parent` 所指的 `apostol:` 上级（如 `apostol:weighted-integral-inner-product`、`apostol:theorem-15-9-properties-of-norms`）未核实是否真实存在于 `data/inherited/`；statement 与 parent 的一致性也未查。 | **本片全部指控只在 T1 层做出**，T2/T3 未验。见第五节。 |
| 7 | **第 12 章内容不可核**（`source/` 下只有 ch15、strang-ch3、strang-ch4）。L25 claim 3「定理 12.2 所列性质恰是内积公理」只能判为合理推断，**无法逐字核实**。 | L25 claim 3 悬空。 |
| 8 | 未复核 `grep -F` 锚点子串校验本身，直接采信"全部锚点为原文逐字子串"；但第 6 节的字符长度数字全部由分片重新实测。 | 锚点是否逐字存在于所标 file 中未逐条复验。 |
| 9 | 未读任何其它审查员输出（`审查-形式D-*` 其余、`裁定-*`、`_捞回-*`、`audit-*`），亦未读被污染的旧实验目录。分片明确声明**冲突时不应默认本报告为准**。 | 与其它分片的重复/冲突未消解。 |
| 10 | E-1/E-2/E-3 的**严重度分组是分片自设判据**，任务书未给分级标准。分片自陈：20 例 E 的**事实认定**（哪条 claim 无锚、缺口在哪、核真结果）比分组更可靠。 | 合并时应以事实认定为准，分组只作参考。 |

### 3.2 分片留给裁定方的 3 处死因待定

- **L33 claim 3**：`0` KEEP + 强制改写；「若审计从严，可按 `6`（锚点不支撑其所挂断言）计」。
- **L43 末句**：`0` KEEP + 强制改写；「若从严按全域量词计，可入 `1`」。
- **L41 claim 2**：分片称 `1` 的**低度候选**（真实但轻微的过度断言），未定。

### 3.3 分片自陈的表述不确/偏松处（非未决，但已标出不确定）

- **L30 claim 3** 用"逆否"一词「稍松但可接受」。
- **L35 claim 2** 的"两次"公理使用计数「偏松」（若把可加性与对称性分开数则为三条），但在复情形下自洽。
- **L36 claim 3** 的因果指派「要绕一道」——直接原因是复数算术 z+conj(z)=2Re(z)，(1') 只是先让 (y,x) 写成 conj((x,y))。
- **L42** 的 "vacuous" 用词「不确」，准确说是"无解/无定义"。
- **L31 claim 2** 的唯一性量词分片核为**经得起检验、非 `1`**（这是一条明确的否定裁决，合并时不要误当未决）。

## 四 分片自身的记账错

我把分片给出的**所有**可机械核对的数字（25 条 n_anchors、§6 的 9 个行长、`\tag{15.7}` 的唯一出现位置、
sections 分布）都用 `tmp_index/` 复核了一遍。**结论：字符数与行长零错误**，
n_anchors 与 nodes.tsv 逐条一致，`\tag{15.7}` 唯一性成立。记账错只出在**分类归组与内部计数**上。

**E-1 确认（分片自身的错，非我的新裁定）**

1. **§3 的 E 组表漏两条、多两条，20 例这个总数靠错误抵消才凑上。**
   §1 与 §5 都写「形式 E 确认 20 例」「支撑充分 2 例（行 37、42）」。但 §3 的三张组表实际列出：
   E-1 组 7 例（24/29/31/32/39/44/45）、E-2 组 9 例（21/22/23/28/30/34/35/36/41）、
   E-3 组 4 例（27/38/42/37）＝共 20 条。问题是：
   - **L37 与 L42 被放进 E-3 组**，而这两条在 §1/§5/§8 里都判**支撑充分**、明确不算在 23 个有缺陷节点内。§3 的 E-3 组内文也自陈"整体仍判支撑充分"，即组表与判定自相矛盾。
   - **L26 与 L40 在三张组表里都不出现**，而它们在 §8 里分别判"形式 E + 方程编号错引"与"形式 E（轻微）"，按 §5 的 23 例口径应当在 E 组内。
   净效果：多算 2（37、42）、漏算 2（26、40），总数恰好仍是 20，**掩盖了归组错误**。合并时若按 §3 的组表取 E 清单，会把两条支撑充分的节点当成缺陷、同时漏掉方程编号错引那条。

2. **L30 的形态标签在同一份报告内三处不一致。**
   §8 批 2 的小标题写「**形式 D**（射程不足），末句为确认缺陷」；§1 的总账把 D 型限定为 3 例（25、33、43），不含 30；§3 把 30 放进 E-2 组。§5 与 §3 的 E-2 条目确实注明"兼有 D 型特征"、"行 30 兼具两型特征、在 E-2 组记录"，所以这是**已披露但未统一**的双标。合并时需明确 L30 归 D 还是 E，否则 D 型计数在 3 与 4 之间摆动。

**E-2 用词不严（不影响数字，但会误导合并）**

3. **§6 第 3 项的"去重后共 4 例"表述混乱。** 该项的小标题只写"行 32 与行 30"，但结论句把受害集合列为"行 28、30、32，加上……行 45"。数字 4 是对的（28/30/32/45），但要读到句末才能拿到清单，且与小标题不符。

4. **§4.1 的"机器扫描命中 4 条全部为假阳性"未给命中的节点行号。** 假阳性成因（"de**finite**ness" 内含 "finite" 子串）写得很清楚，但 4 条具体是哪 4 个节点未列，无法复核。属信息缺失而非算错。

**未发现的错类（明确排除，供合并放心）**

- 未发现「把有锚说成无锚」或反向的错：我抽查了 L24/29/30/32/33/39/42/43/44/45 的 anchors.tsv 实际条目与引文，与分片描述一致（例如 L30 两锚确为 `:133` 与 `:121`、均不含 "‖x‖ = 0 if x = O"；L43 两锚确为 `:151` 的 183 字符片段与 `:105` 的 90 字符片段）。
- 未发现行号错引：`:19`=21 字符、`:22`=35、`:27`=68、`:32`=39、`:40`=86、`:108`=26、`:123`=29、`:125`=42、`:127`=40、`:129`=57、`:15`=49、`:17`=63 全部与 source-lines.tsv 的 charlen 列吻合。
  （**唯一微差**：分片 §8 批 3 把 `:32` 的 (1') 定义式称"可引"，而实测 `:32` 只有 **39 字符**——仍在 30–200 内，故"可引"成立，但分片多处建议"补锚 `:32`"时未提它离下限只有 9 字符。属提示不足，不是错。）

## 五 主通道统计

### 5.1 分片自己的层次：全部指控都是 T1 层

分片 §9 第 6 项明确「**未读 `data/edges-D3.jsonl`，故未审查任何边**」。
因此本片 23 条指控（3 D + 20 E）**无一例外只在 T1（节点自身 anchors）层做出**，
T2（自发边 evidence）与 T3（part-of 后代）**在分片内根本没进入视野**。
按四层证据池定义，这意味着本片每一条指控都**尚未排除 T2 塌陷的可能**。

### 5.2 我用 exposure.tsv / pool.tsv 补的 T2/T3 计数（只报数，不改分片裁决）

| 层 | 计数（本片 25 节点） |
| --- | --- |
| T3（part-of 后代证据） | **0 条，25 节点全部 `n_partof_desc = 0`** —— 本片无一节点有 part-of 后代，故 T3 通道在本片**结构性不存在**，不可能塌陷 |
| T2 存在（池里有自发边 evidence） | 24/25 节点有 T2；唯 L30 `d:norm-vanishes-at-the-zero-element` 的 T2=0（exposure.tsv） |
| T2 **引入新源行**（去重后 (file,quote) 不同于 T1 的） | **仅 5/25 节点**：L29(+1)、L31(+1)、L32(+1)、L34(+2)、L40(+1)。其余 20 节点的 T2 全是 T1 的逐字重复 |
| T1 去重后不同 (file,quote) 数 | 11 节点为 2、14 节点为 1（与 n_anchors 一致） |

按「冗余度的正确度量是**新引入的源行数**」这条口径：**T2 对本片 20/25 节点是纯重复、零增量**。

### 5.3 那 5 条有 T2 增量的，逐条看增量是否命中分片指认的缺口

- **L29**：T2 新增 `edges-H1.jsonl:12` 的 `15.10.md:119` 引文，但引的是该行**第一句** "Since it may be possible to define an inner product in many different ways, the norm of an element **will depend on** the choice of inner product."（142 字符）。分片指认的缺口是**同一行的末句** "fundamental properties of norms that **do not depend on** the choice of inner product"。同一源行的两半分别落在两处、且语义**恰好相反**——这正是过报通道 (b)/(c) 的教科书形态（`source-lines.tsv` 实测 `15.10.md:119` 有 **4 个句末标点**，明确禁止按行号做覆盖比对）。**T2 未命中缺口。**
- **L31**：T2 新增 `:133`（"follow at once from the axioms…"）。分片指认的缺口是 `:22`、`:151`、`15.11.md:52`。**未命中。**
- **L32**：T2 新增 `:133`，同上。分片指认的缺口是 `:40`、`:15`。**未命中。**
- **L34**：T2 新增 `:133` 与 `:145`。分片指认的缺口是 `:17`（公理(2)）与 `:139`。**未命中。**
- **L40**：T2 新增 `edges-D3.jsonl:40` 引的 `:151` **完整 183 字符**（含 "In a real Euclidean space V"）。分片指认的缺口是 `:125`（(b)）与 `:22`（公理(4)）——**未命中**；但分片同时抱怨"锚 1 切走了句首实空间限定、是被浪费的机会"，这一条**被 T2 补上了**（实空间限定确已在图里，只是记在边上）。这是本片**唯一一处 T2 部分塌陷**，性质为记账位置问题。

**小结（供合并裁决）**：本片 23 条指控中，T2 能构成塌陷抗辩的**至多 1 条**（L40 的实空间限定那一半），
其余 22 条的 T2 或不存在（L30）或为 T1 逐字重复或指向不相关行。T3 通道在本片不存在。
另附：TD（指向本片节点的边）确有若干条（如 L35 有 3 条、L39/L36/L43/L31 各 2 条，pool-TD-excluded.tsv），
按四层定义**一律不入池**，合并时不得用它们为本片节点抗辩。

### 5.4 死因 1 与死因 6 的分流提示

本片三处从严备选中，**L43 末句与 L41 claim 2 属死因 `1`（量词过度断言）**——按规则「死因 1 不因边证据而免除」，
即便日后发现相关边证据也**不能**免除；**L33 claim 3 属死因 `6`**，按规则死因 6 会因边证据免除，
但上表已显示 L33 的 T2 为 T1 逐字重复（`:133`，无增量），故该免除路径**在本片数据下不成立**。

## 顺带发现

- L26 的方程编号错引被分片判 `0`，但"statement 声称 (15.7) 是 CS 的编号"是对教材出处的虚假陈述，形态上更接近死因 `3`（虚构出处）而非 `0`；建议裁定方复核该死因归属。
- L25 与 L33/L43 结构同型（末句超射程），但分片只给 L33/L43 了从严备选死因，L25 只给 `0`；从严标准在分片内未一致施用。
- 本片 25 节点的 node_type 实测为 concept/theorem/**method**/**notation**（nodes.tsv），`method` 与 `notation` 不在封闭词表（concept/theorem/proof-step/example）内；分片 §7 只登记了 L35/36/37 之间 method 与 theorem 的**不一致**，未指出两个词本身**越出词表**。全 nodes-D3.jsonl 计 method 12 个、notation 5 个。
- 分片 §9 第 6 项存疑的 parent 节点存在性，索引可直接消解：`apostol:theorem-15-9-properties-of-norms`（inherited/nodes-A1.jsonl:55）、`apostol:orthogonal-elements`（:58）、`apostol:norm`（:53）、`apostol:norm-notation`（:54）均真实存在；tmp_index/README 亦报"悬空 id 引用 0 处"。
- 分片 §7 第 5 项的共享引文范围明显偏窄：`shared-quotes.tsv` 显示 L45 的 `15.11.md:98` 引文有 **8 个持有者**（含 edges-D4、node:d:15-10-nonzero-hypothesis-is-essential 等）、L30/L33 的 `:133` 有 **7 个**、L28/L43 的 `:105` 有 **6 个**，远多于分片"3 对片区内 + 2 对跨片区"的统计，B 型排查须重做。
- 分片 §7 第 6 项指出 `origin: model` 字段只在 edge schema、不在 node schema，故 23 个含无锚 claim 的节点无一能标 origin:model；分片建议上报 schema 设计而非记生产者——这条与本片 20 例 E 的成因直接相关，值得裁定方单列。
- 本片有 `audit_fix` 痕迹两处（L24 带 `audit-A1`、L40 带 `audit-B`），分片称 L40 的拆分干净、L24 的前次审计"没碰锚点覆盖"；若要追踪历次审计的遗漏模式，这两条是样本。
