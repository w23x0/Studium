# 形式 D / E 锚点支撑性审查 — nodes-D2.jsonl 第 36–58 节点

状态：已完成（23/23 节点全部审完）

## 1. 覆盖声明

审查范围：`data/nodes-D2.jsonl` 第 36–58 行（1-indexed，文件行序），**共 23 个节点，全部审完，无遗漏、无静默截断**。

| 行 | id |
| --- | --- |
| 36 | `d:exponential-independence-select-the-largest-exponent` |
| 37 | `d:exponential-independence-multiply-by-a-normalizing-exponential` |
| 38 | `d:exponential-law-used-in-the-shift` |
| 39 | `d:exponential-independence-negativity-of-the-shifted-exponents` |
| 40 | `d:exponential-independence-limit-argument-isolates-one-coefficient` |
| 41 | `d:exponential-independence-delete-the-killed-term-and-recurse` |
| 42 | `d:exponential-independence-uses-no-inner-product` |
| 43 | `d:thm-15-5-hypothesis-independent-set-of-k-elements` |
| 44 | `d:thm-15-5-conclusion-any-k-plus-one-elements-of-the-span-are-dependent` |
| 45 | `d:thm-15-5-proof-delegated-to-theorem-12-8` |
| 46 | `d:thm-15-5-generality-transfer-uses-only-the-linear-space-axioms` |
| 47 | `d:finite-basis-independence-clause` |
| 48 | `d:finite-basis-spanning-clause` |
| 49 | `d:finite-basis-two-clauses-are-logically-independent` |
| 50 | `d:finite-basis-finiteness-clause` |
| 51 | `d:finite-dimensional-defined-by-existence-of-a-finite-basis` |
| 52 | `d:zero-space-declared-finite-dimensional-by-fiat` |
| 53 | `d:infinite-dimensional-operational-criterion-no-finite-spanning-set` |
| 54 | `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets` |
| 55 | `d:thm-15-6-hypothesis-finite-dimensional-space` |
| 56 | `d:thm-15-6-apply-theorem-15-5-to-the-first-basis` |
| 57 | `d:thm-15-6-upgrade-from-k-plus-one-to-more-than-k` |
| 58 | `d:thm-15-6-first-inequality-m-at-most-k` |

实际覆盖的原文：`15.06.md`（全 31 行）、`15.07.md`（全 63 行）、`15.08.md`（全 39 行）逐行读过；`15.11.md`、`15.13.md`、`15.14.md`、`15.09-exercises.md`、`15.16-exercises.md` 就 basis / infinite 两个关键词做穷举检索（用于核行55 的章级否定断言）。未读任何其它审查员的输出文件，未读 `M08实验-Apostol微积分卷1/`。

**方法说明**：本报告不给"N/N 锚点已验证"式结论。我确实跑了一遍 `grep -F` 等价校验（22 条锚点全部逐字命中，2 个节点零锚点），但**这个数字没有证明力**，只用来确认我读到的锚点文本没被生产者改写。下面的判定全部来自逐 claim 手工映射。

**总体结论（先给结论）**：23 个节点中，**22 个的 statement 末句或倒数第二句存在锚点射程之外的断言**。前人"缺陷集中在末句"的经验在本片区被强化到近乎全覆盖 —— 生产者的写作模板是「首句复述锚点 + 末句加一条洞见」，而那条洞见系统性地在射程之外。其中 **5 个属实质缺陷（含 1 处数学错误、1 处量词过度断言、1 处归因错误、1 处自相矛盾）**，7 个属结论正确但必须补锚，10 个属可保留的轻度元评论。

分级：

| 级别 | 行号 | 处置 |
| --- | --- | --- |
| **一级：实质缺陷** | 36, 37, 42, 45, 54 | 不改则判死（死因见第 2 节） |
| **二级：确认形式 D/E，结论正确须补锚** | 43, 44, 46, 47, 48, 49, 52, 53, 57 | KEEP + 强制补锚/改写 |
| **三级：轻度射程不足（真元评论）** | 38, 39, 40, 41, 50, 51, 55, 56, 58 | KEEP，登记不判 |

## 2. 确认的形式 D 实例（射程不足 SCOPE-SHORTFALL）

### 2.1 一级：实质缺陷

**行36 `d:exponential-independence-select-the-largest-exponent` | KILL | 死因 6 + 4 | 归因错误**

claim 分解：
1. 取 a_M 为 n 个指数中的最大者。
2. 这用到实数的全序与指标集的有限性，故最大值存在。
3. **正是这个选择使其余所有指数都小一个严格负的量。**

锚点 `15.07.md:51` "Let $a_{M}$ be the largest of the n numbers $a_{1},\ldots ,a_{n}$ ." 的射程**止于 claim 1**。claim 2 无锚（真）。**claim 3 是错的**：仅"取最大值"只给出 `a_k - a_M ≤ 0`；`< 0` 的严格性来自 Example 7 的 *distinct* 假设（`15.07.md:39`），不来自这个选择动作。缺口不是"少了个锚点"，而是**把严格性的来源归错了对象**。同批行39 自己写 "This is where the distinctness hypothesis is consumed"，两节点对同一事实给出互斥归因。

改法：claim 3 改为「取最大值给出 a_k − a_M ≤ 0；严格负号另需 distinct 假设（行39）」。

**行37 `...multiply-by-a-normalizing-exponential` | KILL | 死因 6 | 依赖过度声明**

claim 分解：
1. 把关系式整体乘 e^{−a_M x}，各指数 a_k 被替换为 a_k − a_M。
2. **该步骤合法且可逆，因为指数函数从不取零。**
3. 它把第 M 项塌缩为常数 c_M。

锚点 `15.07.md:51` "Multiplying both members of (15.2) by $e^{-a_M x}$ , we obtain" 止于 "we obtain" —— **claim 1 的后半（替换结果）在锚点右侧的公式行 `:54`**，claim 3 也只能由 `:54` 推出。**claim 2 无锚且断言过强**：15.07 通篇不提指数非零；且证明只做 (15.2)→(15.3) 的**正向**乘法，正向乘法对任何函数都合法，**根本不需要非零性**（非零性只有在需要由 (15.3) 回推 (15.2) 时才用得上，而证明不需要）。缺口：声明了一个证明并未使用的依赖。

改法：删掉"可逆/从不取零"，或改为「正向乘法即足；若要反推才需非零性，而证明不需要」。锚点右扩/补 `:54` 以覆盖 claim 1 后半与 claim 3。

**行42 `d:exponential-independence-uses-no-inner-product` | KILL | 死因 4 | 工具清单一错一漏**

claim 分解：
1. **该论证用到：实数的序、指数加法律、指数的非零性、一个极限。**
2. 它不用内积、范数、正交性、微分、维数计数。
3. **这就是它能在全体实值函数空间中成立的原因。**

本节点 **0 锚点**，已标 `origin:model`，`origin_note` 声称"通过检视 Example 7 每一步得出"。claim 2 对 Example 7 **属实**，是本节点的实质价值（与 Example 6 的多项式证明确实用微分构成有效对照）。但 claim 1 同时出现两个方向的失实：

- **列入了证明不使用的项**：非零性（同行37，系统性同源误解）。
- **漏掉了证明最显眼的工具：数学归纳法**。`15.07.md:45` 明写 "We can prove this by induction on $n$ ."（39 字符，完全可锚），行41 自己就以归纳假设收尾。一份自称逐步检视得出的工具清单漏掉整个证明的组织原则，说明 `origin_note` 所述方法未真正执行。

claim 3（末句）是因果过度声明：全体实值函数空间上本来就没有自然内积/范数，"不用内积"不构成"能在该空间用"的**原因**；真正原因是逐点求值与极限在该空间有定义。

改法：claim 1 删非零性、补归纳法；claim 3 降为"因此可迁移到没有内积结构的空间"。改后 claim 2 值得保留。

**行45 `d:thm-15-5-proof-delegated-to-theorem-12-8` | KILL | 死因 6 + 1 | 越界且与同批节点自相矛盾**

claim 分解：
1. **Apostol 在 15.07 里没给出任何新论证。**
2. 他指出 V=V_n 时该命题即 Theorem 12.8，然后论证旧证明可移植。
3. **所以维数理论的承重墙在第 15 章内没有证明。**

锚点 `15.07.md:56` "When $V = V_{n}$ , Theorem 15.5 reduces to Theorem 12.8." 只覆盖 claim 2 的前半。claim 1 与 claim 3 **都与原文冲突**：`15.07.md:61` 以 "Proof." 起头，并给出一个实质性的元层论证（审查 12.8 的证明只依赖线性空间性质 ⇒ 移植合法 ⇒ 对任意 V 有效）。同批**行46 自己**把这一步称作 "This audit, not any new computation, is **what makes Theorem 15.5 hold** in arbitrary linear spaces" —— 即同一片区的两个节点，一个说"这一步使定理成立"，另一个说"没有证明"。

改法：claim 1/3 改为「第 15 章内没有**自足**证明，组合核心留在 12.8；章内提供的是移植合法性的元层论证」。

**行54 `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets` | KILL | 死因 1 | 量词/连接词过度断言**

claim 分解：
1. **"Equivalently"**：V 无限维 **⟺** 对每个 n 都能造出 n 个独立元素。
2. 若 V 有 k 元基，则由 15.5 每 k+1 元集相关，与判别法在 n=k+1 处矛盾。

本节点 **0 锚点**，已标 `origin:model`，`origin_note` 写 "Derived here from Theorem 15.5"。claim 2 只证了 **判别法 ⇒ 无限维** 这一个方向。**反向（无限维 ⇒ 对每个 n 存在 n 个独立元素）在本章资源内推不出来**：它需要"独立集不 span 时可加一个 span 外元素扩张"的引理，而 Apostol 唯一相关的 Theorem 15.7(a)（`15.08.md:21`）**前提恰是 dim V = n 有限**，用不上无限维情形。`origin_note` 只覆盖了已证的那一半，未声明另一半是缺口。

缺口即在 "Equivalently" 这个词：它宣称的是双向，手上只有单向。

改法：改为 "A sufficient criterion:"，或如实注明反向需基扩张引理、本章未提供。边界核查：零空间处一致（零空间连 1 个独立元素也造不出，n=1 即失败，与它被定义为有限维相容）✓。

### 2.2 二级：确认形式 D，结论正确但必须补锚

**行43 | KEEP + 强制改写 | 本片区最典型的形式 D**

claim：(1) 假设固定计数 k 且要求 S 独立 —— 锚点 `15.07.md:59` 前半命中。(2) **末句：独立性正是使 k 成为紧阈值的原因，也是定理说 k+1 而非更小数目的原因** —— 无锚。

Apostol 在 15.07 通篇**不讨论 k+1 的最优性/紧性**。该断言本身为真（S 自身即 L(S) 中 k 个独立元素，故 k 元集不必然相关，k+1 确实紧），但这是**需要独立补证的定理级事实**，绝非"取一个 k 元独立集"这句话说得出来的。锚点射程止于"假设是什么"，末句却断言"这个假设的最优性"——**这是本片区最纯粹的射程不足样本，且它极易通过主题相关性抽查**（引文确实在讲独立集和 k）。

改法：末句改为"（推论，非原文）k+1 是紧的，因 S 自身即 L(S) 中 k 个独立元素"，并标 `origin` 混合。

**行44 | KEEP + 补锚 | 有限性依据落在另一节**

claim：(1) 结论全称量化于 L(S) 的 k+1 元集而非 V —— 锚点 `15.07.md:59` 后半命中 ✓。(2) **末句：这 k+1 个元素不必在 S 中，只需能表为 S 成员的有限组合** —— 无锚于 15.07。

L(S) = 全体**有限**线性组合的定义在 `15.06.md:19`，而本节点 `sections` 只写 `["15.07"]`、锚点只有 1 条。生产者用词 "finite combinations" 与原文 "finite linear combination" 一致，说明他知道有限性要紧，只是没把依据挂上。修法：补 `15.06.md:19` 为第二锚点，`sections` 加 15.06（同片区行49、行52 已如此处理，故属遗漏而非能力问题）。

**行46 | KEEP + 补锚 | 射程止于前提，未及结论**

claim：(1) 重用 12.8 证明的理由是对该证明的审查，只依赖 V_n 是线性空间 —— 锚点命中 122 字符（含原文 OCR 噪声 "nc on any other"，未修正，合规）✓。(2) **末句结论：正是这一审查使 15.5 在任意线性空间成立** —— 锚点止于 "...special property of $V_{n}$ ."，"在任意线性空间成立"的依据是紧接的 "Therefore the proof given for Theorem 12.8 is valid for any linear space $V$ ."（78 字符，已验证存在）。修法：补为第二锚点（当前 1 锚点，不触上限）。

**行47 | KEEP + 补锚 | 同文件内可锚而未锚**

claim：(1) 有限基定义的两从句之一 —— 锚点命中整句定义 ✓。(2) **末句：独立性正是使表示唯一的东西；去掉它则仍可 span 但系数不再确定** —— 无锚。唯一性论证就在同一文件 `15.08.md:35`（"But since the basis elements are independent, this implies $c_{i}=d_{i}$ for each i"，83 字符）。修法：补第二锚点即闭合。

**行48 | KEEP + 补锚**

claim：(1) 第二从句 —— 锚点（行47 锚点的 62 字符子串）命中 ✓。(2) **末句：span 正是使表示存在的东西；去掉它，独立集只表示它自己 span 内的元素** —— 后半是 `15.06.md:19` 的 L(S) 定义之同义重述，可锚未锚。

**行49 | KEEP + 强制补锚 | 谓词落在锚点之外（形式 D）+ 见证例整体无锚（形式 E）**

claim：(1) 两从句互不推出（已标 `origin:model` ✓）。(2) **{O,i,−i,j,−j,i+j} span V_2 而不独立**。(3) **V_2 中单个非零向量独立而不 span V_2**。

锚点 `15.06.md:21` 的 51 字符 "$\{i,j\}$ , $\{i,j,i+j\}$ , $\{O,i,-i,j,-j,i+j\}$ ." **只列出了集合，不含 "span V_2" 这个谓词** —— 谓词在同一行前半的 "the space $V_{2}$ is spanned by each of the following sets of vectors:"。**引文的主题对、对象对，但断言的谓词在射程之外，这是标准形式 D。** 修法：锚点左扩至 122 字符（实测合规，远在 200 上限内）。

claim 2 的"不独立"这半也无锚：依据是 `15.07.md:23` Example 3（O∈S ⇒ 相关，48 字符）或 `:21` Example 2（一元素为另一元素标量倍 ⇒ 相关，73 字符），而 `sections` 未列 15.07。claim 3 **完全无锚**，15.06/15.08 中无此例。

关键问题：`origin:model` 只声明了"两从句互不推出"这个**结论**是模型推的，**没有声明两个见证例的关键谓词也是模型补的**，声明不足。

**行52 | KEEP + 补锚 | 形式 E（详见第 3 节）**

**行53 | KEEP + 补锚 | 结论落在锚点左侧**

claim：(1) Example 4 给出判别法的可用形式。(2) **多项式空间是无限维的**，因为虽有无限集 span 它却无有限集 span 它。(3) **末句：失败点在 span，而不在独立性。**

锚点 `15.08.md:17` 从 "Although the infinite set..." 起（115 字符），**"The space of all polynomials $p(t)$ is infinite-dimensional." 在同一行前半、锚点之外** —— 锚点覆盖了理由，未覆盖结论。修法：左扩取整个 Example 4 = 187 字符（实测合规）。claim 3 无锚：对多项式空间为真（`{1,t,t²,…}` 独立，见 `15.07.md:31` Example 6），但作为对"无限维"的一般刻画写法含混（"任何无限维空间都在 span 上失败"近乎同义反复）。

**行57 | KEEP + 补锚 | 形式 E（详见第 3 节）**

### 2.3 三级：轻度射程不足（登记不判）

- **行38** —— 锚点是 `15.07.md:54` 的公式 (15.3)，即**加法律的结果**而非定律本身。claim「这是指数函数的性质而非线性空间的性质」在 15.07 内**不可锚**（e^a·e^b=e^{a+b} 全节不出现），末句反事实「没有它这个乘法完全不会简化」亦无锚。另：本节点**未标 `origin:model`**，而同批行42/49/54 都标了，按生产者自己的约定即为不一致。
- **行39** —— 锚点完整覆盖首句。「这里消耗了 distinctness 假设」无锚，但 `15.07.md:39` 的 78 字符句可直接补为第二锚点（当前 1 锚点）。
- **行40** —— 锚点 120 字符逐字覆盖"x→+∞ / k≠M 项趋零 / c_M=0"三要素 ✓。末句「这是分析性步骤，用到极限与指数衰减，而这些概念不在线性空间公理中」为需回 15.02/15.03 公理表核的元评论（属实）。
- **行41** —— 首尾 claim 均由 146 字符锚点覆盖 ✓。仅"余下 n−1 个指数**互不相同**"不在锚点内（distinct 集合的子集自动继承，轻微）。
- **行50** —— 有限性是**规定**而非导出，与原文措辞相符 ✓。末句「它使 15.6 的元素计数成为良定义整数」无锚（15.6 在同文件 `:5`，可锚）。
- **行51** —— 锚点 95 字符含**两个析取项**，而 statement 只讲第一项（覆盖不足于锚点的反向情形，非缺陷，零空间另由行52 分管）。末句「这正是为什么给 V 附数之前需要 15.6」为节点自作的动机说明。
- **行55** —— 见第 6 节（章级否定存在断言，SPEC 结构性盲区，不判生产者）。
- **行56** —— 本片区**分析质量最高**的一条。锚点写 "every set of k+1 elements **in V**"，而 15.5 原文说 **in L(S)**；该替换合法且**是 Apostol 自己做的**（S span V ⇒ L(S)=V），锚点直接见证，与行44 强调"in L(S) 而非 V"不矛盾 ✓。仅"span 用于把 L(S) 认同为 V"这一步在锚点内是隐含的（锚点未出现 L(S)），需回 `15.07.md:59` + `15.06.md:19` 才闭合。
- **行58** —— 锚点命中 ✓。末句「此处只用到 T 的独立性，span 性质还用不上」为真元评论（span 在下一句 "The same argument with S and T interchanged" 交换角色时才用上）。

## 3. 确认的形式 E 实例（无锚点从句）

**行52 `d:zero-space-declared-finite-dimensional-by-fiat` | KEEP + 补锚 | 死因 0**

claim 分解：(1) "or if V consists of O alone" 是第二析取项而非第一项的推论 —— 锚点 1（`15.08.md:3`）✓。(2) **由 Example 4，空集是独立的** —— **无锚**。(3) 由 15.06 约定空集 span {O} —— 锚点 2（`15.06.md:19`）逐字命中 ✓。(4) **末句：Apostol 单列此情形是为了不让定义依赖那两条边界约定** —— 作者意图推测，原文无任何依据，不可锚。

claim 2 是形式 E：Example 4 在 `15.07.md:25`，节点 `sections` **已含 15.07**，且**只用了 2 个锚点、尚有 1 个空位** —— 纯遗漏。注意长度：裸句 "The empty set is independent." 为 **29 字符，恰差 1 字符触 SPEC 下限**；加前缀作 "EXAMPLE 4. The empty set is independent." 为 40 字符，合规可用。故 SPEC 下限**不构成真实阻碍**。

数学核查：空集有限 ✓、独立（15.07:25）✓、span {O}（15.06:19）✓ ⇒ 节点"形式上冗余"的判断**正确**。claim 4 建议降格为"效果上使定义不依赖那两条边界约定"（去掉意图归因）。

**行57 `d:thm-15-6-upgrade-from-k-plus-one-to-more-than-k` | KEEP + 补锚 | 死因 0**

claim 分解：(1) 15.5 说 k+1，证明却需要"多于 k"。(2) 这一升级在原文中是沉默的。(3) **它依赖"含相关子集的集合是相关的"，即 15.07 的 Example 1** —— **无锚**。

锚点 `15.08.md:7` 的 63 字符 "Therefore, every set of more than k elements in V is dependent." **恰好见证了"沉默"本身**（原文只有一个 "Therefore," 没有任何理由）—— 这是本片区**锚点选取最贴切**的一条。但 claim 3 **点名引用了原文的 Example 1 却不给锚点**：Example 1 在 `15.07.md:19`（77 字符，已验证存在），`sections` 只写 `["15.08"]`、只用 1 个锚点。这是最容易补、也最不该漏的一类形式 E。

数学核查：Example 1 确实说 "If a subset T of a set S is dependent, then S itself is dependent."，对"多于 k"元的集合取其任一 k+1 元子集即得（对无限集亦成立）✓。

**行49 的两处形式 E** —— 见 2.2：claim 2 的"不独立"半句（依据在未列入 sections 的 15.07）与 claim 3（V_2 中单个非零向量，源文件中无此例，整句无锚）。

**行42、行54：整节点零锚点** —— 两者都走了 SPEC 第 55 行的逃逸口（`origin: model` + 如实说明），形式上不算违反"至少 1 条"。但两者的 `origin_note` 都**只覆盖了实际断言的一部分**（行42 的 note 声称检视了每一步，实际清单一错一漏；行54 的 note 只覆盖已证的单向），即**逃逸口被用来承载了比声明更强的断言**。这是形式 E 在 `origin:model` 节点上的变体：不是"没有锚点"，而是"声明没有覆盖到全部无锚内容"。

## 4. 有限维假设与量词专项检查

### 4.1 有限维假设的增删 —— 未发现任何一例

逐条核过本片区所有引用定理的节点：

| 行 | 引用的定理 | 原文有无有限维假设 | 节点是否忠实 |
| --- | --- | --- | --- |
| 43, 44 | Theorem 15.5（`15.07.md:59`） | **无**（只要求 S 独立、k 元） | ✓ 未擅加 |
| 45, 46 | Theorem 15.5 的证明（`:61`） | 无 | ✓ |
| 54 | 引用 15.5 反证 | 无 | ✓ 未擅加 |
| 55 | Theorem 15.6（`15.08.md:5`） | **有**（"Let $V$ be a finite-dimensional linear space"） | ✓ 保留且明确点出 |
| 56, 57, 58 | 15.6 的证明步骤（`:7`） | 承 15.6 前提 | ✓ |
| 51, 52 | finite dimensional 定义（`:3`） | —— | ✓ |

**结论：本片区无"给原文没有有限维假设的定理擅加该假设"，也无"抹掉原文有的"。** 行55 尤其干净：它把 15.6 的有限维前提当作**受审对象**（"stated only for finite-dimensional V"）而非默认背景，这正是任务书提示的 `15.11.md:7`（15.10 首句无有限维假设）vs `:43`/`:77`（15.11/15.12 有）那类陷阱的正确处理方式。

### 4.2 量词专项

**发现 1 例过度断言：行54 的 "Equivalently"**（详见 2.1）。宣称双向等价，只给出单向论证；反向需基扩张引理，而 Apostol 的 15.7(a) 前提是有限维、用不上。→ 死因 1。

**准量词问题 1 例：行43 的"紧阈值"**（详见 2.2）。不是量词错，而是把一个未证的最优性断言接在只讲假设的锚点后面。

**核过未发现问题的量词点**：
- 行44 的 "universally quantified over **all** sets of k+1 elements" —— 与原文 "every set of $k+1$ elements" 一致 ✓；且正确把范围限定在 L(S) 而非 V ✓。
- 行56 把 15.5 的 "in L(S)" 用作 "in V" —— **合法，且是 Apostol 自己做的替换**（S span V ⇒ L(S)=V），锚点直接见证 ✓。
- 行57 的 "every set of more than k" 与原文一致 ✓。
- 行58 的 "cannot have more than k elements" 与 `m \leq k` 一致 ✓。
- 行42 的否定清单（"no inner product, no norm, no orthogonality, no differentiation, no dimension count"）—— 对 Example 7 逐项核过，**否定部分全部属实** ✓（问题在正面清单，见 2.1）。

### 4.3 有限/无限 与 distinct（集合式定义）专项

Apostol 的 dependence 定义（`15.07.md:3`）建立在 "a **finite** set of **distinct** elements in S" 上，是集合式而非列表式；independence 是"不 dependent"的残余定义（`:9`）；`:17` 明确 "the present definition is not restricted to finite sets"。据此核：

- **行41**："余下关系式含 n−1 个指数**互不相同**的指数函数" —— distinct 不在锚点内，但作为 distinct 集合的子集自动继承，数学无误（轻，见 2.3）。
- **行44**：用 "finite combinations" 与原文 "finite linear combination" 一致 ✓，但依据在未挂锚的 `15.06.md:19`（形式 D，见 2.2）。
- **行52**：空集独立 —— 依据 `15.07.md:25` Example 4，数学正确，缺锚（形式 E）。
- **行53/54**：无限维处理 —— 行53 保持残余（负向）定义的形态 ✓；行54 试图给出正向刻画而量词越界 ✗。
- **行55**：确认全章无"无限基"概念（穷举核验见 2.3 与第 6 节），节点判断属实。
- **未发现任何一例把集合式定义误当列表式**（即误允许重复元素）。本片区节点未触及"重复元素"话题 —— 这既是没出错，也说明该陷阱在本片区**未被覆盖**（见第 9 节）。

## 5. 判定支撑充分的节点 id 清单

以下 9 个节点，逐 claim 映射后**未发现锚点射程之外的实质断言**（末句若为元评论则内容属实、不夸大，可保留）：

- `d:exponential-independence-negativity-of-the-shifted-exponents`（行39）
- `d:exponential-independence-limit-argument-isolates-one-coefficient`（行40）
- `d:exponential-independence-delete-the-killed-term-and-recurse`（行41）
- `d:finite-basis-finiteness-clause`（行50）
- `d:finite-dimensional-defined-by-existence-of-a-finite-basis`（行51）
- `d:thm-15-6-hypothesis-finite-dimensional-space`（行55）
- `d:thm-15-6-apply-theorem-15-5-to-the-first-basis`（行56）
- `d:thm-15-6-first-inequality-m-at-most-k`（行58）
- `d:exponential-law-used-in-the-shift`（行38）—— 有保留：它的核心断言在 15.07 内**结构性不可锚**（见第 6 节），且未标 `origin:model`；判"充分"是就其数学内容而言，不是就其锚点合规性而言。

**明确不列入本清单的**：行36、37、42、45、54（一级缺陷），行43、44、46、47、48、49、52、53、57（二级，须补锚后才算充分）。

## 6. 因 SPEC 30 字符下限 / 3 锚点上限导致证据不足的实例计数

**结论先行：本片区因 SPEC 长度/数量约束而真正无法取证的实例 = 0。** 另有 2 例属 SPEC 的**其它**结构性盲区（非长度、非数量），另计。

### 6.1 3 锚点上限：0 例（约束在本片区完全不紧）

23 个节点的锚点数：1 条 × 20、2 条 × 1（行52）、0 条 × 2（行42/54）。**没有任何一个节点用满 3 条。** 我在第 2、3 节提出的全部补锚建议（行39、44、46、47、48、49、50、52、53、57）都能在 3 条以内完成。**"3 锚点上限造成证据不足"在本片区不成立** —— 缺口来自遗漏，不来自约束。

### 6.2 30 字符下限：0 例（唯一逼近的一例可加前缀绕过）

实测了 12 条候选补锚引文（见附录批 3 记录），长度分布 39–187 字符，全部合规。**唯一逼近下限的一例**：行52 需要的 Example 4 裸句 "The empty set is independent." = **29 字符，差 1 字符**。但加上原文自带的 "EXAMPLE 4. " 前缀后为 **40 字符**，合规且不违反"不改标点、不跨行"。故该例**不构成真实阻碍**。

任务书提到的 Axiom 5–9 公式过短问题（16/18/22/22/36 字符）**在本片区不触发**：15.06–15.08 的核心内容是定义与定理的散文陈述，长度充裕；本片区没有任何节点需要直接引用公理方程。唯一涉及公理表的是行40 末句"这些概念不在线性空间公理中"，但那是**否定存在断言**（见 6.3），不是"想引公式引不了"。

### 6.3 SPEC 的其它结构性盲区：2 例（登记，不判生产者）

- **行55**：「Apostol 在本章从未引入无限基」是**章级否定存在断言**。逐字锚点机制原理上**无法见证"没有"** —— 要证它需遍历全章。我已代为穷举核验（`grep -c basis\|bases`：15.06:1、15.08:13、15.09-ex:2、15.11:9、15.13:5、15.14:3、15.16-ex:2，逐条读过，全部为 finite/orthogonal/orthonormal basis 或 "basis of n elements"；`15.13.md:3` 的 "finite or infinite dimensional" 与 `:5` 的 "finite or infinite sequence" 指的是空间与序列，**无一处把无限集称为 basis**）⇒ **断言属实**。SPEC 缺一个"否定存在断言需附检索证据"的条目。
- **行38（及行40 末句、行42 claim 2）**：核心断言是"**某工具/某性质不属于本节的资源**"（指数加法律不是线性空间性质；极限不在公理中；不用内积）。这类断言的依据**在源文件中根本不存在**（15.07 全节不写 e^a·e^b=e^{a+b}），不是长度问题也不是数量问题，而是**源缺失**。SPEC 第 55 行的处置（找不到逐字依据 ⇒ 不收录或标 `origin: model`）在此适用，但行38 **未标** `origin:model`（行42 标了）—— 属生产者的合规遗漏，非 SPEC 缺陷。

## 7. 其它维度问题登记（不裁定）

1. **同批节点自相矛盾（行45 vs 行46）** —— 行45 称"第 15 章内没有证明"，行46 称同一步骤"正是使 15.5 成立的东西"。已在 2.1 判行45，此处登记为**片区内一致性问题**，供裁定员决定是否两条一并改写。
2. **系统性同源误解：指数非零性（行37 claim 2、行42 claim 1）** —— 同一处误解出现在两个节点，说明不是笔误。建议一并修，并检查片区外是否还有第三处。
3. **`origin:model` 标注不一致** —— 行42/49/54 标了，行38（核心断言在本节不可锚）未标。生产者知道并使用这个约定，故属遗漏。
4. **`origin_note` 覆盖不足（行42、行49、行54）** —— note 声明的范围小于 statement 中实际的无锚内容（详见第 3 节末）。
5. **`sections` 字段与实际依赖不符（3 例）** —— 行44 依赖 15.06 而 sections 只写 15.07；行49 依赖 15.07（Example 2/3）而 sections 写 15.06+15.08；行57 依赖 15.07（Example 1）而 sections 只写 15.08。对照：行49、行52 会为跨节依赖列多个 section，说明约定存在，这 3 例是漏填。
6. **锚点真包含（B 型温和形态，行47/48）** —— 行48 的 62 字符锚点是行47 的 110 字符锚点的子串，两节点各自声称"我管定义的这一个从句"，但锚点本身不能区分二者。其余同行共用的情形（行39/40/41 同出 `15.07.md:57`；行43/44 同出 `:59`；行45/46 同出 `:61`）均为**不重叠子串**，分工干净 ✓。
7. **statement 覆盖不足于锚点（行51，反向情形）** —— 锚点 95 字符含"or if V consists of O alone"两个析取项，statement 只讲第一项。不判缺陷（零空间由行52 专管），仅登记这个方向也存在不对齐。
8. **`atomic_reason` 与实际拆分的张力（行42）** —— 写 "it enumerates rather than decomposes"，但既然是枚举，就应逐项可核；实际枚举出一个假项、漏一个真项。该字段在此未起到自查作用。
9. **未审边文件** —— `data/edges-D2.jsonl` 中涉及这 23 个 id 的边及其 `evidence` 未纳入本次审查（任务书主攻 statement 的 D/E 型）。见第 9 节。

## 8. 附录：逐批工作记录

### 批 1：行 36–40（15.07 Example 7 指数函数无关性证明步骤）

已读 `source/apostol-ch15/15.07.md` 全文（63 行），逐节点拆 claim：

- **行36 `d:exponential-independence-select-the-largest-exponent`** — 3 条 claim。C1「取 a_M 为 n 个指数中最大者」锚点逐字命中（`15.07.md:51`）。C2「使用了实数的全序与指标集的有限性，故最大值存在」**无锚**（真，但 Apostol 未作此陈述）。C3「正是这个选择使其余指数都严格更小」**无锚且归因错误**：仅取最大值只给出 `a_k - a_M ≤ 0`，严格负号来自 Example 7 假设中的 *distinct*（`15.07.md:39`），而非"取最大"这个动作。与行39自己的说法（"This is where the distinctness hypothesis is consumed"）直接冲突。→ 形式 D + 归因错误。
- **行37 `...multiply-by-a-normalizing-exponential`** — 3 条 claim。C1 前半（乘 e^{-a_M x}）锚点命中，后半（各指数被替换为 a_k - a_M）落在锚点句尾 "we obtain" 之外，实际结果在 `:54` 的公式行。C2「该步骤合法且**可逆**，因为指数函数**从不取零**」**无锚**：15.07 通篇未提指数非零，且证明只需正向推导，根本不需要可逆性/非零性。→ 形式 D + 依赖过度声明。C3「把第 M 项塌缩为常数 c_M」无锚（可由 `:54` 推出）。
- **行38 `d:exponential-law-used-in-the-shift`** — 锚点是 `:54` 的公式 (15.3)，即**该定律的结果**，不是定律本身。C1「这是指数函数的性质而非线性空间的性质」**无锚**：加法律 e^a·e^b=e^{a+b} 在 15.07 中根本不出现，此断言在本节内不可锚。C2（末句）「没有它这个乘法完全不会简化关系式」为反事实推断，无锚。该节点**未标 origin:model**，而同批的行42/49/54 都标了 —— 按生产者自己的约定即为不一致。→ 形式 D。
- **行39 `...negativity-of-the-shifted-exponents`** — C1 锚点逐字命中且完整。C2「这里消耗了 distinctness 假设」**无锚**，但 `15.07.md:39` 有 "If $a_1, \ldots, a_n$ are distinct real numbers" 可作第二锚点（当前只用 1 个锚点，未触 3 锚点上限）。C3 反事实（并列会给出零指数、该项不衰减）无锚但正确。→ 形式 D（轻，可直接补锚修复）。
- **行40 `...limit-argument-isolates-one-coefficient`** — C1 锚点是完整句，逐字覆盖 "x→+∞ / 各 k≠M 项趋零 / c_M=0" 三要素 ✓。C2（末句）「这是决定性步骤，且是分析性的：用到极限与指数衰减，这些概念在线性空间公理中不存在」**无锚**，需回 15.02/15.03 公理表核（属实）。→ 形式 D（轻，元评论）。

批 1 小结：5 个节点全部呈现"首句锚点命中 + 末句无锚洞见"的形态；其中行36 的末句为**归因错误**，行37 的"可逆/非零"为**依赖过度声明**，其余为可修复的射程不足。

### 批 2：行 41–45（Example 7 收尾 + Theorem 15.5 拆解）

- **行41 `...delete-the-killed-term-and-recurse`** — 3 条 claim。C1（删去第 M 项）、C3（归纳假设杀掉余下系数）锚点逐字命中（`15.07.md:57`，146 字符，含 "n-1 coefficients"）。C2「余下关系式含 n-1 个**指数互不相同**的指数函数」中"互不相同"不在锚点内（锚点只说 "remaining n-1 coefficients"）；此为 distinct 集合的子集自动继承的性质，轻微。→ 支撑基本充分（轻射程不足）。
- **行42 `d:exponential-independence-uses-no-inner-product`** — **0 锚点**，已标 `origin:model`。3 条 claim。C2（否定清单：无内积/范数/正交/微分/维数计数）**对 Example 7 属实**，是本节点的实质价值。但 **C1 的正面清单有两处硬错**：
  - **列入了证明并不使用的东西**：「nonvanishing of the exponential」。Apostol 只做 (15.2)→(15.3) 的**正向**乘法，正向乘法对任意函数都合法，不需要 e^{-a_M x}≠0；非零性只有在需要**反向**（由 15.3 回推 15.2）时才用得上，而证明不需要反向。→ **数学错误（死因 4 级别的失实断言）**，且与行37 的 C2 同源，是同一处系统性误解。
  - **漏掉了证明最显眼的工具：数学归纳法**。`15.07.md:45` 明写 "We can prove this by induction on $n$ ."（39 字符，可锚），行41 自己也以归纳假设收尾。一份声称"逐步检视 Example 7 每一步得出"的工具清单漏掉归纳法，说明 `origin_note` 所述的检视方法并未真正执行。
  - C3「这就是它能在全体实值函数空间中成立的原因」为因果过度声明：该空间上本来就没有自然内积/范数可用，"不用内积"并不构成"能在该空间用"的**原因**（真正原因是逐点求值与极限在该空间有定义）。该空间的定义在 `15.07.md:27`（"the underlying linear space V is the set of all real-valued functions defined on the real line"），可锚但未锚。
  → 判：**形式 D + 数学错误**。建议：删掉 nonvanishing、补入 induction，把 C3 降格为"因此可用于"而非"因此才能用"。
- **行43 `d:thm-15-5-hypothesis-independent-set-of-k-elements`** — C1（固定计数 k + 要求 S 独立）锚点命中（`15.07.md:59`）。**C2（末句）「独立性正是使 k 成为紧阈值的原因，也是定理说 k+1 而非更小数目的原因」无锚**：Apostol 在 15.07 通篇没有讨论 k+1 的**最优性/紧性**。该断言本身为真（S 自身是 L(S) 中 k 个独立元素，故 k 个不必然相关，k+1 确实是紧的），但这是**审查者需自行补证的定理级事实**，不是"取一个 k 元独立集"这句话说得出来的。→ **形式 D 确认，且是本片区最典型的一例**：锚点管住"假设是什么"，末句却断言"这个假设的最优性"。
- **行44 `d:thm-15-5-conclusion-...`** — C1（全称量化于 L(S) 而非 V 的 k+1 元集）锚点逐字命中。**C2（末句）「这 k+1 个元素不必在 S 中，只需能表为 S 成员的有限组合」无锚于 15.07**：L(S)=全体**有限**线性组合的定义在 `15.06.md:19`，而本节点 `sections` 只写了 `["15.07"]`，锚点也只有 15.07 一条。→ **形式 D/E 确认**。注：生产者用词 "finite combinations" 与原文 "finite linear combination" 一致，说明他知道有限性要紧，只是没把依据挂上。修法：加第二锚点 `15.06.md:19` 的 L(S) 定义句，`sections` 补 15.06（同片区行49、行52 已如此处理，故非能力问题而是遗漏）。
- **行45 `d:thm-15-5-proof-delegated-to-theorem-12-8`** — 3 条 claim。C2 前半（V=V_n 时归结为 12.8）锚点命中。**C1「Apostol 在 15.07 里没给出任何新论证」与 C3（末句）「维数理论的承重墙在第 15 章内没有证明」双双越界**：`15.07.md:61` 以 "Proof." 起头，并给出了一个实质性的**元层论证**（审查 12.8 的证明只依赖线性空间性质 ⇒ 移植合法）。行46 自己就把这一步称作 "This audit, not any new computation, is what makes Theorem 15.5 hold" —— 即同批节点承认它是**使定理成立的东西**，行45 却称其为"没有证明"。→ **形式 D + 与同批节点自相矛盾**。准确说法应是"第 15 章内没有**自足**证明，组合核心在 12.8"。

### 批 3：行 46–50（15.5 的移植论证 + 15.08 有限基定义三分）

- **行46 `d:thm-15-5-generality-transfer-uses-only-the-linear-space-axioms`** — C1（重用 12.8 证明的理由是对该证明的审查，只依赖 V_n 是线性空间）锚点逐字命中 122 字符（含原文 OCR 噪声 "nc on any other"，未修正，合规）。**C2（末句）「正是这一审查而非任何新计算，使 15.5 在任意线性空间中成立」的结论部分无锚**：锚点止于 "...special property of $V_n$ ."，而 `15.07.md:61` 紧接的 "Therefore the proof given for Theorem 12.8 is valid for any linear space $V$ ."（78 字符）才是"在任意线性空间成立"的依据。→ **形式 D（射程止于前提，未及结论）**，修法：补该 78 字符句为第二锚点（当前仅 1 锚点，不触上限）。
- **行47 `d:finite-basis-independence-clause`** — C1（有限基定义的两个从句之一）锚点命中整句定义（`15.08.md:3`）。**C2（末句）「独立性正是使元素表示唯一的东西；去掉它则集合仍可 span 但系数不再被确定」无锚**：唯一性论证在同文件 `15.08.md:35`（"But since the basis elements are independent, this implies $c_{i}=d_{i}$ for each i"，83 字符）。→ **形式 D，且是同文件内可锚而未锚**，修法直接补第二锚点。
- **行48 `d:finite-basis-spanning-clause`** — 锚点为行47 锚点的 62 字符**子串**（同一句 `15.08.md:3`）。C1 命中。**C2（末句）「span 正是使表示存在的东西；去掉它，独立集只表示它自己 span 内的元素」无锚**——后半实为 `15.06.md:19` 的 L(S) 定义之同义重述，可锚未锚。→ **形式 D**。另登记：行47/行48 的锚点互为子串，同一句被两个节点当作各自"那一个从句"的依据，锚点本身不能区分二者（B 型的温和形态），见第 7 节。
- **行49 `d:finite-basis-two-clauses-are-logically-independent`** — 已标 `origin:model`，1 锚点（`15.06.md:21` 的三个集合列举，51 字符）。3 条 claim：
  - C2「{O,i,-i,j,-j,i+j} span V_2 而不独立」——**锚点只列出了集合，没有包含"span V_2"这个谓词**。谓词在同一行的前半句 "the space $V_{2}$ is spanned by each of the following sets of vectors:"。**这正是标准的形式 D：引文的主题对、对象对，但断言的谓词在射程之外。** 修法：锚点左扩至 122 字符（实测 `15.06.md:21` 上取 "the space $V_{2}$ is spanned by each of the following sets of vectors: ...$\{O,i,-i,j,-j,i+j\}$" = 122 字符，远在 200 上限内）。
  - "不独立"这半也无锚：依据是 `15.07.md:23` Example 3（O∈S ⇒ S 相关，48 字符）或 `:21` Example 2（一元素是另一元素的标量倍 ⇒ 相关，73 字符）；而本节点 `sections` 为 `["15.06","15.08"]`，**未列 15.07**。→ 形式 E。
  - C3「V_2 中单个非零向量独立而不 span V_2」**完全无锚**，15.06/15.08 中无此例。→ 形式 E。
  → 判：**形式 D + 形式 E（两个见证例中，一个谓词越界、一个整体无锚）**。`origin:model` 只声明了"两从句互不推出"这个结论是模型推的，没有声明**两个见证例的关键谓词也是模型补的**，声明不足。
- **行50 `d:finite-basis-finiteness-clause`** — C1（定义说的是有限集 S）、C2（有限性是**规定**而非由独立性或 span 导出）锚点命中且论断与原文措辞相符。**C3（末句）「它使 Theorem 15.6 中的元素计数成为良定义的整数」无锚**：15.6 在同文件 `:5`，可锚未锚，属轻度射程不足（"良定义整数"这个说法是解释性的，原文不作此语）。→ 形式 D（轻）。

批 2–3 小结：**末句集中命中率极高** —— 行42/43/44/45/46/47/48/50 的缺陷全部落在 statement 末句或倒数第二句；行49 是唯一一个缺陷落在**中段见证例**上的。另确认：全部修补所需引文都在源文件中存在且落在 30–200 字符区间内，无一被 SPEC 下限卡住。

### 批 4：行 51–54（finite/infinite dimensional 定义与判别法）

- **行51 `d:finite-dimensional-defined-by-existence-of-a-finite-basis`** — C1「V 有某个有限基则称有限维」锚点逐字命中整句（`15.08.md:3`，95 字符，**含 "or if V consists of O alone" 这个第二析取项**，而 statement 只讲了第一析取项 —— 属 statement 覆盖不足于锚点，非缺陷，且零空间情形另有行52 专管）。C2（定义是对基的存在量化）由锚点 "if it **has** a finite basis" 支撑 ✓。**C3（末句）「这正是为什么在给 V 附上一个数之前需要 Theorem 15.6」无锚**：15.6 在 `:5`、dimension 定义在 `:9`（90 字符，可锚）。推理正确（存在量化 + 可能多个基 ⇒ 需先证等势才能定义 dim），但属节点自作的动机说明。→ 形式 D（轻）。
- **行52 `d:zero-space-declared-finite-dimensional-by-fiat`** — 有 `audit_fix: audit-A1` 标记，2 锚点。逐条：C1（"or if V consists of O alone" 是第二析取项而非第一项的推论）锚点 1 命中；C3（按 15.06 约定空集 span {O}）锚点 2 逐字命中 `15.06.md:19` ✓。**C2「由 Example 4，空集是独立的」无锚**：Example 4 在 `15.07.md:25`，"The empty set is independent." 为 29 字符（**恰好差 1 字符触 SPEC 30 下限**），但加前缀 "EXAMPLE 4. The empty set is independent." 为 40 字符，合规且可用；本节点 `sections` 已含 15.07，且**只用了 2 个锚点，尚有 1 个空位**。→ **形式 E（可修，且 SPEC 下限不构成真实阻碍——加 4 字前缀即过关）**。C4（末句）「Apostol 单列此情形是为了不让定义依赖那两条边界约定」为**作者意图推测**，原文无任何依据，不可锚。→ 形式 D（意图归因，建议降格为"效果上使定义不依赖…"）。数学核查：空集有限 ✓、独立（15.07:25）✓、span{O}（15.06:19）✓ ⇒ "形式上冗余"的判断**正确**。
- **行53 `d:infinite-dimensional-operational-criterion-no-finite-spanning-set`** — 1 锚点（`15.08.md:17` 后半，115 字符）。C1「Example 4 给出判别法的可用形式」+ C2「多项式空间是无限维的，因为虽有无限集 span 它，却没有有限集 span 它」——**"是无限维的"这个结论落在锚点左侧**：锚点从 "Although the infinite set..." 起，而 "The space of all polynomials $p(t)$ is infinite-dimensional." 在同一行前半、锚点之外。→ **形式 D（射程止于理由，未含结论）**。修法：锚点左扩取整个 Example 4 = 187 字符，仍在 200 上限内（已实测）。**C3（末句）「失败点在 span，而不在独立性」无锚**：对多项式空间为真（`{1,t,t²,…}` 是独立的无限集，见 `15.07.md:31` Example 6），但作为对"无限维"的一般刻画写法含混（任何无限维空间都"在 span 上失败"是同义反复）。→ 形式 D。
- **行54 `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets`** — **0 锚点**，已标 `origin:model`。**这是本片区最重的量词问题**：
  - C1 用 "**Equivalently**" 宣称"对每个 n 都能造出 n 个独立元素"与无限维**等价（双向）**。
  - C2 只给出了**一个方向**的论证：若 V 有 k 元基，则由 15.5 每 k+1 元相关，与 n=k+1 处的判别法矛盾。即只证了 **判别法 ⇒ 无限维**。
  - **反向（无限维 ⇒ 对每个 n 存在 n 个独立元素）在第 15 章内无法由所引资源得到**：它需要"独立集不 span 时可加一个 span 外元素扩张"的引理；Apostol 唯一相关的 Theorem 15.7(a)（`15.08.md:21`）**前提是 dim V = n 有限**，恰恰用不上无限维情形。`origin_note` 写 "Derived here from Theorem 15.5" 只覆盖了已证的那一半。
  → 判：**形式 D + 量词过度断言（死因 1）**，因为 "Equivalently" 是一个比手上论证强的量词/连接词。修法：改为 "A sufficient criterion:"，或如实注明反向需要基扩张引理且本章未提供。附核：零空间边界一致（零空间连 1 个独立元素也造不出，判别法在 n=1 即失败，与它被定义为有限维一致）✓。

### 批 5：行 55–58（Theorem 15.6 等势定理拆解）

- **行55 `d:thm-15-6-hypothesis-finite-dimensional-space`** — C1（只对有限维 V 陈述、只比较有限基）锚点逐字命中定理全句（`15.08.md:5`，110 字符）✓，**未增删有限维假设**，与原文一致。**C2（末句）「它对无限基不置一词，而 Apostol 在本章从未引入无限基」为章级否定存在断言**。已穷举核验：`grep -c basis\|bases` 全章 = 15.06:1、15.08:13、15.09-ex:2、15.11:9、15.13:5、15.14:3、15.16-ex:2；逐条读过，全部为 finite basis / orthogonal basis / orthonormal basis / basis of n elements，`15.13.md:5` 的 "finite or infinite sequence" 与 `:3` 的 "finite or infinite dimensional" 讲的是序列与空间，**没有任何一处把无限集称为 basis** ⇒ **C2 属实**。但它是**无法用单条逐字引文见证的否定存在断言**（要证"没有"，需遍历全章），→ 记入第 6 节（SPEC 的结构性盲区，非生产者过失）。
- **行56 `d:thm-15-6-apply-theorem-15-5-to-the-first-basis`** — C1（S 独立且 span V ⇒ 15.5 给出 V 中每 k+1 元集相关）锚点逐字命中（`15.08.md:7`，111 字符）✓。关键校验：锚点写 "every set of $k+1$ elements **in V**"，而 15.5 原文（`15.07.md:59`）说的是 **in L(S)**；替换合法且**是 Apostol 自己做的**（S span V ⇒ L(S)=V），锚点直接见证了这个替换，与行44 强调"in L(S) 而非 V"不矛盾。C2「两个从句在此都被消耗：独立性满足前提、span 使 L(S) 与 V 等同」——前半由锚点 "Since S is independent and spans V" 直接支撑；**后半"span 用于把 L(S) 认同为 V"这一步的推理链在锚点内是隐含的**（锚点没有出现 L(S)），需回 `15.07.md:59` + `15.06.md:19` 才闭合。→ 形式 D（轻）；这是本片区**分析质量最高**的一条，判断正确，只是锚点未覆盖它自己指出的那个认同步骤。
- **行57 `d:thm-15-6-upgrade-from-k-plus-one-to-more-than-k`** — C1（15.5 说 k+1，证明却需要"多于 k"）+ C2「这一升级在原文中是沉默的」锚点命中（`15.08.md:7` 的 "Therefore, every set of more than k elements in V is dependent."，63 字符）——**锚点恰好见证了"沉默"：原文只有一个 "Therefore," 没有理由**，这是本片区**锚点选取最贴切**的一条 ✓。**C3（末句）「它依赖于'含相关子集的集合是相关的'这一事实，即 15.07 的 Example 1」无锚**：Example 1 在 `15.07.md:19`（77 字符，可锚），而本节点 `sections` 只写 `["15.08"]`、只用 1 个锚点。→ **形式 E（点名引用了原文某处却不给锚点，是最容易补的一类）**。数学核查：Example 1 确实说 "If a subset T of a set S is dependent, then S itself is dependent."，对"多于 k"的集合取其 k+1 元子集即得 ✓（对无限集也成立）。
- **行58 `d:thm-15-6-first-inequality-m-at-most-k`** — C1（T 独立 ⇒ m ≤ k）锚点逐字命中（`15.08.md:7`，56 字符）✓；其推理依赖前一句"多于 k 元即相关"，由行57 分管，分工合理。**C2（末句）「此处只用到 T 是基的独立性那一半，其 span 性质还用不上」无锚**：属对证明用料的元观察，真（span T 的性质在下一句 "The same argument with S and T interchanged" 交换角色时才用上）。→ 形式 D（轻，元评论）。

批 4–5 小结：行54 为量词过度断言（"Equivalently" 只有单向论证），行53 与行49 同型（结论/谓词落在锚点左侧、锚点左扩即可修），行52、行57 为点名引用却不挂锚的形式 E。行55 的否定存在断言属 SPEC 无法覆盖的结构性盲区。行56、行57 的分析实质正确且锚点选取贴切，仅末句/隐含步骤未锚。

### 批 6：全片区交叉核（有限维假设、量词、锚点复用）

- 有限维假设增删：行55 未给 15.6 增删假设 ✓；行43/44 未给 15.5 加有限维假设 ✓（15.5 原文确实无此假设）；行54 引用 15.5 时也未误加 ✓。**本片区无"给原文没有的定理加上有限维假设"或"抹掉原文有的"的实例。**
- 有限/无限与 distinct：行41 的 "distinct" 未锚（轻）；行44 用 "finite combinations" 与原文 "finite linear combination" 一致但依据在 15.06 未锚；行52 关于空集的三项校验数学上全对；行53/54 关于无限维的处理见上。**没有发现把 15.07 的集合式定义误当列表式（允许重复元素）的实例** —— 本片区节点未触及"重复元素"话题。
- 锚点复用（B 型温和形态）：行47（110 字符）与行48（62 字符）为 `15.08.md:3` 同一句的包含关系；行39/40/41 三者同出 `15.07.md:57` 但为该行的三个不重叠子串（分别是 "If k≠M..."、"Therefore, when x→+∞..."、"Deleting the M th term..."），分工干净 ✓；行43/44 同出 `15.07.md:59` 的不同子串（前半假设 / 后半结论），分工干净 ✓；行45/46 同出 `15.07.md:61` 的不同子串 ✓。仅行47/48 一例为真包含。

## 9. 未覆盖事项声明

以下事项**没有**做，不为凑数编结论：

1. **边（edges）未审**。`data/edges-D2.jsonl` 中以这 23 个 id 为 src/dst 的边、及其 `origin`/`evidence` 字段，一条都没看。任务书主攻 statement 内的 D/E 型，边的 evidence 支撑性是同类问题的另一个面，**可能同样存在射程不足，但本报告不提供任何关于边的结论**。
2. **B 型（借用引文）未做全库排查**。我只在本片区 23 个节点内部比对了 `(file, quote)` 对，发现 1 例真包含（行47/48）。**跨片区、跨文件的 quote 复用没有查** —— 本片区某锚点是否同时被 D1/D3/D4/X 的节点引用、且那边才是真正需要它的地方，未知。（查这个需要读其它数据文件的全量 quote，可做但超出本次范围。）
3. **A 型（变量位置错）未系统排查**。逐条读 statement 时顺带核了变量（如行36 的 a_M、行58 的 m/k），未发现错位，但**没有把它当作独立的检查维度**跑一遍。
4. **Theorem 12.8 的内容未核**。行45/46 的判断建立在"12.8 的证明只依赖线性空间性质"这个 Apostol 自述上。第 12 章不在本实验源文件范围内（`source/apostol-ch15/` 只有 15.x），故**无法独立验证该自述是否成立**。若它不成立，行45/46 的整个分析都要重做。
5. **行43"k+1 紧性"的独立证明未写全**。我论证了 S 自身是 L(S) 中 k 个独立元素故 k 元集不必然相关 ⇒ k+1 紧。这足以说明该断言**为真**，但没有检查 Apostol 是否在第 12 章某处给出过这个观察（同 4）。
6. **行54 反向蕴含的可证性只做了"本章内不可证"的判断**。它在一般代数中当然为真（用 Zorn 或有限维刻画），但那需要本章之外的工具；我**没有**去论证"用 15.01–15.16 的全部内容是否拼得出来"，只核了最相关的 15.7(a) 用不上。
7. **"重复元素/列表式 vs 集合式"陷阱在本片区无实例**。这不是"检查通过"，是**该陷阱没有出现在这 23 个节点里**（它们不讨论重复元素）。若该陷阱确实存在于 D2，应在片 59–80 或片 1–35 中找。
8. **未读任何其它审查员的输出**，故本报告与 `审查-形式D-D2.md`、`裁定-*.md`、`_捞回-*.md` 等文件**可能重复或冲突**，我不知道也不做协调。冲突处请以裁定员为准，但请注意我的判定是独立得出的。
9. **`15.02` 公理表只做了标题级核对**：确认 `15.02.md` 有且仅有 AXIOM 1–10（闭合性 ×2、交换、结合、零元、负元、标量结合、两条分配、单位元），其中无任何涉及极限/收敛/序的条目 ⇒ 行40、行42 末句"极限与指数衰减不在线性空间公理中"属实。但我**没有**逐条读完 10 条公理的完整正文，是按标题＋数学显然性处理的。
