# 审查报告：形式 D / E 锚点支撑性 — nodes-D4.jsonl 第 28–54 节点

状态：已完成

---

## 1. 覆盖声明

审毕 `data/nodes-D4.jsonl` 第 28–54 行（1-indexed，到文件末尾），共 **27 个节点，无遗漏**。逐节点 id 与判定：

| 行 | id | 判定 |
| --- | --- | --- |
| 28 | `d:orthogonality-to-a-set-equals-orthogonality-to-its-span` | 形式 E（已披露，轻） |
| 29 | `d:orthogonal-complement-is-a-name-reserved-for-subspaces` | 形式 D |
| 30 | `d:orthogonal-decomposition-existence` | **形式 E（重）** |
| 31 | `d:finite-dimensionality-supplies-a-finite-orthonormal-basis` | **形式 E（末句）** |
| 32 | `d:s-defined-as-the-sum-of-projections-along-the-basis-elements` | 支撑充分 |
| 33 | `d:s-lies-in-s-because-it-is-a-linear-combination-of-the-basis` | 支撑充分 |
| 34 | `d:s-perp-is-orthogonal-to-each-basis-element` | 形式 E（末句，可修） |
| 35 | `d:orthogonality-to-the-basis-extends-to-all-of-s` | 形式 E（末句，边已缓解） |
| 36 | `d:orthogonal-decomposition-uniqueness` | 形式 D + E（末句） |
| 37 | `d:uniqueness-reduces-to-a-single-vanishing-difference` | 支撑充分 |
| 38 | `d:the-difference-lies-in-both-s-and-s-perp` | 形式 E（末句，边已缓解） |
| 39 | `d:uniqueness-concludes-by-self-orthogonality` | 支撑充分 |
| 40 | `d:pythagorean-expansion-of-the-squared-norm` | 支撑充分（正面样板） |
| 41 | `d:pythagorean-cross-terms-vanish-by-orthogonality` | 支撑充分 |
| 42 | `d:projection-on-a-subspace-is-defined-through-an-orthonormal-basis` | 形式 D |
| 43 | `d:projection-is-the-s-component-of-the-orthogonal-decomposition` | 形式 D（已披露） |
| 44 | `d:projection-does-not-depend-on-the-chosen-orthonormal-basis` | 形式 D（已披露，含借用锚） |
| 45 | `d:distance-is-the-norm-of-the-difference` | 形式 D（末句含全称量词） |
| 46 | `d:trivial-case-when-x-already-lies-in-s` | 形式 D（射程止于 V_3） |
| 47 | `d:nearest-point-by-dropping-a-perpendicular` | 支撑充分 |
| 48 | `d:approximation-inequality-with-equality-only-at-the-projection` | **形式 D（重）** |
| 49 | `d:approximation-proof-starts-from-the-orthogonal-decomposition` | 形式 D（轻） |
| 50 | `d:x-minus-t-splits-orthogonally` | 支撑充分（受 SPEC 阻挡一处） |
| 51 | `d:pythagorean-split-of-the-approximation-error` | 形式 E（末句）+ 表述错 |
| 52 | `d:equality-holds-exactly-when-t-equals-s` | 支撑充分 |
| 53 | `d:strict-minimality-rests-on-positivity-of-the-norm` | 支撑充分（末句不可锚） |
| 54 | `d:fourier-coefficients-in-cosine-and-sine-form` | 形式 E（两条自算关系无锚） |

合计：形式 D 9 例（29、36、42、43、44、45、46、48、49），形式 E 8 例（28、30、31、34、35、38、51、54；36 兼有 E 从句，计入 D），支撑充分 10 例。

**全片区最重要的一条元观察**：这 27 个节点**没有一个用满 3 条锚点**（最多 2 条，其中 4 个只有 1 条）。因此下述所有 D/E 缺陷**都不能用 SPEC 的 3 锚点上限来解释**——空锚位一直在那里。真正受 SPEC 字符下限阻挡的只有 2 处公式（见第 6 节），且都不是上述缺陷的成因。

**"缺陷集中在末句"这条前人经验在本片区强烈复现**：17 个有缺陷的节点里，**11 个的缺口正好落在 statement 的最后一句**（28、31、34、35、36、38、41-邻、45、46、51、54）。生产者在 15.14/15.15 这两节的行为高度规律：先逐句贴合原文写出被锚点覆盖的推理，然后在末尾追加一条"结构洞见"或"负向断言"（"never uses it"、"only by"、"the same bridge as"、"not an inequality but an identity"），而这类句子系统性地落在锚点射程之外。

## 2. 确认的形式 D 实例（射程不足 SCOPE-SHORTFALL）

### 2.1 行 48 `d:approximation-inequality-with-equality-only-at-the-projection`（最严重）

claim 分解：
1. 证明是"两步，从单一恒等式导出断言的两半"。
2. 第一步把 `x − t` 改写为 `(x − s) + (s − t)` 并施用勾股公式。
3. 该施用被许可，因为 `x − s ∈ S⊥` 而 `s − t ∈ S`。
4. 由此得 `‖x−t‖² = ‖x−s‖² + ‖s−t‖²`，进而得不等式。
5. "全程没有任何极小化论证"（no minimization argument anywhere）。
6. 第二步从同一恒等式读出等号情形：等号迫使 `‖s−t‖ = 0`。
7. 把 `‖s−t‖ = 0` 变成 `t = s` 的是 Theorem 15.9(a)(b)（`‖z‖ = 0 ⟺ z = O`），**而不是 S 的任何性质**。

锚点射程止于何处：两条锚点分别是 `15.15.md:3` 的 "Then the projection of x on S is nearer to x than any other element of S." 与 `15.15.md:9` 的 "for all $t$ in $S$ ; the equality sign holds if and only if $t = s$ ."——**两条都只是定理陈述（15.15:3 与 :9），一条都没有进入证明体（15.15:11–23）**。

缺口：claim 2–7，即 statement 的全部实质内容，在本节点无任何锚点。这正是 D 型最危险的形态：锚点主题高度相关（同一条定理、同一个投影、同一个等号条件），"扫一眼觉得对"，但射程只覆盖"断言是什么"，完全不覆盖"断言为什么成立、由哪一步成立"。而本节点的 `name_en` 自称的就是证明结构（"as a two-step verification"），即节点的存在理由整个落在射程之外。

我已独立去原文核过 claim 2–7：全部为真（`15.15.md:14、17、20、23`；claim 7 的归因见 `15.10.md:123–125`，Apostol 本人在 :23 并未点名 15.9）。

建议改法：把锚点 1 换成 `15.15.md:17` 的 "Since $s - t \in S$ and $x - s = s^{-} \in S^{\perp}$ , this is an orthogonal decomposition of $x - t$ , so its norm is given by the Pythagorean formula"（覆盖 claim 2、3），锚点 2 换成 `15.15.md:23` 的 "But $\| s - t\|^2 \geq 0$ , so we have $\| x - t\|^2 \geq \| x - s\|^2$ , with equality holding if and only if $s = t$ ."（覆盖 claim 4、6），第三个空位保留 `:9`。claim 7 的 15.9 归因应标 `origin: model`。死因编码 **6**（锚点不支撑其所挂断言），但因断言本身为真、且改法只需换锚不需改文，建议裁定 **0 = KEEP + 强制换锚**。

### 2.2 行 42 `d:projection-on-a-subspace-is-defined-through-an-orthonormal-basis`

claim：(1) 对有限维 S 及其正交规范基，`x` 在 S 上的投影是 `s = Σ (x, e_i) e_i`；(2) 该定义是借选定的基给出的，不是内蕴的。

锚点 1（`15.14.md:60`，"Let $S$ be a finite-dimensional subspace..."）覆盖 claim 2 与 claim 1 的前提；锚点 2（`15.14.md:66`，"is called the projection of $x$ on the subspace $S$ ."）只给出被命名者的名字。**两条锚点一头一尾夹住定义，中间那个真正的公式（`15.14.md:63`）没有引。** 缺口：claim 1 的公式本体。

这是可零成本修好的：`15.14.md:63` 整行 `s = \sum_ {i = 1} ^ {n} (x, e _ {i}) e _ {i}` 是 44 字符，落在 SPEC 的 30–200 区间内，且本节点有 1 个空锚位。死因 **0 = KEEP + 补锚 15.14:63**。

### 2.3 行 43 `d:projection-is-the-s-component-of-the-orthogonal-decomposition`

claim：(1) 定义投影的公式与存在性证明中产生 `s` 的公式**是同一个公式**，故投影 = 分解的 S 分量；(2) 这条同一性是 Theorem 15.16 得以谈论"投影"的前提；(3) Apostol 从未明说，它来自"投影定义逐字就是存在性证明的 (15.18)"。

节点的核心 claim 1 是一条**公式逐字相同**的断言，而**两条锚点里一个公式都没有**（一条是命名句 `:66`，一条是过渡句 `:68`）。缺口：读者拿着这两条锚点无法核验"同一个公式"。claim 2 由锚点 2 覆盖；claim 3 是自陈。

缓解：节点标了 `origin: model`，且 statement 末句主动交代了推理来源——这是诚实披露，不同于第 3 节那些静默追加。建议 **0 = KEEP + 补锚**：把 `15.14.md:33`（83 字符，含 `(15.18)` 全式）或 `15.14.md:63`（44 字符）填入空位，同一性即可被读者机械核验。

### 2.4 行 29 `d:orthogonal-complement-is-a-name-reserved-for-subspaces`

claim：(1) `S⊥` 总是存在；(2) `S⊥` 总是子空间；(3) 但 Apostol 只在 S 本身是子空间时才称其为正交补；(4) 这条限制是术语性的、非数学的。

唯一锚点 "In case S is a subspace, then $S^{\perp}$ is called the orthogonal complement of S." 只覆盖 claim 3。缺口：claim 1、2——而 claim 2 恰恰是本节点用来支撑 claim 4（"限制是术语性的"）的全部力量所在。若 `S⊥` 不总是子空间，claim 4 就垮了。

关键点：原文 `15.14.md:11` **整行 186 字符**，同时含 "It is a simple exercise to verify that $S^{\perp}$ is a subspace of V, whether or not S itself is one." 与节点已引的后半句。一条锚点即可覆盖 claim 1–3，且 186 落在 30–200 内。节点只用了 1/3 锚位。缓解：`edges-D4.jsonl:71` 以该前半句作 evidence 承载了 `requires → apostol:orthogonal-complement-is-a-subspace`，故图层面不缺证据，缺的是节点层面。死因 **0 = KEEP + 把锚点扩到整行**。

### 2.5 行 44 `d:projection-does-not-depend-on-the-chosen-orthonormal-basis`

claim：(1) S 的不同正交规范基给出同一个 `s`；(2) 理由是 Theorem 15.15 的唯一性一半钉死了 `x` 的 S 分量；(3) Apostol 未评论此事；(4) 没有它定义就是基依赖的。

锚点 1 只是 `15.14.md:44` 的宣告句 "Next we prove that the orthogonal decomposition (15.16) is unique."，锚点 2 是投影定义的前提句。缺口：claim 1→2 之间缺一步桥——**"任一正交规范基造出的 s 都满足 s ∈ S 且 x − s ∈ S⊥，因此都是同一个分解的 S 分量"**。唯一性只说"分解唯一"，不自动说"每个基造出的都是那个分解"；后半来自存在性证明（`15.14.md:36、42`，即节点 33、35 的内容）。锚点射程止于"唯一性存在"，未及"每个基都落进唯一性的适用范围"。

附带 B 型：锚点 1 的 `(file, quote)` 对同时被行 36 使用，而行 36 是它真正的归属者（那里它是被证对象的宣告）；行 44 借用它来指代一条自己并不直接使用的事实。

缓解：标了 `origin: model`，且 statement 承认 Apostol 未评论。死因 **0 = KEEP + 换锚**（把锚点 1 换成 `15.14.md:36` 的 "Since s is a linear combination of the basis elements, s lies in S." 或 `:42` 的结论句，并用空位保留唯一性引用）。

### 2.6 行 36 `d:orthogonal-decomposition-uniqueness`

claim：(1) 给定两个表示 `x = s + s⊥ = t + t⊥`，其中 `s, t ∈ S`、`s⊥, t⊥ ∈ S⊥`；(2) 证明给出 `s = t` 且 `s⊥ = t⊥`；(3) **这一半用不到任何有限性或基**。

锚点 2 是 `15.14.md:44` 的 "Suppose that $x$ has two such representations, say"——**在 "say" 处截断**，而 claim 1 里的归属条件（`s, t ∈ S`、`s⊥, t⊥ ∈ S⊥`）出现在下一段 `15.14.md:50` 的 "where $s$ and $t$ are in $S$ , and $s^{\perp}$ and $t^{\perp}$ are in $S^{\perp}$ . We wish to prove that $s = t$ and $s^{\perp} = t^{\perp}$ ."。这条 148 字符的句子同时覆盖 claim 1 的归属条件与 claim 2，且节点有 1 个空锚位——射程缺口纯属未取。

claim 3 是形式 E，见 3.3。死因 **0 = KEEP + 补锚 15.14:50**。

### 2.7 行 45 `d:distance-is-the-norm-of-the-difference`

claim：(1) `x` 与 `y` 的距离定义为 `‖x − y‖`；(2) **因此 15.14 与 15.15 中的每一个极小化陈述都是关于范数的陈述**；(3) 这就是勾股公式能解决它的原因。

唯一锚点精确覆盖 claim 1。缺口：claim 2 带全称量词 "Every ... in 15.14 and 15.15"，横跨两整节，锚点射程只有一条定义。claim 3 同样无锚。

我已逐一核过 claim 2 涉及的全部极小化陈述：`15.14.md:3`（"whose distance from x is as small as possible"）、`15.15.md:3–9`（`‖x−s‖ ≤ ‖x−t‖`）、`15.15.md:51`（"the norm $\|f - f_{n}\|$ is as small as possible"）、`15.15.md:59`（"the norm $\| f - f_n\|$ is smallest"）。**claim 2 为真**。但支撑它的是我的穷举，不是节点的锚点。节点只用 1/3 锚位；`15.14.md:3` 可截取 100 字符子串 "Given an element x in V, to determine an element in S whose distance from x is as small as possible." 直接支撑 claim 2 的 15.14 一半。死因 **0 = KEEP + 补锚**，或把 claim 2 的量词降为示例式表述。

### 2.8 行 46 `d:trivial-case-when-x-already-lies-in-s`

claim：(1) 当 `x ∈ S` 时最近元是 `s = x`；(2) 距离为零；(3) Apostol 在 V_3 图示中先给出这一情形。

两条锚点（`15.14.md:7` 的 "If $x \in S$ , then clearly $s = x$ is the solution." 与 `15.14.md:5` 的 "Given x in V, the problem is to find, in the plane S, that point s nearest to x."）**全部落在 15.14:5–7 的 V_3 特例段内**——那一段的前提是 "Here V is the vector space $V_{3}$ and S is a two-dimensional subspace, a plane through the origin."。而 claim 1 的措辞是无限定的一般断言，节点 `node_type` 还是 `theorem`。射程：锚点只管 V_3 中的平面 S，断言覆盖任意 Euclidean 空间的任意子空间。

另有一处 E：claim 2 "giving distance zero" ——两条锚点里既无 "distance" 也无 "norm"，无锚（真值显然，由 `15.14.md:3` 的距离定义加 Theorem 15.9(a) 得到）。

缓解：claim 3 主动交代了出处是 V_3 图示，细心读者能看出射程。对照行 47 `d:nearest-point-by-dropping-a-perpendicular`——它把 "In the V_3 picture" 写进了 statement 本身，射程与锚点严丝合缝。同一对相邻节点，同样的原文段落，一个自限了射程一个没有，说明这是笔误级而非认知级的疏漏。死因 **0 = KEEP**，建议 claim 1 加回 "In the V_3 illustration"，或补 `15.14.md:3` 锚点把一般情形接上。

### 2.9 行 49 `d:approximation-proof-starts-from-the-orthogonal-decomposition`（轻）

claim：(1) 第一步是引 Theorem 15.15 写 `x = s + s⊥`；(2) 这是有限维假设进入 Theorem 15.16 的一条通道；(3) **另一条通道是定理陈述自身的 "the projection of x on S" 一语，因为该概念在 15.14 中只对有具备有限正交规范基的子空间定义**。

锚点 1、2 覆盖 claim 1、2 完整。缺口：claim 3 依赖的事实——"15.14 的投影定义以 S 有限维为前提"——出现在 `15.14.md:60`，本节点没引（该句被行 42、44 引用过，长度合规）。节点有 1 个空锚位。死因 **0 = KEEP + 补锚 15.14:60**。

## 3. 确认的形式 E 实例（无锚点从句）

### 3.1 行 30 `d:orthogonal-decomposition-existence`（最严重，且未披露）

claim 分解（statement 共两句，第二句为追加洞见）：
1. 存在性拆成四项义务，它们分别完成、依赖不同的前置结果。
2. 取 S 的有限正交规范基——**有限维假设在此被消耗，经由 Gram-Schmidt**。
3. 定义 `s` 为 `x` 沿该基各元投影之和。
4. 验 `s ∈ S`（它是基元的线性组合）。
5. 验 `s⊥ = x − s` 与 S 正交，这一步本身需要"从整个 S 归约到基元"。
6. **只有第二项是构造，另三项是验证。**
7. **基的选取是唯一的任意输入。**
8. **正因如此，唯一性一半一旦证成，就使构造与该选取无关。**

锚点只有两条：`15.14.md:30` 的 "First we prove that an orthogonal decomposition (15.16) actually exists." 与 `15.14.md:30` 的 "Given x, define the elements s and $s^{\perp}$ as follows:"。射程止于"存在性先证"+"要定义 s 和 s⊥ 了"。

缺口：**claim 1 与 claim 6、7、8 全部无锚**，且 claim 2 里 "via Gram-Schmidt" 这一归因也无锚（原文 `15.14.md:30` 只说 "Since S is finite-dimensional, it has a finite orthonormal basis"，Gram-Schmidt 的桥在 `15.13.md:82` 的 Theorem 15.14）。claim 3、4、5 的内容分别在 `15.14.md:33、36、36–42`，本节点未引，但由子节点 32、33、34、35 各自承载并锚定——这部分可接受（父节点作纲要）。

真正的问题是 claim 1、6、7、8：这是一整套**模型自造的证明分类学**（四义务、构造 vs 验证、任意输入、唯一性使之良定），原文无一字对应，而**节点没有 `origin: model` 字段**。SPEC 第 55 行要求"找不到可逐字引用的依据 ⇒ 不要收录，或标 `origin: model` 并如实说明"。对比同片区的行 40（`d:pythagorean-expansion-of-the-squared-norm`）——它同样重构了原文压缩掉的中间步骤，但在 statement 里写明 "Apostol collapses this expansion into a single displayed line, so the intermediate cross-term form is a reconstruction." 并标了 `origin: model`。行 30 具备同样的条件却没做。

我独立核过 claim 6、7、8 的真值：claim 6 与 claim 8 为真；claim 7 措辞过强——`x` 也是输入，但在"任意/可选"的意义下基确实是唯一任意项，可接受。

死因 **0 = KEEP + 补 `origin: model` + 在 statement 内如实说明 claim 1/6/7/8 是模型分类学**。若裁定员要求严格执行 SPEC 第 55 行，则末句（claim 8）应删。附带问题见 7.1（"four obligations" 与 5 个 part-of 子节点不符）。

### 3.2 行 31 `d:finite-dimensionality-supplies-a-finite-orthonormal-basis`（末句）

claim：(1) S 有限维这一假设**恰好被用一次**，用于引 Theorem 15.14 得到有限正交规范基 `e_1,...,e_n`；(2) 这使定义 `s` 的有限和有意义；(3) **唯一性一半与勾股公式从不使用它。**

锚点 1（`15.14.md:30`）覆盖 claim 1 的动作本体；锚点 2（`15.14.md:18`，Theorem 15.15 的假设句）覆盖前提。缺口：

- claim 1 的量词 "exactly once" 无锚——这是一条关于整篇证明的**穷举性负向断言**，原文不可能有对应句。
- **claim 3 完全无锚**，且它是末句。

我已独立穷举核对：Theorem 15.15 证明体是 `15.14.md:30–58`，"finite-dimensional / finite" 仅出现于 :30 一处；唯一性段（:44–50）只用了 S 与 S⊥ 对差封闭、以及"零元是唯一自正交元"；勾股段（:52–58）只用了内积公理与正交性。**claim 1、3 均为真。** 但支撑它们的是我的穷举，不是任何引文——这类断言原则上不可锚，SPEC 的处置方式就是标 `origin: model`。本节点无该字段。

死因 **0 = KEEP + 补 `origin: model`**（不判生产者数学错，断言为真）。

### 3.3 行 36 `d:orthogonal-decomposition-uniqueness`（末句；D 型部分见 2.6）

末句 claim 3："No finiteness or basis is used in this half." 无锚、不可锚（负向穷举断言），且节点无 `origin: model`。真值：为真（同 3.2 的穷举）。与行 31 的 claim 3 是同一条事实的两次陈述，两处都未披露——**这是一个成对出现的系统性疏漏，不是孤例**。死因 **0 = KEEP + 补 `origin: model`**。

### 3.4 行 51 `d:pythagorean-split-of-the-approximation-error`（末句，并含表述错）

claim：(1) 对 `x − t` 的分解施用勾股公式得 `‖x−t‖² = ‖x−s‖² + ‖s−t‖²`；(2) **这是一条恒等式而非不等式，才是逼近定理的实质内容**；(3) **任何竞争者的误差比在 s 处的误差正好多出 `‖s−t‖²`。**

锚点 1（`15.15.md:20` 的公式）、锚点 2（`15.15.md:17` 的 "so its norm is given by the Pythagorean formula"）合起来精确覆盖 claim 1。缺口：claim 2、3 无锚，均在末句区。claim 2 是价值判断（"实质内容"），无害但属模型评述；claim 3 是**数值/量级断言**且**严格读为假**——见 7.2。

死因 **0 = KEEP + 改末句措辞**（把 "the error" 改成 "the squared error"，即 `‖x−t‖² − ‖x−s‖² = ‖s−t‖²`，则为真）。

### 3.5 行 54 `d:fourier-coefficients-in-cosine-and-sine-form`（两条自算关系无锚）

claim：(1) 代入 (15.20) 的显式三角函数把投影公式 (15.21) 改写为经典形式 (15.22)；(2) **经典 `a_k`、`b_k` 与内积 `(f, φ_k)` 相差 `φ_k` 的规范化因子**；(3) **例如 `(f, φ_{2k−1}) = √π · a_k`**；(4) **k = 0 项贡献 `a_0/2`**；(5) 无新数学，只是换规范化约定。

锚点 1（`15.15.md:39` 的 "Using the formulas in (15.20), we can rewrite (15.21) in the form"）覆盖 claim 1；锚点 2（`15.15.md:48` 的 `a_k`、`b_k` 定义式）给出 `a_k` 的定义。缺口：**claim 2、3、4 依赖 `φ_{2k−1}(x) = cos kx/√π` 与 `φ_0(x) = 1/√(2π)` 这两个显式表达式，即 `15.15.md:28` 的 (15.20)，本节点未引。** 没有它，读者无从核验 `√π` 与 `a_0/2` 这两个数值从哪来——而这正是本实验第 4 项专项要防的"模型自算的符号值无锚"。

两条关系我已人工验算（见 4.4），**均为真**。`15.15.md:28` 整行 211 字符略超 SPEC 200 上限，但锚点只要求逐字连续**子串**，取其中 ≤200 字符的一段（例如 `\varphi_ {0} (x) = \frac {1}{\sqrt {2 \pi}}, \quad \varphi_ {2 k - 1} (x) = \frac {\cos k x}{\sqrt {\pi}}`）即合规；节点尚有 1 个空锚位。**所以这不是 SPEC 阻挡，是未取。** 死因 **0 = KEEP + 补锚 (15.20) 子串**。

### 3.6 行 34 `d:s-perp-is-orthogonal-to-each-basis-element`（末句，可修）

末句："Here the orthonormality of the basis is what makes (s, e_j) collapse to a single coefficient." 无锚。原文 `15.14.md:42` 只写 "But from (15.18), we find that $(s, e_j) = (x, e_j)$ "——**为什么能 find，Apostol 在 15.14 留给了读者**。真值为真：`(s,e_j) = Σ_i (x,e_i)(e_i,e_j) = (x,e_j)`，用的正是 `(e_i,e_j) = δ_ij`。

可修且原文有现成句：`15.11.md:64` 的 "since $(e_i, e_j) = 0$ if $i \neq j$ . This implies (15.8), and when $(e_j, e_j) = 1$ , we obtain (15.9)."（90 字符，合规），那里 Apostol 对完全相同的塌缩做了显式说明。节点有 1 个空锚位。`edges-D4.jsonl:84` 已以 `requires → apostol:orthonormal-basis` 部分承载，但其 evidence 是 15.14:30 的取基句，仍不解释塌缩。死因 **0 = KEEP + 补锚 15.11:64**。

### 3.7 行 28 `d:orthogonality-to-a-set-equals-orthogonality-to-its-span`（已披露，轻）

claim：(1) 对一族元素的正交性推广到它们的一切线性组合；(2) 故 `S⊥` 与 `L(S)⊥` 是同一个集合；(3) **这是"S 是否为子空间无关紧要"的结构性理由：过渡到张成什么也不改变。**

锚点 1（`15.13.md:35`）覆盖 claim 1，锚点 2（`15.14.md:9` 的正交于集合的定义）覆盖 claim 2 的语义。缺口：claim 3（末句）无锚——它指向的是 `15.14.md:11` 的 "whether or not S itself is one"，本节点未引。

真值核验：claim 2 为真（`⊆` 由 `S ⊆ L(S)`；`⊇` 由线性性，复空间下 `(x, Σc_i s_i) = Σ c̄_i (x, s_i) = 0` 同样成立）。claim 3 为真且是有意义的洞见（`S⊥ = L(S)⊥` 且 `L(S)` 恒为子空间，故一般情形归约到子空间情形）。

缓解：节点标了 `origin: model` 且 `atomic_reason` 交代"教材未明写此等价"。**这是披露到位的样本**。唯一遗留：`atomic_reason` 称其"是单一等式"，与 statement 实含两条断言不符（见 7.3）。死因 **0 = KEEP**。

### 3.8 行 35 `d:orthogonality-to-the-basis-extends-to-all-of-s`（末句，边已缓解）

末句："This is the same span-extension bridge used inside the Gram-Schmidt proof, applied to a basis instead of a partial list." 无锚。节点只有 1 条锚点（`15.14.md:42` 的结论句），2 个空锚位，而 `15.13.md:35` 的 "Therefore, it is orthogonal to every element in the subspace" 正是那座桥、且已被行 9 用作锚点、长度合规。

缓解较强：`edges-D4.jsonl:87` 以 `requires → d:orthogonality-to-generators-extends-to-their-span` 承载，evidence 正是 15.13:35；且行 9 自身的 statement 明写 "Apostol uses this bridge twice: here, and again in Theorem 15.15 when passing from the basis elements e_j to all of S."。所以跨节点的复用关系在图里是有据的，缺的只是本节点自身的锚点。死因 **0 = KEEP + 补锚 15.13:35**。

### 3.9 行 38 `d:the-difference-lies-in-both-s-and-s-perp`（末句，边已缓解）

末句："Membership in both sides is forced **only** by S and S perp being closed under differences." 无锚，且带 "only" 量词。节点只有 1 条锚点（`15.14.md:50` 的 "But $s - t \in S$ and ..."），2 个空锚位，而 `S⊥` 是子空间的依据 `15.14.md:11` 未引。

真值：为真（`s−t ∈ S` 用 S 对差封闭；`t⊥−s⊥ ∈ S⊥` 用 S⊥ 对差封闭）。缓解：`edges-D4.jsonl:93` 以 15.14:11 全句作 evidence 承载 `requires → apostol:orthogonal-complement-is-a-subspace`，`:94` 以 `origin: model` 承载子空间判别准则。死因 **0 = KEEP + 补锚 15.14:11**。

## 4. 四个专项检查

### 4.1 有限维假设的增删

本片区 27 个节点全部落在 15.13–15.15，是有限维假设流转的主战场。逐条核对结果：**没有发现任何一个节点给原文加了它没有的有限维假设，也没有抹掉它写了的。** 具体：

| 节点 | 对有限维的处理 | 原文对照 | 判定 |
| --- | --- | --- | --- |
| 28 | 未涉及有限维；只谈"S 是否为子空间"无关 | `15.14.md:9` 的正交于集合定义对任意子集 S 成立，无有限性 | 正确，未加 |
| 29 | "S perp always exists and is always a subspace"，对任意子集 | `15.14.md:11` "whether or not S itself is one"，无有限性 | 正确，未加 |
| 30、31 | 有限维用于取有限正交规范基，"exactly once" | `15.14.md:18`（定理假设）、`:30`（唯一消耗点） | 正确 |
| 36 | 明确声明唯一性一半**不用**有限性 | `15.14.md:44–50` 确实不含 finite | 正确，未抹 |
| 40、41 | 勾股部分不提有限维 | `15.14.md:52–58` 确实不含 | 正确 |
| 42 | 投影定义要求 S 有限维 | `15.14.md:60` "Let $S$ be a finite-dimensional subspace" | 正确，未抹 |
| 44 | 依赖唯一性（无有限性）证良定 | 一致 | 正确 |
| 48、49 | Theorem 15.16 的有限维经"两条通道"进入 | `15.15.md:3` 定理假设含 finite-dimensional | 正确 |

**行 49 值得单独表扬**：它是本片区唯一显式追踪"有限维假设经由哪几条路径进入 Theorem 15.16"的节点，并指出第二条通道是定理陈述里 "the projection of x on S" 一语（因该概念在 15.14 只对有限维子空间定义）。这条分析正确且非平凡——若只看 `requires → 15.15` 一条边，会漏掉投影概念自带的有限维负担。缺陷仅在于第二条通道的依据未锚（见 2.9），不在数学。

任务书点出的对照点（`15.11.md:7` Theorem 15.10 首句**无**有限维、`:43` Theorem 15.11 与 `:77` Theorem 15.12 **有** finite-dimensional）在我这一片区无节点覆盖——nodes 28–54 的 `sections` 只出现 15.10（仅节点 53 引正定性）、15.13（节点 28 引一锚）、15.14、15.15。我已核过 15.11 三条定理的原文措辞与任务书描述一致，但本片区没有节点断言它们，故无可判之处。这属于覆盖范围问题，登记在第 9 节。

### 4.2 实 / 复 Euclidean space 分裂

原文的分裂点：`15.10.md:13` 内积定义以 "A real linear space V" 开头；`:29–43` 给出复空间的 Hermitian 替换 `(x, y) = conj(y, x)`；`:45` 明确 **"the theorems of this chapter are valid for complex Euclidean spaces as well. When we use the term Euclidean space without further designation, it is to be understood that the space can be real or complex."**；`:105` 范数定义、`:121` Theorem 15.9、`:151` 角度定义（此条限定 "In a real Euclidean space"）。

15.14 与 15.15 通篇用不带限定词的 "Euclidean space"，故按 `15.10.md:45`，**本片区涉及的全部定理（15.15、15.16、投影、正交补）实复通用**。核对结果：

- **没有一个节点把 15.14/15.15 的结论错误地限制到实空间**，也没有一个节点声称某条只在实空间成立。这一片区在实/复分裂上是干净的。
- 节点 40 `d:pythagorean-expansion-of-the-squared-norm` 是唯一触及展开细节的节点，称展开 `(s+s⊥, s+s⊥)` 为 "pure axiom application"。我核过复空间下的可行性：在第二个变元上的加性由公理 (2) 直给，在第一个变元上的加性需经对称公理 (1)（实）或 Hermitian 公理 (1')（复）；两者都在 `15.10.md:15/32` 的公理表内，故 "pure axiom application" 在实复两种情形下均为真。**无缺陷。**
- 节点 41 称两个混合项 `(s, s⊥)` 与 `(s⊥, s)` 均为零。复空间下由 Hermitian 公理 `(s⊥,s) = conj((s,s⊥)) = conj(0) = 0`，成立。**无缺陷。** 附注：这两个混合项在原文 `15.14.md:55` 从未显式出现（只写 "the remaining terms being zero"），节点 40 已就此作了重构披露，节点 41 沿用其术语，可接受。
- **唯一一处需登记的轻微不匹配**：节点 53 `d:strict-minimality-rests-on-positivity-of-the-norm` 的两条锚点，锚点 1 是 `15.10.md:125` 的 Theorem 15.9(b)（该定理以 "In a Euclidean space" 开头，实复通用 ✔），锚点 2 是 `15.10.md:22` 的 `(x, x) > 0 \quad i f \quad x \neq O`——**这一条出自 15.10:13–25 的"实"内积公理块**（该段以 "A real linear space V is said to have an inner product" 开头）。而节点 53 的 `sections` 是 ["15.10", "15.15"]，服务的是 15.15 中不带限定词的 Euclidean space。原文 `15.10.md:29` 的 "an inner product $(x, y)$ is a complex number satisfying the same axioms as those for a real inner product, except that the symmetry axiom is replaced by..." 授权了正定性公理向复情形的移植，**但这条授权句本身未被节点 53 引用**。严格说这是一处 D 型的射程细缝：锚点 2 的射程止于实空间公理块，断言服务于通用 Euclidean space。因锚点 1（Theorem 15.9(b)）本身已通用、足以独立支撑断言，我判此条**不构成独立缺陷**，仅登记。

关于共轭出现的两处（`15.10.md:136` 三角不等式证明、`15.11.md:80` Parseval 公式）：本片区无节点断言这两处，不适用。

### 4.3 反常积分 / 积分内积的收敛与连续性条件

结论：**本专项在片区 28–54 内没有可判之处，风险未材化。**

核查过程：我逐一检查了 27 个节点中一切可能涉及积分内积正定性的位置。

- 只有节点 54 `d:fourier-coefficients-in-cosine-and-sine-form` 触及积分内积（Fourier 系数 `a_k`、`b_k` 的积分式）。它断言的是**规范化因子的换算**，完全不涉及 `∫f² = 0 ⇒ f = 0` 这一步，也不涉及内积公理的验证。**无缺陷。**
- 节点 45 `d:distance-is-the-norm-of-the-difference` 的 claim 2 涵盖 15.15 的两个函数空间例子，但它断言的只是"这些极小化陈述都是关于范数的"，不涉及正定性。**无缺陷。**
- `15.10.md:59` 的 `C(a, b)` 连续性限制、`:73` 的权函数 `w`、`:75–81` 的 Example 5（反常积分情形），在本片区**无任何节点引用**（我已 grep 确认 nodes 28–54 的锚点 file 只出现 15.10、15.13、15.14、15.15，且 15.10 的两条锚点均在 :22 与 :125 的正定性处）。
- 顺带核实任务书指出的断裂点本身在原文成立：`15.10.md:59` 确以 "the linear space of all real-valued functions **continuous** on an interval $[a, b]$" 限定空间，而 `15.15.md:25、53` 两个例子同样以 `C(0, 2π)`、`C(-1, 1)` 的连续函数空间为载体——**Apostol 全程保持了连续性限制**，本片区节点也没有把它删掉（节点 54 未提空间，属未提及而非误述）。

### 4.4 计算值人工验算

本片区**只有一个节点含模型自算的符号值**：节点 54，共两条。已逐条手算：

**(1) `(f, φ_{2k−1}) = √π · a_k`**

由 `15.15.md:28` 的 (15.20)：`φ_{2k−1}(x) = cos kx / √π`。
由 `15.15.md:36` 的 (15.21)：`(f, φ_k) = ∫₀^{2π} f(x) φ_k(x) dx`。
故 `(f, φ_{2k−1}) = ∫₀^{2π} f(x) · cos kx/√π dx = (1/√π) ∫₀^{2π} f(x) cos kx dx`。
由 `15.15.md:48`：`a_k = (1/π) ∫₀^{2π} f(x) cos kx dx`，即 `∫₀^{2π} f(x) cos kx dx = π a_k`。
代入：`(f, φ_{2k−1}) = (1/√π)(π a_k) = √π a_k`。**✔ 正确。**

（顺带核 `b_k` 的同型关系：`(f, φ_{2k}) = (1/√π)(π b_k) = √π b_k`，同样成立。节点未断言此条，不构成缺陷。）

**(2) "the k = 0 term contributes a_0/2"**

由 (15.20)：`φ_0(x) = 1/√(2π)`。
(15.21) 中 k = 0 项 `= (f, φ_0) φ_0 = [∫₀^{2π} f(x)·(1/√(2π)) dx] · (1/√(2π)) = (1/(2π)) ∫₀^{2π} f(x) dx`。
由 `15.15.md:48` 取 k = 0：`a_0 = (1/π) ∫₀^{2π} f(x) cos 0 dx = (1/π) ∫₀^{2π} f(x) dx`。
故 `a_0/2 = (1/(2π)) ∫₀^{2π} f(x) dx`，与 k = 0 项**逐项相等**；且 `15.15.md:42` 的 (15.22) 首项正是 `(1/2) a_0`。**✔ 正确。**

**结论：节点 54 的两条自算值均为真，但两者都无锚点支撑**（依据 (15.20) 未引），已作形式 E 记入 3.5。这正好印证任务书的判断——机器筛查召回 1/4 的那类"符号形式无锚值"，靠人工验算才能确认真值，靠人工读锚点才能确认它无锚；两件事都不能靠 `grep -F` 的 100% 通过率。

其余 26 个节点经逐一扫读，**不含任何模型自行计算的数值或符号值**（节点 51 出现 `‖s−t‖²` 但那是原文 `15.15.md:20` 的公式项，不是自算值；其表述问题另记 7.2）。任务书列举的 `(e−1/e)/2`、`3/e`、`e² − 1`、`pi − 2 sin x`、`x = 1 and y = i` 均不在本片区。

附带独立验算（不属本片区节点，但我读到了）：`15.15.md:59–75` Example 2 中 `(f, φ_1) = √(3/2) · 2/π` 与 `f_1(t) = √(3/2)(2/π) φ_1(t) = (3/π) t`。核：归一 Legendre 一次多项式 `φ_1(t) = √(3/2) t`，则 `√(3/2)(2/π)·√(3/2) t = (3/2)(2/π) t = (3/π) t`。**✔ 与原文一致。** 本片区无节点覆盖 Example 2（登记在第 9 节）。

## 5. 判定支撑充分的节点 id 清单

以下 10 个节点，claim 逐条映射到锚点后无缺口（或缺口小到不值得报，已在括号注明）：

1. 行 32 `d:s-defined-as-the-sum-of-projections-along-the-basis-elements` —— 两锚分别覆盖公式 (15.18) 与"每项是沿 e_i 的投影 / s 是投影之和"，逐字对应。
2. 行 33 `d:s-lies-in-s-because-it-is-a-linear-combination-of-the-basis` —— 两锚覆盖两句。（"which span S" 来自基的定义，未锚，可忽略。）
3. 行 37 `d:uniqueness-reduces-to-a-single-vanishing-difference` —— 锚 1 逐字含移项与目标化归，锚 2 给 (15.19)。
4. 行 39 `d:uniqueness-concludes-by-self-orthogonality` —— 锚 1 覆盖自正交推零，锚 2 覆盖"分解唯一"结论；末句为无害元评述且有锚 2 支撑。
5. 行 40 `d:pythagorean-expansion-of-the-squared-norm` —— **本片区处理"原文压缩掉的中间步骤"的正面样板**：重构了交叉项形态，在 statement 内明说是重构，并标 `origin: model`。
6. 行 41 `d:pythagorean-cross-terms-vanish-by-orthogonality` —— 两锚覆盖消项理由与结果式 (15.17)。（"因为 s∈S、s⊥∈S⊥"到"s 与 s⊥ 正交"差一步 S⊥ 定义，可忽略。）
7. 行 47 `d:nearest-point-by-dropping-a-perpendicular` —— 射程与断言严丝合缝：statement 自限 "In the V_3 picture"，锚 1 为该情形原句，锚 2 "motivates the discussion that follows" 精确支撑末句的"动机"断言。**与行 46 构成对照（见 2.8）。**
8. 行 50 `d:x-minus-t-splits-orthogonally` —— 锚 1 一句覆盖两处归属判断 + "这是 x−t 的正交分解" + 勾股公式的许可，锚 2 给全称量词 "for any t in S"。（分解式本身受 SPEC 字符下限阻挡，见 6.1。）
9. 行 52 `d:equality-holds-exactly-when-t-equals-s` —— 锚 1 逐字含 `‖s−t‖² ≥ 0`、不等式、iff 条件；锚 2 给定理陈述侧的 iff。末句"不只是极小点而是唯一极小点"由 iff 直接支撑。
10. 行 53 `d:strict-minimality-rests-on-positivity-of-the-norm` —— 本体断言由 `15.10.md:125` 的 Theorem 15.9(b) 逐字支撑。（末句 "Without positivity the theorem would only give a non-strict minimum" 是反事实陈述，原则上不可锚，属可接受的无锚类型；锚点 2 的实空间公理块出处见 4.2 末段登记。）

## 6. 因 SPEC 30 字符下限 / 3 锚点上限导致证据不足的实例计数

**计数：2 个被阻挡的公式，影响 3 处节点-claim。3 锚点上限在本片区一次也没有成为约束。**

### 6.1 30 字符下限阻挡（实测行长）

| 原文位置 | 内容 | 字符数 | 受影响 |
| --- | --- | --- | --- |
| `15.15.md:6` | `\| x - s \| \leq \| x - t \|` | **28** | 节点 48、52 |
| `15.15.md:14` | `x - t = (x - s) + (s - t).` | **26** | 节点 50 |

`15.15.md:6` 这一条尤其值得记入 SPEC 缺陷账：**它就是 Theorem 15.16（逼近定理）的核心不等式本身**，全章最重要的结论式之一，28 字符，差 2 个字符无法引用。任何要断言"逼近定理说的是 `‖x−s‖ ≤ ‖x−t‖`"的节点，都只能引它旁边的散文（"Then the projection of x on S is nearer to x than any other element of S." 或 "for all $t$ in $S$ ..."）。节点 48、52 正是这样做的。**这部分不判生产者。**

`15.15.md:14` 的分解式同理，26 字符。节点 50 只能引 `15.15.md:17` 的散文句"Since s−t ∈ S and x−s = s⁻ ∈ S⊥, this is an orthogonal decomposition of x−t..."来间接指代该分解。所幸那句散文信息量足够，故我仍判节点 50 支撑充分。

（与任务书列出的 Axiom 6/7/8/9 一类相同性质：短公式行天然低于 30 下限。本片区新增的两条实测数据可补入 `SPEC缺陷-锚点规则量化.md` 的计数。）

### 6.2 200 字符上限——未构成阻挡

`15.15.md:28`（(15.20) 的 φ 定义）整行 **211 字符**，超上限。但 SPEC 只要求 quote 是逐字连续**子串**，取其中一段（如 `\varphi_ {0} (x) = \frac {1}{\sqrt {2 \pi}}, \quad \varphi_ {2 k - 1} (x) = \frac {\cos k x}{\sqrt {\pi}}`）即合规。同理 `15.14.md:9`（245 字符）、`15.14.md:3`（318 字符）都可取子串。**故不计入阻挡**，节点 54、45 的缺锚仍判生产者可修。

### 6.3 3 锚点上限——本片区零阻挡

实测 27 个节点的锚点数：1 条的 4 个（行 29、35、38、45），2 条的 23 个，**3 条的 0 个**。**没有任何一个节点用满配额**。因此第 2、3 节列出的全部 17 处 D/E 缺陷，一处都不能归因于 3 锚点上限——每个缺陷节点都至少剩 1 个空位，而所需的补锚句我已逐条查明存在且长度合规（`15.14.md:11`/186、`:33`/83、`:50`/148、`:63`/44、`15.11.md:64`/90、`15.13.md:35`、`15.15.md:17`、`:23`、`:28` 子串）。**这一条是本片区对"SPEC 是否该背锅"这个问题的明确回答：不该。**

## 7. 顺带发现的其它维度问题（仅登记，不裁定）

**7.1 行 30 的"四项义务"与图结构不符。** statement 称存在性 "splits into four obligations"，`atomic_reason` 也写"由四步互相独立的验证组成"。但 `edges-D4.jsonl` 中以 `part-of` 指向 `d:orthogonal-decomposition-existence` 的子节点实测有 **5 个**（行 31、32、33、34、35，对应 edges 第 73、76、80、82、86 行）。原文 `15.14.md:30–42` 的步骤实为五项：取基、定义 s 与 s⊥、s ∈ S、(15.16) 成立、s⊥ ∈ S⊥；节点 30 的四项合并了"(15.16) 成立"（该项由行 33 的第二句承载）。数学无错，但计数与自己的子树不一致，且"四"这个数字在 statement 与 `atomic_reason` 里出现两次。建议改为五，或明说合并了哪一项。

**7.2 行 51 末句把"误差"与"误差平方"混同。** 原句："the error of any competitor exceeds the error at s by exactly ‖s−t‖²"。严格读为**假**：成立的是 `‖x−t‖² − ‖x−s‖² = ‖s−t‖²`（误差平方之差），而误差本身之差 `‖x−t‖ − ‖x−s‖` 一般不等于 `‖s−t‖²`（量纲都不对）。前一句刚给出平方形式的恒等式，故善意读者会自动补上"squared"，但字面为假。改法：把 "the error" 两处改为 "the squared error"。若裁定员从严，可按死因 **4**（数学错误）处理；我倾向 **0 = KEEP + 改措辞**，因为这是末句追加洞见时的表述疏失，不是理解错误。

**7.3 `atomic_reason` 与 statement 实含断言数不符（2 例）。**
- 行 28：`atomic_reason` 称"是单一等式"，但 statement 含两条断言（`S⊥ = L(S)⊥`，以及"这是子空间性无关的结构性理由"）。
- 行 30：见 7.1。

**7.4 六个节点含模型自造内容但缺 `origin: model`。** 实测 27 个节点中只有 4 个有 `origin: model`（行 28、40、43、44）。而行 **30、31、36、45、48、51** 的 statement 均含原文无对应句的模型断言（证明分类学、负向穷举断言、全称量词概括、证明结构重述、价值判断），按 SPEC 第 55 行应标注。**这是本片区最系统化的单项问题**，且它与形式 E 高度重合——凡末句追加洞见的节点，几乎都同时缺锚点和缺 `origin` 标注。建议裁定员把"末句洞见必须标 origin: model 或删"作为一条可机械检查的生产规则。

**7.5 行 46 的 `node_type` 值得复核。** 它被标为 `theorem`，但内容是 V_3 图示中的一句平凡观察（"若 x ∈ S 则解就是 x"），原文 `15.14.md:7` 用的是 "clearly"。与相邻的行 47（同段落、同性质，标为 `concept`）不一致。属分类一致性问题，非正确性问题。

**7.6 B 型（借用引文）在本片区的分布。** 我统计了片区内锚点 `(file, quote)` 对的复用（仅统计涉及行 28–54 的）：共 9 对被复用，其中跨到本片区外的 4 对（与行 9、14、15、19 共享）。经核对，只有 **1 处构成真正的借用**：行 44 借用行 36 的 "Next we prove that the orthogonal decomposition (15.16) is unique."（已记入 2.5）。其余复用（如行 42/43 共享投影命名句、行 48/52 共享 iff 条件句、行 15/19/53 共享正定性句）都是同一事实被多个相邻节点各自正当引用，不判缺陷。

**7.7 未系统审查边。** 我按需 grep 了与本片区节点相关的 `edges-D4.jsonl` 条目（第 52–120 行区间的相关行）以判断"被推迟的内容是否由边承载"，但**没有系统审查边的 rel 方向、evidence 支撑性或完整性**。这不在本次任务范围内，登记以免被误认为已覆盖。

## 8. 附录：逐批工作记录

### 批 1（行 28–34，7 个节点）初判

- 28 `d:orthogonality-to-a-set-equals-orthogonality-to-its-span` — 末句"subspace-ness of S is irrelevant"无锚（对应原文 15.14:11 前半未引），形式 E。
- 29 `d:orthogonal-complement-is-a-name-reserved-for-subspaces` — 唯一锚只管命名约定，"always a subspace"无锚；15.14:11 整行约 182 字符本可一锚覆盖两半 ⇒ 形式 D，生产者可修。
- 30 `d:orthogonal-decomposition-existence` — 两锚只覆盖"先证存在"+"定义 s"；四义务分解、"只有第二项是构造"、"基的选取是唯一任意输入"、末句"唯一性使构造与选取无关"全部无锚且未标 origin: model ⇒ 形式 E。
- 31 `d:finite-dimensionality-supplies-a-finite-orthonormal-basis` — 末句"唯一性一半与勾股公式从不使用有限维"无锚（我独立核 15.14:50、:52–58，为真）；"exactly once"量词亦无锚。形式 E（末句）。
- 32 `d:s-defined-as-the-sum-of-projections-along-the-basis-elements` — 支撑充分。
- 33 `d:s-lies-in-s-because-it-is-a-linear-combination-of-the-basis` — 支撑充分。
- 34 `d:s-perp-is-orthogonal-to-each-basis-element` — 末句"正交规范性使 (s,e_j) 塌缩成单个系数"无锚（原文 15.14:42 把这步留给读者）。轻度形式 E，待查 requires 边。

### 批 2（行 35–41，7 个节点）初判

- 35 `d:orthogonality-to-the-basis-extends-to-all-of-s` — 末句"与 Gram-Schmidt 证明里同一座张成推广桥"无锚，只用了 1/3 锚点额度，15.13:35 本可引 ⇒ 形式 E，可修。
- 36 `d:orthogonal-decomposition-uniqueness` — 末句"No finiteness or basis is used in this half"无锚（真，我核过 15.14:50）；锚 2 止于"say"，未及 15.14:50 的"where s and t are in S..."⇒ 形式 D + E。
- 37 `d:uniqueness-reduces-to-a-single-vanishing-difference` — 支撑充分。
- 38 `d:the-difference-lies-in-both-s-and-s-perp` — 末句"只由 S 与 S⊥ 对差封闭所迫"无锚（S⊥ 是子空间在 15.14:11，本节点未引）。轻度形式 E。
- 39 `d:uniqueness-concludes-by-self-orthogonality` — 支撑充分。
- 40 `d:pythagorean-expansion-of-the-squared-norm` — 支撑充分，且如实自陈"Apostol 把展开压成一行,交叉项形态是重构"，标了 origin: model。本片区处理无锚推理的正面样板。
- 41 `d:pythagorean-cross-terms-vanish-by-orthogonality` — 支撑充分（"precisely because s∈S 且 s⊥∈S⊥"到"s 与 s⊥ 正交"差一步 S⊥ 定义，可接受）。

### 批 3（行 42–48，7 个节点）初判

- 42 `d:projection-on-a-subspace-is-defined-through-an-orthonormal-basis` — 两锚一头一尾夹住定义，中间的定义式 15.14:63（43 字符，合规且有空余锚位）未引 ⇒ 形式 D。
- 43 `d:projection-is-the-s-component-of-the-orthogonal-decomposition` — 核心断言是"两条公式逐字相同"，而两锚都不含任何公式 ⇒ 形式 D（有 origin: model 与自陈作缓解）。
- 44 `d:projection-does-not-depend-on-the-chosen-orthonormal-basis` — 锚 1 只宣告"接下来证唯一性"，缺"任一正交规范基造出的 s 都满足 s∈S 且 x−s∈S⊥"这一中间桥 ⇒ 形式 D；该锚同时被节点 36 使用，属借用。
- 45 `d:distance-is-the-norm-of-the-difference` — 末句"15.14 与 15.15 中每一个极小化陈述因此都是关于范数的陈述"含量词"every"且无锚（15.14:3 的"whose distance from x is as small as possible"本可引，尚有 2 个空锚位）⇒ 形式 D。
- 46 `d:trivial-case-when-x-already-lies-in-s` — 首句作一般断言，两锚全在 V_3 特例段（15.14:5–7）内 ⇒ 形式 D（射程只到 V_3）；第二句自陈провenance 作缓解。
- 47 `d:nearest-point-by-dropping-a-perpendicular` — 支撑充分（末句"motivation"由锚 2"motivates the discussion that follows"覆盖）。
- 48 `d:approximation-inequality-with-equality-only-at-the-projection` — 两锚只是定理陈述，全部证明结构断言（两步、恒等式、许可条件、Theorem 15.9(a)(b)、"no minimization argument anywhere"）在本节点无锚 ⇒ 形式 D。

### 批 4（行 49–54，6 个节点）初判

- 49 `d:approximation-proof-starts-from-the-orthogonal-decomposition` — 第二条"通道"依赖 15.14:60（投影定义只对有限维子空间给出）未引 ⇒ 轻度形式 D。
- 50 `d:x-minus-t-splits-orthogonally` — 支撑充分；分解式 15.15:14 仅 26 字符，受 SPEC 30 下限阻挡（记入第 6 节）。
- 51 `d:pythagorean-split-of-the-approximation-error` — 末句无锚，且"the error of any competitor exceeds the error at s by exactly ||s−t||^2"把误差与误差平方混同（严格读为假）⇒ 记入第 7 节 + 形式 E。
- 52 `d:equality-holds-exactly-when-t-equals-s` — 支撑充分。
- 53 `d:strict-minimality-rests-on-positivity-of-the-norm` — 末句为反事实陈述，无锚且不可锚；本体断言由 15.10:125 逐字支撑。判轻度 E / 可接受。
- 54 `d:fourier-coefficients-in-cosine-and-sine-form` — 两条模型自算关系我已人工验算：(f,φ_{2k−1})=√π·a_k ✓、k=0 项贡献 a_0/2 ✓（推导见 4.4）。但 (15.20) 的 φ 定义（15.15:28）未引，两条关系在本节点无锚 ⇒ 形式 E，可修（有空锚位）。

### 批 5：交叉验证与量化（在初判之后做，用于修正初判）

已完成的核实动作与结果：

1. **Theorem 15.14 的内容核实**（为判节点 31）：`15.13.md:82` = "THEOREM 15.14. Every finite-dimensional Euclidean space has an orthonormal basis."。节点 31 引用它得"S 有限维 ⇒ S 有有限正交规范基"，正确（S 作为子空间自身是 Euclidean space）。`15.13.md:80` 给出 Gram-Schmidt 到正交规范基的归一化路径，故节点 30 的 "via Gram-Schmidt" 归因也为真（虽无锚）。
2. **15.11 三条定理的有限维措辞核实**：`:7` Theorem 15.10 首句 "In a Euclidean space V, every orthogonal set of nonzero elements is independent." 无有限维（后半句才引入 dim V = n）；`:43` Theorem 15.11 与 `:77` Theorem 15.12 均写 finite-dimensional。与任务书一致。本片区无节点断言它们 ⇒ 记入第 9 节。
3. **行长实测**（用于第 6 节，修正了我对节点 54 与 45 的初判严重度）：`15.15.md:6` = 28 字符、`:14` = 26 字符（**两条真阻挡**）；`:28` = 211 字符（超上限但可取子串，**不构成阻挡**）；`15.14.md:11` = 186、`:33` = 83、`:50` = 148、`:63` = 44、`15.11.md:64` = 90（**全部合规，补锚方案可行**）。因此把节点 54 从"SPEC 阻挡"改判为"生产者可修"。
4. **锚点数配额实测**：27 个节点无一用满 3 条（4 个用 1 条，23 个用 2 条）⇒ 3 锚点上限在本片区零阻挡，写入 6.3。这一条推翻了"缺陷可归因于配额不足"的可能辩解。
5. **锚点复用统计**（B 型旁证）：涉及本片区的复用共 9 对，只有行 44 借用行 36 一处构成真借用。写入 7.6。
6. **子节点计数核实**：`part-of → d:orthogonal-decomposition-existence` 实测 5 条（edges 73/76/80/82/86），而节点 30 自称"四项义务" ⇒ 写入 7.1。`part-of → 逼近定理节点` 4 条、`→ 唯一性节点` 3 条，与各自 statement 的分步描述不矛盾。
7. **`origin` 字段普查**：27 个节点中仅行 28、40、43、44 有 `origin: model`；行 30、31、36、45、48、51 含模型自造断言而未标 ⇒ 写入 7.4。
8. **4.4 的两条自算值手算完毕**（`√π a_k`、`a_0/2`），均为真；顺带手算 Example 2 的 `f_1(t) = (3/π) t` 与原文一致。

初判到定稿的三处修正：(a) 节点 54 由"SPEC 阻挡"改为"可修"；(b) 节点 35、38 因 edges 87/93 承载而降低严重度；(c) 节点 51 的末句问题从单纯 E 型升级为 E 型 + 表述错（7.2）。

## 9. 未覆盖事项声明

1. **审查范围完整**：第 28–54 行全部 27 个节点逐条审毕，无静默截断。
2. **专项 4.1 的部分对照点在本片区无节点**：任务书要求核 `15.11.md:7`（Theorem 15.10 无有限维）、`:43`（Theorem 15.11 有）、`:77`（Theorem 15.12 有）是否被节点增删。片区 28–54 的锚点只覆盖 15.10、15.13、15.14、15.15 四个文件，**没有任何节点断言 Theorem 15.10/15.11/15.12**。我已读原文确认三条定理的措辞与任务书描述一致，但无节点可判。这三条若有节点，应在片 1–27 或其它 D 文件中，不在我的范围。
3. **专项 4.3 无测试用例**：`15.10.md:59`（C(a,b) 连续性）、`:73`（权函数 w）、`:75–81`（Example 5 反常积分）在本片区无节点引用，故"∫f²=0 ⇒ f=0 在仅 Riemann 可积类上断裂"这一风险点在片区 28–54 内没有可判之处。判定为"风险未材化"，而非"已检查通过"。
4. **15.15 的两个例子覆盖不全**：Example 1（三角多项式逼近）只有节点 54 覆盖其系数换算一角；**Example 2（Legendre 多项式逼近，`15.15.md:53–77`）在本片区完全无节点**——包括 `(f,φ_0) = 0`、`(f,φ_1) = √(3/2)(2/π)`、`f_1(t) = (3/π) t`、`(f,φ_2) = 0` 故亦为最佳二次逼近这几条。我出于验算习惯独立核了 `f_1(t) = (3/π) t`（见 4.4 末），与原文一致，但这不是对节点的审查。Example 1 的 (15.21)→(15.22) 改写、`15.15.md:31` 的 `dim S = 2n+1`、`:51` 的"逼近优于任何其它三角多项式"同样无节点。这属图谱覆盖缺口，登记供裁定员判断是否需要补节点。
5. **未做的检查**：(a) 未系统审查任何边（见 7.7）；(b) 未做 A 型（变量位置错）与 C 型（端点当中间步骤）的专项排查——这两型不在我的任务分工内，我只在逐条读 statement 时顺带留意，未发现明显实例，但**这不构成"本片区无 A/C 型"的结论**；(c) 未与其它审查员的判定交叉比对（按硬禁未读其它 `审查-形式D-*.md`、`裁定-*.md`、`_捞回-*.md`、`audit-*`），故本报告的判定完全独立，可能与他人重叠或冲突；(d) 未读任何 `M08实验-Apostol微积分卷1/` 目录。
6. **未验证 `grep -F` 逐字性**：已有脚本报告 100% 通过，我未重跑。我的工作前提是"引文确实是原文子串"，专注判断"引文是否支撑其所挂断言"。我在阅读原文（15.10、15.11、15.13、15.14、15.15）过程中顺带比对的锚点均与原文一致，未发现伪造出处。
