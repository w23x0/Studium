追加 4 条，编号甲–丁。「已核」= 本轮回查提案原文或 `report/` 目标行；「采信」= 照抄碎片自报值。篇幅所限只装配我能自证的条目，其余入未覆盖清单。

### 追加裁决 甲 —— A7 主体在文件里不存在

**裁决：`A7-H层整合.md` 整份撤回，H 层改由 G3a/G3b/G3c/G3e 承载。已核。**该文件磁盘全长 **13 行 / 1284 字符**，§1–§7 只以第 13 行一条 `<!--SKELETON: §1 机械核验 / …-->` 存在；§0 自报的 34 项裁决与 3 处新发现从未写出，其机械合规结论的依据在未写出的 §1，移出本轮。
**与既存裁决 3 不冲突**：裁决 3 只依赖 §0 的口径自我更正与 G3a–G3e 的速览动词，仍成立；本条只取消 A7 的落盘载体资格。**对裁决 3 补一处口径**：522 = `data/nodes-*` 397 + `inherited/` 125，**不含 `structures-*.jsonl` 的 24 行**（计入则 546）；A7 未交代，照抄会与按 546 计数的提案对不上（采信 C1-02）。

### 追加裁决 乙 —— A1 的 §3/§4/§5 同样不存在

**裁决：§0/§1/§2 采纳，§3/§4/§5 落选·无法装配。已核。**`A1-互证塌缩检查.md` 实测 **146 行**，止于 §2 后的 `<!-- MARK-3 -->`。故 §0 第 32 行把「§3.2 的 6 条重指必须先做完」列为 `apply_kills.py` 的**强制**前置——该前置本身不可查，**不采纳**，否则 21 条待杀永不放行。可装配（采信 C1-01）：21 条待杀 id 全命中无重复，两口径边计数 71/24/44/3 与 82/26/53/3 全中，零残留实测 0。
**两个数字不可引用**（已核）：①「引文彻底消失 7 条」——§2.2 表实为 LOST **9** 条，第 144 行自称「§0 已改」而第 19 行现文仍是 7；②「29 − 9 = 22」推导错（9 个争议里 B2 判 KILL 的只有 7 个），22 可用、算式不得进验收单。另 §2.2 NEAR 那条可杀但**须补路径**：存活侧在 `data/inherited/nodes-A1.jsonl:15`，A1 漏写 `inherited/`。

### 追加裁决 丙 —— A6 两个主方案都走，一处自评须先删

**裁决：`nodes-D2.jsonl:36` 与 `nodes-D4.jsonl:51` 按 §1.4/§2.4 改 `statement` 并各补 1 条锚点，改后采纳。**A2（`15.07.md` L39，78 字符）与 B3（`15.15.md` L11，82 字符）全库 `(file,quote)` 使用者均 0；两节点现 1 / 2 条锚点，加后 2 / 3 条，未触上限。
**已核并修正 C1-02 一处**：主方案定稿（A6 第 182 行）**确已**换成平方口径（`the squared distance from x to t exceeds … by exactly ||s - t||^2`），故 C1-02 称「保留了它刚批评的 `exceeds … by exactly` 结构」不准确；但平方化不改 `exceeds` 的严格性含义，t = s 时歧义仍在，落笔前删去 §2.3（第 167 行）那句自评，不涉数据改动。
连带：§1.5/§2.6 三条「不要做」采纳；§2.5 换 96 字符长跨不采纳（已被 `edges-A2-s3.jsonl:4`、`edges-D4.jsonl:126` 占用）；两备选互斥、移出本轮。**顺序约束**：填满 `:51` 第三槽后无空位，后续给它补锚须排在 A6 前。921 → 923 未复核。

### 追加裁决 丁 —— A10 六项落盘，两处形式硬伤 + 三条引用限制

**裁决：§6.1/§6.3/§6.4 采纳，§6.2/§6.5/§4.2 改后采纳。**
- §6.2（41 → 42）**须子串替换**。已核：`审查-形式B-共享引文.md:15` 实测 115 字符，`41 个不同节点**（` 之后还有一个 id 与顿号；A10 的「现文」只截到 `（`，整行覆盖会截掉行尾 id。
- **§6 编号须主控重排后才能引用**。已核：`### 6.5` 出现两次（第 **392**、**422** 行），`### 6.4`（第 **406** 行）排在第一个 6.5 后。
- §6.3 须同时保留「整批 34 个」与「剩余 28 个未审」（已审 6 = 5 借用锚 + 1 合法）。
- §4.2 用**析取** `exonly or intra>=3`（合取实测把召回从 20/25 打到 5/25）；`>=3` 按 §3.3 自述不足以定阈值、**标暂定**；review 集不得标「形式 B 缺陷集」（49 命中已确认 20 个中 15 个是形式 E、不判死）。§6.5 三句须与 §3.3 三条局限同段写入。
- **三条引用限制**：「召回」只是对已知正例的覆盖率、分母未知；0.34 是下界非精确率；§八 复算只证计算无误。

### 冲突待人工裁定 —— `nodes-D4.jsonl:51` 改写是否连带补 `origin: model`

- **C1-02 侧（依据 `A6-两处数学缺陷.md` §〇 与 §1.7/§2.7）**：只改 `statement`，其余字段一律不动。已核 A6 第 17 行逐字为「`atomic`、`atomic_reason` 一律不动，边一条不动」，该清单**不含 `origin`**——A6 从未提及此字段，「一律不动」是否辖及新增字段，文本本身不判。
- **C1-05 侧（依据 `G6d-D4-片28-54-摘要.md` §7.4）**：该摘要实测缺 `origin` 的 6 个节点（30、31、36、45、48、**51**）含 `:51`，且 A6 自陈其 C1b 落在 T2，正是 §7.4 要求补 `origin: model` 的形态。

两者都在同一行的执行路径上，不裁无法排序。附注：裁决 2 已改用 `unanchorable` 作无锚凭据；若采 C1-05，须确认补的是**严重度标签**而非豁免凭据。

**本片未覆盖**（碎片有裁决意见、本节未装配，须另片承接）：①**A9 三处更正**，其中更正三对裁决 1 有一条顺序约束——`inherited/nodes-A1.jsonl:8` 两项写入完成后才可把 `audit-A5-A1-verdicts.jsonl:8` 的 cause 由 6 改 0，否则 verdicts 处于「无缺陷而缺陷仍在」的中间态；②**A3 全部**（§0「全图无锚节点恰好 4 个」可支持裁决 2；§1 须作前置裁断项）；③**A4 全部**（验收只能用 918/917）；④**A5 全部**（§5.1 脚本须改定点插入）；⑤**G6d 六份摘要全部**（四处死因改判、两处绑定同改、R-5 八节点、`node_type` 越词表 14 个节点，及落选项）；⑥碎片自报的 `report/` 行号未独立定位（仅 `审查-形式B-共享引文.md:15` 已核）；⑦全库锚点计数（923 / 918 / 917）未实测。
