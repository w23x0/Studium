# 装配片 C3-01（P01–P03）

三份提案原文已逐节回查（P01 24815 字符、P02 19429、P03 23062），下列逐字文本均照抄原文，未转述。

## 主体

**1. G5-P01-cause6 入 SPEC.md｜条文 A：整行替换 `SPEC.md:100`。** 旧行逐字为
`死因编号:`1` 量词过度断言 / `2` 编造成员归属 / `3` 伪造教材出处 / `4` 数学错误 / `5` 伪抽象·层塌缩。`；
新段为该文 §二「条文 A」全段（原文第 87–140 行，约 45 行代码块），含新增 `0` 与 `6`、3/6/1 三环互斥表、
`6` 的五步判定程序、T2/T3 塌陷分型表、「TD 排除的理由是语义的不是体量的」、`6-不可修` 标记法。
**采纳**：现有 `:100` 无 6 也无 0，`:93` 的「引文是否真在书里」只覆盖死因 3，缺口是原文取证过的。

**2. P01｜条文 A 硬依赖 S2 项 2。** 判定程序第 2 步引用「证据池:四层,前三层入池」小节（S2 项 2 在
`SPEC.md:57` 之后新增）。**采纳，且必须同批落地**；若 S2 项 2 不落地，条文 A 须自带四层池完整定义表，
否则第 2 步指向空引用（原文第 173–175 行明写）。

**3. P01｜条文 B：`SPEC.md:98` 段末追加，不整行替换。** 追加文本逐字为原文第 147–150 行
（`0` 只能配 `KEEP`；`FIX | 0` 是格式错误；`FIX | 6` 与 `KILL | 6` 以「是否存在可引依据」分界）。
**采纳**：`:98` 的整行替换权已由 P01 自行让给 S3 三档版（原文 §1.3(d)），本条对两种 `:98` 版本都可原样追加。

**4. P01｜条文 C：整段替换 `SPEC.md:93–94`。** 新段为原文第 156–161 行，在审计 A 原两行后加三句：
「引文真在书里」只排除 `3` 不排除 `6`、每条锚点须再问「它管到这句断言了吗」、
「N/N 锚点已验证」永远不得当作正确性保证。**采纳**。

**5. P01｜施加顺序 `100` → `98` → `93` 倒序。** **采纳**：A 插入 45 行会把 B、C 的行号推走（原文第 171 行）。

**6. P01｜判定程序不适用于 `L2:` 及以上抽象层。** **改后采纳**：这句只写在反例 3（原文第 296 行），
条文 A 的判定程序本体里没有排除句，照原样落地会让 24 个 `L2:` 节点全部判 6（假阳性 24）；
须把「本判定程序不适用于 `L2:` 及以上抽象层节点」补进条文 A 第 1 步之前。

**7. P01｜量化口径改用源行。** 采纳原文 §4.3 表：546 全口径按 T1 源行 327、按 T1+T2+T3 源行 **263**
（正式口径）、误计入 TD 222；522 口径 303 / **239** / 198；TD 误计入只洗掉 41 个 = **15.6%**。
**采纳**：条文 A 已把 15.6% 写进条文，用来封住「靠体量推翻 TD 排除」这条路。S2 的 51.9%（271/522）
应改记为 52.3%（273/522），差异来自切句时对 `$…$` 的屏蔽细节。

**8. G5-P02｜替换 `SPEC.md:53–54` 两行为六条。** 新文本为原文第 80–97 行整块。净变化：2 行改 6 行，
另在 `SPEC.md:73–74` 之后插入第 6 条；schema 新增字段 **0 个**。**采纳**。

**9. P02｜删掉 `grep -F` 作为跨行 quote 的校验手段，改全文子串测试。** **采纳，且在与 S1 合并时以 P02 为准**：
S1 的逐字条文写「`grep -F` 与 `quote in text` 均可直接校验」为实测错误——把公理 6 引入句与公理 9 公式
拼成的 114 字符假 quote 在 `15.02.md` 上命中 52 行，与真窗口完全一样，Python `bad in text` 返 False。
`grep -F` 恰好放行了「不跨行」这条本想防的伪造。

**10. P02｜唯一性作附加闸门，不作替代判据。** **采纳**：纯唯一性判据会让 215 条短内容行里 174 条
（80.9%）立刻合法，其中只有 36 条是数学、138 条是标题/散文/HTML；apostol-ch15 里长度 10 的文件内
唯一子串就有 38,866 条，唯一性在本语料上是恒真式。

**11. P02｜第 6 条把边 `evidence` 纳入同一套长度与唯一性约束。** **采纳，附 5 条回溯 FIX 清单**（原文
§4.1 表，逐字照抄）：`data/edges-D1.jsonl:10`（`Closure axioms`，14 字符）、`:11`（`Axioms for addition`，19）、
`:23`（`Closure axioms`，14）三条 <30；`data/edges-A2-s2.jsonl:41` 与 `data/edges-D4.jsonl:108`
（`s = \sum_ {i = 1} ^ {n} (x, e _ {i}) e _ {i}`）文件内出现 2 次。5 条全部可修，无一需 KILL。
921 条锚点因新增唯一性/长度/换行变违规均为 **0**。

**12. P02｜上下窗口都越界时「维持不可引」，不启用中途起始退化形态。** **采纳**：那 6 条退化 quote
（`15.09-exercises.md:5/7/9/11/13`、`15.13.md:46`）首字符全是被截断的单词或半个 LaTeX 花括号，
通过一切机器检查却人不可读，且全部不在已确证受损节点里——用难读锚点换零收益。

**13. P02｜伴随的 `tools/` 侧两处（非 SPEC 文本，但不改则修订无强制力）。**
`verify_anchors.py:56` 的 `quote and quote in text` 对跨行子串已正确工作、无需改动，但须新增
30–200 界与唯一性两项检查；`export_obsidian.py:145` 的 `lines.append(f"  > {quote}")` 会被跨行 quote
破坏，修法 `quote.replace("\n", "\n  > ")`。**采纳为伴随项**，与条文同批。

**14. G5-P03｜A 段：触发条件从「行间公式」放宽到「断言独占一行」。** **改后采纳，并须改挂靠对象**：
P03 原文第 105 行说 A 段替换的是 `S1-SPEC-锚点规则.md:81–83`，但本轮 `:53–54` 的替换权已归 P02，
而 P02 §二 第 5 条自身仍写「**行间公式必须连同它的断言一起引**」——A 段应替换的是 **P02 第 5 条**，
不是 S1 第 4 条。A 段逐字为 P03 原文第 64–71 行，含「带编号标签的定理子条款行(形如 `(a) ...`、`(b) ...`)
同样适用」与「不得改为引用它的证明或它所属定理的抬头来顶替」。

**15. P03｜B 段与 P02 第 5 条 (2)(3) 档合并，不并列。** **改后采纳**：P02 第 5 条已含「向上越界改向下」
与「上下都越界维持不可引」；B 段真正新增的只有两点——向下退让时**必须**追加定理抬头行为第二锚点以
保留量词，以及禁止从实质行中途起始截断（会切掉 `THEOREM 15.x.`，制造 A 型宾位错）。装配时须合并成
一条，两处各写会漂移。

**16. P03｜参照案例照抄（原文第 91 行，转义前 157 字符、全语料唯一）。** **采纳**：
`{"file":"15.10.md","quote":"THEOREM 15.9. In a Euclidean space, every norm has the following properties for all elements $x$ and $y$ and all scalars $c$ :\n\n(a) $\\| x\\| = 0$ if $x = O$ ."}`

**17. P03｜事实纠正：定理 15.9(a) 在 `15.10.md:123`，不在 `15.09-exercises.md`。** **采纳**：
后者是「§15.9 习题」，全语料 `grep -n "15\.9"` 仅 4 处命中，`15.09-exercises.md` 里那处是标题
`## 15.9 Exercises`。任务书与交接文档 §4.10 的待议项标题须回改。

**18. P03｜4 条源行从不可引变可引（FIX 清单，逐字照抄）。** `15.04.md:31` 11 字符 `(a) 0x = O.`、
`:33` 14 字符 `(b) $aO = O$ .`、`:35` 28 字符 `(c) $(-a)x = -(ax) = a(-x).$`、
`15.10.md:123` **29** 字符 `(a) $\| x\| = 0$ if $x = O$ .`。**采纳**。落地形态（原文 §5.1）：
15.9(a)/15.3(a)/15.3(b) 走向上（157/178/194 字符），15.3(c) 向上 **224 越界 24 字符**，走向下（80）
＋追加 `15.04.md:29` 抬头行（165 字符）为第二锚点。4/4 可解，0 条需中途起始。

**19. P03｜「禁止顶替」不回溯判违规，只作 FIX 依据。** **采纳**：那 7 条锚点（涉 4 节点）逐字属实、
也与节点相关，只是没管到断言本体，属 FIX 而非 KILL。其中 `d:norm-vanishes-at-the-zero-element`
可判死因 **6**（`exposure.tsv` 实测 T1=2/T2=0/T3=0，两条锚点皆借用引文：`15.10.md:121` 抬头 5 个持有者、
`:133` Proof 句 7 个持有者），是 P01 条文 A 可直接引用的干净标本。

## 落选项

- P01：为 `L2:` 引入「TM 层 = members 的池并集」——原文实测 24/24 会获非空池、新增 913 条不同
  `(file,quote)`，但作者自己不建议（会成为伪抽象的掩体）且明言「留给主控裁」，本轮移出。
- P01：`SPEC.md:33` 节点类型词表（`concept/method/theorem/notation`）与任务书封闭词表
  （`concept/theorem/proof-step/example`）不一致——原文列在「顺带发现」且注明无人认领，移出本轮。
- P01：15 条未校验 model-quote（`data/edges-X.jsonl:106–121`）进池——原文实测 15/15 逐字命中源行、
  本轮不产生错误免除，且明写「条文 A 无须为此加规则」，不入条文。
- P02：把唯一性改成全语料唯一——原文不建议（`file` 字段已限定范围），且会使
  `d:exponential-weight-makes-improper-integral-converge` 成为 921 条里唯一的回溯违规锚点。
- P02：动 30 这个地板数——原文选择不动（抬到 33 违规 2 条、35 违规 8 条、40 违规 32 条），不入本轮。
- P02：回改交接文档 §3.9「正确的旋钮是唯一性，不是长度」与 SPECQ/S1 的 35 vs 36 口径分歧——
  属文档维护，不是 SPEC 条文，移出本轮（若 SPEC 写入「数学承载行」一词，须同时写死判据）。
- P03：A 段触发条件写成正则 `^\([a-z]\)`——原文自己否决：会把 5 行习题选项
  （`15.09-exercises.md:39/63/65`、`15.12-exercises.md:5/87`，后者仅 3 字符裸 `(a)`）一并放进来。
- P03：121/123/125/133 四行的 `n_sentence_end`——原文自标「**此项未核**」（`source-lines.tsv` 的
  `file` 列写作 `apostol-ch15/15.10.md`、`anchors.tsv` 写裸文件名，直接 join 全空），不作依据。

**本片未覆盖**：P01 与 S3 在 `SPEC.md:98` 上撞车的最终合并文本（须待 S3 切片；本片只确认 P01 已让权
并给出可追加的 4 行）；P03 第 6 项耦合的 3 锚点上限（`d:thm-15-3c` 追加后正好触顶）须待 P04 裁定。
