# 待应用清单（三位裁定员已返回,本文件已按裁定结果重写)

原版写于等待期,记的是 B2 的判决加我自己的初步核验。三位裁定员返回后**结论变了不少**,
所以整份重写。上一版的"5 个维持的 KILL"**不再成立**。

判决原则:**按证据强度定,不按人数定。**下面每条都记了是谁的证据、可否机械复核。

## 〇、裁定员的可比性问题(先说清,否则下面的表会被误读)

三位不是投票席。**ADJ1 与 ADJ2 判的是同一件事**(B2 的冗余/删除测试判决对不对),互相可比;
**ADJ3 判的是另一个轴**——statement 是否为真、是否精确、是否有锚。
ADJ3 的 "KEEP / 0" 意思是"没查出假命题",**不是**"驳回冗余指控"。
把三列并成一个分歧率是范畴错误。所以:

- **分歧率只在 ADJ1 vs ADJ2 之间算:9 个节点分歧 3 个 = 33%。**
- ADJ3 的产出是**前置条件**:一个 statement 若为假,先改写,改写后再谈冗余。

## 一、九个争议节点的合并裁定

| 节点 | B2 | ADJ1 | ADJ2 | ADJ3(真值轴) | **合并裁定** |
|---|---|---|---|---|---|
| `x:dimension-versus-rank` | FIX | FIX | FIX | 须改写(首句缺"有限维") | **FIX**,一致 |
| `x:independence-criterion-and-its-consequences` | FIX | FIX | FIX | KEEP | **FIX**,一致 |
| `d:ten-axioms-listed-in-three-groups` | KILL | KILL | **FIX** | KEEP | **FIX**(见 §2.1) |
| `d:axioms-2-7-8-9-are-the-scalar-dependent-axioms` | KILL | **FIX** | **KEEP** | 须改写(首句假) | **改写,不删**,三方一致不删 |
| `d:an-example-is-a-set-plus-two-explicit-operations` | KILL | KILL | **KEEP** | 须改写(末句假) | **改写+压缩,不删**(见 §2.2) |
| `d:example-1-verification-is-the-field-axioms-of-r` | KILL | KILL | KILL | 须改写(末句因果) | **KILL**,但须先并入 parent |
| `d:function-space-zero-is-the-everywhere-zero-function` | KILL | KILL | KILL | KEEP | **KILL**,零残留 |
| `d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n` | KILL | KILL | KILL | KEEP | **KILL** |
| `x:least-squares-is-a-projection` | KILL | **FIX** | **FIX** | KEEP | **FIX,不删**,两名冗余裁定员一致翻案 |

**维持的 KILL 从 5 个降到 3 个。**被救回的是 `x:least-squares-is-a-projection`
(ADJ1、ADJ2 各自独立翻案:它是 `strang:solvability-condition` 的唯一持有者,
2 个 L1 成员在 X 层别无落点,5 条入边会悬空)与 `d:ten-axioms-listed-in-three-groups`。

## 二、我对两处 ADJ1/ADJ2 分歧的裁断(已机械复核)

### 2.1 `d:ten-axioms-listed-in-three-groups` — 判 FIX,采 ADJ2

**两人其实不在同一件事上分歧。**冗余本身两人都认(2/4/4 可从三个 L1 分组节点一眼数出,
ADJ2 明说"这一路攻不动")。分歧在**处置**:裸删还是先改边再删。

我核了 ADJ2 的边计数,**逐条对上**:

```
OUT edges-D1.jsonl:5    part-of --> apostol:linear-space
IN  edges-D1.jsonl:10   part-of <-- apostol:closure-axioms
IN  edges-D1.jsonl:11   part-of <-- apostol:axioms-for-addition
IN  edges-D1.jsonl:12   part-of <-- apostol:axioms-for-multiplication-by-numbers
IN  edges-D1.jsonl:106  requires <-- d:examples-are-asserted-to-satisfy-the-axioms-and-left-to-the-reader
IN  edges-D1.jsonl:112  requires <-- d:example-1-verification-is-the-field-axioms-of-r
```

5 条入边,其中 3 条是三个 L1 分组节点的 `part-of`。**`apply_kills.py` 把边送进墓地,不改指向**,
所以裸删会让三个分组节点失去它们唯一的汇聚父节点。ADJ2 的前置条件是对的:
**先把 :10/:11/:12 改指 `apostol:linear-space`,再删。**ADJ1 自己在 §3.7 也登记了另一节点的悬空入边问题,
这里只是漏了同一条检查。

### 2.2 `d:an-example-is-a-set-plus-two-explicit-operations` — 判"改写+压缩",部分采 ADJ2

ADJ2 说末句那张十二例运算来源普查表无处可复得;ADJ1 说各例自己的节点里明写。**两人各对一半。**

已查:`d:operations-of-example-N-*` 系列**只有 1、2、3、4 四个**(`nodes-D1.jsonl:45/48/51/54`),
例 5–12 **没有运算节点**,也没有独立的"函数空间前言"节点。所以:

- 第一桶(例 1–3 自带运算)、第二桶(例 4 继承 V_n)**可复得**,ADJ1 对。
- 第三桶(例 5–12 共用前言)**在这些节点里不存在**,ADJ2 对。

唯一另外承担第三桶的是 `d:for-function-spaces-closure-is-the-only-real-content`
(`nodes-D1.jsonl:62`,"Every success and every failure among Examples 5 to 12 is decided by that single question")
——**而 B2 也判了它 KILL(cause 5)**。所以按 B2 全表执行的话第三桶会一起消失。

**裁断:删掉可复得的前两句,保留末句普查表,节点存活。**ADJ3 另发现末句
"the section fixes the operations before the sets"为假(例 1–4 反驳),这一句在压缩时一并删掉。

## 三、发现的方法论缺陷:批量 KILL 的互证坍塌

上面 §2.2 不是孤例。我对 B2 全部 29 个 KILL 做了一次机械检查——
**一个 KILL 的存活论证是否点名了另一个同样被 KILL 的节点**,查出 3 例:

| 被杀节点 | 它的理由引以为据的节点 | 后者状态 |
|---|---|---|
| `d:example-1-verification-is-the-field-axioms-of-r` | `d:example-3-verification-reduces-to-arithmetic-in-each-component` | 也是 KILL |
| `d:example-2-shows-the-scalars-decide-real-or-complex` | `d:real-in-real-linear-space-refers-to-the-scalars` | 也是 KILL |
| `d:example-3-verification-reduces-to-arithmetic-in-each-component` | `d:operations-of-example-3-are-componentwise` | 也是 KILL |

**删除测试是逐节点做的,批量执行时会互证坍塌:**A 判死因为 B 承担,B 判死因为 C 承担,
一起删则内容无人承担。ADJ1 独立发现了其中 example-1/example-3 这一例("B2 引的 example-3 先例并不对称"),
但另两例三位裁定员都没查到——因为**这是全表性质,不是单节点性质,逐节点裁定看不见它。**

**执行约束(新增,强制):**`apply_kills.py` 跑之前必须先跑这项互证检查,
对每个坍塌对做人工裁断,决定救哪一个。**不做这一步就执行,会静默丢内容。**

## 四、执行顺序(每步都先 dry-run)

1. **先改写,再删。**改写 3 个:`d:axioms-2-7-8-9-...`(ADJ3 §3 有定稿文本)、
   `d:an-example-...`(压缩为末句普查表)、`x:dimension-versus-rank`(补"有限维")。
   改写后**必须**按形式 D 程序把新 statement 拆成 claim 逐条核锚点射程,再跑
   `python tools/verify_anchors.py data/nodes-D1.jsonl source`。
   锚点过校验只证明引文未被篡改,不证明它支撑新句子。
2. **改边**:`edges-D1.jsonl:10/11/12` 三条 `part-of` 改指 `apostol:linear-space`。
3. **执行 3 个 KILL**(不是 5 个):
   ```
   python tools/apply_kills.py --verdicts report/audit-B2-verdicts.jsonl --tag audit-B2 \
       --only d:example-1-verification-is-the-field-axioms-of-r \
              d:function-space-zero-is-the-everywhere-zero-function \
              d:degree-exactly-n-is-not-a-subspace-of-degree-at-most-n --dry-run
   ```
   `d:example-1-...` 的十条映射表**必须先并入 parent**(ADJ1 定为强制条件),不可裸删。
4. **不再需要**原 §2 的悬空成员修复:`L2:orthogonality-as-a-certificate-of-optimality` 的
   `members` 里那个 `x:least-squares-is-a-projection` **不删了**,引用不会悬空。
5. **22 个无冲突 KILL**:先跑 §3 的互证检查(已知至少 2 个坍塌对在这批里),
   逐对裁断后再分批执行,与冲突批分开以保持墓地 `_killed_by` 可区分。
6. **B2 剩下 1 个无冲突 FIX**:可直接应用。
7. 全部完成后重跑 `check_graph.py`、`check_layer.py`、`verify_anchors.py`。
   **`verify_anchors.py` 的验收力度比我原先写的弱,见下 §4.1,不能只看它的退出码。**

### 4.1 `verify_anchors.py` 不能作为唯一验收(已读源码 + 实测)

上面第 1 步和第 7 步都写了"跑 `verify_anchors.py`"。读过源码后必须给这个保证加两条限制:

- **它只校验 `quote in text`**(`tools/verify_anchors.py:56`)。SPEC 的「30–200 字符」和「不跨行拼接」
  **从未被任何机器校验覆盖**。好消息是实测 921 条锚点 min 32 / max 198 / 换行 0,**0 违规**——
  规则没强制但被自愿遵守了。所以第 1 步改写后**要手量新锚点的字符数**,校验器不会替你拦。
- **无锚节点不计入失败,退出码仍为 0。**`:75` 是 `return 1 if failures or bad_json else 0`,
  无锚节点进的是 `nodes_without_anchor` 而不是 `failures`。`pass rate` 的分母是锚点数,
  所以无锚节点在这个指标里结构性不可见。实测 `nodes-D2.jsonl` 输出
  `pass rate 100.0%` 与 `nodes with NO anchor: 4`,`EXIT=0`,同一次运行。

**验收时必须同时看 stdout 里的 `nodes with NO anchor` 行,不能只看退出码。**
(是否给校验器打补丁让它对长度和无锚失败,归入 SPEC 待议,不在本清单的执行范围内。)

### 4.2 新增待办:4 个零证据节点(`nodes-D2.jsonl`)

同一次检查查出 4 个 `d:` 节点**既无 anchors、触及它们的边也无 evidence**,即引文支撑为零。
按 SPEC,`d:` 层必须有 1–3 条锚点(只有 L2+ 可无锚),所以这是真违规,不是过报。

| 节点 | 行 | type | 边数 / 带证据 |
| --- | --- | --- | --- |
| `d:exponential-independence-uses-no-inner-product` | :42 | concept | 3 / 0 |
| `d:infinite-dimensional-operational-criterion-arbitrarily-large-independent-sets` | :54 | method | 3 / 0 |
| `d:thm-15-7b-argument-adjoining-an-outside-element-breaks-the-count` | :72 | method | 3 / 0 |
| `d:adjoining-an-element-outside-the-span-preserves-independence` | :73 | theorem | 4 / 0 |

**四个都 `origin: 'model'`,不是伪造归属(死因 2),是"应有锚而无锚"的合规缺口。**
处置**不是删**,分两类:

- :42 和 :54 是**可补锚的**——两者都在陈述 Apostol 写过的东西(15.07 的证明工具清单、15.08 的无穷维判据),
  先按形式 D 程序拆 claim,再去 `15.07.md` / `15.08.md` 找锚点。
- :72 和 :73 是**证明重构**——互为父子,合起来是 Theorem 15.7(b) 的反证骨架,
  :73 的 `atomic_reason` 自己写了"Not atomic: it needs the separate lemma…"。
  这一类**可能补不出锚**(Apostol 的证明是连续散文,拆出的引理没有独立句子承载),
  须先解决 SPEC 待议问题:**证明重构类节点能否允许无锚,只要 `origin: model` 如实标注?**
  在这个问题裁定前不要动这两个,也不要为了凑锚点去硬挂一条射程不足的引文——
  那正是形式 D 缺陷的制造方式。

## 五、裁定员另外查出、须单独处理的正确性缺陷

- **ADJ1 §3.1:A4a 那条尚未落盘的修复文本本身是假的**,会引入新错误。**不得应用。**
  (与上一轮的记录一致,这是第二次独立确认。)
- ADJ1 §3.2:`x:least-squares-is-a-projection` 的 4.1 锚点射程不足(形式 D)。该节点现在存活,故此项必修。
- ADJ1 §3.3:`apostol:function-space` 有形式 E 无锚从句,先存缺陷。
- ADJ1 §3.4:`d:example-1-...` 的十条映射并入 parent 后**仍无锚点**。
- ADJ1 §3.5:B2 的一条修复建议在现行 schema 下不可执行。
- ADJ2 §4.1:B2 判决文本有 3 处事实错误;§4.2:B2 报告正文有 1 处计数矛盾。
  **B2 的 reason 字段须重写的至少有 3 个节点**(含虚构从句一例:B2 把一句节点里不存在的话当作被杀对象引用)。
- ADJ3 §3:4 个节点须改写,定稿文本在该节。两条**假** claim:
  `d:axioms-2-7-8-9-...` 首句(按字面读为六条,且与自身次句矛盾)、
  `d:an-example-...` 末句("先定运算后定集合",例 1–4 反驳)。
- ADJ3 的末句规律再次成立:**5 条缺陷中 3 条在末句**,另 2 条在首句。43 条 claim 里 **24 条无锚**。

## 六、形式 B 审查员带来的待办(报告 `审查-形式B-共享引文.md`)

### 6.1 三处真漂移(死因 6),须改锚点或改文,不是删

| 节点 | 问题 | 处置 |
|---|---|---|
| `d:degree-exactly-n-fails-axiom-2-at-the-scalar-zero` | 点名 Axiom 2,而 Apostol 未点名任何公理;他给的例子是加法方向(Axiom 1) | 改文或改锚 |
| `d:degree-exactly-n-has-no-zero-element` | 点名 Axioms 5/6(加法组),引文说的是闭包组 | 改文或改锚 |
| `d:for-function-spaces-closure-is-the-only-real-content` | 锚点只覆盖一条边缘断言的 1/3,承重的方法论断言无锚 | **重挂锚点,不得丢弃** |

第三条与 §2.2 直接联动:**它是例 5–12 那一桶的唯一另一个承担者,而 B2 判了它 KILL。**
两条独立线索都指向"不能删它",故 **`d:for-function-spaces-closure-is-the-only-real-content`
从 22 个无冲突 KILL 批里撤出,改为重挂锚点。**这是第 4 个被救回的节点。

前两条**不可归 SPEC**:Axiom 2 全文 170 字符、Axiom 5 全文 85 字符,可引而未引。

### 6.2 7 处借用锚点:摘锚点,不动节点(死因 0)

含 6 节点习题组。摘除后这些节点会变成无锚或少锚,须同批检查是否触发 SPEC 的锚点数下限。

### 6.3 新发现的缺陷批次:`nodes-CX2.jsonl` 全部 15 个节点

机制:模型解了习题,锚点只钉住题面。`15.05-exercises.md` 与 `15.09-exercises.md`
**都没有答案**(已验,`grep -ci answer` 各 0),所以那些具体诊断 100% 是模型计算。

**处置:登记为形式 E(出处缺陷),不判死。**审查员独立复核了五个节点的算术,全对。
缺的是出处而非正确性。同样适用于 15.12 Exercise 1 的五个节点(该文件 152 行,零处答案)。
**这 15 个节点需要的是 `origin` 字段如实标注,不是删除。**这条并入 SPEC 议题
(SPEC 是否允许、以及如何要求节点声明模型计算内容)。

### 6.4 SPEC 归因 8 处,其中三组的高共享是 SPEC 直接造成的

组 3、8、9:想引的公式不可引,于是所有人退回同一段邻近散文。**这三组不应记任何死因。**
审查员另把 `apostol:theorem-15-15-orthogonal-decomposition` 从 SPEC 归因里**摘出来**
——那处是第三个锚点槽空着,不是 SPEC 的错。这个区分做得对。

新测的 SPEC 实例:内积公理(3) 21 字符、范数定义式 26、Theorem 15.16 主不等式 25、
**Theorem 15.9(a) 29 字符——差一个字符**。
另有两条"靠 OCR 噪声侥幸过线":Axiom 5 的式子因 `f o r a l l x i n V.` 被拆开才到 36 字符,
内积公理(4) 因 `\quad i f \quad` 才到 35。**OCR 干净的话这两条也会死。**
即本章核心公式的可引性部分取决于 OCR 留下多少噪声——这条要写进实验报告。

**但 Theorem 15.9(a) 这一处不构成真障碍**(详见 `_D分片-主控核验.md`):
证明需要的是 (b) 的逆否,(b) 有 42 字符、合法可引;不可引的 (a) 恰是不需要的那一支。
