**裁决：A10 §6.1 采纳，清单 :199–200 改为「34 个节点 `origin`/`origin_note` 命中 0/34」。已核。** `A10-批次分层信号.md` 第 215 行逐字给出「真正未披露的批次是 `A2-x2`：34 个节点，`origin`/`origin_note` 命中 0/34」。§4.1 与 15/15 复算未复核。

**裁决：A10 §6.2 采纳，`审查-形式B-共享引文.md:15` 的「41 个不同节点」改「42」。已核。** `A10-批次分层信号.md` 第 375–379 行给出现文 41、改为 42，及「跨两组的节点实测恰好 3 个，45 − 3 = 42」。

**裁决：A10 §6.3 改后采纳，新增 6.5 节须同时保留「34 个」与「已审 6 / 剩余 28」两数。已核（数字侧）。** `A10-批次分层信号.md` 第 395–397 行：34/34 锚点落 `15.12-exercises.md`(25)/`15.16-exercises.md`(9)、两文件 `grep -ci answer` 各 0、「只读过其中 6 个(5 判借用锚 + 1 判合法)，剩余 28 个从未被审查」。

**裁决：A10 §6.4 采纳，SPEC 增补「锚点的出处充分性」条。已核（前提侧）。** `A10-批次分层信号.md` 第 303–306 行实测四习题文件 78/80/152/77 行、`grep -ci answer` 各 0，故该判据是演绎前提非统计外推。§八 二值性实测未复核。

**裁决：A10 §6.5 改后采纳，方法论三句须与 §3.3 三条局限同段落写入。未复核。** 单调率序列 83%/19%/13%/0%、`nbatches==1` 仅 1 组支撑、`intra>=6` 仅 2 组支撑，均未在 `A10-批次分层信号.md` 命中。附歧义：该文件第 176–178 行的 0.24 是 S2 召回 0.50 的 95% CI 下界，碎片却把「0.24→0.50」当前后对比数，**冲突待人工裁定**。

**裁决：A10 §4.2 析取式采纳，`>=3` 须标暂定，review 集不得标为「形式 B 缺陷集」。已核。** `A10-批次分层信号.md` 第 253 行逐字为 `review(v) = exonly(v) or intra(v) >= 3`；第 161 行 S2 单独已 20/25、CX2 列 15/15，故 review 集主体是形式 E。合取的 5/25 未复核。

**裁决：A10 全份降级条款采纳为引用限制，下游引 A10 的比率须带那三句限定。已核（数值侧）。** `A10-批次分层信号.md` 第 163 行 S2 OR S1(`>=3`) 命中 70、24/25、末列 0.34；第 174–178 行召回列分母确为「10 个 POS」，支持「是覆盖率不是召回」的降级。

**裁决：A9 更正一采纳，改 `_D分片-主控核验.md:525`，分片的 100 字符无误。已核。** `A9-记账错更正.md` 第 11 行：「分片的 100 字符是对的（实测 100，逐字子串）。89 是 `edges-D2.jsonl:85` 边引文那个另一条子串的长度」；第 31 行实测 `15.08.md:15` 整行 233 字符含三句。

**裁决：A9 更正二采纳，替换 `审查-形式D-D3-片21-45.md:89` 整行，`:125` 移出「本该引而未引」。已核。** `A9-记账错更正.md` 第 99–100 行：实测 42 字符、`in 15.10.md` 为 True、命中 `15.10.md:125`、「是该节点已有的唯一锚点（T1），不是缺的」。

**裁决：A9 更正三采纳撤回主控原判，`apostol:zero-element` cause 6 维持、6→0 不成立。已核。** `A9-记账错更正.md` 第 13 行：「6 → 0 不成立。支撑它的那条引文只走 TD（该节点是 `dst`），按四层定义 TD 不入池。cause 6 维持」。

**裁决：A9 更正三处置采纳，补 `sections` 与补第三条锚点同批执行，先锚点后死因的顺序约束随文装配。已核（双改侧）。** `A9-记账错更正.md` 第 13 行「A5 给的 fix（补 sections 并 补第三条锚点）本来就是对的，主控只采了一半」；第 157–159 行给出该 66 字符引文与 `edges-A1.jsonl:18`。`15.03.md:19`、整行 359 字符、额度 2/3 未复核。

**裁决：A9 更正三的 SPEC 议题剥离采纳，本节点摘出、`sections` 语义议题待议。已核。** `A9-记账错更正.md` 第 237 行：`sections` 只在 `SPEC.md:62` 与 `:84` 两处 schema 示例出现，全文无一句定义其语义。

**裁决：A9 派生检查改后采纳，只登记为新增待办，不得作结论。已核。** `A9-记账错更正.md` 第 288 行给出该检查（`:450` 之前、理由引了「该节点是 dst 的边」的塌陷判定须重算），并明言「没有做这个全量重扫，这是本提案留下的缺口」。

**裁决：A10 §4.2「S2 AND S3」合取判据落选。未复核。** 命中 34、25 正例仅覆盖 5 个、按定义排除 CX2 整批，三数未在 `A10-批次分层信号.md` 命中。

**裁决：A10 §3.2「S3 `nodecl` 单独作筛子」落选，无选择性。已核。** `A10-批次分层信号.md` 第 162 行 S3 命中 473、末列 0.02。分母 522 与 91% 未复核。

**裁决：A10 §6.5「按批次名分层」落选。未复核。** 「`intra>=3` 的 30 个命中里 15 个来自 D1」这一 FP 分布未在 `A10-批次分层信号.md` 命中。

**裁决：A10 §5.1 第 3 条「`intra / |batch|` 归一化式」移出本轮。未复核。** 金标准无小批次正例可定标、A10 自身不提出该式，两点均未复核。

**裁决：「把 S2 当形式 B 检测器」这一读法不采纳。已核。** `A10-批次分层信号.md` 第 161 行 S2 命中 49、CX2 列 15/15、合计 20/25，与「20 个中 15 个属形式 E」的数字骨架一致。

**裁决：在 §3.3 之上定 `>=3` / `>=4` 硬阈值移出本轮。已核。** `A10-批次分层信号.md` 第 176–177 行两条 95% CI 为 [0.60, 0.98] 与 [0.49, 0.94]，重叠，只够定方向。

**裁决：A9 简报预设第 1 条「分片的 100 字符是错的」不采纳。已核。** `A9-记账错更正.md` 第 11 行明言两数各量不同子串，要改的是主控核验文件。

**裁决：A9 主控原路径「只改 `sections`、不补锚点」不采纳。已核。** `A9-记账错更正.md` 第 13 行「主控只采了一半」，A5 原 fix 两项本就都对。

**裁决：A9「`:321–448` 全量重扫」移出本轮，仅作缺口登记。已核。** 同上，`A9-记账错更正.md` 第 288 行自认未做。
